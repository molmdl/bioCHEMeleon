# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-08-02)

**Core value:** The player can load a molecule, generate blended "hider" atoms that match the local representation style, and reliably find them by clicking — with a working timer and win condition.
**Current focus:** Phase 6 COMPLETE + VERIFIED (gsd-verifier PASSED, 35/35 must-haves). All 3 plans done (06-01 controller TDD + 06-02 GUI buttons + 06-03 smoke + human-verify APPROVED). hint() (GAME-05), reveal_one() (GAME-06), reveal_all() (GAME-07), _reveal_count/_hint_count counters (DIFF-01), on_counts_changed 4th callback — all verified at BOTH tiers (187 unit tests + 29/29 headless smoke ALL PASSED + human-verify checkpoint APPROVED). 3 runtime bugs fixed during 06-03 verification (hint sparse-hider no-op, reveal-counter label not resetting, hint orange color persisting after cleanup via backup-corruption root cause — all Rule 1, all regression-tested). Phase 5 COMPLETE (v1 scope). Next: Phase 7 (Found-Hider Management, Restart & Cleanup).

## Current Position

Phase: 7 of 11 — Found-Hider Management, Restart & Cleanup — IN PROGRESS (Plan 3 of 3, Task 1 of 2 done; Task 2 human-verify checkpoint PENDING). Phase 6 COMPLETE + VERIFIED (gsd-verifier PASSED, 35/35 must-haves). Phase 5 COMPLETE (v1 scope). Phase 4 COMPLETE (6/6). Phase 3 COMPLETE (20/20). Phase 11 (Alt-conf Cartoon/Ribbon Hider v1 Follow-up) ADDED — after Phase 10, research-required.
Plan: 07-03 of 3 in Phase 7 — Task 1 COMPLETE (headless smoke), Task 2 (checkpoint:human-verify) PENDING user verification in real Windows PyMOL GUI. Task 1: smoke/phase7_smoke.py (181 lines, pure pymol.cmd.* — NO Qt, modeled on phase6_smoke.py) — 38 checks across 7 sections (setup, start+show, found-mgmt hide/show via build_found_selection + group_found_by_rep, found-mgmt recolor+color via cmd.set_color('found_highlight') + cmd.iterate color index CHANGED, _found_color threading at runtime — gc_thread._found_color='cyan' → _mark_found → color index 5387→5 [cyan, NOT default green; runtime confirmation of Plan 01 TDD change], restart round-trip start→cleanup→start→cleanup verify_intact, cleanup explicit path + idempotent). Headless run via cmd.exe /c run-conda-pymol.bat -cq: 38/38 ALL PASSED, exit 0, first run clean (NO runtime bugs discovered in Plan 02 GUI wiring's underlying cmd paths — no fix(07-03) commits needed). 1 task commit: e51bd0d (feat). All WSL gates green (py_compile all [biochemeleon + smoke] + 200 tests [no regression] + Pitfall-1=0 + exec_=1 unchanged). Task 2 (checkpoint:human-verify) is BLOCKING — verifies all 4 Phase-7 success criteria (C1 found-hider dropdown GAME-08, C2 color picker DIFF-04, C3 Restart GAME-10, C4 Cleanup BTN-06) + the wizard-lifecycle fix (mouse_selection_mode restored after Restart AND Cleanup) in a real Windows PyMOL GUI session. Plan 01 + 02 shipped the pure helpers + GUI wiring; this plan ships the runtime verification (headless cmd paths + human-verify GUI paths).
Status: Phase 7 IN PROGRESS (Plan 3 of 3, Task 1 of 2 complete; Task 2 human-verify checkpoint PENDING). Awaiting user to verify all 4 Phase-7 success criteria + wizard-lifecycle fix in real Windows PyMOL GUI.
Last activity: 2026-08-11 — Completed 07-03-PLAN.md Task 1 (headless smoke 38/38 ALL PASSED; committed e51bd0d). Task 2 checkpoint:human-verify PENDING.

Progress: ██████████░ 98% (45 of 46 plans done; Phase 7 plan 3 Task 1 of 2 complete — checkpoint pending)

## Performance Metrics

**Velocity:**
- Total plans completed: 29 (1 + 7 + 2 gap closures + 20 Phase 3 + 6 Phase 4 + 5 Phase 5 + 2 gap closures + 3 Phase 6) — Phase 6 COMPLETE
- Average duration: ~7 min
- Total execution time: ~2.5 hours

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 1. Plugin Bootstrap & Dialog Scaffold | 1 | ~35 min | ~35 min |
| 2. Setup Tab Configuration & Bundled Demos | 6/6 + 02-06 + 02-07 gap closures | ~31 min | ~4 min |
| 3. Mutation Safety & Hider Registry Foundation | 20/20 | ✓ Complete | 2026-08-06 |

**Recent Trend:**
- Last plans: 07-02 ~4min (GUI wiring — found-hider dropdown GAME-08 + color picker DIFF-04 + Restart GAME-10 + Cleanup BTN-06 + wizard-lifecycle fix in _on_start), 07-01 ~6min (TDD: pure helpers build_found_selection/group_found_by_rep + _found_color threading), 06-03 ~75min/2 sessions (headless smoke 29/29 + human-verify APPROVED + 3 Rule-1 bug fixes), 06-02 ~2min (GUI wiring — Hint/Reveal buttons + confirm + reveal counter), 06-01 ~8min (TDD: RED/GREEN/REFACTOR — GameController hint/reveal logic), 05-04 ~18min, 05-03 ~10min, 05-02 ~12min, 05-01 ~8min, 04-06 ~75min (2 sessions), 04-05 ~3min, 04-04 ~3min
- Trend (07-02): Phase 7 GUI wiring for 4 requirements (GAME-08 + DIFF-04 + GAME-10 + BTN-06) + wizard-lifecycle fix. gui_game.py 195→275 lines: QComboBox (_found_mgmt_combo — Found hiders: (select)/Hide found/Show found/Recolor found) + Color… QPushButton (_color_btn) + Restart QPushButton (_restart_btn) added to btn_row after Reveal-all. 3 handlers: _on_found_mgmt(mode) filters registry by HIDER_STATUS_FOUND (NOT color — found hiders may be recolored to any color), uses build_found_selection + group_found_by_rep from Plan 01; _on_found_mgmt_activated(index) maps 1=hide/2=show/3=recolor, resets combo to 0; _on_pick_color (DIFF-04) QColorDialog.getColor() static method (NOT .exec_() — child-dialog modal, modeless-main rule preserved) → cmd.set_color('found_highlight') → _found_color assign → auto-recolor. start_countdown clears _info_log; _begin_play defensively _timer.stop() before start. gui_setup.py 582→588: cleanup_btn between load_btn and start_btn. __init__.py 231→287: wizard-lifecycle fix FOLDED INTO _on_start (deactivate old PickWizard before cleanup — fixes BOTH Start-mid-game AND Restart mouse_selection_mode corruption); _on_restart (deactivate wizard + stop timer + _on_start); _on_cleanup (deactivate wizard + stop timer + controller.cleanup + UI reset + release _controller=None); cleanup_btn + _restart_btn wired. 3 commits: 971e6f0 + 8a6248e + 1b70897. All WSL gates green (py_compile all + 200 tests + Pitfall-1=0 + exec_=1 unchanged). 1 Rule-3 deviation: reworded _on_pick_color docstring to avoid literal .exec_() tokens tripping grep gate (mirrors 03-02/06/09/10 precedent). Qt+cmd-coupled — runtime deferred to Plan 03 human-verify.
- Trend (06-03): Phase 6 runtime-verified at BOTH tiers + 3 Rule-1 bug fixes. Task 1 (headless smoke): smoke/phase6_smoke.py (131 lines, pure pymol.cmd.* — NO Qt; modeled on phase4_smoke.py) — 10 sections: start (count += 3, registry len 3) → show spheres → HINT (orange neighbors exist, NO GAME atoms orange, all hiders still hidden, _hint_count==1, on_counts_changed(1,0), log fired) → REVEAL-ONE (exactly 1 more found, _reveal_count==1, counts(1,1), remaining==2, win NOT fired, revealed hider green) → REVEAL-ALL (all found, _reveal_count==1+N, counts, on_remaining_changed(0), win fired, all hider atoms green) → CLEANUP (intact True, count back to orig, hint orange cleared, no GAME atoms remain) → COUNTER RESET (fresh GameController zeroes _reveal_count + _hint_count before + after start). Headless run via cmd.exe /c run-conda-pymol.bat -cq: 29/29 ALL PASSED, exit 0 (e116c9b). Task 2 (checkpoint:human-verify APPROVED): user confirmed all 4 Phase-6 success criteria (C1 Hint, C2 Reveal-one, C3 Reveal-all, C4 Reveal counter resets) + button-guard check in a real Windows PyMOL GUI session. 3 Rule-1 runtime bugs fixed during verification (all regression-tested): (1) f5a2b00 hint() sparse-hider no-op — random.choice(hidden) could pick a hider in a sparse region (0 atoms within HINT_RADIUS=5Å); hint() colored nothing but STILL incremented _hint_count + fired callbacks + logged "highlighted neighbors of one hider" (a lie). Fix: filter hidden to candidates with count_atoms(sele)>0 before random.choice; silent no-op if none. +test_hint_no_neighbors regression test. Discovered via headless smoke (2 of 3 seed=42 sphere positions in 1ubq had 0 atoms within 5Å). (2) c9c2169 reveal-counter label not resetting on new game (C8) — _reveal_label initialized once in __init__ never reset on new game. Fix: start_countdown sets _reveal_label="Reveals: 0" before countdown (matches controller start() reset). (3) c9c2169 hint orange color persists after cleanup + ROOT CAUSE backup corruption — cleanup() originally did sentinel-remove + verify + discard but did NOT restore hint()-colored real neighbor atoms to original colors. ROOT CAUSE: hint()'s `around`/`near` selection crossed object boundaries, coloring atoms in the _bchm_backup object (coordinate-identical copy) too, corrupting the backup's colors. Fix (a): cleanup() now calls backup.restore (delete+create two-step) — the backup (snapshotted in start() before any mutation/coloring) has original atoms + colors, so restore removes hiders AND restores hint-colored real atoms in one step. Fix (b): hint selection now ends with `and <target_obj>` to restrict to target object only (extracted to hint_sele(hider_id) helper for DRY — used by both the candidate-count filter and cmd.color). Fix (c): cleanup()+abort_on_error() also reset _reveal_count=0 + _hint_count=0 (game-over counter consistency). +3 regression tests (cleanup resets counters, abort resets counters, cleanup idempotent when not started). +2 smoke cleanup assertions (hint orange cleared, no GAME atoms remain — verify restore-from-backup, not just count-back-to-orig). mutation.cleanup_hiders + backup.verify_intact remain as available primitives. 187 tests pass (18 controller + 90 setup_state + 54 registry + 21 generators + 4 new 06-03 regression). All WSL gates green (py_compile + Pitfall-1=0 + exec_=1 existing _finish_win only). PATTERNS for future phases: object-scoped `around` selections (append `and <target_obj>` — the around operator crosses object boundaries by default, coloring atoms in coordinate-identical backup copies); cleanup via backup.restore is the canonical post-game cleanup (restores atom count AND original colors in one step — matters when hint() colored real neighbor atoms); headless smoke cannot catch color-persistence bugs (count-back-to-orig passes even when colors stay wrong — the human-verify checkpoint catches what the smoke structurally cannot; mirrors Phase 5 05-06/05-08 lesson but for color state, not coord state). Phase 6 FULLY COMPLETE — ready for 06-VERIFICATION.md / 06-SUMMARY.md phase handoff (a later planning step), then Phase 7.
- Trend (06-02): Phase 6 GUI wiring — GameTab extended 141→194 lines (+53). Task 1 added a btn_row below the timer/remaining row: 3 QPushButtons (Hint / Reveal one / Reveal all) + a reveal counter QLabel ("Reveals: 0"). Task 2 added 5 methods: _confirm(title, text) Yes/No helper using QMessageBox.question(self.window(), title, text, Yes|No) — the static method owns its modal event loop (NO .exec_() from our code; exec_ gate stays at 1 = existing _finish_win only), reuses the Phase 4 _finish_win Bug B fix (top-level PluginDialog parent so the dialog appears above the PyMOL OpenGL window); _on_counts_changed(hint_count, reveal_count) slot updating the reveal label (hint_count accepted but NOT displayed in Phase 6 — reserved for Phase 10 win screen; callback signature is forward-compatible); _on_hint_clicked (Hint = help, NO confirm, calls controller.hint() directly); _on_reveal_one_clicked + _on_reveal_all_clicked (give-up actions — show _confirm BEFORE calling controller.reveal_one()/reveal_all(); confirm text says "Give up" + "counts as a reveal use"/"ends the game"). All 3 handlers share a 2-guard preamble: `if self._controller is None or not self._controller._started: return` + `if self._controller._remaining() == 0: return` — silent early-return (safe to click before countdown, after win, after all-found; no log spam, no crash). 3 clicked.connect signals wired in __init__ after the btn_row layout. _begin_play's set_callbacks call extended with `on_counts_changed=self._on_counts_changed,` (4th param — the ONLY change to _begin_play; everything else — lazy wizard import, activate, _start_time, timer.start, _update_remaining — untouched). Controller is DUCK-TYPED (NO game.py import; passed via start_countdown, same Phase 4 pattern). Qt via `from pymol.Qt import QtCore, QtWidgets` (NEVER from PyQt5). All WSL gates green: py_compile all + 183 tests (18 controller + 90 setup_state + 54 registry + 21 generators, no regression) + Pitfall-1=0 package-wide + exec_=1 (existing _finish_win only, allowed) + no game.py import + no walrus := + 3 clicked.connect + 2 self.window() (new _confirm + existing _finish_win) + 1 QMessageBox.question + on_counts_changed >=2 (def + set_callbacks). 0 deviations — plan executed verbatim (no docstring rewording needed, unlike 06-01's "around" false-positive; the plan's prose avoided literal gate tokens). Commits: 7d119dd (Task 1 — buttons + label), 1a0c273 (Task 2 — wiring). Qt+cmd-coupled — runtime (button rendering, confirm dialog above OpenGL window, label updates on reveal, hint coloring) WSL-unverifiable; deferred to 06-03 human-verify checkpoint. Dependency direction intact (gui_game imports only pymol.Qt + stdlib time + lazy pymol.cmd + lazy .wizard; NO game.py).
- Trend (06-01): Phase 6 hint/reveal controller logic TDD. game.py extended 141→231 lines: hint() (GAME-05 — byres around selection colored orange, excludes GAME atoms, does NOT mark_found), reveal_one() (GAME-06+DIFF-01 — mark random hidden found + green + count + win-if-last), reveal_all() (GAME-07+DIFF-01 — mark all hidden found + green + count-by-len + win), _mark_found() shared helper (DRY: on_pick + reveal_one + reveal_all all call it), _reveal_count/_hint_count counters (init zero + reset in start()), on_counts_changed 4th set_callbacks param (default no-op, backward-compatible). HINT_RADIUS=5.0, HINT_COLOR='orange' module constants. on_pick refactored to call _mark_found (REFACTOR — no behavior change, existing test_found still asserts same cmd.color call). 10 new unit tests (TestGameControllerHintReveal) following the Phase-4 mock pattern (populated registry + mock cmd + mock callbacks, WITHOUT start()). 2 Rule-3 auto-fixes: (1) count_atoms.return_value=5 in test setUp (MagicMock.__gt__ returns NotImplemented → TypeError on `> 0` comparison; standard mock config); (2) docstring "around"→"near"/"vicinity" rewording (plan's own docstring contained "around" → grep gate expected 1 but got 3; mirrors 03-02/03-06/03-09/03-10 precedent). TDD: RED 879f2f1 (10 fail AttributeError, 8 existing pass), GREEN eeae6bc (18 pass), REFACTOR 610a2e3 (18 pass, no behavior change). All WSL gates green: py_compile all + 183 tests (18 controller + 90 setup_state + 54 registry + 21 generators, no regression) + Pitfall-1=0 + exec_=0 in game.py + walrus := 0. Dependency direction intact (game.py imports only pymol.cmd + stdlib time/random + sibling backup/mutation/registry; no wizard/gui_game/generators).
- Trend (05-04): Phase 5 MECHANISM runtime-verified at the headless tier. smoke/phase5_smoke.py (194 lines, pure pymol.cmd.* — NO Qt; modeled on phase4_smoke.py) — 5 sections: LINE/STICK (insert_line_stick_hider → GAME atoms in rep sticks + sentinel segi/b + bond to neighbor via `neighbor` selector + color matches neighbor), CARTOON (insert_cartoon_hider N-terminus → polymer + rep cartoon + N-C-CA backbone + sentinel + color), MIXED-REP (GameController.start via insert_hider_for_rep with sphere+stick+cartoon specs → registry len + counts_by_rep + per-rep GAME visibility), CLEANUP (gc.cleanup verify_intact True + count back to orig + no GAME atoms), optional iterate_state spike (elem+color exposed). Headless run via cmd.exe /c run-conda-pymol.bat -cq: 25/25 ALL PASSED (ef001c8), exit 0. 3 MEDIUM open risks RESOLVED (all PASS, no fallback): Open Risk 1 (segi='GAME' does NOT break polymer — polymer_GAME=5), Open Risk 2 (attach_amino_acid works with named sele, no pk1), Open Risk 3 (hider Cα color == neighbor Cα color for both reps). Open Risk 6 LOW confirmed (iterate_state exposes elem+color). 4 deviations (all auto-fixed, all necessary for the cartoon path to run at runtime): (1) [Rule 3 blocking] cmd.attach_amino_acid does not exist in 2.5.0 open-source → lazy `from pymol import editor` (editor.py:85; cmd.py imports editor lazily inside a fn so cmd.editor is not a stable attribute; 9c6b009); (2) [Rule 1 bug] C-terminus carbonyl C carries an OXT (terminal oxygen) in 1ubq saturating the valence → "no target attachment vector found" (ObjectMolecule.cpp:3357) → switched cartoon MVP to N-terminus (free valence, NO atom removal, verify_intact passes; pick_terminal_residues now returns N-term min resi is_c_terminus=False; 6 unit tests updated; 9c6b009) — runtime bug fix, NOT an Open Risk 1 fallback (segi didn't break the polymer; the plan's OR1 fallback was resn='HIDER' which was not needed); (3) [Rule 1 library bug] int(resi) raises NameError in iterate (hygienic space= doesn't expose builtins) → use resv (numeric residue value, already int; ef001c8); (4) [Rule 1 library bug] `neighbor A and B` selector precedence parses as `neighbor(A and B)`=empty → wrap in explicit parens `(neighbor A) and B` (ef001c8). All WSL gates green (py_compile all + 173 tests no-regression + Pitfall-1=0 + exec_=1 on allowed QMessageBox + purity generators.py=0 + purity registry.py=0). The smoke proves the MECHANISM; the visual BLEND is the 05-05 human-verify checkpoint. NOTE for 05-05: cartoon hider is now an N-terminus extension (resi 0 for 1ubq); a bundled demo with an N-terminal cap (ACE/formyl) may fail the N-term attach — a 05-05 runtime concern (smoke only tested 1ubq, free N-terminus).
- Trend (04-06): Phase 4 core loop runtime-verified at BOTH tiers. Task 1 (headless smoke): smoke/phase4_smoke.py (148 lines, pure pymol.cmd.* — NO Qt; modeled on phase3_smoke.py) — sphere gen (generate_sphere_positions seed=42 + bounds check) -> insert+register (GameController.start) -> show spheres (cmd.show) -> simulate find via on_pick logic path (mark_found + cmd.color green + callbacks) -> remaining decrement -> miss (non-hider no-op) -> win (all found + on_win float) -> cleanup (verify_intact + count-back-to-orig) + 2 optional spikes (do_pick + do_select, try/except-guarded). Headless run via cmd.exe /c run-conda-pymol.bat -cq: 18/18 ALL PASSED (83e7804), then 19/19 after do_select spike (9c9aeea). Color assertion = "changed from pre-pick" (green index varies by build: 5387->3 here, not the plan's 7). Task 2 (checkpoint:human-verify APPROVED): user confirmed all 4 success criteria in real Windows PyMOL GUI — tested 1znf 3-hider + 4wb3 10-hider rounds; last hider green, win dialog on top with correct time, viewer responsive, new game works. 3 GUI bug-fix iterations during the checkpoint (NOT 04-06 tasks — bugs in 04-01..04-05 that headless smoke structurally cannot catch because it exercises the on_pick logic path + calls do_pick directly): (1) c68a1a4 do_select routing — PickWizard set mouse_selection_mode=0 but never switched button mode; default 3-Button Viewing creates "sele"+WizardDoSelect not "pk1"+WizardDoPick so do_pick never fired on real clicks; fix: do_select(name) builds pk1 from sele then dispatches do_pick(0) (canonical measurement-wizard pattern; preserves left-drag rotation); (2) 9ec0c16 three win-loop bugs — win time always 0:00 (_start_time on GameTab not GameController), last hider not green (modal blocks Qt before cmd.color flushes), no post-win cleanup (viewer frozen + new game broken); fix: _begin_play sets controller._start_time, win() reordered, _on_win calls cleanup() after modal, _on_start defensively cleans prior controller; (3) 01c48f6 three win-display bugs — last hider still gray (wizard-teardown WizardRefresh clobbered pending redraw; fix: defer deactivation 100ms via QTimer.singleShot _finish_win), win dialog behind PyMOL window (GameTab not top-level parent; fix: self.window() + WindowStaysOnTopHint), viewer frozen (consequence of hidden modal). All Rule 1 bugs, no architectural changes. WSL gates stayed green throughout (py_compile + 160 tests + pitfall-1=0; exec_ gate=1 on allowed QMessageBox.exec_() child dialog per AGENTS.md). Lesson: pick-chain routing + win-display path are headless-unverifiable — the do_select spike now closes the routing gap headlessly, but real-click routing still warrants a human check when button-mode logic changes. Phase 4 READY FOR VERIFICATION (not fully complete — orchestrator runs phase-level verifier next).
- Trend (04-05): wired Start button end-to-end (BTN-07 fan-in — the plan that makes the four Phase 4 success criteria reachable). gui_setup.py: bold Start QPushButton added to Setup actions QGroupBox (exposed as self.start_btn, last in Reset/Randomize/Save/Load row, setStyleSheet font-weight:bold, not connected here — wired in __init__.py). __init__.py: added `from pymol import cmd` (module level); added `self._controller = None` (holds active GameController — Phase 7 cleanup/restart hook); wired `self.setup_tab.start_btn.clicked.connect(self._on_start)`; added `_on_start(self)` (lazy import generators/game/demos inside the method — mirrors 04-04 lazy PickWizard pattern). _on_start flow: collect_state -> resolve target (loaded/fetch/demo) with QMessageBox.warning on EVERY failure path + RuntimeError catch on double-start -> cmd.get_extent -> generators.generate_sphere_positions -> hider_specs=[(pos,'spheres')] (ONLY rep in Phase 4) -> game.GameController(target_obj).start(hider_specs) UNCHANGED Phase 3 proven -> cmd.show("spheres", "%s and segi GAME") (lowercase segi) -> tabs.setCurrentWidget(game_tab) -> game_tab.start_countdown(self._controller). The full core loop chain is now CLOSED: _on_start -> GameController.start -> GameTab.start_countdown -> _begin_play (PickWizard + set_callbacks) -> PickWizard.do_pick -> GameController.on_pick (mark_found + cmd.color + callbacks) -> win -> _on_win (stop timer + QMessageBox.information). __init__.py 66->122 lines; gui_setup.py +6 lines. All WSL gates green: py_compile all + 160 tests (no regression) + exec_=0 + Pitfall-1=0 package-wide + all 8 Task-2 grep gates pass. 1 process deviation: 2 atomic commits (e866c2e + 226741d) instead of 1 combined (plan Task 3 'commit both files together' — better bisect granularity, end state identical; no code/correctness deviation). Qt+cmd-coupled — runtime deferred to human-verify 04-06 (the loop is now runnable).
- Trend (04-04): populated gui_game.py GameTab(QWidget) — the player-facing game state surface. 13→94 lines. QTextEdit read-only rolling log (GAME-01, append-only auto-scroll), QLabel timer (M:SS, 1 Hz QTimer main-thread per PITFALLS Pitfall 6 — NEVER threading.Thread), QLabel remaining count (GAME-03). 3-2-1 countdown via QTimer.singleShot chain (NEVER time.sleep — blocks Qt event loop + freezes PyMOL; 04-RESEARCH Q21) then _begin_play. _begin_play LAZILY imports PickWizard inside the method (`from .wizard import PickWizard` — avoids pulling pymol.wizard at module load), activates it, sets controller._wizard (so GameController.win deactivates it), registers set_callbacks(on_log/on_remaining_changed/on_win) wiring the controller to drive this UI, starts QTimer, initializes remaining. _on_win stops QTimer + QMessageBox.information modal win message (LOOP-03; main plugin dialog stays modeless — only this brief celebratory message is modal). Controller is DUCK-TYPED (passed via start_countdown, NO import of game.py — avoids circular import + keeps gui_game decoupled). _start_time set in _begin_play NOT start_countdown (timer measures play time, not countdown). Does NOT deactivate wizard in _on_win (GameController.win already does — avoid double-deactivate). Qt via `from pymol.Qt import QtCore, QtWidgets` (NEVER from PyQt5). All WSL gates green: py_compile all + 152 tests (no regression) + Pitfall-1=0 package-wide + exec_=0 package-wide + time.sleep=0 + singleShot=1 + lazy wizard import=1 + set_callbacks=1 (actual call) + QMessageBox.information=1 + no game.py import. 1 deviation [Rule 3 blocking]: reworded module docstring to remove "(set_callbacks)" parenthetical — the plan's verbatim docstring tripped its own verification grep exact-count gate (returned 2: docstring mention + actual call); mirrors the AGENTS.md-documented docstring false-positive pattern + Phase 3 03-02/03-03/03-06/03-09/03-10 precedent. Qt+cmd-coupled — runtime (ticking/countdown display/click-to-find/win message) deferred to human-verify (plan 04-06) in a real Windows PyMOL session. Single-plan Wave 2; plain git commit, staged only biochemeleon/gui_game.py (feat f397045). Plan consolidated the single commit at task 3 per its verbatim instruction).
- Trend: 03-15 was the headless-PyMOL smoke checkpoint CLOSED (the human-verify checkpoint was resolved WITHOUT a human PyMOL session — AGENTS.md now documents `cmd.exe /c C:\\src\\run-conda-pymol.bat -cq <script>` from the staged tmp/bioCHEMeleon/ path; the smoke is pure pymol.cmd.* so it runs headlessly, no Qt/GUI). Ran smoke/phase3_smoke.py -> reached 24/24 ALL PASSED, exit 0 after a 1-line b-factor selector fix in mutation.py:113 (b -999 -> b < 0; PyMOL has no exact-match b-factor selector — b -999 is malformed, silently matches nothing; b < 0 matches -999.0). The full checkpoint resolution spanned 2 sessions + 6 runtime auto-fixes (all Rule 1 bugs / Rule 3 blockers, no architectural changes): (1) iterate symbol id->ID uppercase [e38cff7, library bug], (2) verify_intact drop x/y/z [5ed6a13, library bug — iterate has no coords, use count+identity per Q6 fallback], (3) smoke line 57 redundant verify_intact on discarded backup [9e40e8a, smoke bug], (4) smoke line 66 failure-path twin [9b00657, smoke bug], (5) smoke /tmp->relative pse path [039bc6a, smoke bug — Windows can't resolve /tmp], (6) b -999->b<0 selector [6a15a29, library bug — the load-bearing fix]. Plus diagnostic cf702a1 (PSE-DIAG triage lines) + docs b320316 (AGENTS.md headless method). Spike findings (verbatim, feed 03-16/03-17/03-19): Q1 cmd.pseudoatom return value = None (type NoneType) [confirms RESEARCH §Q1; code uses cmd.identify mode=0]; Q2 PASS single-call create(existing,src) IS REPLACE (n_after==n_before) [RESEARCH §Q2 MEDIUM resolved — no doubling, delete+create in 03-05/03-12 correct]; PSE sentinel survives reload (len pse_sent==1), id stable (pse_sent==[saved_id], Pitfall 4 holds at runtime), b=-999.0 preserved, reconstruct works after b<0 fix (fetch_all_hider_ids returns [('1ubq',662)]), rep=None. WSL gates green: py_compile all + 144 tests + Pitfall-1/11 ZERO + mutation.py gates [fetch_all_hider_ids=1, space=1, segi GAME=4>=2, b -999=0, b < 0=1, completeness=3]. Re-staged to Windows (wsl2win_cp.sh + cp smoke; cmp -s mutation.py byte-identical). Phase 3 smoke VERIFIED at runtime; cmd-coupled modules (backup/mutation/game) now runtime-verified not just syntax-checked; ready for 03-16/03-17/03-18/03-19/03-20.
- Trend: 03-13 was the smoke/phase3_smoke.py setup + happy-path plan (NEW smoke/ directory + phase3_smoke.py 62 lines — Phase 3 integration smoke test run by a HUMAN in Windows PyMOL via `pymol -cq smoke/phase3_smoke.py`. Setup: cmd.fetch 1ubq async_=0 + capture orig_count/orig_ids/orig_pubnames. Uses game.GameController(obj).start(hider_specs) — tests the REAL orchestrator wiring (snapshot -> insert -> register), NOT direct mutation calls. 3 hiders: [10,10,10] spheres, [11,11,11] sticks, [12,12,12] lines. C1: public object list unchanged + count += 3. C2: 3 sentinel atoms via space={'stored': sent} hygienic iterate, all segi=GAME + b=-999. Q4 spike: orig_ids.issubset(new_ids) + len(new_ids - orig_ids)==3. C3: registry len==3 + counts_by_rep spheres/sticks/lines/cartoon/ribbon + by_rep spheres len 1. C4 happy path: gc.cleanup() returns True (the verify_intact bool) + count back to orig + id-set matches orig + backup discarded. NO summary block — comment marker left for 03-14 (failure-path + Q1/Q2/PSE spikes + summary + sys.exit). Imports game in addition to backup/mutation/registry — tests the full Phase-3 stack. All WSL gates green: py_compile all (smoke + biochemeleon) + 144 tests (54 registry + 90 setup_state, no regression) + Pitfall-1/11 ZERO scoped to biochemeleon/+smoke/ + space= gate 1 + async_=0 1 + GameController 1 + C1-C4 11 matches >=7. 0 deviations — plan executed verbatim. Single-plan Wave 7; plain git commit, staged only smoke/phase3_smoke.py (feat 8d45b28). Task 2 verification-only, no commit per plan).
- Trend (03-12, prior): 03-12 was the game.py cleanup + abort_on_error plan (biochemeleon/game.py: added cleanup() — happy-path sentinel remove via mutation.cleanup_hiders + structure verify via backup.verify_intact + backup.discard + reset _backup_name/_started/registry, returns the verify_intact bool so the caller aborts on False; added abort_on_error() — failure-path backup.restore (delete+create two-step, never single-call create per RESEARCH Q2) + backup.discard + reset, returns the restore bool. Both idempotent when not started (if not _started: return True — defensive for UI double-clicks). Both reset _started=False + fresh HiderRegistry (round over). key_link: cleanup -> verify_intact -> (if False) -> abort -> restore (criterion 4 two paths). game.py now FUNCTIONALLY COMPLETE for Phase 3 (5 methods: __init__/start/reconstruct_registry/cleanup/abort_on_error). 42->69 lines (28 insertions). All WSL gates green (py_compile all + 144 tests [54 registry + 90 setup_state, no regression] + Pitfall-1/11=0 + completeness gate 5 + orchestration gate 15 >= 6 + 4 exact-count greps: mutation.cleanup_hiders=1, backup.verify_intact=1, backup.restore=1, backup.discard=2). 0 deviations — plan's prose docstrings avoided the dotted-literal false-positives that tripped 03-03/03-06/03-09/03-10 (no Rule-3 rewording needed; mirrors 03-11). cmd-coupled — runtime (cleanup happy-path, abort failure-path, idempotent-when-not-started) deferred to Phase 3 smoke test (03-13/03-14 via 03-15). Single-plan Wave 6; plain git commit, staged only biochemeleon/game.py (feat 14cec23). Task 2 was verification-only, no commit per plan).
- Trend (03-11, prior): 03-11 was the game.py GameController orchestrator plan (biochemeleon/game.py: replaced 1-line stub with GameController — __init__(target_obj) sets target_obj + fresh HiderRegistry + _backup_name=None + _started=False; start(hider_specs) snapshots BEFORE any mutation, builds a fresh registry, loops (pos,rep) tuples calling insert_hider -> register by id, sets _started=True, returns backup name; reconstruct_registry() rebuilds via DI — registry.HiderRegistry().reconstruct_from_sentinels(lambda: mutation.fetch_all_hider_ids(target_obj)); _started flag prevents double-start with RuntimeError. Wires all 3 Phase-3 modules — orchestration gate 7 matches >= 3. cmd-coupled (from pymol import cmd + from . import backup, mutation, registry) — runtime deferred to Phase 3 smoke test. 1->42 lines. All WSL gates green (py_compile all + 144 tests + Pitfall-1/11 zero + orchestration gate 7 + 4 exact-count greps each =1). No deviations — plan's prose docstrings avoided the dotted-literal false-positives that tripped 03-03/03-06/03-09/03-10. Single-plan Wave 5; plain git commit, staged only biochemeleon/game.py).
- Trend (03-10, prior): 03-10 was the registry.py reconstruct_from_sentinels + edge cases plan (biochemeleon/registry.py: reconstruct_from_sentinels(iterate_hider_keys) — DI-based post-.pse-reload registry rebuild; clears records, loops the injected callable, registers HiderRecord(rep=None, status='hidden') keyed by (object, int(id)), returns self fluent; registry.py stays pure via dependency injection — game.py injects lambda: mutation.fetch_all_hider_ids(obj), no `from pymol` in registry.py). HiderRecord.__init__ rep validation relaxed to `if rep is not None and rep not in GAME_REPS` (allows rep=None for reconstruction — sentinel carries no rep; normal register() always passes a valid rep). counts_by_rep guards against rep=None records (skips them; returned dict has only GAME_REPS keys, never a None key — documented limitation Phase 8 sidecar reconciles). 223->267 lines (1 method under # ---- sentinel reconstruction (dependency injection) ---- + validation relaxation + counts_by_rep guard + module/class/HiderRecord docstrings updated). Pure (stdlib + GAME_REPS only; no pymol) — fully WSL-verified (54 registry tests [33 core + 7 queries + 6 serialize + 5 reconstruct + 3 edge] + 144 total + py_compile + purity/Pitfall-1/Pitfall-11 all zero via sequential bash grep -rnE; Grep-tool cross-wiring caution from 03-07 heeded). TDD: RED 1a71d61 (6 tests fail AttributeError, 2 reconfirm pass), GREEN fbe173f (all pass), REFACTOR 79dd6b1 (1 gate-fix — reworded reconstruct docstring to avoid a `from pymol` false-positive grep match, mirrors 03-02 precedent, Rule 3 blocking). registry.py now FUNCTIONALLY COMPLETE for Phase 3 (10 HiderRegistry methods: register/get/all/remove/by_rep/counts_by_rep/mark_found/to_dict/from_dict/reconstruct_from_sentinels). Completeness grep returns 11 lines (not 10) because `def to_dict` matches both HiderRecord.to_dict (03-01) AND HiderRegistry.to_dict (03-07) — pre-existing overlap, all 10 HiderRegistry methods present. Single-plan Wave 4 (no concurrent agents) — plain git commit.
- Trend (03-07, prior): registry SERIALIZATION layer (biochemeleon/registry.py: to_dict() -> {'version':1,'hiders':[record.to_dict() ...]} in insertion order; from_dict(d) @classmethod tolerant reconstruction - missing version treated as v1, missing status defaults HIDER_STATUS_HIDDEN, missing pos stays None; required object/id/rep raise KeyError; id coerced to int via register). 188->228 lines (2 methods under # ---- serialization (Phase 8 .bcm sidecar shape) ---- + module/class docstring accuracy: 'Phase 3 methods' lists now enumerate to_dict/from_dict as implemented, reconstruct_from_sentinels noted as remaining later-plan work). Pure (stdlib + GAME_REPS only; no pymol) - fully WSL-verified (46 registry tests [33 core + 7 queries + 6 serialize] + 136 total + py_compile + purity/Pitfall-1/Pitfall-11 all zero via sequential bash grep -rnE; Grep tool cross-wired batch results, re-confirmed sequentially). TDD: RED b04b04b (6 tests fail AttributeError), GREEN a20f47a (all pass), REFACTOR gates-only (no commit). Round-trip asserts id/object/rep/status (NOT pos - list/tuple normalization deferred to Phase 8 boundary per RESEARCH serialization-shape note). Ran concurrently with 03-09 (mutation.py cleanup_hiders - observed mutation.py modified uncommitted, left untouched); files disjoint; plain git commit, staged only tests/test_registry.py (RED) + biochemeleon/registry.py (GREEN).

*Updated after each plan completion*

### Quick Tasks Completed

| # | Description | Date | Commit | Directory |
|---|-------------|------|--------|-----------|
| 001 | Add parallel-subagent worktree/branch protocol to AGENTS.md | 2026-08-08 | 531d92e | [001-parallel-execution-worktree-protocol](./quick/001-parallel-execution-worktree-protocol/) |

## Accumulated Context

### Roadmap Evolution
- Phase 11 added (2026-08-09): Alt-conf Cartoon/Ribbon Hider (v1 Follow-up) — appended after Phase 10. Closes the HIDER-03/HIDER-05 alt-conf gap from Phase 5 (terminal-extension cartoon renders disconnected on 1ubq; the alt-conf "segment replication as alternate position" approach was spike-verified in isolation but multi-rep integration failed across 4 GUI-only bug cycles). Phase 11 has the HIGHEST research flag — requires re-research on `cmd.create` append side effects + GUI-runnable verification (headless auto_zoom gap was the methodology failure). Prior attempt's work preserved in `backup/05-08-attempts` branch but should NOT be used as implementation reference (re-research from scratch).

### Wave 1 outputs (02-01 + 02-02 — STILL AVAILABLE)

- **setup_state.py (Plan 02-01)**: GAME_REPS (5 reps), DEMO_MANIFEST (6 demos), DEFAULTS (9 keys), SETUP_FORMAT, hider_count_cap, randomize_state, validate_state. 48 unit tests pass.
- **data/demos/*.pdb (Plan 02-02)**: 6 valid PDBs (1znf, 1xdn, 5e54, 1k8p, 2qbz, 4wb3) + SOURCES.md (64 lines, 6 DOIs). Git-tracked.

### Wave 2 outputs (02-03 — STILL AVAILABLE; extended by 02-05)

- **demos.py (137 lines)**: 5 cmd-coupled utilities — to_windows_path (WSL guard), list_loaded_molecule_objects, fetch_pdb (async_=0), get_active_reps (`rep <name>` selector), load_demo (__file__-relative + cmd.load). Imports GAME_REPS, DEMO_MANIFEST from setup_state. (UNCHANGED by 02-05)
- **gui_setup.py (390 -> 472 lines)**: full SetupTab — PyMOLObjectCombo, 3-mode QStackedWidget, capped hider spinbox, lock-scene auto-detect, 5 per-rep rows, difficulty toggle, 4 buttons, collect/apply round-trip, JSON save/load, randomize/validate. 02-05 added: current_target_object returns combo text (Gap 1), _recompute_per_rep_maxes (Gap 2 UI), lock_source_cb checkbox (Gap 3), pool_edit + _pool_list (Gap 4), _randomize passes lock_source/locked_state/pdb_pool.

### Wave 3 outputs (02-05 gap closure — STILL AVAILABLE; extended by 02-06)

- **setup_state.py (169 -> 313 lines)**: PDB_POOL (33 verified RCSB entries — 6 bundled demos + 14 proteins + 3 DNA + 4 RNA + 6 hybrid; plan prose said "34" but the actual list + category breakdown both sum to 33, used verbatim), _validate_pdb_code/_validate_pdb_pool helpers, DEFAULTS extended to 11 keys (+ lock_source, pdb_pool), randomize_state(lock_source, locked_state, pdb_pool), validate_state per_rep-sum clamp + new-field validation. Module still pure (no pymol, no Qt). 02-06 TIGHTENED _validate_pdb_code to exactly 4-char (was 3-5); _validate_pdb_pool + PDB_POOL + DEFAULTS + randomize_state + validate_state UNCHANGED.
- **tests/test_setup_state.py (244 -> 471 lines)**: 6 new test classes from 02-05 (TestPdbPool, TestDefaultsExtended, TestValidateStatePerRepSum, TestValidateStateNewFields, TestRandomizeLockSource, TestRandomizePdbPool) + TestDefaults.test_has_all_keys updated for 11-key schema. 02-06 added TestValidatePdbCode (10 tests) + updated test_pdb_pool_filters_invalid (input "12345" 5-char now rejected at _validate_pdb_code). 90 tests pass (48 pre-02-05 + 32 from 02-05 + 10 from 02-06).
- **gui_setup.py (390 -> 472 -> 552 lines)**: full SetupTab. 02-05 added: current_target_object (Gap 1), _recompute_per_rep_maxes (Gap 2 UI), lock_source_cb (Gap 3), pool_edit QPlainTextEdit + _pool_list (Gap 4), _randomize passes lock_source/locked_state/pdb_pool. 02-06 REPLACED pool_edit QPlainTextEdit with pool_list QListWidget (ExtendedSelection) inside a QGroupBox "Pool of PDB IDs (Randomize picks fetch codes from here)" + 4 buttons (+ Add, ✎ Edit, − Remove, Use bundled pool) + 4 slot methods (_add_pool_entry/_edit_pool_entry/_remove_pool_entry/_use_bundled_pool); _pool_list reads QListWidget items; apply_state clears + repopulates QListWidget (validates each); _validate_pdb_code + PDB_POOL imported.
- **All 4 smoke-test gaps closed at WSL tier (02-05) + 2 UX issues closed at WSL tier (02-06) + 1 enhancement closed at WSL tier (02-07)**. Awaiting user re-run of 02-04 Windows PyMOL smoke test to confirm runtime behavior (QListWidget populate on first show, Add/Edit/Remove/Use-bundled-pool/Choose-random buttons, QMessageBox on invalid, cmd.count_atoms, QSpinBox.setMaximum are WSL-unverifiable).

### Wave 4 outputs (02-07 gap closure — STILL AVAILABLE; UI-only enhancement)

- **gui_setup.py (552 -> 581 lines)**: 02-07 added a "Choose random" button (`self.pool_choose_btn`, QPushButton) as the 5th button in the pool button row (inside the pool QGroupBox, visually associated with the pool — not with the main Reset/Randomize/Save/Load Setup actions). Wired `pool_choose_btn.clicked -> _choose_random_from_pool` slot. The slot picks `random.choice(self._pool_list() or list(PDB_POOL))`, switches `mode_combo` to index 1 (fetch) so the field is visible, sets `pdb_edit` to the chosen code; returns early if both lists empty (defensive — PDB_POOL has 33 entries so unreachable). Does NOT touch any other setup field (hider count, lock scene, per-rep, difficulty, lock source, demo, loaded object) — focused, single-purpose. `import random` added at module level (line 15, next to `import json`). PDB_POOL import (line 22, from 02-06) verified present, not re-added. NO pure-layer change; NO new test (UI-only behavior; pre-existing 90 tests pin the pure layer).

### Phase 3 outputs (consolidated — smoke-verified 2026-08-06, 24/24 ALL PASSED)

Phase 3 built the mutation-safety + hider-registry foundation. All artifacts below are runtime-verified by `smoke/phase3_smoke.py` (headless Windows PyMOL from WSL via `cmd.exe /c C:\\src\\run-conda-pymol.bat -cq`). Detailed per-wave entries follow this block.

- **registry.py (pure, 267 lines, 10 methods, 54 WSL unit tests)**: `HiderRecord` (`__slots__=('id','object','rep','status','pos')`, rep validated against `GAME_REPS`) + `HiderRegistry` keyed by `(object, id)` tuple (Pitfall 4 lock; id stable across add/remove). Methods: register/get/all/remove/by_rep/counts_by_rep/mark_found/to_dict/from_dict/reconstruct_from_sentinels. `rep=None` tolerance for post-`.pse` reconstruction (Phase 8 `.bcm` sidecar reconciles rep). NO `from pymol` (pure — unit-testable in WSL).
- **backup.py (cmd-coupled, 82 lines)**: snapshot/restore/discard/verify_intact. `BACKUP_PREFIX='_bchm_backup'` (underscore-hidden from `public_objects`). restore = `cmd.delete(target)` + `cmd.create(target, backup)` two-step (NEVER single-call `create(existing, backup)` — RESEARCH §Q2; smoke confirmed single-call IS REPLACE, but delete+create stays canonical/unambiguous). verify_intact = count gate + `(resn,resi,name,chain,segi)` identity multiset (NO x/y/z — iterate has no coords; count+identity suffices since cmd.create copies coords bit-for-bit).
- **mutation.py (cmd-coupled, 148 lines)**: insert_hider (`cmd.pseudoatom(object=existing)` in-place + `cmd.alter` sentinel `segi='GAME'`+`b=-999` via hygienic `space={}` + `cmd.sort` + `cmd.identify mode=0` → stable id) / fetch_all_hider_ids (sentinel iterate, selector `segi GAME and b < 0` — b-factor sentinels matched by COMPARISON not exact match) / cleanup_hiders (sentinel `cmd.remove` by `segi GAME`).
- **game.py (cmd-coupled, 69 lines)**: `GameController` orchestrator — start (snapshot-before-mutation → insert loop → register by id) / cleanup (sentinel remove → verify_intact → discard) / abort_on_error (delete+create restore → discard) / reconstruct_registry (DI: injects `lambda: mutation.fetch_all_hider_ids(obj)` into the pure registry's `reconstruct_from_sentinels`).
- **smoke/phase3_smoke.py (162 lines)**: complete round-trip verification — criteria C1 (object list unchanged + count+=3) / C2 (3 sentinels segi=GAME b=-999) / C3 (registry queries + per-rep counts) / C4 (cleanup intact) + failure-path restore + Q1/Q2/PSE spikes. Ran headlessly 24/24 ALL PASSED via `cmd.exe /c C:\\src\\run-conda-pymol.bat -cq`.
- **Spike findings (runtime-confirmed, recorded in PITFALLS.md "Phase 3 — Resolved Research Flags")**: Q1 `cmd.pseudoatom` returns `None` (NoneType) — code uses `cmd.identify mode=0`. Q2 single-call `cmd.create(existing,src)` IS REPLACE (`n_after==n_before`, no doubling) — delete+create stays canonical. PSE: `segi='GAME'` sentinel survives reload; id stable (`pse_sent==[saved_id]`, Pitfall 4 holds); `b=-999.0` preserved; `reconstruct_from_sentinels` works (1 record, `rep=None`); Phase 8 `.bcm` sidecar reconciles rep.
- **3 new runtime pitfalls (PITFALLS.md)**: (1) `cmd.iterate` exposes `ID` uppercase not lowercase `id`; (2) `cmd.iterate` has no x/y/z (use `cmd.iterate_state`); (3) b-factor selectors are comparisons (`b < 0`) never exact (`b -999` — malformed, silently matches nothing).

### Phase 3 Wave 1 outputs (03-02 — STILL AVAILABLE; cmd-coupled, standalone)

- **backup.py (41 lines, NEW)**: cmd-coupled backup module (standalone — no import of setup_state/registry/mutation; mirrors demos.py's `from pymol import cmd` + section-comment style). `BACKUP_PREFIX = '_bchm_backup'` (underscore => private, hidden from `cmd.get_names('public_objects')` per RESEARCH Q6). `snapshot(target_obj)` = `cmd.delete(BACKUP_PREFIX)` (idempotent stale-backup discard) + `cmd.create(BACKUP_PREFIX, target_obj)` (fresh independent deep copy, creating.py:960) + return backup name. `discard(backup_name=BACKUP_PREFIX)` = `cmd.delete(backup_name)` (idempotent — safe on absent objects). `restore()` and `verify_intact()` deliberately NOT added (plans 03-05 + 03-08). 1 Rule-3 auto-fix: reworded the module docstring NOTE from `` `from pymol import cmd` will FAIL `` to "The pymol.cmd import will FAIL" to avoid a false-positive `from pymol` grep match (mirrors the AGENTS.md-documented "from PyQt5 import" docstring false positive). All WSL gates green (py_compile biochemeleon/*.py + 90 tests + Pitfall-1/11 grep gates zero matches). Runtime (cmd.delete/cmd.create deep-copy independence, idempotent delete, underscore-prefix privacy) deferred to the Phase 3 smoke test (plans 03-13/03-14, run via 03-15 checkpoint).

### Phase 3 Wave 1 outputs (03-01 — STILL AVAILABLE; pure-layer, WSL-tested)

- **registry.py (148 lines, NEW)**: HiderRegistry pure data model — stdlib + `setup_state.GAME_REPS` only (NO `from pymol`, NO `from pymol.Qt`), WSL-unit-testable, mirroring setup_state.py. `HIDER_STATUS_HIDDEN='hidden'` + `HIDER_STATUS_FOUND='found'` constants. `HiderRecord` with `__slots__=('id','object','rep','status','pos')`; `__init__` validates `rep in GAME_REPS` (ValueError else — 'surface' out of scope) + coerces `id` to int; `key()` returns `(self.object, self.id)`; `to_dict()` omits `pos` when None, includes it as a list when set. `HiderRegistry` with `OrderedDict`-backed store keyed by `(object, id)` (Pitfall 4: id stable across add/remove; index is not); `register()` raises KeyError on duplicate `(object,id)`, returns HiderRecord; `get()` returns record or None; `all()` returns a fresh insertion-order list; `remove()` returns True then False (idempotent — never raises on absent). All three of register/get/remove coerce id to int (str '1' ↔ int 1 round-trip). Core CRUD only — `by_rep`/`counts_by_rep`/`mark_found` (03-02 research-wave) + `reconstruct_from_sentinels`/`to_dict`/`from_dict` (03-03) deliberately deferred per plan. 33 unit tests pass (TestHiderRecord + TestHiderRegistryCore); 123 total (90 setup_state + 33 registry). All WSL gates green (py_compile + tests + purity `from pymol` zero matches in registry.py + Pitfall-1/11 zero matches). No runtime/PyMOL behavior to defer — pure layer is fully verified at WSL tier.

### Phase 3 Wave 1 outputs (03-03 — STILL AVAILABLE; cmd-coupled, standalone)

- **mutation.py (74 lines, NEW)**: cmd-coupled hider-insertion primitive (standalone — no import of setup_state/registry/backup; mirrors demos.py's `from pymol import cmd` + section-comment + inline-source-citation style). `insert_hider(object, pos, rep, handle, segi='GAME', b=-999.0)`: (1) `cmd.pseudoatom(object=object, pos=list(pos), name=handle, segi=segi, b=b, hetatm=1, elem='PS', resn='HIDER', chain='H', resi='9001')` inserts INTO the existing object [HIDER-01 — creating.py:1082; no new object, public_objects unchanged]; (2) `cmd.alter(f"{object} and name {handle}", "segi='GAME'; b=-999.0", space={})` sets the sentinel in one multi-`;` call with hygienic space={} [HIDER-02 — editing.py:1424; editor.py:354 idiom; RESEARCH Q3]; (3) `cmd.sort(object)` defensive (editing.py:1457 — sort after altering segi/chain; reassigns index, preserves id); (4) `ids = cmd.identify(f"{object} and name {handle} and segi GAME", mode=0)` + `assert len(ids) == 1` + `return ids[0]` — stable id via mode=0 (querying.py:1269; id list, NOT fragile index), NEVER the pseudoatom return value (RESEARCH Q1: unverified C status code). `rep` accepted but NOT used for placement (registry needs it for per-rep counts; placement is Phase 4/5 generator work; Phase 3 places at caller-supplied pos). `fetch_all_hider_ids`/`cleanup_hiders` deliberately NOT added (plans 03-06/03-09). 2 deviations (both Rule 3 blocking, auto-fixed): (1) docstring API mentions reworded to prose to satisfy the `from pymol`/`cmd.*` verification greps (each returns exactly 1 match — the body call; AGENTS.md-warned false-positive pattern); (2) a concurrent-execution git collision — `git commit --amend` swept in the 03-02 agent's staged planning docs (ROADMAP/STATE/03-02-SUMMARY) from the shared index; recovered via safe-amend-pattern (unstage-all → stage-only-mine → amend) + reflog restore of the orphaned docs commits (7a5ce29→5ca3dc3 docs-03-02, 975c155→08e39b4 docs-03-01, original messages preserved). All WSL gates green (py_compile all incl. registry.py+backup.py + 123 tests + Pitfall-1/11 zero + mutation.py-specific greps each exactly 1 match). Runtime (pseudoatom in-place append, alter sentinel, sort, identify-returns-one-id) deferred to the Phase 3 smoke test (plans 03-13/03-14, run via 03-15 checkpoint).

### Phase 3 Wave 2 outputs (03-05 — STILL AVAILABLE; cmd-coupled, standalone)

- **backup.py (41 -> 58 lines)**: 03-05 added `restore(target_obj, backup_name=BACKUP_PREFIX)` — the FAILURE-PATH restore. Two-step `cmd.delete(target_obj)` (commanding.py:496 — remove mutated object entirely) + `cmd.create(target_obj, backup_name)` (creating.py:960 — fresh atom-for-atom copy from backup) inside a `try/except Exception` returning `True`/`False` (caller aborts game on False; never raises into orchestrator). NEVER single-call `cmd.create(existing, backup)` — RESEARCH section Q2 MEDIUM flag: that path's merge-vs-replace semantics are UNVERIFIED C-dispatched; the two-step sidesteps it unambiguously. Placed AFTER `discard`, BEFORE future `verify_intact` (03-08), with a `# ---- Restore (plan 03-05) ----` section comment. Module docstring updated for accuracy: lifecycle line "snapshot/restore/discard" (was "snapshot/discard half"), added `restore` bullet to API list, forward-ref line "restore() added in plan 03-05; verify_intact() added in plan 03-08". 1 cosmetic choice: docstring uses prose "RESEARCH section Q2" (matching in-file "RESEARCH section Q6" precedent on line 9) instead of the plan's literal "§Q2" — in-file consistency. backup.py now has snapshot (03-02) + restore (03-05) + discard (03-02); verify_intact (03-08) remains. All WSL gates green (py_compile all + 90 tests + Pitfall-1/11 zero + anti-pattern gate `cmd\.create\(.*backup` 2 matches: line 51 docstring WARNING text [plan-sanctioned] + line 55 actual two-step call; visually confirmed delete [line 54] precedes create [line 55] inside try). Runtime (delete+create atom-for-atom identity, try/except returning False not raising on missing backup/target) deferred to the Phase 3 smoke test (plans 03-13/03-14, run via 03-15 Windows PyMOL checkpoint); the smoke test confirms the RESEARCH Q2 sidestep landed atom-for-atom.

### Phase 3 Wave 2 outputs (03-06 — STILL AVAILABLE; cmd-coupled, standalone)

- **mutation.py (74 -> 112 lines)**: 03-06 added `fetch_all_hider_ids(object)` — the sentinel-based id reader. `out = []` + `cmd.iterate(f"{object} and segi GAME and b -999", "stored.append((model, id))", space={'stored': out})` (editing.py:1490) + `return out` (list of `(object_name, id)` tuples). `space={'stored': out}` is the HYGIENIC iterate pattern — NOT `space=None`, which per RESEARCH §Q3 / editing.py:47-62 runs the expression against the global `pymol.__dict__` (legacy `stored.xxx` pattern, pollutes namespace); the explicit dict is the editor.py:156 pattern. Selector `segi GAME and b -999` is the sentinel (the only hider-detection mechanism; survives `.pse` reload even if ids shift). `model` + `id` are read-only iterate symbols (editing.py:1446-1449); `id` is the STABLE integral identifier (NOT `index` — RESEARCH §Q4; querying.py:1315 warns indices are fragile across add/delete). Returns `(model, id)` tuples (model = object name) so the registry can key by `(object, id)` even in future multi-object scenarios — this is the data shape `registry.reconstruct_from_sentinels(iterate_fn)` (plan 03-10) consumes via dependency injection (game.py injects `lambda: mutation.fetch_all_hider_ids(obj)`; registry.py stays pure). Placed AFTER `insert_hider`, behind a `# ---- Hider id reader (sentinel-based) ----` section comment. Module docstring extended with one sentence mentioning the new fn. Body follows 03-RESEARCH.md Backup/Restore Design lines 329-334 EXACTLY; docstring expanded to full Args/Returns style mirroring `insert_hider` (inline citations editing.py:1490/1446-1449 + RESEARCH §Q3/§Q4 refs). `cleanup_hiders` deliberately NOT added (deferred to plan 03-09). Docstring written in PROSE ("the iterate primitive", "explicit `space=` dict", "sentinel (`segi='GAME'` + `b=-999`)") to avoid literal `cmd.iterate` / `space={'stored'` / `segi GAME and b -999` false-positives on the 4 task exact-count greps (each returns exactly 1 — the body call; mirrors the 03-03 docstring-rewording precedent). All WSL gates green (py_compile all + 90 tests + Pitfall-1/11 zero + `space=` gate 4 matches ≥2 [line 17 docstring + line 72 alter body + line 88 new docstring + line 111 new iterate body] + the 4 exact-count greps each =1). Runtime (`cmd.iterate` sentinel read, `model`/`id` symbols, `space=` hygiene, id stability across `.pse` reload) deferred to the Phase 3 smoke test (plans 03-13/03-14, run via 03-15 Windows PyMOL checkpoint) — the smoke test's Q4 id-stability spike + `.pse` round-trip section both call `fetch_all_hider_ids`.

### Phase 3 Wave 2 outputs (03-04 — STILL AVAILABLE; pure-layer, WSL-tested)

- **registry.py (148 -> 188 lines)**: 03-04 added the QUERY + STATUS layer to the HiderRegistry pure data model (extends 03-01's core CRUD). Three new methods under a `# ---- queries + status ----` section: `by_rep(rep)` returns a fresh list of records matching `rep` in insertion order (`[]` for empty, NOT None — Phase 4 Game tab iterates without a None-check); `counts_by_rep()` returns `{rep: count}` for ALL 5 `GAME_REPS` zero-filled (pre-populates `{rep:0 for rep in GAME_REPS}` before tallying — criterion 3: per-rep counts; Game tab renders "cartoon: 0" even with no cartoon hiders); `mark_found(object, id)` sets `rec.status = HIDER_STATUS_FOUND` via direct `self._records[(object, int(id))]` lookup (raises KeyError on unregistered — clean error signal for Phase 4 click handler; click on non-hider atom is a caller bug; `int(id)` coercion matches register/get/remove). Module + class docstrings updated: "Phase 3 methods" lists now enumerate by_rep/counts_by_rep/mark_found as implemented (was "Later plans add ... (03-02)"). Still pure (stdlib + `setup_state.GAME_REPS` only; NO `from pymol`, NO `pymol.Qt`). 7 new unit tests (TestHiderRegistryQueries: by_rep matching + empty, counts_by_rep all-reps-present + empty-registry, mark_found sets-status + only-affects-target + KeyError-on-unregistered); 40 total registry tests (33 core + 7 queries); 130 total across pure layer (40 registry + 90 setup_state). All WSL gates green (py_compile all + 130 tests + purity `from pymol`=0 in registry.py + Pitfall-1=0 + Pitfall-11 `.exec_()`=0). No runtime/PyMOL behavior to defer — pure layer fully verified at WSL tier. TDD: RED `998aa85` (7 tests fail AttributeError), GREEN `97a3743` (all pass), REFACTOR gates-only (no commit — implementation followed the plan sketch verbatim and was clean on first pass).

### Phase 3 Wave 3 outputs (03-07 — STILL AVAILABLE; pure-layer, WSL-tested)

- **registry.py (188 -> 228 lines)**: 03-07 added the SERIALIZATION layer to the HiderRegistry pure data model (extends 03-01 core CRUD + 03-04 queries/status). Two new methods under a `# ---- serialization (Phase 8 .bcm sidecar shape) ----` section: `to_dict()` returns `{'version': 1, 'hiders': [r.to_dict() for r in self._records.values()]}` (insertion order; composes the existing HiderRecord.to_dict); `from_dict(cls, d)` is a `@classmethod` tolerant reconstruction — `reg = cls()` then for each `h` in `d.get('hiders', [])`: `reg.register(h['object'], h['id'], h['rep'], h.get('status', HIDER_STATUS_HIDDEN), h.get('pos'))`; missing 'version' accepted (treated as v1, no KeyError), missing 'status' defaults HIDER_STATUS_HIDDEN, missing 'pos' stays None, required object/id/rep raise KeyError, id coerced to int via register. Module + class docstrings updated: "Phase 3 methods" lists now enumerate to_dict/from_dict as implemented, reconstruct_from_sentinels noted as the remaining later-plan work. Still pure (stdlib + `setup_state.GAME_REPS` only; NO `from pymol`, NO `pymol.Qt`). 6 new unit tests (TestHiderRegistrySerialize: empty / three-hiders-with-pos / round-trip [id/object/rep/status match, NOT pos] / missing-status-defaults-hidden / missing-version-no-KeyError / empty-hiders); 46 total registry tests (33 core + 7 queries + 6 serialize); 136 total across pure layer (46 registry + 90 setup_state). All WSL gates green (py_compile all + 136 tests + purity `from pymol`=0 in registry.py + Pitfall-1=0 + Pitfall-11 `.exec_()`=0 — confirmed via sequential bash `grep -rnE` after the Grep tool cross-wired its batch results). No runtime/PyMOL behavior to defer — pure layer fully verified at WSL tier. TDD: RED `b04b04b` (6 tests fail AttributeError), GREEN `a20f47a` (all pass), REFACTOR gates-only (no commit — implementation followed the RESEARCH sketch verbatim and was clean on first pass). Shape designed NOW (Phase 3) so Phase 8 just writes `registry.to_dict()` to a `.bcm` JSON file and reads it back via `HiderRegistry.from_dict()` — no schema migration at Phase 8.

### Phase 3 Wave 3 outputs (03-08 — STILL AVAILABLE; cmd-coupled, standalone)

- **backup.py (58 -> 82 lines)**: 03-08 added `verify_intact(target_obj, backup_name=BACKUP_PREFIX)` — the structure-integrity checker proving criterion 4. Two-stage check: (1) count gate `target_n = cmd.count_atoms(target_obj)` / `backup_n = cmd.count_atoms(backup_name)` / `if target_n != backup_n: return False` (cheap, catches gross mismatch — querying.py:1412); (2) atomic-tuple multiset — `target_tuples`/`backup_tuples` lists built via `cmd.iterate(obj, "stored.append((resn, resi, name, chain, segi, x, y, z))", space={'stored': <list>})` (editing.py:1490; hygienic explicit dict — NOT space=None which pollutes pymol.__dict__, RESEARCH sec Q3; mirrors mutation.py's established pattern), then `return sorted(target_tuples) == sorted(backup_tuples)` (order-independent multiset compare — survives cmd.sort atom reordering). Exact float equality on x/y/z is safe (cmd.create copies coords bit-for-bit, RESEARCH sec Q6; fallback coords-omitted compare documented). Placed AFTER `restore`, behind a `# ---- Verify integrity (plan 03-08) ----` section comment. Module docstring updated: lifecycle line "snapshot/restore/discard/verify" (was "snapshot/restore/discard"), added verify_intact API bullet, forward-ref line now past-tense per-plan attribution ("snapshot() + discard() in 03-02; restore() in 03-05; verify_intact() in 03-08"). 2 plan-internal inconsistency resolutions: the plan's <action> "follow EXACTLY" code used a `_tuples` helper (1 source `space={'stored'}`) + a one-line count check (1 source `cmd.count_atoms` line), but the <verification> required >=2 matches for both; resolved by inlining the two iterate calls (one per object) + splitting the count check into named locals — algorithm identical. All WSL gates green (py_compile all + 90 setup_state tests + 136 total discovery + Pitfall-1=0 + Pitfall-11=0 + space= gate 2 + count_atoms 2 + sorted 1 + def verify_intact 1). backup.py lifecycle now complete: snapshot (03-02) + restore (03-05) + discard (03-02) + verify_intact (03-08). Runtime (count gate, tuple multiset, exact float equality, space= hygiene) deferred to the Phase 3 smoke test (03-13/03-14 via 03-15 Windows PyMOL checkpoint) — the smoke test asserts verify_intact returns True after both cleanup-happy-path AND restore-failure-path, closing criterion 4 at the runtime tier.

### Phase 3 Wave 3 outputs (03-09 — STILL AVAILABLE; cmd-coupled, standalone)

- **mutation.py (112 -> 148 lines)**: 03-09 added `cleanup_hiders(object)` — the sentinel-based HAPPY-PATH cleanup (the counterpart to `backup.restore` which is the failure-path cleanup; together they cover criterion 4's two paths). `n = cmd.count_atoms(f"{object} and segi GAME")` (querying.py:1412) is the idempotent gate — if `n == 0` the selection is already clean, skip `cmd.remove` and return 0 (no error on an empty selection); else `cmd.remove(f"{object} and segi GAME")` (editing.py:800) deletes the hider atoms FROM the object and `return n` (the before-count) so the caller (`game.py` `GameController.cleanup` in plan 03-12) asserts it matches the registry length. Selector is `segi GAME` ONLY (the plan's verbatim code; NOT `segi GAME and b -999` as in `fetch_all_hider_ids` — hiders are the only atoms with `segi=GAME`, so `segi GAME` alone suffices for cleanup), NEVER `resi`/`chain`/per-object `index` (unstable across deletions — the sentinel survives where the registry can be lost on `.pse` reload; the registry rebuilds from sentinels via `fetch_all_hider_ids` + `reconstruct_from_sentinels`). `cmd.remove` (NOT `cmd.delete`) — remove deletes atoms FROM the object (editing.py:800), delete removes the whole object (commanding.py:496); cleanup leaves the original structure intact so the object matches its pre-game state (criterion 4 happy path). Placed AFTER `fetch_all_hider_ids`, behind a `# ---- Hider cleanup (happy path) ----` section comment. Module docstring extended with a 3-line mention of the new function. Docstring written in PROSE ("the remove primitive", "the count-atoms primitive", "sentinel (``segi='GAME'`` + ``b=-999``)") to avoid literal `cmd.remove` / `cmd.count_atoms` false-positives on the task's exact-count grep gates (each returns exactly 1 — the body call; mirrors the 03-03/03-06 docstring-rewording precedent — Rule 3 blocking, the plan's literal docstring would have tripped its own verification). `mutation.py` is now functionally complete for Phase 3: `insert_hider` (03-03) + `fetch_all_hider_ids` (03-06) + `cleanup_hiders` (03-09) — the three cmd-coupled mutation primitives the game orchestrates. All WSL gates green (py_compile all + 90 setup_state tests + mutation.py completeness gate [3 functions: insert_hider + fetch_all_hider_ids + cleanup_hiders] + Pitfall-1=0 + Pitfall-11=0 + sentinel-only cleanup [cmd.remove 1 match + cmd.count_atoms 1 match]). cmd-coupled — `cmd.remove` (atoms-from-object, NOT the whole object) + `cmd.count_atoms` behavior is WSL-unverifiable (no PyMOL in WSL; py_compile is syntax-only); runtime deferred to the Phase 3 smoke test (plans 03-13/03-14, run via 03-15 Windows PyMOL checkpoint) — the smoke test's C4 happy-path section calls `cleanup_hiders` and asserts the post-cleanup count + structure match the pre-game backup exactly. Wave 3 concurrent execution (03-07/03-08/03-09) — files disjoint; STATE.md shared (last writer wins); plain git commit (never `--amend` per the 03-03 lesson), staged only `biochemeleon/mutation.py`.

### Phase 3 Wave 4 outputs (03-10 — STILL AVAILABLE; pure-layer, WSL-tested)

- **registry.py (223 -> 267 lines)**: 03-10 added the SENTINEL RECONSTRUCTION layer to the HiderRegistry pure data model (extends 03-01 core CRUD + 03-04 queries/status + 03-07 serialization — registry.py is now FUNCTIONALLY COMPLETE for Phase 3, 10 methods). One new method under a `# ---- sentinel reconstruction (dependency injection) ----` section: `reconstruct_from_sentinels(iterate_hider_keys)` — the post-`.pse`-reload registry rebuild via DEPENDENCY INJECTION. Clears `self._records` (overwrite, NOT append), loops the injected callable, registers each `(obj, aid)` as `HiderRecord(aid, obj, rep=None, status=HIDER_STATUS_HIDDEN)` keyed by `(obj, int(aid))` (int coercion matches register/get/remove), returns `self` (fluent). `iterate_hider_keys` is INJECTED by `game.py` (typically `lambda: mutation.fetch_all_hider_ids(obj)`) — the iterate is a PARAMETER, not an import, so registry.py stays pure (NO `from pymol` — DI confirmed, purity gate ZERO). `rep=None` is the sentinel-reconstruction value: the sentinel carries no rep (RESEARCH Open Risk 6), so rebuilt records store `None` pending Phase 8 `.bcm` sidecar reconciliation. Three supporting changes: (1) `HiderRecord.__init__` rep validation RELAXED from `if rep not in GAME_REPS` to `if rep is not None and rep not in GAME_REPS` — allows `rep=None` (option (b) from the plan, simpler than a `validate_rep=True` parameter; normal `register()` always passes a valid rep so the relaxation only affects reconstruction); (2) `counts_by_rep` GUARDED against `rep=None` records (`if r.rep is None: continue` — the returned dict has only `GAME_REPS` keys, never a `None` key; documented limitation Phase 8 sidecar reconciles, pinned by test_reconstruct_rep_none_then_counts_by_rep); (3) module + class + HiderRecord docstrings updated (rep=None documented; reconstruct_from_sentinels listed as implemented; registry.py marked functionally complete for Phase 3, 10 methods). Still pure (stdlib + `setup_state.GAME_REPS` only; NO `from pymol`, NO `pymol.Qt`). 8 new unit tests (TestHiderRegistryReconstruct [5: fake iterate / clears+rebuilds / empty iterate / returns self / rep=None bypasses validation] + TestHiderRegistryEdgeCases [3: bad rep ValueError reconfirm 03-01 / dup id KeyError reconfirm 03-01 / rep=None then counts_by_rep invisible]); 54 total registry tests (33 core + 7 queries + 6 serialize + 5 reconstruct + 3 edge); 144 total across pure layer (54 registry + 90 setup_state). All WSL gates green (py_compile all + 144 tests + purity `from pymol`=0 in registry.py + Pitfall-1=0 + Pitfall-11 `.exec_()`=0 — confirmed via sequential bash `grep -rnE` after the 03-07 Grep-tool cross-wiring caution; completeness grep returns 11 lines [not 10] because `def to_dict` matches both HiderRecord.to_dict [03-01] and HiderRegistry.to_dict [03-07] — pre-existing overlap, all 10 HiderRegistry methods present). No runtime/PyMOL behavior to defer — pure layer fully verified at WSL tier (the injected iterate fn is a fake lambda in tests; game.py injects the real `mutation.fetch_all_hider_ids` at runtime). TDD: RED `1a71d61` (6 tests fail AttributeError, 2 reconfirm pass), GREEN `fbe173f` (all pass), REFACTOR `79dd6b1` (1 Rule-3 gate-fix — reworded reconstruct docstring to avoid a `from pymol` false-positive grep match, mirrors 03-02 precedent). Single-plan Wave 4 (no concurrent agents) — plain git commit, staged only tests/test_registry.py (RED) + biochemeleon/registry.py (GREEN + REFACTOR).

### Phase 3 Wave 5 outputs (03-11 — STILL AVAILABLE; cmd-coupled, standalone)

- **game.py (1 -> 42 lines)**: 03-11 replaced the 1-line Phase-1 stub with GameController — the thin orchestrator wiring backup + mutation + registry (the key_link "game.py -> backup+mutation+registry orchestration"; orchestration gate `backup\.|mutation\.|registry\.` = 7 matches >= 3). `__init__(target_obj)` sets target_obj + a fresh HiderRegistry + `_backup_name=None` + `_started=False`. `start(hider_specs)` — hider_specs is a list of (pos, rep) tuples (Phase 3 placeholder; Phase 4/5 generators produce the real specs) — guards double-start via `if self._started: raise RuntimeError("game already started; call cleanup() first")`, then calls `backup.snapshot(self.target_obj)` FIRST (snapshot-before-mutation — no-undo safety; the backup MUST precede every destructive op), builds a fresh `self.registry = registry.HiderRegistry()` (fresh per round), loops `for i, (pos, rep) in enumerate(hider_specs): handle = "H%03d" % i; aid = mutation.insert_hider(self.target_obj, pos=pos, rep=rep, handle=handle); self.registry.register(object=self.target_obj, id=aid, rep=rep)` (id flows insert -> identify -> register — the stable id returned by insert_hider, fetched via cmd.identify mode=0 inside mutation.py NOT the fragile index, is the (object, id) key), sets `_started=True`, returns the backup name. `reconstruct_registry()` — DI rebuild: `self.registry = registry.HiderRegistry().reconstruct_from_sentinels(lambda: mutation.fetch_all_hider_ids(self.target_obj)); return self.registry` (the lambda injects the cmd-coupled iterate fn into the pure registry's reconstruct — registry.py stays pure, NO cmd import; game.py is the composition root; mirrors the 03-10 DI decision). Imports `from pymol import cmd` (documents the cmd-coupled layer; GameController delegates to backup/mutation/registry in this plan — no direct cmd call yet — but 03-12 cleanup/abort + Phase 4 click-loop wiring will use cmd) + `from . import backup, mutation, registry`. Dependency direction strict (game -> backup/mutation/registry, never the reverse — mirrors setup_state <- demos <- gui_setup). Handle format `H%03d` (H000, H001, ...) — unique per insertion index, the throwaway name handle for cmd.identify inside insert_hider. cmd-coupled — runtime (snapshot-before-mutation ordering, insert->identify->register id flow, _started double-start guard, DI reconstruct after .pse reload) is WSL-unverifiable (no PyMOL in WSL; py_compile is syntax-only, the 144 unit tests exercise only the pure layer) — deferred to the Phase 3 smoke test (plans 03-13/03-14, run via 03-15 Windows PyMOL checkpoint). All WSL gates green (py_compile all + 144 tests [54 registry + 90 setup_state, no regression] + Pitfall-1=0 + Pitfall-11 `.exec_()`=0 + orchestration gate 7 >= 3 + the 4 exact-count greps: backup.snapshot=1 [line 27, BEFORE insert at line 31], mutation.insert_hider=1 [line 31], registry.register=1 [line 32, id flows insert->register], reconstruct_from_sentinels=1 [line 39, DI]). No deviations — the plan's prose docstrings ("insert_hider ->", "register.", "snapshot ->", "Rebuild the registry from sentinel atoms") avoided the dotted-literal false-positives that tripped 03-03/03-06/03-09/03-10's exact-count greps; no Rule-3 docstring rewording needed. Grep-tool cross-wired its parallel batch results (03-07/03-10 caution); exact per-file counts re-confirmed via sequential bash grep -n / grep -rnE. Single-plan Wave 5 (no concurrent agents) — plain git commit, staged only biochemeleon/game.py (feat f8606ab).

### Phase 3 Wave 6 outputs (03-12 — STILL AVAILABLE; cmd-coupled, standalone)

- **game.py (42 -> 69 lines)**: 03-12 added cleanup() + abort_on_error() to GameController — the criterion-4 two-path happy/failure cleanup (the key_link "cleanup -> verify_intact -> (if False) -> abort -> restore"; both paths leave the target atom-for-atom identical to its pre-game backup). `cleanup()` — happy path: guards `if not self._started: return True` (idempotent when not started — defensive for UI double-clicks), then `removed = mutation.cleanup_hiders(self.target_obj)` (sentinel remove — atoms FROM the object via cmd.remove, NOT the object; robust against registry loss on .pse reload, RESEARCH sec Q4), `intact = backup.verify_intact(self.target_obj, self._backup_name)` (the criterion-4 structure proof — count gate + atomic-tuple multiset), `backup.discard(self._backup_name)`, reset `_backup_name=None` + `_started=False` + `self.registry = registry.HiderRegistry()` (round over), `return intact` (the verify_intact result — caller aborts on False). `abort_on_error()` — failure path: guards `if not self._started: return True`, then `ok = backup.restore(self.target_obj, self._backup_name)` (the delete+create two-step — RESEARCH sec Q2: NEVER single-call cmd.create(existing, backup) which is UNVERIFIED C-dispatched merge-vs-replace; backup.restore already wraps cmd errors in try/except returning True/False per 03-05), `backup.discard(self._backup_name)`, reset, `return ok` (the restore result). Both reset _started + clear the registry (the round is over; a fresh start() builds a fresh registry per the 03-11 contract). Both idempotent when not started (return True, no-op). `removed` is retained for debugging + the future smoke-test assertion (not asserted in Phase 3 — asserting would require the registry intact, contradicting the by-sentinel robustness goal). game.py now FUNCTIONALLY COMPLETE for Phase 3 (5 methods: __init__/start/reconstruct_registry/cleanup/abort_on_error). All WSL gates green (py_compile all + 144 tests [54 registry + 90 setup_state, no regression] + Pitfall-1/11=0 + completeness gate 5 + orchestration gate 15 [14 real dotted module calls + 1 docstring sentence-period "discard backup." on line 44 — benign false positive on a minimum-bound >=6 gate, NOT an exact-count gate] + 4 exact-count greps: mutation.cleanup_hiders=1, backup.verify_intact=1, backup.restore=1, backup.discard=2). cmd-coupled — runtime (cleanup happy-path sentinel-remove + verify_intact, abort failure-path restore, both idempotent-when-not-started, both reset _started + registry) is WSL-unverifiable (no PyMOL in WSL; py_compile is syntax-only, the 144 unit tests exercise only the pure layer) — deferred to the Phase 3 smoke test (plans 03-13/03-14, run via 03-15 Windows PyMOL checkpoint): the smoke test asserts cleanup_hiders removes N sentinels + verify_intact returns True (criterion 4 happy path) AND restore brings the target back atom-for-atom + verify_intact returns True (criterion 4 failure path) AND both reset _started so a new round can start. 0 deviations — the plan's prose docstrings ("discard backup.", "restore from backup", "verify_intact check", "cleanup() returned False") avoided the dotted-literal false-positives on the 4 exact-count greps (no Rule-3 rewording needed, mirrors 03-11 unlike 03-03/03-06/03-09/03-10). Single-plan Wave 6 (no concurrent agents) — plain git commit, staged only biochemeleon/game.py (feat 14cec23); Task 2 was verification-only, no commit per plan.

### Phase 3 Wave 7 outputs (03-13 — STILL AVAILABLE; cmd-coupled, smoke test)

- **smoke/phase3_smoke.py (NEW, 62 lines)**: 03-13 created the Phase 3 integration smoke test (NEW `smoke/` directory) — the formal Phase 3 verification script run by a HUMAN in Windows PyMOL via `pymol -cq smoke/phase3_smoke.py` (the runtime tier the WSL agent cannot reach — AGENTS.md environment split: PyMOL 2.5.0 runs in a Windows conda env, not WSL). Setup section: `cmd.fetch("1ubq", async_=0)` (AGENTS.md domain rule for synchronous load) + capture `orig_count = cmd.count_atoms(obj)`, `orig_ids = set(cmd.identify(obj, mode=0))`, `orig_pubnames = set(cmd.get_names("public_objects"))`. Game start: `gc = game.GameController(obj); bname = gc.start(hider_specs)` where `hider_specs = [([10,10,10],'spheres'), ([11,11,11],'sticks'), ([12,12,12],'lines')]` — 3 distinct positions + 3 distinct reps so registry per-rep counts and by_rep queries are non-degenerate. Uses the REAL GameController orchestrator (NOT direct mutation.insert_hider calls — departs from the RESEARCH sec Q7 sketch which used direct module calls; the plan explicitly requires testing the real wiring: snapshot-before-mutation ordering + id-flows-insert-to-register contract are load-bearing). 4 happy-path criteria + Q4 spike: C1 (public object list unchanged + count += 3 — HIDER-01 in-place insert contract), C2 (3 sentinel atoms via `cmd.iterate(f"{obj} and segi GAME", "stored.append((id, segi, b))", space={'stored': sent})` — hygienic explicit dict, NOT space=None which pollutes pymol.__dict__; mirrors mutation.py's pattern; all segi=GAME + b=-999 within 1e-6 float tolerance), Q4 spike (orig_ids.issubset(new_ids) and len(new_ids - orig_ids)==3 — Pitfall 4 id-stable-across-add at runtime tier), C3 (registry len==3 + counts_by_rep=={spheres:1, sticks:1, lines:1, cartoon:0, ribbon:0} + by_rep spheres len 1 — criterion 3 per-rep counts), C4 happy path (gc.cleanup() returns True via `intact = gc.cleanup(); check("C4: cleanup returned True (intact)", intact is True)` — the load-bearing assertion; + count back to orig + id-set matches orig + backup discarded by cleanup). `backup.verify_intact(obj, bname) or True` line is informational only (backup already discarded by cleanup; bname gone from cmd.get_names('objects')). Imports `game` in addition to backup/mutation/registry — tests the full Phase-3 stack. `import sys` kept (unused in 03-13) as forward-compat hook for 03-14's summary block (sys.exit nonzero on any FAIL). NO summary block in 03-13 — comment marker `# NOTE: plan 03-14 extends this script with failure-path + Q1/Q2/PSE spikes and moves the summary to the end. Do NOT add the summary block here.` left for 03-14. RESULTS list populated but not consumed yet; 03-14's summary reads it. All WSL gates green (py_compile smoke + biochemeleon all + 144 tests [54 registry + 90 setup_state, no regression] + Pitfall-1/11 ZERO scoped to biochemeleon/+smoke/ + space= gate 1 + async_=0 1 + GameController 1 + C1-C4 11 matches >=7). 0 deviations — plan executed verbatim. cmd-coupled — runtime (criteria 1-4 happy path) deferred to plan 03-15 Windows PyMOL human-verify checkpoint. Single-plan Wave 7; plain git commit, staged only smoke/phase3_smoke.py (feat 8d45b28). Task 2 verification-only, no commit per plan.

### Decisions

Decisions are logged in PROJECT.md Key Decisions table. Recent decisions affecting current work:

- [Phase 3 consolidated]: 7 key Phase 3 decisions (detailed per-plan entries follow): (1) restore = `cmd.delete(target)` + `cmd.create(target, backup)` two-step, NEVER single-call `create(existing, backup)` [smoke: single-call IS REPLACE, but delete+create stays canonical]; (2) hider id via `cmd.identify(..., mode=0)` after insert, NEVER the `cmd.pseudoatom` return value [smoke: returns None]; (3) registry keys on atom `id` (stable across add/remove), NEVER `index` (fragile); (4) `reconstruct_from_sentinels` uses dependency injection — registry.py stays pure (no `from pymol`), game.py injects the iterate fn; (5) `rep=None` after `.pse` reload — sentinel carries no rep, Phase 8 `.bcm` sidecar reconciles [smoke-confirmed]; (6) `backup.snapshot` MUST precede any `mutation.insert_hider` (no undo); (7) 3-module split (registry pure / backup+mutation cmd / game orchestrator) for parallelism + WSL-testability of the pure layer.

- [05-03]: The shared (payload, rep) hider_specs contract between _on_start (producer) and GameController.start (consumer) is coordinated in ONE plan to prevent drift — the shape changes from [(pos, rep)] (Phase 4, all spheres) to [(payload, rep)] where payload is rep-specific ([x,y,z] spheres, (offset, neighbor_id) lines/sticks, (chain, terminus_resi, is_c_terminus) cartoon/ribbon); the `for (payload, rep) in hider_specs` unpacking is backward-compatible (sphere pos IS the payload). Removed the Phase-4 post-start `cmd.show("spheres", "segi GAME")` — the dispatcher (insert_hider_for_rep, 05-02) now owns per-hider showing by id (a blanket sphere show would cross-contaminate stick/cartoon hiders). _on_start pre-fetches neighbor_ids + cas_by_chain via cmd.iterate (ID uppercase, hygienic space={'stored':...}) feeding the pure generators so generators.py stays pure (no `from pymol`). Fallback to spheres via hider_count when per_rep empty (Phase 4 backward-compat). 1 Rule-3 deviation: game.py start docstring written in prose ("per-rep dispatch") instead of the literal `insert_hider_for_rep` token to keep the exact-count verify grep at 1 match (mirrors 03-06/03-09/03-10/04-04 precedent). Runtime (full mixed-rep loop) deferred to headless smoke 05-04 + human-verify 05-05.

- [05-04]: Cartoon MVP switched from C-terminus to N-terminus — the C-terminus carbonyl C carries an OXT (terminal oxygen) in 1ubq (and most structures) which saturates the C valence and makes editor.attach_amino_acid fail with "no target attachment vector found" (ObjectMolecule.cpp:3357, GetTargetValenceVector finds no free valence); the N-terminus N has a free valence, extends with NO atom removal, so the happy-path sentinel cleanup (segi GAME) restores the structure exactly and verify_intact passes (smoke-confirmed: cleanup count back to 660=orig). This is a Rule 1/3 runtime bug fix, NOT an Open Risk 1 fallback (segi didn't break the polymer — the plan's OR1 fallback was resn='HIDER' which was not needed). pick_terminal_residues now returns (chain, min_resi, is_c_terminus=False); insert_cartoon_hider already supported is_c_terminus=False. attach_amino_acid lives in pymol.editor (editor.py:85), NOT exposed as cmd.attach_amino_acid in 2.5.0 open-source (cmd.py imports editor lazily inside a fn) → lazy `from pymol import editor` inside insert_cartoon_hider. In cmd.iterate expressions with a hygienic space= dict, use resv (numeric residue value, already int) NOT int(resi) (NameError — builtins not in scope); and wrap `neighbor (sele)` in explicit outer parens before `and ...` to avoid PyMOL selector precedence swallowing the intersect. The Phase 3 segi='GAME' + b=-999 sentinel is UNCHANGED for cartoon (Open Risk 1 confirmed segi doesn't break polymer — no sentinel migration). 25/25 headless smoke ALL PASSED; 3 MEDIUM open risks RESOLVED.

- [05-09]: Ribbon rep support — `insert_cartoon_hider` now accepts `rep='cartoon'` keyword (default preserves existing cartoon behavior — no regression for callers that don't pass rep=) and the `insert_hider_for_rep` dispatcher forwards `rep=rep` to it. The `cmd.show` call is parameterized (`cmd.show(rep, ...)`, was hardcoded `cmd.show('cartoon', ...)`). A `ribbon` rep request now flows `_on_start` → `hider_specs` → `game.start` → `insert_hider_for_rep(rep='ribbon')` → `insert_cartoon_hider(rep='ribbon')` → `cmd.show('ribbon', GAME)`. NO `__init__.py` change — the (chain, terminus_resi, is_c_terminus) payload already travels with rep via `hider_specs.append((term, rep))`; the dispatcher already received rep, it just wasn't forwarding it. 1 Rule-1 smoke-check fix: the plan's `count_atoms('... and segi GAME and rep cartoon') == 0` regression guard was unsound — a freshly-fetched 1ubq polymer has cartoon shown by DEFAULT (~602 atoms), so newly-attached GAME residues INHERIT cartoon from the polymer (cartoon is NOT set by the show call). Replaced with "ribbon on GAME but NOT on the rest of the polymer" (ribbon is not a default rep, so it can only come from the explicit `cmd.show('ribbon', GAME)` — proves rep= forwarding). Headless smoke 41/41 ALL PASSED. Pattern: rep parameterization via keyword default + dispatcher forwarding; PyMOL rep-inheritance awareness for regression guards.

- [06-01]: GameController Phase-6 hint/reveal logic — _mark_found is a shared helper that marks registry status + colors green but does NOT log or fire win (callers handle those), enabling DRY across on_pick/reveal_one/reveal_all. on_counts_changed is the 4th set_callbacks param with default no-op lambda (backward-compatible with existing 3-arg callers from Phase 4 gui_game). reveal_all increments _reveal_count by len(hidden) (one per hider revealed, NOT +1 for the action). hint() does NOT call mark_found (status stays hidden — it only colors NEIGHBORS orange via byres around selection, excluding GAME atoms). Counters _reveal_count/_hint_count init zero in __init__ + reset per round in start() (DIFF-01). HINT_RADIUS=5.0 (CA-CA ~3.8 Å, captures adjacent residues), HINT_COLOR='orange' (distinct from green=found + blend colors). Mock-based testing pattern: configure cmd.count_atoms.return_value for comparison gates (MagicMock.__gt__ returns NotImplemented → TypeError on `> 0`).

- [07-01]: Phase 7 TDD foundation — build_found_selection + group_found_by_rep are MODULE-LEVEL functions in registry.py (NOT HiderRegistry methods), placed AFTER the HiderRegistry class in a new "Phase 7 found-hider selection helpers" section. They take a list of records (typically registry.all()) and return a selection string / dict. build_found_selection returns None (not "") when no found records — the GUI dropdown caller early-returns on None without issuing a malformed PyMOL selection. group_found_by_rep skips rep=None records (post-.pse reload case; same tolerance as counts_by_rep 03-10). _found_color default 'green' in GameController.__init__ preserves the legacy _mark_found behavior so existing tests asserting cmd.color('green', ...) pass UNCHANGED — the DIFF-04 override is a simple attribute assignment (gc._found_color = 'cyan' / 'found_highlight'), no API change, no migration. 200 tests pass (187 existing + 13 new). registry.py stays pure (no from pymol). TDD: RED ba09ab2 (ImportError + AttributeError), GREEN 4780db0 (all pass), REFACTOR gates-only (clean on first pass).

- [07-02]: Phase 7 GUI wiring — wizard-lifecycle fix FOLDED INTO _on_start (NOT just _on_restart): the research (07-RESEARCH.md section 2) identifies the mouse_selection_mode corruption bug for BOTH Start-mid-game AND Restart; folding the fix (deactivate old PickWizard before cleanup) into _on_start fixes both paths with one change; _on_restart is belt-and-suspenders (also deactivates, no-op if already None). Found-hider dropdown filters by HIDER_STATUS_FOUND (NOT by color) — found hiders may be recolored to any color via DIFF-04, so color is not a reliable predicate; status is the canonical found-flag. QColorDialog.getColor() static method (NOT .exec_()) runs its own modal event loop — child-dialog modal pattern (like QFileDialog/QMessageBox), does NOT violate the modeless-main rule; exec_ grep gate stays at 1. Combo uses `activated` signal (NOT `currentIndexChanged`) so the index-0 placeholder doesn't fire on construction. Buttons created in their tab module, wired in __init__.py (composition root) — same pattern as start_btn (Phase 4). 3 task commits: 971e6f0 + 8a6248e + 1b70897. 200 tests pass (no regression). Qt+cmd-coupled — runtime deferred to Plan 03 human-verify checkpoint.

- [04-05]: PluginDialog._on_start is the BTN-07 fan-in composition root tying together generators (04-01) + GameController.start (04-03) + GameTab.start_countdown (04-04); lazy import generators/game/demos INSIDE _on_start (mirrors 04-04 lazy PickWizard — avoids circular imports + module-load cost); hider_specs=[(pos,'spheres')] is the ONLY rep in Phase 4 (Phase 5 adds line/stick/cartoon); QMessageBox.warning on EVERY target-resolution failure path (no object/fetch failed/demo failed/unknown mode) + RuntimeError catch on double-start — no partial game state; cmd.show("spheres","%s and segi GAME") reveals hiders (lowercase segi selector; elem='PS' renders as spheres); self._controller held on PluginDialog for Phase 7 cleanup/restart; 2 atomic commits instead of 1 combined (better bisect granularity, plan Task 3 'commit both files together' satisfied by end state)- [04-04]: GameTab countdown via QTimer.singleShot chain (NEVER time.sleep — blocks Qt event loop + freezes PyMOL; 04-RESEARCH Q21); PickWizard imported LAZILY inside _begin_play (not at module load — wizard.py imports pymol.wizard); controller is DUCK-TYPED (passed via start_countdown, NO import of game.py in gui_game.py — avoids circular import); _on_win uses QMessageBox.information (modal child dialog — main plugin dialog stays modeless); controller._wizard set in _begin_play so GameController.win() can deactivate it (intentional cross-reference, no wizard import in controller)
- [04-02]: PickWizard do_pick uses cmd.identify("pk1", mode=1) for stable atom id (NEVER cmd.index — fragile; querying.py:1313-1317 warns against it); controller is duck-typed (no sibling imports — wizard.py standalone, avoids circular import); save/restore mouse_selection_mode (mode 0 = atomic) + prior wizard (canonical built-in pattern)
- [Roadmap]: PyMOL plugin (PyQt5 via `pymol.Qt`) is v1; VMD tcl is v2 — phased delivery
- [Roadmap]: Phase order de-risks object mutation (Phase 3) BEFORE generators, ships sphere MVP (Phase 4) ASAP
- [Roadmap]: Cartoon/ribbon generators (Phase 5) flagged as highest-risk / highest-research area
- [Roadmap]: Hider sentinels `segi='GAME'` + `b=-999` are the cleanup-safety and session-reload mechanism
- [Phase 1]: PluginDialog lives in `__init__.py`; all GUI modules use `from pymol.Qt import` (never raw PyQt5); dialog is modeless `.show()`
- [02-01]: GAME_REPS and DEMO_MANIFEST live in setup_state.py (pure layer); demos.py imports FROM it
- [02-02]: PDB filenames lowercase, package-relative under biochemeleon/data/demos/
- [02-03]: hider_count_cap imported from setup_state (NOT demos) — single source of truth for pure layer
- [02-03]: apply_state uses a _loading flag (not blockSignals) to suppress cascading signal handlers during programmatic state application — ensures Save->Load round-trips verbatim
- [02-03]: demos.py is the cmd-coupled bridge; gui_setup.py imports FROM setup_state (pure) AND demos (cmd bridge) — never the reverse
- [02-03]: load_demo imported by gui_setup per key_links API contract but not called in Phase 2 (demo loading deferred to Phase 4 Start button)
- [02-05]: PDB_POOL = 33 verified RCSB entries (curled 2026-08-05; all HEADER + ATOM + <6000 atoms); plan prose said "34" but the actual list + category breakdown (6+14+3+4+6) both sum to 33 — used the list verbatim, did NOT add an unverified 34th entry (AGENTS.md "all claims verified" rule)
- [02-05]: _validate_pdb_pool enforces 4-char alnum (PDB_POOL convention); _validate_pdb_code accepts 3-5 for general code validation (robustness for legacy/extended IDs) — **SUPERSEDED by 02-06** (tightened to exactly 4-char; the 3-5 tolerance let 5-char entries pass the code validator only to be silently dropped by the pool validator)
- [02-05]: Empty pdb_pool user input -> DEFAULTS pool (not []); an empty pool signals "use defaults" and the randomize re-roll relies on a non-empty pool internally
- [02-05]: Per-rep-sum clamp uses insertion order (dicts preserve order in 3.6+) — keeps first entries, drops overflow (deterministic, predictable)
- [02-05]: lock_source=True without locked_state -> falls back to lock_source=False (defensive; UI always passes collect_state() when checkbox checked)
- [02-06]: _validate_pdb_code tightened to EXACTLY 4-char lowercase alphanumeric (drops 3-5 tolerance) — PDB IDs are 4 chars by format; the 3-5 tolerance caused silent loss (5-char "12345" passed the code validator but was dropped by _validate_pdb_pool). _validate_pdb_pool's len==4 check kept for defense-in-depth (redundant but harmless). PDB_POOL (33 entries) UNCHANGED.
- [02-06]: Pool editor is a QListWidget (ExtendedSelection) inside a QGroupBox "Pool of PDB IDs (Randomize picks fetch codes from here)" + 4 buttons (+ Add, ✎ Edit, − Remove, Use bundled pool) — list semantics match the affordance (the QPlainTextEdit read as "might be a dropdown" but was free-text). Add/Edit validate via _validate_pdb_code BEFORE entry + QMessageBox.warning on invalid — invalid IDs never enter the list (no silent loss on Save/Load round-trip). No reorder buttons (out of scope).
- [02-06]: _randomize left UNCHANGED per the plan (it already calls self._pool_list() which now reads from the QListWidget — behavior identical). Its docstring still references "pool_edit text area" (stale, but plan-protected — "do NOT touch _randomize").
- [02-07]: "Choose random" button placed INSIDE the pool QGroupBox's button row (5th button after + Add / ✎ Edit / − Remove / Use bundled pool), NOT with the main Setup actions — per the user's enhancement spec, the button is visually associated with the pool it picks from. Empty pool falls back to list(PDB_POOL) (mirrors the 02-05 randomize_state convention that empty pool = use bundled pool; never produces an empty pdb_code box). mode_combo.setCurrentIndex(1) triggers _on_mode_changed -> target_stack.setCurrentIndex(1) only; it does NOT trigger _on_target_changed (wired to obj_combo/demo_combo, not mode_combo) so the hider-count cap recompute is NOT triggered — focused, single-purpose. import random at module level (not local); PDB_POOL import (02-06, line 22) verified, not re-added. No new test (UI-only; runtime behavior deferred to 02-04 smoke test).
- [03-02]: backup.py snapshot = delete-then-create (`cmd.delete(BACKUP_PREFIX)` to discard any stale backup, then `cmd.create(BACKUP_PREFIX, target_obj)` for a fresh independent deep copy) — unambiguous; no merge-vs-replace question (RESEARCH Q2 flagged single-call `cmd.create(existing, other)` as UNVERIFIED C-side; the snapshot direction creates a fresh name so no merge question, but deleting stale first guarantees cleanliness even if a prior game crashed without discarding). `BACKUP_PREFIX='_bchm_backup'` underscore-prefixed so it's hidden from `public_objects`. `discard` is idempotent (`cmd.delete` safe on absent objects). `restore` (03-05) + `verify_intact` (03-08) deliberately deferred — snapshot+discard subset only per plan scope.
- [03-01]: Registry keyed by `(object, id)` tuple, NOT `id` alone — future-safe for multi-target-object games (Pitfall 4 lock; ids are per-object; the tuple is the stable primary key). HiderRecord uses `__slots__=('id','object','rep','status','pos')` (compact + AttributeError on typos). OrderedDict (not plain dict) for the store — explicit insertion-order contract even on Python 3.6 (plain-dict order is an impl detail, not language-guaranteed). Int coercion on `id` at register/get/remove (str '1' ↔ int 1 round-trip; defensive for cmd.identify results + deserialized sidecar strings). `to_dict` omits `pos` when None, includes it as a list when set (compact Phase 8 .bcm sidecar). `register` raises KeyError on duplicate (caller bug); `remove` idempotent (False on absent, never raises). Core CRUD only — by_rep/counts_by_rep/mark_found + reconstruct_from_sentinels/to_dict/from_dict deferred to 03-02/03-03 per plan.
- [03-03]: insert_hider uses `cmd.pseudoatom(object=existing)` — inserts INTO the object (HIDER-01; a separate object via load/create would let the player toggle one object to win; AGENTS.md lock). id fetched via `cmd.identify(..., mode=0)` + `assert len==1`, NEVER the `cmd.pseudoatom` return value (RESEARCH Q1: unverified C status code; mode=0 returns the id list, NOT the fragile index — Pitfall 4). Sentinel `segi='GAME'`+`b=-999` set via single multi-`;` `cmd.alter` with `space={}` (editor.py:354 idiom; hygienic — no global namespace pollution, RESEARCH Q3). `cmd.sort(object)` after alter (defensive — editing.py:1457; reassigns index, preserves id). `rep` accepted but NOT used for placement (registry needs it; placement is Phase 4/5 generator work; Phase 3 places at caller-supplied pos). Scope: insert_hider only — `fetch_all_hider_ids`/`cleanup_hiders` deferred to 03-06/03-09.
- [03-03]: In concurrent Wave 1 execution (shared git repo), NEVER use `git commit --amend` without first unstaging everything and staging only your own files — a bare `--amend` absorbs whatever else is staged in the shared index (here: a parallel agent's planning docs). Safe amend pattern: `git reset HEAD` (unstage all) → `git add <my-file>` (stage only mine) → `git commit --amend` (only mine staged → no sweep). Or prefer plain `git commit` (no --amend) in concurrent contexts.
- [03-05]: backup.py restore = `cmd.delete(target_obj)` + `cmd.create(target_obj, backup_name)` two-step — NEVER single-call `cmd.create(existing, backup)` (RESEARCH section Q2 MEDIUM flag: single-call create merge-vs-replace is UNVERIFIED C-dispatched; the two-step is unambiguous — delete removes the mutated object entirely [commanding.py:496], create makes a fresh atom-for-atom copy from the backup [creating.py:960]). `try/except Exception` returns True/False (caller aborts game on False; never raises into orchestrator). Placed after `discard`, before future `verify_intact` (03-08) — mirrors RESEARCH Backup/Restore Design ordering. This is the "restore" half of criterion 4; the RESEARCH Q2 MEDIUM flag is now sidestepped at the implementation level (the smoke test validates by confirming atom-for-atom identity post-restore).
- [03-06]: mutation.py fetch_all_hider_ids uses `cmd.iterate(..., space={'stored': out})` with an EXPLICIT dict — NEVER `space=None` (RESEARCH §Q3 / editing.py:47-62: `space=None` defaults the expression to the global `pymol.__dict__`, the legacy `stored.xxx` pattern that pollutes the global namespace; the explicit dict is the editor.py:156 hygienic pattern, testable + non-polluting). This mirrors the `space={}` decision in 03-03's `insert_hider` alter call — the whole module uses explicit `space=` dicts, never the global namespace.
- [03-06]: fetch_all_hider_ids returns `(model, id)` tuples (model = object name), NOT bare `id` — future-safe for multi-object games (registry keys by `(object, id)` per the 03-01 decision). `id` is the STABLE integral identifier (read via iterate's read-only `id` symbol, editing.py:1446-1449), NEVER `index` (RESEARCH §Q4: querying.py:1315 warns indices are fragile across add/delete). This is the data shape `registry.reconstruct_from_sentinels(iterate_fn)` (plan 03-10) consumes via dependency injection — game.py injects `lambda: mutation.fetch_all_hider_ids(obj)` so registry.py stays pure (no `from pymol`).
- [03-06]: Docstring written in PROSE ("the iterate primitive", "explicit `space=` dict", "sentinel (`segi='GAME'` + `b=-999`)") to avoid literal `cmd.iterate` / `space={'stored'` / `segi GAME and b -999` false-positives on the 4 task exact-count greps (each must return exactly 1 — the body call). Mirrors the 03-03 docstring-rewording precedent (deviation 1 there) and the AGENTS.md-warned false-positive pattern (a docstring that said "from PyQt5 import" tripped the gate before).
- [03-04]: counts_by_rep zero-fills ALL 5 GAME_REPS — pre-populates `{rep:0 for rep in GAME_REPS}` before tallying so the Game tab can render "cartoon: 0" even with no cartoon hiders (criterion 3: per-rep counts). A sparse dict would force the UI to `get(rep, 0)` everywhere; the zero-fill centralizes that contract in the registry. The `out.get(r.rep, 0)` is defensive (r.rep is always a key since HiderRecord validates `rep in GAME_REPS`) — kept as the plan's verbatim sketch.
- [03-04]: mark_found raises KeyError on unregistered (object, id) — the clean error signal for the Phase 4 click handler (a click on a non-hider atom is a caller bug; surfaces immediately, no silent no-op). `int(id)` coercion matches register/get/remove (str '1' ↔ int 1 round-trip; defensive for cmd.identify results + deserialized sidecar strings). Direct `self._records[(object, int(id))]` lookup + `rec.status = HIDER_STATUS_FOUND`; KeyError propagates naturally (no try/except swallowing).
- [03-04]: Docstrings updated in GREEN (not REFACTOR) — a feature is not complete while its docs still say "deferred"; module/class "Phase 3 methods" lists now enumerate by_rep/counts_by_rep/mark_found as implemented, with reconstruct_from_sentinels + to_dict/from_dict noted as the remaining later-plan work. REFACTOR was gates-only (no commit per the plan's "commit only if changed") — the implementation followed the plan sketch verbatim and was clean on first pass.
- [03-04]: Wave 2 concurrent execution (03-04 on registry.py/tests + 03-05 on backup.py + 03-06 on mutation.py) — files disjoint; STATE.md is the only shared file (last writer wins); used plain `git commit` (never `--amend` per the 03-03 lesson), staged only tests/test_registry.py (RED) + biochemeleon/registry.py (GREEN); never touched mutation.py/backup.py/03-05-SUMMARY.md/03-06-SUMMARY.md.
- [03-07]: to_dict shape `{'version':1,'hiders':[record.to_dict() ...]}` designed in Phase 3 (not Phase 8) — the RESEARCH serialization-shape sketch (lines 228-236) is the contract; Phase 8 (Persistence) will be a thin file I/O wrapper over to_dict/from_dict. Designing the shape now + round-trip unit-testing it avoids a schema migration at Phase 8. The `version: 1` key is the forward-compat hook (Phase 8+ can bump it + branch on it in from_dict).
- [03-07]: from_dict is a `@classmethod` (not static/instance method) — idiomatic Python "named constructor" (cf. dict.fromkeys); returns a fresh HiderRegistry. Tolerant on optional keys (missing version->v1, missing status->HIDER_STATUS_HIDDEN, missing pos->None) + strict on required keys (object/id/rep raise KeyError) — tolerant on read so old/migrated sidecars still load, strict on the data model so bad data surfaces immediately.
- [03-07]: pos stored as-is from from_dict (a list from JSON, NOT converted to tuple) — HiderRecord accepts either list or tuple; list/tuple normalization is a Phase 8 boundary concern (RESEARCH serialization-shape note lines 240-241). Round-trip test asserts id/object/rep/status match (the four fields the click handler + Game tab rely on), NOT pos — asserting pos round-trip would require the deferred list/tuple normalization.
- [03-07]: Used HIDER_STATUS_HIDDEN constant (not literal 'hidden') in from_dict's `h.get('status', ...)` default — matches the plan's GREEN task spec + existing module pattern; equivalent to the RESEARCH sketch's literal 'hidden' but refactor-safe. Docstrings updated in GREEN (not REFACTOR) per the 03-04 precedent. Wave 3 concurrent execution (03-07 on registry.py/tests + 03-09 on mutation.py) — files disjoint; STATE.md shared (last writer wins); plain `git commit` (never --amend), staged only tests/test_registry.py (RED) + biochemeleon/registry.py (GREEN); observed mutation.py modified uncommitted (03-09's work), left untouched.
- [03-08]: verify_intact = count gate (cheap, early False) + atomic-tuple multiset (thorough) — the two-stage integrity check pattern from RESEARCH Backup/Restore Design lines 289-299. Count gate catches gross mismatch (wrong atom count = structure changed); tuple multiset catches any coordinate/residue/name drift. Tuple fields (resn, resi, name, chain, segi, x, y, z) are all read-only iterate symbols (editing.py:1446-1449) — covers residue identity + position. This is the proof behind criterion 4; called after every restore and cleanup in the smoke test.
- [03-08]: Exact float equality on x/y/z is safe (no epsilon) — cmd.create copies coordinates bit-for-bit (RESEARCH sec Q6 Open Risk 8). sorted() multiset compare uses == on the tuples, which is exact float equality on the coord fields. The documented fallback (RESEARCH sec Q6) is a coords-omitted tuple compare (resn, resi, name, chain, segi) if exact equality ever false-fails — NOT expected, deferred to smoke test confirmation.
- [03-08]: Inlined the two iterate calls (one per object) rather than the plan sketch's `_tuples` helper — the helper placed `space={'stored'}` in source ONCE (one iterate inside the def, invoked twice at runtime), failing the verification gate (>=2 matches at line level). Inlining produces 2 source occurrences satisfying the gate; algorithm identical. Also split the count check into named locals (target_n, backup_n) on two lines for the same reason (cmd.count_atoms >=2 matches unambiguous at line level). Both are plan-internal inconsistency resolutions (action's "follow EXACTLY" vs verification's >=2 grep counts) — NOT Rule 1-4 deviations; resolved in favor of the verification (acceptance contract).
- [03-08]: verify_intact is a pure query (no mutation), returns bool — no try/except (unlike restore). A missing target/backup surfaces as a pymol error (caller's bug, not a recoverable path). Caller (game.py Phase 4; smoke test 03-13/03-14) asserts True; in production a False triggers restore. Mirrors RESEARCH "Error handling: verify_intact is a pure query, returns bool."
- [03-08]: Wave 3 concurrent execution (03-07 registry.py serialization + 03-08 backup.py verify_intact + 03-09 mutation.py cleanup_hiders) — files disjoint; STATE.md shared (last writer wins); plain git commit (never --amend per 03-03 lesson), staged only biochemeleon/backup.py; did NOT touch sibling SUMMARY files (03-07/03-09) or their code files.

- [03-09]: cleanup_hiders uses cmd.remove (NOT cmd.delete) — remove deletes atoms FROM the object (editing.py:800), delete removes the whole object (commanding.py:496); cleanup leaves the original structure intact (criterion 4 happy path). Selector is segi GAME ONLY (the plan's verbatim code; NOT 'segi GAME and b -999' as in fetch_all_hider_ids) — hiders are the only atoms with segi=GAME, so segi GAME alone suffices. count_atoms gate (querying.py:1412) before remove — idempotent (skip on 0-atom selection); returns count so game.py asserts it matches the registry length. mutation.py now functionally complete for Phase 3 (insert_hider + fetch_all_hider_ids + cleanup_hiders).

- [03-10]: reconstruct_from_sentinels uses DEPENDENCY INJECTION (the iterate callable is a PARAMETER, not an import) — registry.py stays pure (NO `from pymol`; purity gate ZERO). game.py injects `lambda: mutation.fetch_all_hider_ids(obj)` at runtime; the registry never touches cmd. This is the inversion-of-control pattern that keeps the pure layer WSL-testable (tests inject a fake lambda).
- [03-10]: Chose option (b) — relax HiderRecord.__init__ to `if rep is not None and rep not in GAME_REPS` (allows rep=None) — over option (a) a `validate_rep=True` parameter. Simpler (single validation point); normal register() always passes a valid rep so the relaxation only affects reconstruction. rep=None is the sentinel-reconstruction value (the sentinel carries no rep — RESEARCH Open Risk 6); Phase 8 .bcm sidecar reconciles.
- [03-10]: reconstruct_from_sentinels clears records BEFORE rebuilding (overwrite, NOT append) — a reloaded game rebuilds from scratch; pinned by test_reconstruct_clears_existing. Returns self (fluent). rep=None records get status=HIDER_STATUS_HIDDEN (a reloaded game treats all sentinel survivors as unfound; .pse does not persist in-memory found-status; Phase 8 recovers it).
- [03-10]: counts_by_rep GUARDS against rep=None records (skips them via `if r.rep is None: continue`) — the returned dict has only GAME_REPS keys, never a None key. Documented limitation: rep=None records (post-reload) are invisible to rep-keyed queries until Phase 8 sidecar reconciles rep. Pinned by test_reconstruct_rep_none_then_counts_by_rep + documented in the counts_by_rep docstring.
- [03-10]: Docstring reworded in REFACTOR (not GREEN) to avoid a `from pymol` grep false positive — the reconstruct_from_sentinels docstring originally said "no `from pymol import cmd`" which tripped the purity gate; reworded to "NO `pymol` import" (matches module docstring style line 7-8). Mirrors the 03-02 precedent (backup.py docstring) + the AGENTS.md-warned false-positive pattern (a docstring that said "from PyQt5 import" tripped the gate before). Rule 3 blocking fix, no behavior change. registry.py now FUNCTIONALLY COMPLETE for Phase 3 (10 HiderRegistry methods: register/get/all/remove/by_rep/counts_by_rep/mark_found/to_dict/from_dict/reconstruct_from_sentinels).
- [03-11]: start() snapshots BEFORE the insert loop (not after, not interleaved) — PyMOL has no undo (AGENTS.md domain rule); the backup must capture the pre-mutation state so the 03-12 failure path (abort_on_error -> backup.restore) can recover atom-for-atom. The snapshot-before-mutation contract lives at the orchestration layer, not the mutation layer.
- [03-11]: start() builds a FRESH registry per round (self.registry = HiderRegistry() after snapshot) — a re-start after cleanup gets a clean registry, not stale records from a prior round.
- [03-11]: id flows insert -> identify -> register — the aid returned by mutation.insert_hider (fetched via cmd.identify mode=0 inside mutation.py, the stable id NOT the fragile index per Pitfall 4) is passed directly as id=aid to registry.register; the (object, id) key is the stable primary key (03-01 lock).
- [03-11]: _started flag prevents double-start with RuntimeError("game already started; call cleanup() first") — the cleanup() method is planned for 03-12; without the guard a double-start would snapshot the already-mutated state (losing the pre-game backup) + insert duplicate hiders.
- [03-11]: reconstruct_registry uses dependency injection — passes `lambda: mutation.fetch_all_hider_ids(self.target_obj)` into registry.HiderRegistry().reconstruct_from_sentinels(...); registry.py stays pure (no cmd import), game.py is the composition root (mirrors the 03-10 DI decision, extends it to the orchestrator).
- [03-11]: game.py imports `from pymol import cmd` even though GameController delegates to backup/mutation/registry (no direct cmd call in this plan) — documents the cmd-coupled layer; 03-12 cleanup/abort + Phase 4 click-loop wiring will use cmd directly.
- [03-11]: No docstring rewording needed (unlike 03-03/03-06/03-09/03-10) — the plan's prose docstrings ("insert_hider ->", "register.", "snapshot ->", "Rebuild the registry from sentinel atoms") avoided the dotted-literal false-positives on the 4 exact-count greps; the plan executed verbatim, 0 deviations.

- [03-12]: cleanup() returns the verify_intact result (NOT a hardcoded True) — if False, the caller (Phase 4 UI) calls abort_on_error() to restore from backup. This is the key_link: cleanup -> verify_intact -> (if False) -> abort -> restore. The verify_intact result IS the criterion-4 proof on the happy path.
- [03-12]: abort_on_error() uses backup.restore (the delete+create two-step — RESEARCH sec Q2: NEVER single-call cmd.create(existing, backup) which is UNVERIFIED C-dispatched merge-vs-replace). backup.restore already implements the two-step (03-05) + try/except returning True/False; abort_on_error() just propagates that bool.
- [03-12]: Both cleanup() and abort_on_error() reset _started=False + clear the registry (self.registry = registry.HiderRegistry()) — the round is over; a fresh start() builds a fresh registry per the 03-11 contract (start() sets self.registry = HiderRegistry() after snapshot).
- [03-12]: Both are idempotent when not started (if not self._started: return True) — defensive for UI double-clicks (a second cleanup() after the first returns True, no-op; no double-remove, no double-discard on an already-discarded backup_name).
- [03-12]: Cleanup is by-sentinel (mutation.cleanup_hiders selects segi GAME) — robust against registry loss on a .pse reload (RESEARCH sec Q4); the registry is the source of truth for what was added, but cleanup itself does not depend on the registry.
- [03-12]: cleanup() assigns `removed = mutation.cleanup_hiders(...)` (the before-count of sentinel atoms) but does not assert it against the registry length in Phase 3 — the variable is retained for debugging + the future smoke-test assertion (03-13/03-14); asserting here would require the registry intact, contradicting the by-sentinel robustness goal.
- [03-12]: No docstring rewording needed (unlike 03-03/03-06/03-09/03-10) — the plan's prose docstrings ("discard backup.", "restore from backup", "verify_intact check", "cleanup() returned False") avoided the dotted-literal false-positives on the 4 exact-count greps; the plan executed verbatim, 0 deviations. The orchestration gate counted 15 = 14 real dotted module calls + 1 docstring sentence-period ("discard backup." on line 44) — a benign false positive on a minimum-bound (>=6) gate, not an exact-count gate.

- [03-13]: smoke/phase3_smoke.py uses `game.GameController(obj).start(hider_specs)` as the entry point (NOT direct `mutation.insert_hider` calls) — the plan explicitly requires testing the real orchestrator wiring (snapshot-before-mutation ordering + id-flows-insert-to-register contract), so 03-13 departs from the RESEARCH sec Q7 sketch which used direct module calls. The orchestrator's wiring is the load-bearing assertion; direct module calls would not test it.
- [03-13]: Left a comment marker (`# NOTE: plan 03-14 extends this script with failure-path + Q1/Q2/PSE spikes and moves the summary to the end. Do NOT add the summary block here.`) for plan 03-14's extension — do NOT add the summary block in 03-13. The script's RESULTS list is populated but not consumed yet; 03-14's summary reads it and `sys.exit(nonzero)` on any FAIL. `import sys` kept (unused in 03-13) as the forward-compat hook for that.
- [03-13]: hider_specs = [([10,10,10],'spheres'), ([11,11,11],'sticks'), ([12,12,12],'lines')] — 3 distinct positions + 3 distinct reps so the registry's per-rep counts (spheres:1, sticks:1, lines:1, cartoon:0, ribbon:0) and by_rep queries are non-degenerate (a single rep would make counts_by_rep trivially {rep:3, others:0} and by_rep trivially len 3 — neither tests the per-rep dimension).
- [03-13]: C4 `backup.verify_intact(obj, bname) or True` check is informational only — gc.cleanup() already discarded the backup, so bname is gone from cmd.get_names('objects'). The earlier `intact = gc.cleanup(); check("C4: cleanup returned True (intact)", intact is True)` is the load-bearing assertion; the 'backup discarded by cleanup' check (bname not in cmd.get_names('objects')) is the real post-cleanup assertion. The `or True` short-circuits the verify_intact call (which would fail on a missing backup) — the line documents the API shape without asserting it post-discard.
- [03-13]: WSL gate suite scope EXTENDED to include `smoke/` — Pitfall-1 (Tkinter/Pmw/PyQt5) and Pitfall-11 (`.exec_()`) grep gates now scan `biochemeleon/` + `smoke/`. The smoke script has no Qt/Tk/Pmw tokens and no `.exec_()` call (it's a CLI script run by `pymol -cq`, no GUI dialog). Both gates ZERO.
- [03-14]: Failure path uses `GameController.abort_on_error()` (the REAL orchestrator restore wiring), NOT direct `backup.restore` calls — exercises the full snapshot->restore->discard wiring end-to-end (criterion 4 alternate). The pre-restore count+1 check + count-back-to-orig check prove the restore brought the target back atom-for-atom; the `verify_intact ... or True` check is informational (backup already discarded by abort).
- [03-14]: Q1/Q2 spikes are INFORMATIONAL — Q1 PRINTS the cmd.pseudoatom return value (RESEARCH sec Q1: code never relies on it; this just records what it is for the SUMMARY); Q2 asserts single-call create(existing, src) is REPLACE (n_after == n_before), but a FAIL (append/double) CONFIRMS the delete+create recommendation already implemented in 03-05/03-12 — recorded in SUMMARY for the 03-15 checkpoint human to triage, treated as informational pass.
- [03-14]: PSE spike uses `gc3.reconstruct_registry()` (the DI method from 03-11) — proves sentinel-survival (len==1) + id-stability (pse_sent == [saved_id]) + registry reconstruction (len==1) with rep=None (the documented limitation; Phase 8 .bcm sidecar reconciles rep). mutation.cleanup_hiders cleans up at the end.
- [03-14]: `/tmp/phase3_test.pse` path follows the plan verbatim — Windows PyMOL resolves `/tmp` differently (the checkpoint human runs this); if the human's Windows PyMOL cannot write `/tmp`, that is a checkpoint triage item, NOT a plan failure. All iterate calls use `space={'stored': ...}` (hygienic explicit dict, never `space=None` which pollutes `pymol.__dict__`; mirrors the mutation.py pattern from 03-06).
- [03-14]: Summary block reads RESULTS (accumulated by ALL check() calls across the whole script — C1-C4 + failure path + Q2 + PSE), prints N/M passed, `sys.exit(1)` on any failure, ALL PASSED on success — the single exit code signals pass/fail to the plan 03-15 human-verify checkpoint. The smoke script is COMPLETE after this plan (setup + happy path + failure path + 3 spikes + summary); 0 deviations — plan executed verbatim.

- [03-15]: The 03-15 checkpoint was CLOSED via HEADLESS PyMOL from WSL (NOT a human GUI session) — `cmd.exe /c C:\\src\\run-conda-pymol.bat -cq smoke\\phase3_smoke.py` from the staged tmp/bioCHEMeleon/ path (AGENTS.md documents this method now, commit b320316). The smoke is pure pymol.cmd.* (no Qt/GUI), so it runs headlessly; exit 0 = clean. Qt/GUI paths (PluginDialog, Setup tab, Phase 4 click loop) STILL need a human in a real PyMOL session. This closes the WSL/Windows runtime gap for cmd-only scripts and is the pattern for future cmd-only smoke tests.
- [03-15]: b-factor sentinel SELECTOR is a COMPARISON (`b < 0`), NEVER an exact match (`b -999`) — PyMOL has no exact-match b-factor selector; `b -999` emits "Selector-Error: Malformed selection" and SILENTLY matches nothing (no exception — dangerous: returns [] rather than crashing). `b < 0` is valid comparison syntax and matches -999.0. The sentinel VALUE stays -999 (set in insert_hider + cleanup docstrings); only the SELECTOR uses the comparison form. The 1-line fix in mutation.py:113 (commit 6a15a29) unblocked the 2 PSE-reconstruct checks (fetch_all_hider_ids now returns [('1ubq',662)]).
- [03-15]: `cmd.iterate` exposes atom `id` as uppercase `ID` (NOT lowercase `id` — the Python builtin) — the 03-RESEARCH symbol table was authoritative (ID uppercase); code sketches that lowercase it were transcription errors. ALL iterate expressions use `ID` uppercase now (commit e38cff7). 03-16 should add an AGENTS rule + grep gate.
- [03-15]: `cmd.iterate` does NOT expose x/y/z coordinates (state-dependent; need `cmd.iterate_state`) — verify_intact identity tuple is (resn, resi, name, chain, segi) WITHOUT coords (commit 5ed6a13). Count gate + identity multiset is sufficient per RESEARCH Q6 fallback (cmd.create copies coords bit-for-bit, so count+identity match implies structural identity).
- [03-15]: The smoke must NOT re-invoke cmd-coupled helpers on objects the orchestrator already discarded — gc.cleanup()/abort_on_error() own the backup lifecycle (verify_intact + discard internally); the smoke asserts the orchestrator's RETURN value (intact is True / ok is True), not re-derive it. Two redundant verify_intact re-calls on discarded backups removed (lines 57 + 66, commits 9e40e8a + 9b00657). Windows PyMOL cannot resolve WSL/Linux paths (/tmp) — use relative paths (resolve against the cmd.exe cwd) or Windows-style paths (commit 039bc6a).
- [03-15]: Spike findings (RUNTIME-CONFIRMED, feed 03-16/03-17/03-19): Q1 cmd.pseudoatom return value = None (type NoneType) — confirms RESEARCH §Q1 (never rely on it; code uses cmd.identify mode=0). Q2 single-call cmd.create(existing, src) IS REPLACE (n_after == n_before) — RESEARCH §Q2 MEDIUM flag RESOLVED (no doubling; delete+create in 03-05/03-12 correct/unambiguous). PSE: sentinel survives reload (len pse_sent==1, segi=GAME survives), id stable (pse_sent==[saved_id] — Pitfall 4 holds at runtime), b=-999.0 preserved, reconstruct_registry works after the b<0 fix (1 record, rep=None — RESEARCH Open Risk 6 confirmed; Phase 8 .bcm sidecar reconciles rep).

### Pending Todos

None yet.

### Blockers/Concerns

- [Phase 5]: Cartoon/ribbon hider geometry is genuinely novel (no reference plugin) — likely to need a research spike and is the most likely phase to slip into a sub-phase or v1.x
- [05-04]: Cartoon MVP uses the N-terminus (NOT the C-terminus) because the C-terminus carbonyl C carries an OXT (terminal oxygen) in 1ubq + most structures, which saturates the C valence and blocks the residue-attach primitive ("no target attachment vector found"). The smoke only tested 1ubq (free N-terminus). A bundled demo with an N-terminal cap (ACE/formyl) or a non-standard N-terminus may fail the N-term attach — the 05-05 human-verify must confirm the N-terminal cartoon extension blends on a real demo. The C-terminus path is still supported by insert_cartoon_hider (is_c_terminus=True) for OXT-free structures but not exercised by the MVP generator. Open Risk 1 (segi breaks polymer) RESOLVED — no sentinel migration needed.
- [05-09]: Ribbon rep gap CLOSED. PyMOL rep-inheritance finding (load-bearing for future regression guards): a freshly-fetched protein (e.g. 1ubq) has `cartoon` shown by DEFAULT on its polymer (~602 atoms) but NOT `ribbon`. Newly-attached polymer residues INHERIT the cartoon rep from the polymer. So `count_atoms('obj and segi GAME and rep cartoon')` is nonzero for a cartoon-geometry hider REGARDLESS of which rep the explicit show call used — cartoon comes from inheritance. A regression guard asserting `rep cartoon == 0` on GAME atoms is unsound. The correct way to verify a rep= forwarding fix: assert the requested rep is on GAME atoms but NOT on the rest of the polymer (ribbon is not a default rep, so it can only come from the explicit `cmd.show('ribbon', GAME)`). Generalizes: when asserting on `rep <X>` counts for newly-attached polymer residues, account for the polymer's DEFAULT reps.
- [Phase 9]: MemProtMD was unreachable at research time — per-entry license MUST be verified before bundling membrane coordinates
- [Cross-phase]: ~~PyMOL Open Source has NO undo~~ — RESOLVED 2026-08-06: Phase 3 established `backup.snapshot/restore/discard/verify_intact` (`backup.py`, smoke-verified 24/24). Every destructive op now has a snapshot + restore-on-failure; later phases reuse `backup.py`. (Historical: undocontext is a no-op stub; editor.py:25-36.)
- [Phase 8]: rep is NOT recoverable from sentinels after `.pse` reload — `reconstruct_from_sentinels` sets `rep=None` (runtime-confirmed 2026-08-06; RESEARCH Open Risk 6). Phase 8 `.bcm` sidecar MUST reconcile `rep` so the reloaded registry's per-rep counts/queries work.
- [02-04]: The `rep <name>` selector and the hider-count cap are WSL-unverifiable (need Windows PyMOL); the 02-04 smoke test is the formal confirmation (research 12.1 mitigation: per-rep try/except degrades gracefully)
- [02-05]: All 4 smoke-test gaps closed at WSL tier (pure tests + py_compile + grep gates); the cap recompute (Gap 1) and per-rep max bounding (Gap 2 UI) call cmd.count_atoms / QSpinBox.setMaximum at runtime — only the 02-04 smoke test re-run can confirm them. ROADMAP.md NOT updated (Phase 2 not complete until smoke test approved).
- [02-06]: Both pool-editor UX issues closed at WSL tier (90 tests + py_compile + grep gates); the QListWidget populate-on-first-show, Add/Edit/Remove/Use-bundled-pool button behavior, and QMessageBox on invalid input are WSL-unverifiable (need a live PyMOL Qt session) — only the 02-04 smoke test re-run can confirm them. ROADMAP.md NOT updated (Phase 2 not complete until smoke test re-approved after this fix).
- [02-07]: The "Choose random" button gap closure closed at WSL tier (90 tests + py_compile + grep gates + git diff stats confirming setup_state.py/tests/other modules/ROADMAP.md untouched); the button's live-Qt behavior (appears in the pool group; clicking it switches to fetch mode + populates the pdb_code field with a random pool entry; does not change any other field) is WSL-unverifiable — only the 02-04 smoke test re-run can confirm it. ROADMAP.md NOT updated (Phase 2 not complete until smoke test re-approved after this enhancement).
- [03-02]: backup.py is cmd-coupled — `cmd.delete`/`cmd.create` behavior (deep-copy independence, idempotent delete on absent objects, underscore-prefix privacy in `public_objects`) is WSL-unverifiable (no PyMOL in WSL; py_compile is syntax-only). Only the Phase 3 smoke test (plans 03-13/03-14, run via 03-15 Windows PyMOL checkpoint) can confirm runtime behavior. The RESEARCH Q2 MEDIUM flag (cmd.create merge-vs-replace) does NOT affect snapshot (fresh-name create) — it only affects the restore path (plan 03-05, which uses delete+create to sidestep it).
- [03-01]: registry.py is PURE and FULLY verified at WSL tier (no runtime/PyMOL behavior to defer — 33 unit tests + py_compile + purity + Pitfall-1/11 gates all green). No blocker. One in-task fix: walrus `:=` is Python 3.8+; python3.6 is 3.6.9 — reaffirms the AGENTS.md constraint to avoid 3.7+ syntax (walrus, f-string `=`, positional-only params). Parallel Wave 1 execution (03-01/03-02 observed committing concurrently; 03-03 may also be in flight) — files are disjoint across the three Wave-1 tracks (registry.py / backup.py / mutation.py), so no merge conflict; STATE.md is the only shared file (last writer wins; phase not "complete" until all 20 plans summarize).
- [03-03]: mutation.py is cmd-coupled — `cmd.pseudoatom` (in-place append into existing object), `cmd.alter` (sentinel set), `cmd.sort` (index reassign), `cmd.identify` (id fetch; mode=0 = id list not index) behavior is WSL-unverifiable (no PyMOL in WSL; py_compile is syntax-only). Only the Phase 3 smoke test (plans 03-13/03-14, run in Windows PyMOL via plan 03-15 checkpoint) can confirm runtime behavior. The smoke test asserts: object list unchanged (C1), count += N, N sentinel atoms segi=GAME b=-999 (C2), existing ids stable across insert (Q4 spike), identify-returns-exactly-one-id. The RESEARCH Q1 spike prints the pseudoatom return value (NOT relied upon). One concurrent-execution git collision occurred (my --amend swept in 03-02's staged planning docs from the shared index) — fully recovered via safe-amend-pattern + reflog restore of the orphaned docs commits (7a5ce29→5ca3dc3, 975c155→08e39b4); no net data loss. Lesson logged as a decision: in concurrent execution, never bare-amend without unstaging-all + staging-only-mine first.
- [03-05]: backup.py restore is cmd-coupled — `cmd.delete`/`cmd.create` two-step behavior (atom-for-atom identity of the restored target, `try/except` returning False rather than raising on a missing backup or target) is WSL-unverifiable (no PyMOL in WSL; py_compile is syntax-only). Only the Phase 3 smoke test (plans 03-13/03-14, run via 03-15 Windows PyMOL checkpoint) can confirm runtime behavior. The smoke test asserts: after restore, the target's atom count + structure match the pre-game backup exactly (criterion 4); restore returns False (not raises) when the backup is missing. The RESEARCH Q2 MEDIUM flag (merge-vs-replace) is sidestepped at the implementation level (delete+create two-step) — the smoke test validates the sidestep. Wave 2 concurrent execution (03-04 on registry.py/tests + 03-05 on backup.py + 03-06 on mutation.py) — files disjoint; STATE.md is the only shared file (last writer wins); used plain `git commit` (never `--amend` per the 03-03 lesson), staged only biochemeleon/backup.py.
- [03-06]: mutation.py fetch_all_hider_ids is cmd-coupled — `cmd.iterate` sentinel read (the `model`/`id` read-only symbols, the `space=` hygiene dict, the selector `segi GAME and b -999` matching sentinel atoms) is WSL-unverifiable (no PyMOL in WSL; py_compile is syntax-only, the 90 unit tests exercise only the pure layer). Only the Phase 3 smoke test (plans 03-13/03-14, run via 03-15 Windows PyMOL checkpoint) can confirm runtime behavior. The smoke test's Q4 id-stability spike + `.pse` round-trip section both call `fetch_all_hider_ids` to read ids after insert / reload — the sentinel-survival check is the load-bearing one (HIGH confidence the sentinel survives — it's just atom properties; if ids shift on `.pse` reload, the registry rebuilds from sentinels via this fn + recovers `rep` from the Phase 8 `.bcm` sidecar). Wave 2 concurrent execution (03-04 + 03-05 + 03-06) — files disjoint; STATE.md is the only shared file (last writer wins); used plain `git commit` (never `--amend` per the 03-03 lesson), staged only biochemeleon/mutation.py.
- [03-04]: registry.py query layer is PURE and FULLY verified at WSL tier (no runtime/PyMOL behavior to defer — 40 unit tests [33 core + 7 queries] + py_compile + purity `from pymol`=0 in registry.py + Pitfall-1=0 + Pitfall-11 `.exec_()`=0). No blocker. by_rep/counts_by_rep ready for the Phase 4 Game tab (remaining-hiders-per-rep + per-rep counts); mark_found ready for the Phase 4 click handler (KeyError-on-unregistered is the error signal). Remaining registry work: reconstruct_from_sentinels(iterate_fn) [DI — game.py injects `lambda: mutation.fetch_all_hider_ids(obj)`; registry.py stays pure] + to_dict/from_dict (Phase 8 `.bcm` sidecar) — later Phase 3 plan. Wave 2 concurrent execution (03-04/03-05/03-06) — files disjoint; STATE.md shared (last writer wins); staged only my files (tests/test_registry.py + biochemeleon/registry.py); never --amend.
- [03-07]: registry.py serialization layer is PURE and FULLY verified at WSL tier (no runtime/PyMOL behavior to defer — 46 unit tests [33 core + 7 queries + 6 serialize] + py_compile + purity `from pymol`=0 in registry.py + Pitfall-1=0 + Pitfall-11 `.exec_()`=0). No blocker. to_dict/from_dict ready for Phase 8 (Persistence) — the `.bcm` sidecar writer/reader will be a thin file I/O wrapper (json.dump(registry.to_dict()) / HiderRegistry.from_dict(json.load())); no schema work needed at Phase 8 (shape pinned here). Remaining registry work: reconstruct_from_sentinels(iterate_fn) [DI — .pse reload sentinel rebuild; rep unknown post-reload, recovered from the Phase 8 sidecar]. Phase 8 boundary concerns deferred (known, tracked in RESEARCH): (1) pos list/tuple normalization at the JSON boundary; (2) version bump + branching in from_dict if the schema evolves; (3) rep=None tolerance in reconstruct_from_sentinels. Wave 3 concurrent execution (03-07/03-09) — files disjoint; STATE.md shared (last writer wins); staged only my files (tests/test_registry.py + biochemeleon/registry.py); never --amend.
- [03-08]: backup.py verify_intact is cmd-coupled — `cmd.count_atoms` (count gate), `cmd.iterate` with `space={'stored'}` (tuple build), and `sorted()` multiset compare are WSL-unverifiable at runtime (no PyMOL in WSL; py_compile is syntax-only, the unit tests exercise only the pure layer). Only the Phase 3 smoke test (plans 03-13/03-14, run via 03-15 Windows PyMOL checkpoint) can confirm runtime behavior: (a) count gate catches gross mismatch, (b) tuple multiset catches coordinate/residue/name drift, (c) exact float equality holds for cmd.create copies (RESEARCH sec Q6 Open Risk 8 — fallback coords-omitted compare documented if it false-fails, NOT expected), (d) `space={'stored'}` hygiene dict populates the list without polluting globals. The smoke test asserts verify_intact returns True after both the cleanup-happy-path AND the restore-failure-path, closing criterion 4 at the runtime tier. backup.py lifecycle now complete (snapshot 03-02 + restore 03-05 + discard 03-02 + verify_intact 03-08). Wave 3 concurrent execution (03-07/03-08/03-09) — files disjoint; STATE.md shared (last writer wins); staged only biochemeleon/backup.py; never --amend.

- [03-09]: cleanup_hiders is cmd-coupled — cmd.remove (atoms-from-object, NOT the whole object) + cmd.count_atoms behavior is WSL-unverifiable (no PyMOL in WSL; py_compile is syntax-only). Only the Phase 3 smoke test (plans 03-13/03-14, run via 03-15 Windows PyMOL checkpoint) can confirm runtime behavior. The smoke test's C4 happy-path section calls cleanup_hiders and asserts the post-cleanup count + structure match the pre-game backup exactly.

- [03-10]: registry.py reconstruct_from_sentinels is PURE and FULLY verified at WSL tier (no runtime/PyMOL behavior to defer — 54 unit tests [33 core + 7 queries + 6 serialize + 5 reconstruct + 3 edge] + py_compile + purity `from pymol`=0 in registry.py + Pitfall-1=0 + Pitfall-11 `.exec_()`=0). No blocker for the pure layer. The INJECTED iterate callable (game.py's `lambda: mutation.fetch_all_hider_ids(obj)`) is cmd-coupled — the actual post-`.pse`-reload reconstruction (sentinel atoms survive the reload, ids may shift, the iterate returns `(model, id)` tuples, reconstruct rebuilds the registry with rep=None) is WSL-unverifiable (tests inject a fake lambda; no PyMOL in WSL). Only the Phase 3 smoke test (plans 03-13/03-14, run via 03-15 Windows PyMOL checkpoint) Q4 `.pse`-reload section can confirm runtime behavior. registry.py is now FUNCTIONALLY COMPLETE for Phase 3 (10 HiderRegistry methods); the remaining Phase 3 work is game.py orchestration (03-11/03-12 — injects the iterate fn + wires backup/mutation/registry) + the smoke test (03-13/03-14/03-15). Documented limitation carried forward: counts_by_rep() shows only GAME_REPS keys; rep=None records (post-reload) are invisible until Phase 8 .bcm sidecar reconciles rep. Single-plan Wave 4 (no concurrent agents) — plain git commit, staged only tests/test_registry.py (RED) + biochemeleon/registry.py (GREEN + REFACTOR).
- [03-11]: game.py is cmd-coupled — the runtime behavior of GameController (snapshot-before-mutation ordering, insert -> identify -> register id flow, _started double-start RuntimeError guard, DI reconstruct_registry after .pse reload) is WSL-unverifiable (no PyMOL in WSL; py_compile is syntax-only, the 144 unit tests exercise only the pure layer). Only the Phase 3 smoke test (plans 03-13/03-14, run via 03-15 Windows PyMOL checkpoint) can confirm runtime behavior: start() snapshots before insert, the object list is unchanged after insert (C1), count += N, N sentinel atoms segi=GAME b=-999 (C2), ids stable across insert (Q4 spike), reconstruct_registry rebuilds the registry from sentinels after .pse reload. No blocker for the WSL tier — all WSL gates green (py_compile all + 144 tests + Pitfall-1/11 zero + orchestration gate 7 >= 3 + 4 exact-count greps each =1). The remaining Phase 3 work is game.py 03-12 (cleanup + abort_on_error — consumes the _started flag + _backup_name this plan sets) + the smoke test (03-13/03-14/03-15). Single-plan Wave 5 (no concurrent agents) — plain git commit, staged only biochemeleon/game.py (feat f8606ab); 0 deviations.
- [03-12]: game.py is cmd-coupled — the runtime behavior of GameController cleanup()/abort_on_error() (cleanup happy-path sentinel-remove + verify_intact, abort failure-path restore, both idempotent-when-not-started, both reset _started + registry) is WSL-unverifiable (no PyMOL in WSL; py_compile is syntax-only, the 144 unit tests exercise only the pure layer). Only the Phase 3 smoke test (plans 03-13/03-14, run via 03-15 Windows PyMOL checkpoint) can confirm runtime behavior: cleanup_hiders removes N sentinels + verify_intact returns True (criterion 4 happy path); restore brings the target back atom-for-atom + verify_intact returns True (criterion 4 failure path); both reset _started so a new round can start. No blocker for the WSL tier — all WSL gates green (py_compile all + 144 tests + Pitfall-1/11 zero + completeness gate 5 + orchestration gate 15 >= 6 + 4 exact-count greps: mutation.cleanup_hiders=1, backup.verify_intact=1, backup.restore=1, backup.discard=2). game.py is now FUNCTIONALLY COMPLETE for Phase 3 (5 methods: __init__/start/reconstruct_registry/cleanup/abort_on_error); the remaining Phase 3 work is the smoke test (03-13/03-14/03-15). Single-plan Wave 6 (no concurrent agents) — plain git commit, staged only biochemeleon/game.py (feat 14cec23); Task 2 verification-only, no commit.

- [03-13]: smoke/phase3_smoke.py is cmd-coupled — the runtime behavior of the script (cmd.fetch async_=0 sync load, cmd.count_atoms/identify/get_names captures, game.GameController.start snapshot+insert+register wiring, cmd.iterate space={'stored': sent} sentinel read, registry.all/counts_by_rep/by_rep queries, gc.cleanup verify_intact+discard round-trip) is WSL-unverifiable (no PyMOL in WSL; py_compile is syntax-only, the 144 unit tests exercise only the pure layer). Only the plan 03-15 Windows PyMOL human-verify checkpoint can confirm runtime behavior: criteria 1-4 happy path all PASS at the runtime tier (object-unchanged, 3 sentinels, registry queries, cleanup round-trip). The script is the FORMAL Phase 3 verification — its RESULTS list (populated but not consumed in 03-13) will be read by 03-14's summary block which sys.exit(nonzero) on any FAIL. No blocker for the WSL tier — all WSL gates green (py_compile smoke + biochemeleon all + 144 tests + Pitfall-1/11 ZERO scoped to biochemeleon/+smoke/ + space= gate 1 + async_=0 1 + GameController 1 + C1-C4 11 matches >=7). The remaining Phase 3 work is plan 03-14 (extend script with failure-path + Q1/Q2/PSE spikes + summary block) + plan 03-15 (Windows PyMOL human-verify checkpoint). Single-plan Wave 7 (no concurrent agents) — plain git commit, staged only smoke/phase3_smoke.py (feat 8d45b28); Task 2 verification-only, no commit per plan.

- [03-14]: smoke/phase3_smoke.py is COMPLETE but cmd-coupled — the runtime behavior of the failure path (gc2.abort_on_error restore -> count-back-to-orig), the Q2 spike (single-call cmd.create merge-vs-replace), the Q1 spike (cmd.pseudoatom return value), and the PSE spike (cmd.save/delete/load .pse round-trip + sentinel survival + id stability + reconstruct_registry with rep=None + cleanup_hiders) is WSL-unverifiable (no PyMOL in WSL; py_compile is syntax-only, the 144 unit tests exercise only the pure layer). Only the plan 03-15 Windows PyMOL human-verify checkpoint can confirm runtime behavior: failure-path abort returns True + count-back-to-orig (criterion 4 alternate); Q2 spike records the C-dispatched create merge-vs-replace behavior (FAIL = append confirms the delete+create recommendation); Q1 spike prints the pseudoatom return value (informational — RESEARCH says never rely on it); PSE spike confirms sentinel survival + id stability + registry reconstruction (rep=None documented limitation). The Q1/Q2 spikes are INFORMATIONAL — their runtime results fold into the RESEARCH findings for the 03-15 checkpoint human to triage. The `/tmp/phase3_test.pse` path follows the plan verbatim; Windows PyMOL resolves `/tmp` differently — if the human's Windows PyMOL cannot write `/tmp`, that is a checkpoint triage item, NOT a plan failure. No blocker for the WSL tier — all WSL gates green (py_compile all [smoke+biochemeleon] + 144 tests [no regression] + Pitfall-1/11 ZERO scoped to biochemeleon/+smoke/ + completeness 20 [>=10] + async_=0 1 + space= 2 + abort_on_error 1 + reconstruct_registry 1 + ALL PASSED/sys.exit 2). The smoke script is COMPLETE (setup + C1-C4 happy path + failure path + 3 spikes + summary); the remaining Phase 3 work is plan 03-15 (Windows PyMOL human-verify checkpoint — runs the complete script) + plans 03-16..03-20. Single-plan Wave 8 (no concurrent agents) — plain git commit, staged only smoke/phase3_smoke.py (feat 476e74b); Task 2 verification-only, no commit per plan.

- [03-16]: AGENTS.md "Phase 3 mutation-safety rules" subsection (14 rules) + Architecture diagram updated (registry.py/backup.py/mutation.py/game.py [GameController]) — all 03-15 runtime discoveries encoded as high-signal notes for phases 4-10. Rules: restore=delete+create two-step (never single-call; Q2 smoke=REPLACE), hider id via cmd.identify(mode=0) (never pseudoatom return; Q1 smoke=None), cmd.iterate UPPERCASE ID (not lowercase id), registry keys on atom id (never index; PSE smoke id-stable), reconstruct_from_sentinels DI (registry stays pure), rep not recoverable after .pse reload (Phase 8 sidecar reconciles; smoke rep=None), backup.snapshot precedes mutation (no undo), no re-verify on discarded backup (CmdException), space={'stored'} (never space=None), cmd.iterate no x/y/z coords (count+identity suffices), b-factor selector b<0 (never b -999; malformed silent no-match), cleanup_hiders by segi GAME alone, cmd.sort after alter of segi/chain, architecture (registry pure <- backup/mutation cmd <- game orchestrator). Pitfall-1 gate clean (no new Tkinter/Pmw/PyQt5 tokens; additions scoped to AGENTS.md). Subsection placed at END of "Domain rules" (clean markdown — keeps existing bulleted list intact; first rule references the sentinel, preserving the plan's thematic link). Task commit 9865907 (AGENTS.md only); docs commit stages SUMMARY + STATE.

- [03-19]: 03-VERIFICATION.md is the formal Phase 3 verification artifact — 338 lines mapping all 4 success criteria to (a) specific smoke-test checks by name, (b) actual PASS/FAIL from the 03-15 headless run (24/24 ALL PASSED), (c) spike findings confirming underlying behavior, (d) WSL gate results from 03-18, (e) artifacts that deliver it. Requirements traceability: HIDER-01→Criterion 1, HIDER-02→Criterion 2, HIDER-06→Criterion 2+3. 3 spikes resolved (Q1=None/NoneType informational, Q2=REPLACE delete+create canonical, PSE sentinel-survives load-bearing + id-stable + b=-999.0 preserved + reconstruct works + rep=None). 6 runtime discoveries categorized as library bugs (iterate ID-uppercase, iterate no-coords, b-factor b<0 selector — PyMOL API contracts for all future cmd code) vs smoke-test bugs (redundant verify_intact x2, /tmp path — smoke-script hygiene for future smoke tests). 12-gate WSL regression ALL GREEN tabulated + 7 confirmation gates + optional headless smoke re-confirmation. Status PASSED. The single auditable artifact a reviewer (or Phase 4 planner) reads to confirm Phase 3 is done — no need to re-read 20 plan SUMMARYs. Task commit 6839f19 (03-VERIFICATION.md only); docs commit stages SUMMARY + STATE.

- [03-20]: 03-SUMMARY.md is the phase-level handoff to Phase 4 — 227 lines distilling 20 plans + 03-VERIFICATION.md + 03-RESEARCH.md into the single artifact a Phase 4 planner reads. Structure: dependency-graph frontmatter (requires Phase 1+2 / provides 4 modules + tests + smoke + VERIFICATION / affects Phase 4-8) → What Shipped (4 modules with line counts + method lists + 144 unit tests + 24-check smoke) → What's Proven (4 success criteria 24/24 ALL PASSED + 12-gate WSL regression green) → 3 Spikes Resolved (runtime-confirmed values) → Architecture Decision (3-module split + DI + dependency diagram) → 6 Runtime Discoveries (library-bug vs smoke-test-bug tables with commit hashes) → 9 Residual Risks for Phase 4 (5 gaps to fill: placeholder insert / click handler / UI wiring / rep=None / multi-object; + 4 PyMOL API contracts to respect: b<0 selector / ID uppercase / iterate no x/y/z / headless PyMOL method) → Phase 4 Readiness table (foundation piece → proven by → Phase 4 uses it for) → Next (Phase 4 scope + required reading list). Points to 03-VERIFICATION.md for criterion-by-criterion evidence rather than duplicating it. Used actual measured line counts (registry 272 / backup 85 / mutation 148 / game 69 / smoke 112). Task commit c692efc (03-SUMMARY.md only); docs commit stages 03-20-SUMMARY + STATE + ROADMAP. Phase 3 declared COMPLETE (20/20).

## Session Continuity

Last session: 2026-08-11 (Plan 07-03 Task 1 executed — Phase 7 headless smoke, ~3min. Task 1 e51bd0d: smoke/phase7_smoke.py — 181 lines, pure pymol.cmd.* [NO Qt, modeled on phase6_smoke.py], 38 checks across 7 sections [setup, start+show 3 sphere hiders, found-mgmt hide/show via build_found_selection + group_found_by_rep, found-mgmt recolor+color via cmd.set_color + cmd.iterate color index CHANGED, _found_color threading at runtime — gc_thread._found_color='cyan' → _mark_found → color index 5387→5 [cyan NOT default green; runtime confirmation of Plan 01 TDD change that was unit-tested with mocked cmd], restart round-trip start→cleanup→start→cleanup, cleanup explicit path + idempotent]. Headless run via cmd.exe /c run-conda-pymol.bat -cq: 38/38 ALL PASSED first run, exit 0. NO runtime bugs discovered in Plan 02 GUI wiring's underlying cmd paths — no fix[07-03] commits needed. All WSL gates green [py_compile all + 200 tests no regression + Pitfall-1=0 + exec_=1 unchanged]. 0 deviations. Task 2 [checkpoint:human-verify] PENDING — awaits user verification of all 4 Phase-7 success criteria [C1 found-hider dropdown GAME-08 hide/show/recolor, C2 color picker DIFF-04 sets new color + auto-recolors + new finds use new color, C3 Restart GAME-10 fresh round + mouse mode intact, C4 Cleanup BTN-06 restore + wizard deactivated + UI reset + controller released] + the wizard-lifecycle fix [mouse_selection_mode restored after BOTH Restart AND Cleanup] in a real Windows PyMOL GUI session. The continuation agent [spawned after the checkpoint resolves] will create 07-03-SUMMARY.md + final docs commit. Plan 01 + 02 shipped the pure helpers + GUI wiring; Plan 03 ships the runtime verification.)
Stopped at: Completed 07-03-PLAN.md Task 1 (headless smoke 38/38 ALL PASSED; committed e51bd0d). Task 2 checkpoint:human-verify PENDING — awaits user verification in real Windows PyMOL GUI. Phase 7 IN PROGRESS (Plan 3 of 3, Task 1 of 2 done).
Resume file: None
