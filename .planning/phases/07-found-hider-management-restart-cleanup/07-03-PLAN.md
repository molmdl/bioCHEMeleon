---
phase: 07-found-hider-management-restart-cleanup
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - smoke/phase7_smoke.py
autonomous: false

must_haves:
  truths:
    - "Player can hide all found hiders via the dropdown"
    - "Player can show all found hiders via the dropdown in their original reps"
    - "Player can recolor all found hiders via the dropdown using a player-chosen color"
    - "Player can pick a highlight color via a color picker"
    - "New finds use the player-chosen color, not hardcoded green"
    - "Player can press Restart for a fresh round (object restored, new hiders, fresh timer/log)"
    - "Player can press Cleanup to restore the original object (no hiders, original atom count)"
    - "After Cleanup, the game state is fully reset (no dangling wizard, no running timer)"
  artifacts:
    - path: "smoke/phase7_smoke.py"
      provides: "Headless smoke for Restart/Cleanup/found-mgmt/color"
      contains: "phase7"
  key_links:
    - from: "smoke/phase7_smoke.py"
      to: "GameController.start/cleanup/on_pick"
      via: "pure pymol.cmd.* path (no Qt)"
      pattern: "GameController"
    - from: "smoke/phase7_smoke.py"
      to: "registry.build_found_selection"
      via: "import + call the pure helper"
      pattern: "build_found_selection"
---

<objective>
Create smoke/phase7_smoke.py (headless, pure pymol.cmd.* — NO Qt) to verify
the cmd-coupled Phase 7 mechanics, then run a human-verify checkpoint for all
4 success criteria (GUI paths + wizard lifecycle that headless cannot reach).

Purpose: The headless smoke verifies the WSL-testable cmd paths (Restart
round-trip, Cleanup restore, found-mgmt selector hide/show/recolor, color
set_color + iterate, _found_color threading). The human-verify checkpoint
covers the Qt/GUI paths that headless cannot reach (QColorDialog modal
picker, dropdown QComboBox UI, Cleanup/Restart button click behavior, wizard
deactivation on Restart/Cleanup restoring mouse_selection_mode, log clearing,
timer reset, tab switching).

Output: smoke/phase7_smoke.py with 6+ sections (Restart round-trip, Cleanup
restore, found-mgmt hide/show, found-mgmt recolor, color set_color + iterate,
_found_color threading) — 25+ checks, all passing headlessly via
`cmd.exe /c C:\\src\\run-conda-pymol.bat -cq`. Human-verify checkpoint
APPROVED (all 4 Phase 7 success criteria confirmed in real Windows PyMOL GUI).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-found-hider-management-restart-cleanup/07-RESEARCH.md
@.planning/phases/07-found-hider-management-restart-cleanup/07-01-SUMMARY.md
@.planning/phases/07-found-hider-management-restart-cleanup/07-02-SUMMARY.md
@smoke/phase6_smoke.py
@biochemeleon/game.py
@biochemeleon/registry.py
@biochemeleon/backup.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create smoke/phase7_smoke.py + run headlessly</name>
  <files>smoke/phase7_smoke.py</files>
  <action>
    Create smoke/phase7_smoke.py modeled on smoke/phase6_smoke.py (pure
    pymol.cmd.* — NO Qt; AGENTS.md headless path). Use the same check(name,
    cond) + RESULTS + summary + sys.exit(1) pattern.

    Import: `from pymol import cmd` and `from biochemeleon import generators,
    game, registry, backup` (NO Qt, NO wizard — the smoke tests pure cmd
    paths only).

    Structure (6 sections, 25+ checks):

    1. SETUP: cmd.fetch("1ubq", async_=0) + capture orig_count.

    2. START + SHOW: generate 3 sphere positions (seed=42), gc.start(specs),
       verify count += 3, registry len == 3, cmd.show("spheres", "1ubq and
       segi GAME"), verify 3 visible.

    3. FOUND-MGMT HIDE/SHOW: mark 1 hider found (gc._mark_found(id) — directly
       call the helper; or gc.on_pick(id) to go through the full path). Build
       found selection: `from biochemeleon.registry import
       build_found_selection, group_found_by_rep; found = [r for r in
       gc.registry.all() if r.status == registry.HIDER_STATUS_FOUND];
       sele = build_found_selection(found, "1ubq")`. 
       HIDE: cmd.hide("everything", sele), verify count_atoms("1ubq and rep
       spheres and id X") == 0 (hidden hider invisible). 
       SHOW: by_rep = group_found_by_rep(found); for rep, ids in
       by_rep.items(): cmd.show(rep, "1ubq and id X+Y"). Verify count_atoms
       back to 1 (visible again).

    4. FOUND-MGMT RECOLOR + COLOR: cmd.set_color('found_highlight',
       [0.5, 0.5, 0.0]) (a non-default color — yellow-ish). gc._found_color =
       'found_highlight'. cmd.color('found_highlight', sele). Verify color
       changed: cmd.iterate(sele, "stored.append(color)", space={'stored':
       colors}) — check the color index changed from the pre-recolor value
       (green's index). Use a "changed from pre-recolor" assertion (like
       phase4_smoke — green's exact index varies by build).

    5. _FOUND_COLOR THREADING: fresh GameController('1ubq'), register a hider
       manually OR start a new game. Set gc._found_color = 'cyan'. Call
       gc._mark_found(id). Verify cmd.color was called with 'cyan' (iterate
       the hider atom's color — should be cyan's index, not green's). This
       confirms the Plan 01 TDD change works at runtime (not just in unit
       tests with mocked cmd).

    6. RESTART ROUND-TRIP: gc.cleanup() → verify_intact True (backup.verify_intact).
       Create gc2 = game.GameController('1ubq'), gc2.start(new_specs with
       different seed or positions). Verify count += 3 (new hiders), registry
       len == 3 (fresh), verify_intact True (backup still valid for the new
       round). gc2.cleanup() → verify_intact True + count back to orig + no
       GAME atoms (cmd.count_atoms("1ubq and segi GAME") == 0). This tests
       the start→cleanup→start cycle that _on_restart uses at the cmd layer
       (the GUI _on_restart + wizard lifecycle is human-verify).

    7. CLEANUP EXPLICIT PATH: Start a game (gc3), gc3.cleanup(). Verify:
       backup.verify_intact True, count back to orig, no GAME atoms remain.
       This re-verifies the Cleanup button's underlying cmd path (the GUI
       _on_cleanup + wizard deactivation + UI reset is human-verify).

    8. SUMMARY: print pass/fail counts, sys.exit(1) on any fail.

    IMPORTANT — headless limitations (human-verify covers these):
    - QColorDialog: Qt widget, cannot run headless (human-verify C2)
    - QComboBox dropdown UI: Qt widget (human-verify C1)
    - PickWizard lifecycle (activate/deactivate, mouse_selection_mode): needs
      real PyMOL GUI (human-verify C3/C4)
    - Timer reset, log clearing, tab switching: Qt widgets (human-verify)

    Run headlessly (AGENTS.md headless method):
    ```bash
    # 1. Stage to Windows-facing path
    bash wsl2win_cp.sh
    mkdir -p tmp/bioCHEMeleon/smoke && cp smoke/phase7_smoke.py tmp/bioCHEMeleon/smoke/
    # 2. Run headlessly (no GUI, ~30-60s)
    cd tmp/bioCHEMeleon && timeout 120 cmd.exe /c "C:\\src\\run-conda-pymol.bat -cq smoke\\phase7_smoke.py" 2>&1 | tail -60
    # 3. Check exit code: 0 = clean (or sys.exit(1) caught); nonzero = crash
    ```

    If any check fails, debug + fix + re-run (same pattern as Phase 6 06-03).
    If a check reveals a runtime bug in the Plan 02 GUI wiring (e.g., a
    selector is wrong, build_found_selection returns None unexpectedly, a
    color command fails), fix the source file (gui_game.py / __init__.py /
    registry.py / game.py) and re-run the smoke. Commit runtime fixes
    separately from the smoke commit.
  </action>
  <verify>
    cd tmp/bioCHEMeleon && timeout 120 cmd.exe /c "C:\\src\\run-conda-pymol.bat -cq smoke\\phase7_smoke.py" 2>&1 | tail -60
    ALL checks pass (25+ checks, exit 0). If any fail, fix + re-run until all
    pass. Then commit the smoke:
    git add smoke/phase7_smoke.py
    git commit -m "feat(07-03): headless smoke for Phase 7 (Restart/Cleanup/found-mgmt/color)"
    If source files were fixed during smoke verification, commit them:
    git add biochemeleon/*.py
    git commit -m "fix(07-03): runtime fix for <bug description>"
  </verify>
  <done>
    smoke/phase7_smoke.py exists, 25+ checks, ALL PASSED headlessly via
    cmd.exe /c run-conda-pymol.bat -cq. Exit 0. Any runtime bugs discovered
    during smoke verification are fixed + committed. The smoke covers:
    Restart round-trip (start→cleanup→start→verify_intact), Cleanup restore,
    found-mgmt hide/show/recolor, color set_color + iterate, _found_color
    threading at runtime (not just unit tests).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 7 complete GUI: found-hider management dropdown (GAME-08) with
    hide/show/recolor modes + color picker (DIFF-04) + Restart button (GAME-10)
    on the Game tab; Cleanup button (BTN-06) on the Setup tab. The wizard
    lifecycle is fixed (deactivate old PickWizard before cleanup in _on_start
    — fixes mouse_selection_mode corruption for BOTH Start-mid-game AND
    Restart). The headless smoke (Task 1) verified the pure cmd paths; this
    checkpoint verifies the Qt/GUI paths + wizard lifecycle in a real Windows
    PyMOL session.
  </what-built>
  <how-to-verify>
    In a real Windows PyMOL session (via setenv.bat → pymol):

    PREREQUISITE: Load a molecule (e.g., fetch 1ubq or load a bundled demo
    like 1znf). Press Start (Setup tab) → verify 3-2-1 countdown → Game tab.
    Click a hider to find it → verify it turns the highlight color (default
    green). Find at least 1-2 hiders before testing the dropdown.

    C1: Found-hider dropdown (GAME-08):
    1. After finding 1+ hiders, locate the "Found hiders" dropdown (QComboBox)
       in the Game tab button row.
    2. Select "Hide found" → verify the FOUND hiders disappear from the viewer
       (they become invisible). Hidden (unfound) hiders should remain visible.
    3. Select "Show found" → verify the found hiders REAPPEAR in their original
       representations (spheres/lines/sticks/cartoon — whatever rep they were).
    4. Select "Recolor found" → verify found hiders recolor to the current
       highlight color (default green).
    5. Verify the dropdown resets to "Found hiders: (select)" after each action
       (so you can re-select the same mode).

    C2: Color picker (DIFF-04):
    6. Click the "Color…" button → verify the QColorDialog opens (a standard
       color picker modal dialog).
    7. Pick a non-default color (e.g., cyan, magenta, or yellow) → click OK.
    8. Verify existing found hiders RECOLOR to the new color immediately
       (the _on_pick_color handler auto-recolors via _on_found_mgmt('recolor')).
    9. Find another hider (click a new hider in the viewer) → verify the NEW
       find uses the NEW color (not green). This confirms _found_color
       threading works for future finds.
    10. Cancel the color picker (click Cancel) → verify no color change occurs.

    C3: Restart (GAME-10):
    11. Press the "Restart" button on the Game tab.
    12. Verify: object restored to original (no old hiders visible — they're
        gone), NEW hiders generated (you should see new hider atoms in the
        viewer), fresh 3-2-1 countdown begins, timer resets to 0:00, info log
        is cleared (no old messages).
    13. CRITICAL — wizard lifecycle: After the Restart countdown finishes,
        verify you can LEFT-DRAG to rotate the view (mouse is NOT stuck in
        atomic-pick mode). Then click a hider → verify it works (the click
        finds the hider). If left-drag does NOT rotate (stuck in pick mode),
        the wizard lifecycle fix failed — describe the issue.
    14. Find a hider in the restarted round → verify it uses the current
        highlight color.

    C4: Cleanup (BTN-06):
    15. Switch to the Setup tab. Locate the "Cleanup model" button (between
        "Load Setup…" and "Start" — button 6 per spec).
    16. Press "Cleanup model".
    17. Verify: object restored to original (atom count matches pre-Start —
        no hider atoms visible, no GAME atoms), wizard deactivated (LEFT-DRAG
        rotates the view — mouse mode restored to pre-game), timer stopped,
        info log cleared, "Remaining: -" + "Reveals: 0" + "0:00" labels reset.
    18. Verify the viewer is interactive (rotate, zoom) after Cleanup — no
        dangling wizard, no frozen state.
    19. Press Start again → verify a fresh game starts (Cleanup released the
        controller via _controller = None; Start creates a new one via
        _on_start). The full Start flow should work (countdown, hiders, click
        to find).
    20. Press Cleanup with no game running → verify it's a no-op (no crash,
        no error — the `if self._controller is None: return` guard works).

    If all steps pass, type "approved". If any step fails, describe the issue
    (which step, what happened vs what was expected). If the wizard lifecycle
    fix failed (step 13 or 17 — mouse stuck in pick mode), this is a BLOCKER
    that must be fixed before Phase 7 can be marked complete.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Phase 7 Plan 03 verification:
- [ ] smoke/phase7_smoke.py created, 25+ checks, ALL PASSED headlessly (exit 0)
- [ ] Human-verify checkpoint: all 4 success criteria confirmed in real PyMOL GUI
  - C1: Found-hider dropdown hide/show/recolor works (GAME-08)
  - C2: Color picker sets new color + auto-recolors + new finds use new color (DIFF-04)
  - C3: Restart restores + regenerates + fresh timer/log + mouse mode intact (GAME-10)
  - C4: Cleanup restores + deactivates wizard + resets UI + releases controller (BTN-06)
- [ ] Any runtime bugs discovered during verification are fixed + committed
- [ ] Wizard lifecycle fix verified: left-drag rotates after Restart AND Cleanup
</verification>

<success_criteria>
- Headless smoke: 25+ checks ALL PASSED (Restart/Cleanup/found-mgmt/color/_found_color)
- Human-verify: APPROVED (all 4 Phase 7 success criteria in real PyMOL GUI)
- Wizard lifecycle fix confirmed: mouse_selection_mode restored after Restart + Cleanup
- All WSL gates green (py_compile + 187+ tests + Pitfall-1 + exec_)
</success_criteria>

<output>
After completion, create `.planning/phases/07-found-hider-management-restart-cleanup/07-03-SUMMARY.md`
</output>
