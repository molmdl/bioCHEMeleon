---
phase: 08-persistence-and-shareable-puzzles
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - biochemeleon/registry.py
  - tests/test_registry.py
autonomous: true

must_haves:
  truths:
    - "A sentinel-rebuilt registry (rep=None, all hidden) + a .bcm hiders list -> reconcile sets rep + status per matching (object, id)"
    - "A sentinel not in .bcm stays rep=None, status='hidden' (missing_from_bcm flagged)"
    - "A .bcm hider not in sentinels is NOT registered (missing_from_pse flagged) — no ghost entries"
    - "A .bcm hider with rep not in GAME_REPS is skipped (bad_rep flagged, rec.rep stays None — no raise)"
    - "A .bcm hider with status not in (hidden, found) defaults to hidden (no raise)"
    - "reconcile_with_bcm returns a ReconcileMismatches namedtuple of (missing_from_bcm, missing_from_pse, bad_rep)"
    - "After reconcile, counts_by_rep reflects the .bcm reps (not all-zero from the rep=None sentinel-rebuild state)"
    - "reconcile_with_bcm runs in WSL without PyMOL installed (pure: no from pymol import; stdlib + GAME_REPS only)"
  artifacts:
    - path: "biochemeleon/registry.py"
      provides: "HiderRegistry.reconcile_with_bcm pure method + ReconcileMismatches namedtuple"
      contains: "def reconcile_with_bcm"
    - path: "tests/test_registry.py"
      provides: "TestReconcileFromBcm class (~12 unit tests)"
      contains: "class TestReconcileFromBcm"
  key_links:
    - from: "HiderRegistry.reconcile_with_bcm"
      to: "self._records (sentinel-rebuilt records keyed by (object, id))"
      via: "in-place mutation of rec.rep + rec.status by (object, id) match"
      pattern: "self\\._records\\.items"
    - from: "HiderRegistry.reconcile_with_bcm"
      to: "GAME_REPS (rep validation)"
      via: "bcm_rep not in GAME_REPS -> bad_rep flag, rec.rep stays None"
      pattern: "bcm_rep not in GAME_REPS"
    - from: "HiderRegistry.reconcile_with_bcm"
      to: "counts_by_rep (existing method, registry.py:178)"
      via: "after reconcile, rep is no longer None so counts_by_rep returns non-zero"
      pattern: "counts_by_rep"
---

<objective>
TDD the WSL-testable pure-layer foundation for Phase 8's `.bcm` sidecar
reconciliation: `HiderRegistry.reconcile_with_bcm` — the merge that overrides
sentinel-rebuilt records (rep=None, all hidden) with `.bcm` per-hider metadata
(rep + status) by matching `(object, id)`.

Purpose: After `cmd.load(.pse)`, the sentinel atoms (`segi='GAME'` + `b=-999`)
survive with stable `id`s but the in-memory `HiderRegistry` is gone and the
sentinel carries no `rep`. `reconstruct_from_sentinels` (Phase 3) rebuilds with
`rep=None` + `status='hidden'` for every sentinel. WITHOUT reconcile, two
existing functions break: `counts_by_rep()` (registry.py:195) skips `rep=None`
records → all-zero per-rep counts; `group_found_by_rep()` (registry.py:311)
skips `rep=None` found records → found-mgmt "Show found" shows nothing.
Reconcile is MANDATORY for a usable reloaded game (research §10.6). TDD-ing it
here gives WSL confidence BEFORE the cmd-coupled persistence (Plan 03) and
GUI wiring (Plan 04).

Output: `HiderRegistry.reconcile_with_bcm` pure method (~25 lines) +
`ReconcileMismatches` namedtuple in `registry.py` + `TestReconcileFromBcm`
class (~12 tests) in `tests/test_registry.py`. Purity preserved: no
`from pymol import` in `registry.py`. Existing tests pass unchanged.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-persistence-and-shareable-puzzles/08-state-reconstruction-RESEARCH.md
@biochemeleon/registry.py
@tests/test_registry.py
</context>

<feature>
  <name>HiderRegistry.reconcile_with_bcm — .bcm metadata merge onto sentinel-rebuilt records</name>
  <files>biochemeleon/registry.py, tests/test_registry.py</files>
  <behavior>
    Pure method on HiderRegistry (no `from pymol import`; stdlib + GAME_REPS only).
    Precondition: `self` was rebuilt via `reconstruct_from_sentinels` (records
    keyed by `(object, id)`, `rep=None`, `status='hidden'`).

    For each `.bcm` hider dict, find the matching rebuilt record by
    `(object, int(id))` and set `rep` + `status` (and `pos` if present).
    Returns a `ReconcileMismatches` namedtuple of three mismatch lists
    (empty when the .bcm perfectly matches the sentinels). Mutates
    `self._records` in place. Does NOT raise on corrupt sidecar — degraded
    is playable, refuse is the caller's policy.

    Cases (input -> expected output):
      - 3 sentinels + 3 .bcm entries (1 found, reps: spheres/sticks/cartoon)
        -> 3 records with real rep; 1 found, 2 hidden; mismatches all empty.
      - sentinel id=4 not in .bcm -> stays rep=None, status='hidden';
        missing_from_bcm == [('o', 4)].
      - .bcm lists id=99 (not in sentinels) -> NOT registered (no 4th record);
        missing_from_pse == [('o', 99)]; registry has 3 records, not 4.
      - .bcm hider with rep='surface' (not in GAME_REPS) -> rec.rep stays None;
        bad_rep == [('o', 1, 'surface')]; no raise.
      - .bcm hider with status='revealed' (unknown) -> rec.status='hidden';
        no raise.
      - .bcm hider with pos=[1.0,2.0,3.0] -> rec.pos == [1.0,2.0,3.0] (list).
      - reconcile_with_bcm([]) on 3-sentinel registry -> all 3 stay rep=None,
        hidden; missing_from_bcm has 3 entries.
      - reconcile_with_bcm(None) -> graceful (treated as empty list); 3
        missing_from_bcm entries.
      - .bcm hider (object='other', id=1) doesn't match sentinel ('o', 1) ->
        sentinel stays rep=None, hidden; .bcm entry flagged missing_from_pse.
      - .bcm hider with id='5' (str from JSON) matches sentinel id=5 (int)
        via int(h['id']) coercion.
      - Full round-trip: register 3 -> to_dict -> reconstruct_from_sentinels
        (fake) -> reconcile_with_bcm(to_dict['hiders']) -> records match
        original (id/object/rep/status).
      - After reconcile, counts_by_rep reflects .bcm reps (not all-zero).
  </behavior>
  <implementation>
    In `biochemeleon/registry.py`:

    1. At the TOP of the file (after line 28 `from collections import OrderedDict`,
       before `from .setup_state import GAME_REPS`), add:
       ```
       from collections import OrderedDict, namedtuple
       ```
       (Modify the existing `from collections import OrderedDict` line to also
       import `namedtuple` — do NOT add a second import line.)

    2. After the constants section (after line 39 `HIDER_STATUS_FOUND = 'found'`),
       add the namedtuple:
       ```
       #: Returned by :meth:`HiderRegistry.reconcile_with_bcm` — three classes
       #: of disagreement between the sentinel-rebuilt registry and the .bcm
       #: sidecar. Empty lists = perfect match. The caller (game.py /
       #: __init__.py) decides whether to log warnings or refuse the load;
       #: the registry stays usable regardless (degraded = playable).
       ReconcileMismatches = namedtuple(
           'ReconcileMismatches',
           ['missing_from_bcm',   # [(object, id)] sentinel atoms NOT in .bcm
            'missing_from_pse',   # [(object, id)] .bcm hiders NOT in sentinels
            'bad_rep'])           # [(object, id, bad_rep)] .bcm reps not in GAME_REPS
       ```

    3. Inside the `HiderRegistry` class, AFTER `reconstruct_from_sentinels`
       (after line 272, end of the method), add the new method:
       ```
       def reconcile_with_bcm(self, bcm_hiders):
           """Reconcile sentinel-rebuilt records with .bcm sidecar metadata.

           Precondition: ``self`` was rebuilt via
           :meth:`reconstruct_from_sentinels` (records keyed by
           ``(object, id)``, ``rep=None``, ``status='hidden'``). For each
           ``.bcm`` hider dict, find the matching rebuilt record by
           ``(object, id)`` and set ``rep`` + ``status`` (and ``pos`` if
           present). Returns a :data:`ReconcileMismatches` namedtuple of
           three mismatch lists; the caller decides whether to log warnings
           or refuse the load.

           Pure (no ``from pymol``). Takes a list of dicts (the
           ``.bcm['registry']['hiders']`` list). Mutates ``self._records``
           in place.

           Mismatch handling (never raises — degraded is playable):
             - sentinel NOT in .bcm (``missing_from_bcm``): left as
               ``rep=None`` + ``status='hidden'`` (real atom, still clickable).
             - .bcm hider NOT in sentinels (``missing_from_pse``): NOT
               registered (ghost entry would corrupt counts_by_rep + on_pick).
             - .bcm rep not in :data:`GAME_REPS` (``bad_rep``): rec.rep stays
               ``None`` (do NOT raise — corrupt sidecar should not kill load).
             - .bcm status not in (hidden, found): defaults to ``'hidden'``.
           """
           missing_from_bcm = []
           missing_from_pse = []
           bad_rep = []
           bcm_index = {}
           for h in bcm_hiders or []:
               try:
                   key = (h['object'], int(h['id']))
               except (KeyError, TypeError, ValueError):
                   continue  # malformed .bcm entry — skip
               bcm_index[key] = h
           for key, rec in self._records.items():
               h = bcm_index.get(key)
               if h is None:
                   missing_from_bcm.append(key)
                   continue
               bcm_rep = h.get('rep')
               if bcm_rep is not None and bcm_rep not in GAME_REPS:
                   bad_rep.append((key[0], key[1], bcm_rep))
                   # leave rec.rep = None
               else:
                   rec.rep = bcm_rep
               bcm_status = h.get('status', HIDER_STATUS_HIDDEN)
               if bcm_status not in (HIDER_STATUS_HIDDEN, HIDER_STATUS_FOUND):
                   bcm_status = HIDER_STATUS_HIDDEN
               rec.status = bcm_status
               if 'pos' in h and h['pos'] is not None:
                   rec.pos = list(h['pos'])
           for key, h in bcm_index.items():
               if key not in self._records:
                   missing_from_pse.append(key)
           return ReconcileMismatches(
               missing_from_bcm=missing_from_bcm,
               missing_from_pse=missing_from_pse,
               bad_rep=bad_rep)
       ```

    In `tests/test_registry.py`:

    Add a new `TestReconcileFromBcm` class AFTER the existing classes (end of
    file). Mirror the `TestHiderRegistryReconstruct` pattern (lines 451-532):
    build a sentinel-rebuilt registry via `reconstruct_from_sentinels` with a
    fake iterate fn, then call `reconcile_with_bcm` with a .bcm hiders list,
    assert merged records + returned mismatches. Import `ReconcileMismatches`
    from `biochemeleon.registry` (add to the existing import block at lines
    26-30). Build `.bcm` hider dicts as plain Python dicts (the shape
    `registry.to_dict()` produces: `{'id': int, 'object': str, 'rep': str,
    'status': str, 'pos': [x,y,z] (optional)}`).

    Test methods (12):
      - test_perfect_match_sets_rep_and_status
      - test_missing_from_bcm_stays_rep_none_hidden
      - test_missing_from_pse_not_registered
      - test_bad_rep_skipped_with_warning
      - test_bad_status_defaults_to_hidden
      - test_pos_restored_from_bcm
      - test_empty_bcm_hiders_list
      - test_none_bcm_hiders
      - test_mismatched_object_not_merged
      - test_id_int_coercion_in_bcm_index
      - test_round_trip_to_dict_reconstruct_reconcile
      - test_counts_by_rep_after_reconcile_reflects_bcm_reps
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1 (RED): Write failing tests for reconcile_with_bcm</name>
  <files>tests/test_registry.py</files>
  <action>
    1. In `tests/test_registry.py`, update the import block at lines 26-30 to
       also import `ReconcileMismatches`:
       ```
       from biochemeleon.registry import (
           HiderRecord, HiderRegistry, ReconcileMismatches,
           HIDER_STATUS_HIDDEN, HIDER_STATUS_FOUND,
           build_found_selection, group_found_by_rep,
       )
       ```

    2. Add a new `TestReconcileFromBcm` class at the END of the file (after
       the last existing class). Each test builds a sentinel-rebuilt registry
       via `HiderRegistry().reconstruct_from_sentinels(fake_keys)` where
       `fake_keys` is a lambda returning `[('o', 1), ('o', 2), ('o', 3)]`
       (or a subset), then calls `reg.reconcile_with_bcm(bcm_hiders)` and
       asserts the merged records + returned `ReconcileMismatches`.

       Write all 12 test methods listed in the `<implementation>` block above.
       Use plain dicts for `.bcm` hiders, e.g.:
       `{'id': 1, 'object': 'o', 'rep': 'spheres', 'status': 'found'}`
       For the round-trip test: `reg.register('o', 1, 'spheres')` etc. →
       `d = reg.to_dict()` → `reg2 = HiderRegistry().reconstruct_from_sentinels(
       lambda: [('o', r['id']) for r in d['hiders']])` →
       `reg2.reconcile_with_bcm(d['hiders'])` → assert records match.
       For `test_counts_by_rep_after_reconcile_reflects_bcm_reps`: after
       reconcile with 3 sphere hiders, `reg.counts_by_rep()['spheres'] == 3`
       (NOT 0 — the load-bearing regression for the rep=None limitation).

    3. Run: `python3.6 -m unittest tests.test_registry -v 2>&1 | tail -30`
       New tests MUST fail (RED) with `ImportError: cannot import name
       'ReconcileMismatches'` (doesn't exist yet). Existing tests MUST still
       pass (no regression — the new tests are additive).

    4. Commit the RED state:
       `git add tests/test_registry.py`
       `git commit -m "test(08-01): add failing tests for reconcile_with_bcm"`
  </action>
  <verify>
    python3.6 -m unittest tests.test_registry -v 2>&1 | tail -30
    New tests FAIL (RED — ImportError for ReconcileMismatches). Existing tests
    pass. Then:
    git log --oneline -1
  </verify>
  <done>
    New tests fail with ImportError (ReconcileMismatches / reconcile_with_bcm
    don't exist yet). Existing tests pass (no regression). RED commit made:
    `test(08-01): add failing tests for reconcile_with_bcm`.
  </done>
</task>

<task type="auto">
  <name>Task 2 (GREEN): Implement reconcile_with_bcm + ReconcileMismatches</name>
  <files>biochemeleon/registry.py</files>
  <action>
    1. In `biochemeleon/registry.py`:
       a. Modify line 28 `from collections import OrderedDict` to also import
          `namedtuple`:
          `from collections import OrderedDict, namedtuple`
       b. After the constants section (after line 39 `HIDER_STATUS_FOUND =
          'found'`), add the `ReconcileMismatches` namedtuple (see the
          `<implementation>` block in the `<feature>` above for the exact code).
       c. Inside `HiderRegistry`, AFTER `reconstruct_from_sentinels` (after
          line 272), add the `reconcile_with_bcm` method (see the
          `<implementation>` block for the exact code — ~25 lines including
          docstring).

    2. Run: `python3.6 -m unittest tests.test_registry -v 2>&1 | tail -10`
       ALL tests MUST pass (GREEN) — the 12 new tests + the existing tests.
       The existing tests pass UNCHANGED (reconcile_with_bcm is additive; no
       existing method is modified).

    3. Commit the GREEN state:
       `git add biochemeleon/registry.py`
       `git commit -m "feat(08-01): implement reconcile_with_bcm pure method"`
  </action>
  <verify>
    python3.6 -m unittest tests.test_registry -v 2>&1 | tail -10
    ALL tests pass (GREEN — 12 new + existing). Then:
    python3.6 -m py_compile biochemeleon/registry.py && echo "PY_COMPILE_OK"
    rg -n "from pymol" biochemeleon/registry.py | wc -l   # MUST be 0 (purity)
    git log --oneline -1
  </verify>
  <done>
    All tests pass (12 new + existing). py_compile clean. registry.py purity
    intact (0 `from pymol` matches). GREEN commit made:
    `feat(08-01): implement reconcile_with_bcm pure method`.
    `reconcile_with_bcm` + `ReconcileMismatches` exist in `registry.py`
    (pure, no pymol).
  </done>
</task>

<task type="auto">
  <name>Task 3 (REFACTOR + full WSL gate suite)</name>
  <files>biochemeleon/registry.py</files>
  <action>
    1. Review the implementation for obvious improvements (DRY, clarity,
       naming). The method is ~25 lines following the `<implementation>`
       sketch — likely clean on first pass. If no changes needed, skip the
       REFACTOR commit. If improvements are made, run tests to confirm they
       still pass, then commit:
       `git commit -m "refactor(08-01): clean up reconcile_with_bcm"`

    2. Run the FULL WSL gate suite (mirrors AGENTS.md commands):

       a. Syntax check every module:
          `python3.6 -m py_compile biochemeleon/*.py`

       b. Run ALL pure-layer unit tests:
          `python3.6 -m unittest tests.test_setup_state tests.test_registry -v`

       c. Pitfall-1 gate (MUST return ZERO matches):
          `rg -n "import Tkinter|import tkinter|from tkinter|import Pmw|from Pmw|app\.root|grab_set|mainloop|Toplevel|menuBar\.addmenuitem|from PyQt5 import|import PyQt5" biochemeleon/`

       d. exec_ gate (unchanged — existing msg.exec_() in gui_game.py only;
          NO new .exec_()):
          `rg -n "\.exec_\(\)" biochemeleon/`

       e. registry.py purity gate (MUST be 0 — no pymol import):
          `rg -n "from pymol" biochemeleon/registry.py`

       f. reconcile_with_bcm gate (MUST be 1 — the method def):
          `rg -n "def reconcile_with_bcm" biochemeleon/registry.py`

       g. ReconcileMismatches gate (MUST be 1 — the namedtuple def):
          `rg -n "ReconcileMismatches = namedtuple" biochemeleon/registry.py`

    3. If ALL gates pass, Phase 8 Plan 01 is complete. If any gate fails,
       fix the issue and re-run.
  </action>
  <verify>
    python3.6 -m py_compile biochemeleon/*.py && echo "PY_COMPILE_OK" && \
    python3.6 -m unittest tests.test_setup_state tests.test_registry 2>&1 | tail -5 && \
    rg -n "import Tkinter|import tkinter|from tkinter|import Pmw|from Pmw|app\.root|grab_set|mainloop|Toplevel|menuBar\.addmenuitem|from PyQt5 import|import PyQt5" biochemeleon/ | wc -l && \
    rg -n "\.exec_\(\)" biochemeleon/ | wc -l && \
    rg -n "from pymol" biochemeleon/registry.py | wc -l && \
    rg -c "def reconcile_with_bcm" biochemeleon/registry.py && \
    rg -c "ReconcileMismatches = namedtuple" biochemeleon/registry.py
    Expected: PY_COMPILE_OK, all tests pass, Pitfall-1=0, exec_ unchanged,
    registry purity=0, reconcile_with_bcm=1, ReconcileMismatches=1.
  </verify>
  <done>
    All WSL gates green. py_compile clean. All tests pass (existing + 12 new).
    Pitfall-1=0. exec_ unchanged. registry.py purity intact (0 from pymol).
    reconcile_with_bcm + ReconcileMismatches in registry.py (1 each). If
    REFACTOR changes were made, committed; otherwise no REFACTOR commit.
  </done>
</task>

</tasks>

<verification>
Phase 8 Plan 01 TDD verification:
- [ ] RED: 12 new tests fail (ImportError for ReconcileMismatches), existing pass
- [ ] GREEN: all tests pass (12 new + existing)
- [ ] REFACTOR: all WSL gates green (py_compile + tests + Pitfall-1 + exec_ + purity)
- [ ] reconcile_with_bcm exists in registry.py (pure, no pymol)
- [ ] ReconcileMismatches namedtuple exists in registry.py
- [ ] After reconcile, counts_by_rep reflects .bcm reps (not all-zero)
- [ ] Existing tests pass UNCHANGED (additive, no method modified)
</verification>

<success_criteria>
- HiderRegistry.reconcile_with_bcm pure method in registry.py (~25 lines)
- ReconcileMismatches namedtuple in registry.py
- 12 unit tests in TestReconcileFromBcm (tests/test_registry.py)
- All tests pass (existing + 12 new)
- All WSL gates green (py_compile + Pitfall-1 + exec_ + purity)
- TDD: RED commit + GREEN commit (+ optional REFACTOR commit)
</success_criteria>

<output>
After completion, create `.planning/phases/08-persistence-and-shareable-puzzles/08-01-SUMMARY.md`
</output>
