---
phase: 08-persistence-and-shareable-puzzles
plan: 05
type: execute
wave: 4
depends_on: ["08-04"]
files_modified:
  - smoke/phase8_smoke.py
autonomous: false

must_haves:
  truths:
    - "cmd.save(pse_path, target_obj) with a bare object name excludes _bchm_backup from the .pse (scoped save — source-verified HIGH, runtime-verified by smoke)"
    - "cmd.load(pse_path, partial=1) merges the .pse into the existing session (obj absent -> present after load; no-collision path)"
    - "GameController.import_state(bcm_dict) reconstructs registry from sentinels + reconciles rep + found-status + sets _started/_backup_name/_is_imported"
    - "Found-status round-trips through .bcm (checkpoint import: N found preserved)"
    - "Rep reconciles from .bcm (post-reconcile counts_by_rep non-zero)"
    - "Restart-on-imported restores to the imported state (all-hidden for puzzle; found-status for checkpoint)"
    - "Cleanup-on-imported two-step removes hiders + restores count to orig"
    - "Collision is detectable (target_obj in loaded molecules -> refuse check would fire)"
    - "Timer resume: _start_time = time.time() - elapsed (checkpoint import math)"
    - "GUI: Save mid-game + reload resumes the game (timer, found-status, registry)"
    - "GUI: Generate & export writes .bcmz + stays on Setup + Cleanup restores scene"
    - "GUI: Import loads .bcmz + play begins + click-to-find works + win fires"
    - "GUI: Restart-on-imported restores (no re-generation); Cleanup-on-imported two-step"
    - "GUI: Found-mgmt dropdown works after reload (Show found shows found hiders in their rep)"
    - "GUI: Timer resume display (checkpoint import shows saved elapsed during countdown, then counts up)"
  artifacts:
    - path: "smoke/phase8_smoke.py"
      provides: "Headless smoke (~30-40 checks, pure pymol.cmd.* — export/import round-trip for checkpoint + puzzle kinds)"
      contains: "phase8_smoke"
  key_links:
    - from: "smoke/phase8_smoke.py"
      to: "persistence.build_bcm_dict + persistence.write_bcmz + persistence.read_bcmz"
      via: "exercises the cmd-coupled round-trip headlessly"
      pattern: "build_bcm_dict|write_bcmz|read_bcmz"
    - from: "smoke/phase8_smoke.py"
      to: "GameController.import_state + cmd.save(target_obj) + cmd.load(partial=1)"
      via: "verifies the scoped save + merge load + import_state reconstruction"
      pattern: "import_state|cmd\\.save|cmd\\.load"
    - from: "smoke/phase8_smoke.py"
      to: "registry.reconcile_with_bcm + counts_by_rep + group_found_by_rep"
      via: "verifies rep reconciliation + per-rep counts recovery after reload"
      pattern: "reconcile_with_bcm|counts_by_rep"
---

<objective>
Write `smoke/phase8_smoke.py` (headless: export→import round-trip for both
checkpoint + puzzle kinds, verifying scoped-save excludes backup, sentinel
rebuild, reconcile rep+found-status, timer resume, counters, resolve_target)
+ checkpoint:human-verify (GUI: the 3 Phase 8 success criteria +
Restart/Cleanup-on-imported + found-mgmt after reload + timer resume display).

Purpose: This is the two-tier verification plan. Task 1 (auto) writes +
stages + runs the headless smoke (pure `pymol.cmd.*`, NO Qt — the proven
Phase 3-7 pattern) to verify the cmd-coupled round-trip. Task 2
(checkpoint:human-verify) covers the Qt GUI paths that CANNOT run headlessly
(QFileDialog, tab-switching, countdown, PickWizard, click-to-find, timer
display) — the user exercises the 3 success criteria in a real Windows
PyMOL session.

**Open risks the smoke + checkpoint must confirm** (from the research):
1. Custom `cmd.set_color` survival across `.pse` round-trip (UNVERIFIED) —
   `found_color_rgb` in `.bcm` is the safety net.
2. Scoped save excludes `_bchm_backup` (source-verified HIGH, runtime
   UNVERIFIED) — smoke asserts `_bchm_backup not in all_objects` after load.
3. `cmd.load(partial=1)` merge (no-collision path) — smoke verifies;
   collision path (refuse dialog) is human-verify.
4. `tempfile.TemporaryDirectory()` / `NamedTemporaryFile` in Windows PyMOL
   for `cmd.save` (UNVERIFIED) — likely works (Phase 3 used relative paths).

Output: `smoke/phase8_smoke.py` (~200 lines, ~30-40 checks, pure
`pymol.cmd.*`). Headless run exits 0 (ALL PASSED). Human-verify checkpoint
covers the 3 success criteria + the imported-game lifecycle (Restart/
Cleanup/found-mgmt/timer resume).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-persistence-and-shareable-puzzles/08-file-format-RESEARCH.md
@.planning/phases/08-persistence-and-shareable-puzzles/08-export-import-workflow-RESEARCH.md
@.planning/phases/08-persistence-and-shareable-puzzles/08-state-reconstruction-RESEARCH.md
@.planning/phases/08-persistence-and-shareable-puzzles/08-03-PLAN.md
@.planning/phases/08-persistence-and-shareable-puzzles/08-04-PLAN.md
@smoke/phase7_smoke.py
@biochemeleon/persistence.py
@biochemeleon/game.py
@biochemeleon/registry.py
@biochemeleon/backup.py
@biochemeleon/mutation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write + stage + run smoke/phase8_smoke.py headlessly</name>
  <files>smoke/phase8_smoke.py</files>
  <action>
    Write `smoke/phase8_smoke.py` modeled on `smoke/phase7_smoke.py` (pure
    `pymol.cmd.*`, NO Qt — AGENTS.md: headless path cannot run Qt). Use the
    `check(name, cond)` + `RESULTS` list pattern + `sys.exit(1)` on any fail.
    Run via the proven staging pattern: `bash wsl2win_cp.sh` to stage
    `biochemeleon/` to `tmp/bioCHEMeleon/biochemeleon/`, copy the smoke to
    `tmp/bioCHEMeleon/smoke/phase8_smoke.py`, then
    `cd tmp/bioCHEMeleon && timeout 120 cmd.exe /c "C:\\src\\run-conda-pymol.bat -cq smoke\\phase8_smoke.py" 2>&1 | tail -50`.

    The smoke covers these sections (research §8.1 + §9.2):

    ```python
    # smoke/phase8_smoke.py — Phase 8 headless smoke. Run: pymol -cq smoke/phase8_smoke.py
    # Verifies the cmd-coupled Phase-8 export/import round-trip headlessly
    # (pure pymol.cmd.* only, NO Qt). GUI paths (QFileDialog, tab-switching,
    # countdown, PickWizard, click-to-find, timer display) are deferred to
    # the human-verify checkpoint (Task 2).
    import sys, os, json, tempfile, zipfile, time
    from pymol import cmd
    from biochemeleon import game, registry, backup, mutation, persistence, generators
    from biochemeleon.registry import (HIDER_STATUS_FOUND, HIDER_STATUS_HIDDEN,
                                        build_found_selection, group_found_by_rep)

    RESULTS = []
    def check(name, cond):
        RESULTS.append((name, bool(cond)))
        print(("PASS" if cond else "FAIL") + ": " + name)
    ```

    **Section A — SETUP**: `cmd.fetch("1ubq", async_=0)`; `orig_count =
    cmd.count_atoms("1ubq")`; check `orig_count > 0`.

    **Section B — START + PLAY PARTIAL** (checkpoint save): start a game
    with 3 sphere hiders (use `generators.generate_sphere_positions` with
    `seed=42` — same as phase7_smoke); `gc.start(hider_specs)`; check
    `count == orig + 3` + `registry len == 3`; mark 1 found
    (`gc._mark_found(first_id)` — uses default `_found_color='green'`);
    set `gc._reveal_count = 1`, `gc._hint_count = 2`,
    `gc._start_time = time.time() - 42.5` (simulate 42.5s elapsed). State
    now: 3 hiders, 1 found, 2 hidden, 1 reveal, 2 hints, 42.5s elapsed.

    **Section C — SAVE (checkpoint)**: capture `elapsed = time.time() -
    gc._start_time` (~42.5); build `setup_state` dict (mirror
    `gui_setup.collect_state()` shape — use a minimal valid dict with
    `format`, `target_mode`, `selected_object`, `hider_count`, `per_rep`,
    etc.); `bcm_dict = persistence.build_bcm_dict(gc, setup_state,
    kind='checkpoint', elapsed=elapsed)`; write the .pse via
    `cmd.save(pse_path, "1ubq")` (bare name — scoped save); write the .bcmz
    via `persistence.write_bcmz(bcmz_path, bcm_dict, pse_path)`; check the
    .bcmz file exists + `os.path.getsize > 0`; open the .bcmz with zipfile +
    check `game.pse` + `game.bcm` in `namelist()`; read + parse the .bcm via
    `persistence.parse_bcm_dict(zf.read('game.bcm'))`; check `magic`, `version
    == 1`, `kind == 'checkpoint'`, `target_object == '1ubq'`,
    `len(registry.hiders) == 3`, `statuses.count('found') == 1`,
    `abs(timer_elapsed - 42.5) < 1.0`, `reveal_count == 1`, `hint_count == 2`.

    **Section D — LOAD + VERIFY SCOPED SAVE** (backup excluded): `cmd.delete(
    "all")` (clear session); extract the .pse from the .bcmz to a temp dir
    + `cmd.load(pse_path)` (partial=0 default — REPLACE for the clean-check);
    check `"1ubq" in cmd.get_names("objects")`; check
    `"_bchm_backup" not in cmd.get_names("all_objects")` (the load-bearing
    scoped-save assertion); check
    `cmd.count_atoms("1ubq and segi GAME") == 3` (sentinels survived).

    **Section E — RECONSTRUCT + RECONCILE**: build `gc2 = game.GameController(
    "1ubq")`; `gc2.reconstruct_registry()` (sentinel rebuild — rep=None, all
    hidden); check `len(gc2.registry.all()) == 3`; check all `rep is None` +
    all `status == 'hidden'`; read the .bcm from the .bcmz; `mismatches =
    persistence.apply_bcm_dict(gc2, bcm_dict)`; check
    `mismatches.missing_from_bcm == []`, `missing_from_pse == []`,
    `bad_rep == []`; check post-reconcile: 1 found, 2 hidden, all
    `rep == 'spheres'`; check `gc2._reveal_count == 1`, `_hint_count == 2`,
    `_found_color == 'green'`.

    **Section F — COUNTS_BY_REP RECOVERED**: `gc2.registry.counts_by_rep()`
    -> `spheres == 3` (NOT 0 — the rep=None limitation is fixed by reconcile).

    **Section G — FOUND-MGMT SELECTION RECOVERED**: `found = [r for r in
    gc2.registry.all() if r.status == HIDER_STATUS_FOUND]`;
    `build_found_selection(found, "1ubq")` -> not None;
    `group_found_by_rep(found)` -> `{'spheres': [id]}`;
    `cmd.count_atoms(sele)` -> 1.

    **Section H — FULL IMPORT_GAME ROUND-TRIP** (via the GUI's path, minus
    Qt): `cmd.delete("all")`; `pse_path, bcm_dict = persistence.read_bcmz(
    bcmz_path)`; `names_before = set(cmd.get_names('public_objects',
    enabled_only=True))`; `cmd.load(pse_path, partial=1)` (MERGE — the
    Discrepancy 1 choice); `target_obj = persistence.resolve_target(bcm_dict,
    names_before, demos_list)` where `demos_list = [n for n in
    cmd.get_names('public_objects', enabled_only=True) if cmd.get_type(n)
    == 'object:molecule']`; check `target_obj == '1ubq'`;
    `gc3 = game.GameController(target_obj)`; `gc3.import_state(bcm_dict)`;
    check `gc3._started == True`, `gc3._is_imported == True`,
    `gc3._backup_name == '_bchm_backup'`, `gc3._imported_bcm == bcm_dict`;
    check `len(gc3.registry.all()) == 3`, 1 found, 2 hidden, all rep='spheres'.

    **Section I — RESTART-ON-IMPORTED** (cmd layer): mirror
    `_on_restart_imported`: `backup.restore(gc3.target_obj, gc3._backup_name)`;
    `backup.discard(gc3._backup_name)`; `gc3.reconstruct_registry()`;
    `persistence.apply_bcm_dict(gc3, gc3._imported_bcm)`; `gc3._reveal_count
    = 0; gc3._hint_count = 0; gc3._start_time = None`;
    `gc3._backup_name = backup.snapshot(gc3.target_obj)`.
    NOTE: gc3 is a CHECKPOINT (1 found, per Section H). After
    `apply_bcm_dict(gc3, gc3._imported_bcm)`, reconcile_with_bcm restores the
    checkpoint's found-status (1 found) — NOT all-hidden. (For a puzzle
    import, the .bcm has all-hidden, so restart-on-imported would restore to
    0 found — but gc3 is a checkpoint, so we check the checkpoint path here.)
    `n_found = sum(1 for r in gc3.registry.all() if r.status == HIDER_STATUS_FOUND)`;
    check `n_found == 1` ("restart-on-imported: found-status restored from .bcm");
    check `gc3._started == True`.

    **Section J — CLEANUP-ON-IMPORTED** (two-step): `backup.restore(
    gc3.target_obj, gc3._backup_name)`; `backup.discard(gc3._backup_name)`;
    `mutation.cleanup_hiders(gc3.target_obj)`; check
    `cmd.count_atoms("1ubq and segi GAME") == 0`; check
    `cmd.count_atoms("1ubq") == orig_count` (hiders gone, count back to orig).

    **Section K — PUZZLE (Generate & export) ROUND-TRIP**: `cmd.delete(
    "all")`; `cmd.fetch("1ubq", async_=0)`; `gc4 = game.GameController("1ubq")`;
    `gc4.start([([20.0, 20.0, 20.0], "spheres")])`; build_bcm_dict with
    `kind='puzzle'`; cmd.save(pse_path, "1ubq") + write_bcmz; `gc4.cleanup()`
    (educator cleans up); read the .bcmz + check `kind == 'puzzle'`,
    `started == False`, `timer_elapsed == 0.0`, all `status == 'hidden'`;
    `cmd.delete("all")`; `cmd.load(pse_path, partial=1)` +
    `gc5 = game.GameController("1ubq")`; `gc5.import_state(bcm_dict)`; check
    all `status == 'hidden'`, `gc5._started == True`, `gc5._start_time is
    None` (puzzle = fresh start).

    **Section L — COLLISION DETECTABILITY**: `cmd.fetch("1ubq", async_=0)`;
    `loaded = [n for n in cmd.get_names('public_objects', enabled_only=True)
    if cmd.get_type(n) == 'object:molecule']`; check `'1ubq' in loaded` (the
    refuse-first check would fire — GUI-only, but the cmd-layer
    detectability is verified here).

    **Section M — SUMMARY**: `n_pass = sum(1 for _, ok in RESULTS if ok)`;
    `print("\n=== phase8_smoke: %d/%d PASS, %d FAIL ===" % (n_pass,
    len(RESULTS), len(RESULTS) - n_pass))`; `sys.exit(1 if n_pass <
    len(RESULTS) else 0)`.

    After writing the smoke:
    1. `python3.6 -m py_compile smoke/phase8_smoke.py && echo OK` (syntax).
    2. Stage: `bash wsl2win_cp.sh` (copies `biochemeleon/` to
       `tmp/bioCHEMeleon/biochemeleon/`).
    3. Stage the smoke: `mkdir -p tmp/bioCHEMeleon/smoke && cp smoke/phase8_smoke.py tmp/bioCHEMeleon/smoke/`.
    4. Run headlessly:
       `cd tmp/bioCHEMeleon && timeout 120 cmd.exe /c "C:\\src\\run-conda-pymol.bat -cq smoke\\phase8_smoke.py" 2>&1 | tail -50`
    5. Check exit code: 0 = ALL PASSED; nonzero = a check FAILED (read the
       FAIL lines + triage). If a check fails, read the research's Open
       Risks section for the likely cause + the fallback.
    6. If ALL PASSED, commit:
       `git add smoke/phase8_smoke.py`
       `git commit -m "test(08-05): headless smoke for export/import round-trip"`
  </action>
  <verify>
    python3.6 -m py_compile smoke/phase8_smoke.py && echo "PY_COMPILE_OK"
    bash wsl2win_cp.sh && mkdir -p tmp/bioCHEMeleon/smoke && cp smoke/phase8_smoke.py tmp/bioCHEMeleon/smoke/
    cd tmp/bioCHEMeleon && timeout 120 cmd.exe /c "C:\\src\\run-conda-pymol.bat -cq smoke\\phase8_smoke.py" 2>&1 | tail -50
    # Exit 0 = ALL PASSED. Look for "=== phase8_smoke: N/N PASS, 0 FAIL ===".
    git log --oneline -1
  </verify>
  <done>
    smoke/phase8_smoke.py written + staged + run headlessly. Exit 0 (ALL
    PASSED). The scoped-save (backup excluded), sentinel rebuild, reconcile
    rep+found-status, timer resume math, Restart/Cleanup-on-imported,
    puzzle round-trip, collision detectability are all verified at the cmd
    layer. Commit: `test(08-05): headless smoke for export/import round-trip`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 8 GUI wiring (Plan 04): the 3 buttons (Generate & export on Setup;
    Import puzzle… + Save checkpoint on Game) + the timer-resume fix + the
    Restart/Cleanup-on-imported routing. The headless smoke (Task 1) verified
    the cmd-coupled round-trip (scoped save, sentinel rebuild, reconcile,
    import_state, Restart/Cleanup-on-imported). This checkpoint verifies the
    Qt GUI paths that CANNOT run headlessly: QFileDialog, tab-switching,
    3-2-1 countdown, PickWizard click-to-find, timer display, found-mgmt
    dropdown after reload.
  </what-built>
  <how-to-verify>
    **Setup**: Launch Windows PyMOL via `setenv.bat` → `pymol`. Load the
    bioCHEMeleon plugin (Plugins menu → bioCHEMeleon). Load a molecule (e.g.
    `fetch 1ubq` or use a bundled demo). Open the plugin dialog.

    **Success Criterion 1 — Save mid-game + reload** (GAME-09):
    1. On the Setup tab, configure a small game (e.g. 3 sphere hiders on
       1ubq). Click Start. Play partway (find 1-2 hiders; use Hint/Reveal if
       you like). Note the timer value (e.g. 0:42) + the number found.
    2. On the Game tab, click "Save checkpoint". A QFileDialog opens. Pick
       a save path (e.g. `~/test_checkpoint.bcmz`). Click Save.
       - EXPECT: a success log line "Saved checkpoint to ...". The timer
         continues from where it was (the dialog+save time is NOT counted
         — the pause-capture-dialog-save-resume fix).
    3. Close PyMOL. Reopen PyMOL + the plugin. Switch to the Game tab. Click
       "Import puzzle…". Pick the `test_checkpoint.bcmz` you just saved.
       - EXPECT: the game loads. The timer shows ~0:42 (the resumed elapsed)
         during the 3-2-1 countdown, then counts up (43, 44, ...). The
         found-hiders from the checkpoint are still found (visible in their
         found color). The remaining count is correct (3 - found_count).
         The reveal/hint counters match the checkpoint.
    4. Click a remaining hider in the viewer. EXPECT: "Found one!" + the
       hider recolors. The remaining count decrements. Find all remaining
       hiders. EXPECT: the win dialog fires with the resumed+continued
       elapsed time.

    **Success Criterion 2 — Generate & export** (BTN-05):
    1. On the Setup tab, configure a puzzle (e.g. 3 sphere hiders on 1ubq).
       Click "Generate & export" (between "Load Setup…" and "Cleanup model").
       - EXPECT: a QFileDialog opens ("Generate & export puzzle"). Pick a
         save path (e.g. `~/test_puzzle.bcmz`). Click Save.
       - EXPECT: a success dialog "Puzzle exported to ... Your model still
         has the generated hiders. Press Cleanup to restore your scene."
       - EXPECT: you stay on the Setup tab (NOT switched to Game — no
         countdown, no play).
    2. Visually verify the generated hiders are in the viewer (3 sphere
       hiders on 1ubq).
    3. Click "Cleanup model" (Setup tab). EXPECT: the hiders are removed;
       the model is back to its pre-export state (atom count restored).

    **Success Criterion 3 — Import + play** (GAME-04):
    1. (Continuing from Criterion 2, or after re-exporting.) Close + reopen
       PyMOL + the plugin (fresh session). Switch to the Game tab. Click
       "Import puzzle…". Pick `test_puzzle.bcmz`.
       - EXPECT: the puzzle loads. The 3-2-1 countdown runs. The timer
         starts at 0:00 (puzzle = fresh start, NOT resumed). The PickWizard
         activates (mouse enters picking mode).
    2. Click a hider in the viewer. EXPECT: "Found one!" + recolor + the
       remaining count decrements. Find all 3 hiders. EXPECT: the win
       dialog fires with the solve time.

    **Restart-on-imported**:
    1. After importing a puzzle (Criterion 3) + finding some hiders, click
       "Restart" (Game tab).
       - EXPECT: the game restarts to the imported initial state (all
         hiders hidden, timer 0:00). The hiders are NOT re-generated (same
         positions as the imported puzzle). The countdown runs + play
         resumes. (This verifies _on_restart_imported — NO re-generation.)

    **Cleanup-on-imported**:
    1. After importing + playing, click "Cleanup model" (Setup tab).
       - EXPECT: the model returns to a clean state (no hiders, no hint-orange
         real atoms). The atom count matches the original 1ubq. (This
         verifies the two-step: backup.restore + mutation.cleanup_hiders.)

    **Found-mgmt after reload**:
    1. Import a checkpoint with 2 found hiders (Criterion 1). Open the
       found-mgmt dropdown (Game tab). Click "Show found".
       - EXPECT: the 2 found hiders are shown in their ORIGINAL rep (e.g.
         spheres), NOT skipped. (This verifies the rep reconciliation —
         without reconcile, group_found_by_rep skips rep=None records and
         "Show found" shows nothing.)
    2. Click "Recolor found". EXPECT: both found hiders recolor to the
       found_color. Click "Hide found". EXPECT: both hide.

    **Custom found-color round-trip (Open Risk 1)**:
    1. Start a game, pick a custom found-color via the Color… button
       (Phase 7). Find 1-2 hiders (they show the custom color). Save
       checkpoint. Close + reopen + Import. EXPECT: the found hiders still
       show the custom color (the .pse preserved it, OR the
       found_color_rgb safety net re-registered it). If the custom color is
       lost (shows a fallback), note it — the found_color_rgb safety net
       needs the GUI handler to populate it (a follow-up if broken).

    **Collision refuse (Discrepancy 1)**:
    1. Load `1ubq` in PyMOL. Import a `test_checkpoint.bcmz` whose
       `target_object == '1ubq'`.
       - EXPECT: a "Name collision" dialog: "An object named '1ubq' is
         already loaded. Please rename or delete it before importing this
         game." The import does NOT proceed. Rename `1ubq` (e.g. via
         `cmd.set_name('1ubq', '1ubq_orig')`) + retry the import — it now
         succeeds (the partial=1 MERGE loads the puzzle's 1ubq alongside
         your 1ubq_orig).

    If ALL of the above work, Phase 8 is COMPLETE. If any step fails,
    describe the issue (what you did, what you expected, what you saw) —
    the planner will triage + create a gap-closure plan.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to triage</resume-signal>
</task>

</tasks>

<verification>
Phase 8 Plan 05 verification:
- [ ] smoke/phase8_smoke.py written + py_compile clean
- [ ] Headless smoke run: exit 0 (ALL PASSED) — scoped save, sentinel rebuild,
      reconcile, import_state, Restart/Cleanup-on-imported, puzzle round-trip
- [ ] Human-verify: 3 success criteria (Save+reload, Generate&export, Import+play)
- [ ] Human-verify: Restart-on-imported (no re-generation)
- [ ] Human-verify: Cleanup-on-imported (two-step, clean molecule)
- [ ] Human-verify: Found-mgmt after reload (Show found shows found in their rep)
- [ ] Human-verify: Timer resume display (checkpoint shows saved elapsed, counts up)
- [ ] Human-verify: Collision refuse (Discrepancy 1 — refuse-first dialog fires)
- [ ] Human-verify: Custom found-color round-trip (Open Risk 1 — color survives)
</verification>

<success_criteria>
- smoke/phase8_smoke.py exits 0 headlessly (~30-40 checks ALL PASSED)
- Human-verify APPROVED for all 3 Phase 8 success criteria + imported-game lifecycle
- Open Risks confirmed (scoped save, partial=1 merge, custom color round-trip, collision)
- Phase 8 COMPLETE (3 success criteria met: Save/load, Generate&export, Import+play)
</success_criteria>

<output>
After completion, create `.planning/phases/08-persistence-and-shareable-puzzles/08-05-SUMMARY.md`
</output>
