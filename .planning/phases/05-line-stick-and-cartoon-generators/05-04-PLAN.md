---
phase: 05-line-stick-and-cartoon-generators
plan: 04
type: execute
wave: 3
depends_on:
  - "05-03"
files_modified:
  - smoke/phase5_smoke.py
autonomous: true

must_haves:
  truths:
    - "Headless smoke: line/stick GAME atoms appear in rep sticks after insert_line_stick_hider + cmd.show (count_atoms > 0)"
    - "Headless smoke: cartoon GAME C-alpha appears in rep cartoon after insert_cartoon_hider + cmd.show (count_atoms > 0) — the corrected count_atoms('... and rep cartoon') check (NOT the roadmap's wrong cmd.count('cartoon',...))"
    - "Headless smoke: GAME atoms are in the polymer selector after attach_amino_acid + alter segi='GAME' (Open Risk 1 MEDIUM — count_atoms('obj and segi GAME and polymer') > 0)"
    - "Headless smoke: the new cartoon residue has N-C-C-alpha backbone (count_atoms >= 3)"
    - "Headless smoke: sentinel set on all new hiders (segi=GAME, b=-999 via iterate)"
    - "Headless smoke: hider color matches neighbor color (Open Risk 3 MEDIUM — iterate neighbor C-alpha color + hider C-alpha color, assert equal)"
    - "Headless smoke: attach_amino_acid works with a NAMED selection (Open Risk 2 MEDIUM — no pk1 needed)"
    - "Headless smoke: cleanup_hiders restores orig count + verify_intact True (structure intact after sentinel remove)"
    - "Headless smoke: insert_hider_for_rep dispatches correctly for all 3 rep families (spheres, lines/sticks, cartoon/ribbon)"
  artifacts:
    - path: "smoke/phase5_smoke.py"
      provides: "Phase 5 headless smoke — line/stick + cartoon + mixed-rep + 3 MEDIUM open-risk assertions + cleanup safety"
      contains: "insert_cartoon_hider"
      min_lines: 120
  key_links:
    - from: "smoke/phase5_smoke.py"
      to: "headless Windows PyMOL"
      via: "bash wsl2win_cp.sh; cd tmp/bioCHEMeleon; timeout 120 cmd.exe /c C:\\src\\run-conda-pymol.bat -cq smoke\\phase5_smoke.py"
      pattern: "cmd\\.attach_amino_acid|cmd\\.bond|count_atoms.*rep"
---

<objective>
Write and run a headless smoke test (`smoke/phase5_smoke.py`) that verifies the
Phase 5 line/stick + cartoon + mixed-rep insertion at runtime via the Windows
headless PyMOL path (`cmd.exe /c C:\\src\\run-conda-pymol.bat -cq`). The smoke
asserts the 6+ visibility/polymer/sentinel/color/cleanup checks from
05-RESEARCH.md §6 Q21, AND surfaces the 3 MEDIUM open risks as gated assertions
(Open Risk 1: segi-doesn't-break-polymer; Open Risk 2: attach_amino_acid with
named selection; Open Risk 3: color matches neighbor). If a MEDIUM risk fails,
iterate the fallback (mirrors the Phase 3 03-15 smoke-debug pattern).

Purpose: The pure generators are WSL-unit-tested (plan 05-01), and the
cmd-coupled inserters are syntax-checked (plan 05-02), but the actual
`cmd.bond` / `cmd.attach_amino_acid` / `cmd.show` / `count_atoms("... and rep
...")` behavior can ONLY be verified at the PyMOL runtime tier. The smoke
closes this gap headlessly (AGENTS.md: pure `pymol.cmd.*` runs headlessly via
`cmd.exe /c run-conda-pymol.bat -cq`; NO Qt). The visual blend (success
criterion 3) is the human-verify checkpoint (plan 05-05) — the smoke proves the
MECHANISM, the human proves the BLEND.

Output: `smoke/phase5_smoke.py` (~140 lines, pure `pymol.cmd.*` — NO Qt).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-line-stick-and-cartoon-generators/05-RESEARCH.md
@AGENTS.md

# The Phase 4 smoke (the pattern to mirror — check/pass/summary + headless run):
@smoke/phase4_smoke.py

# The Phase 3 smoke (the iterate/count_atoms/sentinel pattern):
@smoke/phase3_smoke.py

# The modules under test:
@biochemeleon/generators.py
@biochemeleon/mutation.py
@biochemeleon/game.py
@biochemeleon/registry.py
</context>

<tasks>

<task type="auto">
  <name>Write + run headless smoke test for line/stick + cartoon + mixed-rep + 3 open risks</name>
  <files>smoke/phase5_smoke.py</files>
  <action>
    Create `smoke/phase5_smoke.py` modeled on `smoke/phase4_smoke.py` (same
    `RESULTS`/`check(name, cond)`/`=== SUMMARY ===` pattern, `sys.exit(1)` on
    fail). Use `from biochemeleon import generators, game, registry, mutation`.
    Pure `pymol.cmd.*` only — NO Qt (AGENTS.md: headless path cannot run Qt).
    The script MUST cover these sections:

    **--- 1. setup: fetch 1ubq + capture orig count ---**
    `cmd.fetch("1ubq", async_=0)`; `obj = "1ubq"`;
    `orig_count = cmd.count_atoms(obj)`.

    **--- 2. LINE/STICK hider via insert_line_stick_hider ---**
    - Pick a real neighbor atom id: `nbr_ids = cmd.identify("%s and not segi GAME and name CA" % obj, mode=0)`; `neighbor_id = nbr_ids[0]`.
    - `offset = [0.5, 0.5, 0.5]` (a fixed small offset for reproducibility).
    - `stick_id = mutation.insert_line_stick_hider(obj, offset=offset, neighbor_id=neighbor_id, handle="S001", rep="sticks")`.
    - `check("stick: returns int id", isinstance(stick_id, int))`.
    - `cmd.show("sticks", "%s and id %d" % (obj, stick_id))` — NOTE: insert_line_stick_hider already shows, but re-showing by id is idempotent; OR rely on the insert's show. To be safe, assert after the insert's own show:
    - `check("stick: GAME atoms in rep sticks", cmd.count_atoms("%s and segi GAME and rep sticks" % obj) > 0)`.
    - `check("stick: sentinel set", ...)` — iterate `"%s and id %d" % (obj, stick_id)`, read `(segi, b)`, assert `segi == 'GAME'` and `abs(b - (-999.0)) < 1e-6`.
    - `check("stick: hider bonded to neighbor", ...)` — `cmd.count_atoms("%s and id %d and bound_to id %d" % (obj, stick_id, neighbor_id)) > 0` OR use a neighbor/bound selector; if the `bound_to` selector syntax is uncertain, fall back to `cmd.count_atoms("(neighbor %s and id %d) and segi GAME" % (obj, stick_id)) > 0` (the hider is a neighbor of the neighbor) — document whichever works.
    - `check("stick: color matches neighbor (blend)", ...)` — iterate the neighbor's color and the hider's color, assert equal: `nbr_col = []; cmd.iterate("%s and id %d" % (obj, neighbor_id), "stored.append(color)", space={'stored': nbr_col})`; `hdr_col = []; cmd.iterate("%s and id %d" % (obj, stick_id), "stored.append(color)", space={'stored': hdr_col})`; `check("stick: color matches neighbor", nbr_col and hdr_col and nbr_col[0] == hdr_col[0])` (Open Risk 3 for line/stick).

    **--- 3. CARTOON hider via insert_cartoon_hider ---**
    - Find the C-terminus: `cas_list = []; cmd.iterate("%s and polymer and name CA" % obj, "stored.append((chain, int(resi), ID))", space={'stored': cas_list})`; group by chain; pick the longest chain's max resi: `chain = max(cas_by_chain, key=lambda c: len(cas_by_chain[c]))`; `term_resi = max(r[0] for r in cas_by_chain[chain])`; `is_c_terminus = True`.
    - `ca_id = mutation.insert_cartoon_hider(obj, chain=chain, terminus_resi=term_resi, is_c_terminus=True, handle="C001")`.
    - `check("cartoon: returns C-alpha int id", isinstance(ca_id, int))`.
    - **Open Risk 2 (MEDIUM):** `check("cartoon: attach_amino_acid worked with named sele (Open Risk 2)", ca_id > 0 and cmd.count_atoms("%s and segi GAME and name CA" % obj) > 0)` — if this FAILS (attach needs pk1), the fix is to add `cmd.edit(terminus_sele)` before attach_amino_acid inside insert_cartoon_hider; iterate the mutation.py fix and re-run.
    - **Open Risk 1 (MEDIUM):** `check("cartoon: GAME atoms in polymer (Open Risk 1)", cmd.count_atoms("%s and segi GAME and polymer" % obj) > 0)` — if this FAILS (alter segi='GAME' broke the polymer trace), the fallback is to change the cartoon sentinel to `resn='HIDER'` + `b=-999` (keep segi = neighbor's segi) and migrate the cleanup selector; document the fallback decision in the SUMMARY.
    - `check("cartoon: GAME C-alpha in rep cartoon", cmd.count_atoms("%s and segi GAME and name CA and rep cartoon" % obj) > 0)` (mutagenesis.py:570 pattern; the corrected count_atoms check — NOT the roadmap's wrong `cmd.count('cartoon',...)`).
    - `check("cartoon: residue has N-C-C-alpha backbone", cmd.count_atoms("%s and segi GAME and (name N or name CA or name C)" % obj) >= 3)`.
    - `check("cartoon: sentinel set", ...)` — iterate the new residue, assert segi=GAME + b=-999.
    - **Open Risk 3 (MEDIUM):** `check("cartoon: C-alpha color matches neighbor (blend)", ...)` — iterate the neighbor (terminal residue) Cα color and the new Cα color, assert equal. The neighbor Cα is `"%s and chain %s and resi %d and name CA" % (obj, chain, term_resi)` (the original terminal residue, NOT the new one). The new Cα is `"%s and id %d" % (obj, ca_id)`. Assert colors equal. If the `cartoon_color` setting overrides, the explicit color may not show — document.

    **--- 4. MIXED-REP via insert_hider_for_rep + GameController.start ---**
    - Clean up the single-hider tests first: `mutation.cleanup_hiders(obj)` + verify count restored.
    - Build a mixed-rep hider_specs list directly (NOT via _on_start — that's Qt-coupled; the smoke tests the cmd path):
      ```python
      extent = cmd.get_extent(obj)
      sphere_pos = generators.generate_sphere_positions(extent, 1, seed=42)[0]
      nbr_ids = cmd.identify("%s and not segi GAME and name CA" % obj, mode=0)
      line_off = generators.generate_line_stick_offsets(1, seed=42)[0]
      cas = []; cmd.iterate("%s and polymer and name CA" % obj, "stored.append((chain, int(resi), ID))", space={'stored': cas})
      cas_by = {}
      for ch, ri, cid in cas: cas_by.setdefault(ch, []).append((ri, cid))
      terminals = generators.pick_terminal_residues(cas_by, max_chains=1)
      term = terminals[0] if terminals else None
      hider_specs = [(sphere_pos, "spheres"), ((line_off, nbr_ids[0]), "sticks")]
      if term:
          hider_specs.append((term, "cartoon"))
      ```
    - `gc = game.GameController(obj); gc.start(hider_specs)`.
    - `check("mixed: start succeeded (backup returned)", gc._backup_name is not None)`.
    - `check("mixed: registry len == len(hider_specs)", len(gc.registry.all()) == len(hider_specs))`.
    - `check("mixed: counts_by_rep has each rep", all(gc.registry.counts_by_rep().get(rep, 0) >= 1 for _, rep in hider_specs))`.
    - `check("mixed: GAME atoms in rep spheres", cmd.count_atoms("%s and segi GAME and rep spheres" % obj) > 0)`.
    - `check("mixed: GAME atoms in rep sticks", cmd.count_atoms("%s and segi GAME and rep sticks" % obj) > 0)`.
    - `check("mixed: GAME atoms in rep cartoon", cmd.count_atoms("%s and segi GAME and rep cartoon" % obj) > 0)` (only if term was added).

    **--- 5. CLEANUP safety ---**
    - `intact = gc.cleanup()`.
    - `check("cleanup: returned True", intact is True)`.
    - `check("cleanup: count back to orig", cmd.count_atoms(obj) == orig_count)`.
    - `check("cleanup: no GAME atoms remain", cmd.count_atoms("%s and segi GAME" % obj) == 0)`.

    **--- 6. (optional spike) iterate_state exposes elem/color? ---**
    - LOW risk (Open Risk 6): wrap in try/except. Test whether
      `cmd.iterate_state(1, "%s and id %d" % (obj, nbr_ids[0]), "stored.append((elem, color))", space={'stored': spike})`
      returns non-None elem/color. If it returns None, the insert_line_stick_hider
      needs to split into iterate_state (coords) + iterate (elem/color). Document
      the finding; a FAIL here is acceptable (informational).

    Then STAGE + RUN headlessly per AGENTS.md:
    ```bash
    bash wsl2win_cp.sh
    mkdir -p tmp/bioCHEMeleon/smoke && cp smoke/phase5_smoke.py tmp/bioCHEMeleon/smoke/
    cd tmp/bioCHEMeleon && timeout 120 cmd.exe /c "C:\\src\\run-conda-pymol.bat -cq smoke\\phase5_smoke.py" 2>&1 | tail -60
    ```
    Always `cd` into the staged Windows path first (AGENTS.md). Check exit code:
    0 = clean; nonzero = crash. Triage any failure:
    - A MEDIUM open risk fails (Open Risk 1/2/3) → iterate the fallback in
      mutation.py (documented above), re-stage + re-run. Mirrors Phase 3 03-15
      smoke-debug pattern.
    - A Rule-1 library bug (e.g., `bound_to` selector syntax) → fix the smoke
      assertion.
    - A real Phase-5 bug → fix the source module (mutation.py / game.py /
      generators.py) and re-stage + re-run.
    Re-run until ALL main checks pass (the optional spike may log a deferral
    without failing the run).

    Commit: `git add smoke/phase5_smoke.py && git commit -m "test(05-04): add phase5 headless smoke (line/stick + cartoon + mixed-rep + 3 open risks)"`
  </action>
  <verify>
    The headless smoke run prints `N/N passed` with `ALL PASSED` and exits 0
    (the optional iterate_state spike may either pass or log a deferral — both
    acceptable; only the main checks must all pass). Capture the tail output.

    Also confirm WSL-side:
    - `python3.6 -m py_compile smoke/phase5_smoke.py` clean
    - `python3.6 -m unittest tests.test_setup_state tests.test_registry tests.test_generators tests.test_game_controller -v` all pass (no regression)

    If a MEDIUM open risk required a fallback (e.g., Open Risk 1 segi breaks
    polymer → switched to resn='HIDER' sentinel), the mutation.py change is part
    of THIS plan's iteration — commit the fix + the smoke together, and
    document the fallback decision in the SUMMARY (which sentinel, why, and
    that cleanup_hiders + fetch_all_hider_ids were migrated if needed).
  </verify>
  <done>
    smoke/phase5_smoke.py exists (~140 lines, pure pymol.cmd.* — NO Qt); staged
    to tmp/bioCHEMeleon/; headless run via cmd.exe /c run-conda-pymol.bat -cq
    prints ALL PASSED (main checks pass; optional spike either passes or logs a
    deferral); the 3 MEDIUM open risks are resolved (pass, or fallback applied +
    documented); committed.
  </done>
</task>

</tasks>

<verification>
Phase 5 is runtime-verified (headless tier) when:
- Headless smoke prints ALL PASSED for the main checks (line/stick in rep sticks,
  cartoon Cα in rep cartoon + polymer + N-C-Cα backbone, sentinel set, color
  matches neighbor, mixed-rep registry counts, cleanup restores orig) and exits 0
- The 3 MEDIUM open risks are resolved:
  - Open Risk 1: `count_atoms("obj and segi GAME and polymer") > 0` passes (OR
    the resn='HIDER' fallback is applied + documented + cleanup migrated)
  - Open Risk 2: attach_amino_acid with a named selection works (OR cmd.edit
    fallback applied + documented)
  - Open Risk 3: hider Cα color == neighbor Cα color (documented; human-verify
    confirms the visual blend in plan 05-05)
- WSL unit tests still pass (no regression from any fallback iteration)
</verification>

<success_criteria>
- Phase 5 line/stick + cartoon + mixed-rep insertion is proven at the headless
  cmd-coupled tier (WSL-runnable, automated).
- The 3 MEDIUM open risks are resolved (pass or documented fallback).
- Ready for plan 05-05 (human-verify checkpoint) to confirm the visual blend
  (success criterion 3) in a real PyMOL GUI session.
</success_criteria>

<output>
After completion, create `.planning/phases/05-line-stick-and-cartoon-generators/05-04-SUMMARY.md`
</output>
