---
phase: 05-line-stick-and-cartoon-generators
verified: 2026-08-09T15:39:28Z
status: passed
score: 5/5 must-haves verified
re_verification:
  previous_status: partial (05-05 human-verify PARTIAL APPROVAL)
  previous_score: 3/5 (cartoon-disconnected + line/stick IndexError + ribbon unsupported were the 3 gaps)
  gaps_closed:
    - "Line/stick IndexError (gap #2) — closed via 05-07 (cherry-picked)"
    - "Ribbon rep unsupported (gap #3) — closed via 05-09 (this verification confirms the fix)"
  gaps_remaining: []
  regressions: []  # all previously-passing checks still pass (33 existing + 8 new = 41/41)
  deferred_by_design:
    - "Cartoon terminal-extension renders disconnected on 1ubq (gap #1) — DEFERRED to Phase 11 (alt-conf segment replication). NOT a v1 gap: the criterion's literal requirement (render visibly, cmd.count('cartoon','obj and segi GAME') > 0) IS met; the disconnected look is cosmetic and the game is playable."
---

# Phase 5: Line/Stick & Cartoon Generators Verification Report

**Phase Goal:** Hiders can blend into line/stick and cartoon/ribbon representations, not just spheres. Cartoon/ribbon is the "L"-complexity swing feature (novel C-alpha geometry) and the phase most likely to need deeper research.
**Verified:** 2026-08-09T15:39:28Z
**Status:** **passed** — Phase 5 v1 scope complete; alt-conf cosmetic gap deferred to Phase 11 by design.
**Re-verification:** Yes — after gap closure (05-07 line/stick + 05-09 ribbon). The 05-05 PARTIAL identified 3 gaps; 05-07 closed gap #2, 05-09 closed gap #3; gap #1 (disconnected cartoon on 1ubq) is formally deferred to the new Phase 11 added to the roadmap.

## Goal Achievement

### Observable Truths (Phase 5 Success Criteria from ROADMAP.md)

| # | Truth (criterion) | Status | Evidence |
|---|-------------------|--------|----------|
| 1 | Line/stick hiders are generated as new atoms that mimic connected atoms or occupy alternate positions, blending with the line/stick representation | ✓ VERIFIED | `mutation.py:164-249` `insert_line_stick_hider` (substantive, ~85 lines): inserts pseudoatom near neighbor, bonds it (same-object bond, `editing.py:717`), shows rep by id. Headless smoke section 2 PASS: `stick: GAME atoms in rep sticks`, `stick: hider bonded to neighbor`, `stick: color matches neighbor (Open Risk 3)`. Mixed-rep smoke section 4 PASS: `mixed: GAME atoms in rep sticks`. |
| 2 | Cartoon/ribbon hiders are generated by extending at a terminal or replicating a segment (loop) as an alternate position using C-alpha geometry, and render visibly under the cartoon representation (verified: `cmd.count('cartoon', 'obj and segi GAME') > 0`) | ✓ VERIFIED (v1 scope) | Two halves — **Cartoon (terminal-extension):** `mutation.py:252-480` `insert_cartoon_hider` (substantive, ~228 lines): attaches TWO glycine residues at a terminus via `editor.attach_amino_acid` (C-alpha geometry). Smoke section 3 PASS: `cartoon: GAME C-alpha in rep cartoon`, `cartoon: 2 GAME C-alphas in rep cartoon (2-residue tube, 05-05)`. The roadmap's literal count assertion is met via the corrected `count_atoms("... and name CA and rep cartoon") > 0` pattern (the roadmap's `cmd.count('cartoon', ...)` is wrong API — mutagenesis.py:570 pattern). **Ribbon (05-09 gap closure):** `insert_cartoon_hider` now accepts `rep='cartoon'` (line 254) and the dispatcher forwards `rep=rep` (line 536); the show call is parameterized `cmd.show(rep, ...)` (line 479, was hardcoded `'cartoon'`). Smoke section 5c PASS: `ribbon: GAME C-alpha in rep ribbon`, `ribbon: 2 GAME C-alphas in rep ribbon`, `ribbon: rep ribbon ONLY on GAME atoms`. The alt-conf segment-replication alternative ("or replicating a segment") was attempted (05-06/05-08) and REVERTED — formally deferred to Phase 11; the terminal-extension approach satisfies the "extending at a terminal" half of the criterion. The disconnected-on-1ubq cosmetic look is a KNOWN, DOCUMENTED, ACCEPTED limitation (deferred to Phase 11) — NOT flagged as a gap per the verification scope. |
| 3 | A game started with mixed representations (sphere + line/stick + cartoon) produces hiders in each selected rep, all tracked in the registry | ✓ VERIFIED | Headless smoke section 4 (mixed-rep via `GameController.start`): builds `hider_specs = [(sphere_pos, "spheres"), ((line_off, nbr_ids2[0]), "sticks")]` + `(term, "cartoon")`, calls `gc.start(hider_specs)`, asserts PASS: `mixed: registry len == len(hider_specs)`, `mixed: counts_by_rep has each rep`, `mixed: GAME atoms in rep spheres`, `mixed: GAME atoms in rep sticks`, `mixed: GAME atoms in rep cartoon`. 1znf 4-hider mixed-rep was additionally confirmed in the 05-05 human-verify. |

**Score:** 3/3 success criteria verified (all 3 Phase 5 criteria met within v1 scope; the alt-conf cosmetic gap is deferred to Phase 11 by design).

### 05-09 Must-Haves (Gap Closure Plan — the just-executed work)

| # | Truth | Status | Evidence (independently verified against codebase, NOT SUMMARY claim) |
|---|-------|--------|----------------------------------------------------------------------------|
| 1 | A ribbon-rep hider (cartoon-geometry insert with rep='ribbon') renders in the RIBBON representation, NOT cartoon | ✓ VERIFIED | `mutation.py:479` `cmd.show(rep, _id_sele(all_game_ids))` (parameterized; grep confirms ZERO remaining `cmd.show('cartoon'` literals in `biochemeleon/`). Headless smoke section 5c re-run INDEPENDENTLY (not trusting SUMMARY): `PASS: ribbon: GAME C-alpha in rep ribbon`, `PASS: ribbon: 2 GAME C-alphas in rep ribbon`, `PASS: ribbon: rep ribbon ONLY on GAME atoms (polymer has no ribbon default -> rep= forwarded)`. Diag: `GAME_rep_ribbon=2 GAME_rep_cartoon=13 polymer_rep_ribbon=0` — confirms ribbon shown on GAME atoms via the explicit `cmd.show('ribbon', GAME)` call, NOT a global show (polymer_rep_ribbon=0). |
| 2 | Existing cartoon hiders still render in cartoon (rep='cartoon' default preserved — no regression) | ✓ VERIFIED | `mutation.py:254` signature `segi='GAME', b=-999.0, rep='cartoon')` — the default preserves existing cartoon behavior (every existing caller is unaffected). Headless smoke section 3 re-run INDEPENDENTLY: `PASS: cartoon: GAME C-alpha in rep cartoon`, `PASS: cartoon: 2 GAME C-alphas in rep cartoon (2-residue tube, 05-05)` — no regression (these sections insert cartoon hiders WITHOUT passing `rep=`, relying on the default). |
| 3 | The insert_hider_for_rep dispatcher forwards rep='ribbon' to insert_cartoon_hider (rep=rep kwarg) | ✓ VERIFIED | `mutation.py:531-536`: `elif rep in ('cartoon', 'ribbon'):` → `return insert_cartoon_hider(object, chain=chain, terminus_resi=terminus_resi, is_c_terminus=is_c_terminus, handle=handle, rep=rep)`. The dispatcher receives `rep` from `game.py:41-43` (`for i, (payload, rep) in enumerate(hider_specs): ... mutation.insert_hider_for_rep(self.target_obj, rep, payload, handle)`), which receives it from `__init__.py:177` `hider_specs.append((term, rep))` for `rep in ('cartoon', 'ribbon')` from `per_rep`. Full pipeline wired end-to-end. |
| 4 | Headless smoke including the new ribbon section passes (ALL PASSED, exit 0) | ✓ VERIFIED INDEPENDENTLY | Re-ran headless smoke from WSL via `cmd.exe /c C:\\src\\run-conda-pymol.bat -cq smoke\\phase5_smoke.py` from staged `tmp/bioCHEMeleon` (not trusting the 05-09-SUMMARY claim). Output: `=== SUMMARY ===` `41/41 passed` `ALL PASSED`, `EXIT=0`. All 8 new ribbon checks PASS + all 33 existing checks PASS (no regression). |
| 5 | WSL gates stay green (py_compile all + 90 tests + Pitfall-1=0 + exec_=allowed-only) | ✓ VERIFIED INDEPENDENTLY | Re-ran all 4 gates from WSL: (a) `python3.6 -m py_compile biochemeleon/*.py` → `PY_COMPILE_OK`; (b) `python3.6 -m unittest tests.test_setup_state` → `Ran 90 tests ... OK`; (c) Pitfall-1 grep → ZERO matches (clean); (d) exec_ grep → 1 match on `gui_game.py:137` (QMessageBox.exec_() — ALLOWED per AGENTS.md; NOT the main dialog). |

**Score:** 5/5 must-haves verified.

### Required Artifacts (Three-Level Verification)

| Artifact | Expected | Exists | Substantive | Wired | Status | Details |
|----------|----------|--------|-------------|-------|--------|---------|
| `biochemeleon/mutation.py` | `insert_cartoon_hider` accepts `rep='cartoon'`; show call uses `rep`; `insert_hider_for_rep` forwards `rep=rep` | ✓ EXISTS | ✓ SUBSTANTIVE (620 lines) | ✓ WIRED | ✓ VERIFIED | Line 254 signature `rep='cartoon'`; line 479 `cmd.show(rep, _id_sele(all_game_ids))`; line 536 `handle=handle, rep=rep)`. No remaining `cmd.show('cartoon'` literal (grep returned 0 matches across `biochemeleon/`). Two `cmd.show(rep,` matches (lines 248 + 479) and two `handle=handle, rep=rep` matches (lines 530 + 536) are EXPECTED — line 248/530 are the pre-existing lines/sticks path, line 479/536 are the 05-09 cartoon/ribbon changes. Both correct, not discrepancies. |
| `smoke/phase5_smoke.py` | Section 5c — ribbon rep check | ✓ EXISTS | ✓ SUBSTANTIVE (342 lines) | ✓ WIRED (run by headless PyMOL) | ✓ VERIFIED | Section 5c at line 251 (`# --- 5c. RIBBON REP SUPPORT (05-09 gap closure: rep forwarding) ---`), 8 new `check()` calls. Regression guard was corrected per Rule 1 (the plan's `rep cartoon == 0` was unsound — cartoon is inherited from 1ubq's default polymer rep; replaced with `rep ribbon on GAME` + `rep ribbon NOT on polymer` which correctly isolates the explicit show call's effect). No Qt imports (pure `pymol.cmd.*`). |
| `biochemeleon/setup_state.py` | `GAME_REPS` includes `'ribbon'` (validates ribbon is a playable rep) | ✓ EXISTS | ✓ SUBSTANTIVE | ✓ WIRED | ✓ VERIFIED | Line 23 `GAME_REPS = ['lines', 'sticks', 'spheres', 'cartoon', 'ribbon']` — `ribbon` is a first-class rep. |
| `biochemeleon/game.py` | `GameController.start` iterates `(payload, rep)` and forwards `rep` to dispatcher + registry | ✓ EXISTS | ✓ SUBSTANTIVE (141 lines) | ✓ WIRED | ✓ VERIFIED | Lines 41-44: `for i, (payload, rep) in enumerate(hider_specs): ... aid = mutation.insert_hider_for_rep(self.target_obj, rep, payload, handle); self.registry.register(object=self.target_obj, id=aid, rep=rep)`. The rep flows from `hider_specs` into the dispatcher AND into the registry. |
| `biochemeleon/__init__.py` | `_on_start` builds `hider_specs` with `(term, rep)` for `rep in ('cartoon', 'ribbon')` | ✓ EXISTS | ✓ SUBSTANTIVE | ✓ WIRED | ✓ VERIFIED | Lines 161-177: `for rep, count in per_rep.items(): ... elif rep in ('cartoon', 'ribbon'): ... hider_specs.append((term, rep))`. The rep travels from `per_rep` into `hider_specs`. NOT modified by 05-09 (confirmed: `git diff --stat HEAD~3 HEAD` shows `__init__.py` unchanged in the 05-09 commits). |

### Key Link Verification (Rep-Forwarding Pipeline)

The full pipeline `_on_start` → `hider_specs` → `game.start` → `insert_hider_for_rep` → `insert_cartoon_hider(rep=rep)` → `cmd.show(rep, ...)`:

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `__init__.py:161` `for rep, count in per_rep.items()` | `__init__.py:177` `hider_specs.append((term, rep))` | `(term, rep)` tuple for `rep in ('cartoon', 'ribbon')` | ✓ WIRED | The requested rep (`'cartoon'` or `'ribbon'`) is packed into the `(payload, rep)` tuple. |
| `__init__.py:224` `self._controller.start(hider_specs)` | `game.py:41` `for i, (payload, rep) in enumerate(hider_specs)` | `hider_specs` list | ✓ WIRED | `hider_specs` passed to `GameController.start`. |
| `game.py:43` `mutation.insert_hider_for_rep(self.target_obj, rep, payload, handle)` | `mutation.py:483` `def insert_hider_for_rep(object, rep, payload, handle)` | positional `rep` arg | ✓ WIRED | Dispatcher receives `rep`. |
| `mutation.py:531-536` `elif rep in ('cartoon', 'ribbon'):` → `insert_cartoon_hider(..., handle=handle, rep=rep)` | `mutation.py:252` `def insert_cartoon_hider(..., rep='cartoon')` | `rep=rep` kwarg forwarding | ✓ WIRED | **THE 05-09 FIX** — the dispatcher now forwards `rep` (was `handle=handle)` only, dropping `rep`). |
| `mutation.py:479` `cmd.show(rep, _id_sele(all_game_ids))` | PyMOL `cmd.show` primitive (`viewing.py:491`) | `rep` parameter (was hardcoded `'cartoon'`) | ✓ WIRED | **THE 05-09 FIX** — the show call uses `rep` (was `cmd.show('cartoon', ...)`). |

**Result:** A `ribbon` rep request now flows end-to-end: `_on_start` → `hider_specs.append((term, 'ribbon'))` → `game.start` loop → `insert_hider_for_rep(rep='ribbon')` → `insert_cartoon_hider(rep='ribbon')` → `cmd.show('ribbon', GAME)`. Independently confirmed by the headless smoke diag: `GAME_rep_ribbon=2 polymer_rep_ribbon=0`.

### Requirements Coverage

| Requirement | Status | Blocking Issue |
|-------------|--------|----------------|
| HIDER-03 (line/stick hiders) | ✓ SATISFIED | None — `insert_line_stick_hider` works (smoke sections 2 + 4 + 5b). The 05-07 IndexError fix (CA neighbor selection aligned with smoke + defensive ValueError) closes the GUI regression. |
| HIDER-05 (cartoon/ribbon hiders) | ✓ SATISFIED (v1 scope) | None for v1 — `insert_cartoon_hider` works for both `rep='cartoon'` (terminal-extension, renders visibly) and `rep='ribbon'` (05-09 parameterization). The alt-conf segment-replication approach for a connected cartoon visual on 1ubq is DEFERRED to Phase 11 (formally added to roadmap); the terminal-extension approach satisfies the v1 criterion (renders visibly under cartoon, `count_atoms > 0`). |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `biochemeleon/game.py` | 1, 12, 13 | "placeholder insert" in docstrings | ℹ️ Info | NOT a stub — historical docstring referring to Phase 3's now-replaced placeholder insert ("Phase 4/5: real generators replace _placeholder_hiders"). Accurate architecture documentation, not unfinished work. |
| `biochemeleon/__init__.py` | 42 | "Phase 1 ships placeholder tabs only" | ℹ️ Info | NOT a stub — historical docstring documenting Phase 1 → Phase 2 progression. Accurate. |
| `biochemeleon/mutation.py` | — | (none) | — | No TODO/FIXME/XXX/HACK/placeholder/stub patterns. |
| `smoke/phase5_smoke.py` | — | (none) | — | No TODO/FIXME/XXX/HACK/placeholder/stub patterns. |

**No blockers.** No warnings. The 4 "placeholder" matches are historical docstrings (architecture evolution documentation), not active stubs.

### Human Verification Required

**None blocking.** The 05-09 plan is explicitly autonomous with headless smoke as the gate (mirroring the 05-07 precedent — minimal 1-file fix + smoke section + headless smoke, no human-verify checkpoint). The headless smoke IS the intended gate and was independently re-verified (41/41 ALL PASSED, exit 0).

**Optional context (not blocking):** The 05-05 human-verify already covered the visual blend for cartoon hiders (and found the disconnected-on-1ubq cosmetic look, deferred to Phase 11). The ribbon rep uses the SAME cartoon-geometry insert (only the `cmd.show` rep differs), so the ribbon visual blend behavior is structurally identical to cartoon — a separate GUI human-verify of ribbon's visual blend would not surface new information beyond what 05-05 already found for cartoon. The headless `count_atoms("... and segi GAME and name CA and rep ribbon") > 0` assertion is the correct gate for "ribbon hiders render visibly" (the roadmap's literal `cmd.count('cartoon','obj and segi GAME') > 0` requirement, adapted for ribbon).

### Gaps Summary

**No gaps in the v1 scope.** All 5 05-09 must-haves verified. All 3 Phase 5 success criteria met within v1 scope.

**Deferred to Phase 11 by design (NOT v1 gaps):**
- **Alt-conf segment replication** (gap #1 from 05-05): the cartoon terminal-extension renders "disconnected" on 1ubq (cosmetic — hiders clickable, game playable). The "replicate a segment (loop) as an alternate position using C-alpha geometry" alternative was spike-verified in isolation (05-06) but multi-rep integration failed in GUI (05-08, 4 failed cycles, reverted). A new Phase 11 ("Alt-conf Cartoon/Ribbon Hider") was added to the roadmap (commit `3544f35`) to close this post-v1. This is a KNOWN, DOCUMENTED, ACCEPTED limitation — NOT counted as a failure per the verification scope.

**Note on the roadmap's `cmd.count('cartoon', ...)` literal:** the roadmap's success criterion 2 literally reads `cmd.count('cartoon', 'obj and segi GAME') > 0`, but `cmd.count` is the wrong API for this in PyMOL 2.5.0 (the correct pattern is `cmd.count_atoms("... and rep cartoon")`, per `mutagenesis.py:570`). The smoke uses the correct `count_atoms` pattern, which is semantically equivalent to what the roadmap intended ("render visibly under the cartoon representation"). This is a roadmap prose imprecision, not a code gap — the underlying requirement (cartoon rep shown on GAME atoms) IS met.

---

## Verification Method

This verification was performed by:
1. **Reading the actual source files** (`biochemeleon/mutation.py`, `smoke/phase5_smoke.py`, `biochemeleon/setup_state.py`, `biochemeleon/game.py`, `biochemeleon/__init__.py`) — NOT trusting the 05-09-SUMMARY claims.
2. **Grepping for the 3 specific 05-09 changes** in `mutation.py` (signature `rep='cartoon'` at line 254, `cmd.show(rep, ...)` at line 479, `handle=handle, rep=rep` at line 536) — all confirmed present.
3. **Grepping for residual hardcoded `cmd.show('cartoon'`** across `biochemeleon/` — ZERO matches (the hardcoded literal is gone).
4. **Tracing the full rep-forwarding pipeline** from `_on_start` through `hider_specs` → `game.start` → `insert_hider_for_rep` → `insert_cartoon_hider(rep=rep)` → `cmd.show(rep, ...)` — all links wired.
5. **Independently re-running the headless smoke** from WSL via `cmd.exe /c C:\\src\\run-conda-pymol.bat -cq smoke\\phase5_smoke.py` (NOT trusting the SUMMARY's 41/41 claim) — result: `41/41 passed`, `ALL PASSED`, `EXIT=0`, with the diag `GAME_rep_ribbon=2 GAME_rep_cartoon=13 polymer_rep_ribbon=0` confirming the rep-forwarding fix.
6. **Independently re-running all 4 WSL gates** (py_compile + 90 unit tests + Pitfall-1 grep + exec_ grep) — all green.
7. **Confirming the 3 expected 05-09 commits** exist in `git log` (`2d487af` fix, `c04a813` test, `d202290` docs).
8. **Confirming `__init__.py` was NOT modified** by 05-09 (`git diff --stat HEAD~3 HEAD` shows only `mutation.py`, `phase5_smoke.py`, `STATE.md`, `05-09-SUMMARY.md`).
9. **Scanning for anti-patterns** (TODO/FIXME/placeholder/stub) in the modified files — none found (the 4 "placeholder" matches are historical docstrings, not stubs).

---

_Verified: 2026-08-09T15:39:28Z_
_Verifier: OpenCode (gsd-verifier)_
_Method: goal-backward verification against actual codebase + independent headless smoke re-run + independent WSL gate re-run_
