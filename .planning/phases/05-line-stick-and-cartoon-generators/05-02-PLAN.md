---
phase: 05-line-stick-and-cartoon-generators
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - biochemeleon/mutation.py
autonomous: true

must_haves:
  truths:
    - "insert_line_stick_hider creates a bonded pseudoatom near a neighbor (cmd.pseudoatom + cmd.bond), sets the segi='GAME'+b=-999 sentinel AND copies the neighbor's color/elem for blending, shows the rep on the new atom, and returns the new atom's stable id"
    - "insert_cartoon_hider attaches a glycine residue at a C-terminus via cmd.attach_amino_acid, sets the sentinel + copies the neighbor C-alpha's color/ss, shows cartoon on the new residue, and returns the new C-alpha's stable id"
    - "insert_hider_for_rep dispatches per rep: spheres->insert_hider (+cmd.show spheres by id), lines/sticks->insert_line_stick_hider, cartoon/ribbon->insert_cartoon_hider"
    - "All new hiders keep segi='GAME' + b=-999.0 sentinel so Phase 3 cleanup (segi GAME) and read path (segi GAME and b < 0) work with ZERO migration"
    - "The coloring decision (option c) is applied: KEEP the sentinel UNCHANGED + cmd.alter color/elem/ss to neighbor values so the default rendering blends (NOT elem='PS' like Phase 4 spheres)"
  artifacts:
    - path: "biochemeleon/mutation.py"
      provides: "insert_line_stick_hider + insert_cartoon_hider + insert_hider_for_rep dispatcher — the three cmd-coupled insertion primitives for line/stick + cartoon + mixed-rep dispatch"
      contains: "def insert_hider_for_rep"
  key_links:
    - from: "insert_line_stick_hider"
      to: "cmd.bond"
      via: "pseudoatom at neighbor+offset -> bond(hider, neighbor, order=1) — same-object bond (editing.py:717)"
      pattern: "cmd\\.bond"
    - from: "insert_cartoon_hider"
      to: "cmd.attach_amino_acid"
      via: "attach_amino_acid(terminus_sele, 'gly', ss=4) -> fuse mode 2 -> real backbone geometry -> polymer trace (editor.py:85-270)"
      pattern: "cmd\\.attach_amino_acid"
    - from: "insert_cartoon_hider"
      to: "cmd.show"
      via: "cmd.show('cartoon', 'obj and resi N and segi GAME') — newly-fused atoms do NOT inherit reps (mutagenesis.py:660-667)"
      pattern: "cmd\\.show.*cartoon"
    - from: "insert_hider_for_rep"
      to: "cmd.show"
      via: "spheres path: insert_hider (unchanged) + cmd.show('spheres', 'obj and id N') — dispatcher owns the sphere show so _on_start no longer shows ALL GAME atoms as spheres (avoids cross-contamination with stick/cartoon hiders)"
      pattern: "cmd\\.show.*spheres"
---

<objective>
Add three new cmd-coupled functions to `mutation.py`: `insert_line_stick_hider`
(pseudoatom + `cmd.bond` for line/stick hiders), `insert_cartoon_hider`
(`cmd.attach_amino_acid` for cartoon/ribbon hiders), and `insert_hider_for_rep`
(per-rep dispatcher). The existing `insert_hider` (sphere) stays UNCHANGED.

Purpose: The Phase 5 research (05-RESEARCH.md §5 Q19) prescribes ADDING new
functions (not overloading `insert_hider`) because the signatures diverge: sphere
needs `pos`, line/stick needs `neighbor_id`, cartoon needs `chain+terminus_resi`.
The dispatcher lets `GameController.start` (plan 05-03) stay a thin
`(payload, rep)` loop. Each insert function applies the LOCKED coloring decision
(research §4 Q15 option c): KEEP `segi='GAME'`+`b=-999` sentinel UNCHANGED AND
`cmd.alter` color/elem/ss to neighbor values so the default rendering blends
(this directly addresses the user's concern that "white sphere hiders like
Phase 4 would be too easy" — line/stick/cartoon hiders MUST blend or they'd be
trivially visible).

Output: `biochemeleon/mutation.py` extended (~148 → ~280 lines, 3 new functions).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-line-stick-and-cartoon-generators/05-RESEARCH.md
@AGENTS.md

# The module to extend (read the existing insert_hider pattern first):
@biochemeleon/mutation.py

# The pure generators (plan 05-01 produces these — read for the offset/terminal data shapes):
@biochemeleon/generators.py

# The registry (validates rep against GAME_REPS — no changes needed):
@biochemeleon/registry.py

# The Phase 4 smoke (the cmd.count_atoms("... and rep <name>") pattern to mirror):
@smoke/phase4_smoke.py
</context>

<tasks>

<task type="auto">
  <name>Add insert_line_stick_hider + insert_cartoon_hider + insert_hider_for_rep to mutation.py</name>
  <files>biochemeleon/mutation.py</files>
  <action>
    EXTEND `biochemeleon/mutation.py` (do NOT rewrite — keep the existing
    `insert_hider`, `fetch_all_hider_ids`, `cleanup_hiders` intact and UNCHANGED).
    Add three new functions AFTER `cleanup_hiders`, behind a new section comment
    `# ---- Line/stick + cartoon inserters (Phase 5) ----`.

    Follow 05-RESEARCH.md §10 code examples verbatim, with these refinements
    (documented inline):

    **1. insert_line_stick_hider(object, offset, neighbor_id, handle, rep='sticks',
         segi='GAME', b=-999.0, bond_order=1):**
    - Read the neighbor's coords + elem + color via `cmd.iterate_state(1,
      "%s and id %d" % (object, neighbor_id), "stored.append((x, y, z, elem,
      color))", space={'stored': nbr})` (05-RESEARCH.md §2 Q3: iterate_state
      exposes x/y/z; §4 Q13: iterate exposes color/elem — if iterate_state does
      NOT expose elem/color at runtime, split into iterate_state for coords +
      iterate for (elem,color) joined by ID; the smoke in plan 05-04 will catch
      this — LOW risk, Open Risk 6).
    - `pos = [nx + offset[0], ny + offset[1], nz + offset[2]]`
    - `cmd.pseudoatom(object=object, pos=pos, name=handle, segi=segi, b=b,
      hetatm=0, elem=n_elem, resn='HIDER', chain=n_chain_or_'H', resi='9001')`
      — set `elem` to the neighbor's elem (NOT 'PS' — 05-RESEARCH.md §4 Q17: both
      elem AND color for belt-and-suspenders blend). For chain, read it from the
      neighbor too (or default 'H' — the sentinel-alter overrides chain below).
    - Sentinel + blend alter (single hygienic call — 05-RESEARCH.md §4 Q14):
      `cmd.alter("%s and name %s" % (object, handle),
      "segi='GAME'; b=-999.0; color=stored_c",
      space={'stored_c': n_color})` — KEEP segi='GAME'+b=-999 sentinel (option c),
      ADD color=neighbor's color for blending.
    - `cmd.sort(object)` (defensive — editing.py:1457).
    - `hider_ids = cmd.identify("%s and name %s and segi GAME" % (object, handle),
      mode=0)`; `assert len(hider_ids) == 1`; `hider_id = hider_ids[0]`.
    - `cmd.bond("%s and id %d" % (object, hider_id), "%s and id %d" % (object,
      neighbor_id), order=bond_order)` — same-object bond (editing.py:717
      satisfied; 05-RESEARCH.md §2 Q4).
    - `cmd.show(rep, "%s and id %d" % (object, hider_id))` — show ONLY this atom
      (NOT all GAME atoms — avoids cross-contamination with sphere/cartoon
      hiders; 05-RESEARCH.md §3 Q11: newly-fused atoms do NOT inherit reps).
    - `return hider_id`.

    **2. insert_cartoon_hider(object, chain, terminus_resi, is_c_terminus,
         handle, aa='gly', ss=4, hydro=0, segi='GAME', b=-999.0):**
    - `residue_sele = "%s and chain %s and resi %d" % (object, chain,
      terminus_resi)`.
    - Read the neighbor (terminal residue) Cα props for blending:
      `nbr = []; cmd.iterate("%s and name CA" % residue_sele,
      "stored.append((chain, ss, color))", space={'stored': nbr})`;
      `n_chain, n_ss, n_color = nbr[0]` (05-RESEARCH.md §4 Q13: iterate exposes
      chain/ss/color lowercase).
    - Build the attach selection (single N or C atom — editor.py:125):
      `attach_sele = "%s and name %s" % (residue_sele, "C" if is_c_terminus else
      "N")`.
    - `cmd.attach_amino_acid(attach_sele, aa, ss=ss, hydro=hydro)` — fuses a real
      residue with backbone geometry via `cmd.fuse(...,2)` (editor.py:160/212;
      05-RESEARCH.md §3 Q6). `ss=4` (flat/loop — least conspicuous; Q6).
      `hydro=0` (no hydrogens). Use a NAMED selection (NOT pk1 — Open Risk 2:
      editor.py:125 general path accepts any sele; the smoke will confirm; if it
      requires pk1, fallback: `cmd.edit(attach_sele)` first — document in smoke).
    - Compute `new_resi = terminus_resi + 1` (C-terminus extension, forward) or
      `terminus_resi - 1` (N-terminus, backward). MVP uses C-terminus only
      (`is_c_terminus=True`), so `new_resi = terminus_resi + 1`.
    - `new_sele = "%s and chain %s and resi %d" % (object, n_chain, new_resi)`.
    - Sentinel + blend alter (single hygienic call):
      `cmd.alter(new_sele, "segi='GAME'; b=-999.0; color=stored_c; ss=stored_ss",
      space={'stored_c': n_color, 'stored_ss': n_ss})` — KEEP sentinel, ADD
      color=neighbor Cα color (cartoon tube follows Cα color — Q16) + ss for
      blend.
    - `cmd.sort(object)` (defensive — editing.py:1457; 05-RESEARCH.md §6 Q23).
    - `cmd.show('cartoon', "%s and resi %d and segi GAME" % (object, new_resi))`
      — show ONLY the new residue (NOT all GAME atoms; Q11: newly-fused atoms
      do NOT inherit reps).
    - `ids = cmd.identify("%s and name CA and segi GAME and resi %d" % (object,
      new_resi), mode=0)`; `assert len(ids) == 1`; `return ids[0]` (the Cα — the
      cartoon-representative atom the player clicks).

    **3. insert_hider_for_rep(object, rep, payload, handle):**
    Dispatcher (05-RESEARCH.md §5 Q19):
    - `rep == 'spheres'`: `aid = insert_hider(object, pos=payload, rep=rep,
      handle=handle)` (UNCHANGED insert_hider — no show inside it) then
      `cmd.show("spheres", "%s and id %d" % (object, aid))` (dispatcher owns the
      sphere show — by id, NOT all GAME — so _on_start no longer does
      `cmd.show("spheres", "%s and segi GAME")` which would cross-contaminate
      stick/cartoon hiders); `return aid`.
    - `rep in ('lines', 'sticks')`: `offset, neighbor_id = payload`;
      `return insert_line_stick_hider(object, offset=offset,
      neighbor_id=neighbor_id, handle=handle, rep=rep)` (the insert function
      shows its own rep by id).
    - `rep in ('cartoon', 'ribbon')`: `chain, terminus_resi, is_c_terminus =
      payload`; `return insert_cartoon_hider(object, chain=chain,
      terminus_resi=terminus_resi, is_c_terminus=is_c_terminus, handle=handle)`
      (the insert function shows its own rep; for ribbon, the same residue
      renders in both — 05-RESEARCH.md §3 Q10).
    - `else`: `raise ValueError("unknown rep %r" % (rep,))`.

    Update the module docstring to mention the three new Phase 5 functions and
    the coloring strategy (option c: sentinel unchanged + alter color/elem/ss
    to neighbor). Write docstrings in PROSE (avoid literal `cmd.bond` /
    `cmd.attach_amino_acid` / `from pymol` in docstrings — they trip the
    verification greps; mirrors the 03-03/03-06/03-09 docstring-rewording
    precedent, Rule 3 blocking).

    Do NOT change `insert_hider`, `fetch_all_hider_ids`, or `cleanup_hiders`.

    Commit: `git add biochemeleon/mutation.py && git commit -m "feat(05-02): add insert_line_stick_hider + insert_cartoon_hider + insert_hider_for_rep dispatcher"`
  </action>
  <verify>
    Run the full WSL gate suite (all MUST be green — these are syntax + unit
    tests only; the cmd-coupled functions are runtime-verified by the headless
    smoke in plan 05-04):
    - `python3.6 -m py_compile biochemeleon/*.py` (syntax check every module —
      must be clean)
    - `python3.6 -m unittest tests.test_setup_state tests.test_registry tests.test_generators tests.test_game_controller -v` (ALL pass — no regression; the new functions are cmd-coupled so NOT unit-tested in WSL)
    - Purity gate (generators.py + registry.py + setup_state.py MUST have NO pymol — the new code is in mutation.py which IS cmd-coupled, so this gate applies to the pure modules only):
      use the Grep tool for `from pymol|import pymol` in `biochemeleon/generators.py` → 0 matches
      use the Grep tool for `from pymol|import pymol` in `biochemeleon/registry.py` → 0 matches
    - Pitfall-1 gate (MUST return ZERO across the package):
      use the Grep tool for `import Tkinter|import tkinter|from tkinter|import Pmw|from Pmw|app\.root|grab_set|mainloop|Toplevel|menuBar\.addmenuitem|from PyQt5 import|import PyQt5` in `biochemeleon/` → 0 matches
    - exec_ gate (any hits must be on QFileDialog/QMessageBox, NEVER the main dialog):
      use the Grep tool for `\.exec_\(\)` in `biochemeleon/` → only QMessageBox.exec_() / QFileDialog.exec_() on child dialogs (unchanged from Phase 4)
    - Completeness gate (mutation.py has the 3 new functions + the 3 existing):
      use the Grep tool for `^def ` in `biochemeleon/mutation.py` → 6 matches (insert_hider, fetch_all_hider_ids, cleanup_hiders, insert_line_stick_hider, insert_cartoon_hider, insert_hider_for_rep)
    - Sentinel gate (the new functions keep segi='GAME' + b=-999):
      use the Grep tool for `segi='GAME'` in `biochemeleon/mutation.py` → >= 3 matches (existing insert_hider + new line_stick + cartoon)
      use the Grep tool for `b < 0` in `biochemeleon/mutation.py` → 1 match (fetch_all_hider_ids unchanged)
      use the Grep tool for `b -999` in `biochemeleon/mutation.py` → 0 matches (NEVER the exact-match selector)
  </verify>
  <done>
    mutation.py extended with 3 new functions (~280 lines total); existing 3
    functions unchanged; py_compile clean; all WSL unit tests pass (no
    regression); sentinel gates pass (segi='GAME'+b=-999 in all insert paths,
    b<0 read path unchanged, b-999 selector absent); Pitfall-1/exec_ gates pass;
    committed.
  </done>
</task>

</tasks>

<verification>
Phase 5 cmd-coupled inserters are syntax-verified (WSL) and ready for headless
smoke (plan 05-04) when:
- `python3.6 -m py_compile biochemeleon/*.py` → clean
- All WSL unit tests pass (no regression from the mutation.py extension)
- mutation.py has 6 functions (3 existing + 3 new)
- All insert paths use segi='GAME' + b=-999 sentinel; read path uses b < 0; no
  `b -999` exact-match selector anywhere
- The coloring decision (option c) is embedded: sentinel UNCHANGED + alter
  color/elem to neighbor values (NOT elem='PS' for line/stick/cartoon)
- Runtime behavior (cmd.bond same-object, attach_amino_acid with named sele,
  segi-doesn't-break-polymer, color-follows-Cα) is deferred to the headless
  smoke (plan 05-04) which asserts the 3 MEDIUM open risks
</verification>

<success_criteria>
- insert_line_stick_hider, insert_cartoon_hider, insert_hider_for_rep exist in
  mutation.py with correct signatures and the coloring decision (option c)
  embedded.
- The existing insert_hider / fetch_all_hider_ids / cleanup_hiders are UNCHANGED
  (Phase 3/4 invariants preserved — zero migration).
- The dispatcher is ready for plan 05-03 (game.py + __init__.py wiring) to call
  as `mutation.insert_hider_for_rep(self.target_obj, rep, payload, handle)`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-line-stick-and-cartoon-generators/05-02-SUMMARY.md`
</output>
