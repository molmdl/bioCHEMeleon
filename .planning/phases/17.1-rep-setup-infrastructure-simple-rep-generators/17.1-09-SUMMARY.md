---
phase: 17.1-rep-setup-infrastructure-simple-rep-generators
plan: 09
subsystem: vmd-game-engine
tags: [vmd, tcl-8.5, tachyon, headless-smoke, lines-rep, bonded-hiders, both-endpoints, dispatch, pixel-proxy]

# Dependency graph
requires:
  - phase: 17.1-08
    provides: the bonded-tier smoke template (round -> mimicry -> bonds -> read-back -> Tachyon -> found -> cleanup) with the _parse_dat/_cnear/_feq helpers, pinned element-color table, salmon trap, dot-Rad bound, and the Directional_Light filter
  - phase: 17.1-06
    provides: game.tcl 4-arg per-tier dispatch (start_game {molid hider_count {per_rep} {lock_scene}}), GAME_REPS-ordered per_rep stash, derived tier codes, registry assign_reps
  - phase: 17.1-04
    provides: hiders::tier_reps name-keyed pair dict, mark_found_visual re-assert (restores render-emptied selections)
  - phase: 17.1-01
    provides: rep_tiers pure module (tiers_from_per_rep, style_args Lines -> bare "Lines")
provides:
  - "vmd/smoke/phase17_lines_smoke.tcl -- Lines-tier end-to-end proof through the real dispatch, including the both-endpoints dot-vs-bond render semantics (hiders-only Lines rep = 0 FCylinder + radius-0 dots; the scene rep draws the stubs)"
  - "Raw .dat confirmation that a Lines rep renders an unpartnered selected atom as a Sphere Rad 0.002 and a both-endpoints bond as FCylinders (13 stub cylinders observed from 13 hider bonds)"
affects: [17.1-10-licorice, 17.1-11-cpk, 17.1-12-dynamicbonds, 17.1-13-capstone, 17.1-14-gui-verify, 17.2-cartoon-generators]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Both-endpoints render semantics pinned headlessly: a bond-style rep draws a bond ONLY when both endpoints are in the rep's selection -- a hiders-only rep draws radius-0 dots, the scene's own all-selection rep draws the bond stubs (the P-3 overlay decision, now render-proven for Lines)"

key-files:
  created:
    - vmd/smoke/phase17_lines_smoke.tcl
  modified: []

key-decisions:
  - "Single-file adaptation of the proven 17.1-08 template: only the tier (Points -> Lines), the found-marked index (425 -> 424 per the plan), the render file names, and the tier-specific comments changed -- every parser helper, pinned constant, and assert structure byte-identical"
  - "Hider-rep-exclusive render asserts BOTH directions of the both-endpoints rule: 0 FCylinder + exactly 3 dots (Rad < 0.02) in the hiders-only render (probe rep styled Lines), AND (A.cyl - B.cyl) >= 3 in the scene-diff (the scene rep draws the stubs)"
  - "max_dot_rad 0.02 kept from 17.1-08 (research probe E pinned Rad 0.002 for Lines lone-atom dots) -- a full VDW-radius sphere (the wrong primitive) can never pass"

patterns-established:
  - "Tier-smoke per-tier deltas are: start_game per_rep dict, tier style asserts, probe rep style, found-marked index, render file names, remaining_by_rep keys -- everything else is shared template (validated by the Lines copy)"

# Metrics
duration: 6 min
completed: 2026-09-04
---

# Phase 17.1 Plan 09: Lines-Tier End-to-End Smoke Summary

**Lines tier proven end-to-end through the real 17.1-06 dispatch with the both-endpoints dot-vs-bond semantics render-pinned (hiders-only Lines rep = 0 FCylinders + 3 radius-0 dots; the scene's all-selection rep draws the 13 stub cylinders), PASS=1 3/3 runs.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-09-04T18:23:06Z
- **Completed:** 2026-09-04T18:29:56Z
- **Tasks:** 1
- **Files modified:** 1 created (no lib files touched)

## Accomplishments
- Lines round through the real dispatch: `start_game 1znf 3 {Lines 3} 0` -> 427 atoms, hiders {424 425 426}, user3==1.0 numeric (real atom keeps 0.0, P6), registry {Lines 3}
- Bonded chemistry certified: every hider numbonds >= 1 (VMD added 13 hider bonds over the 426 real ones -- extra H/N bonds are the normal blend-positive behavior), element/radius/name mimic anchors exactly
- Rep read-back exact: hidden/found == `Lines` + `Element`/`ColorID 7` + the literal `user3 1` selections via tier_reps + repindex; numreps == pre+2
- THE BOTH-ENDPOINTS PROOF (raw .dat inspected, not just marker-trusted): hiders-only Lines render = **0 FCylinder**, exactly **3 Spheres Rad 0.002**, dot colors cyan/blue/red (within the pinned element set, zero salmon); scene-diff A(all) vs B(not-GAM) = **cyl 765 - 752 = 13** (= exactly the 13 hider bonds, stubs drawn by the SCENE rep), sph diff == 0
- Found partition + cleanup: mark_found_visual(424) -> 1 found / 2 hidden, remaining_by_rep {Lines 2}; cleanup -> registry 0, game molid dead, restored original 424 atoms with pre-start numreps
- PASS=1 on 3/3 staged runs (independent PRNG anchor draws), 0 `ERROR)` / 0 `bad switch` / 0 other error classes in every full log, "Exiting normally" every run; Tcl 8.6 gate zero

## Task Commits

Each task was committed atomically:

1. **Task 1: Write + run the Lines-tier end-to-end smoke** - `ac866f5` (test)

**Plan metadata:** (this commit)

## Files Created/Modified
- `vmd/smoke/phase17_lines_smoke.tcl` - the Lines-tier end-to-end smoke incl. the both-endpoints dot-vs-bond render proof (565 lines)

## Decisions Made
- **Template-copy discipline**: diffed the new smoke against 17.1-08's Points smoke before running -- only the tier swap, found-marked index (424 per plan), render filenames, and comments differ; all parser helpers/constants/assert structure identical (the 17.1-08 multi-attribute-get and Directional_Light lessons carried verbatim).
- **Both directions asserted**: the hiders-only render pins "hider rep draws NO bonds" (0 FCylinder) and the scene-diff pins "the scene rep DOES draw them" (>= 3) -- the P-3 overlay decision is proven from both sides, matching the plan's must-have truth.
- **Raw-evidence inspection**: read lines_hiders.dat directly after the 3/3 PASS (the plan-prescribed inspect-and-pin step) -- confirmed 0 FCylinder / 3 Rad-0.002 Spheres / cyan+blue+red dot colors, and the scene-diff cylinder delta equal to the exact hider bond count (13).

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None new. The established noise classes appeared as pinned: 6 "Unusual bond between residues" warnings per run (normal bonded-round noise) and stray numeric echo lines (benign top-level `set` echoes).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Wave-5 tier smokes 17.1-10 (Licorice) and 17.1-11 (CPK) can copy this file identically: their per-tier deltas are start_game's per_rep dict, the tier/probe style asserts, the render file names, and (for Licorice/CPK) the hider-render primitive expectations (bond-radius balls / CPK-scaled spheres instead of dots -- same 0-FCylinder rule).
- 17.1-12 (DynamicBonds) additionally needs the bare-style scene prep (17.1-10's pattern) and the `DynamicBonds 1.6` read-back equality.
- The both-endpoints semantics are now render-proven for one bond style (Lines); the remaining bond-family smokes re-prove it per style cheaply from the same template.
- No lib defects found; no blockers for 17.1-10..12.

---
*Phase: 17.1-rep-setup-infrastructure-simple-rep-generators*
*Completed: 2026-09-04*
