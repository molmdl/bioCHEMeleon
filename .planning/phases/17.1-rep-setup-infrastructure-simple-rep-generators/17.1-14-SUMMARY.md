---
phase: 17.1-rep-setup-infrastructure-simple-rep-generators
plan: 14
subsystem: gui
tags: [vmd, tcl-85, gui-verify, pick-contract, lock-scene, auto-driver, human-verify, checkpoint]

# Dependency graph
requires:
  - phase: 17.1 (plans 01-13)
    provides: rep_tiers pure module, 4-arg dispatch composition root (game.tcl), N-tier hiders, registry rep API, bonded generators, per-tier + capstone smokes, full-suite green gate (headless-green phase)
  - phase: 16-mvp-core-loop-sphere
    provides: the LOCKED pick contract (first-click p-press quirk), pick_verify.tcl harness patterns, the 16-13 active-game guard whose restored original figures in finding 2
provides:
  - vmd/gui/dialog.tcl on_start 4-arg start_game threading — the GUI path shares ONE lock-scene/randomize derivation with console + restart
  - vmd/tests/rep_verify.tcl — the Tk-guarded auto-logged GUI driver (pv_log / pv_round2 / pv_report / pv_cleanup), superseding the stale pick_verify.tcl
  - APPROVED consolidated GUI checkpoint record (2026-09-05): per-tier blend + pick-through + found-visibility + lock-scene round-trip, 142-line clean auto-log
affects: [phase-17.2 cartoon generators, phase-19 polish (2 candidate fixes), phase-22 in-game help (p-first note), 17.1 phase verifier]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Auto-logged GUI driver: Tk-guard if-wrap whole body, pv_log open-append + flush-per-line to rep_verify_log.txt, driver executes the setup itself (source extension, load demo, apply_state, call on_start, after-delayed auto-dump) — the human's pastes shrink to source / pv_round2 / pv_report"
    - "Rep lookups in GUI drivers resolve via hiders::tier_reps + mol repindex with the -1 guard (never the removed hiders::hidden_rep/found_rep vars)"

key-files:
  created:
    - vmd/tests/rep_verify.tcl
  modified:
    - vmd/gui/dialog.tcl

key-decisions:
  - "pick_verify.tcl stays UNREPAIRED as a Phase-16 historical artifact — superseded by rep_verify.tcl; its ~10 dead hidden_rep/found_rep reads remain and it must NOT be run in future sessions"
  - "Driver cosmetic defects (pv_report finds=0 counter bug; round-2 state dump echoing the INPUT per_rep instead of the DERIVED distribution) recorded NON-BLOCKING and not fixed — user disposition 'record only'; the registry lines in the auto-log were authoritative"
  - "Lock-scene round 2 deriving {Licorice 2} (quick-008 random-subset, sum<=hc semantics pinned by 17.1-13 Round B) ACCEPTED as-is by the user — not requesting a full-count change"
  - "GUI checkbox-lock sync (v1 parity) stays deferred per plan context — ROADMAP SC2 requires Start-time detection + matching generation only"

patterns-established:
  - "Consolidated GUI checkpoint shape: ONE session, ONE driver, 2 rounds (6-tier mixed + lock-scene) covers blend gestalt + pick-through + found-visibility + lock-scene round-trip for the whole phase"

# Metrics
duration: ~17 min agent execution across two segments (tasks 1-2 ~12 min 2026-09-04T21:09-21:20Z; close-out ~5 min 2026-09-05T08:29-08:35Z) + overnight user GUI session
completed: 2026-09-05
---

# Phase 17.1 Plan 14: Consolidated GUI Human-Verify Checkpoint Summary

**GUI checkpoint APPROVED (2026-09-05): on_start threads per_rep + lock_scene into the 4-arg start_game, and the rep_verify.tcl auto-driver proves per-tier blend, pick-through, found-visibility, and the lock-scene round-trip in one 2-round real-VMD session with a 142-line clean auto-log — the last unverified surface of Phase 17.1.**

## Performance

- **Duration:** ~17 min agent execution (2 segments; the user's GUI session + verdict came in between, and the close-out finished 2026-09-05T08:35Z)
- **Started:** 2026-09-04T21:09Z (task 1)
- **Completed:** 2026-09-05T08:35Z (close-out)
- **Tasks:** 3 (2 auto + 1 checkpoint:human-verify)
- **Files modified:** 2 (1 created, 1 modified)

## Accomplishments
- **Task 1 — GUI/console semantic unification:** `on_start` (dialog.tcl step 4) now calls `game::start_game $molid $hider_count [dict get $state per_rep] [dict get $state lock_scene]` — the Setup form's per-rep rows and lock-scene checkbox flow into the SAME resolve_per_rep/lock derivation the console path uses. Lock-scene and randomize semantics are defined once, inside start_game (commit db4677c).
- **Task 2 — auto-driver:** `vmd/tests/rep_verify.tcl` (499 lines, Tk-guarded — headless no-op) auto-sources the extension, loads 1k8p, applies the crafted 6-tier state, calls `on_start` itself, registers the pick observer, and auto-logs every state dump + PICK event to `rep_verify_log.txt` (open-append, flush per line, vmdcon echo). Human pastes limited to `source` / `pv_round2` / `pv_report` per the standing auto-issue/auto-log directive (commit 50146ad).
- **Task 3 — the consolidated checkpoint ran and was APPROVED:** one real-VMD GUI session, 2 rounds (6-tier mixed; lock-scene with a pre-styled Licorice scene). All phase-level GUI truths verified — see Checkpoint Verdict below.

## Task Commits

1. **Task 1: Thread per_rep + lock_scene through on_start** - `db4677c` (feat)
2. **Task 2: Write the rep_verify.tcl auto-driver** - `50146ad` (test)
3. **Task 3: Consolidated GUI human-verify checkpoint** - no commit (checkpoint; APPROVED verdict recorded in this SUMMARY)

**Plan metadata:** (this commit — docs)

## Files Created/Modified
- `vmd/gui/dialog.tcl` - on_start step 4 now passes per_rep + lock_scene to start_game (4-arg; the only GUI-layer change)
- `vmd/tests/rep_verify.tcl` - the Tk-guarded auto-driver: pv_log, pv_state (per-tier dump via tier_reps + repindex), pv_round2 (lock-scene helper), pv_report, pv_cleanup/pv_cleanup_check

## Checkpoint Verdict (Task 3 — APPROVED, 2026-09-05)

**Session shape:** Windows VMD GUI, Tk console, staging `tmp/biochemeleon-vmd`, driver auto-logged to `tmp/biochemeleon-vmd/rep_verify_log.txt` (142 lines, **0** `ERROR)` / `bad switch` / `no-such-variable`, 58 PICK lines). Round 1 = 6 tiers × 1 hider; round 2 = lock-scene after styling the scene to Licorice.

**Orchestrator verdict mapping (a)-(e), all ✓:**

| Item | Verdict | Evidence |
|---|---|---|
| (a) per-rep blend gestalt | ✓ as designed + difficulty note | All 6 styles WERE applied — log lines 32-54 read-backs: Lines / VDW / Licorice / CPK / Points / `DynamicBonds 1.6`, hidden color Element, found ColorID 7, user3 1-6. User: "only sphere are sphere, others kept as default lines, hard to spot i had to cheat with manual representation to finish the test" — the non-sphere tiers blend INTO the default-Lines scene exactly as the feature intends; difficulty-calibration note for Phase 19 |
| (b) pick-through per style | ✓ | All 6 round-1 hiders found by real clicks (mol=1, hiders 555-560, e.g. log 16:01-16:03); round-2 hiders 555/556 found on mol=4 (16:11:49, 16:12:08) |
| (c) found-visibility | ✓ | "hiders turn green after click" |
| (d) lock-scene round-trip | ✓ Licorice-only | Round-2 derived distribution = `{Licorice 2}` (log 16:08:43 registry remaining_by_rep); scene style read `Licorice 0.3 12 12` off the displayed molecule and mirrored onto the fresh target before start |
| (e) log cleanliness | ✓ | 0 error-class lines; `=== SESSION REPORT: rounds=2` present |

**Win boxes (user-reported, consistent with the log's PICK timeline):** round 1 "You found all 6 hiders in 4:45!", round 2 "You found all 2 hiders in 3:26!".

**Recorded user findings (verbatim observations):**

1. "only sphere are sphere, others kept as default lines, hard to spot i had to cheat with manual representation to finish the test" — blend feature working; **difficulty-calibration note for Phase 19**.
2. "hiders turn green after click" — found-visibility works.
3. "may need to advice the player to start with pressing p" — **Phase 22 in-game help note** (the first-click quirk itself is already documented as locked behavior in vmd/AGENTS.md).
4. "need to hide the 1k8p object in vmd main before able to click" (round 2) — the log confirms: 6 picks landed on **mol=3 (the restored ORIGINAL from the 16-13 guard cleanup)** at 16:09:08-16:10:20 before the user hid it; then mol=4 picks found both hiders. Known 16-15-documented wart category; **Phase 19 candidate fix**: hide/deselect the restored original after guard cleanup.
5. "round 2 does only show licorice" — lock-scene detection works; derived distribution = `{Licorice 2}` (quick-008 random-subset semantics, pinned by 17.1-13 Round B; **user accepted this behavior** — NOT requesting the full-count change).

**Driver cosmetics (non-blocking, record only — NOT fixed):**
- `pv_report` printed `finds=0` (its `::pv_finds` counter never bumped; line 141) while the registry lines were correct (remaining 0, both rounds complete).
- The round-2 state dump echoed the INPUT per_rep/hider_count (6-tier/6) instead of the DERIVED values (`{Licorice 2}`/2) — the registry + tier lines in the same dump were authoritative.

## Decisions Made
- **pick_verify.tcl left unrepaired/superseded:** per plan context, the Phase-16 driver keeps its dead `hidden_rep`/`found_rep` reads as a historical artifact; rep_verify.tcl is the only GUI driver going forward. (This also closes the long-standing "pick_verify.tcl will break the next GUI session" pending todo — the session was run on rep_verify.tcl instead.)
- **Driver cosmetics disposition — record, don't fix:** test-only file, session complete, registry lines authoritative; user explicitly marked non-blocking.
- **Lock-scene subset count accepted as-is:** round 2's `{Licorice 2}` (n ∈ [1,6] draw) is the pinned quick-008 semantics; no full-count change requested.
- **Checkbox-lock sync stays deferred** (plan context): Start-time detection + matching generation is the SC2 requirement; the sync UI is a registered future refinement.

## Deviations from Plan

None - plan executed exactly as written. (Tasks 1-2 implemented per the plan's action blocks; the checkpoint returned APPROVED. The driver cosmetic defects above were discovered during the checkpoint and dispositioned by the user as record-only — documented under Decisions/Issues rather than auto-fixed, per the explicit user verdict.)

## Issues Encountered
- **Restored-original-intercepts-picks (known wart category, 16-15):** after the 16-13 guard cleanup restores the original molecule, it remains visible and picked-through — round 2 logged 6 mol=3 picks before the user manually hid it. Registered as a **Phase 19 candidate fix** (hide/deselect the restored original after guard cleanup).
- **Blend difficulty calibration:** non-sphere tiers (Lines/Points/Licorice/CPK stubs vs the default-Lines scene) are extremely hard to spot at a glance — by design, but a Phase 19 difficulty knob may want to bias rounds toward sphere-tier mixes or pre-style scenes.
- No blocking issues; the session completed both rounds with a clean log.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- **Phase 17.1 is functionally complete: 14/14 plans, GUI checkpoint APPROVED.** The phase verifier (orchestrator) runs next as the final step before phase close.
- **pick_verify.tcl:** stays unrepaired/superseded — do NOT run it in future sessions (dead `hidden_rep`/`found_rep` reads); rep_verify.tcl is the GUI driver.
- **Phase 19 candidate fixes recorded:** (1) hide/deselect the restored original after the 16-13 guard cleanup (it intercepts picks); (2) blend difficulty calibration (non-sphere tiers invisible against default-Lines scenes).
- **Phase 22 note:** in-game help should advise "press p first" (first-click quirk is locked behavior).
- **Driver cosmetics** (pv_report counter; round-2 dump echo) are recorded, non-blocking, and may be cleaned up opportunistically if rep_verify.tcl is reused.
- Carry-forward into 17.2 (cartoon generators): the 17.1 seam is `IMPLEMENTED_TIERS` + TIER_KINDS in rep_tiers.tcl; the pre-styled-scene smoke pattern (17.1-10) is the template for proving Cartoon/NewCartoon blend.

---
*Phase: 17.1-rep-setup-infrastructure-simple-rep-generators*
*Completed: 2026-09-05*
