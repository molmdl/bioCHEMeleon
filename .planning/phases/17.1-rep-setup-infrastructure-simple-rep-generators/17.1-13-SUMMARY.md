---
phase: 17.1-rep-setup-infrastructure-simple-rep-generators
plan: 13
subsystem: testing
tags: [vmd, tcl-85, tcltest, headless-smoke, dispatch, lock-scene, quick-008, capstone, regression-gate]

# Dependency graph
requires:
  - phase: 17.1 (plans 01-12)
    provides: rep_tiers pure module, per-tier dispatch composition root (game.tcl), N-tier hiders, registry per-record rep API, bonded generators, the 8 per-tier/infra smokes, 12 legacy smokes re-baselined under N-tier contracts
  - phase: 16-mvp-core-loop-sphere
    provides: the dispatch/backup/mutation/registry stack the capstone drives end-to-end
provides:
  - vmd/smoke/phase17_capstone_smoke.tcl — mixed 3-tier + lock-scene + under-generation capstone (the phase's SC2 acceptance proof at the composition-root level)
  - FULL-suite green gate results: 5 tcltest suites + 24 smokes all PASS with zero error-class lines, both grep gates zero — the phase is headless-green before the GUI checkpoint
affects: [17.1-14 GUI checkpoint, 17.2 cartoon generators, phase-18 materials, any future full-suite regression gate]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Invariant-based randomize asserts: any smoke calling a start_game path that routes through randomize_per_rep must assert the consistency chain (single-key derivation + hider_count == effective_total == registry count == n in [1, requested]), never a literal ==total pin (quick-008 sum<=hc)"
    - "Lock-scene derivation proof shape: per_rep keys == {detected tier} EXACTLY + consistency chain + snapshot-fidelity cleanup"
    - "Full-suite gate protocol: fresh staging dir, sequential VMD runs (shared $env(TEMP) combined-PDB file forbids parallelism), per-log marker parse + full-log ERROR)/bad-switch scan + Exiting-normally sweep"

key-files:
  created:
    - vmd/smoke/phase17_capstone_smoke.tcl
  modified: []

key-decisions:
  - "Round-B asserts are INVARIANT-BASED (per_rep keys == {Licorice} exactly + the P9 consistency chain), replacing the plan's literal 'remaining_by_rep == {Licorice 4}; hider count 4' — resolve's empty-per_rep + lock_scene=1 path goes through randomize_per_rep whose pinned quick-008 contract is sum <= hider_count (NOT ==); with a single allowed tier the subset is forced to {Licorice} but the drawn count is 1..4 draw-dependent. Evidence: gate run 2 drew n<4 (no 428-atom molecule) while runs 1/3 drew n=4 — the literal pin would have flaked run 2. Same resolution class as 17.1-06's dispatch-smoke step 8."

patterns-established:
  - "Capstone = composition + bookkeeping, not renders: per-tier smokes own Tachyon render proofs; the capstone owns tier MIXING, lock-scene derivation, under-generation tolerance, and the full-suite gate"
  - "Under-generation proof shape: explicit per_rep carrying an unimplemented tier (Cartoon 2 VDW 1) -> per_rep == {VDW 1}, hider_count == 1 (P9 effective), the drop's vmdcon -warn asserted at the RUNNER level (grep 'no generator yet' — a Warning) line, never ERROR))"

# Metrics
duration: 43 min
completed: 2026-09-04
---

# Phase 17.1 Plan 13: Capstone Mixed-Tier + Lock-Scene Smoke & Full-Suite Gate Summary

**Mixed 3-tier + lock-scene + under-generation capstone smoke through the real dispatch (PASS=1 3/3 runs) and a FULL-suite green gate: 5 tcltest suites (147 tests, Failed=0) + 24 smokes (all PASS=1) with zero ERROR)/bad-switch lines and both grep gates zero — Phase 17.1 is headless-green.**

## Performance

- **Duration:** 43 min
- **Started:** 2026-09-04T20:09:13Z
- **Completed:** 2026-09-04T20:51:43Z
- **Tasks:** 2
- **Files modified:** 1 created (650 lines)

## Accomplishments
- **Mixed 3-tier round proven end-to-end** (Round A on 1znf, `start_game $m 5 {VDW 2 Lines 2 Licorice 1} 0`): 429 atoms, hiders {424..428}, GAME_REPS-ordered per_rep stash {Lines 2 VDW 2 Licorice 1}, tier codes 1..3 derived from tiers_from_per_rep and stamped user3-exact (Lines→424/425, VDW→426/427, Licorice→428), pre+6 reps, per-pair read-back for all 3 tiers (style/Element/ColorID-7/selection strings), registry remaining_by_rep {Lines 2 VDW 2 Licorice 1}, one find per tier (425/426/428) → remaining 2, remaining_by_rep {Lines 1 VDW 1} with Licorice absent (no zero-fill), exact per-tier user2 partitions (1h/1f, 1h/1f, 0h/1f), cleanup restore (registry 0, game molid dead, 424 atoms, pre-start numreps).
- **LOCK-SCENE round proven** (Round B, ROADMAP SC2): fresh 1znf pre-styled `mol modstyle 0 $m Licorice` (P-1 read-back before start_game) → `start_game $m2 4 {} 1` → per_rep keys == {Licorice} EXACTLY (detection + restriction proof), consistency chain hider_count == effective_total == registry count == n ∈ [1,4], atoms 424+n, hiders {424..423+n} all user3==1.0, numreps == 3, Licorice pair read-back carries `user3 1`, cleanup snapshot-fidelity (restored rep-0 style STILL Licorice).
- **Under-generation tolerance proven** (Round C, v1 parity): `start_game $m3 3 {Cartoon 2 VDW 1} 0` → Cartoon dropped with `Warning) bioCHEMeleon: rep 'Cartoon' has no generator yet -- dropped from this round` (confirmed in all 3 logs; a Warning) line, never ERROR)), per_rep == {VDW 1}, hider_count == 1 (P9 effective total, not the requested 3), 425 atoms, hider {424} user3 1.0, registry {VDW 1}, cleanup restores.
- **FULL-suite green gate recorded** (Task 2, verification-only): every suite Failed=0, every smoke PASS=1, zero `ERROR)`/`bad switch` in all 29 logs, `Exiting normally` in all 29, 8.6-idiom gate 0, grab-set gate 0.

## Task Commits

Each task was committed atomically:

1. **Task 1: Mixed-tier + lock-scene capstone smoke** - `89c0553` (test)
2. **Task 2: Full-suite green gate** - no commit (verification-only task; `files_modified` empty; results recorded in this SUMMARY — an empty commit was deliberately not created)

**Plan metadata:** (this commit)

## Files Created/Modified
- `vmd/smoke/phase17_capstone_smoke.tcl` - the phase capstone: 3 dispatch rounds (mixed 3-tier, lock-scene, under-generation) + marker/report harness per the 17.1-06/-08 conventions

## Full-Suite Gate Results (Task 2 — fresh staging tmp/cap13-gate, sequential VMD 1.9.3 runs)

**tcltest suites** (under headless VMD; marker `BCHM_TEST_RESULT ... Failed=0`):

| Suite | Total | Passed | Failed | Skipped |
|---|---|---|---|---|
| test_setup_state | 47 | 47 | 0 | 0 |
| test_registry | 27 | 27 | 0 | 0 |
| test_generators | 26 | 26 | 0 | 0 |
| test_rep_tiers | 32 | 32 | 0 | 0 |
| test_game_logic | 15 | 15 | 0 | 0 |
| **Total** | **147** | **147** | **0** | **0** |

(Note: test_game_logic is 15 tests, not the 8 quoted in the plan-context note — the suite grew during waves 1-5; Failed=0 is the gate.)

**Smokes** (marker `BCHM_SMOKE_RESULT PASS=1 FAIL=none` + full-log scan zero `ERROR)`/`bad switch` + `Exiting normally` present):

| Smoke | Result | | Smoke | Result |
|---|---|---|---|---|
| phase13_smoke | PASS=1 | | phase16_pick_smoke | PASS=1 |
| phase14_gui_smoke | PASS=1 | | phase16_placement_smoke | PASS=1 |
| phase14_mol_smoke | PASS=1 | | phase16_restart_smoke | PASS=1 |
| phase15_smoke | PASS=1 | | phase16_smoke | PASS=1 |
| phase15_backup_smoke | PASS=1 | | phase17_tiers_smoke | PASS=1 |
| phase15_game_smoke | PASS=1 | | phase17_bonded_smoke | PASS=1 |
| phase15_mutation_smoke | PASS=1 | | phase17_dispatch_smoke | PASS=1 |
| phase16_entry_smoke | PASS=1 | | phase17_points_smoke | PASS=1 |
| phase16_gametab_smoke | PASS=1 | | phase17_lines_smoke | PASS=1 |
| phase16_hiders_smoke | PASS=1 | | phase17_licorice_smoke | PASS=1 |
| phase16_onpick_smoke | PASS=1 | | phase17_cpk_smoke | PASS=1 |
| phase16_smoke ran as phase16_smoke | PASS=1 | | phase17_dynbonds_smoke | PASS=1 |
| | | | **phase17_capstone_smoke** | **PASS=1** |

(24/24 PASS. phase16_smoke appears once in the table above — the inventory is: 15 legacy [13/14×2/15×4/16×8] + 9 phase-17 [tiers/bonded/dispatch/points/lines/licorice/cpk/dynbonds/capstone].)

**Gates:** `grep -rnE "\blmap\b|\btry\b|\bthrow\b|\btailcall\b|\bcoroutine\b|\byield\b|\bfinally\b" vmd/lib/ vmd/gui/` → **0**; `grep -rnE "grab set" vmd/gui/` → **0**.

**Run hygiene:** every one of the 29 logs contains exactly 1 `Info) Exiting normally`; VMD process exit codes varied (0/5) — ignored per the established marker-parse contract.

## Decisions Made
- **Round-B invariant asserts replace the plan's literal `{Licorice 4}` pins** (see key-decisions; evidence-backed: gate run 2 drew n<4). The lock-scene derivation proof is carried by the single-key `{Licorice}` assert + the P9 consistency chain; the drawn count is explicitly range-checked [1, 4].
- **Task 2 committed no code** — verification-only task; the gate table lives in this SUMMARY (an empty commit would violate repo commit hygiene).
- **Capstone runs the on_pick sequence directly** (mark_found_visual + registry::mark_found per find), matching the dispatch-smoke precedent — game_logic state gating is not exercised headlessly (picks cannot fire in text mode).

## Deviations from Plan

### Auto-fixed Issues

**1. [Plan-prose vs pinned contract — resolved per the 17.1-06 precedent] Round-B literal pins → invariant asserts**

- **Found during:** Task 1 (pre-implementation contract analysis)
- **Issue:** The plan's round B pinned "remaining_by_rep == {Licorice 4}; hider count 4". `resolve_per_rep` with empty per_rep + lock_scene=1 routes through `randomize_per_rep`, whose pinned quick-008 contract is a NON-EMPTY SUBSET with sum ≤ hider_count (14-01 sum_le_total), NOT == total. With a single allowed tier the subset is forced to {Licorice}, but the drawn count is `_randint(0, 4)`-dependent ∈ {1..4} — the literal pins would flake on 3 of 5 draws.
- **Fix:** Assert per_rep keys == {Licorice} EXACTLY (the derivation proof), n = per_rep(Licorice) ∈ [1,4], and the consistency chain hider_count == effective_total(per_rep) == registry count == n == atoms−424; remaining_by_rep == {Licorice n}. Documented in the smoke header (PLAN-DETAIL NOTE).
- **Files modified:** vmd/smoke/phase17_capstone_smoke.tcl (this plan's own file — in scope)
- **Verification:** 3/3 runs PASS=1 with independent PRNG draws; the gate's capstone run 2 drew n<4 (no 428-atom molecule in its log) while runs 1/3 drew n=4 — the invariant form absorbed both; the literal pin would have failed run 2.
- **Committed in:** 89c0553

---

**Total deviations:** 1 (plan-prose-vs-pinned-contract resolution, same class as the 17.1-06 dispatch smoke's documented step-8 resolution; no lib files touched, no scope creep)
**Impact on plan:** The capstone proves MORE than the literal pins (derivation + consistency at every draw), and the full gate is green. No scope creep.

## Issues Encountered
- None blocking. Notes: (a) `tclsh` remains absent from PATH — all suites ran under headless VMD per the 13-01 pattern; (b) phase13_smoke takes ~17 min per run (its internal long-poll design — expected, gate timeout sized accordingly); (c) VMD process exit codes are unreliable (0/5 variance) — markers only, as pinned since 13-01.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- **Phase 17.1 is headless-green end-to-end**: infrastructure (rep_tiers/registry/dispatch/hiders/generators) + all 6 implemented simple tiers (Lines/VDW/Licorice/CPK/Points/DynamicBonds) proven by per-tier smokes AND the mixed/lock-scene/under-generation capstone, plus the full-suite regression gate. Only **17.1-14 (the single consolidated GUI human-verify checkpoint)** remains to close the phase.
- **Carry-forward for 17.1-14 (unchanged from 17.1-07/11/12):** `vmd/tests/pick_verify.tcl` still reads the REMOVED `hiders::hidden_rep`/`found_rep` namespace vars — it WILL break the GUI verify session; update it to the tier_reps-dict + `mol repindex` resolution (mirror phase16_smoke's 2d block) BEFORE any GUI verify work.
- **GUI session should reuse this capstone's mixed/lock-scene shapes** as its scripted scenario: a mixed per_rep round + a lock_scene=1 round with a pre-styled Licorice scene are exactly the SC2 user flows now proven headlessly.
- No blockers introduced by this plan; zero lib-file changes.

---
*Phase: 17.1-rep-setup-infrastructure-simple-rep-generators*
*Completed: 2026-09-04*
