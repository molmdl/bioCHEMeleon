---
phase: 17.1
plan: 12
type: execute
wave: 5
depends_on: ["17.1-06", "17.1-08"]
files_modified: [vmd/smoke/phase17_dynbonds_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "The DynamicBonds hider rep is created with the EXPLICIT cutoff: style args {DynamicBonds 1.6} and the read-back string-compares to `DynamicBonds 1.6` exactly (the default 3.0 draws spurious 2.3–2.9 Å bonds — P-5)"
    - "Hider offsets are strictly < 1.6 (nearest-real-atom distance per hider ∈ [1.19, 1.61] with the placement band 1.2–1.6) so every hider stub draws under the explicit cutoff (strict < test)"
    - "A hiders-only DynamicBonds render draws dots (0 FCylinders — both-endpoints rule); the scene (pre-styled DynamicBonds) draws the stubs (scene-diff cylinders >= n)"
    - "Registry remaining_by_rep == {DynamicBonds n}; per-tier found-marking partitions"
  artifacts:
    - path: "vmd/smoke/phase17_dynbonds_smoke.tcl"
      provides: "DynamicBonds-tier end-to-end proof incl. the explicit-cutoff read-back and offset-vs-cutoff constraint"
  key_links:
    - from: "vmd/smoke/phase17_dynbonds_smoke.tcl"
      to: "vmd/lib/rep_tiers.tcl"
      via: "style_args {DynamicBonds} → {DynamicBonds 1.6} flows through tier_specs → mol modstyle"
      pattern: "DynamicBonds 1.6"
---

<objective>
Prove the DynamicBonds tier end-to-end — the explicit-cutoff rep.

Purpose: DynamicBonds recomputes bonds per-frame from its cutoff param; the DEFAULT 3.0 Å is inclusive-ish and draws spurious 2.3–2.9 Å bonds (probe p7 sweep). The generator rule is: explicit ~1.6 cutoff + hider offsets strictly < 1.5. This smoke pins the read-back-with-params validation (the one style whose read-back is multi-word) and the offset-vs-cutoff constraint.
Output: `vmd/smoke/phase17_dynbonds_smoke.tcl` PASS=1.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-RESEARCH-simple-generators.md
@.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-RESEARCH-game-reps-viability.md
@vmd/smoke/phase17_points_smoke.tcl
@vmd/smoke/phase17_licorice_smoke.tcl

Key facts (do NOT re-derive):
- DynamicBonds semantics (probe E/G/p7): param 1 = bond distance cutoff (STRICT < — pairs at exactly the cutoff do NOT bond), default 3.0; param 2 = bond radius. `mol modstyle $i $m DynamicBonds 1.6` reads back `DynamicBonds 1.6` — the read-back string-compare validates the FULL param string (`[join $sargs " "]`); bare `DynamicBond` / `Point` are INVALID no-ops (never hand-type style strings — always rep_tiers::style_args).
- Hiders-only rep + both-endpoints → dots, not bonds; the scene rep draws the stub. Pre-styled-scene pattern per 17.1-10 (`mol modstyle 0 $m DynamicBonds` — bare, as a user scene would be; the scene rep's own default cutoff applies to IT, which is the user's choice, not ours).
- Offset constraint: placement band [1.2, 1.6]; nearest-real-atom distance per hider ∈ [1.19, 1.61]; the hider-anchor bond (< 1.6 for anchors with cutoff ≤ 1.95) always draws under the 1.6 hider-rep cutoff — assert nearest-real < 1.6 strictly per hider (offset-vs-cutoff constraint).
- Tachyon technique + regex starters + element colors + salmon trap + staging/harness: per 17.1-08. Demo 1znf (424 atoms), `start_game $molid 3 [dict create DynamicBonds 3] 0`.
- THIS PLAN EDITS NO LIB FILES (defects → SUMMARY for a gap plan).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write + run the DynamicBonds-tier end-to-end smoke</name>
  <files>vmd/smoke/phase17_dynbonds_smoke.tcl</files>
  <action>
Copy the 17.1-08 harness; demo 1znf; pre-start `mol modstyle 0 $m DynamicBonds` (bare — user-scene parity); round `game::start_game $molid 3 [dict create DynamicBonds 3] 0`:
1. Round asserts: atoms 427; hider idxs {424 425 426}; user3 ≈ 1.0; registry count 3; remaining_by_rep DynamicBonds == 3 (the registry rep field carries the BARE GAME_REPS name even though the style args are multi-word — assert the key is `DynamicBonds`, not `DynamicBonds 1.6`).
2. Mimicry + bonds: as 17.1-08 (numbonds ≥ 1 — certifies Lines/Licorice/CPK even though DynamicBonds ignores the bond list).
3. Offset-vs-cutoff: per hider, nearest-real-atom distance ∈ [1.19, 1.60] (strictly < 1.6 — compute hider coords vs all real coords once per hider; ≤3 hiders × 424 atoms is fine in tcl).
4. Rep read-back (the multi-word validation): hidden/found reps' read-back style == `DynamicBonds 1.6` EXACTLY (via tier_reps names + repindex + COMBINED-braces); hidden color `Element`; selections `... and user3 1`; numreps == pre_start + 2.
5. Tachyon renders:
   a. Baseline-zero.
   b. Hider-rep exclusive (selection `resname GAM and beta < 0`, style args `DynamicBonds 1.6` via {*}, color `Element`): FCylinder count == 0 (hiders ≥ 4 Å apart — no GAM-GAM pairs within 1.6) + sphere-dot count == 3; NO salmon.
   c. Scene-diff (scene rep DynamicBonds, selection all): (A.cyl − B.cyl) ≥ 3 (stubs drawn by the scene rep); (A.sph − B.sph) == 0. Delete the probe rep after renders.
6. Found-marking: mark 426 → found 1 / hidden 2; remaining_by_rep DynamicBonds == 2.
7. Cleanup restore (rep-0 style reads back `DynamicBonds`) + marker + exit.
  </action>
  <verify>Staged run: `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log; read-back style equality with `DynamicBonds 1.6` asserted; hider-rep render 0 FCylinders.</verify>
  <done>DynamicBonds tier proven: explicit-cutoff style args flow through the dispatch, multi-word read-back validation works, offsets are cutoff-safe, per-tier partition + restore hold.</done>
</task>

</tasks>

<verification>
- `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log
- Registry key is the bare `DynamicBonds` name; read-back is the full `DynamicBonds 1.6`
- Tcl 8.6 gate zero; no lib files touched
</verification>

<success_criteria>
- The P-5 spurious-bond rule (explicit 1.6 + offsets < 1.6) is proven end-to-end through the dispatch
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-12-SUMMARY.md`
</output>
