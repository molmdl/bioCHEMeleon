---
phase: 17.1
plan: 10
type: execute
wave: 5
depends_on: ["17.1-06", "17.1-08"]
files_modified: [vmd/smoke/phase17_licorice_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "A Licorice-tier round produces bonded hiders; the hider rep renders bond-radius spheres at the hider positions (0 FCylinders — both-endpoints rule)"
    - "With the scene PRE-STYLED to Licorice, the scene-diff shows BOTH the hider spheres (+n) and the stub cylinders (+>=n) — ball+stick blending proven in-scene"
    - "Read-back validation: style string-compares to `Licorice` exactly; registry remaining_by_rep == {Licorice n}; per-tier found-marking partitions"
  artifacts:
    - path: "vmd/smoke/phase17_licorice_smoke.tcl"
      provides: "Licorice-tier end-to-end proof incl. the pre-styled-scene render diff (the pattern CPK/DynamicBonds reuse)"
  key_links:
    - from: "vmd/smoke/phase17_licorice_smoke.tcl"
      to: "vmd/lib/game.tcl"
      via: "game::start_game $molid n {Licorice n} 0 after mol modstyle 0 $m Licorice"
      pattern: "start_game"
---

<objective>
Prove the Licorice tier end-to-end, including blending into a scene that is itself styled Licorice.

Purpose: Licorice = ball+stick (UG node63): lone atoms draw a bond-radius sphere, bonded pairs add the cylinder. Pre-styling the scene rep to Licorice before start_game proves the full blend geometry in-scene (snapshot carries the style through backup::apply — also exercises lock-scene-adjacent style survival).
Output: `vmd/smoke/phase17_licorice_smoke.tcl` PASS=1.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-RESEARCH-simple-generators.md
@.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-RESEARCH-game-reps-viability.md
@vmd/smoke/phase17_points_smoke.tcl

Key facts (do NOT re-derive):
- Licorice render semantics (probe E/p6b): bonded pair with both endpoints selected → 2 spheres + 2 FCylinders (ball+stick; same-color halves merge); lone/unpartnered selected atom → bond-radius sphere, no cylinder. Hiders-only rep → n spheres + 0 FCylinders.
- Licorice defaults read back as bare `Licorice` (params optional) — style_args gives `{Licorice}`.
- PRE-STYLED SCENE pattern (new in this smoke, reused by 17.1-11/12): on the ORIGINAL molecule BEFORE start_game, `mol modstyle 0 $m Licorice` (the default rep, selection all) → backup::snapshot captures it → backup::apply re-applies it on the game molecule → the scene diff measures the actual Licorice blend. (mol modstyle on rep 0 of a fresh demo is safe; canonical spelling from GAME_REPS.)
- Tachyon technique + regex starters + element colors + salmon trap + staging/harness: per 17.1-08. Demo 1k8p (555 atoms — exercises the second bundled demo), `start_game $molid 3 [dict create Licorice 3] 0`.
- THIS PLAN EDITS NO LIB FILES (defects → SUMMARY for a gap plan).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write + run the Licorice-tier end-to-end smoke</name>
  <files>vmd/smoke/phase17_licorice_smoke.tcl</files>
  <action>
Copy the 17.1-08 harness; demo 1k8p; BEFORE start_game: `mol modstyle 0 $m Licorice` and capture pre_start numreps. Round `game::start_game $molid 3 [dict create Licorice 3] 0`:
1. Round asserts: atoms 558; hider idxs {555 556 557}; user3 ≈ 1.0; registry count 3; remaining_by_rep Licorice == 3.
2. Mimicry + bonds: as 17.1-08 (element/radius/name; numbonds ≥ 1).
3. Rep read-back: hidden/found style == `Licorice` exactly; Element / ColorID 7; selections `... and user3 1`; numreps == pre_start + 2.
4. Tachyon renders:
   a. Baseline-zero (harness proof).
   b. Hider-rep exclusive (selection `resname GAM and beta < 0`, style `Licorice`, color `Element`): sphere count == 3 (bond-radius balls); FCylinder count == 0 (both-endpoints); colors ∈ element set; NO salmon.
   c. Scene-diff (scene rep now Licorice, selection all): A=`all` vs B=`not resname GAM` → (A.sph − B.sph) == 3 (hider balls) AND (A.cyl − B.cyl) ≥ 3 (hider stubs). Delete the probe rep after renders.
5. Found-marking: mark 556 → found 1 / hidden 2 (user3 1 conjunct); remaining_by_rep Licorice == 2.
6. Cleanup: restored original numreps == pre_start and its rep-0 style reads back `Licorice` (snapshot fidelity); registry 0; game molid dead. Marker + exit.
  </action>
  <verify>Staged run: `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log; scene-diff shows +3 spheres AND +≥3 cylinders.</verify>
  <done>Licorice tier proven: balls in the hider rep, full ball+stick blend in a Licorice scene, exact read-back, per-tier partition, style-faithful restore.</done>
</task>

</tasks>

<verification>
- `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log
- Tcl 8.6 gate zero; no lib files touched
</verification>

<success_criteria>
- HIDER-04's Licorice semantics proven incl. in-scene blend geometry
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-10-SUMMARY.md`
</output>
