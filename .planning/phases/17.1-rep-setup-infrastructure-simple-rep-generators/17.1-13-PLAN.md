---
phase: 17.1
plan: 13
type: execute
wave: 6
depends_on: ["17.1-07", "17.1-08", "17.1-09", "17.1-10", "17.1-11", "17.1-12"]
files_modified: [vmd/smoke/phase17_capstone_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "A MIXED 3-tier round (VDW 2 + Lines 2 + Licorice 1) runs end-to-end: 5 records, base+6 reps, tier codes 1..3 stamped, registry remaining_by_rep {VDW 2 Lines 2 Licorice 1}, per-tier found partitions, cleanup restores"
    - "A LOCK-SCENE round (scene pre-styled Licorice, lock_scene 1) derives all hiders onto the detected tier: remaining_by_rep == {Licorice n}"
    - "The FULL suite gate is green: all 4 tcltest suites + all 15+ smokes PASS with zero ERROR)/bad switch, and the Tcl 8.6 gate returns zero"
  artifacts:
    - path: "vmd/smoke/phase17_capstone_smoke.tcl"
      provides: "mixed-tier + lock-scene headless capstone (the phase's SC2 acceptance proof)"
  key_links:
    - from: "vmd/smoke/phase17_capstone_smoke.tcl"
      to: "vmd/lib/{game,rep_tiers,hiders}.tcl"
      via: "mixed per_rep + lock_scene=1 rounds through start_game"
      pattern: "start_game"
---

<objective>
Capstone headless smoke: a mixed multi-tier round AND a lock-scene round through the real dispatch, plus the full-suite green gate.

Purpose: the per-tier smokes prove single tiers; the capstone proves the phase's actual acceptance shape — tiers MIXING in one round with correct per-tier bookkeeping, and the lock-scene derivation producing a scene-matched round (ROADMAP SC2). The full-suite gate declares the whole phase headless-green before the GUI session.
Output: `vmd/smoke/phase17_capstone_smoke.tcl` PASS=1 + full-suite green log.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-RESEARCH-rep-infra.md
@vmd/smoke/phase17_dispatch_smoke.tcl
@vmd/lib/game.tcl

Key facts (do NOT re-derive):
- Tier order is GAME_REPS order (Lines VDW Licorice CPK Points DynamicBonds): per_rep {VDW 2 Lines 2 Licorice 1} → tiers = {1 Lines 2} {2 VDW 2} {3 Licorice 1}; hider index slices follow that order (file order == record order).
- Lock-scene headless recipe: load demo → `mol modstyle 0 $m Licorice` (scene now has a GAME_REPS-style rep) → `start_game $molid n {} 1` → resolve_per_rep detects {Licorice} → randomize puts ALL n on Licorice → remaining_by_rep == {Licorice n}.
- Contracts per 17.1-06/08; Tachyon technique available but NOT required here (per-tier smokes own renders; the capstone owns round composition + bookkeeping).
- Staging + harness conventions per 17.1-08. Full-suite gate = run EVERY suite/smoke in vmd/tests + vmd/smoke staged, grep each log for its marker AND `ERROR)`/`bad switch`.
- THIS PLAN EDITS NO LIB FILES (defects → SUMMARY for a gap plan).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mixed-tier + lock-scene capstone smoke</name>
  <files>vmd/smoke/phase17_capstone_smoke.tcl</files>
  <action>
Harness per 17.1-08. Demo 1znf:
A. MIXED round: `set gs [game::start_game $molid 5 [dict create VDW 2 Lines 2 Licorice 1] 0]`
   1. atoms 429; hider idxs {424..428}; tier codes: derive via rep_tiers::tiers_from_per_rep then assert user3 slices — code 1 (Lines) ≈ 1.0 on idxs 424-425, code 2 (VDW) ≈ 2.0 on 426-427, code 3 (Licorice) ≈ 3.0 on 428.
   2. numreps == pre_start + 6 (3 pairs); read-back per pair via tier_reps + repindex: style Lines/VDW/Licorice respectively, hidden Element / found ColorID 7, selections with `user3 1|2|3`.
   3. Registry: count 5; remaining_by_rep keys {Lines VDW Licorice} with counts {2 2 1}; set_difficulty-agnostic.
   4. Find one per tier (mark 425, 426, 428): remaining 2; remaining_by_rep {Lines 1 VDW 1}; per-tier user2 partitions exact.
   5. `game::cleanup $gs` → registry 0; game molid dead; restored original intact.
B. LOCK-SCENE round: fresh load 1znf → `mol modstyle 0 $m2 Licorice` → `set gs2 [game::start_game $m2 4 {} 1]` → assert: remaining_by_rep == {Licorice 4} (ALL hiders on the detected tier); hider count 4; user3 ≈ 1.0 on all; the Licorice pair's read-back selections carry `user3 1`; numreps == 1 + 2 (scene had 1 rep) + ... compute: pre-styled scene numreps (1) → +2 = 3. Cleanup gs2 → restored original's rep-0 style still reads `Licorice` (snapshot fidelity).
C. Under-generation tolerance: fresh load; `start_game $m3 3 [dict create Cartoon 2 VDW 1] 0` → Cartoon dropped with a warning line in the log; registry count == 1; remaining_by_rep == {VDW 1}; game_state hider_count == 1 (effective total — P9). Cleanup.
D. Marker + exit.
  </action>
  <verify>Staged run: `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log (the Cartoon-drop warning is a vmdcon -warn, NOT an `ERROR)`).</verify>
  <done>Mixed-tier bookkeeping, lock-scene derivation, and under-generation tolerance all proven headlessly.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite green gate</name>
  <files></files>
  <action>
Run EVERY pure test + smoke staged and record a results table in the SUMMARY:
- tcltest suites: test_setup_state, test_registry, test_generators, test_rep_tiers, test_game_logic → each `BCHM_TEST_RESULT ... Failed=0`.
- smokes (13 + 15/16 legacy): phase13_smoke, phase14_gui_smoke, phase14_mol_smoke, phase15_smoke, phase15_backup_smoke, phase15_game_smoke, phase15_mutation_smoke, phase16_entry_smoke, phase16_gametab_smoke, phase16_hiders_smoke, phase16_onpick_smoke, phase16_pick_smoke, phase16_placement_smoke, phase16_restart_smoke, phase16_smoke, phase17_tiers_smoke, phase17_bonded_smoke, phase17_dispatch_smoke, phase17_points_smoke, phase17_lines_smoke, phase17_licorice_smoke, phase17_cpk_smoke, phase17_dynbonds_smoke, phase17_capstone_smoke → each `BCHM_SMOKE_RESULT PASS=1 FAIL=none`.
- Full-log scan per run: zero `ERROR)` AND zero `bad switch` (false-PASS guard).
- Gates: `grep -rnE "\blmap\b|\btry\b|\bthrow\b|\btailcall\b|\bcoroutine\b|\byield\b|\bfinally\b" vmd/lib/ vmd/gui/` → zero; `grep -rnE "grab set" vmd/gui/` → zero.
If ANY suite/smoke is red: diagnose; if the fix is inside THIS plan's file scope (only phase17_capstone_smoke.tcl) fix and re-run; otherwise record the defect in the SUMMARY for a gap-closure plan (do NOT touch other files).
  </action>
  <verify>The recorded table shows every suite Failed=0 and every smoke PASS=1, with the two grep gates returning zero.</verify>
  <done>The entire 17.1 headless surface is green on fresh staging.</done>
</task>

</tasks>

<verification>
- Capstone smoke PASS=1 (mixed + lock-scene + under-generation), zero `ERROR)` / `bad switch`
- Full-suite table: all tcltest Failed=0; all smokes PASS=1; both grep gates zero
</verification>

<success_criteria>
- ROADMAP SC2 (lock-scene detects + matches; randomize distributes) and the 10-rep GAME_REPS validation coverage are proven at the composition-root level
- The phase is headless-green and ready for the single consolidated GUI checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-13-SUMMARY.md`
</output>
