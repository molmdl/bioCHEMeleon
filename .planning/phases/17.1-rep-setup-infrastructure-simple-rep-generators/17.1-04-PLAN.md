---
phase: 17.1
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [vmd/lib/hiders.tcl, vmd/smoke/phase17_tiers_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "add_hider_reps {molid tier_specs} adds ONE hidden/found rep PAIR per active tier (2N reps) with the tier's style, Element coloring on hidden, ColorID 7 on found, and per-tier user3-conjoined sentinel selections"
    - "Rep pairs are tracked by STABLE NAME (mol repname captured immediately after addrep post-apply) and resolved via mol repindex at use time with a -1 guard — never by cached index"
    - "Every rep's style is validated by molinfo read-back string-compare (catch NEVER fires on a bad style — viability P-1)"
    - "stamp_tier_codes {molid tier_of} sets numeric user3 tier codes per atom batch ($sel set user3 <int> broadcasts; non-numeric errors — P7/P10)"
    - "mark_found_visual {molid idx} keeps its signature, sets user2=1, and re-asserts the STORED literal selection strings of ALL pairs (resolved via repindex) — the mandatory re-evaluation re-assert"
    - "Neither rep is EVER hidden (no mol showrep off anywhere); beta is NEVER written"
  artifacts:
    - path: "vmd/lib/hiders.tcl"
      provides: "N-tier rep-pair management: add_hider_reps (tier_specs), stamp_tier_codes, mark_found_visual; name-keyed tier_reps/tier_styles namespace dicts replace hidden_rep/found_rep index vars"
      exports: ["add_hider_reps", "stamp_tier_codes", "mark_found_visual"]
    - path: "vmd/smoke/phase17_tiers_smoke.tcl"
      provides: "headless 2-tier proof: mutate → stamp → add pairs → read-back asserts → per-tier partition after a find → repindex -1 guard"
  key_links:
    - from: "vmd/lib/hiders.tcl (tier_reps selections)"
      to: "atom user3 field"
      via: "per-tier selection conjunct 'user3 <code>'; stamp_tier_codes writes user3 BEFORE add_hider_reps in the production order (static single-frame molecules never re-evaluate cached selections on field change)"
      pattern: "user3"
    - from: "vmd/lib/game.tcl (17.1-06)"
      to: "vmd/lib/hiders.tcl"
      via: "stamp_tier_codes then add_hider_reps in start_game; mark_found_visual from on_pick"
      pattern: "hiders::(stamp_tier_codes|add_hider_reps|mark_found_visual)"
---

<objective>
Generalize `vmd/lib/hiders.tcl` from the Phase-16 2-rep VDW pair to N per-tier hidden/found rep pairs, plus its headless smoke.

Purpose: VMD colors/styles per-REP — a Lines-tier hider must render in a Lines rep to blend. One hidden/found pair per ACTIVE tier (2N reps) with static per-tier selections is the probe-verified design (research rep-infra RQ4, probe3 end-to-end). This plan is the mol bridge all generators dispatch through.
Output: generalized `vmd/lib/hiders.tcl` + `vmd/smoke/phase17_tiers_smoke.tcl` (PASS=1).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-RESEARCH-rep-infra.md
@.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-RESEARCH-game-reps-viability.md
@vmd/lib/hiders.tcl
@vmd/smoke/phase16_hiders_smoke.tcl

Key facts (do NOT re-derive — probe-verified):
- Rep names are MONOTONIC and never reset: `mol repname $m $i` captured IMMEDIATELY AFTER each addrep (post-apply) is stable for the round; `mol repindex $m $name` resolves the current index; deleted/unknown names return -1 (NO error); `mol repname` with an out-of-range INDEX errors (P1/P2). Never cache indices across rep mutations.
- One pair per tier (research RQ4.1): hidden tier-t = style + `mol modcolor Element` + `mol modselect {resname GAM and beta < 0 and user2 < 1 and user3 <t>}`; found tier-t = same style + `mol modcolor ColorID 7` + `{resname GAM and beta < 0 and user2 > 0 and user3 <t>}`. Verified end-to-end on 2 tiers (probe3, read-back byte-identical).
- user3 is the per-atom tier channel: settable on multi-atom selections with a NUMERIC scalar (broadcast; non-numeric → "bad data in 0th element" P7); selectable exact-match `user3 1`; read-back is FLOAT (1.0) — compare numerically only (P10). Real atoms default 0 — the `resname GAM and beta < 0` conjunct must NEVER be relaxed (P6).
- COMBINED-BRACES read-back ONLY: `molinfo $m get "{rep $i} {selection $i} {color $i} {material $i}"` (single-field form FAILS). Style validation = read-back string-compare, NEVER catch (catch never fires on a bad style; `mol modstyle` with an out-of-range rep index silently no-ops — guard indices against numreps).
- DynamicBonds needs an explicit cutoff: the tier's style ARGLIST is `{DynamicBonds 1.6}` → call `mol modstyle $i $m {*}$sargs`; read-back echoes `DynamicBonds 1.6` (compare against `[join $sargs " "]`). Other tiers: bare single-word styles.
- ORDERING CONTRACT (production, enforced by game.tcl 17.1-06): user3 is STAMPED BEFORE add_hider_reps — a static single-frame molecule never re-evaluates cached rep selections on an atom-field change (the mandatory-modselect-reassert lesson), so a rep added before stamping would cache an empty selection.
- NEVER-HIDE rule (UG node140): hidden reps cannot be picked — `mol showrep off` is NEVER issued; neither rep is ever hidden. NEVER write beta. Always `$sel delete`. One `variable` per line. Tcl 8.5 only.
- modselect re-assert: after EVERY user2 write, re-issue modselect for ALL pairs with the UNCHANGED literal strings (timestep-based re-eval only). Storing each pair's exact selection strings in the namespace dict at add time keeps the strings literally unchanged at re-assert time.
- Staging + smoke conventions: copy phase16_hiders_smoke.tcl's harness (failures list + `_bail` + per-step catch + `BCHM_SMOKE_RESULT PASS=1 FAIL=none` marker + `exit` + `[pwd]`-based sourcing of lib files in dep order: setup_state, registry, generators, game_logic, demos, backup, mutation, hiders — registry sourced EXACTLY ONCE). NOTE: do NOT source rep_tiers.tcl in this smoke — plan 04 is wave-1 PARALLEL with 17.1-01 (which creates that file), so it may not exist in this worktree; this smoke calls zero rep_tiers:: procs (tier_specs are passed in literally). Later smokes (17.1-06+) add rep_tiers to their source order. Run:
  `mkdir -p tmp/biochemeleon-vmd && cp -r vmd tmp/biochemeleon-vmd/` then
  `timeout 300 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/smoke/phase17_tiers_smoke.tcl -eofexit < /dev/null' 2>&1 | grep -E "BCHM_SMOKE_RESULT|ERROR\)|bad switch"` — parse the marker, scan the FULL log.
- NOTE for 17.1-07: this plan REPLACES the `hidden_rep`/`found_rep` index variables — `vmd/smoke/phase16_smoke.tcl` (lines ~212-213) reads them and goes red until 17.1-07 updates it. Do NOT add compat shims; the header comment documents the replacement.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize hiders.tcl to N tier pairs</name>
  <files>vmd/lib/hiders.tcl</files>
  <action>
Rewrite hiders.tcl (keep the header-comment discipline; update the header to describe the N-tier design and the user3-stamp-before-add ordering contract). Namespace `::biochemeleon::hiders`:

Namespace vars (one per line):
- `tier_reps` (dict init `[dict create]`): tier_code → `{hidden_name found_name hidden_sel found_sel}`
- `tier_styles` (dict init `[dict create]`): tier_code → style string (`[join $sargs " "]`)
- Remove `hidden_rep`/`found_rep` entirely.

Exports: add_hider_reps, stamp_tier_codes, mark_found_visual.

`add_hider_reps {molid {tier_specs {{1 VDW}}}}`:
- tier_specs = ordered list of `{code styleArg1 styleArg2 ...}` (e.g. `{{1 VDW}}` or `{{1 VDW} {2 Lines}}` or `{{2 DynamicBonds 1.6}}`); code = 1-based int, styleArgs = the modstyle arg list from rep_tiers::style_args (caller passes it; this proc does not source rep_tiers — it stays standalone).
- `set base [molinfo $molid get numreps]`; iterate specs with a running rep index:
  - hidden: `mol addrep $molid`; idx = base+k; `set hname [mol repname $molid $idx]`; `mol modstyle $idx $molid {*}$sargs`; `mol modcolor $idx $molid Element`; `set hsel "resname GAM and beta < 0 and user2 < 1 and user3 $code"`; `mol modselect $idx $molid $hsel`.
  - found: `mol addrep`; same style; `mol modcolor ... ColorID 7`; `set fsel "resname GAM and beta < 0 and user2 > 0 and user3 $code"`; modselect.
  - VALIDATE both reps (P-1): read back via the COMBINED-braces form; assert read-back style == `[join $sargs " "]`, read-back selection == the exact built string, and the rep index < numreps — on any mismatch `error "hiders::add_hider_reps: style/selection read-back mismatch ..."`.
  - Record `dict set tier_reps $code [list $hname $fname $hsel $fsel]` and `dict set tier_styles $code [join $sargs " "]`.
- Return the tier_reps dict. NO showrep calls anywhere.

`stamp_tier_codes {molid tier_of}`:
- tier_of = dict tier_code → index list (from the dispatch split). For each code: `set sel [atomselect $molid "index $idxs"]`; `$sel set user3 $code` (NUMERIC int — never a string that isn't a number); `$sel delete`.

`mark_found_visual {molid idx}`:
- Guard: `tier_reps` non-empty AND `[molinfo $molid get numreps]` sane, else `error "hiders::mark_found_visual: hider reps not added -- call add_hider_reps first"`.
- `set sel [atomselect $molid "index $idx"]`; `$sel set user2 1`; `$sel delete`.
- For EVERY code in tier_reps (dict for): `set hidx [mol repindex $molid $hidden_name]`; guard -1 or ≥ numreps → error "hider rep '<name>' is gone (repindex -1)"; `mol modselect $hidx $molid $hidden_sel` (the STORED literal string); same for found_name/found_sel.

Tcl 8.5 throughout; braced expr; every atomselect deleted; the sentinel conjunct `resname GAM and beta < 0` verbatim in every selection.
  </action>
  <verify>`grep -c "mol showrep" vmd/lib/hiders.tcl` → 0; `grep -n "user3" vmd/lib/hiders.tcl` shows the per-tier conjuncts + numeric stamp; 8.6 gate zero on the file; no beta writes (grep `set beta` → 0).</verify>
  <done>hiders.tcl provides add_hider_reps (tier_specs, default single VDW pair), stamp_tier_codes, mark_found_visual (all-pairs stored-string re-assert), name-keyed tracking with repindex/-1 guards, read-back validation, no showrep, no beta writes.</done>
</task>

<task type="auto">
  <name>Task 2: Write + run the 2-tier headless smoke</name>
  <files>vmd/smoke/phase17_tiers_smoke.tcl</files>
  <action>
Model on vmd/smoke/phase16_hiders_smoke.tcl's harness (failures list + `_bail` + per-step catch + marker + exit; `[pwd]`-relative sourcing in dep order: setup_state, registry, generators, game_logic, demos, backup, mutation, hiders — registry exactly once; do NOT source rep_tiers.tcl — wave-1 parallel with 17.1-01, this smoke calls zero rep_tiers:: procs). Steps (probe3's flow, bug-fixed):
1. Load demo 1k8p (555 atoms) → `set gs [game::start_game $molid 5]` (2-arg backward-compat is fine for this smoke — it produces a VDW-only round via the CURRENT game.tcl, but this smoke only needs the mutated molecule) — NOTE: this smoke runs against the CURRENT (Phase-16) game.tcl; instead of relying on start_game's rep wiring, drive hiders.tcl DIRECTLY:
   - `set gm [dict get $gs game_molid]`; hider idxs via `resname GAM and beta < 0` → assert exactly {555 556 557 558 559}.
2. `hiders::stamp_tier_codes $gm [dict create 1 {555 556 557} 2 {558 559}]` → read back user3 per index (numeric compare: 555-557 ≈ 1.0, 558-559 ≈ 2.0).
3. `set tr [hiders::add_hider_reps $gm {{1 VDW} {2 Lines}}]` → assert numreps grew by EXACTLY 4 (2 pairs); assert tr has codes 1 and 2 with 4-element values; assert every name is non-empty and `[mol repindex $gm $hname]` resolves in range.
4. Read-back per rep via the COMBINED-braces form: pair-1 hidden = style `VDW` / color `Element` / selection exactly `resname GAM and beta < 0 and user2 < 1 and user3 1`; pair-1 found = `VDW` / `ColorID 7` / `... user2 > 0 and user3 1`; pair-2 hidden/found = style `Lines`, selections with `user3 2`.
5. Per-tier partition BEFORE any find: `atomselect $gm "resname GAM and beta < 0 and user2 < 1 and user3 1"` num == 3; `... user3 2` num == 2; delete sels.
6. `hiders::mark_found_visual $gm 558` (a tier-2 hider) → user2 of 558 > 0 (numeric); tier-1 hiders' user2 still ≤ 0; partition after: user3 1 hidden still 3; user3 2 found == 1, user3 2 hidden == 1.
7. Name resolution + delrep guard: `mol delrep [mol repindex $gm [lindex [dict get $::biochemeleon::hiders::tier_reps 2] 0]] $gm` (delete one game rep) → `catch {hiders::mark_found_visual $gm 555}` errors with the "gone (repindex" message (the -1 guard proof).
8. Style read-back validation positive case already in step 4; also assert tier_styles dict: code 1 → `VDW`, code 2 → `Lines`.
9. End marker `BCHM_SMOKE_RESULT PASS=1 FAIL=none` (or FAIL list via _bail); `exit`.
Do NOT call game::cleanup here (the delrep in step 7 intentionally broke the round; the smoke ends).
  </action>
  <verify>Staged run: `BCHM_SMOKE_RESULT PASS=1 FAIL=none` AND zero `ERROR)` / `bad switch` lines in the FULL output (false-PASS guard).</verify>
  <done>2-tier pairs, user3 stamping, per-tier partitions, name-keyed resolution, -1 guard, and read-back validation all proven headlessly on a real demo.</done>
</task>

</tasks>

<verification>
- `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log
- Tcl 8.6 gate zero on both files
- No `mol showrep`, no beta writes, sentinel conjunct verbatim in every selection string
- Known red until 17.1-07: phase16_smoke.tcl (hidden_rep/found_rep reads) — documented, not fixed here
</verification>

<success_criteria>
- game.tcl (17.1-06) can dispatch any tier mix through add_hider_reps/stamp_tier_codes with the name-keyed contract proven
- The Phase-16 never-hide / never-beta / re-assert invariants hold for N pairs
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-04-SUMMARY.md`
</output>
