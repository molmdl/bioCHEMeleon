---
phase: 17.1-rep-setup-infrastructure-simple-rep-generators
plan: 02
subsystem: registry
tags: [tcl, tcl-8.5, tcltest, vmd, registry, dict, tdd, rep-tiers]

# Dependency graph
requires:
  - phase: 15-mutation-safety-hider-registry
    provides: the PURE registry module (_records dict keyed by atom index, DI reconstruct_from_sentinels) and the mark_found error-wording convention
  - phase: 16-mvp-core-loop-sphere
    provides: the rep field semantics ("" = unstamped, remaining_by_rep skips "") and status_of/count_remaining surface the new API must preserve
provides:
  - registry::set_rep {idx rep} — per-record rep stamp, status-preserving, exact mark_found error wording on unregistered idx
  - registry::assign_reps {mapping} — ATOMIC bulk idx→rep dict assign (validate-every-key-then-apply, empty mapping = no-op)
  - 9 new tcltest cases pinning the error contract, atomicity, and multi-tier remaining_by_rep interaction (27-case suite)
affects: [17.1-06 game.tcl multi-tier dispatch, 17.2 cartoon generators, rep_tiers consumers]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "per-record complement pattern: reconstruct_from_sentinels called ONCE (tier-agnostic), then assign_reps stamps the tier→indices mapping — never reconstruct-per-tier (P8)"
    - "atomic dict mutation: validate every key in a first pass, apply in a second pass (no partial application)"
    - "registry stores rep values as given — GAME_REPS validation is the caller's (rep_tiers/game.tcl) job (RQ3.4)"

key-files:
  created: []
  modified:
    - vmd/lib/registry.tcl
    - vmd/tests/test_registry.test

key-decisions:
  - "set_rep preserves the record's status via the plan's copy-modify-writeback (read record dict → dict set rep on the copy → dict set _records) — status ('found') survives re-stamping"
  - "assign_reps stores rep values WITHOUT GAME_REPS validation — the registry is a dumb store; rep_tiers/game.tcl own tier validation (per plan behavior spec)"
  - "atomicity = validate-then-apply two-pass dict for, matching the plan's 'no partial state on error' truth"
  - "reconstruct_from_sentinels byte-unchanged (P8 guard): diff = export line + appended Phase 17.1 block only"

patterns-established:
  - "Per-record rep API contract: set_rep errors 'hider <idx> not registered' (exact mark_found wording); assign_reps aborts whole-mapping on ANY unregistered key"
  - "Multi-tier stamping shape for game.tcl (17.1-06): reconstruct once → assign_reps {idx rep ...} with the tier→indices split of fetch_hider_indices order"

# Metrics
duration: 10min
completed: 2026-09-03
---

# Phase 17.1 Plan 02: Registry Per-Record Rep API Summary

**Pure per-record rep API (set_rep + atomic assign_reps) TDD'd into the registry — reconstruct-once-then-assign multi-tier contract pinned by 9 new tests, 27/27 green under headless VMD**

## Performance

- **Duration:** 10 min
- **Started:** 2026-09-03T20:37:27Z
- **Completed:** 2026-09-03T20:47:22Z
- **Tasks:** 2 (RED + GREEN; REFACTOR not needed)
- **Files modified:** 2

## Accomplishments
- `registry::set_rep {idx rep}`: stamps ONE record's rep field (overwrites any prior value incl. ""), preserves status, errors `hider <idx> not registered` (exact mark_found wording) on unregistered idx
- `registry::assign_reps {mapping}`: ATOMIC bulk idx→rep assign — validates EVERY key before touching any record (no partial state on error); empty mapping is a no-op; rep values stored as given (GAME_REPS validation is the caller's job per RQ3.4)
- 9 new tcltest cases: set_rep sets/overwrites/errors/keeps-status + assign_reps bulk/atomic/empty-noop/after-mark_found + multi-tier total consistency (per-tier remaining sums to count_remaining)
- `reconstruct_from_sentinels` byte-unchanged (P8 guard held) — the new API is strictly additive; registry still PURE (no source lines, no mol/atomselect/tk, 8.6 gate zero)

## Task Commits

Each task was committed atomically (TDD):

1. **Task 1: RED — failing registry per-record rep tests** - `83df8d0` (test) — Total=27 Passed=18 Failed=9, every new case failing on `invalid command name "::biochemeleon::registry::set_rep"`, pre-existing 18 untouched-green
2. **Task 2: GREEN — implement set_rep + assign_reps** - `484d98f` (feat) — Total=27 Passed=27 Failed=0, purity + 8.6 gates clean, reconstruct byte-unchanged

**Plan metadata:** (this commit)

_REFACTOR: not needed — implementation landed minimal per the plan's prescribed mechanism._

## Files Created/Modified
- `vmd/lib/registry.tcl` — +40/−1: export line gains `set_rep assign_reps`; `# ---- Phase 17.1: per-record rep assignment ----` block with both procs (status-preserving set_rep; validate-then-apply assign_reps)
- `vmd/tests/test_registry.test` — +83: `# ---- Phase 17.1 additions ----` section with the 9 RED cases before the marker block

## Decisions Made
- **Status preservation mechanism:** plan's copy-modify-writeback (read record → `dict set rec rep $rep` on the copy → `dict set _records $idx $rec`) — explicit about the CAREFUL status-preservation requirement (mark_found's multi-level `dict set` one-liner would be equivalent)
- **No GAME_REPS validation in the registry:** assign_reps stores what it is given; tier validation belongs to rep_tiers/game.tcl (RQ3.4) — keeps the registry a pure dumb store and the API reusable for any tier naming
- **Atomicity shape:** first `dict for` validates every key, second applies via set_rep — the "no partial state on error" truth from the plan

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- game.tcl (17.1-06) can now stamp a multi-tier round: ONE `reconstruct_from_sentinels <fetch>` call, then ONE `assign_reps` with the tier→indices mapping (indices split from `fetch_hider_indices` file order per research §RQ3.2) — error contract and atomicity are test-pinned
- Downstream smoke note (P12): the multi-tier path changes the Phase-16 `numreps = base + 2` invariants when 2N tier pairs land — those smoke assertions are updated in the dispatch plan, not here (registry-only change is invisible to them)
- Registry purity + Tcl 8.5 constraints preserved; suite is 27 cases, all runnable under the established headless-VMD tcltest runner

---
*Phase: 17.1-rep-setup-infrastructure-simple-rep-generators*
*Completed: 2026-09-03*
