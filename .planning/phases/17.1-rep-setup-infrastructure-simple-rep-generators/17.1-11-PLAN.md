---
phase: 17.1
plan: 11
type: execute
wave: 5
depends_on: ["17.1-06", "17.1-08"]
files_modified: [vmd/smoke/phase17_cpk_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "A CPK-tier round produces bonded hiders; the hider rep renders CPK-scaled spheres (0 FCylinders in a hiders-only render — both-endpoints rule)"
    - "With the scene PRE-STYLED to CPK, the scene-diff shows hider spheres (+n) and stub cylinders (+>=n); hider sphere radii are the CPK 0.25x VDW scale (ratio check vs a VDW render)"
    - "Read-back validation: style string-compares to `CPK` exactly; registry remaining_by_rep == {CPK n}"
  artifacts:
    - path: "vmd/smoke/phase17_cpk_smoke.tcl"
      provides: "CPK-tier end-to-end proof incl. the 0.25x scale-ratio check (UG node62 'too-small radii are not drawn' guarded)"
  key_links:
    - from: "vmd/smoke/phase17_cpk_smoke.tcl"
      to: "vmd/lib/game.tcl"
      via: "game::start_game $molid n {CPK n} 0 after mol modstyle 0 $m CPK"
      pattern: "start_game"
---

<objective>
Prove the CPK tier end-to-end (VDW + bonds at the CPK 0.25× scale).

Purpose: CPK = VDW spheres scaled 0.25× + bond cylinders (UG node62, probe-measured). The scale ratio is the one per-tier render fact worth pinning headlessly (a wrong scale breaks blending); everything else reuses the 17.1-08/10 template.
Output: `vmd/smoke/phase17_cpk_smoke.tcl` PASS=1.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-RESEARCH-simple-generators.md
@.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-RESEARCH-game-reps-viability.md
@vmd/smoke/phase17_points_smoke.tcl
@vmd/smoke/phase17_licorice_smoke.tcl

Key facts (do NOT re-derive):
- CPK render semantics (probe E/p6 + UG node62): bonded pair both-selected → spheres + FCylinders; lone atom → sphere at 0.25× the VDW scale (probe-measured 0.25×), no cylinder. CPK accepts bare form or 4 args (viability §6.6) — style_args gives `{CPK}`.
- VDW radius at render level (probe K): an element-C atom exports Rad 0.094504 under VDW (res 8.0, scale 1.0 defaults). CPK hider Rad ≈ 0.25 × that. The ratio check: render the SAME atoms under VDW and CPK on the probe rep and assert mean(CPK Rad) / mean(VDW Rad) ∈ [0.20, 0.30].
- Tachyon technique + regex starters + element colors + salmon trap + staging/harness: per 17.1-08; pre-styled-scene pattern per 17.1-10. Demo 1k8p (555 atoms), `start_game $molid 3 [dict create CPK 3] 0`, `mol modstyle 0 $m CPK` pre-start.
- THIS PLAN EDITS NO LIB FILES (defects → SUMMARY for a gap plan).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write + run the CPK-tier end-to-end smoke</name>
  <files>vmd/smoke/phase17_cpk_smoke.tcl</files>
  <action>
Copy the 17.1-10 harness (pre-styled scene); demo 1k8p; `mol modstyle 0 $m CPK` pre-start; round `game::start_game $molid 3 [dict create CPK 3] 0`:
1. Round asserts: atoms 558; hider idxs {555 556 557}; user3 ≈ 1.0; registry count 3; remaining_by_rep CPK == 3.
2. Mimicry + bonds: as 17.1-08.
3. Rep read-back: hidden/found style == `CPK` exactly; Element / ColorID 7; `... and user3 1`; numreps == pre_start + 2.
4. Tachyon renders:
   a. Baseline-zero.
   b. Hider-rep exclusive (selection `resname GAM and beta < 0`, style `CPK`, color `Element`): sphere count == 3; FCylinder count == 0; colors ∈ element set; NO salmon.
   c. Scale ratio on the probe rep (same selection): re-style the probe rep VDW → render → mean sphere Rad; re-style CPK → render → mean sphere Rad; ratio ∈ [0.20, 0.30].
   d. Scene-diff (scene rep CPK, selection all): (A.sph − B.sph) == 3 AND (A.cyl − B.cyl) ≥ 3. Delete the probe rep after renders.
5. Found-marking: mark 557 → found 1 / hidden 2; remaining_by_rep CPK == 2.
6. Cleanup restore (rep-0 style reads back `CPK`) + marker + exit.
  </action>
  <verify>Staged run: `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log; the scale ratio lands in [0.20, 0.30].</verify>
  <done>CPK tier proven: scaled balls, in-scene CPK blend, exact read-back, per-tier partition, style-faithful restore.</done>
</task>

</tasks>

<verification>
- `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log
- Tcl 8.6 gate zero; no lib files touched
</verification>

<success_criteria>
- CPK blending geometry (0.25× scale + bond stubs) proven headlessly
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-11-SUMMARY.md`
</output>
