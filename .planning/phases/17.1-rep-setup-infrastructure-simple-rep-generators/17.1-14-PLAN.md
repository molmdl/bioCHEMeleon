---
phase: 17.1
plan: 14
type: execute
wave: 7
depends_on: ["17.1-13"]
files_modified: [vmd/gui/dialog.tcl, vmd/tests/rep_verify.tcl]
autonomous: false
user_setup: []
must_haves:
  truths:
    - "on_start threads the Setup form's per_rep + lock_scene into game::start_game (4-arg call) — the GUI path gets identical lock-scene/randomize semantics to the console path"
    - "rep_verify.tcl auto-drives the consolidated GUI session: sources the extension, loads the demo, applies a 6-tier setup, calls on_start itself, and AUTO-LOGS every state dump + pick event to rep_verify_log.txt"
    - "The human's required actions are only real clicks (press p once per round; click one hider per style; style the scene for the lock-scene round) plus at most 3 short pastes (source, pv_round2, pv_report)"
    - "Per-tier blend + pick-through + found-visibility + lock-scene round-trip verified in ONE session (the consolidated checkpoint)"
  artifacts:
    - path: "vmd/gui/dialog.tcl"
      provides: "4-arg start_game call in on_start step 4 (the only GUI-layer change)"
    - path: "vmd/tests/rep_verify.tcl"
      provides: "Tk-guarded auto-driver (pick_verify.tcl pattern + the standing auto-issue/auto-log directive): pv_log file logging, pick observer trace, pv_round2 lock-scene helper, pv_report summary, pv_cleanup"
  key_links:
    - from: "vmd/gui/dialog.tcl"
      to: "vmd/lib/game.tcl"
      via: "start_game $molid $hider_count [dict get $state per_rep] [dict get $state lock_scene]"
      pattern: "start_game \\$molid"
    - from: "vmd/tests/rep_verify.tcl"
      to: "vmd/gui/setup_tab.tcl"
      via: "apply_state with a crafted 6-tier state dict + on_start invocation"
      pattern: "apply_state"
---

<objective>
Wire the Setup form into the 4-arg start_game, then run the ONE consolidated GUI human-verify checkpoint via a self-driving auto-logged driver.

Purpose: ROADMAP SC2/SC3/SC4's last mile is visual + interactive (blend quality, pick-through per rep, found visibility, lock-scene round-trip) — headless Tachyon proved geometry, not gestalt or C-side picks. Per the standing directive, the session is driver-driven and auto-logged; the human only clicks.
Output: dialog.tcl threading + `vmd/tests/rep_verify.tcl` + an APPROVED GUI checkpoint record.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-RESEARCH-game-reps-viability.md
@vmd/gui/dialog.tcl
@vmd/gui/setup_tab.tcl
@vmd/tests/pick_verify.tcl

Key facts (do NOT re-derive):
- on_start step 4 (dialog.tcl ~line 207) currently calls `game::start_game $molid [dict get $state hider_count]`. The state dict already carries per_rep (collect_state builds it from the per-rep rows) + lock_scene (the checkbox) — thread both: `[dict get $state per_rep] [dict get $state lock_scene]` (resolve_per_rep inside start_game handles empty/lock derivation identically for console + GUI + restart).
- GUI checkbox-lock SYNC (v1 parity: toggling lock-scene checks/disables the detected per-rep rows) is OPTIONAL and DEFERRED — ROADMAP SC2 requires Start-time detection + matching generation only. Do not build it (registered as a future refinement).
- pick contract (LOCKED): first-click quirk — ONE keyboard `p` press per round arms C-side pick delivery; pasted `mouse mode pick*` never arms. The driver instructs the user to press `p` once per round (that is a real keyboard act, not a paste).
- pick_verify.tcl (16-12) is the structural model: Tk-guard whole body in `if {[info exists ::tk_version]}` (an if-wrap is ONE command in both source and -e evaluation — a clean headless no-op); two-paste-short lines (< ~70 cols); `[info script]` empty under -e → path fallback to cwd-relative `vmd/biochemeleon.tcl`; helpers named pv_*.
- **CAUTION — pick_verify.tcl is STALE after 17.1-04:** it holds ~10 references to the DELETED `hiders::hidden_rep`/`found_rep` index vars (L136-139 pv_state, L253-280 pv_hidden_rep/pv_hide_hidden/pv_show_hidden). Copy from it ONLY the harness patterns (Tk-guard if-wrap, path fallback, two-paste discipline, pv_cleanup/pv_cleanup_check, pv_state's mouse/labels/registry sections). Rebuild ALL rep lookups on `hiders::tier_reps` + `mol repindex` with the -1 guard. Do NOT copy pv_hidden_rep/pv_hide_hidden/pv_show_hidden (their hidden-rep caveat check is superseded — neither rep is ever hidden in the N-tier design). pick_verify.tcl itself stays UNREPAIRED as a Phase-16 historical artifact (superseded by rep_verify.tcl).
- AUTO-DRIVER upgrade (standing directive h): the driver EXECUTES the setup itself — source the extension, load demo 1k8p, `setup_tab::apply_state` a crafted state, call `::biochemeleon::on_start` (a plain proc — callable from the console), auto-dump state after GO via `after 4500`, and AUTO-LOG everything to `rep_verify_log.txt` in cwd (open append + flush per line + vmdcon echo). The user's Tk-console work shrinks to: paste `source vmd/tests/rep_verify.tcl`, press p, click, (round 2) style the scene + paste `pv_round2`, (end) paste `pv_report`.
- Crafted round-1 state: `validate_state [dict create target_mode loaded selected_object <molid> hider_count 6 per_rep [dict create VDW 1 Lines 1 Licorice 1 CPK 1 Points 1 DynamicBonds 1] lock_scene 0 difficulty_easy 1 demo_id 1k8p] <atom_count>` — one hider per simple tier in ONE round (the consolidated blend+pick pass).
- GUI-only checks this session owns (viability §8): per-rep blend gestalt (no salmon dots / stray geometry), pick-through per rep (line stubs + the tiny Points target), found-marking visibility per style (green dot vs green bond tradeoff — record, don't decide), lock-scene round-trip, DynamicBonds default-vs-1.6 look.
- Tcl 8.5; NO grab set; `::tk_version` global qualifier; headless: the whole body no-ops.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread per_rep + lock_scene through on_start</name>
  <files>vmd/gui/dialog.tcl</files>
  <action>
In `::biochemeleon::on_start` step 4 (~line 207), change the call to:
```tcl
if {[catch {::biochemeleon::game::start_game $molid \
                [dict get $state hider_count] \
                [dict get $state per_rep] \
                [dict get $state lock_scene]} gs]} {
```
Update the step-4 comment (one line: per_rep + lock_scene threaded; lock/randomize derivation lives INSIDE start_game so console + GUI + restart share semantics). NOTHING else changes in the flow (steps 1-3.5, 5-7 untouched).
  </action>
  <verify>Headless entry smoke still green: staged `phase16_entry_smoke.tcl` PASS=1 (loading layer); `grep -n "per_rep" vmd/gui/dialog.tcl` shows the threaded args; 8.6 gate zero on dialog.tcl; `grep -rnE "grab set" vmd/gui/` zero.</verify>
  <done>on_start passes the Setup form's per_rep + lock_scene into start_game; the GUI path and console path share one derivation.</done>
</task>

<task type="auto">
  <name>Task 2: Write the rep_verify.tcl auto-driver</name>
  <files>vmd/tests/rep_verify.tcl</files>
  <action>
Model on pick_verify.tcl's structure (Tk-guard if-wrap; pv_* helpers; short lines; path fallback) — per the context CAUTION: copy ONLY the harness patterns + pv_cleanup/pv_cleanup_check; rebuild every rep lookup on tier_reps + repindex; do NOT copy pv_hidden_rep/pv_hide_hidden/pv_show_hidden (stale hidden_rep/found_rep references). New capabilities:
1. `pv_log {msg}` — appends a timestamped line to `[file join [pwd] rep_verify_log.txt]` (handle in a global, open append on first use, flush per line) AND echoes via vmdcon -info. ALL driver output goes through pv_log.
2. Auto-session setup on source (inside the Tk guard): source the extension if absent → load 1k8p if absent → `open_dialog` → build the crafted 6-tier state via validate_state (molid = the loaded 1k8p; atom_count via demos::atom_count) → `setup_tab::apply_state $state` → register the pick observer (`trace add variable ::vmd_pick_event write pv_observe` where pv_observe has the `{args}` signature and pv_log's both PICK lines — the 16-16 exact-value format) → call `::biochemeleon::on_start` → `after 4500` auto-dump: pv_state (round composition: registry remaining_by_rep, hider indices, per-tier rep read-backs via tier_reps + repindex, numreps).
3. `pv_state` — the 17.1 dump: mouse mode, game_logic state/timer, registry count_remaining + remaining_by_rep (per-tier!), game molid atoms + hider indices, EVERY tier pair (loop `hiders::tier_reps`: code → repindex → style/selection/color/shown), labels baseline. Auto-stashes ::pv_gs.
4. `pv_round2` — the lock-scene round: builds a state dict with lock_scene 1 + same hider_count 6 + per_rep {} → apply_state → on_start → after 4500 auto-dump (logs the DERIVED distribution — expected all-one-tier if the user styled the whole scene to one GAME_REPS style). Prints ONE instruction line first: "Style the scene first (Graphics -> Representations -> set the main rep to e.g. Licorice), THEN paste pv_round2."
5. `pv_cleanup` + `pv_cleanup_check` — copied from pick_verify.tcl (deactivate bridge + game::cleanup via ::pv_gs; post-check dump), logging via pv_log.
6. `pv_report` — prints the session summary: rounds run, finds registered (parsed from a ::pv_finds counter bumped by pv_observe when the pick index is a registered hider — query registry::is_hider), remaining, and the log-file path.
7. End-of-source instructions (≤ 8 short lines): press `p` once; click ONE hider of each style (6 finds); found ones turn green — watch per style; then paste pv_round2 (after styling the scene); after round 2's finds paste pv_report. Everything else is auto-logged.
Headless safety: the entire body inside the Tk-guard if-wrap; under -dispdev text it prints ONE vmdcon -warn line and does nothing.
  </action>
  <verify>Staged headless source of rep_verify.tcl (`bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/tests/rep_verify.tcl -eofexit < /dev/null'`): exactly one warn line, NO errors, no dialog attempted; 8.6 gate zero; `grep -c "grab set" vmd/tests/rep_verify.tcl` → 0; full-log scan clean.</verify>
  <done>Auto-driver exists: auto-sources, auto-starts a 6-tier round, auto-logs state + picks to rep_verify_log.txt; human pastes limited to source / pv_round2 / pv_report.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The complete 17.1 phase: 6 simple-tier generators behind lock-scene/randomize infrastructure, exercised in a real VMD GUI via the auto-logged driver (2 rounds: 6-tier mixed + lock-scene).</what-built>
  <how-to-verify>
1. Open a real VMD GUI on Windows. Extensions → Tk Console. `cd` to the staging root (tmp/biochemeleon-vmd — freshly staged with `cp -r vmd tmp/biochemeleon-vmd/`).
2. Paste ONE line: `source vmd/tests/rep_verify.tcl` — the driver loads everything, opens the dialog, STARTS round 1 (6 tiers × 1 hider), and auto-logs state to rep_verify_log.txt.
3. Press `p` ONCE (arms pick delivery — the locked first-click quirk). Find each of the 6 hiders by clicking:
   - VDW + CPK + Points spheres/dots, the Licorice ball, the Lines stub, the DynamicBonds stub.
   - While hunting, judge BLEND per style: hiders hard to spot at a glance; no salmon/pink dots; no stray geometry (record per style: blend OK / suspicious).
   - After each find: the found hider turns GREEN and stays visible (found rep) and the Game-tab remaining label decrements with per-tier counts (easy mode).
4. When all 6 are found (win box), style the scene for round 2: Graphics → Representations → set the main rep's style to Licorice. Then paste: `pv_round2` — the driver starts the lock-scene round and auto-logs the derived distribution.
5. Press `p`, find 2 Licorice hiders — confirm they blend into the Licorice scene and finds register. Paste: `pv_report`, then `pv_cleanup`.
6. Record per item: (a) blend verdict per 6 styles, (b) pick-through verdict per style (esp. the Lines stub click and the Points dot click), (c) found-visibility verdict per style (green dot on bond styles — is it enough?), (d) lock-scene round-trip: did all round-2 hiders land on the styled tier?, (e) any ERROR)/oddity in rep_verify_log.txt.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the failing items (which style / which step).</resume-signal>
</task>

</tasks>

<verification>
- Headless: phase16_entry_smoke PASS=1 after the dialog change; rep_verify.tcl no-ops cleanly in text mode
- GUI checkpoint record: per-style blend + pick-through + found-visibility verdicts + lock-scene round-trip recorded with the auto-log transcript
</verification>

<success_criteria>
- The consolidated GUI checkpoint is APPROVED (or its defects are precisely recorded for gap closure) — the last unverified surface of 17.1
- GUI and console paths share one start_game derivation (4-arg threading)
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-14-SUMMARY.md`
</output>
