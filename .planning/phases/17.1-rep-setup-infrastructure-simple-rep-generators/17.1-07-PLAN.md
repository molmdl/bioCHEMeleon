---
phase: 17.1
plan: 07
type: execute
wave: 4
depends_on: ["17.1-06"]
files_modified: [vmd/smoke/phase16_smoke.tcl, vmd/smoke/phase16_hiders_smoke.tcl, vmd/smoke/phase16_onpick_smoke.tcl, vmd/smoke/phase16_restart_smoke.tcl, vmd/smoke/phase15_game_smoke.tcl, vmd/smoke/phase15_smoke.tcl, vmd/smoke/phase16_entry_smoke.tcl, vmd/smoke/phase16_placement_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "Every pre-17.1 smoke passes against the new contracts by running EXPLICIT VDW-only rounds (per_rep {VDW n}) — the VDW regression baseline is preserved, not weakened"
    - "Selection-string asserts gain the ` and user3 1` conjunct (single-tier rounds ALWAYS conjoin user3 — the probe-verified form)"
    - "phase16_smoke resolves the hidden/found reps via the tier_reps dict + mol repindex (the hidden_rep/found_rep index vars no longer exist)"
    - "phase16_hiders_smoke stamps user3 via stamp_tier_codes (mirroring the production order) before its partition asserts"
    - "numreps growth asserts stay +2 for the explicit VDW-only rounds (base + 2N with N=1)"
  artifacts:
    - path: "vmd/smoke/phase16_smoke.tcl"
      provides: "VDW-only baseline round: 4-arg start_game, tier_reps/repindex rep resolution, user3-conjoined selection asserts"
    - path: "vmd/smoke/phase16_hiders_smoke.tcl"
      provides: "stamp_tier_codes + 1-arg add_hider_reps + updated selection strings + partition asserts"
    - path: "vmd/smoke/phase16_onpick_smoke.tcl, vmd/smoke/phase16_restart_smoke.tcl, vmd/smoke/phase15_game_smoke.tcl, vmd/smoke/phase15_smoke.tcl"
      provides: "VDW-only explicit per_rep on every start_game call (+2 numreps invariants preserved)"
    - path: "vmd/smoke/phase16_entry_smoke.tcl, vmd/smoke/phase16_placement_smoke.tcl"
      provides: "entry smoke's +2 assert and placement smoke's bbox assert restored via explicit VDW-only per_rep (the randomize-across-tiers ripples)"
  key_links:
    - from: "vmd/smoke/* (all 8)"
      to: "vmd/lib/{game,hiders}.tcl"
      via: "explicit VDW-only per_rep keeps the Phase-16 semantics under the N-tier contracts"
      pattern: "per_rep"
---

<objective>
Restore every pre-17.1 smoke to green against the N-tier contracts using explicit VDW-only rounds — the regression baseline.

Purpose: plans 17.1-04/05/06 intentionally changed the contracts (add_hider_reps tier_specs, 5-field records, user3-conjoined selections, randomize-across-implemented default), leaving the Phase-15/16 smokes red. P12 mandates keeping VDW-only rounds green as the regression gate. This plan makes that true with MINIMAL assert churn (explicit per_rep keeps +2 invariants; the only string change is the user3 conjunct).
Output: all 6 legacy smokes PASS=1.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-RESEARCH-rep-infra.md
@vmd/lib/hiders.tcl
@vmd/lib/game.tcl
@vmd/smoke/phase16_smoke.tcl
@vmd/smoke/phase16_hiders_smoke.tcl
@vmd/smoke/phase16_restart_smoke.tcl

Key facts (do NOT re-derive):
- New contracts to code against: `start_game {molid hider_count {per_rep {}} {lock_scene 0}}` (explicit per_rep → exactly those tiers, effective total = sum); `hiders::add_hider_reps {molid {tier_specs {{1 VDW}}}}` (1-arg default still = single VDW pair); selections ALWAYS conjoin `and user3 <code>` (single-tier code = 1); `hiders::stamp_tier_codes {molid tier_of}`; `hiders::tier_reps` dict code → `{hidden_name found_name hidden_sel found_sel}`; rep resolution via `mol repindex $m $name` with -1 guard; `hidden_rep`/`found_rep` vars are GONE.
- numreps invariant: 1 active tier → base + 2 (unchanged); the smokes use explicit VDW-only rounds so +2 holds everywhere.
- user2/user3 read-backs are FLOAT — numeric compares only. Sentinel selector `resname GAM and beta < 0` untouched.
- Each smoke's harness (failures list + _bail + marker + exit + full-log ERROR)/bad switch scan) is UNCHANGED — only the game-driving calls and asserts adapt.
- Staging: `mkdir -p tmp/biochemeleon-vmd && cp -r vmd tmp/biochemeleon-vmd/` then per smoke:
  `timeout 300 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/smoke/<file> -eofexit < /dev/null' 2>&1 | grep -E "BCHM_SMOKE_RESULT|ERROR\)|bad switch"`
- Tcl 8.5 only. NO lib-file edits in this plan (smoke files only — if a LIB defect surfaces, log it in the SUMMARY for a gap plan; the dispatch smoke 17.1-06 already proved the lib green).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the two hider-rep smokes (string/var contract changes)</name>
  <files>vmd/smoke/phase16_smoke.tcl, vmd/smoke/phase16_hiders_smoke.tcl</files>
  <action>
A. `phase16_smoke.tcl`:
   - Every `game::start_game` call gains the explicit VDW per_rep: `game::start_game $molid <n> [dict create VDW <n>] 0` (same counts as today).
   - Replace the `set rh $::biochemeleon::hiders::hidden_rep` / `found_rep` reads (~lines 212-213): resolve from the tier dict — `set tr1 [dict get $::biochemeleon::hiders::tier_reps 1]`; `set rh [mol repindex $gm [lindex $tr1 0]]`; `set rf [mol repindex $gm [lindex $tr1 1]]`; guard -1 via _bail.
   - Update the two exact selection-string asserts (~lines 223, 233) to `resname GAM and beta < 0 and user2 < 1 and user3 1` and `... user2 > 0 and user3 1`.
   - Everything else (atom counts, viewpoint asserts, registry counts, cleanup) unchanged.
B. `phase16_hiders_smoke.tcl`:
   - After start_game (switch its call to explicit VDW per_rep as above) and BEFORE add_hider_reps, add the production-order stamp: `hiders::stamp_tier_codes $gm [dict create 1 <the 5 hider indices>]` (indices already computed in the smoke).
   - `add_hider_reps $gm` 1-arg call still works (default {{1 VDW}}) — keep it, and note the default in a comment.
   - Update the two exact-string asserts (~lines 121, 132) with the `and user3 1` conjunct.
   - Partition asserts (atomselect user2 counts) unchanged; add one user3 partition check (user3 1 num == 5).
Run both staged → PASS=1 each.
  </action>
  <verify>Both staged runs: `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in full logs.</verify>
  <done>The two hider-rep smokes green under the tier contracts with explicit VDW-only rounds.</done>
</task>

<task type="auto">
  <name>Task 2: Update the numreps-invariant smokes + entry/placement ripples + full legacy-suite run</name>
  <files>vmd/smoke/phase16_onpick_smoke.tcl, vmd/smoke/phase16_restart_smoke.tcl, vmd/smoke/phase15_game_smoke.tcl, vmd/smoke/phase15_smoke.tcl, vmd/smoke/phase16_entry_smoke.tcl, vmd/smoke/phase16_placement_smoke.tcl</files>
  <action>
For each of the four numreps-invariant smokes: every `game::start_game` call gains `[dict create VDW <n>] 0` as args 3-4 (counts unchanged). The numreps-growth asserts (+2 / saved_numreps+2 / base_reps+2) then hold as-is — verify each assert's arithmetic still matches (the round's OWN base per lesson f/16-15: viewpoint asserts already target the round's own snapshot — untouched). phase16_restart_smoke's `_assert_round` takes the expected rep count implicitly via base_reps — confirm no hidden `+2` hardcodes beyond the existing ones. phase16_onpick_smoke additionally: its mark_found path now runs against user3-stamped reps — no assert changes needed (it asserts user2/registry only), but ADD the stamp call after start_game IF the smoke asserts rep-selection CONTENT (inspect; it does not per the 17.1-06 grep — keep minimal).
Then the two newly-owned ripples (both red under randomize-across-tiers per 17.1-06's known-red note):
- `phase16_entry_smoke.tcl`: its 2-arg `start_game` call (~line 220) + `post_reps == pre_reps + 2` assert (~line 242) go flaky-red when the default randomize spreads hiders across multiple tiers (+2N). Fix with its existing pattern: pass explicit `[dict create VDW <n>] 0` per_rep so the round stays single-tier (+2 preserved). All other asserts unchanged.
- `phase16_placement_smoke.tcl`: its bbox ±0.001 assert breaks if its round lands BONDED-tier hiders (anchor-relative placement can sit up to ~1.6 Å past the bbox edge). Fix the same way: explicit VDW-only per_rep on its start_game call so every hider comes from make_placeholder_hiders (bbox-inside by construction). If the smoke calls make_placeholder_hiders/mutate DIRECTLY (no start_game), only the 5-field record-shape ripple from 17.1-05 applies — verify and keep minimal.
Then run the FULL legacy suite staged, one command per smoke, and record all results:
phase15_smoke, phase15_game_smoke, phase15_backup_smoke (should be untouched-green — run to confirm), phase15_mutation_smoke (green since 17.1-05 — confirm), phase16_smoke, phase16_hiders_smoke, phase16_entry_smoke, phase16_gametab_smoke, phase16_onpick_smoke, phase16_pick_smoke, phase16_placement_smoke, phase16_restart_smoke.
  </action>
  <verify>All 12 legacy smokes `BCHM_SMOKE_RESULT PASS=1 FAIL=none`; zero `ERROR)` / `bad switch` across all full logs (grep each).</verify>
  <done>The entire pre-17.1 smoke suite is green against the N-tier contracts; the VDW-only regression baseline is explicitly preserved.</done>
</task>

</tasks>

<verification>
- 12/12 legacy smokes PASS=1, zero `ERROR)` / `bad switch` in every full log
- Tcl 8.6 gate zero on all touched files (smoke files)
- No lib files touched (git status shows only vmd/smoke/*.tcl changes)
</verification>

<success_criteria>
- P12 satisfied: the phase-16 baseline (VDW-only rounds) is green under the new contracts, so any later regression is attributable to 17.1+ changes
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-07-SUMMARY.md`
</output>
