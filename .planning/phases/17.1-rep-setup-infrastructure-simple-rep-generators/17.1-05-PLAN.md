---
phase: 17.1
plan: 05
type: execute
wave: 2
depends_on: ["17.1-03"]
files_modified: [vmd/lib/mutation.tcl, vmd/smoke/phase17_bonded_smoke.tcl, vmd/smoke/phase15_mutation_smoke.tcl, vmd/smoke/phase16_placement_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "make_bonded_hiders {molid count {occupied_hiders}} returns records {name element x y z} where name+element are COPIED from distinct heavy-atom anchors (element C N O S P, never GAM) and coords are 1.2–1.6 Å from the anchor (place_bonded_hider), |coord| ≤ 9999"
    - "Hider records carry the sentinel fields unchanged (resname GAM / beta %6.1f -999.0 / segid GAME / chain G / resid 9001) and a NON-BLANK element in PDB cols 77-78 (the load-bearing field — blank degrades to element X radius 1.50)"
    - "write_combined_pdb consumes the 5-field record shape; make_placeholder_hiders emits the same shape with element = HID_ELEMENT (C) — ONE record shape everywhere"
    - "After mutate+reload every hider has numbonds >= 1 (never == 1 — extra bonds are normal) and its element/radius/name match a real atom's"
    - "Cross-hider separation is enforced: this tier's placed hiders (sep 4.0) + the occupied_hiders arg (earlier tiers this round, sep 4.0) + a per-anchor `within 3.0` real-atom neighborhood (sep 1.4, anchor excluded)"
  artifacts:
    - path: "vmd/lib/mutation.tcl"
      provides: "make_bonded_hiders (mol bridge: anchor sampling + neighborhood fetch + pure placement + record build); _hider_record gains the element param; write_combined_pdb/make_placeholder_hiders updated to the 5-field shape"
      exports: ["make_bonded_hiders", "make_placeholder_hiders", "write_combined_pdb", "tag_sentinels", "fetch_hider_indices", "mutate"]
    - path: "vmd/smoke/phase17_bonded_smoke.tcl"
      provides: "headless chemistry proof: bond law (numbonds ≥ 1), element/radius/name mimicry, sentinel round-trip, 78-col record layout with non-blank element"
  key_links:
    - from: "vmd/lib/mutation.tcl (make_bonded_hiders)"
      to: "vmd/lib/generators.tcl"
      via: "sample + place_bonded_hider (mutation already sources generators.tcl)"
      pattern: "generators::(sample|place_bonded_hider)"
    - from: "vmd/lib/mutation.tcl (_hider_record)"
      to: "PDB cols 77-78"
      via: "element %2s field copied from the anchor — the load-bearing blend field"
      pattern: "%2s"
---

<objective>
Add `make_bonded_hiders` to `vmd/lib/mutation.tcl` and carry the anchor's name+element through the record/writer path — plus its headless chemistry smoke.

Purpose: simple-tier hiders must MIMIC their anchor (name copied → Name-coloring blend; element copied → correct VDW radius + Element-coloring blend; blank/mangled element silently degrades to radius-1.50 element X — the probe-J defect class). This plan is the only mol-bridge change to the PDB-rebuild path in 17.1.
Output: `vmd/lib/mutation.tcl` with make_bonded_hiders + 5-field records; `vmd/smoke/phase17_bonded_smoke.tcl` PASS=1; legacy mutation smokes updated for the record-shape change.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-RESEARCH-simple-generators.md
@vmd/lib/mutation.tcl
@vmd/lib/generators.tcl
@vmd/smoke/phase15_mutation_smoke.tcl

Key facts (do NOT re-derive — probe-verified):
- Record spec (research §9.1) — reuse the shipped format string VERBATIM, only the values change:
  `ATOM  %5d %4s%1s%-3s %1s%4d%1s   %8.3f%8.3f%8.3f%6.2f%6.1f      %-4s%2s`
  name = anchor.name (%4s right-justified); element = anchor.element (%2s cols 77-78, THE load-bearing field); resname GAM / chain G / resid 9001 / occ 1.00 / beta %6.1f -999.0 (NEVER %6.2f — overflows and corrupts segid) / segid GAME. Records are 78 chars.
- Bond law: load-time distance search bonds the hider to EVERYTHING within `d < 0.6×(ri+rj)` across chain/segid — a hider at ≤ 1.6 Å from any C/N/O/S anchor bonds with margin. Extra bonds normal; assert numbonds ≥ 1, never == 1.
- Element table: C 1.70 cyan, N 1.55 blue, O 1.52 red, S 1.80 yellow, P 1.55 tan; blank/unknown → X 1.50 + unpredictable Name color. Name coloring prefix-matches the name — copying the anchor's name guarantees the same color as the anchor under Name coloring.
- `atomselect get {kw1 kw2}` returns per-atom LISTS — lassign flat-splits wrongly (research Pitfall 5). Use SINGLE-KEYWORD gets (`$sel get name`, `$sel get element`, ...) and lindex the 1-element result.
- Anchor policy (research §3.3): heavy atoms only — selection text `not resname GAM and element C N O S P` (H excluded: caps the cutoff at 1.62 and may be absent); distinct anchors via generators::sample; if anchors < count, cap count at the anchor count (v1 under-generation parity — the tier simply produces fewer).
- Neighborhood fetch per anchor (performance: never loop all atoms in tcl): `atomselect $m "within 3.0 of index $aid and not index $aid"` (C-side spatial hash; 3.0 = MIN_SEP_REAL 1.4 + D_MAX 1.6 margin) → small position list for occupied triples with sep 1.4. Prior hiders (this tier + the occupied_hiders arg from earlier tiers) get sep 4.0 triples.
- generators API (17.1-03, exists after that plan): `sample {lst n {seed {}}}`, `place_bonded_hider {anchor occupied {seed {}}}` (occupied = {x y z sep} triples), constants D_MIN/D_MAX/MIN_SEP_REAL/MIN_SEP_HIDER/MAX_COORD. mutation.tcl ALREADY sources generators.tcl (line 17) — no new source lines.
- Production callers pass NO seed (continuous global PRNG stream — same convention as make_placeholder_hiders).
- Tcl 8.5: foreach+lappend, brace expr, $sel delete after EVERY atomselect, one variable per line.
- Staging + smoke conventions as in 17.1-04 (BCHM_SMOKE_RESULT marker; full-log `ERROR)`/`bad switch` scan; lib source order: setup_state, registry, rep_tiers, generators, game_logic, demos, backup, mutation).
- RIPPLE (owned HERE, not by 17.1-07): the record shape {name x y z} → {name element x y z} breaks `vmd/smoke/phase15_mutation_smoke.tcl`'s shape assert (~line 63: "records not {name x y z}") — update it to the 5-field shape + assert element non-blank. Check `vmd/smoke/phase16_placement_smoke.tcl` for any 4-field consumption/shape assert and update likewise; run both green.
</context>

<tasks>

<task type="auto">
  <name>Task 1: make_bonded_hiders + element-carrying records</name>
  <files>vmd/lib/mutation.tcl</files>
  <action>
In `vmd/lib/mutation.tcl`:
1. `_hider_record {serial name element x y z}` — add the `element` parameter; pass it as the `%2s` field (replacing the HID_ELEMENT constant reference in the format call). Update the proc comment (element is load-bearing; %6.1f beta rule unchanged).
2. `write_combined_pdb {molid hider_records out_path}` — destructure `foreach {nm el x y z} $r { break }` and call `_hider_record $serial $nm $el $x $y $z`.
3. `make_placeholder_hiders {molid count}` — emit `[list [format "G%02d" $i] $HID_ELEMENT $x $y $z]` (5-field shape; element = HID_ELEMENT "C" as Phase 16 shipped). Update its comment (record shape now 5-field; Phase-17.1 note).
4. NEW `make_bonded_hiders {molid count {occupied_hiders {}}}` → records list:
   - anchors: `set asel [atomselect $molid {not resname GAM and element C N O S P}]`; if `[$asel num] == 0` → `$asel delete` + `error "make_bonded_hiders: no heavy-atom anchors"`. `set aids [$asel get index]` (ONE bulk get; performance note in comment); `$asel delete`. If `[llength $aids] < $count` { set count [llength $aids] } (under-generation tolerance, v1 parity).
   - `set chosen [::biochemeleon::generators::sample $aids $count]`.
   - foreach aid: open `atomselect $molid "index $aid"`; read coords/name/element via SINGLE-KEYWORD gets (`get x`, `get y`, `get z`, `get name`, `get element` — lindex [list 0]); `$sel delete`.
   - neighborhood: `set nsel [atomselect $molid "within 3.0 of index $aid and not index $aid"]`; read x/y/z lists (3 single-keyword gets) → build occupied triples `{x y z 1.4}`; `$nsel delete`. Append this tier's previously-placed positions + the `occupied_hiders` arg as `{x y z 4.0}` triples.
   - `set pos [::biochemeleon::generators::place_bonded_hider [list $ax $ay $az] $occupied]` (NO seed); `lappend recs [list $aname $aelem {*}$pos]`; record the placed position for subsequent occupied lists.
   - Return recs. NO seed args anywhere (global PRNG stream).
5. Update the namespace export line to add make_bonded_hiders. Update the file header comment (17.1: bonded single-atom tier; element copied from anchor).
Tcl 8.5; brace expr; every atomselect deleted; no 8.6 idioms.
  </action>
  <verify>Load-clean check: `tclsh vmd/lib/mutation.tcl` (SKIP if tclsh is unavailable — STATE.md 13-01: tclsh is NOT installed in WSL; the staged headless-VMD run below covers load-clean); 8.6 gate zero on the file; `grep -n "element C N O S P" vmd/lib/mutation.tcl` shows the anchor filter; `grep -c "get x" vmd/lib/mutation.tcl` ≥ 1 (single-keyword gets).</verify>
  <done>mutation.tcl builds 5-field records; make_bonded_hiders implements the anchor policy, neighborhood occupied lists, and pure placement with no new source lines.</done>
</task>

<task type="auto">
  <name>Task 2: Bonded chemistry smoke + legacy mutation-smoke updates</name>
  <files>vmd/smoke/phase17_bonded_smoke.tcl, vmd/smoke/phase15_mutation_smoke.tcl, vmd/smoke/phase16_placement_smoke.tcl</files>
  <action>
A. Write `vmd/smoke/phase17_bonded_smoke.tcl` (harness per context; source order includes rep_tiers). Steps on demo 1znf (424 atoms):
1. `set recs [mutation::make_bonded_hiders $m0 5]` → assert 5 records, each 5 fields; every name ∈ the molecule's real atom names (collect `[atomselect $m0 all] get name` once, assert membership) and NOT matching G%02d; every element ∈ {C N O S P} and equals SOME real atom's element; coords finite.
2. Direct PDB-layout assert (the probe-J guard): `mutation::write_combined_pdb $m0 $recs $outpdb` → read the file; the LAST 5 ATOM lines each have string length 78, cols 77-78 non-blank (`string index` / `string range`), cols 18-20 == GAM, segid cols 73-76 == GAME, beta field == " -999.0"? (assert `[string range $l 60 65]` eq "-999.0" allowing the %6.1f spacing — inspect one line first and pin the exact slice).
3. `set gm [mutation::mutate $m0b $recs]` (reload path) → assert atom count == 424 + 5; hider idxs = {424 425 426 427 428} via `resname GAM and beta < 0`.
4. Bond law: every hider's numbonds ≥ 1 (`[$s get numbonds]` numeric); every hider's element parsed == its written element; hider radius == the element's VDW radius (C 1.70 / N 1.55 / O 1.52 / S 1.80 / P 1.55, epsilon 1e-6) — the blank-element regression guard; hider name ∈ real names; nearest-real-atom distance per hider ∈ [1.19, 1.61] (loop the ≤5 hiders × coords — small; fine in tcl).
5. Sentinel round-trip: betas all -999.0 (numeric), segids all GAME, count 5.
6. Occupied separation: for each hider, min distance to every OTHER hider ≥ 3.9 (sep 4.0 relaxation may dip slightly — assert ≥ 3.9; if the relax fallback fired for a crowded pair, assert only ≥ 1.19 and LOG the relaxation in the smoke output — inspect first, then pin the assertion that holds).
7. Marker + exit.
B. Update `vmd/smoke/phase15_mutation_smoke.tcl`: the record-shape assert (~line 63) from `{name x y z}` to `{name element x y z}` + element non-blank; keep all other asserts; run green.
C. Inspect `vmd/smoke/phase16_placement_smoke.tcl` for 4-field record consumption or shape asserts; update if present; run green.
  </action>
  <verify>All three smokes staged: `BCHM_SMOKE_RESULT PASS=1 FAIL=none` each, zero `ERROR)` / `bad switch` in each full log.</verify>
  <done>Bond law, element/name mimicry, sentinel round-trip, 78-col layout, and hider separation proven headlessly; legacy mutation smokes green against the 5-field shape.</done>
</task>

</tasks>

<verification>
- All three smokes PASS=1 with zero `ERROR)` / `bad switch` in full logs
- Tcl 8.6 gate zero on all touched files
- mutation.tcl sources NOTHING new (still setup_state + generators only); registry NOT sourced (would wipe _records)
- `grep -c "%6.2f%6.1f" vmd/lib/mutation.tcl` == 1 (the occupancy %6.2f is LEGITIMATE and stays; the gate pins the adjacent pair — occupancy %6.2f followed by beta %6.1f — proving the beta field was not widened to %6.2f, the segid-corruption overflow)
</verification>

<success_criteria>
- Any tier (17.1-06+) can request bonded hiders and get anchor-mimicking, auto-bonding, sentinel-correct GAM records
- The probe-J defect class (blank element → radius X) is regression-guarded by the smoke
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-05-SUMMARY.md`
</output>
