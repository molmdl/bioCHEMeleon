---
phase: 17.1-rep-setup-infrastructure-simple-rep-generators
verified: 2026-09-05T09:03:25Z
status: passed
score: 4/4 must-haves verified
re_verification: false
gaps: []
human_verification: []   # GUI checkpoint APPROVED by user 2026-09-05 (see record below)
---

# Phase 17.1: Rep Setup Infrastructure & Simple Rep Generators — Verification Report

**Phase Goal:** Hiders can be generated for the simpler VMD representations (Lines/VDW/Licorice), with the lock-scene vs randomize infrastructure and research-driven GAME_REPS list established — the foundation all later generators build on.
**Verified:** 2026-09-05T09:03:25Z
**Status:** PASSED (4/4 success criteria verified)
**Re-verification:** No — initial verification
**Method:** Code-level artifact verification (existence / substantive / wired) + independent fresh-staging headless re-run of 6 smokes and 4 test suites + Tcl-8.5 idiom gates + GUI checkpoint evidence audit. SUMMARY claims were NOT trusted; every claim below was re-derived from the code or re-executed.

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | 10-rep GAME_REPS list defined in the pure layer; surface/volumetric reps excluded as anti-features | ✓ VERIFIED | `vmd/lib/setup_state.tcl:10` — `GAME_REPS {Lines VDW Licorice CPK Cartoon NewCartoon Trace Tube Points DynamicBonds}` (all 10, incl. the 17.2 seam Cartoon/NewCartoon/Trace/Tube). Anti-feature exclusion explicit at `setup_state.tcl:8-9` ("Surface/volumetric reps are out of scope (anti-features — they do not fit the discrete-atom blend-in mechanic)") and in `vmd/AGENTS.md:85`. Pure-layer consumption: `rep_tiers.tcl` sources setup_state (line 43) and iterates GAME_REPS for all output dicts; `IMPLEMENTED_TIERS {Lines VDW Licorice CPK Points DynamicBonds}` + `TIER_KINDS` at rep_tiers.tcl:48-59. test_setup_state 47/47 + test_rep_tiers 32/32 green (independently re-run). |
| 2 | Lock-scene detects active reps (molinfo numreps + per-rep query) and generates matching hiders; randomize distributes across GAME_REPS | ✓ VERIFIED | Detection: `backup.tcl:43-48` — `molinfo $molid get numreps` + per-rep `{rep $i} {selection $i} {color $i} {material $i}` query loop → snapshot reps. game.tcl:181 feeds snapshot reps to `rep_tiers::scene_reps_to_per_rep`; game.tcl:197 `resolve_per_rep` with lock_scene restriction (`allowed = detected ∩ IMPLEMENTED_TIERS`, rep_tiers.tcl:161-171) or randomize over `randomize_per_rep` (quick-008 semantics, rep_tiers.tcl:192-194). Full chain GUI→game verified: setup_tab lock_scene checkbox (setup_tab.tcl:34/184/280) → dialog.tcl on_start 4-arg threading (dialog.tcl:209-212) → start_game. Re-run evidence: capstone Round B (lock-scene) PASS=1, dispatch smoke (randomize invariants) PASS=1; live GUI log `rep_verify_log.txt` line 98 "read scene style 'Licorice 0.300000 12.000000 12.000000' off displayed molid 1", line 114 "registry remaining_by_rep = Licorice 2". |
| 3 | Line/Licorice/VDW hiders mimic connected atoms and blend with line/licorice/VDW reps | ✓ VERIFIED | Bonded machinery: generators.tcl bonded placement (D_MIN 1.2 / D_MAX 1.6 bond band, `sample`/`unit_vector`/`bonded_offset`/`reject_overlaps`/`place_bonded_hider` + 7 constants, lines 30-181) — 26/26 tests green (re-run). mutation.tcl `make_bonded_hiders` (line 118): anchor mimicry (`not resname GAM and element C N O S P`, name+element COPIED), 5-field `{name element x y z}` records, element in PDB cols 77-78 `%2s` (`_hider_record` line 187, format line 194 — the load-bearing blend field). Render proofs re-run: bonded smoke PASS=1 (mimicry, bond law numbonds ≥ 1, radius == element VDW, 78-col layout), Lines smoke PASS=1 (hiders-only rep 0 FCylinder + 3 Rad-0.002 dots; scene-diff +13 stub cylinders — the both-endpoints blend semantics), Licorice smoke PASS=1 (ball-radius spheres + in-scene ball+stick blend via pre-styled scene diff). **Design note (not a gap):** the plan pins `VDW→free` (17.1-01-PLAN line 79/103; asserted by the 32-case suite) — VDW hiders are the Phase-16 element-correct sphere tier (element C, radius == C VDW 1.70), which blends under VDW scenes without needing a bond partner; the bonded-mimicry requirement text (REQUIREMENTS HIDER-04: "Line/Licorice hiders — new atoms mimic connected atoms or alternate positions") names Line/Licorice, matching the implementation. VDW blend was GUI-verified (Phase 16 won round) and re-confirmed in the 17.1 checkpoint round-1 gestalt. |
| 4 | Reps tracked by stable name (repname/repindex), never by index | ✓ VERIFIED | hiders.tcl: `tier_reps`/`tier_styles` name-keyed dicts (lines 97-99), `mol repname` captured immediately after each addrep (lines 149/159), `mol repindex` resolution at use time with -1/out-of-range guards (lines 241/246). `hidden_rep`/`found_rep` index-cached vars are GONE from all live code — remaining references are historical comments only, plus the superseded `pick_verify.tcl` (user-dispositioned: do NOT run; superseded by rep_verify.tcl). Name-keyed smoke (phase17_tiers) re-run PASS=1 including delrep → -1 guard. |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Exists | Substantive | Wired | Status |
|----------|----------|--------|-------------|-------|--------|
| `vmd/lib/rep_tiers.tcl` | PURE tier module: IMPLEMENTED_TIERS, TIER_KINDS, 8 exported procs | ✓ (236 lines) | ✓ all 8 procs (implemented/tier_kind/style_args/scene_reps_to_per_rep/filter_per_rep_to_scene/resolve_per_rep/tiers_from_per_rep/effective_total); zero mol/atomselect/tk | ✓ consumed by game.tcl (181/197/202/215/288) + entry source line (biochemeleon.tcl:85) + 7 smokes source it | ✓ VERIFIED |
| `vmd/tests/test_rep_tiers.test` | 32-case suite pinning the dispatch contract | ✓ | ✓ 32 `test` cases; BCHM_TEST_RESULT marker | ✓ re-run 32/32 | ✓ VERIFIED |
| `vmd/lib/registry.tcl` set_rep/assign_reps | per-record rep stamp + atomic bulk assign | ✓ | ✓ set_rep :142 (status-preserving copy-modify-writeback), assign_reps :159 (validate-every-key-then-apply) | ✓ game.tcl:292 ONE bulk call after ONE reconstruct (:248) | ✓ VERIFIED |
| `vmd/tests/test_registry.test` | 27-case suite incl. 9 Phase-17.1 cases | ✓ | ✓ | ✓ re-run 27/27 | ✓ VERIFIED |
| `vmd/lib/generators.tcl` bonded placement | sample/unit_vector/bonded_offset/reject_overlaps/place_bonded_hider + constants | ✓ | ✓ 5 procs (:110/:129/:148/:163/:178) + 7 constants (:30-36) | ✓ mutation.tcl make_bonded_hiders consumes place_bonded_hider + qualified MIN_SEP_* constants | ✓ VERIFIED |
| `vmd/tests/test_generators.test` | 26-case suite | ✓ | ✓ | ✓ re-run 26/26 | ✓ VERIFIED |
| `vmd/lib/hiders.tcl` N-tier | add_hider_reps/stamp_tier_codes/mark_found_visual, name-keyed dicts, repindex resolution | ✓ | ✓ add_hider_reps :121 (2N pairs, read-back validation), stamp_tier_codes :199, mark_found_visual :220 (all-pairs re-assert) | ✓ game.tcl:280/290 stamp-BEFORE-add ordering enforced at composition root; on_pick → mark_found_visual via game.tcl:413 | ✓ VERIFIED |
| `vmd/lib/mutation.tcl` make_bonded_hiders | 5-field records, element cols 77-78 | ✓ | ✓ :118 (anchor sampling + neighborhood + pure placement), _hider_record :187 `%2s`, write_combined_pdb 5-field destructure :208 | ✓ game.tcl:219 bonded branch; occ_hiders accumulation :227-230 | ✓ VERIFIED |
| `vmd/lib/game.tcl` 4-arg start_game | per-tier dispatch, lock-scene from snapshot, ONE reconstruct + ONE assign_reps, game_state per_rep | ✓ | ✓ :129 signature `{molid hider_count {per_rep {}} {lock_scene 0}}`; 16-13 guard byte-preserved :140-173; per-tier loop :213-233; ONE mutate :236; ONE reconstruct :248; ONE assign_reps :292; 4-key game_state :297 | ✓ restart symmetry :342-354 (per_rep pass-through, lock_scene defensive 0); entry sources it | ✓ VERIFIED |
| `vmd/biochemeleon.tcl` source line | rep_tiers in pure block after game_logic | ✓ | ✓ :85 with ordering-comment updates (:71, :80-85, :100-105) | ✓ entry is the GUI load path | ✓ VERIFIED |
| GAME_REPS 10 reps | Lines VDW Licorice CPK Cartoon NewCartoon Trace Tube Points DynamicBonds | ✓ | ✓ setup_state.tcl:10 (unchanged all-phase per 17.1-01) | ✓ 17.2 seam documented (IMPLEMENTED_TIERS widening = one constant) | ✓ VERIFIED |
| 9 phase-17 smokes | tiers/bonded/dispatch/points/lines/licorice/cpk/dynbonds/capstone | ✓ (all 9: 327/333/455/550/565/622/672/799/650 lines) | ✓ all carry the full-log-scan + marker harness; capstone = mixed 3-tier + lock-scene + under-generation | ✓ 6/6 re-run independently PASS=1 (capstone, dispatch, tiers, bonded, lines, licorice); points/cpk/dynbonds covered by the 17.1-13 full-suite gate (24/24) | ✓ VERIFIED |
| `vmd/gui/dialog.tcl` on_start threading | 4-arg start_game with per_rep + lock_scene | ✓ | ✓ :209-212 — the only GUI-layer change, exactly as planned | ✓ shares ONE derivation with console + restart (semantic unification) | ✓ VERIFIED |
| `vmd/tests/rep_verify.tcl` | Tk-guarded auto-logged GUI driver | ✓ (499 lines) | ✓ Tk guard :56 (`::tk_version` absent → no-op headless); tier_reps + repindex resolution; pv_log/pv_round2/pv_report/pv_cleanup | ✓ ran in the approved GUI session (log produced) | ✓ VERIFIED |
| `tmp/biochemeleon-vmd/rep_verify_log.txt` | 142-line clean auto-log | ✓ | ✓ exactly 142 lines; 0 error-class lines; live style read-backs lines 32-54 (Licorice :40, `DynamicBonds 1.6` :52, found ColorID 7 :42/:54); 6 finds round 1 (atoms 555-560 on mol=1) + 2 lock-scene finds round 2 (556/555 on mol=4, :138/:140); SESSION REPORT :141 | ✓ produced by rep_verify.tcl during the APPROVED session | ✓ VERIFIED |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| setup_tab lock_scene checkbox | start_game | dialog.tcl on_start 4-arg (`[dict get $state lock_scene]`) | WIRED | dialog.tcl:209-212; state collected with `lock_scene [expr {!!$lock_scene}]` (setup_tab.tcl:280) |
| start_game | rep_tiers resolution | `scene_reps_to_per_rep(snapshot.reps)` → `resolve_per_rep` → `tiers_from_per_rep` | WIRED | game.tcl:181/197/202; detection input = the round's OWN backup::snapshot |
| backup::snapshot | molinfo per-rep query | `molinfo numreps` + `{rep $i} {selection $i} {color $i} {material $i}` loop | WIRED | backup.tcl:43-48 — the SC2 detection primitive |
| start_game | mutation generators | free→make_placeholder_hiders / bonded→make_bonded_hiders (tier_kind dispatch) | WIRED | game.tcl:216-219 with occ_hiders accumulation :227-230 |
| start_game | registry | ONE 1-arg reconstruct_from_sentinels (DI command-prefix form) + ONE bulk assign_reps | WIRED | game.tcl:248-253 / :292 (P8 anti-pattern guarded) |
| start_game | hiders | stamp_tier_codes BEFORE add_hider_reps (ordering contract) | WIRED | game.tcl:280→:290; specs carry style_args (DynamicBonds 1.6 :288) |
| on_pick / GUI pick | mark_found_visual + registry | pick_bridge → game.tcl:413 on_pick → mark_found_visual (repindex-resolved) | WIRED | name-keyed tier_reps re-assert; GUI-proven (6+2 real-click finds in the approved session) |
| rep_tiers | setup_state GAME_REPS | `source` + fully-qualified `::biochemeleon::setup_state::GAME_REPS` | WIRED | rep_tiers.tcl:43, 108/113/128/150/197/214 |
| make_bonded_hiders | generators pure placement | qualified `::biochemeleon::generators::MIN_SEP_REAL/MIN_SEP_HIDER` + place_bonded_hider | WIRED | mutation.tcl:118-176 (namespace-qualified constants — the silent-empty trap avoided) |

### Requirements Coverage

| Requirement | Status | Blocking Issue |
|-------------|--------|----------------|
| HIDER-06 (research-driven 10-rep GAME_REPS list; surface/volumetric anti-features) | ✓ SATISFIED | None — SC1 verified |
| HIDER-04 (Line/Licorice hiders mimic connected atoms or alternate positions) | ✓ SATISFIED | None — SC3 verified (bonded generators + render proofs + GUI blend) |

Note: REQUIREMENTS.md tracker rows (lines 134/136) still read "Pending" — phase-close bookkeeping for the orchestrator to flip; not a code gap.

### Test / Smoke Gate Results (independently re-run on fresh staging `tmp/verify-171`, sequential VMD 1.9.3 runs, full-log scans)

**Test suites** (marker `BCHM_TEST_RESULT`):

| Suite | Result |
|---|---|
| test_rep_tiers | Total=32 Passed=32 Failed=0 |
| test_registry | Total=27 Passed=27 Failed=0 |
| test_generators | Total=26 Passed=26 Failed=0 |
| test_setup_state | Total=47 Passed=47 Failed=0 |
| (test_game_logic 15/15 per the 17.1-13 gate; not re-run — outside 17.1's changed surface) | — |

**Smokes** (marker `BCHM_SMOKE_RESULT PASS=1 FAIL=none` + full-log scan for `ERROR)` / `bad switch` / `no such variable` / `invalid command name` / `can't read` — ALL ZERO in every log; `Info) Exiting normally` present in every log):

| Smoke | Re-run result |
|---|---|
| phase17_capstone_smoke (mixed 3-tier + lock-scene + under-generation) | PASS=1; under-generation Warning `rep 'Cartoon' has no generator yet -- dropped from this round` present as `Warning)`, never `ERROR)` |
| phase17_dispatch_smoke (randomize containment invariants) | PASS=1 |
| phase17_tiers_smoke (name-keyed 2-tier + delrep → -1 guard) | PASS=1 |
| phase17_bonded_smoke (chemistry: mimicry/bond law/PDB layout) | PASS=1 |
| phase17_lines_smoke (both-endpoints render semantics) | PASS=1 |
| phase17_licorice_smoke (ball+stick in-scene blend, pre-styled scene diff) | PASS=1 |
| phase17_points / cpk / dynbonds (not re-run individually) | Covered by the 17.1-13 full-suite gate: 24/24 smokes PASS=1, 147/147 tests, both grep gates zero |

**Static gates (re-run this session):**
- Tcl 8.6 idiom gate `grep -rnE "\blmap\b|\btry\b|\bthrow\b|\btailcall\b|\bcoroutine\b|\byield\b|\bfinally\b" vmd/lib/ vmd/gui/` → **0 matches**
- Modeless gate `grep -rn "grab set" vmd/gui/` → **0 matches**
- False-PASS discipline honored: every re-run log scanned in full for `ERROR)`/`bad switch`/`no such variable` — zero hits (the 17.1-04-era capstone false-PASS class was eliminated by 17.1-07's smoke updates; current phase16_smoke is honestly green)

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| vmd/tests/pick_verify.tcl | 136-139, 255-261 | reads removed `hiders::hidden_rep`/`found_rep` vars | ℹ️ Info (user-dispositioned) | Superseded Phase-16 artifact — must NOT be run in future sessions; rep_verify.tcl is the GUI driver. Explicitly user-approved as non-blocking. |
| vmd/tests/rep_verify.tcl | 141 (log) | pv_report `finds=0` counter never bumped | ℹ️ Info (record-only) | Cosmetic; registry lines in the log were authoritative. User-approved non-blocking. |
| vmd/tests/rep_verify.tcl | (log lines 115-116) | round-2 dump echoes INPUT per_rep/hider_count, not derived values | ℹ️ Info (record-only) | Registry + tier lines authoritative. User-approved non-blocking. |
| lib/gui files | — | TODO/FIXME/placeholder/coming-soon stubs | none | Stub scan clean (all "placeholder" hits are the legitimate `make_placeholder_hiders` proc name / HID_ELEMENT doc). |

**Blockers: none.**

### Human Verification Required

None outstanding — the phase's single consolidated GUI human-verify checkpoint (17.1-14 Task 3) ran and was **APPROVED by the user 2026-09-05**:

**Checkpoint record (verified against the actual log artifact):**
- **Session:** Windows VMD GUI + Tk console, staging `tmp/biochemeleon-vmd`, auto-driver `vmd/tests/rep_verify.tcl`, log `tmp/biochemeleon-vmd/rep_verify_log.txt` (142 lines, 0 `ERROR)`/`bad switch`/`no such variable`).
- **Round 1 (6-tier mixed):** per-tier style read-backs live-read off the molecule (log lines 32-54: Lines / VDW / Licorice / CPK / Points / `DynamicBonds 1.6`, hidden color Element, found ColorID 7, user3 1-6); all 6 hiders found by real clicks (atoms 555-560 on mol=1); win box "You found all 6 hiders in 4:45!" (user-reported, consistent with the log PICK timeline).
- **Round 2 (lock-scene):** scene style read `Licorice 0.300000 12.000000 12.000000` off the displayed molecule (log :98) and mirrored onto the fresh target (:100); derived distribution `{Licorice 2}` (:114 — quick-008 random-subset semantics, pinned by 17.1-13 Round B, **accepted by the user**); both hiders found on mol=4 (:138/:140); win box "You found all 2 hiders in 3:26!".
- **User findings dispositioned:** (1) non-sphere tiers blend into default-Lines scenes "hard to spot" → difficulty-calibration note for Phase 19; (2) "hiders turn green after click" → found-visibility ✓; (3) "advise the player to press p first" → Phase 22 note; (4) restored-original-intercepts-picks after mid-round restart → Phase 19 candidate; (5) round-2 Licorice-only → lock-scene detection working, accepted.
- **Why human:** GUI/picking-by-real-mouse cannot run headless (Tk doesn't load in `-dispdev text`); this checkpoint is the repo's established human gate and it is closed with user approval.

### Gaps Summary

No gaps. All four success criteria verified against the actual codebase with independent fresh-staging execution; all required artifacts exist, are substantive, and are wired; all key links (GUI→start_game→rep_tiers→generators→hiders→registry) are real code paths exercised by both headless smokes and the approved GUI session. The 14/14 plans' SUMMARY claims were spot-checked against code and did not diverge (the deviations each plan recorded were plan-detail corrections with evidence, consistent with the pinned contracts found in the code).

Carry-forwards already registered for later phases (not 17.1 scope): restored-original-intercepts-picks + difficulty calibration (Phase 19), p-first advice (Phase 22), driver cosmetics (opportunistic).

---

_Verified: 2026-09-05T09:03:25Z_
_Verifier: OpenCode (gsd-verifier)_
