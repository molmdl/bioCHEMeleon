---
phase: 17.1
plan: 09
type: execute
wave: 5
depends_on: ["17.1-06", "17.1-08"]
files_modified: [vmd/smoke/phase17_lines_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "A Lines-tier round produces bonded hiders whose HIDER rep renders dots (NOT bonds — the both-endpoints rule) while the scene rep draws the stubs (scene-diff cylinders >= n)"
    - "Hider stub halves in the hider renders carry element colors (cyan/blue/red/yellow/tan) and NEVER salmon (the GAM-name trap)"
    - "Read-back validation: hidden/found style string-compares to `Lines` exactly; per-tier user3 selections; registry remaining_by_rep == {Lines n}"
  artifacts:
    - path: "vmd/smoke/phase17_lines_smoke.tcl"
      provides: "Lines-tier end-to-end proof incl. the both-endpoints dot-vs-bond render semantics"
  key_links:
    - from: "vmd/smoke/phase17_lines_smoke.tcl"
      to: "vmd/lib/game.tcl"
      via: "game::start_game $molid n {Lines n} 0"
      pattern: "start_game"
---

<objective>
Prove the Lines tier end-to-end (HIDER-04's headline tier: line hiders that blend with a Lines scene).

Purpose: Lines is the bond-style rep where the both-endpoints rule matters most — a hiders-only Lines rep draws radius-0 dots, and the BOND is drawn by the scene's own all-selection rep (the overlay decision, viability P-3, decided once for all bond styles in 17.1). The smoke pins that semantics headlessly.
Output: `vmd/smoke/phase17_lines_smoke.tcl` PASS=1.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-RESEARCH-simple-generators.md
@.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-RESEARCH-game-reps-viability.md
@vmd/smoke/phase17_points_smoke.tcl

Key facts (do NOT re-derive):
- Lines render semantics (probe E/p6b/p7): a BONDED hider whose BOTH endpoints are in a Lines rep's selection draws 2 half-cylinders (FCylinder, merged when same-color); a hider whose partners are NOT selected draws a radius-0 dot-sphere (Rad ≈ 0.002). A hiders-only Lines rep → dots ONLY (0 FCylinders). The game's hider reps are overlays; the scene's own all-selection rep draws the bond (current hiders.tcl architecture — the P-3 overlay decision).
- Tachyon technique + regex starters + element color table + salmon trap + staging/harness conventions: IDENTICAL to 17.1-08 (copy that smoke's render helpers; inspect-and-pin the .dat format on first render).
- Bonds draw as two half-bonds colored by the NEAREST atom (UG node85) — the hider half carries the hider's element color; merged same-color halves make FC counts approximate: assert presence + colors, NEVER exact counts.
- Round/dispatch/registry/user3 contracts: per 17.1-08. Demo 1znf (424 atoms), `start_game $molid 3 [dict create Lines 3] 0`.
- THIS PLAN EDITS NO LIB FILES (defects → SUMMARY for a gap plan).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write + run the Lines-tier end-to-end smoke</name>
  <files>vmd/smoke/phase17_lines_smoke.tcl</files>
  <action>
Copy the 17.1-08 harness + render helpers; adapt the tier to Lines. Demo 1znf, round `game::start_game $molid 3 [dict create Lines 3] 0`:
1. Round asserts: atoms 427; hider idxs {424 425 426}; user3 ≈ 1.0; registry count 3; remaining_by_rep Lines == 3.
2. Mimicry + bonds: element ∈ {C N O S P} + radius match + name ∈ real names; every hider numbonds ≥ 1 (certifies the bond list that Lines consumes — DynamicBonds-free tiers rely on it).
3. Rep read-back: hidden/found style == `Lines` exactly; Element / ColorID 7; selections with `and user3 1`; numreps == pre_start + 2.
4. Tachyon renders (probe rep last; baseline-zero first):
   a. Hider-rep exclusive (selection `resname GAM and beta < 0`, style `Lines`, color `Element`): FCylinder count == 0 (both-endpoints rule — hiders-only draws NO bonds); sphere (dot) count == 3; all dot Rad < 0.02 (radius-0 dots, NOT spheres); dot colors within eps of the element-color set; NO salmon.
   b. Scene-diff (scene rep = default Lines selection all): A=`all` vs B=`not resname GAM` → (A.cyl − B.cyl) ≥ 3 (hider stubs drawn by the scene rep); (A.sph − B.sph) == 0.
   Delete the probe rep after renders.
5. Found-marking: mark 424 → user2 > 0; found-selection num 1 / hidden 2 (user3 1 conjunct); remaining_by_rep Lines == 2.
6. Cleanup restore + marker + exit.
  </action>
  <verify>Staged run: `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log; the hider-rep render shows 0 FCylinders (the both-endpoints dot proof).</verify>
  <done>Lines tier proven: dots-only hider rep, scene-drawn stubs, element-colored halves, exact read-back, per-tier partition, clean restore.</done>
</task>

</tasks>

<verification>
- `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log
- Hider-rep render: 0 FCylinders + 3 tiny dots + no salmon
- Tcl 8.6 gate zero; no lib files touched
</verification>

<success_criteria>
- HIDER-04's Lines semantics (bonded pseudoatom analogue blending into a Lines scene) proven headlessly
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-09-SUMMARY.md`
</output>
