---
phase: 17.1
plan: 08
type: execute
wave: 4
depends_on: ["17.1-06"]
files_modified: [vmd/smoke/phase17_points_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "A Points-tier round through the real dispatch produces bonded single-atom hiders: every hider numbonds >= 1, element/radius/name mimic a real anchor, sentinel round-trip holds"
    - "The Points hider rep renders EXACTLY one point per hider (Tachyon: n tiny spheres) with element colors — no salmon, no stray geometry"
    - "The scene (pre-styled Lines) draws the hider stubs via the both-endpoints rule (scene-diff cylinders >= n)"
    - "Read-back validation: hidden/found rep style string-compares to `Points` exactly; registry remaining_by_rep == {Points n}; found-marking partitions per tier"
  artifacts:
    - path: "vmd/smoke/phase17_points_smoke.tcl"
      provides: "first bonded-tier end-to-end proof through the 17.1-06 dispatch (chemistry + rep + render + found + cleanup); the reusable Tachyon probe pattern for 17.1-09..12"
  key_links:
    - from: "vmd/smoke/phase17_points_smoke.tcl"
      to: "vmd/lib/game.tcl"
      via: "game::start_game $molid n {Points n} 0 — the full dispatch path"
      pattern: "start_game"
    - from: "vmd/smoke/phase17_points_smoke.tcl"
      to: "render Tachyon"
      via: "headless pixel-proxy: exclusive-selection renders + primitive/color parse (viability research §7)"
      pattern: "render Tachyon"
---

<objective>
Prove the Points tier end-to-end through the real dispatch — the first bonded-tier round and the template for the remaining tier smokes.

Purpose: Points is the trivially-verifiable bonded tier (one dot per atom, UG node60). Proving the shared bonded chemistry (anchor mimicry + bond law + user3 stamps + registry tiers) through start_game HERE de-risks the four bond-family smokes that follow.
Output: `vmd/smoke/phase17_points_smoke.tcl` PASS=1.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-RESEARCH-simple-generators.md
@.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-RESEARCH-game-reps-viability.md
@vmd/smoke/phase17_dispatch_smoke.tcl
@vmd/lib/game.tcl

Key facts (do NOT re-derive):
- Points: one point per atom, no bonds (UG node60); exports as tiny spheres in Tachyon (probe-verified `8/8`); lone atoms render — but our hiders are BONDED (numbonds ≥ 1 asserted anyway — it certifies the shared chemistry).
- Tachyon technique (viability research §2/§7): `render Tachyon <file>` works in -dispdev text; `axes location off` first; use EXCLUSIVE selections on a dedicated probe rep (added last, deleted last — deleting the highest index never renumbers earlier reps); baseline-zero render (selection `index 999999` → 0 primitives) proves the harness. Parse in-smoke: read the .dat, count `Sphere`/`FCylinder` primitive tokens + extract `Rad`/`Color R G B` floats. FIRST render once and INSPECT the .dat head from WSL, then pin the counting regex (suggested starters: `regexp -all -- {\mFCylinder\y} $txt`, `regexp -all -- {\mSphere\y} $txt`, `regexp -all -- {Rad\s+(-?[0-9.eE+-]+)} $txt`, `regexp -all -- {Color\s+(-?[0-9.eE+-]+)\s+(-?[0-9.eE+-]+)\s+(-?[0-9.eE+-]+)} $txt`).
- Element colors (eps 0.01): C cyan (0.25 0.75 0.75), N blue (0 0 1), O red (1 0 0), S yellow (1 1 0), P tan (0.5 0.5 0.31 approximate — inspect the actual tan RGB from a render and pin). SALMON (1.0 0.6 0.6) = Name-coloring leak on GAM-named atoms — must NEVER appear in a hider render (hiders are anchor-named + reps use Element coloring).
- Bond law + mimicry + sentinel constants: per 17.1-05 context (numbonds ≥ 1; element ∈ {C N O S P}; radius C 1.70 / N 1.55 / O 1.52 / S 1.80 / P 1.55; name ∈ real atom names; sentinel resname GAM / beta -999.0 / segid GAME).
- Contracts: `start_game {molid hider_count {per_rep {}} {lock_scene 0}}`; `hiders::tier_reps` (code → {hidden_name found_name hidden_sel found_sel}); `mol repindex` resolution; user3 single-tier code = 1; registry `remaining_by_rep`.
- Staging: `mkdir -p tmp/biochemeleon-vmd && cp -r vmd tmp/biochemeleon-vmd/` then `timeout 300 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/smoke/phase17_points_smoke.tcl -eofexit < /dev/null' 2>&1 | grep -E "BCHM_SMOKE_RESULT|ERROR\)|bad switch"` — parse the marker, scan the FULL log; every smoke ends with `exit`; hard `error`/`_bail` guards on every expected count (VMD -e swallows statement errors — Pitfall 7).
- THIS PLAN EDITS NO LIB FILES: if a lib defect surfaces, record it in the SUMMARY (repo + failing assert) for a gap-closure plan — do not patch libs in a tier smoke (wave-disjointness).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write + run the Points-tier end-to-end smoke</name>
  <files>vmd/smoke/phase17_points_smoke.tcl</files>
  <action>
Harness per the 17.1-06 dispatch smoke (source order: setup_state, registry, rep_tiers, generators, game_logic, demos, backup, mutation, hiders, game; failures list + `_bail` + marker + exit). Demo 1znf (424 atoms), round `game::start_game $molid 3 [dict create Points 3] 0`:
1. Round asserts: game atoms 427; hider idxs {424 425 426}; user3 ≈ 1.0 on all three (numeric); registry count_hiders 3; remaining_by_rep has key Points with 3.
2. Mimicry: per hider — element ∈ {C N O S P}; radius == element's VDW radius (eps 1e-6); name ∈ the real atom-name set (collected once pre-start).
3. Bonds: every hider numbonds ≥ 1.
4. Rep read-back via tier_reps + repindex (COMBINED-braces form): hidden pair style == `Points` EXACTLY, color `Element`, selection == `resname GAM and beta < 0 and user2 < 1 and user3 1`; found pair `ColorID 7` + the `user2 > 0` variant. numreps == pre_start + 2.
5. Tachyon renders (probe rep added last; `axes location off` first):
   a. Baseline-zero: selection `index 999999` → 0 primitives (harness proof).
   b. Hider-rep exclusive: selection `resname GAM and beta < 0`, style `Points`, color `Element` → sphere count == 3 (points export as tiny spheres); NO FCylinder; every parsed Color within eps of one of the 5 element colors; NO salmon (1.0 0.6 0.6).
   c. Scene-diff (scene rep = default Lines, selection all): render A selection `all`, render B selection `not resname GAM` → (A.cyl − B.cyl) ≥ 3 (hider stubs drawn by the scene rep via the both-endpoints rule); (A.sph − B.sph) == 0.
   Delete the probe rep (highest index) after renders.
6. Found-marking: `hiders::mark_found_visual $gm 425` → user2(425) > 0 numeric; atomselect `resname GAM and beta < 0 and user2 > 0 and user3 1` num == 1; the `< 1` variant num == 2; remaining_by_rep Points == 2.
7. Cleanup: `game::cleanup $gs` → registry 0; game molid dead; restored original intact (numatoms 424).
8. Marker + exit.
  </action>
  <verify>Staged run: `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log; the pinned .dat regexes confirmed against the baseline-zero render (0 primitives).</verify>
  <done>Points tier proven end-to-end: bonded chemistry through the dispatch, exact rep read-back, per-hider point rendering with element colors, scene stub blending, per-tier found-marking, clean restore.</done>
</task>

</tasks>

<verification>
- `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log
- Tcl 8.6 gate zero on the new file; no lib files touched
- No salmon color in any parsed hider render; baseline-zero render has 0 primitives
</verification>

<success_criteria>
- The bonded-tier template (round → mimicry → bonds → read-back → Tachyon → found → cleanup) works and is copied by 17.1-09..12
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-08-SUMMARY.md`
</output>
