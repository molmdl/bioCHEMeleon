---
phase: 17.1-rep-setup-infrastructure-simple-rep-generators
plan: 07
subsystem: testing
tags: [vmd, tcl-8.5, smoke-tests, n-tier-reps, per-rep, regression-baseline, headless-vmd]

# Dependency graph
requires:
  - phase: 17.1-06
    provides: 4-arg start_game dispatch composition root (per_rep/lock_scene), game_state 4-key shape, stamp_tier_codes-before-add_hider_reps ordering, rep_tiers in the entry source list
  - phase: 17.1-04
    provides: N-tier hiders.tcl (tier_reps/tier_styles dicts, mol repindex resolution, user3-conjoined selections; hidden_rep/found_rep vars removed)
  - phase: 17.1-01
    provides: rep_tiers pure module (scene_reps_to_per_rep/resolve_per_rep — the procs the legacy smokes failed on before this plan)
provides:
  - All 12 pre-17.1 legacy smokes PASS=1 under the N-tier contracts (the VDW-only regression baseline preserved, P12 satisfied)
  - The 8 target smokes updated to explicit VDW-only per_rep rounds, tier_reps/repindex rep resolution, user3-conjoined selection asserts, 4-key game_state shape
  - rep_tiers added to the direct-source lists of 7 smokes (entry-order position)
affects: [17.1-08+, 17.2-cartoon-generators, phase-19-game-tab, next GUI verify session (pick_verify.tcl still reads dead vars)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Explicit VDW-only per_rep ([dict create VDW n] 0) = the legacy-smoke regression baseline under the N-tier dispatch contract"
    - "Rep resolution via tier_reps dict + mol repindex with -1 guard (never cached indices / removed namespace vars)"
    - "Rep_tiers must appear in every smoke's direct source list (game.tcl resolves rep_tiers::* at CALL time)"

key-files:
  created: []
  modified:
    - vmd/smoke/phase16_smoke.tcl
    - vmd/smoke/phase16_hiders_smoke.tcl
    - vmd/smoke/phase16_onpick_smoke.tcl
    - vmd/smoke/phase16_restart_smoke.tcl
    - vmd/smoke/phase15_game_smoke.tcl
    - vmd/smoke/phase15_smoke.tcl
    - vmd/smoke/phase16_entry_smoke.tcl
    - vmd/smoke/phase16_placement_smoke.tcl

key-decisions:
  - "Explicit VDW-only per_rep on every legacy start_game call keeps +2 numreps invariants intact — zero assert-churn on the numreps arithmetic (the plan's minimal-churn mandate)"
  - "onpick's gs_shape/stash asserts updated 3-key -> 4-key (17.1-06 additive per_rep key) — stale-contract assert, plan did not enumerate it"
  - "phase16_hiders_smoke keeps its 1-arg add_hider_reps call (default {{1 VDW}}) and adds the production-order stamp_tier_codes before it (idempotent with start_game's internal stamp)"
  - "restart_smoke/15_game_smoke assert per-key dict-exists (not key-list equality), so the 4-key game_state needed no shape-assert changes there"
  - "pick_verify.tcl left untouched — out of plan scope (smoke files only); it still reads the removed hidden_rep/found_rep vars and breaks the NEXT GUI session"

patterns-established:
  - "VDW-only baseline round: [dict create VDW <n>] 0 as start_game args 3-4"
  - "tier_reps code -> {hidden_name found_name hidden_sel found_sel} + mol repindex for rep resolution in smokes"
  - "Selection-string asserts carry the ` and user3 1` conjunct for single-tier rounds"

# Metrics
duration: 12min
completed: 2026-09-04
---

# Phase 17.1 Plan 07: Smoke Inventory Update Summary

**All 12 pre-17.1 legacy smokes restored to PASS=1 under the N-tier contracts via explicit VDW-only per_rep rounds — the Phase-16 VDW regression baseline preserved with +2 numreps invariants intact and zero lib-file changes.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-09-04T03:56:15Z
- **Completed:** 2026-09-04T04:07:17Z
- **Tasks:** 2
- **Files modified:** 8 (all vmd/smoke/*.tcl)

## Accomplishments
- The 8 known-red legacy smokes (phase16_smoke, phase16_hiders_smoke, phase16_onpick_smoke, phase16_restart_smoke, phase15_game_smoke, phase15_smoke, phase16_entry_smoke, phase16_placement_smoke) all PASS=1 on fresh staging with zero `ERROR)` / `bad switch` / `no such variable` / `invalid command name` / `can't read` in every full log
- The 4 untouched smokes (phase15_backup_smoke, phase15_mutation_smoke, phase16_gametab_smoke, phase16_pick_smoke) confirmed still green — full 12/12 legacy suite green
- phase16_smoke resolves hider reps via the tier_reps dict + `mol repindex` (the removed `hidden_rep`/`found_rep` namespace vars), keeping the base..base+1 position assert under the explicit VDW-only round
- phase16_hiders_smoke mirrors the production ordering (stamp_tier_codes BEFORE add_hider_reps) and gains a user3 tier-partition check (`resname GAM and beta < 0 and user3 1` num == 5)
- rep_tiers added to the direct source lists of 7 smokes at the entry-order position (after game_logic / after registry where game_logic is absent) — the `invalid command name "...scene_reps_to_per_rep"` early-failure class from the 17.1-06 evidence eliminated

## Task Commits

Each task was committed atomically:

1. **Task 1: Update the two hider-rep smokes (string/var contract changes)** - `9164b28` (test)
2. **Task 2: Update the numreps-invariant smokes + entry/placement ripples + full legacy-suite run** - `af1971d` (test)

## Files Created/Modified
- `vmd/smoke/phase16_smoke.tcl` - VDW-only round, tier_reps/repindex rep resolution, user3-conjoined selection asserts, rep_tiers in source list
- `vmd/smoke/phase16_hiders_smoke.tcl` - production-order stamp before 1-arg add_hider_reps, user3 conjunct on both exact-string asserts, user3 partition check, rep_tiers in source list
- `vmd/smoke/phase16_onpick_smoke.tcl` - VDW-only round, rep_tiers in source list, game_state/stash shape asserts 3-key -> 4-key
- `vmd/smoke/phase16_restart_smoke.tcl` - explicit VDW-only per_rep on all 4 start_game calls (A1/A2/B/C), rep_tiers in source list
- `vmd/smoke/phase15_game_smoke.tcl` - explicit VDW-only per_rep on both start_game calls (restart pass-through keeps the replay round VDW-only), rep_tiers in source list
- `vmd/smoke/phase15_smoke.tcl` - explicit VDW-only per_rep, rep_tiers in source list
- `vmd/smoke/phase16_entry_smoke.tcl` - 2-arg call -> explicit VDW-only per_rep (+2 assert restored; entry sources rep_tiers since 17.1-06)
- `vmd/smoke/phase16_placement_smoke.tcl` - explicit VDW-only per_rep (free tier => bbox-inside by construction), rep_tiers in source list

## Decisions Made
- **Explicit VDW-only per_rep everywhere** (the plan's core strategy): keeps every numreps assert at base + 2 with zero arithmetic changes, keeps placement-smoke's bbox containment valid (VDW = free tier = make_placeholder_hiders), and preserves the VDW regression baseline (P12)
- **onpick gs_shape/stash asserts updated to the 4-key shape** — the plan's per-file guidance said "no assert changes needed" for onpick, but its step-1 asserts checked the game_state key LIST and stash size, which 17.1-06 changed additively (see Deviations)
- **phase16_hiders_smoke stamp kept explicit** even though start_game's internal stamp (per_rep {VDW 5}, code 1) already covers it — the smoke drives add_hider_reps directly as a unit test of the hiders bridge, so mirroring the production stamp-before-add ordering keeps it self-contained (idempotent write)
- **restart_smoke/15_game_smoke needed no shape-assert changes** — they check `dict exists` per key rather than key-list equality, so the additive per_rep key is invisible to them

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] onpick's game_state shape asserts were stale (3-key)**
- **Found during:** Task 2 (phase16_onpick_smoke.tcl)
- **Issue:** The smoke asserted `[dict keys $gs] ne "game_molid hider_count snapshot"` and stash size == 3; 17.1-06 made game_state 4-key additively ({game_molid hider_count snapshot per_rep}). The assert was never reached pre-plan (the smoke failed earlier on the missing rep_tiers source), so the staleness was invisible until this plan fixed the source list.
- **Fix:** Updated the key-list assert to the 4-key shape and the stash-size assert 3 -> 4 (header comment updated too).
- **Files modified:** vmd/smoke/phase16_onpick_smoke.tcl
- **Verification:** Staged run PASS=1, 0 ERROR)/bad switch in full log
- **Committed in:** af1971d (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** The single fix was required for the smoke to pass at all; no scope creep. All other guidance from the plan (including the 17.1-06 fresh-evidence worklist: rep_tiers in source lists + VDW-only assertion updates) applied as written.

## Issues Encountered
- Executor spawn cwd was the MAIN checkout, not the worktree (the 17.1-01 process lesson recurring): `git rev-parse --abbrev-ref HEAD` returned `main` at start. Resolved by working entirely inside the prepared worktree `tmp/exec-17.1-07` (branch verified `exec/17.1-07` before the first commit; every bash call pinned with an explicit workdir). Zero commits landed on main.
- `rm` is config-denied in this environment; staging refresh used fresh directory names (`tmp/p17-07-s1`, `tmp/p17-07-s2`) instead of delete-and-recreate.
- **Known remaining (out of scope):** `vmd/tests/pick_verify.tcl` (~136-139, ~255-261) still reads the removed `hiders::hidden_rep`/`found_rep` vars — it is a GUI verify driver, not a smoke, and the plan's contract is smoke files only. It will break the NEXT GUI human-verify session until updated (already tracked in STATE.md Pending Todos).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- The entire pre-17.1 smoke suite is green against the N-tier contracts: any future regression is attributable to 17.1+ changes (the plan's success criterion)
- VDW-only regression baseline established as the repeatable pattern: `[dict create VDW <n>] 0` as start_game args 3-4
- Ready for 17.1-08 (next plan in the wave); carry-forward: pick_verify.tcl must be updated BEFORE the next GUI verify session (reads dead hidden_rep/found_rep vars)

---
*Phase: 17.1-rep-setup-infrastructure-simple-rep-generators*
*Completed: 2026-09-04*
