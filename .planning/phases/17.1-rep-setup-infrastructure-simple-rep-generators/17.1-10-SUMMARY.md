---
phase: 17.1
plan: 10
subsystem: testing
tags: [vmd, tcl, tachyon, licorice, ball-stick, render-diff, headless-smoke, dispatch]

# Dependency graph
requires:
  - phase: 17.1-06
    provides: "game.tcl 4-arg per-tier dispatch (start_game {molid n per_rep lock_scene}) + its dispatch smoke"
  - phase: 17.1-08
    provides: "Points-tier end-to-end smoke = the copy template (Tachyon pixel-proxy pattern, parser helpers, pinned constants)"
provides:
  - "Licorice-tier end-to-end proof through the real 17.1-06 dispatch (vmd/smoke/phase17_licorice_smoke.tcl, PASS=1 3/3)"
  - "THE PRE-STYLED-SCENE RENDER-DIFF PATTERN: restyle the scene rep before start_game, then A-vs-B scene diff proves ball+stick blending in-scene -- the pattern 17.1-11 (CPK) and 17.1-12 (DynamicBonds) reuse"
  - "Pinned Licorice evidence: hiders-only rep = bond-radius spheres (Rad 0.0119155) + 0 FCylinders (both-endpoints rule); scene rep selection-all draws every atom as a ball (scene-diff sph == n exactly)"
affects: [17.1-11, 17.1-12, 17.2-cartoon-generators, phase-18-materials]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pre-styled-scene diff: mol modstyle 0 $m <Style> on the ORIGINAL before start_game -> backup::snapshot/apply carries it onto the game molecule -> render A(all) vs B(not resname GAM) measures the actual blend geometry; cleanup asserts the style survives restore (snapshot fidelity)"
    - "Licorice sphere-radius pinning: exact ball Rad asserted (eps 1e-6) on top of the > dot-class bound, pinned from the first render (17.1-08 method)"

key-files:
  created:
    - vmd/smoke/phase17_licorice_smoke.tcl
  modified: []

key-decisions:
  - "Hider-rep exclusive render asserts 3 spheres + 0 FCylinders (both-endpoints rule: no hider partner selected; hiders >= 4 A apart so no GAM-GAM bonds) -- probe p6b semantics, not re-derived"
  - "Licorice ball radius pinned exact (0.0119155, byte-identical x3 spheres, stable across 3 independent runs) instead of a loose bound -- stronger evidence, same pin-from-first-render method as 17.1-08's Rad 0.002 dot pin"
  - "Scene-diff asserts A.sph - B.sph == 3 EXACTLY (Licorice draws a ball for every selected atom, so removing 3 hiders removes exactly 3 balls) vs 17.1-08's == 0 (Lines draws no spheres) -- the per-style sphere-delta contract"

patterns-established:
  - "Pre-styled-scene render diff (reused by 17.1-11/12)"
  - "Snapshot-fidelity style assert: restored original's rep-0 style must read back the pre-styled style after cleanup"

# Metrics
duration: 9min
completed: 2026-09-04
---

# Phase 17.1 Plan 10: Licorice-Tier End-to-End Smoke Summary

**Licorice tier proven end-to-end through the real 17.1-06 dispatch on 1k8p -- including ball+stick blending into a scene that is itself styled Licorice via the new PRE-STYLED-SCENE render diff (hider balls +3 AND scene stub cylinders +>=3), PASS=1 3/3 runs with the exact ball-radius pin 0.0119155.**

## Performance

- **Duration:** 9 min
- **Started:** 2026-09-04T18:22:53Z
- **Completed:** 2026-09-04T18:32:22Z
- **Tasks:** 1
- **Files modified:** 1 (created)

## Accomplishments
- `vmd/smoke/phase17_licorice_smoke.tcl` PASS=1 on 3/3 staged runs (independent PRNG anchor draws; 0 `ERROR)` / 0 `bad switch` / 0 no-such-variable in every full log, "Exiting normally" every run); Tcl 8.6 gate zero; NO lib files touched.
- HIDER-04's Licorice semantics proven headlessly: a hiders-only Licorice rep renders exactly 3 bond-radius spheres (Rad 0.0119155 pinned, all byte-identical) and 0 FCylinders (both-endpoints rule -- no hider partner is in the selection, and hiders sit >= 4 A apart so no GAM-GAM bonds), with element colors (blue N / red O / cyan C observed across runs) and no salmon.
- Ball+stick blending proven IN-SCENE: the original's rep 0 was pre-styled `Licorice` before start_game; backup::snapshot/apply carried it onto the game molecule (scene rep read-back == `Licorice`), and the scene diff A(all) vs B(not resname GAM) showed +3 spheres (hider balls; exact -- every selected Licorice atom draws a ball) AND +12/+14 cylinders across runs (hider stubs via the both-endpoints rule; >= 3 asserted for same-color half-bond merging).
- Full round contract on demo 1k8p (555 atoms -> 558 game molecule, hiders {555 556 557}): user3 == 1.0 on all hiders / 0.0 on a real atom, registry {Licorice 3}, mimicry (element in {C N O S P}, radius == element VDW eps 1e-6 AND == real same-element radius, name in the real name set), numbonds >= 1, exact rep read-back (hidden/found style `Licorice` exactly, Element / ColorID 7, literal user3-1 selections, numreps == pre+2), found partition 1/2 + remaining {Licorice 2}, cleanup restore (registry 0, game molid dead, 555 atoms, numreps == pre_start, rep-0 style STILL `Licorice` -- snapshot fidelity through snapshot->apply->restore).

## Task Commits

Each task was committed atomically:

1. **Task 1: Write + run the Licorice-tier end-to-end smoke** - `0e1b6cf` (test)

**Plan metadata:** (this commit)

## Files Created/Modified
- `vmd/smoke/phase17_licorice_smoke.tcl` - Licorice-tier end-to-end dispatch smoke; the pre-styled-scene render-diff pattern source for 17.1-11 (CPK) and 17.1-12 (DynamicBonds)

## Decisions Made
- Hider-rep exclusive render asserts 3 spheres + 0 FCylinders per probe p6b semantics (bond-radius balls, no cylinders without both endpoints selected) -- plan-pinned facts, not re-derived.
- Licorice ball radius pinned EXACT (0.0119155) and asserted with eps 1e-6 on top of the > 0.002 dot-class bound; observed byte-identical on all 3 spheres and stable across 3 independent runs (Tachyon radii are view-scale-normalized and deterministic for the default fresh-load view -- same stability class as 17.1-08's Rad 0.002 dots and probe K's Rad 0.094504 VDW spheres).
- Scene-diff sphere delta asserted == 3 EXACTLY (per-style contract: Licorice balls every selected atom, so removing the 3 hiders removes exactly 3 balls; 17.1-08's Lines scene asserted == 0) -- the per-style sphere-delta contract for the 17.1-11/12 copies to adapt.

## Deviations from Plan

None - plan executed exactly as written. (The ball-radius pin was the plan-prescribed first-render inspection flow, not a deviation; the scene-rep style read-back assert on the game molecule is the plan step-4c parenthetical "(scene rep now Licorice)" made explicit.)

## Issues Encountered
- None. 1k8p is a nucleic-acid demo (2 nucleic fragments, 0 protein): its `Unusual bond between residues: N (nucleic) and M (none)` load warnings are the documented normal bonded-round noise (hiders bond cross-residue), and its heavy-atom anchors (C/N/O/P) exercised the element-color set cleanly. The tan P color remains inert-pinned from colordefs.dat (no P-anchored hider drawn in 3 runs; set covers it if one appears).
- tclsh was unavailable in this session's PATH for a pre-run `info complete` brace check -- irrelevant: VMD parsed the file cleanly (0 syntax-class errors in 4 total runs).

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- 17.1-11 (CPK) and 17.1-12 (DynamicBonds) copy THIS file's pre-styled-scene diff pattern: pre-style rep 0 with the tier's canonical style, validate read-back before start_game, expect the per-style sphere-delta contract (CPK = VDW-like balls + stubs: sph diff == n; DynamicBonds = bond-only: sph diff == 0 like Lines), keep the Directional_Light Color filter, per-field single-attribute gets, and the full-log ERROR)/bad-switch scan.
- All prior smokes untouched and green; zero lib-file changes means zero regression surface for the Wave-5 merges.
- Carry-forward for the phase verifier: re-run all smokes on fresh staging as usual; this smoke's staging dir is tmp/l10 (gitignored).

---
*Phase: 17.1-rep-setup-infrastructure-simple-rep-generators*
*Completed: 2026-09-04*
