---
phase: 17.1-rep-setup-infrastructure-simple-rep-generators
plan: 03
subsystem: vmd-generators
tags: [tcl, tcltest, vmd-1.9.3, tdd, geometry, fisher-yates, prng, bonded-placement]

# Dependency graph
requires:
  - phase: 16-mvp-core-loop-sphere
    provides: "vmd/lib/generators.tcl (sphere_positions + the seed-once convention) and the headless tcltest harness pattern"
  - phase: 17.1-research-simple-generators
    provides: "probe-verified bond law d < 0.6*(ri+rj), 1.2-1.6 A band, per-entry-sep overlap design, %8.3f overflow pitfall"
provides:
  - "Pure bonded-placement layer in generators.tcl: sample, unit_vector, bonded_offset, reject_overlaps, place_bonded_hider + geometry constants (D_MIN/D_MAX/MIN_SEP_REAL/MIN_SEP_HIDER/MAX_COORD/MAX_TRIES/RELAX_ROUNDS)"
  - "18 tcltest cases pinning the bonded geometry contract (bounds, strict-< boundary, per-entry seps, relaxation termination, overflow clamp, determinism); sphere_positions cases still green (26/26 total)"
affects: [17.1-05 mutation bridge (make_bonded_hiders consumes sample + place_bonded_hider), 17.2 cartoon generators, phase-17.1 verifier]

# Tech tracking
tech-stack:
  added: []   # stdlib tcl only (rand/srand/lset/lassign) — zero new deps
  patterns:
    - "Partial Fisher-Yates public port (setup_state::_sample -> generators::sample) with n<=0 / n>len guards"
    - "Strict-< rejection via squared-distance comparison (d2 < sep2 equivalent to d < sep; boundary passes)"
    - "Per-entry-sep occupied shape {x y z sep} lets one proc enforce MIN_SEP_REAL (1.4) and MIN_SEP_HIDER (4.0)"
    - "Bounded relaxation contract: MAX_TRIES draws -> halve ALL seps x RELAX_ROUNDS -> final drawn candidate accepted with coords clamped (always terminates)"
    - "Seed-once convention: srand at proc top when non-empty; internal procs called seedless (global PRNG stream continues)"

key-files:
  created: []
  modified:
    - "vmd/lib/generators.tcl (5 procs + constants + section header; sphere_positions byte-identical)"
    - "vmd/tests/test_generators.test (18 new cases before the marker block)"

key-decisions:
  - "Strict-< rejection implemented via squared comparison (plan-endorsed); boundary equivalence validated bit-exact (2.4-1.0 == 1.4 in IEEE doubles, so d2 == sep2 at the boundary and strict-< passes it)"
  - "Added an 18th test case (place_clamps_after_exhaustion) pinning the specified-but-untested final-clamp termination branch: an occupied point AT the anchor with sep 6.4 forces every relaxation round to reject (final seps 6.4/3.2/1.6 vs candidate distance [1.2,1.6)), so the clamp path is deterministically exercised"
  - "Seeded geometric test seeds were probe-validated under headless VMD BEFORE the RED commit (shell seed 7 accepts at full seps with min occupied distance 1.79; several plausible seeds -- e.g. 42 -- only pass via relaxation and were rejected); cage seed 11, clamp seed 13"
  - "Comment-only 'mol' mentions kept in generators.tcl header (matches registry.tcl precedent; purity gate targets API calls, of which there are zero)"

patterns-established:
  - "Probe-validate seeded geometric tests before committing them (a seed is only provably good empirically; several seeds silently degrade to relaxed acceptance)"
  - "Scratch-probe checks must be catch-guarded: VMD -e catches top-level errors and continues, so bare puts PASS lines are false-passes (15-04 lesson re-confirmed); also $G::name parses as ONE variable name, not a command prefix"

# Metrics
duration: 3h 45m wall (incl. probe validation + scratch-probe rewrites)
completed: 2026-09-04
---

# Phase 17.1 Plan 03: Pure Bonded-Hider Placement Layer Summary

**Pure bonded-placement geometry (sample / unit_vector / bonded_offset / reject_overlaps / place_bonded_hider) pinning the 1.2–1.6 Å bond band, strict-< per-entry-sep rejection, bounded relaxation, and the 9999.0 %8.3f overflow guard — TDD RED→GREEN, 26/26 tcltest green under headless VMD.**

## Performance

- **Duration:** 3h 45m wall (21:21Z → 01:06Z; includes probe validation cycles and two scratch-probe rewrites after the documented VMD `-e` false-pass pitfall)
- **Started:** 2026-09-03T21:21:47Z
- **Completed:** 2026-09-04T01:06:27Z
- **Tasks:** 2 (RED + GREEN; REFACTOR judged unnecessary — no changes made)
- **Files modified:** 2

## Accomplishments

- The bonded geometry contract is pinned by tests BEFORE the mol bridge consumes it: offsets drawn in [1.2, 1.6] Å (≥ 0.33 Å margin inside every C/N/O/S cutoff under the verified bond law `d < 0.6×(ri+rj)`), rejection is strict-< per-entry-sep, placement terminates via 25-try rounds + 2 sep-halving relaxations with a final clamped draw, and |coord| ≤ 9999.0 always.
- `sample` is a public partial-Fisher-Yates port of `setup_state::_sample` with the n ≤ 0 → empty and n > len → full-shuffle guards the private version lacked.
- sphere_positions left byte-identical (verified by extracting the proc from HEAD and diffing); all 8 pre-existing cases stay green alongside the 18 new ones (Total=26).

## Task Commits

Each task was committed atomically (TDD cycle):

1. **Task 1: RED — failing bonded-placement tests** - `8f85af3` (test: 18 cases, all failing with invalid-command-name; 8 pre-existing green; zero `bad switch`)
2. **Task 2: GREEN — implement the 5 procs** - `d6abefb` (feat: constants + _dist2 + sample/unit_vector/bonded_offset/reject_overlaps/place_bonded_hider; 26/26)

**Plan metadata:** pending (docs commit after this summary)

_REFACTOR: no changes needed — implementation was already minimal and documented; per TDD rules no refactor commit was made._

## Files Created/Modified

- `vmd/lib/generators.tcl` — extended with the Phase 17.1 bonded-placement section: 7 constants (one-per-line namespace vars), private `_dist2`, and the 5 public procs; header documents the bond law, the two sep constants' meanings, the relaxation contract, the overflow guard, and that extra bonds are normal (numbonds ≥ 1 belongs to the bridge/smoke layer); export line now lists all 6 public symbols
- `vmd/tests/test_generators.test` — `# ---- Phase 17.1: bonded placement ----` section with 18 cases before the marker block

## Decisions Made

See `key-decisions` in frontmatter. In short: squared-comparison strict-< (plan-endorsed, bit-exact boundary validated), an added 18th clamp-branch test, probe-validated seeds for all geometric cases, comment-only "mol" mentions kept per registry.tcl precedent.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Added `place_clamps_after_exhaustion` (18th case)**

- **Found during:** Task 1 (RED test design)
- **Issue:** The behavior spec defines the exhaustion branch ("after the last relaxation accept the FINAL drawn candidate with each coord clamped") but the 17 listed cases cannot deterministically reach it (with empty occupied, all-25-draws-over-bound has probability ~3e-11)
- **Fix:** One occupied point AT the anchor with sep 6.4 rejects every candidate at every relaxation level (final seps 6.4/3.2/1.6 vs candidate distance in [1.2, 1.6) — strict <), forcing the clamp path deterministically; probe-validated (seeds 13/21/2 all return mode=clamped with distance preserved in [1.2, 1.6])
- **Files modified:** vmd/tests/test_generators.test
- **Verification:** GREEN run 26/26
- **Committed in:** 8f85af3 (RED) / d6abefb (GREEN)

---

**Total deviations:** 1 auto-added (missing critical test coverage)
**Impact on plan:** No scope creep — the added case tests behavior the plan's own must_haves specify ("terminates via bounded retries + sep-halving relaxation... never exceeds |coord| 9999.0").

## Issues Encountered

- **tclsh unavailable in WSL** (STATE.md 13-01): the plan's optional `tclsh vmd/lib/generators.tcl` load-clean check was skipped; the staged headless-VMD run covers load-clean (26 passing tests prove the file sources cleanly under Tcl 8.5.6).
- **Scratch probe false-passes (first draft):** `$G::_probe_sample` parses as ONE variable name (`G::_probe_sample`), not variable `G` + command suffix; combined with VMD `-e`'s error-continuation, bare `puts` PASS lines printed false PASSes (the 14-02/15-04 lesson, re-confirmed). The probe was rewritten with fully-qualified calls + catch-guarded checks and a hard `error` on any failure.
- **Seed-dependence of geometric tests:** a first pass showed several plausible seeds (e.g. 42 on the shell case) only satisfy the occupied-seps contract via RELAXED rounds (min occupied distance 0.87 < 1.4); seeds were chosen that accept at FULL seps so the test asserts the real contract, not the relaxation fallback.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Ready for 17.1-04 (parallel wave) and 17.1-05: `mutation.tcl` may now consume `generators::sample` (anchor selection) and `generators::place_bonded_hider` (per-hider placement) — mutation already sources generators.tcl.
- Consumers must pass anchors/neighborhoods themselves (this layer NEVER touches mol/atomselect) and must assert `numbonds >= 1`, never `== 1` (extra bonds are normal and blend-positive).
- No blockers or concerns carried forward.

---
*Phase: 17.1-rep-setup-infrastructure-simple-rep-generators*
*Completed: 2026-09-04*
