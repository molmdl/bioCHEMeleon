---
phase: 17.1
plan: 06
type: execute
wave: 3
depends_on: ["17.1-01", "17.1-02", "17.1-03", "17.1-04", "17.1-05"]
files_modified: [vmd/lib/game.tcl, vmd/biochemeleon.tcl, vmd/smoke/phase17_dispatch_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "start_game {molid hider_count {per_rep {}} {lock_scene 0}} is backward compatible: existing 2-arg calls keep working and randomize across IMPLEMENTED_TIERS"
    - "Lock-scene rounds derive `detected` from the round's OWN backup::snapshot reps (game reps structurally absent) and restrict per_rep to detected ∩ IMPLEMENTED_TIERS"
    - "The per-tier loop concatenates record lists in tier order (GAME_REPS order), runs ONE mutate call, splits the sorted hider-index list by cumulative per-tier record counts (file order == record order), and stamps user3 BEFORE add_hider_reps (cached-selection trap)"
    - "Registry: ONE reconstruct_from_sentinels (1-arg) + ONE assign_reps (idx → GAME_REPS name) — never a reconstruct loop (P8)"
    - "game_state gains per_rep (additive) and hider_count = the EFFECTIVE total (sum of per_rep — P9); restart passes per_rep through symmetrically — lock_scene is deliberately NOT stashed in game_state (the stashed per_rep already encodes the resolved lock-scene restriction, so restart's defensive lock_scene read defaults to 0, which is moot for the distribution). The dispatch smoke does not cover restart-with-lock_scene"
    - "Unimplemented tiers requested via per_rep are dropped with a non-blocking vmdcon warning (v1 under-generation parity)"
  artifacts:
    - path: "vmd/lib/game.tcl"
      provides: "per-tier dispatch in start_game + extended restart; game_state {game_molid hider_count snapshot per_rep}"
      exports: ["start_game", "cleanup", "restart", "on_pick", "set_callbacks"]
    - path: "vmd/biochemeleon.tcl"
      provides: "entry source line for lib/rep_tiers.tcl (pure block, after game_logic)"
    - path: "vmd/smoke/phase17_dispatch_smoke.tcl"
      provides: "multi-tier end-to-end dispatch proof (VDW 2 + Lines 2): atoms, 2N reps, user3 stamps, registry remaining_by_rep, per-tier find, effective hider_count, cleanup restore, 2-arg randomize containment"
  key_links:
    - from: "vmd/lib/game.tcl"
      to: "vmd/lib/rep_tiers.tcl"
      via: "scene_reps_to_per_rep(snapshot reps) → resolve_per_rep → tiers_from_per_rep → tier_kind/style_args"
      pattern: "rep_tiers::(scene_reps_to_per_rep|resolve_per_rep|tiers_from_per_rep|tier_kind|style_args)"
    - from: "vmd/lib/game.tcl"
      to: "vmd/lib/registry.tcl"
      via: "reconstruct_from_sentinels DI lambda (COMMAND-PREFIX form) + assign_reps"
      pattern: "registry::(reconstruct_from_sentinels|assign_reps)"
    - from: "vmd/lib/game.tcl"
      to: "vmd/lib/hiders.tcl"
      via: "stamp_tier_codes BEFORE add_hider_reps, both AFTER backup::apply"
      pattern: "hiders::(stamp_tier_codes|add_hider_reps)"
---

<objective>
Extend `game.tcl::start_game` into the per-tier dispatch composition root — lock-scene derivation, per-tier placement loop, multi-tier registry stamping, effective hider_count — plus its headless dispatch smoke.

Purpose: this is the plan that turns Phase-16's single-VDW game into an N-tier game (ROADMAP SC2: lock-scene detects and matches; randomize distributes across GAME_REPS). Every generator tier (17.1-08..12) is exercised through this loop.
Output: extended `vmd/lib/game.tcl` + entry source line + `vmd/smoke/phase17_dispatch_smoke.tcl` PASS=1.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-RESEARCH-rep-infra.md
@vmd/lib/game.tcl
@vmd/lib/rep_tiers.tcl
@vmd/lib/hiders.tcl
@vmd/lib/mutation.tcl
@vmd/biochemeleon.tcl

Key facts (do NOT re-derive):
- Pinned upstream APIs (landed in waves 1-2; consume EXACTLY these):
  - `rep_tiers::scene_reps_to_per_rep {reps_list}` → presence dict (GAME_REPS filter + `resname GAM` sentinel-skip + dedup)
  - `rep_tiers::resolve_per_rep {hider_count per_rep detected lock_scene {seed {}}}` → effective per_rep (GAME_REPS-ordered; drop-overflow clamped; empty → randomize_per_rep over allowed)
  - `rep_tiers::tiers_from_per_rep {per_rep}` → ordered `{code style count}` triples (1-based codes)
  - `rep_tiers::effective_total {per_rep}` → int sum; `rep_tiers::tier_kind {rep}` → free|bonded|""; `rep_tiers::style_args {rep}` → modstyle arg list (`{DynamicBonds 1.6}` for DynamicBonds)
  - `registry::assign_reps {mapping}` (atomic, errors on unregistered idx); `hiders::stamp_tier_codes {molid tier_of}`; `hiders::add_hider_reps {molid tier_specs}` (specs = `{code styleArgs...}`); `mutation::make_bonded_hiders {molid count {occupied_hiders}}` → 5-field records; `mutation::make_placeholder_hiders {molid count}` → 5-field records (element C)
- Ordering is NON-NEGOTIABLE (15/16 precedent + 17.1 research RQ3.2/RQ4.3, one adjustment): guard → snapshot → resolve per_rep → per-tier placement (records) → ONE mutate → backup::apply → stamp_tier_codes → add_hider_reps → registry reconstruct + assign_reps. Stamp BEFORE add: a static single-frame molecule never re-evaluates cached rep selections on an atom-field change — a rep added before the user3 stamp would cache an empty selection.
- tier→indices split WITHOUT extra molinfo: fetch_hider_indices returns file order == record order; records are concatenated in tier order; tier k covers the sorted index slice at its cumulative record offsets. Use ACTUAL record counts (a tier may under-generate; make_bonded_hiders caps at anchor count).
- P9: explicit per_rep REPLACES the total (no top-up) — v1 semantics. game_state hider_count = effective_total(per_rep). win_cb reads game_state hider_count; the win line uses registry::count_hiders (actual) — under-generation tolerable and warned.
- P8: ONE reconstruct (1-arg — no rep) + ONE assign_reps. NEVER reconstruct per tier (clears prior tiers).
- DI rule (lesson a): `[list apply {{molid} {...}} $game_molid]` is a COMMAND-PREFIX VALUE — NEVER bare `[apply ...]`.
- game_state shape: `{game_molid hider_count snapshot per_rep}` — additive 4th key on the 15-05 frozen shape; update start_game's doc comment. cleanup/restart read via dict get/exists (defensive `dict exists` for per_rep/lock_scene in restart).
- Lock-scene derivation runs INSIDE start_game AFTER the 16-13 active-game guard + snapshot (the guard's cleanup restored the original first, so the snapshot is structurally free of game reps; the sentinel filter is defense-in-depth inside rep_tiers).
- Tcl 8.5; game.tcl sources NOTHING (call-time resolution — the ENTRY must source rep_tiers.tcl before game.tcl's first CALL); one variable per line; brace expr.
- Entry edit (biochemeleon.tcl): add `source [file join $_dir lib rep_tiers.tcl]` in the pure block after game_logic.tcl with a one-line Phase-17.1 comment. Mirrors the existing ordering comment.
- Staging + smoke conventions per 17.1-04 (BCHM_SMOKE_RESULT; full-log scan; lib source order now: setup_state, registry, rep_tiers, generators, game_logic, demos, backup, mutation, hiders, game).
- KNOWN RED after this plan (fixed by 17.1-07, do NOT fix here): phase16_smoke/phase16_hiders_smoke/phase16_onpick_smoke/phase16_restart_smoke/phase15_game_smoke/phase15_smoke assert VDW-only numreps(+2)/selection strings, PLUS phase16_entry_smoke (post_reps == pre_reps + 2 over a 2-arg start_game → flaky-red under randomize-across-tiers) and phase16_placement_smoke (bbox ±0.001 assert breaks if a randomized round lands bonded-tier hiders, which anchor-relative placement can push past the bbox edge) — 2-arg start_game now randomizes across IMPLEMENTED_TIERS, so multi-tier rounds break their assumptions. This plan's smoke is the green gate for the dispatch itself.
</context>

<tasks>

<task type="auto">
  <name>Task 1: start_game per-tier dispatch + restart symmetry + entry source line</name>
  <files>vmd/lib/game.tcl, vmd/biochemeleon.tcl</files>
  <action>
1. `proc ::biochemeleon::game::start_game {molid hider_count {per_rep {}} {lock_scene 0}}` — keep the 16-13 active-game guard byte-identical. After the guard:
   a. `set snapshot [::biochemeleon::backup::snapshot $molid]` (unchanged step 1).
   b. `set detected [::biochemeleon::rep_tiers::scene_reps_to_per_rep [dict get $snapshot reps]]`.
   c. `set per_rep [::biochemeleon::rep_tiers::resolve_per_rep $hider_count $per_rep $detected $lock_scene]`.
   d. `set eff_total [::biochemeleon::rep_tiers::effective_total $per_rep]`.
   e. Warn (non-blocking, v1 parity) for each requested-but-unimplemented key: compare the CALLER's per_rep keys against implemented — `foreach r [dict keys $per_rep_in] { if {"" eq [::biochemeleon::rep_tiers::tier_kind $r]} { catch {vmdcon -warn "bioCHEMeleon: rep '$r' has no generator yet -- dropped from this round"} } }` (capture per_rep_in BEFORE resolve; guard non-dict input with catch).
   f. Build the tier table: `set tiers [::biochemeleon::rep_tiers::tiers_from_per_rep $per_rep]`.
   g. Per-tier placement loop (GAME_REPS order): for each `{code style count}`: `set kind [::biochemeleon::rep_tiers::tier_kind $style]`; if kind eq "free" → `set recs [::biochemeleon::mutation::make_placeholder_hiders $molid $count]`; if "bonded" → `set recs [::biochemeleon::mutation::make_bonded_hiders $molid $count $occ_hiders]` where `$occ_hiders` accumulates ALL previously-placed hider positions across tiers this round (list of {x y z} — extract from prior recs); else (empty kind — defense) skip with the warning. Append recs to a flat records list; track per-tier ACTUAL record counts in a list parallel to $tiers.
   h. `set game_molid [::biochemeleon::mutation::mutate $molid $records]` (ONE call).
   i. `::biochemeleon::backup::apply $snapshot $game_molid` (unchanged).
   j. Split the indices — TWO explicit steps: (1) the UNCHANGED 1-arg registry call (the DI command-prefix form, byte-preserved from Phase 15/16): `::biochemeleon::registry::reconstruct_from_sentinels [list apply {{molid} { set sel [atomselect $molid "resname GAM and beta < 0"]; set ids [$sel get index]; $sel delete; return $ids }} $game_molid]`; then (2) the split list: `set idxs [::biochemeleon::mutation::fetch_hider_indices $game_molid]` (same canonical selector; file order == record order). Never assign the DI command prefix itself to $idxs — it is a VALUE for reconstruct's `{*}` expansion, not an index list.
   k. Build `tier_of` (dict code → index list) by slicing $idxs with the cumulative ACTUAL counts; build `idx_to_rep` (dict index → GAME_REPS style name) in the same loop.
   l. `::biochemeleon::hiders::stamp_tier_codes $game_molid $tier_of` (BEFORE add).
   m. Build tier_specs: `foreach t $tiers { lassign $t code style count; lappend specs [list $code {*}[::biochemeleon::rep_tiers::style_args $style]] }`; `::biochemeleon::hiders::add_hider_reps $game_molid $specs`.
   n. `::biochemeleon::registry::assign_reps $idx_to_rep`.
   o. game_state: `dict create game_molid $game_molid hider_count $eff_total snapshot $snapshot per_rep $per_rep`; stash + return (update the proc doc comment: new signature, ordering, P9 note, additive per_rep key).
2. `restart {game_state}` — read hider_count as today; read per_rep + lock_scene defensively (`if {[dict exists $game_state per_rep]} {...} else {set per_rep {}}` etc.) and pass all four args to start_game. MOOTNESS NOTE (must_haves truth): game_state has NO lock_scene key (4-key shape) — the defensive lock_scene read always yields 0, which is harmless because the stashed per_rep already encodes the resolved lock-scene restriction (resolve_per_rep keeps an explicit per_rep verbatim when lock_scene=0).
3. `cleanup` — unchanged.
4. Entry (`vmd/biochemeleon.tcl`): add the rep_tiers.tcl source line in the pure block (after game_logic) + extend the ordering comment.
Tcl 8.5 everywhere; keep every existing comment block that is still true; update the ones that changed.
  </action>
  <verify>`tclsh` cannot load game.tcl standalone (it sources nothing — syntax-check via the smoke instead; skip if tclsh unavailable — the smoke covers it); 8.6 gate zero on both files; `grep -n "list apply" vmd/lib/game.tcl` still shows the COMMAND-PREFIX DI form; call-site count only: `grep -c "::biochemeleon::registry::reconstruct_from_sentinels \[list apply" vmd/lib/game.tcl` == 1 (P8 — the raw-string count is 3+: doc comments L87/L150 + the call; this is the exact 16-13 miscount lesson from STATE.md — assert the CALL LINE, never the substring count).</verify>
  <done>start_game dispatches per-tier with lock-scene/randomize resolution, effective hider_count, stamp-before-add ordering, one reconstruct + one assign_reps; restart symmetric; entry sources rep_tiers.</done>
</task>

<task type="auto">
  <name>Task 2: Multi-tier dispatch smoke</name>
  <files>vmd/smoke/phase17_dispatch_smoke.tcl</files>
  <action>
Harness per context (source order includes rep_tiers + game). Steps on demo 1znf (424 atoms):
1. `set gs [game::start_game $molid 4 [dict create VDW 2 Lines 2] 0]` → assert game_molid atoms == 428; hider idxs == {424 425 426 427}; `dict get $gs hider_count` == 4 (effective); `dict exists $gs per_rep` and its value == `{VDW 2 Lines 2}`.
2. Tier stamps (numeric compare): user3 ≈ 1.0 for the first TWO hider indices, ≈ 2.0 for the last TWO (VDW tier first? NO — tier order is GAME_REPS order: Lines precedes VDW → code 1 = Lines (indices 424-425), code 2 = VDW (426-427). Assert THAT mapping via the tier table: recompute the expected order from rep_tiers::tiers_from_per_rep and assert user3 accordingly — do not hard-code blindly, derive then assert).
3. numreps == scene-base + 4 (capture scene numreps pre-start via the snapshot count: assert `[molinfo $gm get numreps]` == `[llength [dict get $snapshot-reps ...]]`... simpler: assert numreps == pre_start_numreps + 4 where pre_start_numreps was captured on the ORIGINAL molecule before start_game).
4. Rep read-back (COMBINED-braces) per pair via `mol repindex` on the tier_reps names: Lines pair style == `Lines`, selections end with `and user3 1`; VDW pair style == `VDW`, `and user3 2`; hidden color Element, found color `ColorID 7`.
5. Registry: count_hiders == 4; remaining_by_rep == dict with Lines 2 + VDW 2 (order-insensitive dict compare by key lookups).
6. `hiders::mark_found_visual $gm 426` (a VDW hider) → VDW-tier hidden selection count == 1, found == 1; Lines tier untouched (hidden 2 / found 0); remaining_by_rep → Lines 2 VDW 1.
7. Cleanup/restore: `game::cleanup $gs` → registry count 0; game molid deleted (catch molinfo errors); restored molecule numreps == the ORIGINAL's pre-start numreps (no game reps leak).
8. Backward-compat containment: fresh load 1k8p; `set gs2 [game::start_game $m2 5]` (2-arg) → assert registry count == 5; every key of remaining_by_rep ∈ rep_tiers IMPLEMENTED_TIERS; sum of remaining_by_rep values == 5 (randomize distributes the full total); cleanup gs2.
9. Marker + exit.
  </action>
  <verify>Staged run: `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log.</verify>
  <done>Multi-tier dispatch, tier ordering (GAME_REPS order), user3 stamps, registry per-tier counts, per-tier find partitions, effective total, cleanup restore, and 2-arg randomize containment all proven end-to-end.</done>
</task>

</tasks>

<verification>
- `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log
- Tcl 8.6 gate zero on all touched files
- P8/P9 honored: exactly one reconstruct call; hider_count == effective total
- DI form: `[list apply ...]` command prefix (never bare `[apply ...]`)
- Known-red legacy smokes documented for 17.1-07 (not touched here)
</verification>

<success_criteria>
- ROADMAP SC2 mechanics proven headlessly: lock-scene derivation + per-tier matching generation + randomize across implemented tiers
- Any 17.1-08..12 tier smoke can run a full round through this dispatch
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-06-SUMMARY.md`
</output>
