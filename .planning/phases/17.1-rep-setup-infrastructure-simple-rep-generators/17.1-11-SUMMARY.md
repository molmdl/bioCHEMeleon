---
phase: 17.1-rep-setup-infrastructure-simple-rep-generators
plan: 11
subsystem: testing
tags: [vmd, tcl, tachyon, cpk, smoke, headless, render-proxy, scale-ratio]

# Dependency graph
requires:
  - phase: 17.1-06
    provides: start_game 4-arg per-tier dispatch composition root (game.tcl)
  - phase: 17.1-08
    provides: the Tachyon pixel-proxy template (points_smoke structure, _parse_dat, element-color/salmon pins, staging + full-log-scan harness)
provides:
  - "CPK-tier end-to-end proof through the real 17.1-06 dispatch (pre-styled-scene variant): scaled balls, in-scene CPK blend, exact read-back, per-tier partition, style-faithful restore"
  - "The 0.25x CPK scale-ratio check pinned headlessly (UG node62 'too-small radii are not drawn' guard): mean(CPK Rad)/mean(VDW Rad) = 0.25000, per-atom byte-exact 0.25x"
  - "CORRECTED element pins for any future consumer: VMD 1.9.3 element P radius = 1.80 (not 1.55) and Element-P tan renders {0.5 0.5 0.2} (not ochre's {0.5 0.5 0.31})"
affects: [17.1-12 DynamicBonds smoke, 17.1-13/14 GUI checkpoint, 17.2 cartoon generators]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pre-styled-scene smoke pattern: mol modstyle 0 <m> <style> BEFORE start_game -- the snapshot carries the user's styled scene through backup::apply AND cleanup restore; scene-diff runs against the styled rep"
    - "Scale-ratio render check: re-style ONE probe rep VDW -> CPK over the same selection and compare mean sphere Rads -- view-scale/zoom cancels in the ratio, so no absolute Rad pin is needed"
    - "Render-parse purity guard: FCylinder primitives also carry Rad lines -- assert cyl count == 0 before treating a .dat rads list as sphere radii"

key-files:
  created:
    - vmd/smoke/phase17_cpk_smoke.tcl
  modified: []

key-decisions:
  - "Demo 1k8p (per plan, not 1znf): a DNA demo WITH 22 P atoms, so the tan element-color pin is LIVE instead of inert -- which immediately exposed two stale 17.1-08 pins"
  - "Scale checked as a RATIO on one re-styled probe rep (VDW -> CPK, same selection), window [0.20, 0.30]; observed 0.25000 with per-atom byte-exact 0.25x (0.0675211->0.0168803 C, 0.0603718->0.015093 O at view scale ~0.715)"
  - "No Points dot-radius bound reused for CPK hider spheres: CPK Rad ~0.024 sits ABOVE the 0.02 dot bound; the scale is owned by the ratio check"
  - "17.1-10's licorice smoke was unavailable (sibling wave worktree) -- built from the 17.1-08 template plus THIS plan's pinned pre-style mechanics; no sibling files read"

patterns-established:
  - "Pre-styled-scene round: pre-style + P-1 read-back BEFORE start_game; assert the style survives onto the game molecule (apply) and back onto the restored original (cleanup)"

# Metrics
duration: 16 min
completed: 2026-09-04
---

# Phase 17.1 Plan 11: CPK-Tier End-to-End Smoke Summary

**CPK tier proven end-to-end through the real dispatch on 1k8p: hiders render CPK-scaled spheres at a measured 0.25000 ratio of VDW radii (per-atom byte-exact 0.25x) with 0 FCylinders hiders-only and +3 spheres / +>=3 stub cylinders in a pre-styled CPK scene -- plus two corrected element-table pins (P radius 1.80, tan RGB 0.5 0.5 0.2) the P-bearing demo flushed out.**

## Performance

- **Duration:** 16 min
- **Started:** 2026-09-04T18:23:28Z
- **Completed:** 2026-09-04T18:39:37Z
- **Tasks:** 1
- **Files modified:** 1 (created)

## Accomplishments
- `vmd/smoke/phase17_cpk_smoke.tcl` PASS=1 on 3/3 runs after the pin fix (plus 1 pre-fix run), zero `ERROR)` / `bad switch` / error-class lines, "Exiting normally" every run
- The UG node62 guard is proven at two levels: 3 spheres parsed in the hiders-only render (the 0.25x radii ARE drawn) and the ratio check pins the scale itself at 0.25x VDW
- Pre-styled-scene contract proven end-to-end: rep-0 CPK read-back before start_game, on the game molecule after backup::apply, and on the restored original after cleanup (style-faithful restore)

## Task Commits

Each task was committed atomically:

1. **Task 1: Write + run the CPK-tier end-to-end smoke** - `d94d532` (test)

**Plan metadata:** (this commit)

## Files Created/Modified
- `vmd/smoke/phase17_cpk_smoke.tcl` - CPK-tier end-to-end dispatch smoke (672 lines): pre-style + round + mimicry + rep read-back + 6 Tachyon renders (baseline / hiders-only / VDW-ratio / CPK-ratio / sceneA / sceneB) + found-partition + style-faithful cleanup restore

## Verification Results

- `BCHM_SMOKE_RESULT PASS=1 FAIL=none` -- 3/3 independent-PRNG runs (4 total: 1 PASS pre-fix, 1 stale-pin FAIL, then 3 PASS)
- Full-log scans per run: 0 `ERROR)`, 0 `bad switch`, 0 `no such variable` / `invalid command name` / `can't read` / `wrong # args`, 1x "Exiting normally"
- Scale ratio: mean(CPK)/mean(VDW) = 0.25000 (window [0.20, 0.30]); per-atom byte-exact 0.25x in every run
- Hiders-only render: 3 Sphere / 0 FCylinder; colors within 0.01 of the element set; no salmon
- Scene-diff (pre-styled CPK scene rep): A.sph - B.sph == 3 AND A.cyl - B.cyl >= 3
- Read-back: hidden/found style == `CPK` exactly, Element / ColorID 7, literal `user3 1` selections, numreps == pre+2
- Registry: `{CPK 3}` fresh -> `{CPK 2}` after marking 557; cleanup restores 0, game molid dead, 555-atom original, numreps == pre, rep-0 style `CPK`
- Tcl 8.6 gate zero on the smoke; no lib files touched

## Decisions Made
- Demo 1k8p per plan (P-bearing DNA) so the tan pin is exercised; it immediately paid off (see deviations)
- Scale ratio via ONE probe rep re-styled VDW -> CPK over the same selection -- zoom/view-scale cancels, no absolute Rad pin needed (observed view scale ~0.715 from the restored viewpoint)
- CPK hider spheres get NO dot-radius bound (unlike Points/Lines dots at < 0.02): CPK ~0.024 is above it, and the ratio check owns the scale fact
- Built from the 17.1-08 template + this plan's pinned pre-style mechanics (17.1-10's file lives in a sibling wave worktree and was NOT read -- wave-disjointness)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Two stale inert element pins copied from 17.1-08 (P radius, tan RGB)**
- **Found during:** Task 1, first-batch run 2 (a P-anchored hider was drawn -- ~11% chance per round on 1k8p)
- **Issue:** (a) `mimic_rad_555` failed: hider element P read radius 1.7999999523162842 vs the pinned 1.55; (b) a parsed hider color {0.5 0.5 0.2} was rejected vs the pinned tan {0.5 0.5 0.31}. Both 17.1-08 pins were INERT on P-free 1znf and never validated. The LIB behaved correctly throughout: the hider perfectly mimicked real P atoms (the molecule-ground-truth check passed), and Element-P resolved to tan in the render.
- **Fix:** Headless probe on the real binary confirmed element P radius == 1.80 (Bondi vdW; N re-confirmed 1.55) and `color Element P` -> tan. Re-pinned `vdw_rad` P 1.55 -> 1.80 and tan {0.5 0.5 0.31} -> render-observed {0.5 0.5 0.2} (0.31 was ochre's RGB), with discovery comments in the smoke.
- **Files modified:** vmd/smoke/phase17_cpk_smoke.tcl (constants + comments only)
- **Verification:** 3/3 fresh-PRNG runs PASS=1 after the fix
- **Committed in:** d94d532

---

**Total deviations:** 1 auto-fixed (1 bug -- stale smoke constants, not lib behavior)
**Impact on plan:** The fix corrects the smoke's own reference tables; downstream consumers of element pins (17.1-12, GUI checkpoint, any future radius/color assert) should use P=1.80 / tan={0.5 0.5 0.2}. No scope creep, no lib changes.

## Issues Encountered
- Plan context references `vmd/smoke/phase17_licorice_smoke.tcl` (17.1-10), which does not exist in this worktree -- 17.1-10 runs in a sibling wave worktree by design. Resolved by building from the 17.1-08 template plus the plan's explicitly pinned pre-style mechanics ("mol modstyle 0 $m CPK pre-start", scene rep CPK in the diff); no sibling files were read.
- A headless probe initially hung the shell: a heredoc path typo pointed `vmd -e` at a missing script (VMD waits instead of erroring). Rewritten via the write tool at a correct relative path.
- "Unusual bond between residues" warnings appear as normal bonded-round noise (17.1-08 pinned fact); zero BCHM_BONDED_RELAX lines fired on this demo's draws (DNA anchors less crowded than 1znf's protein sp3-H anchors).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Wave 5 complete for this plan; 17.1-12 (DynamicBonds, the last simple-tier smoke) can copy this file's structure -- note DynamicBonds carries style args `DynamicBonds 1.6` (the only multi-token read-back style) and the explicit-cutoff rule
- **Carry-forward for ALL consumers of element pins:** VMD 1.9.3 element P radius = 1.80 and Element-P tan = {0.5 0.5 0.2}; the 17.1-08 points smoke still carries the old inert values (its demo 1znf cannot expose them -- leave or align at the 17.1-13/14 checkpoint)
- Standing blocker unchanged from 17.1-07: `vmd/tests/pick_verify.tcl` still reads the removed hidden_rep/found_rep vars (breaks the NEXT GUI session; update to tier_reps/repindex first)

---
*Phase: 17.1-rep-setup-infrastructure-simple-rep-generators*
*Completed: 2026-09-04*
