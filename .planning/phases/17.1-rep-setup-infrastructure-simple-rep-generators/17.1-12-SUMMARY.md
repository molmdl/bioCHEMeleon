---
phase: 17.1-rep-setup-infrastructure-simple-rep-generators
plan: 12
subsystem: testing
tags: [vmd, tcl, tcl85, dynamicbonds, tachyon, headless-smoke, explicit-cutoff, spurious-bonds, tier-dispatch]

# Dependency graph
requires:
  - phase: 17.1 (17.1-06)
    provides: the per-tier dispatch composition root (4-arg start_game, per-tier placement, 2N rep pairs, 4-key game_state)
  - phase: 17.1 (17.1-08)
    provides: the Points-tier end-to-end smoke template (harness, Tachyon pixel-proxy pattern, pinned constants)
  - phase: 17.1 (17.1-05)
    provides: make_bonded_hiders + the nearest-real-atom <= 1.61 / relaxed-accept evidence the offset assert reuses
  - phase: 17.1 (17.1-01)
    provides: rep_tiers::style_args (DynamicBonds -> {DynamicBonds 1.6})
provides:
  - vmd/smoke/phase17_dynbonds_smoke.tcl — the DynamicBonds-tier end-to-end proof: explicit-cutoff read-back ("DynamicBonds 1.6" EXACT), offset-vs-cutoff constraint, hiders-only 0-FCylinder render, pre-styled-scene stub diff, bare-name registry key, per-tier found partition, cleanup restore with rep-0 style round-trip
  - Evidence-pinned correction of the hiders-only DynamicBonds render model (bonded hiders are geometrically silent; dot rule = no partner within cutoff among ALL atoms)
  - Scale-safe Tachyon cylinder-radius class band (absolute export radii are scene-scale-dependent)
affects: [17.1-13 smoke-inventory update, 17.2 cartoon tiers (Tachyon template reuse), Phase 18 materials, GUI human-verify sessions]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Multi-word style read-back validation: expected string derived from style_args + cross-checked against the lib's tier_styles entry — never hand-typed"
    - "Offset-vs-cutoff three-way assert (copy of 17.1-05): nearest-real > 1.61 bail / >= 1.19 full-sep log / else relaxed log + non-degenerate"
    - "Scale-safe Tachyon primitive-class pinning: uniformity + magnitude band instead of absolute export radii (scene-scale-dependent)"
    - "Selection-liveness discriminator sub-render (Points on the same selection) when a style legitimately draws 0 primitives"

key-files:
  created:
    - vmd/smoke/phase17_dynbonds_smoke.tcl
  modified: []

key-decisions:
  - "Hiders-only DynamicBonds render asserted 0 Sphere AND 0 FCylinder (plan said 3 dots) — p6_geom's own data (2 lone + 2 bonded hiders -> exactly 2 spheres) proves the dot rule keys on having a within-cutoff partner among ALL atoms; a dot would indicate an anchor-less hider (offset violation). A Points discriminator sub-render on the same GAM selection proves the selection was live"
  - "Scene cylinder-radius assert is the scale-safe class band (uniform, 0.005-0.05) — the p6 absolute 0.00991189 is scene-scale-dependent (0.0175322 uniform on the 427-atom 1znf scene)"
  - "Offset assert kept the 17.1-05 proven three-way form (<= 1.61 upper for ALL) instead of the plan's literal [1.19, 1.60] read-back band — %8.3f rounding makes a strict 1.60 read-back upper bound false-fail-prone"

patterns-established:
  - "Multi-word style read-back: derive expected from rep_tiers::style_args, cross-check tier_styles, string-compare read-back"
  - "Relaxed-accept logging tags BCHM_DYNBONDS / BCHM_DYNBONDS_RELAX (mirrors 17.1-05's BCHM_BONDED family)"

# Metrics
duration: 29min
completed: 2026-09-04
---

# Phase 17.1 Plan 12: DynamicBonds-Tier End-to-End Smoke Summary

**DynamicBonds-tier end-to-end proof through the real 17.1-06 dispatch: explicit-cutoff read-back `DynamicBonds 1.6` exact, hider offsets strictly inside the 1.6 cutoff, hiders-only rep geometrically silent (0 FCylinder), pre-styled scene draws the stubs — PASS=1 3/3 runs, 0 ERROR)/bad switch, zero lib changes**

## Performance

- **Duration:** 29 min
- **Started:** 2026-09-04T19:25:50Z
- **Completed:** 2026-09-04T19:55:23Z
- **Tasks:** 1
- **Files modified:** 1 created

## Accomplishments
- The P-5 spurious-bond rule proven end-to-end: `{DynamicBonds 1.6}` flows style_args -> tier_specs -> mol modstyle; hidden/found read-back string-compares to `DynamicBonds 1.6` EXACTLY (the one style whose read-back is multi-word), derived from the pure layer and cross-checked against the lib's stored `tier_styles` entry — never hand-typed
- Offset-vs-cutoff constraint asserted per hider: nearest-real-atom <= 1.61 always (rounding-tolerant form of the strict < 1.6 drawn-offset contract), full-sep accepts >= 1.19, relaxed accepts logged (`BCHM_DYNBONDS_RELAX`) + non-degenerate — exercised across 3 passing runs with 1-3 relaxed draws each
- Hider-rep exclusive render: 0 FCylinder (no GAM-GAM pairs within the strict-< 1.6 cutoff) — plus the evidence-pinned 0 Sphere correction with a Points discriminator sub-render proving selection liveness (3 dots, Rad < 0.02, element colors, no salmon)
- Pre-styled-scene stub proof: scene rep 0 pre-styled to BARE DynamicBonds before start_game (user-scene parity); scene-diff (A all vs B not-GAM) A.cyl - B.cyl = 84 >= 3, A.sph - B.sph = 0, all 3802 scene cylinders uniform + bond-class
- Registry key proven BARE (`DynamicBonds`, not `DynamicBonds 1.6`) via exact key-set assert; found partition 426 -> 1 found / 2 hidden; remaining_by_rep 3 -> 2; cleanup restore incl. rep-0 style reading back `DynamicBonds` after the snapshot -> backup::apply round-trip

## Task Commits

Each task was committed atomically:

1. **Task 1: Write + run the DynamicBonds-tier end-to-end smoke** - `c1eb9e7` (test)

**Plan metadata:** (this commit)

## Files Created/Modified
- `vmd/smoke/phase17_dynbonds_smoke.tcl` - DynamicBonds-tier end-to-end smoke (799 lines): harness + round/mimicry/offset/read-back/renders/found/cleanup asserts, extended `_parse_dat` (per-primitive-class Rad attribution), Points discriminator sub-render

## Decisions Made
- **Hiders-only render model corrected (evidence-pinned):** the plan expected the hider-rep exclusive render to draw "3 dots"; run 1 drew 0 primitives. The researcher's own p6_geom artifacts resolve it: its GAM scene held 2 LONE hiders + 2 BONDED hiders + 4 backbone atoms and `p6_dynbonds_16.dat` exports exactly 2 spheres (the lone ones) — DynamicBonds' per-frame bond search runs over ALL atoms (selected or not); an atom with a within-cutoff partner is "bonded" -> no dot, and the both-endpoints rule then suppresses the cylinder. Every hider has its anchor < 1.6 (the offset invariant this very smoke asserts), so 0 Sphere is the CORRECT pin and is itself an offset-violation detector. The verify gate's load-bearing half (hider-rep render 0 FCylinders) is unchanged and asserted.
- **Scale-safe cylinder-radius pin:** the p6 absolute 0.00991189 bond radius is scene-scale-dependent (uniform 0.0175322 on this 427-atom scene — the Tachyon export scales with the scene). Replaced with the portable class assert: uniform across all cylinders (eps 1e-6) and inside (0.005, 0.05) — an order of magnitude above the 0.002 dot class.
- **Offset assert form:** kept 17.1-05's proven three-way read-back-safe form rather than the plan's literal [1.19, 1.60] band (a strict 1.60 read-back upper bound false-fails when a true 1.5998 A draw rounds up through %8.3f; the orchestrator context pinned "nearest-real-atom <= 1.61").

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Hiders-only DynamicBonds render expectation corrected: 3 dots -> 0 primitives + Points discriminator**
- **Found during:** Task 1 (run 1: `hider_dots:exp=3 got=0`)
- **Issue:** The plan's render (b) expected "sphere-dot count == 3" for the hiders-only `{DynamicBonds 1.6}` rep; actual behavior draws nothing for bonded hiders (dynamic search over ALL atoms finds the within-1.6 anchor -> "bonded"; unselected anchor -> both-endpoints rule suppresses the cylinder). The plan's 3-dots model, if it had held, would actually indicate anchor-less hiders.
- **Fix:** Assert 0 FCylinder AND 0 Sphere with the evidence trail in comments; added a (b2) Points discriminator sub-render (same probe rep re-styled via `style_args Points`, same GAM selection -> exactly 3 dots, Rad < 0.02, element colors, no salmon) proving the (b) selection was live so 0/0 is genuinely the style behavior.
- **Files modified:** vmd/smoke/phase17_dynbonds_smoke.tcl
- **Verification:** runs 2-4 PASS=1; discriminator asserts 3 dots every run
- **Committed in:** c1eb9e7

**2. [Rule 1 - Bug] Scene cylinder-radius absolute pin replaced with a scale-safe class band**
- **Found during:** Task 1 (run 1: `scene_cyl_rad:cylinder rad=0.0175322 not within 0.001 of the p6-pinned 0.00991189`)
- **Issue:** The supplementary absolute radius pin (my addition, modeled on the p6 probe value) is scene-scale-dependent: Tachyon export radii scale with the scene (2-atom p6 scene -> 0.00991189; 427-atom 1znf scene -> 0.0175322, uniform across all 3802 cylinders).
- **Fix:** Assert uniformity (max - min <= 1e-6) + bond-class band (0.005, 0.05); also fixed a latent message bug in the scene sphere-rad bail (`$r` -> `$sr`).
- **Files modified:** vmd/smoke/phase17_dynbonds_smoke.tcl
- **Verification:** runs 2-4 PASS=1 with uniform 0.0175322 cylinders accepted
- **Committed in:** c1eb9e7

---

**Total deviations:** 2 auto-fixed (2 bugs — both plan-expectation corrections backed by the researcher's own p6 probe artifacts)
**Impact on plan:** The verify gate's load-bearing asserts (read-back == `DynamicBonds 1.6`, hider-rep 0 FCylinders, offsets vs cutoff, scene-diff stubs, bare registry key, partition, restore) are all asserted as planned; the two corrections align the remaining asserts with pinned evidence. No lib files touched.

## Issues Encountered
- Run 1 failed on the two asserts above; root-caused against `tmp/p17-research-d/p6_geom.tcl` + `p6_dynbonds_16.dat`/`p6_dynbonds_bare.dat` (researcher's own artifacts, still on disk in the main checkout) — no re-derivation, no lib changes.
- Relaxation fired on 0-3 of 3 hiders per run (0.34-1.42 A nearest-real across runs) — normal per 17.1-05; the three-way assert absorbed every draw without flaking.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- All five simple-tier smokes now exist (Points 17.1-08, Lines 17.1-09, Licorice 17.1-10, CPK 17.1-11, DynamicBonds 17.1-12); 17.1-13's smoke-inventory update can register phase17_dynbonds_smoke alongside them.
- For any future Tachyon-based smoke: absolute export radii are scene-scale-dependent — pin classes (uniformity + magnitude bands), never absolute values copied across scenes.
- The hiders-only DynamicBonds rep is geometrically silent for bonded hiders (blend visibility comes entirely from the scene rep's stubs) — relevant for Phase 18 materials and GUI blend checks: do not expect hider-rep geometry in isolation.
- Carry-forward for the next GUI session unchanged: pick_verify.tcl still reads the dead hidden_rep/found_rep vars (17.1-07 note).

---
*Phase: 17.1-rep-setup-infrastructure-simple-rep-generators*
*Completed: 2026-09-04*
