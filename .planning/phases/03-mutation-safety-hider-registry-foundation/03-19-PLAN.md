---
phase: 03-mutation-safety-hider-registry-foundation
plan: 19
type: execute
wave: 11
depends_on: ["03-15", "03-16", "03-17", "03-18"]
files_modified:
  - .planning/phases/03-mutation-safety-hider-registry-foundation/03-VERIFICATION.md
autonomous: true

must_haves:
  truths:
    - "Hider atoms can be inserted INTO an existing object — the PyMOL object list (public_objects) is unchanged after insertion"
    - "Every inserted hider carries segi='GAME' + b=-999 sentinel and is recorded in a HiderRegistry keyed by atom id"
    - "The registry can be queried for all hiders (id, rep, status) and per-rep counts"
    - "After removing all hiders by sentinel (or restoring from backup), the object's atom count and structure match its pre-game state exactly"
  artifacts:
    - path: "biochemeleon/registry.py"
      provides: "HiderRegistry + HiderRecord (pure, WSL-testable)"
    - path: "biochemeleon/backup.py"
      provides: "snapshot/restore/discard/verify_intact (cmd-coupled)"
    - path: "biochemeleon/mutation.py"
      provides: "insert_hider/fetch_all_hider_ids/cleanup_hiders (cmd-coupled)"
    - path: "biochemeleon/game.py"
      provides: "GameController orchestrator (cmd-coupled)"
    - path: "tests/test_registry.py"
      provides: "Unit tests for the pure registry layer"
    - path: "smoke/phase3_smoke.py"
      provides: "Integration smoke test (run in Windows PyMOL)"
  key_links:
    - from: "biochemeleon/mutation.py"
      to: "biochemeleon/registry.py"
      via: "insert_hider returns id -> registry.register"
      pattern: "register\\(object=.*"
    - from: "biochemeleon/backup.py"
      to: "target object"
      via: "snapshot BEFORE any mutation"
      pattern: "snapshot"
    - from: "biochemeleon/mutation.py"
      to: "sentinel atoms"
      via: "cleanup_hiders removes by segi GAME"
      pattern: "cmd\\.remove.*segi GAME"
    - from: "biochemeleon/backup.py"
      to: "biochemeleon/game.py"
      via: "restore followed by verify_intact"
      pattern: "restore.*verify_intact"
    - from: "biochemeleon/registry.py"
      to: "injected iterate_fn"
      via: "reconstruct_from_sentinels(iterate_fn) — DI keeps registry pure"
      pattern: "reconstruct_from_sentinels"
    - from: "biochemeleon/game.py"
      to: "backup+mutation+registry"
      via: "start()/cleanup()/abort_on_error() orchestration"
      pattern: "backup\\.|mutation\\.|registry\\."
---

<objective>
Write the formal `03-VERIFICATION.md` — the criterion-by-criterion evidence record proving Phase 3's 4 success criteria are met, with the smoke-test results, spike findings, and the 12-gate regression results.

Purpose: VERIFICATION.md is the auditable proof that Phase 3 is complete. It maps each success criterion to the specific smoke-test check(s) that verify it, records the actual PASS/FAIL + spike values from the 03-15 checkpoint, and references the 03-18 regression-gate results. This is the artifact a reviewer (or future phase) reads to confirm Phase 3 is done.
Output: .planning/phases/03-mutation-safety-hider-registry-foundation/03-VERIFICATION.md
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mutation-safety-hider-registry-foundation/03-RESEARCH.md
@.planning/phases/03-mutation-safety-hider-registry-foundation/03-15-SUMMARY.md
@.planning/phases/03-mutation-safety-hider-registry-foundation/03-16-SUMMARY.md
@.planning/phases/03-mutation-safety-hider-registry-foundation/03-17-SUMMARY.md
@.planning/phases/03-mutation-safety-hider-registry-foundation/03-18-SUMMARY.md
@smoke/phase3_smoke.py
</context>

<tasks>

<task type="auto">
  <name>Write 03-VERIFICATION.md — criterion-by-criterion evidence</name>
  <files>.planning/phases/03-mutation-safety-hider-registry-foundation/03-VERIFICATION.md</files>
  <action>
Create .planning/phases/03-mutation-safety-hider-registry-foundation/03-VERIFICATION.md with this structure:

```markdown
# Phase 3: Mutation Safety & Hider Registry Foundation — Verification

**Verified:** 2026-08-05
**Smoke test:** smoke/phase3_smoke.py (run in Windows PyMOL via `pymol -cq`)
**Result:** ALL PASSED (or record failures)

## Success Criteria Evidence

### Criterion 1: Hider atoms inserted INTO an existing object — object list unchanged
**Status:** PASS (or record)
**Evidence:**
- Smoke check "C1: public object list unchanged" — PASS (public_objects set equals orig after insert)
- Smoke check "C1: count += 3" — PASS (count increased by exactly 3, no new object)
- Mechanism: `cmd.pseudoatom(object=existing)` inserts INTO the object (creating.py:1082); no new object created
- Q4 spike "existing ids stable across insert" — PASS (orig_ids ⊆ new_ids; new_ids - orig_ids == {3 new ids})

### Criterion 2: Every hider carries segi='GAME' + b=-999 sentinel + recorded in HiderRegistry keyed by id
**Status:** PASS (or record)
**Evidence:**
- Smoke check "C2: 3 sentinel atoms" — PASS (iterate segi GAME found exactly 3)
- Smoke check "C2: all segi=GAME and b=-999" — PASS (all 3 atoms have segi='GAME' + |b - (-999)| < 1e-6)
- Smoke check "C3: registry len == 3" — PASS (registry registered all 3 by id)
- Mechanism: insert_hider sets sentinel via cmd.alter (editing.py:1424); id fetched via cmd.identify(mode=0) (querying.py:1269); registry.register keys on (object, id)

### Criterion 3: Registry queryable for all hiders (id, rep, status) + per-rep counts
**Status:** PASS (or record)
**Evidence:**
- Smoke check "C3: registry len == 3" — PASS
- Smoke check "C3: per-rep counts" — PASS (counts_by_rep == {spheres:1, sticks:1, lines:1, cartoon:0, ribbon:0})
- Smoke check "C3: by_rep spheres len 1" — PASS
- WSL unit tests: tests/test_registry.py — TestHiderRegistryCore + TestHiderRegistryQueries pass (register/get/all/remove/by_rep/counts_by_rep/mark_found)

### Criterion 4: After cleanup (or restore), object matches pre-game state exactly
**Status:** PASS (or record)
**Evidence — happy path (cleanup by sentinel):**
- Smoke check "C4: cleanup returned True (intact)" — PASS (GameController.cleanup → verify_intact → True)
- Smoke check "C4: count back to orig" — PASS
- Smoke check "C4: id-set matches orig (Q4 spike)" — PASS
**Evidence — failure path (restore from backup):**
- Smoke check "failure-path abort returns True" — PASS (GameController.abort_on_error → backup.restore → True)
- Smoke check "failure-path: count back to orig" — PASS
- Mechanism: cleanup_hiders uses cmd.remove(segi GAME) (editing.py:800); restore uses cmd.delete + cmd.create (RESEARCH §Q2: never single-call create)

## Research Spike Findings (UNVERIFIED → RESOLVED)

### Q1: cmd.pseudoatom return value (id or status?)
**Finding:** [record from 03-15-SUMMARY: the actual return value + type]
**Resolution:** Code NEVER relies on it (uses cmd.identify after insert). Informational. Documented in AGENTS.md.

### Q2: cmd.create merge-vs-replace (single-call create(existing, backup))
**Finding:** [record from 03-15-SUMMARY: REPLACE or APPEND]
**Resolution:** restore uses delete+create (unambiguous) regardless. If APPEND confirmed, reinforces the recommendation. Documented in AGENTS.md + PITFALLS.md.

### Q2b: cmd.create id-preservation across copy
**Finding:** [record from 03-15-SUMMARY: id-set match or mismatch on restore]
**Resolution:** Affects only abort/restore path (registry rebuilt on next Start). Happy path preserves ids. Documented in PITFALLS.md.

### PSE: .pse round-trip id/sentinel stability
**Finding:** [record from 03-15-SUMMARY: id-stable or id-shifted; sentinel-survival PASS]
**Resolution:** Sentinel survives reload (HIGH confidence — load-bearing). If ids shifted, reconstruct_from_sentinels rebuilds registry from sentinels (rep=None; Phase 8 sidecar reconciles). Documented in AGENTS.md + PITFALLS.md.

## WSL Regression Gate Results (from plan 03-18)

[Record the 12-gate results from 03-18-SUMMARY: py_compile, unit tests, Pitfall-1/11 gates, registry purity, cmd-coupled counts, sentinel consistency, space= hygiene, architecture direction, orchestration]

## Requirements Coverage

- HIDER-01 (hiders inserted INTO the same object): ✓ Criterion 1
- HIDER-02 (segi='GAME' + b=-999 sentinel + tracked by id): ✓ Criterion 2
- HIDER-06 (generation records every hider's (object, atom-ID) in registry): ✓ Criterion 2 + 3 (GameController.start → registry.register)

## Conclusion

Phase 3 is VERIFIED: all 4 success criteria PASS, 3/3 requirements (HIDER-01/02/06) satisfied, 3 research spikes resolved, 12-gate regression clean. Ready for Phase 4.
```

Fill in every [record ...] placeholder from the 03-15/03-16/03-17/03-18 SUMMARYs (the actual smoke-test values + gate results). If the smoke test had any FAIL lines, record them honestly and note whether they're blocking (C1-C4) or informational (Q1/Q2/PSE spikes).
Commit: `docs(03-19): write Phase 3 VERIFICATION.md (criterion-by-criterion evidence + spike findings)`
  </action>
  <verify>rg -n "Criterion 1:|Criterion 2:|Criterion 3:|Criterion 4:" .planning/phases/03-mutation-safety-hider-registry-foundation/03-VERIFICATION.md  # 4 matches; rg -n "Q1:|Q2:|PSE:" .planning/phases/03-mutation-safety-hider-registry-foundation/03-VERIFICATION.md  # ≥3 matches; rg -n "HIDER-01|HIDER-02|HIDER-06" .planning/phases/03-mutation-safety-hider-registry-foundation/03-VERIFICATION.md  # 3 matches</verify>
  <done>03-VERIFICATION.md exists with all 4 criteria documented, 3 spike findings recorded, 12-gate results, 3/3 requirements mapped; all placeholders filled from actual smoke results</done>
</task>

</tasks>

<verification>
- rg -n "Criterion 1:|Criterion 2:|Criterion 3:|Criterion 4:" 03-VERIFICATION.md → 4 matches
- rg -n "Q1:|Q2:|PSE:" 03-VERIFICATION.md → ≥3 matches (spike findings)
- rg -n "HIDER-01|HIDER-02|HIDER-06" 03-VERIFICATION.md → 3 matches (requirements)
- No unfilled [record ...] placeholders remain (all filled from actual SUMMARYs)
- rg -n "PASS" 03-VERIFICATION.md → ≥8 matches (criteria + spikes mostly passed)
</verification>

<success_criteria>
- 03-VERIFICATION.md exists with criterion-by-criterion evidence (all 4 criteria)
- Each criterion maps to specific smoke-test checks + mechanism + result
- 3 spike findings (Q1/Q2/PSE) recorded with actual values from the smoke test
- 12-gate regression results documented
- 3/3 requirements (HIDER-01/02/06) mapped to criteria
- Conclusion: Phase 3 VERIFIED, ready for Phase 4
- No unfilled placeholders
</success_criteria>

<output>
After completion, create `.planning/phases/03-mutation-safety-hider-registry-foundation/03-19-SUMMARY.md`
</output>
