---
phase: 03-mutation-safety-hider-registry-foundation
plan: 15
type: execute
wave: 9
depends_on: ["03-14"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Hider atoms can be inserted INTO an existing object — the PyMOL object list (public_objects) is unchanged after insertion"
    - "Every inserted hider carries segi='GAME' + b=-999 sentinel and is recorded in a HiderRegistry keyed by atom id"
    - "The registry can be queried for all hiders (id, rep, status) and per-rep counts"
    - "After removing all hiders by sentinel (or restoring from backup), the object's atom count and structure match its pre-game state exactly"
  artifacts:
    - path: "biochemeleon/registry.py"
      provides: "HiderRegistry + HiderRecord (pure, WSL-testable)"
    - path: "biochemeleon/backup.py"
      provides: "snapshot/restore/discard/verify_intact (cmd-coupled)"
    - path: "biochemeleon/mutation.py"
      provides: "insert_hider/fetch_all_hider_ids/cleanup_hiders (cmd-coupled)"
    - path: "biochemeleon/game.py"
      provides: "GameController orchestrator (cmd-coupled)"
    - path: "tests/test_registry.py"
      provides: "Unit tests for the pure registry layer"
    - path: "smoke/phase3_smoke.py"
      provides: "Integration smoke test (run in Windows PyMOL)"
  key_links:
    - from: "biochemeleon/mutation.py"
      to: "biochemeleon/registry.py"
      via: "insert_hider returns id -> registry.register"
      pattern: "register\\(object=.*"
    - from: "biochemeleon/backup.py"
      to: "target object"
      via: "snapshot BEFORE any mutation"
      pattern: "snapshot"
    - from: "biochemeleon/mutation.py"
      to: "sentinel atoms"
      via: "cleanup_hiders removes by segi GAME"
      pattern: "cmd\\.remove.*segi GAME"
    - from: "biochemeleon/backup.py"
      to: "biochemeleon/game.py"
      via: "restore followed by verify_intact"
      pattern: "restore.*verify_intact"
    - from: "biochemeleon/registry.py"
      to: "injected iterate_fn"
      via: "reconstruct_from_sentinels(iterate_fn) — DI keeps registry pure"
      pattern: "reconstruct_from_sentinels"
    - from: "biochemeleon/game.py"
      to: "backup+mutation+registry"
      via: "start()/cleanup()/abort_on_error() orchestration"
      pattern: "backup\\.|mutation\\.|registry\\."
---

<objective>
Human-verify checkpoint: run the complete `smoke/phase3_smoke.py` in Windows PyMOL and triage the results. This is the FORMAL Phase 3 verification — the only place the cmd-coupled modules (backup.py, mutation.py, game.py) are exercised at runtime, since the WSL agent cannot run PyMOL.

Purpose: The WSL gate suite (py_compile + unit tests + grep gates) only verifies SYNTAX and pure-layer behavior. The 4 success criteria all depend on PyMOL runtime behavior (cmd.pseudoatom inserts INTO the object, sentinel is set, cleanup removes by sentinel, restore leaves structure intact). This checkpoint is where the human runs the script and confirms every PASS line + records the 3 spike findings (Q1/Q2/PSE) for the SUMMARY.
Output: User confirms ALL PASSED (or reports failures); spike findings recorded for plans 03-16/03-17/03-19.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mutation-safety-hider-registry-foundation/03-RESEARCH.md
@smoke/phase3_smoke.py
@AGENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 3 mutation-safety foundation: a pure-layer HiderRegistry (biochemeleon/registry.py, 10 methods, WSL-unit-tested), cmd-coupled backup.py (snapshot/restore/discard/verify_intact), cmd-coupled mutation.py (insert_hider/fetch_all_hider_ids/cleanup_hiders), and a thin orchestrator GameController (biochemeleon/game.py: start/cleanup/abort_on_error/reconstruct_registry). The complete smoke test script (smoke/phase3_smoke.py) exercises all 4 success criteria + the failure path + 3 research spikes (Q1 pseudoatom return value, Q2 create merge-vs-replace, PSE round-trip id/sentinel stability). WSL gates pass (py_compile + 90+ registry unit tests + grep Pitfall-1/11 gates green).
  </what-built>
  <how-to-verify>
BEFORE running: copy the biochemeleon/ package + smoke/ to the Windows side so PyMOL can import them.
1. From WSL: `bash wsl2win_cp.sh` (copies biochemeleon/ to tmp/bioCHEMeleon/).
2. Copy the smoke script to the Windows side: `cp smoke/phase3_smoke.py tmp/bioCHEMeleon/smoke/phase3_smoke.py` (create tmp/bioCHEMeleon/smoke/ if needed).
3. In Windows: open a cmd.exe, run `setenv.bat` (activates the chemtools-win10 conda env), then `cd` to the Windows path of `tmp/bioCHEMeleon/` (e.g., `C:\Users\...\tmp\bioCHEMeleon`).
4. Run the smoke test: `pymol -cq smoke\phase3_smoke.py`
   (OR: launch `pymol`, then in the PyMOL terminal: `run smoke\phase3_smoke.py`)

EXPECTED OUTPUT:
- Every line prints `PASS: ...` (approximately 20 PASS lines).
- Three spike lines print their values:
  - `Q1: cmd.pseudoatom return value = <value> (type <type>)` — informational (records whether it's an int id or a status code).
  - `Q2: single-call create is REPLACE (not append/double)` — should be PASS (if FAIL, it appended, confirming the delete+create recommendation — record and proceed).
  - `PSE: hider id stable across round-trip` — should be PASS (if FAIL, ids shifted on reload — sentinel survival is the load-bearing check; record and proceed).
- Final: `=== SUMMARY ===` then `N/N passed` then `ALL PASSED`, exit 0.

If ANY line prints `FAIL:`:
- Note the failing check name(s) and report them.
- The 4 criteria checks (C1/C2/C3/C4) are BLOCKING — if any fails, Phase 3 is not verified.
- The 3 spikes (Q1/Q2/PSE) are INFORMATIONAL — a FAIL records the finding but does not block (the code is safe either way per RESEARCH mitigations).

What to report back:
- "approved" if ALL PASSED (all criteria + spikes ran, summary shows N/N passed, exit 0).
- OR list the failing check names + the Q1/Q2/PSE spike values (even if PASS — record them for the SUMMARY).
  </how-to-verify>
  <resume-signal>Type "approved" or describe the failures + spike values</resume-signal>
</task>

</tasks>

<verification>
This plan's verification IS the checkpoint task above. The human runs the smoke test in Windows PyMOL and confirms:
- All 4 criteria checks PASS (C1 object-unchanged, C2 sentinel, C3 registry-queries, C4 cleanup-intact)
- Failure-path restore PASS (criterion 4 alternate)
- 3 spikes recorded (Q1 return value, Q2 merge-vs-replace, PSE id-stability)
- Summary: ALL PASSED, exit 0
</verification>

<success_criteria>
- Human runs `pymol -cq smoke\phase3_smoke.py` in Windows PyMOL (chemtools-win10 env via setenv.bat)
- All 4 success criteria checks print PASS
- Failure-path restore prints PASS (criterion 4 alternate: abort_on_error works)
- 3 spike findings recorded (Q1 return value type, Q2 replace-vs-append, PSE id-stable-or-not)
- Summary prints "ALL PASSED" and exits 0
- User types "approved" (or reports failures for gap closure)
</success_criteria>

<output>
After completion, create `.planning/phases/03-mutation-safety-hider-registry-foundation/03-15-SUMMARY.md`
(record the human's smoke-test results + the 3 spike findings here for plans 03-16/03-17/03-19)
</output>
