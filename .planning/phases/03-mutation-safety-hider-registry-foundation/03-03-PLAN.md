---
phase: 03-mutation-safety-hider-registry-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - biochemeleon/mutation.py
autonomous: true

must_haves:
  truths:
    - "Hider atoms can be inserted INTO an existing object — the PyMOL object list (public_objects) is unchanged after insertion"
    - "Every inserted hider carries segi='GAME' + b=-999 sentinel and is recorded in a HiderRegistry keyed by atom id"
    - "The registry can be queried for all hiders (id, rep, status) and per-rep counts"
    - "After removing all hiders by sentinel (or restoring from backup), the object's atom count and structure match its pre-game state exactly"
  artifacts:
    - path: "biochemeleon/registry.py"
      provides: "HiderRegistry + HiderRecord (pure, WSL-testable)"
    - path: "biochemeleon/backup.py"
      provides: "snapshot/restore/discard/verify_intact (cmd-coupled)"
    - path: "biochemeleon/mutation.py"
      provides: "insert_hider/fetch_all_hider_ids/cleanup_hiders (cmd-coupled)"
    - path: "biochemeleon/game.py"
      provides: "GameController orchestrator (cmd-coupled)"
    - path: "tests/test_registry.py"
      provides: "Unit tests for the pure registry layer"
    - path: "smoke/phase3_smoke.py"
      provides: "Integration smoke test (run in Windows PyMOL)"
  key_links:
    - from: "biochemeleon/mutation.py"
      to: "biochemeleon/registry.py"
      via: "insert_hider returns id -> registry.register"
      pattern: "register\\(object=.*"
    - from: "biochemeleon/backup.py"
      to: "target object"
      via: "snapshot BEFORE any mutation"
      pattern: "snapshot"
    - from: "biochemeleon/mutation.py"
      to: "sentinel atoms"
      via: "cleanup_hiders removes by segi GAME"
      pattern: "cmd\\.remove.*segi GAME"
    - from: "biochemeleon/backup.py"
      to: "biochemeleon/game.py"
      via: "restore followed by verify_intact"
      pattern: "restore.*verify_intact"
    - from: "biochemeleon/registry.py"
      to: "injected iterate_fn"
      via: "reconstruct_from_sentinels(iterate_fn) — DI keeps registry pure"
      pattern: "reconstruct_from_sentinels"
    - from: "biochemeleon/game.py"
      to: "backup+mutation+registry"
      via: "start()/cleanup()/abort_on_error() orchestration"
      pattern: "backup\\.|mutation\\.|registry\\."
---

<objective>
Create `biochemeleon/mutation.py` (cmd-coupled) with `insert_hider(object, pos, rep, handle)` — the core hider-insertion primitive.

Purpose: `insert_hider` is the single most critical operation in Phase 3 — it inserts a pseudoatom INTO an existing object (not a new object), sets the `segi='GAME' + b=-999` sentinel, and returns the new atom's stable `id` (fetched via `cmd.identify`, NEVER relying on `cmd.pseudoatom`'s return value — RESEARCH §Q1 confirms the return is an unverified C status code). This is the foundation for HIDER-01 and HIDER-02.
Output: `biochemeleon/mutation.py` with `insert_hider`, syntax-checked in WSL (runtime verified by the Phase 3 smoke test).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mutation-safety-hider-registry-foundation/03-RESEARCH.md
@biochemeleon/demos.py
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Create mutation.py with insert_hider</name>
  <files>biochemeleon/mutation.py</files>
  <action>
Create biochemeleon/mutation.py following 03-RESEARCH.md §"Backup/Restore Design" (lines 313-327) EXACTLY for the insert_hider function:
- Module docstring: "Hider insertion + sentinel + cleanup — cmd-coupled. insert_hider inserts a pseudoatom INTO an existing object, tags the GAME sentinel, and returns the new atom's stable id (fetched via cmd.identify, never the pseudoatom return value)."
- `from pymol import cmd`
- `def insert_hider(object, pos, rep, handle, segi='GAME', b=-999.0):`
  - Docstring: "Insert one hider pseudoatom INTO object. Returns the new atom's id (fetched via identify). The pseudoatom return value is an unverified C status code (RESEARCH §Q1) — NEVER rely on it."
  - `cmd.pseudoatom(object=object, pos=list(pos), name=handle, segi=segi, b=b, hetatm=1, elem='PS', resn='HIDER', chain='H', resi='9001')`  (creating.py:1082)
  - `cmd.alter(f"{object} and name {handle}", "segi='GAME'; b=-999.0", space={})`  (editing.py:1424; editor.py:354 multi-; idiom; space={} avoids global namespace pollution per RESEARCH §Q3)
  - `cmd.sort(object)`  (defensive — editing.py:1457 alter warning: sort after altering segi/chain)
  - `ids = cmd.identify(f"{object} and name {handle} and segi GAME", mode=0)`  (querying.py:1269; mode=0 returns id list, NOT index)
  - `assert len(ids) == 1, "expected 1 new hider id, got %r" % (ids,)`
  - `return ids[0]`
DO NOT add fetch_all_hider_ids or cleanup_hiders yet (plans 03-06, 03-09).
Note: `rep` is accepted as a parameter (the registry needs it) but insert_hider itself does NOT use rep for placement logic — placement is Phase 4/5 generator work. Phase 3's insert_hider places at the given `pos` (caller-supplied). This is intentional: Phase 3 proves the insertion mechanism, not the generator.
Commit: `feat(03-03): add mutation.py insert_hider (pseudoatom + sentinel + identify)`
  </action>
  <verify>python3.6 -m py_compile biochemeleon/mutation.py  # syntax OK; rg -n "from pymol" biochemeleon/mutation.py  # exactly ONE match; rg -n "cmd\.pseudoatom" biochemeleon/mutation.py  # one match; rg -n "cmd\.identify" biochemeleon/mutation.py  # one match; rg -n "cmd\.alter" biochemeleon/mutation.py  # one match</verify>
  <done>mutation.py exists with insert_hider; py_compile passes; contains cmd.pseudoatom(object=...) + cmd.alter(...,space={}) + cmd.sort + cmd.identify(...,mode=0) + assert + return ids[0]</done>
</task>

<task type="auto">
  <name>Run full gate suite (no regression)</name>
  <files>biochemeleon/mutation.py</files>
  <action>
Run the WSL gate suite:
- `python3.6 -m py_compile biochemeleon/*.py` (all compile — mutation.py's `from pymol import cmd` is syntax-only)
- `python3.6 -m unittest tests.test_setup_state -v` (90 existing tests pass)
- Pitfall-1 gate: `rg -n "import Tkinter|import tkinter|from tkinter|import Pmw|from Pmw|app\.root|grab_set|mainloop|Toplevel|menuBar\.addmenuitem|from PyQt5 import|import PyQt5" biochemeleon/` → ZERO matches
- Pitfall-11 gate: `rg -n "\.exec_\(\)" biochemeleon/` → ZERO matches
- Sentinel gate: `rg -n "segi='GAME'|segi=\"GAME\"" biochemeleon/mutation.py` → at least one match (sentinel is set)
- space= gate: `rg -n "space=" biochemeleon/mutation.py` → at least one match (hygienic iterate/alter)
No commit needed (gate run only).
  </action>
  <verify>python3.6 -m py_compile biochemeleon/*.py && python3.6 -m unittest tests.test_setup_state 2>&1 | tail -3  # "Ran 90 tests" + "OK"</verify>
  <done>all modules compile, 90 existing tests pass, grep gates green, sentinel + space= patterns present</done>
</task>

</tasks>

<verification>
- python3.6 -m py_compile biochemeleon/mutation.py → no syntax errors
- python3.6 -m py_compile biochemeleon/*.py → all modules compile
- python3.6 -m unittest tests.test_setup_state -v → 90 tests pass (no regression)
- rg -n "from pymol" biochemeleon/mutation.py → exactly 1 match (the import)
- rg -n "cmd\.pseudoatom\(object=" biochemeleon/mutation.py → 1 match (inserts INTO existing object, not cmd.load/cmd.create)
- rg -n "cmd\.identify.*mode=0" biochemeleon/mutation.py → 1 match (id fetch, not return value)
- rg -n "space=" biochemeleon/mutation.py → ≥1 match (hygienic alter)
- rg Pitfall-1 gate → ZERO matches
- rg Pitfall-11 gate → ZERO matches
</verification>

<success_criteria>
- biochemeleon/mutation.py exists with insert_hider(object, pos, rep, handle)
- insert_hider uses cmd.pseudoatom(object=existing) — inserts INTO the object (HIDER-01)
- insert_hider sets segi='GAME' + b=-999 sentinel via cmd.alter (HIDER-02)
- insert_hider fetches id via cmd.identify(mode=0), NOT the pseudoatom return value (Pitfall: pseudoatom return is unverified C status)
- insert_hider calls cmd.sort after alter (defensive — editing.py:1457)
- Module is cmd-coupled — runtime verified by smoke test later
- All WSL gates green
</success_criteria>

<output>
After completion, create `.planning/phases/03-mutation-safety-hider-registry-foundation/03-03-SUMMARY.md`
</output>
