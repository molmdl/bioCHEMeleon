---
phase: 03-mutation-safety-hider-registry-foundation
plan: 20
type: execute
wave: 12
depends_on: ["03-19"]
files_modified:
  - .planning/phases/03-mutation-safety-hider-registry-foundation/03-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "Hider atoms can be inserted INTO an existing object — the PyMOL object list (public_objects) is unchanged after insertion"
    - "Every inserted hider carries segi='GAME' + b=-999 sentinel and is recorded in a HiderRegistry keyed by atom id"
    - "The registry can be queried for all hiders (id, rep, status) and per-rep counts"
    - "After removing all hiders by sentinel (or restoring from backup), the object's atom count and structure match its pre-game state exactly"
  artifacts:
    - path: "biochemeleon/registry.py"
      provides: "HiderRegistry + HiderRecord (pure, WSL-testable)"
    - path: "biochemeleon/backup.py"
      provides: "snapshot/restore/discard/verify_intact (cmd-coupled)"
    - path: "biochemeleon/mutation.py"
      provides: "insert_hider/fetch_all_hider_ids/cleanup_hiders (cmd-coupled)"
    - path: "biochemeleon/game.py"
      provides: "GameController orchestrator (cmd-coupled)"
    - path: "tests/test_registry.py"
      provides: "Unit tests for the pure registry layer"
    - path: "smoke/phase3_smoke.py"
      provides: "Integration smoke test (run in Windows PyMOL)"
  key_links:
    - from: "biochemeleon/mutation.py"
      to: "biochemeleon/registry.py"
      via: "insert_hider returns id -> registry.register"
      pattern: "register\\(object=.*"
    - from: "biochemeleon/backup.py"
      to: "target object"
      via: "snapshot BEFORE any mutation"
      pattern: "snapshot"
    - from: "biochemeleon/mutation.py"
      to: "sentinel atoms"
      via: "cleanup_hiders removes by segi GAME"
      pattern: "cmd\\.remove.*segi GAME"
    - from: "biochemeleon/backup.py"
      to: "biochemeleon/game.py"
      via: "restore followed by verify_intact"
      pattern: "restore.*verify_intact"
    - from: "biochemeleon/registry.py"
      to: "injected iterate_fn"
      via: "reconstruct_from_sentinels(iterate_fn) — DI keeps registry pure"
      pattern: "reconstruct_from_sentinels"
    - from: "biochemeleon/game.py"
      to: "backup+mutation+registry"
      via: "start()/cleanup()/abort_on_error() orchestration"
      pattern: "backup\\.|mutation\\.|registry\\."
---

<objective>
Write the final `03-SUMMARY.md` — the phase summary a future OpenCode session reads to understand what Phase 3 shipped, what's proven, and what residual risks Phase 4 must handle.

Purpose: SUMMARY.md is the handoff artifact. It distills the 20 plans + verification + research into: what was built (the 3 new modules + orchestrator + smoke test), what's proven (the 4 success criteria + 3 resolved spikes), the architecture decision (3-module split for parallelism), and the residual risks for Phase 4 (rep=None after reload; the placeholder insert in start() must be replaced by the real sphere generator; the click handler needs the registry's mark_found).
Output: .planning/phases/03-mutation-safety-hider-registry-foundation/03-SUMMARY.md
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mutation-safety-hider-registry-foundation/03-RESEARCH.md
@.planning/phases/03-mutation-safety-hider-registry-foundation/03-VERIFICATION.md
@.planning/phases/03-mutation-safety-hider-registry-foundation/03-15-SUMMARY.md
@.planning/phases/03-mutation-safety-hider-registry-foundation/03-18-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Write 03-SUMMARY.md — Phase 3 handoff summary</name>
  <files>.planning/phases/03-mutation-safety-hider-registry-foundation/03-SUMMARY.md</files>
  <action>
Create .planning/phases/03-mutation-safety-hider-registry-foundation/03-SUMMARY.md following the summary template (@~/.config/opencode/get-shit-done/templates/summary.md) with this content:

```markdown
# Phase 3: Mutation Safety & Hider Registry Foundation — Summary

**Phase:** 03-mutation-safety-hider-registry-foundation
**Plans:** 20 (across 12 waves)
**Status:** COMPLETE — verified (4/4 success criteria, 3/3 requirements HIDER-01/02/06, smoke test ALL PASSED)
**Completed:** 2026-08-05

## What Shipped

### New modules (3 + orchestrator)
- **biochemeleon/registry.py** (PURE, stdlib only): `HiderRecord` (__slots__: id/object/rep/status/pos; key=(object,id); to_dict) + `HiderRegistry` (OrderedDict; 10 methods: register/get/all/remove/by_rep/counts_by_rep/mark_found/to_dict/from_dict/reconstruct_from_sentinels). rep=None tolerance for post-reload reconstruction. WSL-unit-tested.
- **biochemeleon/backup.py** (cmd-coupled): `BACKUP_PREFIX='_bchm_backup'`, `snapshot(target)` (delete stale + create fresh), `restore(target, backup)` (delete+create — never single-call create), `discard(backup)` (idempotent), `verify_intact(target, backup)` (count + tuple-multiset).
- **biochemeleon/mutation.py** (cmd-coupled): `insert_hider(object, pos, rep, handle)` (pseudoatom INTO object + alter sentinel + sort + identify→id — NEVER the pseudoatom return value), `fetch_all_hider_ids(object)` (sentinel iterate with space=), `cleanup_hiders(object)` (sentinel remove, returns count).
- **biochemeleon/game.py** (cmd-coupled, replaces 1-line stub): `GameController(target_obj)` with `start(hider_specs)` (snapshot → insert loop → register), `cleanup()` (cleanup_hiders + verify_intact + discard), `abort_on_error()` (restore + discard), `reconstruct_registry()` (DI-based sentinel reconstruction).

### Tests + smoke
- **tests/test_registry.py**: WSL-runnable unit tests (TestHiderRecord, TestHiderRegistryCore, TestHiderRegistryQueries, TestHiderRegistrySerialize, TestHiderRegistryReconstruct, TestHiderRegistryEdgeCases). [record test count from 03-18].
- **smoke/phase3_smoke.py**: complete round-trip verification (setup + criteria 1-4 happy path + failure path + Q1/Q2/PSE spikes + summary). Run in Windows PyMOL: ALL PASSED.

## What's Proven (4 Success Criteria)

1. **Insert INTO existing object**: cmd.pseudoatom(object=existing) — public_objects unchanged; count += N (smoke C1 PASS)
2. **Sentinel + registry**: all hiders carry segi='GAME' + b=-999; registry keys on id via cmd.identify (smoke C2 + C3 PASS)
3. **Registry queries**: all/by_rep/counts_by_rep work; counts zero-fill all 5 GAME_REPS (smoke C3 + unit tests PASS)
4. **Cleanup/restore integrity**: cleanup by sentinel OR restore from backup leaves structure byte-identical (smoke C4 happy + failure PASS; verify_intact PASS)

## Research Spikes Resolved (3 UNVERIFIED → RESOLVED)

- **Q1 (pseudoatom return)**: [record value/type from 03-15] — code never relies on it (uses identify)
- **Q2 (create merge-vs-replace)**: [record REPLACE/APPEND from 03-15] — restore uses delete+create regardless
- **PSE (round-trip id stability)**: [record stable/shifted from 03-15] — sentinel survives (load-bearing); reconstruct_from_sentinels rebuilds if ids shift

## Architecture Decision

3-module split (registry.py pure + backup.py + mutation.py cmd) + game.py orchestrator — enables 3 parallel waves (Waves 1-3 each run 3 plans in parallel across the 3 file-disjoint tracks). Dependency direction preserved: setup_state.py ← registry.py ← backup.py/mutation.py ← game.py.

## Residual Risks for Phase 4

1. **start() uses a placeholder insert** — Phase 3's GameController.start takes `hider_specs` (list of (pos, rep)) and inserts at the given positions. Phase 4 must replace this with the real sphere generator (pick random atoms, place hiders near them as spheres). The registry/insert/snapshot mechanism is proven; only the position-picking logic is Phase 4 work.
2. **rep=None after .pse reload** — reconstruct_from_sentinels sets rep=None (sentinel carries no rep). Phase 4's click handler must tolerate rep=None records (or Phase 8's .bcm sidecar must be loaded to recover rep before play resumes). Documented in AGENTS.md.
3. **No click handler yet** — Phase 4 wires cmd.id_atom(picked_selection) → registry.get(object, id) → registry.mark_found. The registry's mark_found raises KeyError on unregistered atoms (clean error signaling for "clicked a non-hider").
4. **No UI wiring yet** — GameController.start/cleanup/abort are library functions; Phase 4 connects them to the Setup tab's Start button and the Game tab's Restart/Cleanup buttons.
5. **Multi-object games** — Phase 3 assumes one target object; `_bchm_backup` is a single backup name. If Phase 9 supports parallel games on multiple objects, backup naming needs per-target suffixes (out of scope for Phase 3).

## Phase 3 frontmatter (for future planners)

```yaml
phase: 03-mutation-safety-hider-registry-foundation
affects: [04-mvp-core-loop-sphere, 05-line-stick-cartoon-generators, 06-hint-reveal, 07-found-hider-management, 08-persistence]
subsystem: mutation-safety
tech-stack:
  added: ["biochemeleon/registry.py (pure)", "biochemeleon/backup.py (cmd)", "biochemeleon/mutation.py (cmd)", "biochemeleon/game.py (orchestrator)"]
key-files: ["biochemeleon/registry.py", "biochemeleon/backup.py", "biochemeleon/mutation.py", "biochemeleon/game.py", "tests/test_registry.py", "smoke/phase3_smoke.py"]
requires: ["01-plugin-bootstrap-dialog-scaffold"]
```

## Next

Phase 4 — MVP Core Loop (Sphere): replace the placeholder insert with the sphere generator, wire the click handler (cmd.id_atom → registry.mark_found), add the timer + win condition. The foundation (snapshot → insert → register → cleanup/restore) is proven.
```

Fill in every [record ...] placeholder from the 03-15/03-18/03-19 SUMMARYs + VERIFICATION. Be honest about the smoke-test results.
Commit: `docs(03-20): write Phase 3 SUMMARY (handoff to Phase 4)`
  </action>
  <verify>rg -n "What Shipped|What's Proven|Residual Risks|Architecture Decision" .planning/phases/03-mutation-safety-hider-registry-foundation/03-SUMMARY.md  # 4 matches (all sections); rg -n "Phase 4" .planning/phases/03-mutation-safety-hider-registry-foundation/03-SUMMARY.md  # ≥2 matches (handoff); rg -n "registry\.py|backup\.py|mutation\.py|game\.py" .planning/phases/03-mutation-safety-hider-registry-foundation/03-SUMMARY.md  # ≥4 matches</verify>
  <done>03-SUMMARY.md exists with all sections (What Shipped, What's Proven, Spikes Resolved, Architecture, Residual Risks, Next); placeholders filled; Phase 4 handoff clear</done>
</task>

</tasks>

<verification>
- rg -n "What Shipped|What's Proven|Residual Risks|Architecture Decision" 03-SUMMARY.md → 4 matches
- rg -n "Phase 4" 03-SUMMARY.md → ≥2 matches (handoff)
- rg -n "registry\.py|backup\.py|mutation\.py|game\.py" 03-SUMMARY.md → ≥4 matches
- No unfilled [record ...] placeholders
- Content consistent with 03-VERIFICATION.md + 03-15/03-18 SUMMARYs
</verification>

<success_criteria>
- 03-SUMMARY.md exists with all required sections (What Shipped, What's Proven, Spikes Resolved, Architecture, Residual Risks, Next)
- All 4 success criteria documented as proven
- 3 spike findings recorded with actual values
- 5 residual risks for Phase 4 clearly stated (placeholder insert, rep=None, no click handler, no UI, multi-object)
- Phase 4 handoff is actionable (next planner knows exactly what to build on)
- No unfilled placeholders
- Phase 3 is COMPLETE
</success_criteria>

<output>
This is the final Phase 3 plan. After completion, Phase 3 is declared complete.
The 03-20-SUMMARY.md is the phase-level summary (this file IS the summary task's output — no separate summary file needed unless the executor chooses to write one).
</output>
