---
phase: 03-mutation-safety-hider-registry-foundation
plan: 08
type: execute
wave: 3
depends_on: ["03-05"]
files_modified:
  - biochemeleon/backup.py
autonomous: true

must_haves:
  truths:
    - "Hider atoms can be inserted INTO an existing object — the PyMOL object list (public_objects) is unchanged after insertion"
    - "Every inserted hider carries segi='GAME' + b=-999 sentinel and is recorded in a HiderRegistry keyed by atom id"
    - "The registry can be queried for all hiders (id, rep, status) and per-rep counts"
    - "After removing all hiders by sentinel (or restoring from backup), the object's atom count and structure match its pre-game state exactly"
  artifacts:
    - path: "biochemeleon/registry.py"
      provides: "HiderRegistry + HiderRecord (pure, WSL-testable)"
    - path: "biochemeleon/backup.py"
      provides: "snapshot/restore/discard/verify_intact (cmd-coupled)"
    - path: "biochemeleon/mutation.py"
      provides: "insert_hider/fetch_all_hider_ids/cleanup_hiders (cmd-coupled)"
    - path: "biochemeleon/game.py"
      provides: "GameController orchestrator (cmd-coupled)"
    - path: "tests/test_registry.py"
      provides: "Unit tests for the pure registry layer"
    - path: "smoke/phase3_smoke.py"
      provides: "Integration smoke test (run in Windows PyMOL)"
  key_links:
    - from: "biochemeleon/mutation.py"
      to: "biochemeleon/registry.py"
      via: "insert_hider returns id -> registry.register"
      pattern: "register\\(object=.*"
    - from: "biochemeleon/backup.py"
      to: "target object"
      via: "snapshot BEFORE any mutation"
      pattern: "snapshot"
    - from: "biochemeleon/mutation.py"
      to: "sentinel atoms"
      via: "cleanup_hiders removes by segi GAME"
      pattern: "cmd\\.remove.*segi GAME"
    - from: "biochemeleon/backup.py"
      to: "biochemeleon/game.py"
      via: "restore followed by verify_intact"
      pattern: "restore.*verify_intact"
    - from: "biochemeleon/registry.py"
      to: "injected iterate_fn"
      via: "reconstruct_from_sentinels(iterate_fn) — DI keeps registry pure"
      pattern: "reconstruct_from_sentinels"
    - from: "biochemeleon/game.py"
      to: "backup+mutation+registry"
      via: "start()/cleanup()/abort_on_error() orchestration"
      pattern: "backup\\.|mutation\\.|registry\\."
---

<objective>
Add `verify_intact(target_obj, backup_name)` to `biochemeleon/backup.py` — the structure-integrity checker that proves the target matches its backup atom-for-atom.

Purpose: Criterion 4 requires "the object's atom count and structure match its pre-game state exactly" after cleanup or restore. `verify_intact` is the proof: it compares atom count (cheap gate) + atomic-tuple multiset (resn/resi/name/chain/segi/x/y/z) between the target and the backup. Exact float equality is safe because `cmd.create` copies coords bit-for-bit (RESEARCH §Q6). This is called after every cleanup/restore in the smoke test.
Output: backup.py extended with `verify_intact`, syntax-checked in WSL.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mutation-safety-hider-registry-foundation/03-RESEARCH.md
@biochemeleon/backup.py
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Add verify_intact(target_obj, backup_name) to backup.py</name>
  <files>biochemeleon/backup.py</files>
  <action>
Add the `verify_intact` function to biochemeleon/backup.py (after `restore`). Follow 03-RESEARCH.md §"Backup/Restore Design" (lines 289-299) EXACTLY:
```python
def verify_intact(target_obj, backup_name=BACKUP_PREFIX):
    """Return True iff target's structure matches backup: atom count + atomic tuple multiset.
    Tuple = (resn, resi, name, chain, segi, x, y, z). Coords are exact for a create-copy (bit-for-bit)."""
    if cmd.count_atoms(target_obj) != cmd.count_atoms(backup_name):
        return False
    def _tuples(obj):
        out = []
        cmd.iterate(obj, "stored.append((resn, resi, name, chain, segi, x, y, z))",
                    space={'stored': out})            # editing.py:1490
        return out
    return sorted(_tuples(target_obj)) == sorted(_tuples(backup_name))
```
CRITICAL points:
- Count check is the cheap gate (catches gross mismatch — wrong atom count means structure changed).
- Tuple multiset is the thorough check: `sorted(target_tuples) == sorted(backup_tuples)` catches any coordinate/residue/name drift. Order doesn't matter (multiset), so atom reordering by `cmd.sort` doesn't cause false failures.
- `space={'stored': out}` — hygienic iterate (RESEARCH §Q3: never `space=None` which pollutes global namespace).
- Tuple fields `(resn, resi, name, chain, segi, x, y, z)` are all read-only iterate symbols (editing.py:1446-1449) — confirmed writable/readable set.
- Exact float equality is safe: `cmd.create` copies coordinates bit-for-bit (RESEARCH §Q6 Open Risk 8 — fallback is coords-omitted compare if this fails in smoke test).
Commit: `feat(03-08): add backup.py verify_intact (count + tuple-multiset integrity check)`
  </action>
  <verify>python3.6 -m py_compile biochemeleon/backup.py  # syntax OK; rg -n "def verify_intact" biochemeleon/backup.py  # 1 match; rg -n "cmd\.count_atoms" biochemeleon/backup.py  # ≥2 matches (target + backup); rg -n "space={'stored'" biochemeleon/backup.py  # ≥2 matches (two _tuples calls); rg -n "sorted" biochemeleon/backup.py  # 1 match (multiset compare)</verify>
  <done>backup.py has verify_intact; count gate + tuple-multiset; space= hygienic; py_compile passes</done>
</task>

<task type="auto">
  <name>Run full gate suite (no regression)</name>
  <files>biochemeleon/backup.py</files>
  <action>
Run the WSL gate suite:
- `python3.6 -m py_compile biochemeleon/*.py`
- `python3.6 -m unittest tests.test_setup_state -v` (90 pass)
- Pitfall-1 gate → ZERO
- Pitfall-11 gate → ZERO
- space= gate: `rg -n "space=" biochemeleon/backup.py` → ≥2 matches (two iterate calls in verify_intact)
No commit needed.
  </action>
  <verify>python3.6 -m py_compile biochemeleon/*.py && python3.6 -m unittest tests.test_setup_state 2>&1 | tail -3  # "OK"</verify>
  <done>all modules compile, 90 tests pass, grep gates green</done>
</task>

</tasks>

<verification>
- python3.6 -m py_compile biochemeleon/backup.py → no errors
- python3.6 -m py_compile biochemeleon/*.py → all compile
- python3.6 -m unittest tests.test_setup_state -v → 90 pass
- rg -n "def verify_intact" biochemeleon/backup.py → 1 match
- rg -n "cmd\.count_atoms" biochemeleon/backup.py → ≥2 matches
- rg -n "space={'stored'" biochemeleon/backup.py → ≥2 matches (hygienic)
- rg -n "sorted" biochemeleon/backup.py → 1 match (multiset compare)
- rg Pitfall-1 gate → ZERO
- rg Pitfall-11 gate → ZERO
</verification>

<success_criteria>
- backup.py has verify_intact(target_obj, backup_name)
- Count check (cheap gate) + tuple-multiset (thorough) — criterion 4 proof
- Uses space={'stored': out} (hygienic — no global pollution)
- sorted() multiset compare (order-independent — survives cmd.sort)
- Module still cmd-coupled — runtime verified by smoke test
- All WSL gates green
</success_criteria>

<output>
After completion, create `.planning/phases/03-mutation-safety-hider-registry-foundation/03-08-SUMMARY.md`
</output>
