---
phase: 03-mutation-safety-hider-registry-foundation
plan: 18
type: execute
wave: 10
depends_on: ["03-15"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Hider atoms can be inserted INTO an existing object — the PyMOL object list (public_objects) is unchanged after insertion"
    - "Every inserted hider carries segi='GAME' + b=-999 sentinel and is recorded in a HiderRegistry keyed by atom id"
    - "The registry can be queried for all hiders (id, rep, status) and per-rep counts"
    - "After removing all hiders by sentinel (or restoring from backup), the object's atom count and structure match its pre-game state exactly"
  artifacts:
    - path: "biochemeleon/registry.py"
      provides: "HiderRegistry + HiderRecord (pure, WSL-testable)"
    - path: "biochemeleon/backup.py"
      provides: "snapshot/restore/discard/verify_intact (cmd-coupled)"
    - path: "biochemeleon/mutation.py"
      provides: "insert_hider/fetch_all_hider_ids/cleanup_hiders (cmd-coupled)"
    - path: "biochemeleon/game.py"
      provides: "GameController orchestrator (cmd-coupled)"
    - path: "tests/test_registry.py"
      provides: "Unit tests for the pure registry layer"
    - path: "smoke/phase3_smoke.py"
      provides: "Integration smoke test (run in Windows PyMOL)"
  key_links:
    - from: "biochemeleon/mutation.py"
      to: "biochemeleon/registry.py"
      via: "insert_hider returns id -> registry.register"
      pattern: "register\\(object=.*"
    - from: "biochemeleon/backup.py"
      to: "target object"
      via: "snapshot BEFORE any mutation"
      pattern: "snapshot"
    - from: "biochemeleon/mutation.py"
      to: "sentinel atoms"
      via: "cleanup_hiders removes by segi GAME"
      pattern: "cmd\\.remove.*segi GAME"
    - from: "biochemeleon/backup.py"
      to: "biochemeleon/game.py"
      via: "restore followed by verify_intact"
      pattern: "restore.*verify_intact"
    - from: "biochemeleon/registry.py"
      to: "injected iterate_fn"
      via: "reconstruct_from_sentinels(iterate_fn) — DI keeps registry pure"
      pattern: "reconstruct_from_sentinels"
    - from: "biochemeleon/game.py"
      to: "backup+mutation+registry"
      via: "start()/cleanup()/abort_on_error() orchestration"
      pattern: "backup\\.|mutation\\.|registry\\."
---

<objective>
Run the FINAL full-package regression gate: every WSL-runnable check across the whole biochemeleon/ package + smoke/ + tests/, confirming Phase 3 ships clean.

Purpose: Each plan ran its own gates, but this plan runs them ALL together as a final pre-declaration check — catching any cross-plan interaction (e.g., a later plan's edit breaking an earlier plan's grep-gate invariant). This is the "phase-complete gate" before the SUMMARY declares Phase 3 done. It modifies no files; it only runs commands and reports.
Output: A green gate run (all commands pass). If any gate fails, this plan reports it for gap closure.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mutation-safety-hider-registry-foundation/03-RESEARCH.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Run the full Phase 3 regression gate suite</name>
  <files></files>
  <action>
Run ALL of the following commands from the repo root. Each MUST pass (zero exit / "OK" / zero matches). Report any failure verbatim.

1. **py_compile ALL modules** (syntax check — catches import-error-adjacent syntax issues):
   `python3.6 -m py_compile biochemeleon/*.py smoke/phase3_smoke.py`
   Expected: no output, exit 0.

2. **Unit tests — registry** (the new pure-layer tests from plans 03-01/04/07/10):
   `python3.6 -m unittest tests.test_registry -v`
   Expected: "OK" + a test count > 0 (the registry core + queries + serialize + reconstruct + edge case tests).

3. **Unit tests — setup_state** (the existing 90 tests — no regression):
   `python3.6 -m unittest tests.test_setup_state -v`
   Expected: "Ran 90 tests" + "OK".

4. **Combined unit tests** (run together to catch import-order issues):
   `python3.6 -m unittest tests.test_registry tests.test_setup_state -v`
   Expected: "OK".

5. **Pitfall-1 gate** (no Tk/Pmw/PyQt5-raw imports — scoped to biochemeleon/ + smoke/):
   `rg -n "import Tkinter|import tkinter|from tkinter|import Pmw|from Pmw|app\.root|grab_set|mainloop|Toplevel|menuBar\.addmenuitem|from PyQt5 import|import PyQt5" biochemeleon/ smoke/`
   Expected: ZERO matches (no output).

6. **Pitfall-11 gate** (.exec_() only on QFileDialog/QMessageBox, never main dialog):
   `rg -n "\.exec_\(\)" biochemeleon/ smoke/`
   Expected: ZERO matches (Phase 3 added no .exec_()).

7. **Registry purity gate** (registry.py is pure — no pymol import):
   `rg -n "from pymol" biochemeleon/registry.py`
   Expected: ZERO matches.

8. **cmd-coupled module gate** (backup.py, mutation.py, game.py each have exactly ONE `from pymol import cmd`):
   `rg -c "from pymol import cmd" biochemeleon/backup.py biochemeleon/mutation.py biochemeleon/game.py`
   Expected: 1 match per file (3 total).

9. **Sentinel consistency gate** (segi='GAME' + b=-999 used consistently):
   `rg -n "segi.?=.?['\"]GAME['\"]" biochemeleon/mutation.py`
   Expected: ≥1 match (the sentinel is set in insert_hider and selected in fetch_all_hider_ids + cleanup_hiders).

10. **space= hygiene gate** (all iterate/alter use explicit space=):
    `rg -n "space=" biochemeleon/mutation.py biochemeleon/backup.py smoke/phase3_smoke.py`
    Expected: ≥4 matches (alter in insert_hider, iterate in fetch_all_hider_ids, 2× iterate in verify_intact, iterate in smoke).

11. **Architecture direction gate** (setup_state.py still pure — no pymol/Qt):
    `rg -n "from pymol|from pymol\.Qt" biochemeleon/setup_state.py`
    Expected: ZERO matches (unchanged from Phase 2).

12. **Orchestration gate** (game.py wires all 3 modules):
    `rg -n "backup\.|mutation\.|registry\." biochemeleon/game.py`
    Expected: ≥6 matches.

If ALL 12 gates pass, the phase is clean. If ANY fails, report the failing command + output verbatim (do NOT attempt to fix — that's gap closure).
No commit (this plan modifies no files; it's a verification run).
  </action>
  <verify>All 12 commands above pass (run them sequentially; each must produce the expected output)</verify>
  <done>All 12 gates green: py_compile clean, all unit tests pass, all grep gates zero-match or match-count as expected, registry pure, architecture direction preserved, game.py orchestrates all 3 modules</done>
</task>

</tasks>

<verification>
This plan's verification IS the 12-gate suite in the task. All 12 must pass for the phase to be declared clean.
</verification>

<success_criteria>
- py_compile: all biochemeleon/*.py + smoke/phase3_smoke.py compile
- Unit tests: test_registry passes (all new tests), test_setup_state passes (90 existing), combined passes
- Pitfall-1 gate: ZERO matches (biochemeleon/ + smoke/)
- Pitfall-11 gate: ZERO matches
- Registry purity: ZERO `from pymol` in registry.py
- cmd-coupled: exactly 1 `from pymol import cmd` each in backup.py/mutation.py/game.py
- Sentinel: segi='GAME' present in mutation.py
- space= hygiene: ≥4 matches across mutation/backup/smoke
- Architecture: setup_state.py still pure (ZERO pymol/Qt)
- Orchestration: game.py wires backup+mutation+registry (≥6 matches)
- If all 12 green → Phase 3 ships clean; ready for SUMMARY (plan 03-19/03-20)
</success_criteria>

<output>
After completion, create `.planning/phases/03-mutation-safety-hider-registry-foundation/03-18-SUMMARY.md`
(record the 12-gate results: which passed, the test counts, any notes)
</output>
