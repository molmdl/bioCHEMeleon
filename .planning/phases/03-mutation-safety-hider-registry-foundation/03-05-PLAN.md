---
phase: 03-mutation-safety-hider-registry-foundation
plan: 05
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - biochemeleon/backup.py
autonomous: true

must_haves:
  truths:
    - "Hider atoms can be inserted INTO an existing object — the PyMOL object list (public_objects) is unchanged after insertion"
    - "Every inserted hider carries segi='GAME' + b=-999 sentinel and is recorded in a HiderRegistry keyed by atom id"
    - "The registry can be queried for all hiders (id, rep, status) and per-rep counts"
    - "After removing all hiders by sentinel (or restoring from backup), the object's atom count and structure match its pre-game state exactly"
  artifacts:
    - path: "biochemeleon/registry.py"
      provides: "HiderRegistry + HiderRecord (pure, WSL-testable)"
    - path: "biochemeleon/backup.py"
      provides: "snapshot/restore/discard/verify_intact (cmd-coupled)"
    - path: "biochemeleon/mutation.py"
      provides: "insert_hider/fetch_all_hider_ids/cleanup_hiders (cmd-coupled)"
    - path: "biochemeleon/game.py"
      provides: "GameController orchestrator (cmd-coupled)"
    - path: "tests/test_registry.py"
      provides: "Unit tests for the pure registry layer"
    - path: "smoke/phase3_smoke.py"
      provides: "Integration smoke test (run in Windows PyMOL)"
  key_links:
    - from: "biochemeleon/mutation.py"
      to: "biochemeleon/registry.py"
      via: "insert_hider returns id -> registry.register"
      pattern: "register\\(object=.*"
    - from: "biochemeleon/backup.py"
      to: "target object"
      via: "snapshot BEFORE any mutation"
      pattern: "snapshot"
    - from: "biochemeleon/mutation.py"
      to: "sentinel atoms"
      via: "cleanup_hiders removes by segi GAME"
      pattern: "cmd\\.remove.*segi GAME"
    - from: "biochemeleon/backup.py"
      to: "biochemeleon/game.py"
      via: "restore followed by verify_intact"
      pattern: "restore.*verify_intact"
    - from: "biochemeleon/registry.py"
      to: "injected iterate_fn"
      via: "reconstruct_from_sentinels(iterate_fn) — DI keeps registry pure"
      pattern: "reconstruct_from_sentinels"
    - from: "biochemeleon/game.py"
      to: "backup+mutation+registry"
      via: "start()/cleanup()/abort_on_error() orchestration"
      pattern: "backup\\.|mutation\\.|registry\\."
---

<objective>
Add `restore(target_obj, backup_name)` to `biochemeleon/backup.py` — the FAILURE-PATH restore that brings the target back to its pre-mutation state.

Purpose: When a mutation goes wrong (or the player aborts), the target object must be restored to its exact pre-game state. RESEARCH §Q2 confirms that single-call `cmd.create(existing, backup)` has UNVERIFIED merge-vs-replace semantics (C-dispatched) — so restore uses the unambiguous `cmd.delete(target) + cmd.create(target, backup)` two-step. This is the "restore" half of criterion 4.
Output: backup.py extended with `restore`, syntax-checked in WSL (runtime verified by smoke test).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mutation-safety-hider-registry-foundation/03-RESEARCH.md
@biochemeleon/backup.py
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Add restore(target_obj, backup_name) to backup.py</name>
  <files>biochemeleon/backup.py</files>
  <action>
Add the `restore` function to biochemeleon/backup.py (after `discard`, before any future `verify_intact`). Follow 03-RESEARCH.md §"Backup/Restore Design" (lines 274-283) EXACTLY:
```python
def restore(target_obj, backup_name=BACKUP_PREFIX):
    """FAILURE-PATH restore: target ends up atom-for-atom identical to backup.
    Uses delete+create to avoid merge-vs-replace ambiguity (RESEARCH §Q2: single-call
    cmd.create(existing, backup) is UNVERIFIED C-dispatched). Returns True on success,
    False on failure (caller aborts game)."""
    try:
        cmd.delete(target_obj)                       # commanding.py:496 — remove mutated object entirely
        cmd.create(target_obj, backup_name)          # creating.py:960 — fresh copy from backup
        return True
    except Exception:
        return False
```
CRITICAL: use `cmd.delete(target_obj)` + `cmd.create(target_obj, backup_name)` — NEVER single-call `cmd.create(target_obj, backup_name)` without the delete first (that's the UNVERIFIED merge path). The delete removes the mutated object entirely; the create makes a clean atom-for-atom copy from the backup.
Commit: `feat(03-05): add backup.py restore (delete+create, never single-call create)`
  </action>
  <verify>python3.6 -m py_compile biochemeleon/backup.py  # syntax OK; rg -n "def restore" biochemeleon/backup.py  # 1 match; rg -n "cmd\.delete\(target_obj\)" biochemeleon/backup.py  # 1 match (the delete-before-create pattern); rg -n "cmd\.create\(target_obj, backup_name\)" biochemeleon/backup.py  # 1 match</verify>
  <done>backup.py has restore(target_obj, backup_name); uses delete+create two-step; try/except returns bool; py_compile passes</done>
</task>

<task type="auto">
  <name>Run full gate suite (no regression)</name>
  <files>biochemeleon/backup.py</files>
  <action>
Run the WSL gate suite:
- `python3.6 -m py_compile biochemeleon/*.py`
- `python3.6 -m unittest tests.test_setup_state -v` (90 pass)
- Pitfall-1 gate → ZERO matches
- Pitfall-11 gate → ZERO matches (backup.py has no .exec_())
- Anti-pattern gate: `rg -n "cmd\.create\(.*backup" biochemeleon/backup.py` → should show `cmd.create(target_obj, backup_name)` inside restore (the two-step), NOT `cmd.create(target_obj, backup_name)` without a preceding `cmd.delete(target_obj)` on the prior line. Visually confirm the delete precedes the create inside the try block.
No commit needed.
  </action>
  <verify>python3.6 -m py_compile biochemeleon/*.py && python3.6 -m unittest tests.test_setup_state 2>&1 | tail -3  # "OK"</verify>
  <done>all modules compile, 90 tests pass, grep gates green, delete-before-create pattern confirmed in restore</done>
</task>

</tasks>

<verification>
- python3.6 -m py_compile biochemeleon/backup.py → no errors
- python3.6 -m py_compile biochemeleon/*.py → all compile
- python3.6 -m unittest tests.test_setup_state -v → 90 pass
- rg -n "def restore" biochemeleon/backup.py → 1 match
- rg -n "cmd\.delete\(target_obj\)" biochemeleon/backup.py → ≥1 match (inside restore's try block)
- rg Pitfall-1 gate → ZERO
- rg Pitfall-11 gate → ZERO
</verification>

<success_criteria>
- backup.py has restore(target_obj, backup_name=BACKUP_PREFIX)
- restore uses cmd.delete(target) + cmd.create(target, backup) — the unambiguous two-step (NOT single-call create)
- restore returns True on success, False on failure (try/except)
- Module still cmd-coupled — runtime verified by smoke test
- All WSL gates green
</success_criteria>

<output>
After completion, create `.planning/phases/03-mutation-safety-hider-registry-foundation/03-05-SUMMARY.md`
</output>
