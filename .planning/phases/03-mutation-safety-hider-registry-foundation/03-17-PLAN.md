---
phase: 03-mutation-safety-hider-registry-foundation
plan: 17
type: execute
wave: 10
depends_on: ["03-15"]
files_modified:
  - .planning/STATE.md
  - .planning/research/PITFALLS.md
autonomous: true

must_haves:
  truths:
    - "Hider atoms can be inserted INTO an existing object — the PyMOL object list (public_objects) is unchanged after insertion"
    - "Every inserted hider carries segi='GAME' + b=-999 sentinel and is recorded in a HiderRegistry keyed by atom id"
    - "The registry can be queried for all hiders (id, rep, status) and per-rep counts"
    - "After removing all hiders by sentinel (or restoring from backup), the object's atom count and structure match its pre-game state exactly"
  artifacts:
    - path: "biochemeleon/registry.py"
      provides: "HiderRegistry + HiderRecord (pure, WSL-testable)"
    - path: "biochemeleon/backup.py"
      provides: "snapshot/restore/discard/verify_intact (cmd-coupled)"
    - path: "biochemeleon/mutation.py"
      provides: "insert_hider/fetch_all_hider_ids/cleanup_hiders (cmd-coupled)"
    - path: "biochemeleon/game.py"
      provides: "GameController orchestrator (cmd-coupled)"
    - path: "tests/test_registry.py"
      provides: "Unit tests for the pure registry layer"
    - path: "smoke/phase3_smoke.py"
      provides: "Integration smoke test (run in Windows PyMOL)"
  key_links:
    - from: "biochemeleon/mutation.py"
      to: "biochemeleon/registry.py"
      via: "insert_hider returns id -> registry.register"
      pattern: "register\\(object=.*"
    - from: "biochemeleon/backup.py"
      to: "target object"
      via: "snapshot BEFORE any mutation"
      pattern: "snapshot"
    - from: "biochemeleon/mutation.py"
      to: "sentinel atoms"
      via: "cleanup_hiders removes by segi GAME"
      pattern: "cmd\\.remove.*segi GAME"
    - from: "biochemeleon/backup.py"
      to: "biochemeleon/game.py"
      via: "restore followed by verify_intact"
      pattern: "restore.*verify_intact"
    - from: "biochemeleon/registry.py"
      to: "injected iterate_fn"
      via: "reconstruct_from_sentinels(iterate_fn) — DI keeps registry pure"
      pattern: "reconstruct_from_sentinels"
    - from: "biochemeleon/game.py"
      to: "backup+mutation+registry"
      via: "start()/cleanup()/abort_on_error() orchestration"
      pattern: "backup\\.|mutation\\.|registry\\."
---

<objective>
Update `.planning/STATE.md` (Phase 3 complete) and `.planning/research/PITFALLS.md` (resolve the MEDIUM/UNVERIFIED flags the Phase 3 smoke test settled).

Purpose: STATE.md tracks the project's current position — Phase 3 complete means the next phase is Phase 4. PITFALLS.md records resolved/confirmed pitfalls so future research doesn't re-investigate them. The Phase 3 smoke test (plan 03-15) resolved 3 UNVERIFIED behaviors: Q1 (pseudoatom return value), Q2 (create merge-vs-replace + id-preservation), PSE (.pse round-trip id/sentinel stability). These must be recorded as RESOLVED with the actual finding.
Output: STATE.md updated (Phase 3 complete, ready for Phase 4); PITFALLS.md updated (3 flags resolved).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/PITFALLS.md
@.planning/phases/03-mutation-safety-hider-registry-foundation/03-RESEARCH.md
@.planning/phases/03-mutation-safety-hider-registry-foundation/03-15-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Update STATE.md — Phase 3 complete, ready for Phase 4</name>
  <files>.planning/STATE.md</files>
  <action>
Read .planning/STATE.md. Update these sections:
1. "Current focus" line: change "Phase 3 — Mutation Safety & Hider Registry Foundation (next)" to "Phase 4 — MVP Core Loop (Sphere) (next)"
2. "Current Position" section: change to "Phase: 3 of 10 COMPLETE → ready for Phase 4", "Plan: 20 of 20 in Phase 3 (verified passed 4/4 must-haves + 3/3 requirements HIDER-01/02/06)", "Status: Phase 3 verified complete; ready to plan Phase 4", "Last activity: 2026-08-05 — Phase 3 verified (4/4 success criteria, 3/3 requirements HIDER-01/02/06; smoke test ALL PASSED)"
3. "Progress" bar: change to [███████░░░░] 30% (3 of 10 phases)
4. "Performance Metrics > By Phase" table: add row "3. Mutation Safety & Hider Registry Foundation | 20 | [total] | [avg]"
5. "Accumulated Context" section: add a "### Phase 3 outputs" subsection summarizing:
   - registry.py (pure, 10 methods, WSL-unit-tested): HiderRecord + HiderRegistry (register/get/all/remove/by_rep/counts_by_rep/mark_found/to_dict/from_dict/reconstruct_from_sentinels). Key = (object, id). rep=None tolerance for reconstruction.
   - backup.py (cmd-coupled): snapshot/restore/discard/verify_intact. BACKUP_PREFIX='_bchm_backup'. restore = delete+create (never single-call).
   - mutation.py (cmd-coupled): insert_hider (pseudoatom+alter+sort+identify→id)/fetch_all_hider_ids (sentinel iterate)/cleanup_hiders (sentinel remove).
   - game.py (cmd-coupled): GameController orchestrator (start/cleanup/abort_on_error/reconstruct_registry).
   - smoke/phase3_smoke.py: complete round-trip verification (criteria 1-4 + failure path + Q1/Q2/PSE spikes).
   - Record the 3 spike findings from 03-15-SUMMARY (Q1 return type, Q2 replace/append, PSE id-stable/shifted).
6. "Decisions" section: add Phase 3 decisions (restore=delete+create, id via identify not return, registry keys on id, reconstruct via DI, rep=None after reload, snapshot-before-mutation, 3-module split for parallelism).
7. "Blockers/Concerns" section: remove the cross-phase "PyMOL has NO undo" concern (now resolved — Phase 3 established backup/restore). Add any new concerns from the smoke test (e.g., if PSE ids shifted, note "registry must rebuild from sentinels on reload; Phase 8 sidecar for rep").
8. "Session Continuity": update to Phase 3 complete.
Commit: `docs(03-17): update STATE.md — Phase 3 complete, ready for Phase 4`
  </action>
  <verify>rg -n "Phase 3.*COMPLETE|ready for Phase 4" .planning/STATE.md  # ≥1 match; rg -n "Phase 3 outputs" .planning/STATE.md  # 1 match (new subsection); rg -n "registry\.py|backup\.py|mutation\.py" .planning/STATE.md  # ≥3 matches</verify>
  <done>STATE.md reflects Phase 3 complete; Phase 3 outputs summarized; spike findings recorded; ready for Phase 4</done>
</task>

<task type="auto">
  <name>Update PITFALLS.md — resolve the 3 Phase 3 UNVERIFIED flags</name>
  <files>.planning/research/PITFALLS.md</files>
  <action>
Read .planning/research/PITFALLS.md. Find the Phase 3 research flags (or add a new "## Phase 3 — Resolved flags" section if not present). For each of the 3 UNVERIFIED behaviors, record the RESOLUTION from the 03-15 smoke test (use 03-15-SUMMARY.md):

1. **Q1: cmd.pseudoatom return value (id or status?)** — RESOLVED. Smoke test recorded: [record the actual value + type from 03-15-SUMMARY]. Conclusion: code NEVER relies on it (uses cmd.identify after insert); the finding is informational. Status: RESOLVED (informational).

2. **Q2: cmd.create merge-vs-replace (single-call create(existing, backup))** — RESOLVED. Smoke test recorded: [record REPLACE or APPEND from 03-15-SUMMARY]. Conclusion: restore uses delete+create (unambiguous) regardless; the single-call finding confirms the recommendation. Status: RESOLVED (delete+create is the canonical restore; single-call behavior documented).

3. **Q2b: cmd.create id-preservation across copy** — RESOLVED. Smoke test recorded: [record id-set match/mismatch from 03-15-SUMMARY]. Conclusion: affects only the abort/restore path (registry rebuilt on next Start anyway); happy path (cleanup via remove) preserves ids. Status: RESOLVED.

4. **Q4/PSE: .pse round-trip id/sentinel stability** — RESOLVED. Smoke test recorded: [record id-stable or id-shifted from 03-15-SUMMARY]. Conclusion: sentinel survives reload (HIGH confidence); if ids shifted, reconstruct_from_sentinels rebuilds the registry from sentinels (rep=None, Phase 8 sidecar reconciles). Status: RESOLVED (sentinel-survival is load-bearing; id-stability is informational).

Also add any NEW pitfalls discovered during Phase 3 (from the 03-15-SUMMARY or the implementation). Mark each as RESOLVED with the resolution.
Commit: `docs(03-17): resolve Phase 3 UNVERIFIED flags in PITFALLS.md (Q1/Q2/PSE from smoke test)`
  </action>
  <verify>rg -n "RESOLVED" .planning/research/PITFALLS.md  # ≥4 matches (3-4 resolved flags); rg -n "Q1|Q2|PSE" .planning/research/PITFALLS.md  # ≥3 matches (the 3 spikes documented)</verify>
  <done>PITFALLS.md has Phase 3 resolved-flags section; Q1/Q2/Q2b/PSE each recorded with the smoke finding + resolution; future research won't re-investigate</done>
</task>

</tasks>

<verification>
- rg -n "Phase 3.*COMPLETE" .planning/STATE.md → ≥1 match
- rg -n "Phase 3 outputs" .planning/STATE.md → 1 match
- rg -n "registry\.py|backup\.py|mutation\.py" .planning/STATE.md → ≥3 matches
- rg -n "RESOLVED" .planning/research/PITFALLS.md → ≥4 matches
- rg -n "Q1|Q2|PSE" .planning/research/PITFALLS.md → ≥3 matches
- Both files consistent with 03-15-SUMMARY spike findings
</verification>

<success_criteria>
- STATE.md: Phase 3 marked COMPLETE, Phase 4 is next, Phase 3 outputs summarized, spike findings recorded
- PITFALLS.md: 3-4 UNVERIFIED flags (Q1/Q2/Q2b/PSE) marked RESOLVED with actual smoke-test findings
- Future phases/research can read these and not re-investigate
- No regression to existing STATE.md/PITFALLS.md content (additive updates)
</success_criteria>

<output>
After completion, create `.planning/phases/03-mutation-safety-hider-registry-foundation/03-17-SUMMARY.md`
</output>
