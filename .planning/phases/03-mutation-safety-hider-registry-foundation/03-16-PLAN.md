---
phase: 03-mutation-safety-hider-registry-foundation
plan: 16
type: execute
wave: 10
depends_on: ["03-15"]
files_modified:
  - AGENTS.md
autonomous: true

must_haves:
  truths:
    - "Hider atoms can be inserted INTO an existing object — the PyMOL object list (public_objects) is unchanged after insertion"
    - "Every inserted hider carries segi='GAME' + b=-999 sentinel and is recorded in a HiderRegistry keyed by atom id"
    - "The registry can be queried for all hiders (id, rep, status) and per-rep counts"
    - "After removing all hiders by sentinel (or restoring from backup), the object's atom count and structure match its pre-game state exactly"
  artifacts:
    - path: "biochemeleon/registry.py"
      provides: "HiderRegistry + HiderRecord (pure, WSL-testable)"
    - path: "biochemeleon/backup.py"
      provides: "snapshot/restore/discard/verify_intact (cmd-coupled)"
    - path: "biochemeleon/mutation.py"
      provides: "insert_hider/fetch_all_hider_ids/cleanup_hiders (cmd-coupled)"
    - path: "biochemeleon/game.py"
      provides: "GameController orchestrator (cmd-coupled)"
    - path: "tests/test_registry.py"
      provides: "Unit tests for the pure registry layer"
    - path: "smoke/phase3_smoke.py"
      provides: "Integration smoke test (run in Windows PyMOL)"
  key_links:
    - from: "biochemeleon/mutation.py"
      to: "biochemeleon/registry.py"
      via: "insert_hider returns id -> registry.register"
      pattern: "register\\(object=.*"
    - from: "biochemeleon/backup.py"
      to: "target object"
      via: "snapshot BEFORE any mutation"
      pattern: "snapshot"
    - from: "biochemeleon/mutation.py"
      to: "sentinel atoms"
      via: "cleanup_hiders removes by segi GAME"
      pattern: "cmd\\.remove.*segi GAME"
    - from: "biochemeleon/backup.py"
      to: "biochemeleon/game.py"
      via: "restore followed by verify_intact"
      pattern: "restore.*verify_intact"
    - from: "biochemeleon/registry.py"
      to: "injected iterate_fn"
      via: "reconstruct_from_sentinels(iterate_fn) — DI keeps registry pure"
      pattern: "reconstruct_from_sentinels"
    - from: "biochemeleon/game.py"
      to: "backup+mutation+registry"
      via: "start()/cleanup()/abort_on_error() orchestration"
      pattern: "backup\\.|mutation\\.|registry\\."
---

<objective>
Update `AGENTS.md` with the Phase 3 domain rules — the locked decisions future phases (4-10) must follow when working with hiders, backups, and the registry.

Purpose: AGENTS.md is the high-signal notes file OpenCode reads before touching code. Phase 3 established non-obvious rules that are easy to get wrong: restore = delete+create (never single-call create), hider id via cmd.identify (never the pseudoatom return value), registry keyed by id (never index), reconstruct uses DI (registry stays pure), rep=None after .pse reload (Phase 8 sidecar reconciles). These must be documented NOW so Phase 4/5 planners don't rediscover the pitfalls.
Output: AGENTS.md extended with a "Phase 3 mutation-safety rules" subsection under Domain rules.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mutation-safety-hider-registry-foundation/03-RESEARCH.md
@.planning/phases/03-mutation-safety-hider-registry-foundation/03-15-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Add Phase 3 mutation-safety domain rules to AGENTS.md</name>
  <files>AGENTS.md</files>
  <action>
Read AGENTS.md, find the "## Domain rules (easy to get wrong)" section. After the existing bullet about the hider sentinel (`segi='GAME'` + `b=-999`), add a new subsection "### Phase 3 mutation-safety rules" with these bullets (concise — one line each, matching the existing AGENTS.md style):

- **Restore = `cmd.delete(target)` + `cmd.create(target, backup)`**, NEVER single-call `cmd.create(existing, backup)` — merge-vs-replace is UNVERIFIED C-dispatched (RESEARCH §Q2; smoke test confirmed [record REPLACE/APPEND here from 03-15 SUMMARY]).
- **Hider id via `cmd.identify("obj and name <handle> and segi GAME", mode=0)`** after insert — NEVER rely on `cmd.pseudoatom()`'s return value (it's an UNVERIFIED C status code; RESEARCH §Q1; smoke test recorded it as [record type/value from 03-15 SUMMARY]).
- **Registry keys on atom `id`** (stable across add/delete) — NEVER on `index` (fragile, shifts on insert/remove; RESEARCH §Q4, querying.py:1315).
- **`reconstruct_from_sentinels` uses dependency injection** — the iterate function is passed as a parameter so `registry.py` stays pure (no `from pymol import cmd`). Phase 4/8 callers inject `lambda: mutation.fetch_all_hider_ids(obj)`.
- **`rep` is NOT recoverable from sentinels after `.pse` reload** — the sentinel carries only `segi='GAME'` + `b=-999`; `reconstruct_from_sentinels` sets `rep=None`. Phase 8 `.bcm` sidecar reconciles `rep`. Phase 4/8 planners: do NOT assume `rep` survives reload without the sidecar. (RESEARCH Open Risk 6; smoke test [record id-stable/shifted from 03-15 SUMMARY].)
- **`cmd.sort(obj)` after `cmd.alter` of `segi`/`chain`** — defensive (editing.py:1457 alter warning: stale canonical order confounds later `create`/`byres`). `sort` reassigns `index` but preserves `id` — safe for the id-keyed registry.
- **`cmd.iterate`/`cmd.alter` with `space={'stored': ...}`** — NEVER `space=None` (pollutes global `pymol.__dict__`; RESEARCH §Q3, editing.py:59-60).
- **`backup.snapshot` MUST precede any `mutation.insert_hider`** — there is NO undo (undocontext is a no-op stub; editor.py:25-36). The backup is the only recovery mechanism.
- **Architecture: `registry.py` (pure) ← `backup.py`/`mutation.py` (cmd) ← `game.py` (orchestrator)** — strict dependency direction. `registry.py` has NO `from pymol import`. `game.py` wires all three.

Also update the "## Architecture" section's module list to include the 3 new modules (registry.py, backup.py, mutation.py) and update game.py's description from "1 line STUB" to "GameController orchestrator (Phase 3)".

Use the 03-15-SUMMARY.md (smoke test results) to fill in the [record ...] placeholders for Q1/Q2/PSE. If 03-15-SUMMARY.md doesn't have the spike values yet (checkpoint just completed), leave a TODO marker and fill from the user's checkpoint response.
Commit: `docs(03-16): add Phase 3 mutation-safety domain rules to AGENTS.md`
  </action>
  <verify>rg -n "Phase 3 mutation-safety rules" AGENTS.md  # 1 match (new subsection); rg -n "registry\.py|backup\.py|mutation\.py" AGENTS.md  # ≥3 matches (new modules listed); rg -n "delete.*create|single-call" AGENTS.md  # ≥1 match (restore rule)</verify>
  <done>AGENTS.md has Phase 3 mutation-safety rules subsection; restore/delete+create rule, id-via-identify rule, registry-keys-on-id rule, DI rule, rep=None rule, sort rule, space= rule, snapshot-first rule, architecture rule all present</done>
</task>

</tasks>

<verification>
- rg -n "Phase 3 mutation-safety rules" AGENTS.md → 1 match (new subsection)
- rg -n "registry\.py|backup\.py|mutation\.py" AGENTS.md → ≥3 matches (new modules in architecture)
- rg -n "NEVER single-call" AGENTS.md → 1 match (restore rule)
- rg -n "NEVER rely on .*pseudoatom" AGENTS.md → 1 match (id rule)
- rg -n "rep is NOT recoverable" AGENTS.md → 1 match (rep=None rule)
- rg -n "space={'stored'" AGENTS.md → 1 match (hygiene rule)
- AGENTS.md still passes the Pitfall-1 grep gate (the new content doesn't introduce forbidden tokens)
</verification>

<success_criteria>
- AGENTS.md extended with Phase 3 mutation-safety rules (9 rules)
- Architecture section updated to list registry.py/backup.py/mutation.py + game.py as orchestrator
- Spike findings from 03-15 recorded (Q1 return type, Q2 replace/append, PSE id-stable/shifted)
- Future phases (4-10) can read these rules and avoid rediscovering the pitfalls
- AGENTS.md still passes grep gates
</success_criteria>

<output>
After completion, create `.planning/phases/03-mutation-safety-hider-registry-foundation/03-16-SUMMARY.md`
</output>
