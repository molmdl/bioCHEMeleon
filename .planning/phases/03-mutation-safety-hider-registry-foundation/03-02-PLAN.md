---
phase: 03-mutation-safety-hider-registry-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - biochemeleon/backup.py
autonomous: true

must_haves:
  truths:
    - "Hider atoms can be inserted INTO an existing object — the PyMOL object list (public_objects) is unchanged after insertion"
    - "Every inserted hider carries segi='GAME' + b=-999 sentinel and is recorded in a HiderRegistry keyed by atom id"
    - "The registry can be queried for all hiders (id, rep, status) and per-rep counts"
    - "After removing all hiders by sentinel (or restoring from backup), the object's atom count and structure match its pre-game state exactly"
  artifacts:
    - path: "biochemeleon/registry.py"
      provides: "HiderRegistry + HiderRecord (pure, WSL-testable)"
    - path: "biochemeleon/backup.py"
      provides: "snapshot/restore/discard/verify_intact (cmd-coupled)"
    - path: "biochemeleon/mutation.py"
      provides: "insert_hider/fetch_all_hider_ids/cleanup_hiders (cmd-coupled)"
    - path: "biochemeleon/game.py"
      provides: "GameController orchestrator (cmd-coupled)"
    - path: "tests/test_registry.py"
      provides: "Unit tests for the pure registry layer"
    - path: "smoke/phase3_smoke.py"
      provides: "Integration smoke test (run in Windows PyMOL)"
  key_links:
    - from: "biochemeleon/mutation.py"
      to: "biochemeleon/registry.py"
      via: "insert_hider returns id -> registry.register"
      pattern: "register\\(object=.*"
    - from: "biochemeleon/backup.py"
      to: "target object"
      via: "snapshot BEFORE any mutation"
      pattern: "snapshot"
    - from: "biochemeleon/mutation.py"
      to: "sentinel atoms"
      via: "cleanup_hiders removes by segi GAME"
      pattern: "cmd\\.remove.*segi GAME"
    - from: "biochemeleon/backup.py"
      to: "biochemeleon/game.py"
      via: "restore followed by verify_intact"
      pattern: "restore.*verify_intact"
    - from: "biochemeleon/registry.py"
      to: "injected iterate_fn"
      via: "reconstruct_from_sentinels(iterate_fn) — DI keeps registry pure"
      pattern: "reconstruct_from_sentinels"
    - from: "biochemeleon/game.py"
      to: "backup+mutation+registry"
      via: "start()/cleanup()/abort_on_error() orchestration"
      pattern: "backup\\.|mutation\\.|registry\\."
---

<objective>
Create `biochemeleon/backup.py` (cmd-coupled) with `BACKUP_PREFIX`, `snapshot(target_obj)`, and `discard(backup_name)` — the snapshot/discard half of the backup lifecycle.

Purpose: Every destructive mutation needs a pre-mutation backup (PyMOL has NO undo). `snapshot` creates a private independent copy (`_bchm_backup`, underscore-prefixed so it's hidden from `public_objects`); `discard` cleans it up. `restore` and `verify_intact` come in plans 03-05 and 03-08.
Output: `biochemeleon/backup.py` with BACKUP_PREFIX + snapshot + discard, syntax-checked in WSL (runtime verified by the Phase 3 smoke test).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mutation-safety-hider-registry-foundation/03-RESEARCH.md
@biochemeleon/demos.py
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Create backup.py with BACKUP_PREFIX + snapshot + discard</name>
  <files>biochemeleon/backup.py</files>
  <action>
Create biochemeleon/backup.py following 03-RESEARCH.md §"Backup/Restore Design" (lines 257-272) EXACTLY for the snapshot+discard subset:
- Module docstring: "Backup snapshot/restore/discard — cmd-coupled (PyMOL Open Source has NO undo; manual backup mandatory)."
- `from pymol import cmd`
- `BACKUP_PREFIX = '_bchm_backup'`  (underscore => private, hidden from public_objects per RESEARCH §Q6)
- `def snapshot(target_obj):` — discards any stale backup first (`cmd.delete(BACKUP_PREFIX)`), then `cmd.create(BACKUP_PREFIX, target_obj)` (fresh independent copy — creating.py:960), returns `BACKUP_PREFIX`. Docstring: "Create a private independent backup copy of target_obj. Returns backup name. Discards any stale backup first."
- `def discard(backup_name=BACKUP_PREFIX):` — `cmd.delete(backup_name)`. Idempotent. Docstring: "Delete the backup object. Idempotent."
DO NOT add restore or verify_intact yet (plans 03-05, 03-08).
Note: this module is cmd-coupled — `from pymol import cmd` will FAIL at runtime in WSL (no PyMOL), but `py_compile` only checks SYNTAX, not imports, so it passes. Runtime is verified by the Phase 3 smoke test (plan 03-15).
Commit: `feat(03-02): add backup.py snapshot + discard`
  </action>
  <verify>python3.6 -m py_compile biochemeleon/backup.py  # syntax OK; python3.6 -m py_compile biochemeleon/*.py  # all compile; rg -n "from pymol" biochemeleon/backup.py  # exactly ONE match (the import line)</verify>
  <done>backup.py exists with BACKUP_PREFIX + snapshot + discard; py_compile passes; exactly one `from pymol import cmd` line</done>
</task>

<task type="auto">
  <name>Run full gate suite (no regression)</name>
  <files>biochemeleon/backup.py</files>
  <action>
Run the WSL gate suite to confirm no regression:
- `python3.6 -m py_compile biochemeleon/*.py` (all modules compile — backup.py's `from pymol import cmd` is syntax-only, passes)
- `python3.6 -m unittest tests.test_setup_state -v` (existing 90 tests pass — backup.py doesn't touch the pure layer)
- Pitfall-1 gate: `rg -n "import Tkinter|import tkinter|from tkinter|import Pmw|from Pmw|app\.root|grab_set|mainloop|Toplevel|menuBar\.addmenuitem|from PyQt5 import|import PyQt5" biochemeleon/` → ZERO matches
- Pitfall-11 gate: `rg -n "\.exec_\(\)" biochemeleon/` → ZERO matches (backup.py has no .exec_())
No commit needed (gate run only).
  </action>
  <verify>python3.6 -m py_compile biochemeleon/*.py && python3.6 -m unittest tests.test_setup_state 2>&1 | tail -3  # "OK" + "Ran 90 tests"</verify>
  <done>all modules compile, 90 existing tests pass, grep gates green</done>
</task>

</tasks>

<verification>
- python3.6 -m py_compile biochemeleon/backup.py → no syntax errors
- python3.6 -m py_compile biochemeleon/*.py → all modules compile
- python3.6 -m unittest tests.test_setup_state -v → 90 tests pass (no regression)
- rg -n "from pymol" biochemeleon/backup.py → exactly 1 match (the import)
- rg Pitfall-1 gate → ZERO matches
- rg Pitfall-11 gate → ZERO matches
</verification>

<success_criteria>
- biochemeleon/backup.py exists with BACKUP_PREFIX='_bchm_backup', snapshot(target_obj), discard(backup_name)
- snapshot discards stale backup then creates fresh copy; returns backup name
- discard is idempotent (cmd.delete is safe on absent objects)
- Module is cmd-coupled (from pymol import cmd) — runtime verified by smoke test later
- All WSL gates green (py_compile + existing tests + grep gates)
</success_criteria>

<output>
After completion, create `.planning/phases/03-mutation-safety-hider-registry-foundation/03-02-SUMMARY.md`
</output>
