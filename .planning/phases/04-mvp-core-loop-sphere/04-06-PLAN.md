---
phase: 04-mvp-core-loop-sphere
plan: 06
type: execute
wave: 4
depends_on:
  - "04-05"
files_modified:
  - smoke/phase4_smoke.py
autonomous: false

must_haves:
  truths:
    - "Headless smoke: sphere positions generate, hiders insert+register, spheres show, a hider recolors green via cmd.color, remaining counts decrement, win fires when all are found, cleanup restores the object — all pass (exit 0)"
    - "Human-verify: a player presses Start, sees the 3-2-1 countdown, clicks hider atoms that recolor green, watches the timer tick + remaining count drop, and sees the winning message when all are found (the 4 Phase-4 success criteria)"
  artifacts:
    - path: "smoke/phase4_smoke.py"
      provides: "Phase 4 headless smoke test (sphere gen + insert + show + simulate find + win + cleanup)"
      contains: "GameController"
      min_lines: 70
  key_links:
    - from: "smoke/phase4_smoke.py"
      to: "headless Windows PyMOL"
      via: "bash wsl2win_cp.sh; cd tmp/bioCHEMeleon; timeout 90 cmd.exe /c C:\\src\\run-conda-pymol.bat -cq smoke\\phase4_smoke.py"
      pattern: "cmd\\.show|cmd\\.color|GameController"
---

<objective>
Verify the Phase 4 core loop at runtime: (1) a headless smoke test exercising
sphere generation + insertion + show + simulate-find + win + cleanup, run from
WSL via the staged Windows PyMOL path; (2) a human-verify checkpoint where the
player plays a complete round in a real PyMOL GUI session and confirms the
four Phase-4 success criteria.

Purpose: The pure-layer logic is WSL-unit-tested (plans 04-01/04-03), but the
cmd-coupled paths (`cmd.show`/`cmd.color`, the pick chain) and ALL Qt GUI
behavior need runtime verification. The smoke closes the cmd-coupled gap
headlessly (AGENTS.md: pure `pymol.cmd.*` runs headlessly via
`cmd.exe /c C:\\src\\run-conda-pymol.bat -cq`). The human-verify closes the
GUI gap (tab switch, countdown display, ticking timer, real mouse clicks,
recolor, remaining update, winning message — none of which can run from WSL).

Output: `smoke/phase4_smoke.py` (~80 lines) + a resolved human-verify checkpoint.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mvp-core-loop-sphere/04-RESEARCH.md
@AGENTS.md

# Phase 3 smoke (the pattern to follow — check/pass/summary + headless run):
@smoke/phase3_smoke.py

# The modules under test:
@biochemeleon/generators.py
@biochemeleon/game.py
@biochemeleon/wizard.py
@biochemeleon/registry.py
</context>

<tasks>

<task type="auto">
  <name>Write + run headless smoke test</name>
  <files>smoke/phase4_smoke.py</files>
  <action>
    Create `smoke/phase4_smoke.py` modeled on `smoke/phase3_smoke.py` (same
    `RESULTS`/`check(name, cond)`/`=== SUMMARY ===` pattern, `sys.exit(1)` on
    fail). Use `from biochemeleon import generators, game, registry, mutation,
    wizard`. Pure `pymol.cmd.*` only — NO Qt (AGENTS.md: headless path cannot run
    Qt). The script MUST:

    1. `cmd.fetch("1ubq", async_=0)` (AGENTS.md: async_=0 for sync load); set
       `obj = "1ubq"`; capture `orig_count = cmd.count_atoms(obj)`.
    2. Generate sphere positions from the real bounding box:
       `extent = cmd.get_extent(obj)`; `positions = generators.generate_sphere_positions(extent, 3, seed=42)`;
       `check("gen: 3 positions", len(positions) == 3)`;
       `check("gen: positions within bounds", all(extent[0][k] <= positions[i][k] <= extent[1][k] for i in range(3) for k in range(3)))`
       (use a small tolerance — `<=` is fine for uniform; or
       `extent[0][k] - 1e-6 <= p <= extent[1][k] + 1e-6`).
    3. Build `hider_specs = [(pos, "spheres") for pos in positions]` and start
       the game: `gc = game.GameController(obj); gc.start(hider_specs)`;
       `check("start: count += 3", cmd.count_atoms(obj) == orig_count + 3)`;
       `check("start: registry len == 3", len(gc.registry.all()) == 3)`.
    4. Show hiders as spheres:
       `cmd.show("spheres", "%s and segi GAME" % obj)`;
       `check("show: spheres visible", cmd.count_atoms("%s and rep spheres and segi GAME" % obj) == 3)`.
    5. Register a mock callback to capture on_log/on_remaining_changed/on_win
       (use a plain list/dict, NOT unittest.mock — keep it stdlib for the smoke):
       ```python
       logs = []; rems = []; wins = []
       gc.set_callbacks(on_log=lambda m: logs.append(m),
                         on_remaining_changed=lambda r: rems.append(r),
                         on_win=lambda e: wins.append(e))
       gc._start_time = 1234.0   # so win()'s elapsed math works without a real timer
       ```
    6. Simulate finding ONE hider via `on_pick` (the logic path, not the pick
       chain — the pick chain needs a real mouse pick; see 04-RESEARCH.md §H
       Q32 Risk 1). Get the first hider id:
       `hids = [r.id for r in gc.registry.all()]`; `first_id = hids[0]`;
       `gc.on_pick(first_id)`;
       `check("on_pick: record now found", gc.registry.get(obj, first_id).status == registry.HIDER_STATUS_FOUND)`;
       `check("on_pick: recolored green", ...)` — read the color via iterate:
       `cols = []; cmd.iterate("%s and id %d" % (obj, first_id), "stored.append(color)", space={'stored': cols})`;
       `check("on_pick: color is green", cols and cols[0] == 7)` — PyMOL's
       green color index is 7 (CRK, CC=7). If the index is uncertain, fall back
       to checking the color NAME: `cmd.iterate(..., "stored.append(color)")`
       returns the int index; assert it changed from the pre-pick color
       (capture `orig_cols` before on_pick, assert `cols[0] != orig_cols[0]`).
       Document whichever assertion you use.
    7. `check("on_pick: remaining 2", gc._remaining() == 2)`;
       `check("on_pick: on_log fired", len(logs) >= 1)`;
       `check("on_pick: on_remaining_changed fired with 2", rems and rems[-1] == 2)`;
       `check("on_pick: on_win NOT fired yet", len(wins) == 0)`.
    8. Simulate a MISS (non-hider id):
       `miss_logs_before = len(logs); gc.on_pick(999999)`;
       `check("on_pick miss: logged Miss", len(logs) == miss_logs_before + 1 and "Miss" in logs[-1])`;
       `check("on_pick miss: remaining unchanged", gc._remaining() == 2)`.
    9. Find the remaining two:
       `for hid in hids[1:]: gc.on_pick(hid)`;
       `check("win: all found", gc._remaining() == 0)`;
       `check("win: on_win fired", len(wins) == 1 and isinstance(wins[0], float))`.
    10. (Optional spike) Try the pick chain headlessly — MEDIUM confidence
        (04-RESEARCH.md §H Q32 Risk 1). Wrap in try/except so a failure doesn't
        fail the smoke:
        ```python
        try:
            gc2 = game.GameController(obj); gc2.start([([5.0,5.0,5.0],"spheres")])
            gc2._start_time = 1234.0
            w2 = wizard.PickWizard(gc2, obj); w2.activate()
            hid2 = gc2.registry.all()[0].id
            cmd.select("pk1", "%s and id %d" % (obj, hid2))
            w2.do_pick(0)
            ok_pick = gc2.registry.get(obj, hid2).status == registry.HIDER_STATUS_FOUND
            check("spike: do_pick via simulated pk1 marks found", ok_pick)
            w2.deactivate(); gc2.cleanup()
        except Exception as exc:
            print("spike: do_pick-simulation not supported headlessly: %r" % (exc,))
            check("spike: do_pick simulation skipped (deferral to human-verify)", True)
        ```
        This spike is informational — a FAIL here is acceptable and logged, but
        guard it so it never breaks the main smoke exit code (the `check` inside
        the try uses a boolean; if the try raises before the check, the except
        logs and runs a passing check to keep RESULTS consistent).
    11. Cleanup the first game:
        `intact = gc.cleanup()`; `check("cleanup: returned True", intact is True)`;
        `check("cleanup: count back to orig", cmd.count_atoms(obj) == orig_count)`.

    Then STAGE + RUN headlessly per AGENTS.md:
    ```bash
    bash wsl2win_cp.sh
    mkdir -p tmp/bioCHEMeleon/smoke && cp smoke/phase4_smoke.py tmp/bioCHEMeleon/smoke/
    cd tmp/bioCHEMeleon && timeout 90 cmd.exe /c "C:\\src\\run-conda-pymol.bat -cq smoke\\phase4_smoke.py" 2>&1 | tail -50
    ```
    Always `cd` into the staged Windows path first (AGENTS.md: PyMOL resolves
    relative paths against the cmd.exe cwd; use the /mnt/c path so Windows PyMOL
    can read it). Check exit code: 0 = clean (or sys.exit(1) caught by the
    wrapper); nonzero = crash. Triage any failure: a Rule-1 library bug (e.g.,
    color index mismatch) → fix the smoke assertion; a real Phase-4 bug → fix
    the source module and re-stage + re-run. Re-run until ALL checks pass
    (the optional spike may log a deferral without failing the run).

    Commit: `git add smoke/phase4_smoke.py && git commit -m "test(04-06): add phase4 headless smoke (sphere gen+find+win+cleanup)"`
  </action>
  <verify>
    The headless smoke run prints `N/N passed` with `ALL PASSED` and exits 0
    (the optional do_pick spike may either pass or log a deferral — both are
    acceptable; only the main checks must all pass). Capture the tail output.
    Also confirm WSL-side: `python3.6 -m py_compile smoke/phase4_smoke.py` clean,
    and `python3.6 -m unittest tests.test_setup_state -v tests.test_registry -v tests.test_generators -v tests.test_game_controller -v` all pass.
  </verify>
  <done>
    smoke/phase4_smoke.py exists (~80+ lines); staged to tmp/bioCHEMeleon/;
    headless run via cmd.exe /c run-conda-pymol.bat -cq prints ALL PASSED (main
    checks pass; do_pick spike either passes or logs a deferral); committed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full Phase 4 MVP core loop: Start button (gui_setup.py) wired to
    PluginDialog._on_start (__init__.py), which resolves the target, generates
    sphere hider positions (generators.py), starts the game (game.py
    GameController — snapshot/insert/register), shows hiders as spheres, switches
    to the Game tab (gui_game.py), runs a 3-2-1 countdown, then activates a
    PickWizard (wizard.py) and starts a 1 Hz QTimer. Clicking a hider atom fires
    PickWizard.do_pick -> GameController.on_pick -> registry lookup + mark_found +
    cmd.color('green') + log/remaining callbacks; clicking a non-hider is a
    no-op miss. When all hiders are found, GameController.win stops the timer
    and shows a QMessageBox winning message with the time taken.
  </what-built>
  <how-to-verify>
    Run the plugin in a real Windows PyMOL session (NOT WSL — Qt/GUI needs a
    real display):
    1. Launch PyMOL via `setenv.bat` then `pymol` (Windows conda env).
    2. Load a molecule: e.g. `fetch 1ubq` (or pick a bundled demo in the Setup
       tab's demo mode). Confirm it renders.
    3. Open the bioCHEMeleon plugin from the Plugins menu. Confirm the Setup tab
       shows the form with a bold "Start" button in the Setup actions row.
    4. In Setup: select the loaded object (or fetch/demo), set hider count to a
       small number (e.g. 3), leave difficulty on Hard (total only). Click Start.
    5. **Success Criterion 1**: Confirm the object is backed up (a `_bchm_backup`
       object exists — check via the PyMOL object panel, it's underscore-hidden
       from public_objects but visible in the full list), 3 sphere hiders appear
       in the viewer (gray PS spheres), the tab switches to "Game status", and
       the info log shows "Get ready..." then "3" → "2" → "1" → "GO!" (one per
       second — the countdown is NOT instantaneous).
    6. **Success Criterion 2 + 3**: After "GO!", confirm the timer label starts
       ticking up (0:01, 0:02, ...) each second and the remaining label shows
       "Remaining: 3". Left-click a hider sphere in the viewer: confirm it
       recolors GREEN, the info log appends a "Found one! N remaining" message,
       and the remaining label drops to 2. Left-click a NON-hider atom (a real
       atom in the molecule): confirm the log appends "Miss!" and NOTHING harmful
       happens (no recolor, no remaining change, no error).
    7. **Success Criterion 4**: Click the remaining hiders. When the last one is
       found, confirm the timer STOPS (no longer incrementing), and a modal
       "You win!" QMessageBox appears showing the time taken (M:SS). Dismiss it.
    8. Wizard check: confirm the right-side viewer panel showed a "bioCHEMeleon"
       header + "Done (quit game)" button during play. After winning, confirm
       the wizard is deactivated (the panel returns to its prior state) and
       `mouse_selection_mode` is restored to its pre-game value (e.g. if it was
       3-Button Viewing before, it's 3-Button Viewing after — verify by clicking
       to select atoms normally again).
    9. (Optional) Confirm the right-side "Done (quit game)" button deactivates
       the wizard mid-game without crashing (click it during a countdown or
       mid-play; confirm the wizard clears and mouse mode restores).

    Report PASS/FAIL per criterion (C1-C4) + the wizard/mouse-mode checks. If
    any criterion fails, describe the symptom precisely (what you clicked, what
    you expected, what happened) so the next iteration can target it.
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 success criteria pass (+ wizard/mouse-mode restore), or describe the specific failures/symptoms to address.</resume-signal>
</task>

</tasks>

<verification>
Phase 4 is runtime-verified when:
- Headless smoke (Task 1) prints ALL PASSED for the main checks (sphere gen,
  insert, register, show, recolor, remaining, win, cleanup) and exits 0
- Human-verify (Task 2) confirms all 4 Phase-4 success criteria in a real PyMOL
  GUI session: Start→countdown (C1), click→recolor + non-hider no-op (C2),
  log+timer+remaining (C3), win→timer stop + message (C4), plus wizard
  activation + mouse_selection_mode restore
</verification>

<success_criteria>
- The Phase 4 core loop is proven at BOTH tiers: headless cmd-coupled smoke
  (WSL-runnable, automated) AND human GUI verification (real PyMOL session).
- All 8 Phase-4 requirements (LOOP-01/02/03, HIDER-04, BTN-07, GAME-01/02/03)
  are satisfied and the 4 ROADMAP success criteria are met.
- Phase 4 is ready for the phase-level 04-VERIFICATION.md / 04-SUMMARY.md
  handoff (a later planning step, not this plan).
</success_criteria>

<output>
After completion, create `.planning/phases/04-mvp-core-loop-sphere/04-06-SUMMARY.md`
</output>
