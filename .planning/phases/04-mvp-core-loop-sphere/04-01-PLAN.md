---
phase: 04-mvp-core-loop-sphere
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - biochemeleon/generators.py
  - tests/test_generators.py
autonomous: true

must_haves:
  truths:
    - "generate_sphere_positions returns exactly n positions for n > 0"
    - "Every returned position lies within the bounding-box extent"
    - "Same seed produces identical output (deterministic)"
    - "n=0 returns an empty list without error"
  artifacts:
    - path: "biochemeleon/generators.py"
      provides: "generate_sphere_positions(extent, n, seed) -> list[[x,y,z],...]"
      contains: "def generate_sphere_positions"
    - path: "tests/test_generators.py"
      provides: "WSL unit tests for the sphere generator"
      min_lines: 50
  key_links:
    - from: "biochemeleon/generators.py"
      to: "stdlib random"
      via: "import random (NO from pymol — purity)"
      pattern: "^import random$"
---

<objective>
TDD the pure sphere-hider generator: `generate_sphere_positions(extent, n, seed)`.

Purpose: This is the geometry core that produces `(pos, rep)` specs fed into the
proven `GameController.start(hider_specs)`. It MUST be pure (stdlib `random`
only, NO `from pymol`) so it is WSL-unit-testable, mirroring `registry.py` and
`setup_state.py` (AGENTS.md: pure layer ← cmd-coupled layer; never reversed).
`__init__.py` (plan 04-05) will call `cmd.get_extent(target)` then this function
to build `hider_specs = [(pos, 'spheres') for pos in positions]`.

Output: `biochemeleon/generators.py` (~30 lines, pure) + `tests/test_generators.py`
(~60 lines, WSL unit tests) following RED → GREEN → REFACTOR.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mvp-core-loop-sphere/04-RESEARCH.md
@AGENTS.md

# Purity precedent (read before writing):
@biochemeleon/registry.py
@tests/test_registry.py
</context>

<feature>
  <name>generate_sphere_positions</name>
  <files>biochemeleon/generators.py, tests/test_generators.py</files>
  <behavior>
    Pure geometry: given `extent` (the `[[xmin,ymin,zmin],[xmax,ymax,zmax]]` list
    returned by `cmd.get_extent`) and `n`, return a list of `n` distinct
    `[x,y,z]` lists drawn uniform-random inside the bounding box. A `seed`
    makes output deterministic (testability).

    Cases:
      - generate_sphere_positions([[0,0,0],[10,10,10]], 5) -> len==5; every
        coord in [0,10]
      - same seed -> identical list (two calls with seed=42 compare equal,
        element-for-element)
      - different seeds -> different lists (seed=1 vs seed=2 differ)
      - n=0 -> [] (no error)
      - n=1 -> len==1
      - negative-extent box (min > max on an axis) -> still returns n positions
        (rng.uniform handles min>max by drawing in [max,min]; no crash)
      - each position is a 3-element list of floats (not tuple, not nested)
  </behavior>
  <implementation>
    Follow 04-RESEARCH.md §C (Q13) verbatim shape:
    ```python
    """Sphere-hider generator — pure geometry (stdlib random only, WSL-testable).

    NO from pymol, NO from pymol.Qt. The cmd-coupled caller (__init__.py)
    fetches the bounding box via cmd.get_extent and feeds it here; the returned
    positions become (pos, 'spheres') specs for GameController.start.
    """
    import random


    def generate_sphere_positions(extent, n, seed=None):
        """Generate n random [x,y,z] positions within the bounding box.

        extent: [[xmin,ymin,zmin],[xmax,ymax,zmax]] from cmd.get_extent.
        n: number of positions. seed: int for deterministic output (tests).
        Returns: list of [x,y,z] lists (empty list when n <= 0).
        """
        rng = random.Random(seed)
        (xmin, ymin, zmin) = extent[0]
        (xmax, ymax, zmax) = extent[1]
        positions = []
        for _ in range(n):
            positions.append([
                rng.uniform(xmin, xmax),
                rng.uniform(ymin, ymax),
                rng.uniform(zmin, zmax),
            ])
        return positions
    ```
    Keep it ~30 lines. Do NOT import pymol, numpy, or anything beyond stdlib.
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>RED: write tests/test_generators.py (failing)</name>
  <files>tests/test_generators.py</files>
  <action>
    Create `tests/test_generators.py` mirroring the stub pattern in
    `tests/test_registry.py` lines 1-24 (stub `pymol`/`pymol.Qt` via
    `sys.modules` with `MagicMock` BEFORE importing `biochemeleon.*`, because
    `biochemeleon/__init__.py` does `from pymol.Qt import ...` at module level).

    Then `from biochemeleon.generators import generate_sphere_positions` and
    write a `TestGenerateSpherePositions(unittest.TestCase)` class covering
    the behavior cases above:
      - test_count: n=5 -> len==5
      - test_bounds: extent [[0,0,0],[10,10,10]], n=10 -> every coord in [0,10]
      - test_seed_determinism: two calls seed=42 -> equal (element-for-element
        using a recursive list comparison or `==` on the two lists)
      - test_seed_difference: seed=1 vs seed=2 -> not equal
      - test_n_zero: n=0 -> == []
      - test_n_one: n=1 -> len==1
      - test_negative_extent: extent [[10,10,10],[0,0,0]], n=3 -> len==3, no
        crash (coords land in [0,10] since uniform handles reversed bounds)
      - test_returns_lists: each position is a 3-element list of floats
        (isinstance(p, list) and len(p)==3 and all(isinstance(c, float)))

    Run the tests — they MUST fail (the import raises ImportError/AttributeError
    because `biochemeleon/generators.py` does not yet exist or the function is
    absent). Confirm the failure is the expected AttributeError/ModuleNotFoundError,
    NOT a test-logic bug.

    Commit RED: `git add tests/test_generators.py && git commit -m "test(04-01): add failing tests for generate_sphere_positions"`
  </action>
  <verify>
    `python3.6 -m unittest tests.test_generators -v` — tests FAIL (import error
    or AttributeError). Capture the failure summary; confirm it is the expected
    RED state (function/module missing), not a syntax error in the test file.
  </verify>
  <done>
    tests/test_generators.py exists with >= 8 test methods; unittest run fails
    because generate_sphere_positions is absent; RED commit landed.
  </done>
</task>

<task type="auto">
  <name>GREEN: implement biochemeleon/generators.py</name>
  <files>biochemeleon/generators.py</files>
  <action>
    Create `biochemeleon/generators.py` with EXACTLY the implementation in the
    <feature><implementation> block above (~30 lines). Module docstring must
    state "NO from pymol, NO from pymol.Qt" (mirrors registry.py's purity
    docstring). Use stdlib `random` only — NO numpy, NO pymol.

    Do NOT add any other function (line/stick/cartoon generators are Phase 5).
    Keep the module flat (no subdirectories — AGENTS.md: FLAT module pattern).

    Run the tests — they MUST all pass.

    Commit GREEN: `git add biochemeleon/generators.py && git commit -m "feat(04-01): implement generate_sphere_positions (pure sphere generator)"`
  </action>
  <verify>
    Run the full WSL gate suite (all MUST be green):
    - `python3.6 -m py_compile biochemeleon/*.py` (syntax check every module)
    - `python3.6 -m unittest tests.test_generators -v` (new tests ALL pass)
    - `python3.6 -m unittest tests.test_setup_state -v` (90 tests — no regression)
    - `python3.6 -m unittest tests.test_registry -v` (54 tests — no regression)
    - Purity gate (MUST return ZERO matches — generators.py has NO pymol):
      `grep -rnE "from pymol|import pymol" biochemeleon/generators.py` → 0
    - Pitfall-1 gate (MUST return ZERO across the package):
      `grep -rnE "import Tkinter|import tkinter|from tkinter|import Pmw|from Pmw|app\.root|grab_set|mainloop|Toplevel|menuBar\.addmenuitem|from PyQt5 import|import PyQt5" biochemeleon/` → 0
  </verify>
  <done>
    generators.py exists (~30 lines, pure stdlib); 8+ new tests pass; 144 prior
    tests still pass (no regression); purity gate 0; Pitfall-1 gate 0; GREEN
    commit landed.
  </done>
</task>

<task type="auto">
  <name>REFACTOR + final gate check</name>
  <files>biochemeleon/generators.py</files>
  <action>
    Review generators.py for obvious cleanups ONLY (docstring accuracy, no
    dead code). The implementation is intentionally minimal — do NOT add
    features (no min-distance constraint, no near-atom placement — those are
    Phase 5+; 04-RESEARCH.md §C Q13 explicitly says uniform random is
    spec-compliant for MVP). If no changes needed, skip the commit (REFACTOR
    is optional). If a docstring tweak is made, re-run all gates and commit
    `refactor(04-01): ...`.

    Final confirmation: `grep -rnE "from pymol|import pymol|import numpy" biochemeleon/generators.py` → 0 matches (purity holds).
  </action>
  <verify>
    `python3.6 -m unittest tests.test_generators -v` still ALL pass after any
    refactor; purity grep still 0.
  </verify>
  <done>
    generators.py clean; tests green; purity confirmed; ready for plan 04-05
    (__init__.py wiring) to call generate_sphere_positions.
  </done>
</task>

</tasks>

<verification>
Phase-4 sphere generator is WSL-verified when:
- `python3.6 -m unittest tests.test_generators -v` → all pass (count, bounds,
  determinism, edge cases)
- `python3.6 -m py_compile biochemeleon/*.py` → clean (no syntax errors package-wide)
- Purity gate: `grep -rnE "from pymol|import pymol" biochemeleon/generators.py` → 0
- No regression: setup_state (90) + registry (54) tests still green
</verification>

<success_criteria>
- generate_sphere_positions(extent, n, seed) is pure, deterministic-with-seed,
  bounds-respecting, and WSL-unit-tested.
- The function is ready for plan 04-05 to call it as
  `generators.generate_sphere_positions(cmd.get_extent(target), hider_count)`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-mvp-core-loop-sphere/04-01-SUMMARY.md`
</output>
