---
phase: 20-persistence-combined-pdb-bcm-json
plan: 06
type: execute
wave: 4
depends_on: ["20-01", "20-05"]
files_modified: [vmd/lib/game.tcl]
autonomous: true

must_haves:
  truths:
    - "load_game turns a .bcmz into a resumed round: extract+gates -> stable-path PDB -> mol new -> reconstruct_from_sentinels (ONE 1-arg call) -> reconcile_with_bcm (ghosts dropped, never raises, warnings logged) -> tier codes stamped BEFORE any rep re-apply -> hider rep pairs re-derived via add_hider_reps (scene GAM-selection reps FILTERED out of the replay, NEVER verbatim-replayed) -> found hiders replayed (user2 float 1.0 + mark_found_visual) -> viewpoint restored -> resid block re-derived from reloaded CA resids with the sidecar as fallback -> post-import snapshot taken (filename = the STABLE path) -> returns {gs bcm}"
    - "load_game touches NO game_logic state (counters/timer/state machine are the caller's — the GUI's start_round round_reset must not wipe a lib-side restore)"
    - "restart on an imported round restores the imported initial state from the retained bcm + post-import snapshot (backup::restore + _replay_bcm) — no re-generation; the non-imported restart path is byte-unchanged"
    - "An imported won state (remaining == 0) is applied AS-IS: no finish_win, no auto-fire — the round sits finished-but-unwon-flagged (pinned decision, v1 parity)"
  artifacts:
    - path: "vmd/lib/game.tcl"
      provides: "game::load_game {bcmz_path}, private game::_replay_bcm {molid bcm} (shared by load + restart-on-imported), and the imported-round branch in game::restart"
      contains: "proc ::biochemeleon::game::load_game"
  key_links:
    - from: "vmd/lib/game.tcl load_game"
      to: "vmd/lib/registry.tcl reconcile_with_bcm (20-03)"
      via: "reconcile after reconstruct; the 3 mismatch lists are logged as warnings, never raised"
      pattern: "reconcile_with_bcm"
    - from: "vmd/lib/game.tcl _replay_bcm"
      to: "vmd/lib/hiders.tcl stamp_tier_codes/add_hider_reps/mark_found_visual"
      via: "the hiders.tcl cached-selection ordering contract — stamp BEFORE reps"
      pattern: "stamp_tier_codes"
    - from: "vmd/lib/game.tcl load_game"
      to: "vmd/lib/backup.tcl apply"
      via: "minimal snapshot {molid - filename <stable> viewpoint <vp> reps <filtered>} feeds apply unchanged"
      pattern: "backup::apply"
---

<objective>
Land the load-side rebuild chain (SC2): game::load_game reassembles start_game's steps 8-14 from a .bcmz — with the PINNED replay order, the resid-block re-derivation decision, the stable import path, and the restart-on-imported branch sharing one _replay_bcm proc.

Purpose: this is the load-bearing half of persistence — every ordering mistake here silently kills later finds (a rep added before the user3 stamp caches an empty selection forever).
Output: load_game + _replay_bcm + the restart branch in game.tcl, probe-verified headless.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-persistence-combined-pdb-bcm-json/20-RESEARCH-v1-carryover-format.md
@.planning/phases/20-persistence-combined-pdb-bcm-json/20-RESEARCH-integration-gui.md
@vmd/lib/game.tcl
@vmd/lib/hiders.tcl
@.planning/phases/18-materials-exploration/18-02-SUMMARY.md
@.planning/phases/18-materials-exploration/18-04-SUMMARY.md
@.planning/phases/18-materials-exploration/18-07-SUMMARY.md

Load-bearing facts + PINNED decisions:
- Replay order (v1-carryover §4.3, ordering is the contract): (7a) derive tier codes from the reconciled records — distinct reps in GAME_REPS order -> codes 1..N — and hiders::stamp_tier_codes; (7b) set user2 = 1.0 on found indices (FLOAT — read-back is 1.0/0.0); (7c) replay SCENE reps from viewer.reps FILTERED — skip any rep whose selection contains "resname GAM" (those are re-derived, never verbatim-replayed — verbatim leaves tier_reps empty and silently kills later finds) — via backup::apply with a minimal snapshot; (7d) hiders::add_hider_reps with the derived tier specs (style args via rep_tiers::style_args, the game.tcl:376-380 template); (7e) hiders::mark_found_visual per found index (re-asserts ALL pairs' modselect); (7f) hiders::set_found_color $molid <found_colorid> (P-19-04 seam); (7g) found_visible: apply the saved flag via the landed set_found_visible-equivalent (drift-recheck 19-04's landed API) — hidden = showrep-off on the FOUND reps; (8) viewpoint restore rides backup::apply; (9) resid block; (10) post-import snapshot.
- RESID-BLOCK DECISION (pinned): restore by RE-DERIVING from the reloaded molecule — for each registered index read {resid} via a narrow atomselect; if resid >= 9001 AND the GAM residue at that resid has > 1 atom (atomselect "resname GAM and segid GAME and resid $r" num > 1 — disambiguates the simple-hider 9001 sharing), map resid -> idx; register_resid_block the derived map. FALLBACK: if derivation yields an empty map but the sidecar's resid_block is non-empty, validate each pair's index is registered (drop invalid + warn, E10) and register the sidecar map. The sidecar block is still EMITTED (20-05) — lossless record + fallback data.
- Stable import path (E17): extract game.pdb to a staging dir, then file copy to the FIXED path $::env(TEMP)/biochemeleon_import.pdb (delete existing first) — the post-import snapshot's filename must survive later cleanup/restart reads. All runtime paths are Windows paths already (no to_vmd_path needed).
- Errors: mol new failure -> error "could not load the game PDB: <msg>" (E5, REFUSE); zero sentinels fetched -> vmdcon -warn "no hider sentinels found in the loaded PDB — is this a bioCHEMeleon game.pdb?" + PROCEED (E6, v1 allowed empty); reconcile mismatches -> vmdcon -warn per class (E7-E12, WARN never raise); hider_count present + integer and != fetched count -> warn (E9).
- per_rep for the imported gs: DERIVED from the reconciled records (count per rep in GAME_REPS order) — the truthful resolved shape (restart pass-through symmetry, 17.1-06). per_mat: from the sidecar setup (per_mat + material_blending) gated on blending==1, else {} — the exact shape of add_hider_reps' 3rd arg is drift-rechecked against 18-02-SUMMARY (if 18 landed mat_specs as a different shape, adapt + record).
- No game_logic calls ANYWHERE in load_game (counters/timer are the caller's — the GUI applies them after start_round's round_reset, 20-09).
- restart-on-imported: game.tcl retains the imported bcm in a namespace var (imported_bcm) + a flag in the gs (imported 1); game::restart checks the flag FIRST: backup::restore of the post-import snapshot -> _replay_bcm on the new molid -> rebuild gs (hider_count from the retained bcm) -> return. The existing re-generation path stays for non-imported rounds (byte-unchanged branch).
- Imported won state (pinned): apply as-is, NO finish_win, no auto-fire — document in the header comment.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drift-recheck the load-side seams (18-02/18-04/18-07 + hiders/19-04) against the landed code</name>
  <files>.planning/phases/20-persistence-combined-pdb-bcm-json/20-06-SUMMARY.md</files>
  <action>
Read the LANDED vmd/lib/hiders.tcl (add_hider_reps arity — 2-arg today, 18-02 adds the mat_specs 3rd arg), 18-02-SUMMARY (mat_specs shape), 18-04-SUMMARY (materials module + resolve API), 18-07-SUMMARY (5-key gs + per_mat), 19-04-SUMMARY (set_found_color/set_found_visible signatures), and the landed rep_tiers::style_args. Record in the SUMMARY: the exact mat_specs shape to pass, the found-visible restore call, whether materials need an ensure_gameblend call before add_hider_reps (18-04's single owner), and any deltas + adaptations. The contract is the LANDED code.
  </action>
  <verify>
The SUMMARY's seam table covers add_hider_reps arity/mat_specs, found color/visible APIs, materials ensure call, style_args — each with file:line evidence and a matches-pin/delta verdict.
  </verify>
  <done>Load-side seams verified against landed code before the rebuild chain was written.</done>
</task>

<task type="auto">
  <name>Task 2: load_game + _replay_bcm + restart-on-imported</name>
  <files>vmd/lib/game.tcl</files>
  <action>
Add to game.tcl (additive):
1. `proc ::biochemeleon::game::_replay_bcm {molid bcm}` — steps 7a-7g + resid block + hider_count warning, parameterized so BOTH load_game and restart-on-imported call it. Derives tiers from the bcm registry records (GAME_REPS order), stamps user3 BEFORE reps, sets user2 floats, applies scene reps via backup::apply with the GAM-filtered list, add_hider_reps (3rd arg per the Task-1 verdict), mark_found_visual loop, set_found_color, found_visible restore, register_resid_block per the PINNED re-derive-then-fallback algorithm, warns E9 when hider_count mismatches. Every atomselect deleted; every warn via vmdcon -warn.
2. `proc ::biochemeleon::game::load_game {bcmz_path}` -> {gs bcm}:
   a. lassign persistence_mol::bcmz_read $bcmz_path <unique extract dir under $::env(TEMP)> -> {pdb_path bcm_path}; persistence::read_sidecar + persistence::parse_bcm_dict (magic/version REFUSES surface here, E2/E3/E4) — catch-wrapped: re-raise as "import failed: <msg>".
   b. file delete + file copy $pdb_path -> $::env(TEMP)/biochemeleon_import.pdb (the STABLE path); file delete -force the extract dir.
   c. mol new $stable type pdb waitfor all (catch -> error "could not load the game PDB: $msg"); sentinels = mutation::fetch_hider_indices; empty -> the E6 warn + proceed.
   d. registry::reconstruct_from_sentinels [list apply {{molid} {...fetch lambda...}} $new_molid] — ONE call, 1-arg (rep "").
   e. reconcile = registry::reconcile_with_bcm [bcm registry.hiders]; warn the 3 lists when non-empty (v1 parity: proceed degraded).
   f. _replay_bcm $new_molid $bcm.
   g. post-import snapshot = backup::snapshot $new_molid (filename = the STABLE path — survives cleanup/restart).
   h. Build gs: game_molid $new_molid, hider_count (bcm hider_count if integer else registry::count_hiders), snapshot, per_rep (derived from records), per_mat (per Task-1 verdict), imported 1.
   i. Retain: set imported_bcm $bcm (namespace var, info-exists-guarded init).
   j. Return [list $gs $bcm]. NO game_logic calls.
3. game::restart imported branch (head of the landed proc, additive guard): if [dict exists $game_state imported] && imported && imported_bcm non-empty -> restored = cleanup $game_state; set new_m = the restored molid; _replay_bcm $new_m $imported_bcm; snapshot; rebuild gs (imported 1 retained); return gs. ELSE the existing re-generation path byte-unchanged.
Commit: `feat(20-06): game::load_game + _replay_bcm + restart-on-imported (index reconcile, pinned replay order)`.
  </action>
  <verify>
Headless probe (tmp/p20-06/probe_load.tcl, sequential): (A) FULL ROUND-TRIP: start_game 2-tier round -> find 2 hiders (mark_found + mark_found_visual) -> save_game -> cleanup + registry::reset -> load_game the file -> assert: new molid > old; count_hiders == N; per-index status_of matches the saved found-set; remaining_by_rep matches; atomselect user2 reads 1.0 on found indices (float compare); numreps == (scene reps after filter) + 2*tiers; viewpoint molinfo get equals the saved viewpoint (exact viewmaster round-trip); resid block: hider_for_resid resolves a fake resid on a residue round (second sub-case with a Cartoon tier) — re-derivation path (delete the sidecar block pre-load by building a bcm without it and confirming the fallback... no — assert the PRIMARY derivation: pass the sidecar WITH the block but assert the derived map equals it). (B) restart-on-imported: game::restart on the imported gs -> hider_count unchanged, statuses reset to the imported initial state (found statuses replayed for checkpoint kind), no re-generation (atom count == imported combined count, NOT a fresh randomize). (C) Errors: a hand-mangled .bcmz (wrong version) -> load_game errors with the pinned refusal; a .bcmz missing game.pdb -> the E3 message. 0 ERROR)/bad switch, Exiting normally.
  </verify>
  <done>A saved .bcmz resumes headlessly with registry/statuses/reps/viewpoint/resid-block restored in the pinned order, restart-on-imported restores without re-generation, and the refusal surfaces fire with pinned wordings.</done>
</task>

</tasks>

<verification>
- Probe green (PASS markers, 0 ERROR)/0 bad switch, Exiting normally, sequential).
- `grep -c "game_logic::" vmd/lib/game.tcl` — load_game/_replay_bcm bodies contain NO game_logic calls (the landed on_pick/cleanup references remain; verify the NEW procs by reading).
- 8.6-idiom grep -> 0; atomselect hygiene (every `$sel delete` present — grep the new procs).
- The drift-recheck seam table exists in the SUMMARY before the code landed (commit order proves it: SUMMARY commit or Task-1 evidence inside the feature commit).
</verification>

<success_criteria>
The full import/rebuild lifecycle is headless-proven — SC2 minus the GUI — with the pinned replay order, resid-block re-derivation, stable path, and restart-on-imported semantics landed once and shared.
</success_criteria>

<output>
After completion, create `.planning/phases/20-persistence-combined-pdb-bcm-json/20-06-SUMMARY.md` (seam table, replay-order record, probe evidence, the imported-divergence notes for 20-14's checkpoint).
</output>
