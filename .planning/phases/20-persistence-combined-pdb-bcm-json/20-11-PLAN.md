---
phase: 20-persistence-combined-pdb-bcm-json
plan: 11
type: execute
wave: 4
depends_on: ["20-06"]
files_modified: [vmd/smoke/phase20_persistence_smoke.tcl]
autonomous: true

must_haves:
  truths:
    - "The permanent headless proof of the full persistence cycle exists and passes 3 independent runs: start_game -> find k hiders -> save_game -> cleanup+reset -> load_game -> every saved field observable on the resumed round (registry statuses, remaining_by_rep, user2 1.0 floats, numreps == scene+2*tiers, viewpoint exact, counters, resid-block resolution)"
    - "Index stability (the reconcile premise) is asserted: fetch_hider_indices pre-save == post-reload, byte-for-byte"
    - "Generate-only (BTN-05's lib half) is proven: start_game without start_round leaves game_logic::state == 'idle', timer_elapsed 0, and a forced on_pick a no-op"
    - "Refusal surfaces are proven: a wrong-version sidecar and a missing-game.pdb archive each fail with the PINNED messages; the beta-clamp is asserted on the raw PDB bytes (78-char hider lines, element C, segid GAME)"
  artifacts:
    - path: "vmd/smoke/phase20_persistence_smoke.tcl"
      provides: "The phase-20 capstone headless smoke (phase16_smoke conventions): round-trip, index stability, generate-only, refusals, beta-clamp bytes, residue-round resid block"
      contains: "BCHM_SMOKE_RESULT"
  key_links:
    - from: "vmd/smoke/phase20_persistence_smoke.tcl"
      to: "vmd/lib/game.tcl save_game/load_game (20-05/06)"
      via: "the full cycle driven through the REAL public procs (never re-implemented in the smoke)"
      pattern: "save_game|load_game"
    - from: "vmd/smoke/phase20_persistence_smoke.tcl"
      to: "vmd/lib/persistence_mol.tcl (20-04)"
      via: "raw-byte beta-clamp assert on the extracted game.pdb"
      pattern: "write_game_pdb"
---

<objective>
Build the phase-20 permanent headless smoke: ONE script proving SC1/SC2/SC3/SC4's lib-reachable surface — the full save->destroy->load->resume round trip, index stability, generate-only, container/schema refusals, the beta-clamp bytes, and the residue-round resid block — PASS=1 across 3 independent runs.

Purpose: this is the artifact the FULL-SUITE gate (20-12) counts and future phases regression against; the GUI-only remainder (dialogs, buttons, real picks) is explicitly 20-14's.
Output: vmd/smoke/phase20_persistence_smoke.tcl.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/smoke/phase16_smoke.tcl
@.planning/phases/20-persistence-combined-pdb-bcm-json/20-RESEARCH-integration-gui.md
@.planning/phases/20-persistence-combined-pdb-bcm-json/20-05-SUMMARY.md
@.planning/phases/20-persistence-combined-pdb-bcm-json/20-06-SUMMARY.md

Conventions (phase16_smoke is the template):
- -e'd headless script: [pwd] staging root (NEVER [info script] — empty under -e); source the lib stack directly in dependency order (setup_state, registry, generators, game_logic, rep_tiers, bcm_json, persistence, demos, backup, mutation, persistence_mol, hiders, game, pick_bridge) — registry sourced EXACTLY ONCE; fresh staging copy per run (tmp/p20-smoke/), VMD runs SEQUENTIAL (R5: shared $env(TEMP)/biochemeleon_game.pdb).
- Assert with failure-list accumulation + catch+_bail; end with the `BCHM_SMOKE_RESULT PASS=1 FAIL=none` marker (or FAIL=<list>); the runner greps the marker NEVER $?; every run's FULL log is scanned for 0 `ERROR)` / 0 `bad switch` / `Exiting normally` (VMD -e catches top-level errors and CONTINUES — false PASS is possible; nothing trusts the marker alone).
- Timer: real clock, sleep-free; elapsed asserted range-wise, never exact.
- The smoke drives the PURE state machine for play (round_reset/begin_countdown/countdown_tick x4/begin_play) and the REAL save_game/load_game for persistence — set_callbacks with the 19-05 stub idiom (log -> lappend ::P_LOG, remaining -> incr ::P_REM, win -> lappend ::P_WIN) where the flow needs callbacks.
Assert families (integration research §Testing + the 20-06 probe hardened):
1. ROUND-TRIP: 2-tier simple round (VDW 2 Lines 2 on 1k8p) -> find 2 (registry::mark_found + mark_found_visual) -> save_game to staging -> archive asserts (PK magic, tar -tf == exactly game.pdb game.bcm) -> sidecar asserts (kind checkpoint, found statuses, timer int, original_pdb ends 1k8p.pdb) -> cleanup + registry::reset -> load_game -> count_hiders/status_of/remaining_by_rep/user2-float-1.0/numreps/viewpoint-exact/counters restored.
2. INDEX STABILITY: fetch_hider_indices on the pre-save molid == the post-load list.
3. BETA-CLAMP BYTES: read the extracted game.pdb raw — every hider ATOM line length 78, cols 73-76 "GAME", cols 77-78 " C".
4. RESIDUE ROUND: Cartoon-tier round (resid block) -> save -> load -> hider_for_resid resolves the fake resid -> CA map (the re-derivation path).
5. GENERATE-ONLY: start_game (no start_round) -> state "idle", timer_elapsed 0, forced game::on_pick <a hider index> -> no log line, registry untouched (the state gate no-op).
6. REFUSALS: hand-build a sidecar text with version 1 + version 3 -> parse_bcm_dict errors with the pinned wordings; a PK zip missing game.pdb -> bcmz_read/load_game E3 message; a ustar-named-.bcmz -> PK gate message.
</context>

<tasks>

<task type="auto">
  <name>Task 1: phase20_persistence_smoke.tcl — the full-cycle capstone smoke</name>
  <files>vmd/smoke/phase20_persistence_smoke.tcl</files>
  <action>
Write the smoke per the 6 assert families above, following phase16_smoke.tcl's structure section-for-section (header contract comment, failure-list harness, per-family sections with PASS/FAIL counters, the marker line last). Every atomselect deleted; every staging dir cleaned; all exprs braced; Tcl 8.5 only. All lib calls go through the LANDED public procs (save_game/load_game/bcmz_write/bcmz_read/parse_bcm_dict) — the smoke NEVER re-implements orchestration. The refusal sub-cases build their bad inputs with the landed bcm_json/persistence (wrong version text) or raw file writes (ustar bytes, member-missing zip via tar -cf).
Commit: `test(20-11): phase20 persistence smoke — full round-trip, index stability, generate-only, refusals, beta-clamp bytes`.
  </action>
  <verify>
From fresh staging (tmp/p20-smoke-r1..r3), THREE sequential runs: `bash -ic "vmd -dispdev text -e vmd/smoke/phase20_persistence_smoke.tcl -eofexit" < /dev/null` — each greps PASS=1 + 0 ERROR) + 0 bad switch + Exiting normally. Independent PRNG draws across runs must not flip any assert (range-wise pins, never exact random values).
  </verify>
  <done>The phase-20 headless proof exists, is green 3/3, and covers SC1-SC4's lib surface including the beta-clamp and both refusal classes.</done>
</task>

</tasks>

<verification>
- 3/3 independent runs PASS=1 with clean full-log scans (sequential, fresh staging each).
- 8.6-idiom grep on the smoke -> 0; `grep -c "BCHM_SMOKE_RESULT" ` == 1.
- No lib files touched (smoke-only plan): `git status --porcelain` shows only the smoke + SUMMARY.
</verification>

<success_criteria>
The complete persistence cycle is permanently provable headlessly — the gate plan and every future regression run can rely on this artifact.
</success_criteria>

<output>
After completion, create `.planning/phases/20-persistence-combined-pdb-bcm-json/20-11-SUMMARY.md` (assert-family table, 3-run evidence, any range-pin notes).
</output>
