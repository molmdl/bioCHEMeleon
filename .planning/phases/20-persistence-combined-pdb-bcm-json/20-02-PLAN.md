---
phase: 20-persistence-combined-pdb-bcm-json
plan: 02
type: tdd
wave: 2
depends_on: ["20-01"]
files_modified: [vmd/lib/persistence.tcl, vmd/tests/test_persistence.test]
autonomous: true

must_haves:
  truths:
    - "build_bcm_dict assembles the version-2 sidecar dict in the pinned emission order and applies the kind=puzzle forcing (started 0, timer_elapsed 0, reveal_count 0, hint_count 0) regardless of live values"
    - "parse_bcm_dict refuses with the pinned messages: magic mismatch, version > 2 ('Please update bioCHEMeleon.'), version == 1 ('this is a v1 (PyMOL) file...'); applies the documented defaults for missing optional fields and ignores unknown keys"
    - "The sidecar round-trips dict -> to_json -> from_json -> parse -> semantic-equal, including a 12-hider record list, a non-empty resid_block, a full 13-key setup dict with nested per_rep/per_mat, and a 64-number viewpoint"
  artifacts:
    - path: "vmd/lib/persistence.tcl"
      provides: "PURE sidecar-schema module: BCM_MAGIC/BCM_VERSION, BCM_SCHEMA (emit schema), build_bcm_dict, parse_bcm_dict, to_json/from_json, write_sidecar/read_sidecar (utf-8 file I/O)"
      contains: "BIOCHEMELEON-BCM"
    - path: "vmd/tests/test_persistence.test"
      provides: "tcltest suite (WSL, plain tclsh): build forcing, parse gates/defaults, JSON round-trip, refusal messages"
      contains: "tcltest"
  key_links:
    - from: "vmd/lib/persistence.tcl"
      to: "vmd/lib/bcm_json.tcl (20-01)"
      via: "to_json/from_json/write_sidecar/read_sidecar delegate the text layer to ::biochemeleon::bcm_json"
      pattern: "bcm_json"
    - from: "vmd/lib/persistence.tcl"
      to: "vmd/lib/setup_state.tcl"
      via: "rep validation against GAME_REPS + the setup dict shape (DEFAULTS keys) read at call time"
      pattern: "GAME_REPS"
---

<objective>
Land the PURE sidecar-schema module (v1's persistence.py pattern): the version-2 .bcm dict — build with kind forcing, parse with magic/version gates + tolerance defaults, JSON text conversion delegated to 20-01's codec, UTF-8 file I/O. Everything here is dict-in/dict-out and WSL-testable; no mol, no Tk, no game_logic.

Purpose: v1 shipped ALL dict assembly/parsing in a pure module and left cmd-coupled steps to the orchestrator — this module is that pattern, and the loaders/refusers the import flow's error taxonomy (E2/E4/E16) are implemented HERE, once, testably.
Output: vmd/lib/persistence.tcl + vmd/tests/test_persistence.test.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-persistence-combined-pdb-bcm-json/20-RESEARCH-v1-carryover-format.md
@vmd/lib/setup_state.tcl
@vmd/lib/bcm_json.tcl

Load-bearing facts (v1-carryover research §3.3/§3.4 — the authoritative schema):
- Top-level emission order: magic, version, kind, started, timer_elapsed, reveal_count, hint_count, found_colorid, found_visible, hider_count, original_pdb, viewer {viewpoint, reps}, registry {version, hiders[], resid_block[]}, setup.
- Constants: BCM_MAGIC "BIOCHEMELEON-BCM", BCM_VERSION 2; registry sub-dict keeps its own "version": 1 (v1 parity).
- Subset rules binding the emitter/parser: no JSON booleans (ints 0/1), no null (optional fields omitted at the SCHEMA level — but hider-record fields index/rep/status are ALWAYS emitted, rep may be ""), strings carry no raw controls (escaper handles defensively), numbers: ints plain / floats Tcl shortest-repr, nesting depth <= 3, emit in schema order, parse order-independent, pretty and compact both accepted.
- Refusal wording (verbatim ports): magic -> "not a bioCHEMeleon sidecar (magic=%s, expected BIOCHEMELEON-BCM)"; version>2 -> "unsupported .bcm version %d (expected 2). Please update bioCHEMeleon."; version==1 -> "this is a v1 (PyMOL) file; the VMD version reads v2 files only."
- Loader tolerance: missing keys -> defaults (started 0, timer_elapsed 0, reveal_count 0, hint_count 0, found_colorid 7, found_visible 1, original_pdb "", registry.hiders [], resid_block [], setup {}); wrong-typed scalars -> coerce or default; unknown keys ignored (forward compat); rep not in GAME_REPS is NOT a parse error (reconcile owns bad_rep — 20-03); hider record missing/non-integer index -> dropped with a malformed count.
- P-18/P-18 seam (drift-recheck at execution): today DEFAULTS has 11 keys; Phase 18 lands material_blending + per_mat (18-01/18-08) making 13. The setup sub-schema MUST be built from the LANDED setup_state::DEFAULTS key list at execution time, not hardcoded from this plan. per_rep/per_mat emit as DO-I, pdb_pool as LS-S, format/target_mode/selected_object/pdb_code/demo_id as S, hider_count as I, the rest as B.
- Depend on 20-01's module at CALL time (source it in tests; never re-source setup_state inside persistence.tcl — read ::biochemeleon::setup_state::GAME_REPS / DEFAULTS at call time like game.tcl does).
</context>

<feature>
  <name>persistence.tcl — v2 sidecar schema (build/parse/convert/IO), pure</name>
  <files>vmd/lib/persistence.tcl, vmd/tests/test_persistence.test</files>
  <behavior>
    build_bcm_dict {kind live} -> canonical dict in emission order. live is a dict with keys: started, timer_elapsed, reveal_count, hint_count, found_colorid, found_visible, hider_count, original_pdb, viewpoint, reps, hiders, resid_block, setup.
      kind "checkpoint": values pass through (ints coerced via string is integer; hiders/resid_block/reps normalized: hiders = list of {index rep status} sorted by index ascending; resid_block = list of {resid index} sorted by resid ascending; reps = list of {style selection color material} verbatim).
      kind "puzzle": FORCED started 0, timer_elapsed 0, reveal_count 0, hint_count 0 (live values ignored — v1 persistence.py:97-102 parity).
      kind anything else -> error "unknown .bcm kind '$kind'".
      rep validation: every hider rep must be "" or in GAME_REPS -> else error "build_bcm_dict: rep '<r>' is not a GAME_REPS name" (schema drift fails loudly at WRITE time).
    parse_bcm_dict {raw} -> normalized dict + never invents fields the schema omits; behavior:
      magic mismatch -> error "not a bioCHEMeleon sidecar (magic=<actual>, expected BIOCHEMELEON-BCM)".
      version missing/1 -> the v1 refusal message. version > 2 -> the update message. version 2 -> proceed.
      kind not checkpoint/puzzle -> error.
      Optional scalars default per the table above; hider_count absent -> "" (the caller's E9 check is conditional); non-integer timer/reveal/hint coerced via string is integer else default; hider records with missing/non-integer index are DROPPED and counted (normalized dict carries malformed_dropped count); unknown top-level keys ignored.
    to_json {bcm} / from_json {text}: delegate to ::biochemeleon::bcm_json (schema = BCM_SCHEMA); from_json output feeds parse_bcm_dict.
    write_sidecar {path bcm} / read_sidecar {path}: utf-8 file I/O via the bcm_json file helpers (fconfigure -encoding utf-8).
    Round-trip (SC4 unit): build(checkpoint, 12 mixed records incl. 2 found + non-empty resid_block + 13-key setup + 64-number viewpoint) -> to_json -> from_json -> parse_bcm_dict -> every field semantic-equal to the input; build(puzzle, live junk values) -> started/timer/counters all forced 0; parse refusal messages exact; malformed hider record dropped + counted.
  </behavior>
  <implementation>
    RED: write vmd/tests/test_persistence.test first (sources vmd/lib/setup_state.tcl, vmd/lib/bcm_json.tcl, vmd/lib/persistence.tcl — plain tclsh). Run -> FAIL (module absent).
    GREEN: implement persistence.tcl (namespace ::biochemeleon::persistence; BCM_MAGIC/BCM_VERSION/BCM_SCHEMA namespace vars; the five procs; header comment documents the schema table, the deflate-vs-store note — "archive is a standard PK zip; DEFLATE is the bsdtar --format zip default; a STORE-only pure-Tcl fallback exists but is not shipped" — and the source-slot contract: pure block, sourced after setup_state, before registry consumers; NEVER source registry.tcl here).
    The setup sub-schema is derived from ::biochemeleon::setup_state::DEFAULTS keys at call time (dict for -> per-key type map with a pinned default type S; override: hider_count I, per_rep DO-I, per_mat DO-I, pdb_pool LS-S, format S, the bools B). This keeps the module forward-compatible with the landed Phase-18 shape without editing this module again.
    REFACTOR only if obvious. Commits: `test(20-02): add failing persistence schema suite` then `feat(20-02): v2 .bcm sidecar schema module (build/parse/convert/IO)`.
  </implementation>
</feature>

<verification>
- `tclsh vmd/tests/test_persistence.test` -> Failed=0.
- `tclsh vmd/lib/persistence.tcl` loads clean (pure syntax gate).
- `grep -rnE "\blmap\b|\btry\b|\bthrow\b|\btailcall\b|\bcoroutine\b|\byield\b|\bfinally\b" vmd/lib/persistence.tcl` -> 0.
- `grep -c "mol\b\|atomselect" vmd/lib/persistence.tcl` -> only in comments if at all; NO executable mol calls (pure rule).
- DRIFT-RECHECK recorded in the SUMMARY: read the LANDED vmd/lib/setup_state.tcl DEFAULTS + 18-01-SUMMARY/18-08-SUMMARY and confirm the setup key list/type map matches; if Phase 18 landed extra keys, the derived-schema approach must already cover them (record the observed key list).
</verification>

<success_criteria>
The v2 sidecar schema is a pure, test-locked module: build forcing, refusal wording, tolerance defaults, and the SC4 round-trip are all provable in WSL before any orchestration exists.
</success_criteria>

<output>
After completion, create `.planning/phases/20-persistence-combined-pdb-bcm-json/20-02-SUMMARY.md` (schema table as landed, drift-recheck note vs the landed setup_state DEFAULTS, suite count).
</output>
