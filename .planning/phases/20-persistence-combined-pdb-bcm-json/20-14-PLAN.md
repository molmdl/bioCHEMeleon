---
phase: 20-persistence-combined-pdb-bcm-json
plan: 14
type: execute
wave: 7
depends_on: ["20-12", "20-13"]
files_modified: [vmd/AGENTS.md]
autonomous: false

must_haves:
  truths:
    - "G1: the Setup tab shows 'Generate & export' between 'Load Setup...' and 'Start'; the Game tab shows 'Import puzzle...' + 'Save checkpoint' in a row ABOVE the Actions labelframe"
    - "G2/G3: Save mid-game opens the .bcmz dialog; the chosen file is written; the log gains 'Saved checkpoint to <path>'; the timer did NOT bill the dialog time (the post-save dump's elapsed delta is small)"
    - "G4/G5: Importing that same file replays the countdown, RESUMES the timer from the saved elapsed, restores found hiders green + remaining count + reps/viewpoint, and a subsequent find SCORES (the resumed round is playable)"
    - "G6: Save is refused with clear info boxes when idle, during the countdown, and after a win (the pinned won-block)"
    - "G7/G8: Generate & export shows the success box and stays on Setup with hiders visible; Cleanup restores the scene; Cancel cleans up with no dangling hiders; importing the exported puzzle starts fresh (timer 0, all hidden)"
    - "G9: a corrupt/foreign .bcmz import shows the pinned warning and leaves the session stable"
    - "G10 (approvals + divergences): the tar.exe/powershell.exe mechanism is CONFIRMED approved (the .bcmz opens in Explorer as a real zip) — same OS-shipped class as Phase 21's curl.exe; the 4 documented divergences are acknowledged: imported Cleanup/Restart wording ('restore to imported initial state'), won-save block, imported-won no-auto-fire, DEFLATE zip default"
  artifacts:
    - path: ".planning/phases/20-persistence-combined-pdb-bcm-json/20-14-SUMMARY.md"
      provides: "The G1-G10 verdict table (pass/fail per item + persistence_verify_log.txt evidence), the approval record, the APPROVED checkpoint record"
      contains: "APPROVED"
    - path: "vmd/AGENTS.md"
      provides: "Refreshed Phase-20 note: .bcmz v2 persistence shipped (Save/Import/Generate&export), the format one-liner, the divergences, the checkpoint date"
      contains: "Phase-20"
  key_links:
    - from: "the human session"
      to: "vmd/tests/persistence_verify.tcl (20-13 driver)"
      via: "3 pastes (source / pv_p_mid / pv_p_finish) + real dialog interactions"
      pattern: "persistence_verify"
    - from: "the verdict table"
      to: "tmp/p20session/persistence_verify_log.txt"
      via: "auto-logged state/timer/status/file lines as per-item evidence"
      pattern: "persistence_verify_log"
---

<objective>
Run the ONE consolidated human-verify checkpoint for Phase 20 (GAME-09/GAME-04/BTN-05 + the tar.exe approval + the 4 documented divergences), driven by 20-13's persistence_verify.tcl. Then close out the docs.

Purpose: the last unverified surface — dialogs, button clicks, real file pickers, visual resume fidelity. 20-12 declared HEADLESS-GREEN; this is the only remaining work.
Output: an APPROVED (or precisely defect-recorded) checkpoint + refreshed vmd/AGENTS.md.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/20-persistence-combined-pdb-bcm-json/20-13-SUMMARY.md
@.planning/phases/20-persistence-combined-pdb-bcm-json/20-12-SUMMARY.md
@.planning/phases/20-persistence-combined-pdb-bcm-json/20-09-SUMMARY.md

Load-bearing facts:
- 20-13-SUMMARY carries the driver's EXACT paste names + instruction texts — quote them verbatim (the driver is the source of truth; fix drift by re-reading it, never paraphrasing).
- The first-click quirk is LOCKED: ONE keyboard `p` press arms pick delivery per round — the instructions lead with it.
- The timer keeps ticking under modal Tk dialogs (v2 behavior) — the rebase removes the MODEL drift; a ticking label during the dialog is EXPECTED, not a bug.
- 20-12's gate was green; this session stages FRESH (tmp/biochemeleon-vmd20/, do not reuse gate staging).
- The 4 divergences are pinned decisions — the session ACKNOWLEDGES them, it does not re-litigate: (1) imported Cleanup/Restart restores to the imported initial state (documented divergence from v1's hider removal); (2) Save is blocked in the won state with a clear message; (3) an imported won state applies as-is with no auto-fired win; (4) the .bcmz is a DEFLATE zip (bsdtar default).
- Scope discipline: no .planning/STATE.md, ROADMAP.md, or REQUIREMENTS.md edits — the orchestrator/verifier own those.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The complete Phase 20 persistence feature end-to-end in a real VMD GUI: Save checkpoint (pause-capture-rebase + info log line), Import puzzle (teardown -> resume with timer/counters/statuses/reps/viewpoint), Generate & export (generate-only + success box + Cleanup affordance), the refusal paths, and the .bcmz container — driven by persistence_verify.tcl with every state read auto-logged.</what-built>
  <how-to-verify>
1. Stage fresh (WSL): `cp -r vmd tmp/biochemeleon-vmd20/`. Open a real VMD GUI on Windows, Extensions -> Tk Console, `cd` to the staging root (Windows path of tmp/biochemeleon-vmd20). APPROVAL CHECK (G10a): confirm exec'ing System32 tar.exe is approved for the .bcmz container (Phase-21 curl.exe precedent); if REFUSED, stop — the pure-Tcl STORE fallback is a gap-closure swap.
2. Paste 1 (per 20-13-SUMMARY, verbatim): `source vmd/tests/persistence_verify.tcl` — EXPECT: 1znf loads, a 6-hider mixed round starts, the first dump lands. VERIFY G1: Setup tab shows Generate & export between Load Setup... and Start; Game tab shows Import puzzle... + Save checkpoint ABOVE the Actions labelframe.
3. Press `p` ONCE; find 2 hiders (they turn green; remaining drops).
4. Note the timer, click Save checkpoint, save to the PINNED path (tmp/p20session/checkpoint.bcmz), note the timer after — VERIFY G2: the file exists; VERIFY G3: log line "Saved checkpoint to ..."; the elapsed delta across the dialog is small (rebase worked; the label ticking DURING the dialog is expected).
5. Click Import puzzle..., pick the SAME file — VERIFY G4: countdown replays; the timer RESUMES from the saved elapsed (not 0); found hiders still green; Remaining correct; the view/reps look restored. Press `p` once, find one more — VERIFY G5: it scores (log "Found one! ...", remaining drops) — the resumed round is playable.
6. VERIFY G6: with no round live (after the next Cleanup), click Save checkpoint -> info box "No game round to save."; start a round and click Save DURING the countdown -> refused; win a tiny round (or Reveal all) and click Save -> the won-block message (pinned).
7. Setup tab: Generate & export -> save to tmp/p20session/puzzle.bcmz — VERIFY G7: the success box (v1 copy), stays on Setup, hiders visible; click Cleanup (Game tab) -> scene restored. Repeat Generate & export but CANCEL the dialog — VERIFY G7b: no dangling hiders (Cleanup says nothing to clean or the scene is clean). Re-export successfully, then Import the PUZZLE file — VERIFY G8: fresh start (timer from 0, all hiders hidden).
8. VERIFY G9 (refusal): rename any non-.bcmz file to x.bcmz (or hand-corrupt the sidecar) and Import it -> the pinned warning box; the session stays stable.
9. VERIFY G10 (divergences acknowledged + container): open a saved .bcmz in Windows Explorer — it opens as a zip (PK container, 2 members); acknowledge the 4 divergences as designed (imported-cleanup wording, won-save block, imported-won no-auto-fire, DEFLATE).
10. Paste 3: `pv_p_finish` — EXPECT: the final dump (post-import statuses/counters/viewpoint + the export/refusal evidence) + instructions; follow any last cleanup watcher step.
11. Record the verdict: the G1-G10 table item by item (pass/fail + the proving log line), attach persistence_verify_log.txt (expect 0 `ERROR)` / 0 `bad switch`).
  </how-to-verify>
  <resume-signal>Type "approved" or describe the failing items (which G item / which step).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Post-approval doc close-out (vmd/AGENTS.md Phase-20 note) + commit</name>
  <files>vmd/AGENTS.md</files>
  <action>
ONLY after the checkpoint verdict is recorded (APPROVED, or defects precisely recorded for gap closure):
1. Refresh vmd/AGENTS.md with a SHORT Phase-20 note (the length class of the Phase-19 note): the .bcmz v2 persistence shipped — Save checkpoint / Import puzzle (Game tab), Generate & export (Setup tab); format = combined-PDB (beta pre-clamped to -99.9 at save, -999 restored) + hand-rolled .bcm JSON sidecar (magic BIOCHEMELEON-BCM, version 2, index-keyed registry) zipped via System32 tar.exe --format zip (PowerShell fallback; pure-Tcl STORE documented); the 4 divergences (imported-cleanup restore semantics, won-save block, imported-won no-auto-fire, DEFLATE); the tar.exe approval record + date + APPROVED.
2. Do NOT touch .planning/STATE.md, ROADMAP.md, or REQUIREMENTS.md.
3. Commit (single): `docs(20-14): record Phase-20 GUI checkpoint approval + refresh AGENTS Phase-20 note`.
  </action>
  <verify>`git log --oneline -1` shows the docs(20-14) commit; `git status --porcelain` clean except .planning/ artifacts; the SUMMARY contains the verdict table; the AGENTS.md note exists.</verify>
  <done>The checkpoint verdict is recorded with per-item evidence and the repo docs reflect the shipped persistence surface.</done>
</task>

</tasks>

<verification>
- Checkpoint APPROVED (or every failing G item precisely recorded with evidence for gap closure).
- persistence_verify_log.txt: 0 ERROR) / 0 bad switch; the save/import/export/refusal evidence lines present.
- All three requirements (GAME-09, GAME-04, BTN-05) have their FINAL human-verified closure recorded; the tar approval + 4 divergences recorded.
- vmd/AGENTS.md Phase-20 note refreshed; single docs(20-14) commit.
</verification>

<success_criteria>
The ONE consolidated checkpoint is APPROVED with the auto-log as evidence — Phase 20's persistence surface is fully verified end-to-end — and the repo docs reflect it.
</success_criteria>

<output>
After completion, create `.planning/phases/20-persistence-combined-pdb-bcm-json/20-14-SUMMARY.md` (carry the G1-G10 verdict table + the approval record + the divergence acknowledgments, mirroring 19-17-SUMMARY's checkpoint-record shape).
</output>
