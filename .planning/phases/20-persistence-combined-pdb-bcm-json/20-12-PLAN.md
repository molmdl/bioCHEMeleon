---
phase: 20-persistence-combined-pdb-bcm-json
plan: 12
type: execute
wave: 6
depends_on: ["20-01", "20-02", "20-03", "20-04", "20-05", "20-06", "20-07", "20-08", "20-09", "20-10", "20-11"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Every tcltest suite present at gate time runs GREEN (Failed=0) via the staging-only suite_driver.tcl wrapper, with the suite list and case totals DERIVED at execution time (ls + marker parse) and recorded — never copied stale (expect the 6 standing + 18/19 additions + test_bcm_json + test_persistence + extended test_registry/test_game_logic — whatever ls returns IS the list)"
    - "Every smoke present at gate time runs SEQUENTIALLY on fresh staging: phase20_persistence_smoke + every smoke Phase 20 touched (phase16_gametab, phase16_entry) PASS=1 x3, standing smokes PASS=1 x1, and the known pre-existing reds fail EXACTLY in their recorded shapes (counted and reported, NOT fixed)"
    - "Every log: 0 `ERROR)` / 0 `bad switch` / `Exiting normally`; static gates zero: 8.6-idiom, grab-set, the phase-19 bans, plus the phase-20 bans (`tar -a` anywhere in vmd/, `package require json` anywhere in vmd/)"
    - "The verdict PHASE HEADLESS-GREEN is recorded with derived counts — only the GUI checkpoint (20-13/14) remains"
  artifacts:
    - path: ".planning/phases/20-persistence-combined-pdb-bcm-json/20-12-SUMMARY.md"
      provides: "Gate record: derived suite totals, the derived smoke table (PASS/expected-red), static gate outputs, HEADLESS-GREEN verdict"
      contains: "HEADLESS-GREEN"
  key_links:
    - from: "gate run"
      to: "every Phase-20 artifact (20-01..11)"
      via: "fresh staging tmp/cap20-gate, sequential VMD runs, marker parse + full-log scans"
      pattern: "BCHM_(TEST|SMOKE)_RESULT"
---

<objective>
The phase's FULL-SUITE green gate (17.1-13 / 19-15 pattern): every suite + every smoke against a fresh staging copy, sequential (shared $env(TEMP) forbids parallel VMD), markers parsed from logs (never $?), full-log false-PASS scans, static gates, known pre-existing reds confirmed unchanged, counts RE-DERIVED at execution time.

Purpose: HEADLESS-GREEN declaration for Phase 20 — after this, only the GUI checkpoint remains.
Output: gate record in the SUMMARY (no code files touched).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-in-game-actions/19-15-PLAN.md
@.planning/phases/19-in-game-actions/19-15-SUMMARY.md
@.planning/phases/18-materials-exploration/18-14-PLAN.md

Load-bearing rules (19-15 is the verbatim shape template):
- COUNTS ARE NEVER COPIED STALE: the 19-15/18-14 numbers are HISTORY — derive everything at execution time (ls the suites + smokes; read the latest gate SUMMARYs for the prior totals + the phase-20 case counts from 20-01/02/03/09 SUMMARYs; RECORD the derivation arithmetic).
- x3 for phase-20 new/updated smokes (phase20_persistence_smoke, phase16_gametab_smoke, phase16_entry_smoke); x1 for untouched standing smokes.
- Pre-existing-red discipline: the 2 known reds (phase17_dispatch_smoke step-8, phase17_licorice_smoke stale pins) + ANY reds recorded in the landed 18-14/19-15 SUMMARYs — confirm each failure SHAPE matches its record; do NOT fix, do NOT re-diagnose; record "pre-existing, unchanged".
- Static gates: 8.6-idiom grep over vmd/lib/ vmd/gui/ = 0; grab-set over vmd/gui/ = 0; the 19 bans (ttk::combobox, tk_chooseColor, hardcoded ColorIDs in game_tab) = 0; NEW phase-20 bans: `tar -a` over vmd/ = 0; `package require json` over vmd/ = 0.
- SEQUENTIAL VMD runs only; fresh staging tmp/cap20-gate; suites via the staging-only suite_driver.tcl wrapper (sed-swap the suite name per run).
</context>

<tasks>

<task type="auto">
  <name>Task 1: FULL-SUITE gate run + record (counts derived, never hardcoded)</name>
  <files>.planning/phases/20-persistence-combined-pdb-bcm-json/20-12-SUMMARY.md</files>
  <action>
1. Stage a FRESH copy of vmd/ to tmp/cap20-gate/ (Windows-visible).
2. DERIVE + run the suites sequentially via suite_driver.tcl; parse BCHM_TEST_RESULT per log; require Failed=0 everywhere; derive expected totals from the latest gate SUMMARY + the phase-20 suites' SUMMARY counts; record the arithmetic.
3. DERIVE + run the smokes sequentially with the x3/x1 rule; full-log scan EVERY log (0 ERROR) — Stride-classified where the 17.2 convention applies — 0 bad switch, Exiting normally).
4. Static gates (all greps zero): 8.6-idiom; grab-set; 19 bans; phase-20 bans (`tar -a` in vmd/, `package require json` in vmd/); plus `grep -c "biochemeleon_import.pdb" vmd/lib/game.tcl` >= 1 (the stable-path constraint is present).
5. Pre-existing-red confirmation per the discipline above.
6. Record everything in 20-12-SUMMARY.md: derived suite list + totals + arithmetic; derived smoke count + PASS/expected-red table; static gate outputs; sequential-run confirmation; the verdict line `PHASE HEADLESS-GREEN`; the handoff note that only the GUI checkpoint (20-13/14) remains.
Commit: `docs(20-12): record phase-20 full-suite gate (HEADLESS-GREEN)`.
  </action>
  <verify>
The SUMMARY's gate table shows: every derived suite Failed=0; every derived smoke PASS except EXACTLY the recorded pre-existing reds with matching shapes; all scanned logs clean; static gates zero; the HEADLESS-GREEN verdict present. Any OTHER red = a Phase-20 regression -> STOP and gap-close before the GUI checkpoint.
  </verify>
  <done>Phase 20 is HEADLESS-GREEN: full gate recorded with derived counts, zero new regressions, known reds unchanged.</done>
</task>

</tasks>

<verification>
- Derived suite list: all green (Failed=0), totals recorded with derivation arithmetic.
- Derived smoke list: all PASS per the x3/x1 rule except the recorded pre-existing reds (shapes confirmed, not fixed).
- Every log: 0 ERROR) / 0 bad switch / Exiting normally; static gates 0 (incl. the new phase-20 bans).
- No code files modified by this plan (SUMMARY only).
</verification>

<success_criteria>
The full-suite gate declares Phase 20 headless-green with re-derived evidence — the GUI checkpoint is the only remaining plan.
</success_criteria>

<output>
After completion, create `.planning/phases/20-persistence-combined-pdb-bcm-json/20-12-SUMMARY.md` (the gate record itself).
</output>
