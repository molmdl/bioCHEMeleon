---
phase: 20-persistence-combined-pdb-bcm-json
plan: 10
type: execute
wave: 6
depends_on: ["20-09"]
files_modified: [vmd/biochemeleon.tcl, vmd/smoke/phase16_entry_smoke.tcl, vmd/smoke/phase16_gametab_smoke.tcl]
autonomous: true

must_haves:
  truths:
    - "The entry chain sources the three new modules exactly once each, in dependency order: bcm_json + persistence in the PURE block (after setup_state, before the mol bridges) and persistence_mol in the mol block (after mutation, before game) — the dependency direction is never reversed and registry.tcl is still sourced exactly once"
    - "The entry + gametab load-gate smokes assert the FULL new surface (3 modules sourced, save_game/load_game/_prepare_setup/on_export_game/on_save_game/on_import_game defined, the begin-row + export buttons present) and still pass with all pre-existing asserts green"
  artifacts:
    - path: "vmd/biochemeleon.tcl"
      provides: "The three source lines with phase-20 comments (slot rationale per the integration research §Entry/source order)"
      contains: "persistence_mol"
    - path: "vmd/smoke/phase16_entry_smoke.tcl"
      provides: "Extended entry load-gate: new modules + procs asserted"
      contains: "persistence"
  key_links:
    - from: "vmd/biochemeleon.tcl"
      to: "vmd/lib/persistence.tcl + vmd/lib/bcm_json.tcl"
      via: "pure block source lines (call-time consumers: game.tcl, dialog.tcl)"
      pattern: "source \\[file join \\$_dir lib persistence.tcl\\]"
    - from: "vmd/biochemeleon.tcl"
      to: "vmd/lib/persistence_mol.tcl"
      via: "mol-bridge source line after mutation.tcl, before game.tcl"
      pattern: "persistence_mol"
---

<objective>
Wire the three new modules into the entry chain and refresh both load-gate smokes so the shipped surface (everything Phase 20 added) is asserted on every entry load.

Purpose: the entry is the only place source order exists; the load-gate smokes are the cheap regression net that catches a mis-ordered or missing source line forever after.
Output: 3 entry lines + extended phase16_entry_smoke.tcl + phase16_gametab_smoke.tcl handler asserts.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/biochemeleon.tcl
@vmd/smoke/phase16_entry_smoke.tcl
@.planning/phases/20-persistence-combined-pdb-bcm-json/20-RESEARCH-integration-gui.md

Load-bearing facts:
- Entry chain today: pure block (setup_state -> registry -> generators -> game_logic -> rep_tiers) -> mol bridges (demos -> backup -> mutation -> hiders) -> game.tcl -> pick_bridge.tcl -> gui/dialog.tcl. registry is sourced EXACTLY ONCE (re-sourcing wipes _records — the HARD RULE). demos/mutation re-source setup_state/generators harmlessly.
- Slot rationale (integration research §Entry/source order): bcm_json + persistence are PURE (stdlib + dict; persistence reads setup_state constants at call time) -> source in the pure block after setup_state (before registry is fine; the pinned choice: right after setup_state, with a phase-20 comment). persistence_mol is a standalone mol bridge (sources nothing) -> source after mutation.tcl, before game.tcl (game.tcl calls it at call time but the namespace must exist before the first CALL — same discipline as rep_tiers).
- pkgIndex.tcl needs NO change (maps the entry only).
- Load-gate smoke conventions: text-mode entry source + definition asserts; 0 ERROR)/bad switch; the 19-11 extended these smokes for Phase 19's procs — extend ADDITIVELY (never weaken landed asserts). DRIFT-RECHECK the landed smoke structure first.
- GUI procs referenced at call time by buttons: on_save_game/on_import_game/on_export_game — the load-gate asserts they are DEFINED after entry source (they are, post-20-09).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Entry source lines for the three phase-20 modules</name>
  <files>vmd/biochemeleon.tcl</files>
  <action>
Add three source lines with phase-20 comments: (1) after `source [file join $_dir lib setup_state.tcl]` in the pure block: bcm_json.tcl then persistence.tcl (persistence AFTER bcm_json — it delegates text conversion at call time; order between them is not load-bearing but keep dependency order for readability); (2) after mutation.tcl, before hiders/game: persistence_mol.tcl. Update the entry's order-matters comment block to mention the phase-20 slots. NO other lines change; registry still sourced exactly once.
Commit: `feat(20-10): entry wiring — bcm_json/persistence (pure block) + persistence_mol (mol block)`.
  </action>
  <verify>
`grep -c "lib bcm_json.tcl\|lib persistence.tcl\|lib persistence_mol.tcl" vmd/biochemeleon.tcl` == 3; the persistence_mol line sits between mutation.tcl and game.tcl (read the file); registry source count still 1 (`grep -c "lib registry.tcl"` == 1).
  </verify>
  <done>The entry sources the persistence stack in dependency order with zero landed-line changes.</done>
</task>

<task type="auto">
  <name>Task 2: Load-gate smoke refresh (entry + gametab)</name>
  <files>vmd/smoke/phase16_entry_smoke.tcl, vmd/smoke/phase16_gametab_smoke.tcl</files>
  <action>
Extend both smokes additively: (entry) after the landed definition asserts, assert the new namespaces/procs exist (::biochemeleon::bcm_json procs, persistence::build_bcm_dict/parse_bcm_dict, persistence_mol::write_game_pdb/bcmz_write/bcmz_read, game::save_game/load_game, _prepare_setup/on_export_game/on_save_game/on_import_game) and that the source ORDER held (the smoke can read biochemeleon.tcl's text for line-order asserts — a cheap grep-style check inside the smoke); (gametab) assert the begin-row buttons + the three handler names + resume_elapsed behavior already covered by 20-08 — add only the handler-definition asserts that needed 20-09. Keep every landed assert untouched.
Commit: `test(20-10): entry/gametab load-gates assert the phase-20 surface`.
  </action>
  <verify>
Both smokes from fresh staging: `bash -ic "vmd -dispdev text -e vmd/smoke/phase16_entry_smoke.tcl -eofexit" < /dev/null` (and the gametab one) -> PASS=1 x3 each, 0 ERROR)/bad switch, Exiting normally; all pre-existing asserts green.
  </verify>
  <done>Every future entry load asserts the phase-20 surface; the load-gate net is closed.</done>
</task>

</tasks>

<verification>
- Both smokes PASS=1 x3 sequential; 0 ERROR)/0 bad switch; 8.6-idiom + grab-set greps 0.
- Entry order: pure block gains bcm_json+persistence; mol block gains persistence_mol before game.tcl; registry count unchanged.
</verification>

<success_criteria>
The shipped entry chain carries the persistence stack in dependency order, and the standing load-gates fence the whole new surface.
</success_criteria>

<output>
After completion, create `.planning/phases/20-persistence-combined-pdb-bcm-json/20-10-SUMMARY.md` (slot rationale as landed, smoke totals before/after).
</output>
