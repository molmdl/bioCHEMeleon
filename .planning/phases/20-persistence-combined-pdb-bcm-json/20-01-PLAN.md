---
phase: 20-persistence-combined-pdb-bcm-json
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [vmd/lib/bcm_json.tcl, vmd/tests/test_bcm_json.test]
autonomous: true

must_haves:
  truths:
    - "Any .bcm-schema value (strings with backslashes/quotes/newlines/UTF-8, C:/ paths, ints, floats, 0/1 bools, empty lists/dicts, nested per_rep dicts, record lists, flat 64-number viewpoint lists) round-trips through to_json -> from_json with semantic equality"
    - "Garbage/malformed JSON produces a clean catchable tcl error (never a crash, hang, or wrong parse) and the schema-typed emitter errors loudly on unexpected/missing keys"
    - "File I/O round-trips through UTF-8 (fconfigure -encoding utf-8 both directions)"
  artifacts:
    - path: "vmd/lib/bcm_json.tcl"
      provides: "Pure hand-rolled JSON codec in namespace ::biochemeleon::bcm_json — schema-typed emit (S/I/F/B scalars, D closed dict, DO open dict, L record list, LS scalar list), generic recursive-descent parse"
      contains: "namespace eval ::biochemeleon::bcm_json"
    - path: "vmd/tests/test_bcm_json.test"
      provides: "tcltest suite runnable via plain tclsh in WSL — round-trip + adversarial + error-surface cases"
      contains: "tcltest"
  key_links:
    - from: "tmp/p20probe/bcm_json.tcl (probe-verified research artifact)"
      to: "vmd/lib/bcm_json.tcl"
      via: "verbatim lift modulo namespace rename + LS type"
      pattern: "escape_string|_parse_value|emit_object"
---

<objective>
Land the hand-rolled JSON codec (SC4's foundation) as a pure lib module + tcltest suite. The codec ALREADY EXISTS probe-verified (17/17 adversarial round-trips in VMD's exact Tcl 8.5.6) at tmp/p20probe/bcm_json.tcl — this plan lifts it into vmd/lib/ with the repo namespace convention, adds the LS (list-of-scalars) emit type the .bcm schema needs (viewpoint = 64 floats, pdb_pool = strings), and locks the behavior with a test-first suite.

Purpose: every .bcm byte that leaves or enters the game flows through this module; the SC4 round-trip guarantee is only as good as this codec.
Output: vmd/lib/bcm_json.tcl (pure, WSL-testable) + vmd/tests/test_bcm_json.test.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-persistence-combined-pdb-bcm-json/20-RESEARCH-vmd-mechanics.md
@tmp/p20probe/bcm_json.tcl

Load-bearing facts (mechanics research §Unknown 2 + §Pitfalls):
- The probe module is the SOURCE OF TRUTH: schema-typed emit (S/I/F/B, D, DO, L) + generic recursive-descent parse, one-pass `string map` escaper, `\uXXXX` parsed defensively but never emitted, `fconfigure -encoding utf-8` file I/O. Probe evidence: 17/17 adversarial + 12/12 keys + 50-record timing (emit 51 ms / parse 32 ms) + clean error surfaces (z3/z3b/z3c/z3d).
- Tcl 8.5 ONLY: no lmap/try/throw; foreach+lappend; braced expr; `regexp -start` + `^` does NOT anchor at the offset (probe pitfall 4 — the probe module already handles this); every comment inside proc bodies must be brace-balanced (pitfall 5 — an unbalanced brace in a comment aborts the whole namespace eval).
- The probe namespace is `::bcm::json` — the repo convention is `::biochemeleon::<module>`; rename mechanically, keep every proc body byte-identical.
- New requirement the probe did not carry: an `LS` emit type (list of scalars — `LS I`, `LS F`, `LS S`) for viewpoint (flat 64-number list) and pdb_pool (list of strings) + `LS` parse is just the generic array parse (no change needed parse-side).
</context>

<feature>
  <name>bcm_json.tcl — pure JSON codec, lifted + LS-typed</name>
  <files>vmd/lib/bcm_json.tcl, vmd/tests/test_bcm_json.test</files>
  <behavior>
    Round-trip (semantic equality after to_json -> from_json), cases:
      S: "C:\\Users\\x\\1ubq.pdb", "C:/Program Files (x86)/VMD/x y z.pdb", "he said \"hi\" \ and / or", "line1\nline2\ttab", "molećule", "β-factor", "C:\\odd\\\\path", "9001" (numeric-looking string stays a string), "" (empty).
      I: 117898 / 0 / -999.  F: -999.0 / 1.5 / 0.978 (viewpoint-class float).  B: 0 / 1.
      D (closed dict): registry {version I hiders L} round-trips; unexpected key -> error "emit_object: unexpected key"; missing key -> error "emit_object: missing key".
      DO (open dict): per_rep {VDW 6 Cartoon 4} and {} both round-trip; a key holding "" stays a string.
      L (list of record dicts): hiders records round-trip incl. empty list []; field order in output is schema order.
      LS (NEW): viewpoint 64-number flat list round-trips exactly (float stringification is Tcl-8.5-shortest-repr — bit-exact); LS S over pdb_pool strings; empty LS -> [].
    File I/O: write_file -> read_file round-trips UTF-8 strings ("mβl", "ćk") through disk.
    Error surfaces (all caught with catch, message non-empty, never a hang): '{{not json', '{"magic": }', '{"magic": "x",}', 'not json at all', '{}' (schema gate: missing key 'magic').
  </behavior>
  <implementation>
    RED: write vmd/tests/test_bcm_json.test FIRST (tcltest, mirrors the probe_z3 family: source the lib path via [file join [file dirname [info script]] .. lib bcm_json.tcl] — plain tclsh in WSL, no VMD), run `tclsh vmd/tests/test_bcm_json.test` -> must FAIL (module absent).
    GREEN: lift tmp/p20probe/bcm_json.tcl to vmd/lib/bcm_json.tcl: rename namespace ::bcm::json -> ::biochemeleon::bcm_json (every proc head + internal references), add the LS emit branch to the schema walker (list of scalars of one type; empty list OK), add the new header comment (repo-style: what/why/Tcl-8.5 rules/liftable-from-probe note + "archive is a standard PK zip; DEFLATE is the bsdtar --format zip default — STORE-only fallback documented, not shipped" note per the pinned deflate-vs-store decision). Keep ALL proc bodies otherwise byte-identical to the probe. Run the suite -> PASS. Re-run the probe's own adversarial cases as suite cases where they are not already covered.
    REFACTOR: only if an obvious cleanup exists; suite must stay green. Comments must stay brace-balanced (re-run the suite after ANY comment edit).
    Commits: `test(20-01): add failing tcltest suite for bcm_json codec` then `feat(20-01): lift probe-verified bcm_json codec into vmd/lib (LS type added)`.
  </implementation>
</feature>

<verification>
- `tclsh vmd/tests/test_bcm_json.test` -> all tcltest cases pass, Failed=0 (run in WSL, no VMD).
- `tclsh vmd/lib/bcm_json.tcl` loads with no error (pure layer syntax gate).
- Tcl 8.5 gate: `grep -rnE "\blmap\b|\btry\b|\bthrow\b|\btailcall\b|\bcoroutine\b|\byield\b|\bfinally\b" vmd/lib/bcm_json.tcl` -> 0 matches.
- `grep -c "biochemeleon::bcm_json" vmd/lib/bcm_json.tcl` >= 1 and `grep -c "::bcm::json" vmd/lib/bcm_json.tcl` == 0 (rename complete).
</verification>

<success_criteria>
The probe-verified JSON codec is a pure lib module with a green test-first suite — the SC4 round-trip engine exists before any schema or orchestration code touches it.
</success_criteria>

<output>
After completion, create `.planning/phases/20-persistence-combined-pdb-bcm-json/20-01-SUMMARY.md` (lift provenance: which probe procs landed verbatim, the LS addition, suite case count, timing if re-measured).
</output>
