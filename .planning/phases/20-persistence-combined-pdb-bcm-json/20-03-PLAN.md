---
phase: 20-persistence-combined-pdb-bcm-json
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified: [vmd/lib/registry.tcl, vmd/tests/test_registry.test]
autonomous: true

must_haves:
  truths:
    - "records_snapshot returns the live _records dict (idx -> {rep status}) without mutating it, and resid_block returns the live _resid_block dict — the save path's bulk read seam (the registry previously had NO bulk accessor)"
    - "reconcile_with_bcm merges .bcm hider records by index into the sentinel-rebuilt records and returns exactly {missing_from_bcm missing_from_pse bad_rep} — it NEVER raises: unmatched sentinels stay rep '' + hidden (playable), .bcm ghosts are NOT registered (win condition stays reachable), reps outside GAME_REPS land in bad_rep with rep left ''"
    - "status coercion: anything not 'found' -> 'hidden' (silent safe default, E11); malformed entries (missing/non-integer index) are skipped silently (E12, v1 parity)"
  artifacts:
    - path: "vmd/lib/registry.tcl"
      provides: "Phase-20 pure extension: records_snapshot, resid_block, reconcile_with_bcm (port of v1 registry.py:447-510 with the key swapped to index)"
      contains: "reconcile_with_bcm"
    - path: "vmd/tests/test_registry.test"
      provides: "Extended tcltest suite: snapshot read-only-ness, reconcile happy/mismatch/ghost/bad-rep/coercion/malformed cases"
      contains: "reconcile"
  key_links:
    - from: "vmd/lib/registry.tcl reconcile_with_bcm"
      to: "::biochemeleon::setup_state::GAME_REPS"
      via: "membership check for bad_rep classification (read at call time — setup_state sourced by the test)"
      pattern: "GAME_REPS"
    - from: "vmd/lib/registry.tcl (20-03)"
      to: "vmd/lib/game.tcl load path (20-06)"
      via: "load_game calls reconcile_with_bcm after reconstruct_from_sentinels; ghosts never reach _records"
      pattern: "reconcile_with_bcm"
---

<objective>
Give the registry the three pure seams the .bcm save/load needs: a bulk records read (save side), a resid-block read (save side), and the never-raise index-keyed reconcile (load side) — the direct port of v1's shipped reconcile_with_bcm with the key changed from (object, id) to index. Test-first, WSL-only.

Purpose: reconciliation-by-index is SC2's core ("reconstructs the registry from sentinels, and reconciles by index"). The v1 semantics (degrade-don't-raise, ghost rejection) are shipped, human-verified behavior — port them verbatim.
Output: registry.tcl extension + extended test_registry.test.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-persistence-combined-pdb-bcm-json/20-RESEARCH-v1-carryover-format.md
@vmd/lib/registry.tcl
@vmd/lib/setup_state.tcl

Load-bearing facts:
- Algorithm (v1-carryover §4.1, port of registry.py:472-510): (1) index .bcm hiders by int(index); malformed skipped silently; (2) walk _records — no match -> lappend missing_from_bcm, record STAYS rep ""/hidden; match -> rep in GAME_REPS ? set : (bad_rep, rep stays ""), status in {hidden found} ? set : hidden; (3) walk .bcm keys not in _records -> missing_from_pse (ghost — never registered).
- Returns a 3-list {missing_from_bcm missing_from_pse bad_rep}; NEVER raises (the orchestrator logs warnings and proceeds — degraded is playable).
- The existing registry is pure stdlib + GAME_REPS from setup_state (read at call time). _records is idx -> {rep status}; statuses via HIDER_STATUS_HIDDEN/FOUND constants.
- records_snapshot/resid_block are 3-line pure getters — do NOT add setters (the write paths are set_rep/assign_reps/register_resid_block, already landed).
- DRIFT-RECHECK at execution: Phase 19's 19-01 added hidden_indices/found_indices to this file — read the LANDED registry.tcl + 19-01-SUMMARY first; add the new procs additively without touching landed procs; extend the suite without weakening existing cases.
- Tcl 8.5 rules: dict for / foreach+lappend; braced expr; integer coercion via `string is integer -strict` (accepts "2597").
</context>

<feature>
  <name>registry reconcile + bulk snapshot accessors</name>
  <files>vmd/lib/registry.tcl, vmd/tests/test_registry.test</files>
  <behavior>
    records_snapshot {} -> the _records dict value (copy semantics: dict get returns the value; mutating the returned dict must not affect _records — build a fresh dict copy). resid_block {} -> the _resid_block dict (same copy discipline).
    reconcile_with_bcm {bcm_hiders} -> {missing_from_bcm missing_from_pse bad_rep}:
      Happy: 4 sentinel records rebuilt (rep "" hidden); bcm_hiders = 4 records matching by index with reps {VDW Lines} and statuses {found hidden} -> all three lists empty; records now carry the rep/status.
      Missing-from-bcm: sentinel idx 9001 not in bcm_hiders -> missing_from_bcm == {9001}, record stays rep ""/hidden.
      Ghost: bcm index 99999 not among sentinels -> missing_from_pse == {99999}, count_hiders unchanged.
      Bad rep: bcm rep "Ribbons" (not in GAME_REPS) -> bad_rep contains the index, record rep stays "".
      Coercion: bcm status "FOUND" / "bogus" -> hidden (no error, not even bad_rep); bcm index "2597" (string int) matches sentinel 2597; bcm record missing index -> skipped silently (no crash, no lists touched).
      Round-trip invariant: after reconcile, count_remaining reflects the hidden count; remaining_by_rep counts only assigned reps.
  </behavior>
  <implementation>
    RED: extend vmd/tests/test_registry.test with the reconcile + snapshot cases FIRST (the suite already sources setup_state + registry — follow its existing harness). Run -> new cases FAIL.
    GREEN: add records_snapshot, resid_block, reconcile_with_bcm to registry.tcl (additive; namespace export list extended; header comment documents the port provenance v1 registry.py:447-510 + the never-raise contract + the ghost-rejection rationale). Integer-coerce indexes on BOTH sides. Run suite -> PASS; ALL pre-existing cases still pass (no regressions on landed 19-01 procs).
    REFACTOR only if obvious. Commits: `test(20-03): add failing reconcile/snapshot cases to test_registry` then `feat(20-03): registry reconcile_with_bcm + bulk snapshot accessors (index-keyed, never raises)`.
  </implementation>
</feature>

<verification>
- `tclsh vmd/tests/test_registry.test` -> Failed=0 (new + ALL pre-existing cases).
- `grep -rnE "\blmap\b|\btry\b|\bthrow\b|\btailcall\b|\bcoroutine\b|\byield\b|\bfinally\b" vmd/lib/registry.tcl` -> 0.
- `grep -c "proc ::biochemeleon::registry::reconcile_with_bcm\|proc ::biochemeleon::registry::records_snapshot\|proc ::biochemeleon::registry::resid_block" vmd/lib/registry.tcl` == 3.
- DRIFT-RECHECK recorded in the SUMMARY: landed registry.tcl diffed against 19-01-SUMMARY expectations; hidden_indices/found_indices untouched and still green.
</verification>

<success_criteria>
The registry can be snapshotted for save and reconciled for load with v1's exact degrade-don't-raise semantics, proven by a test-first suite in WSL.
</success_criteria>

<output>
After completion, create `.planning/phases/20-persistence-combined-pdb-bcm-json/20-03-SUMMARY.md` (proc contracts, drift-recheck note, suite totals before/after).
</output>
