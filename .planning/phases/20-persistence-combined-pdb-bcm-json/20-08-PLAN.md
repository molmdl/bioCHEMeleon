---
phase: 20-persistence-combined-pdb-bcm-json
plan: 08
type: execute
wave: 4
depends_on: ["20-05"]
files_modified: [vmd/gui/game_tab.tcl, vmd/smoke/phase16_gametab_smoke.tcl]
autonomous: true

must_haves:
  truths:
    - "The Game tab renders 'Import puzzle...' + 'Save checkpoint' in a new begin-row packed ABOVE the Actions labelframe (bottom packs stack upward — the row packs AFTER the Actions frame with -side bottom)"
    - "resume_elapsed rebases the drift-free timer at GO: countdown_step's GO branch — AFTER begin_play — applies timer_start [expr {[clock seconds] - $resume_elapsed}] when resume_elapsed > 0, then CLEARS the var unconditionally (a leaked non-zero value would silently rebase a fresh round's timer)"
    - "The seam is zero-lib: game_logic::timer_start's existing optional `now` injection is the only mechanism — no game_logic changes"
  artifacts:
    - path: "vmd/gui/game_tab.tcl"
      provides: "begin-row (Import puzzle... / Save checkpoint buttons, pinned handler names), resume_elapsed namespace var + GO-branch consume/clear, reset point in the round-reset path"
      contains: "resume_elapsed"
    - path: "vmd/smoke/phase16_gametab_smoke.tcl"
      provides: "Extended load-gate/behavior asserts: the two buttons + command literals + the resume rebase math driven headlessly through the real countdown chain"
      contains: "resume_elapsed"
  key_links:
    - from: "vmd/gui/game_tab.tcl begin-row buttons"
      to: "vmd/gui/dialog.tcl handlers (20-09)"
      via: "pinned 0-arg handler names on_save_game / on_import_game (cross-plan contract; call-time resolution)"
      pattern: "on_save_game|on_import_game"
    - from: "vmd/gui/game_tab.tcl countdown_step GO branch"
      to: "vmd/lib/game_logic.tcl timer_start"
      via: "the optional now argument (game_logic.tcl:151-158) — the resume seam"
      pattern: "timer_start"
---

<objective>
Land the Game-tab persistence seam: the resume_elapsed timer-rebase channel through the countdown GO branch (zero lib changes), the Save/Import begin-row buttons, and the load-gate smoke extension proving both headlessly.

Purpose: the Import/resume workflow (GAME-04) hinges on this seam — the timer must resume from the saved elapsed exactly at GO, and the buttons must exist for 20-09's handlers.
Output: game_tab.tcl seam + buttons; extended phase16_gametab_smoke.tcl.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-persistence-combined-pdb-bcm-json/20-RESEARCH-integration-gui.md
@vmd/gui/game_tab.tcl
@vmd/smoke/phase16_gametab_smoke.tcl
@.planning/phases/19-in-game-actions/19-08-SUMMARY.md

Load-bearing facts:
- Pack mechanics (19-08 §3.2): bottom packs stack upward — pack the new begin-row frame `$parent.beginrow` `-side bottom -fill x` AFTER `pack $af -side bottom ...` (the 19-08 Actions frame) so it lands ABOVE Actions. DRIFT-RECHECK: read the LANDED build proc + 19-08-SUMMARY for the exact frame names/pack order (the Actions labelframe + status row landed there).
- Buttons (labels ASCII, matching the existing "..." style): `ttk::button $br.import -text "Import puzzle..." -command {::biochemeleon::on_import_game}` and `ttk::button $br.save -text "Save checkpoint" -command {::biochemeleon::on_save_game}` (pinned names — the cross-plan contract with 20-09; call-time resolution means headless load-gates pass before the handlers land).
- resume_elapsed: declare `variable resume_elapsed` (ONE per line — the 14-04 rule) in the vars block; NOT widget-bound; initialize lazily via info exists (the load-gate asserts widget vars unset — a non-widget var may init to 0 at source; prefer lazy init to keep the source-time state clean).
- GO branch (countdown_step, game_tab.tcl:234-270): AFTER `begin_play` and AFTER pick_bridge::activate, consume the seam: `if {[info exists resume_elapsed] && $resume_elapsed > 0} { game_logic::timer_start [expr {[clock seconds] - $resume_elapsed}] }` then `set resume_elapsed 0` UNCONDITIONALLY (belt-and-braces: the read+clear is atomic within the branch; a later fresh Start can never inherit a stale rebase).
- reset point: the landed 19-08 reset_view_state (drift-recheck its exact name/shape) also sets resume_elapsed 0 — add the line there too (belt-and-braces reset point per the integration research risk 8).
- The rebase math is headless-testable: drive round_reset -> begin_countdown -> countdown_tick x4 -> begin_play (the pure machine) with resume_elapsed preset, then assert timer_epoch == [clock seconds] - preset (±1 s tolerance for the clock crossing) and that the var read back 0.
- Smoke conventions: failure-list accumulation + BCHM_SMOKE_RESULT PASS=1 FAIL=none marker; runner greps the marker, never $?; full-log scans for ERROR)/bad switch.
</context>

<tasks>

<task type="auto">
  <name>Task 1: resume_elapsed seam — GO-branch rebase + unconditional clear</name>
  <files>vmd/gui/game_tab.tcl</files>
  <action>
DRIFT-RECHECK the landed build/countdown_step/reset_view_state (19-08) first. Then: (1) declare `variable resume_elapsed` in the game_tab vars block (lazy init discipline); (2) in countdown_step's GO branch, after begin_play + activate, add the pinned consume block (rebase when > 0, clear unconditionally — comment pins the leak rationale); (3) add `set resume_elapsed 0` to the landed reset_view_state (or the equivalent round-reset point — the exact proc name from 19-08-SUMMARY). No other proc changes; start_round untouched.
Commit: `feat(20-08): game_tab resume_elapsed seam (GO-branch timer rebase, zero lib changes)`.
  </action>
  <verify>
Extended phase16_gametab_smoke (Task 2) includes the rebase case and passes; `grep -c "resume_elapsed" vmd/gui/game_tab.tcl` >= 4 (declare + GO consume + clear + reset point); the clear is unconditional in the GO branch (read the code).
  </verify>
  <done>The timer resumes from any preset elapsed exactly at GO and can never leak into a subsequent fresh round.</done>
</task>

<task type="auto">
  <name>Task 2: begin-row buttons + gametab smoke extension</name>
  <files>vmd/gui/game_tab.tcl, vmd/smoke/phase16_gametab_smoke.tcl</files>
  <action>
1. build: after the landed Actions-frame pack, create `$parent.beginrow` (ttk::frame), add the two buttons with the PINNED literals, `pack $br.import $br.save -side left -padx 4 -pady 4` then `pack $br -side bottom -fill x` (AFTER the $af pack line — upward stacking). Header comment: GAME-04/GAME-09 row, handlers land in 20-09.
2. Extend vmd/smoke/phase16_gametab_smoke.tcl (additive section, same harness): (a) build-load-gate: the beginrow frame + both buttons exist with the exact command literals; (b) resume math: preset ::biochemeleon::game_tab::resume_elapsed 100 -> drive round_reset/begin_countdown/4 ticks/begin_play through the REAL countdown_step? (countdown_step needs the widget — drive the pure machine + call the consume logic via a real countdown_step ONLY if the smoke's existing harness already builds the tab; else assert via the GO branch's observable: timer_epoch after begin_play + a manual invocation of the same consume code path — follow the smoke's existing pattern for widget-free driving; the marker line records which path was driven) -> assert timer_epoch ≈ clock-100 and resume_elapsed == 0 after; (c) regression: the pre-existing gametab asserts still pass (additive only).
Keep the smoke's conventions: catch+_bail, marker line, no $?.
Commit: `test(20-08): gametab smoke — begin-row buttons + resume rebase asserts`.
  </action>
  <verify>
`bash -ic "vmd -dispdev text -e vmd/smoke/phase16_gametab_smoke.tcl -eofexit" < /dev/null` from fresh staging -> PASS=1 (x3 runs for the updated smoke per the full-suite convention), 0 ERROR)/bad switch, Exiting normally. Pre-existing smoke asserts all still green.
  </verify>
  <done>The Game tab carries the two persistence buttons above Actions, and the resume seam is proven headlessly through the real GO branch.</done>
</task>

</tasks>

<verification>
- Smoke PASS=1 x3, sequential; 0 ERROR)/0 bad switch; 8.6-idiom grep == 0; grab-set grep == 0.
- The begin-row packs after $af (upward stacking renders it ABOVE Actions — recorded in the SUMMARY with the pack-order quote).
- Handler names on_save_game/on_import_game match 20-09's pinned proc names exactly (cross-check the strings).
</verification>

<success_criteria>
The Game-tab persistence surface exists and its timer-resume seam is headless-proven — GAME-04's delivery rail is ready for 20-09's handlers.
</success_criteria>

<output>
After completion, create `.planning/phases/20-persistence-combined-pdb-bcm-json/20-08-SUMMARY.md` (pack-order evidence, the rebase assert recipe, smoke totals).
</output>
