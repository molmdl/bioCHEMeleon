---
phase: 20-persistence-combined-pdb-bcm-json
plan: 13
type: execute
wave: 7
depends_on: ["20-09", "20-10"]
files_modified: [vmd/tests/persistence_verify.tcl]
autonomous: true

must_haves:
  truths:
    - "persistence_verify.tcl exists as the GUI-session driver (19-16 actions_verify pattern): Tk-guarded, 3-paste budget (source / pv_p_mid / pv_p_finish), auto-logging every state read to tmp/p20session/persistence_verify_log.txt"
    - "The driver stages the full checkpoint session: a playable mixed-tier round, a scripted find, Save-dialog instructions with a PINNED file path, an Import-resume leg with timer/counter/visual auto-reads, a Generate&export leg, and refusal-path instructions — every dump line names the G-item it evidences"
    - "A definition-gate run passes headlessly: fake ::tk_version + source -> all pv_p_* procs defined, 0 ERROR) / 0 bad switch, Exiting normally"
  artifacts:
    - path: "vmd/tests/persistence_verify.tcl"
      provides: "The pv_p_* driver family + the paste instructions + the auto-log watcher"
      contains: "pv_p_"
  key_links:
    - from: "vmd/tests/persistence_verify.tcl"
      to: "the human GUI session (20-14)"
      via: "3 pastes + real dialog interactions; the log is the verdict evidence"
      pattern: "persistence_verify_log"
---

<objective>
Build the GUI-session driver for the consolidated checkpoint (the 19-16 pattern): a Tk-guarded script that stages the save/import/export session, auto-logs every observable (stash shape, registry statuses, timer labels, numreps, user2/user3 read-backs, file existence), and hands the human a 3-paste script with pinned file paths.

Purpose: 20-14's verdict table needs per-item evidence; headless text mode cannot click dialogs — the driver makes the human session short and the evidence objective.
Output: vmd/tests/persistence_verify.tcl (+ gitignored session artifacts under tmp/p20session/).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-in-game-actions/19-16-PLAN.md
@.planning/phases/19-in-game-actions/19-17-PLAN.md
@.planning/phases/20-persistence-combined-pdb-bcm-json/20-09-SUMMARY.md

Load-bearing facts:
- 19-16's actions_verify.tcl is the pattern: Tk-guarded (fake ::tk_version definition-gate), procs named pv_*; here pv_p_* (persistence). Auto-log via a file appender (logproc writing timestamped lines). The driver's dumps read: game_logic state/elapsed, registry count/remaining/statuses, atomselect user2/user3 floats on hider indices, molinfo numreps + viewpoint, the stash dict shape, and the saved-file existence + sidecar kind (via the landed persistence::read_sidecar — headless-safe calls from the GUI session).
- Session shape (3 pastes, mirroring 19-17's steps): PASTE 1 `source vmd/tests/persistence_verify.tcl` -> loads 1znf, starts a 6-hider round, arms the first-click guidance, dumps state, prints instructions: press p, find 2 hiders, click Save checkpoint, save to the PINNED path tmp/p20session/checkpoint.bcmz (Windows path form), note the timer before/after the dialog. PASTE 2 `pv_p_mid` -> dumps post-save state (timer did not bill the dialog — read the elapsed delta), then instructs: click Import puzzle..., pick the SAME file, verify the countdown + resumed timer + found hiders still green; find one more (scores). PASTE 3 `pv_p_finish` -> dumps the resumed round evidence (counters, statuses, viewpoint), then instructs the export leg: Setup tab -> Generate & export -> save to tmp/p20session/puzzle.bcmz -> success box + Cleanup restores; then the refusal legs (Save while idle; a corrupt-file import) — each with the expected box copy.
- The driver NEVER automates dialogs (tk_getSaveFile/OpenFile are human-driven) — it stages, dumps, and instructs. The 3-paste budget is the standing 16-16 directive class.
- Staging: the human stages fresh (cp -r vmd tmp/biochemeleon-vmd20/) — the driver says so in paste-1 output; paths in instructions are the Windows forms.
</context>

<tasks>

<task type="auto">
  <name>Task 1: persistence_verify.tcl — pv_p_* driver family + session instructions</name>
  <files>vmd/tests/persistence_verify.tcl</files>
  <action>
Build the driver per the 19-16 pattern: Tk guard (the whole body behind `if ![info exists tk_version] { vmdcon -warn "...GUI only"; return }` — the definition-gate fakes it), the auto-log appender, and procs: pv_p_start (stage + round + dump + paste-2 instructions), pv_p_mid (post-save dump + import instructions), pv_p_finish (post-import dump + export + refusal instructions + cleanup watcher), plus small helpers (pv_p_dump writing the labeled lines). Every dump line names its G-item (G1..G10 of the 20-14 table). Pinned file paths in the instructions: tmp/p20session/checkpoint.bcmz + puzzle.bcmz (Windows forms). NO dialog automation; no grab set; Tcl 8.5 only.
Commit: `test(20-13): persistence_verify.tcl GUI-session driver (pv_p_* family)`.
  </action>
  <verify>
Definition-gate: `bash -ic "vmd -dispdev text -e <staging>/gate13.tcl -eofexit" < /dev/null` where gate13.tcl fakes ::tk_version then sources vmd/tests/persistence_verify.tcl -> all pv_p_* procs defined, 0 ERROR)/bad switch, Exiting normally.
  </verify>
  <done>The checkpoint driver exists, defines cleanly headless, and hands the human a 3-paste session with objective auto-logged evidence.</done>
</task>

</tasks>

<verification>
- Definition-gate green; 8.6-idiom + grab-set greps zero on the driver.
- The instruction texts quote the pinned box copy from 20-09-SUMMARY (cross-check at least the Save-refusal and export-success strings).
</verification>

<success_criteria>
The human session is reduced to 3 pastes + real clicks with every verdict auto-evidenced — the 19-16/17 pattern carried to Phase 20.
</success_criteria>

<output>
After completion, create `.planning/phases/20-persistence-combined-pdb-bcm-json/20-13-SUMMARY.md` (the EXACT paste names + instruction texts — 20-14 quotes them verbatim).
</output>
