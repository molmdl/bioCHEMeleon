---
phase: 20-persistence-combined-pdb-bcm-json
plan: 07
type: execute
wave: 4
depends_on: ["20-05"]
files_modified: [vmd/gui/dialog.tcl, vmd/gui/setup_tab.tcl]
autonomous: true

must_haves:
  truths:
    - "Generate & export produces a shareable .bcmz of the INITIAL game state WITHOUT starting the game: game::start_game runs (molecule mutated, registry populated, hider reps rendered) but game_tab::start_round is NEVER called — game_logic::state stays 'idle', the timer never arms, picks no-op (PINNED: no dry-run flag needed — start_game touches zero game_logic state)"
    - "on_start and on_export_game share ONE prepare helper extracted behavior-preservingly from on_start steps 1-3 (collect_state -> target resolution -> validate_state), with the landed 5-arg start_game + material-toggle gating threaded verbatim"
    - "Export cancel = game::cleanup of the just-generated round + stash clear (no dangling hiders); export success = the v1-verbatim info box, stays on Setup; the exported round's gs is stashed into game_tab::game_state so the landed on_cleanup admits it (educator previews + Cleanup restores)"
  artifacts:
    - path: "vmd/gui/dialog.tcl"
      provides: "::biochemeleon::_prepare_setup (shared helper), ::biochemeleon::on_export_game (BTN-05 handler)"
      contains: "on_export_game"
    - path: "vmd/gui/setup_tab.tcl"
      provides: "Generate & export button in build_actions between Load Setup... and Start (order: reset/random/save/load/export/start)"
      contains: "Generate & export"
  key_links:
    - from: "vmd/gui/dialog.tcl on_export_game"
      to: "vmd/lib/game.tcl save_game (20-05)"
      via: "save_game $gs $fname puzzle $validated_setup — the kind=puzzle forcing rides build_bcm_dict"
      pattern: "save_game"
    - from: "vmd/gui/setup_tab.tcl build_actions"
      to: "vmd/gui/dialog.tcl on_export_game"
      via: "-command {::biochemeleon::on_export_game} (BTN-07 fan-in literal style)"
      pattern: "on_export_game"
---

<objective>
Wire BTN-05 (Generate & export) end-to-end on the Setup tab: the behavior-preserving prepare extraction shared with Start, the export handler (generate-only + dialog + kind=puzzle save + cancel cleanup), and the button between Load Setup and Start per spec order.

Purpose: the educator path of the persistence triad — generate, share, and keep the scene previewable — with v1's shipped UX semantics ported verbatim.
Output: _prepare_setup + on_export_game in dialog.tcl; the button in setup_tab.tcl.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-persistence-combined-pdb-bcm-json/20-RESEARCH-integration-gui.md
@vmd/gui/dialog.tcl
@vmd/gui/setup_tab.tcl
@.planning/phases/18-materials-exploration/18-10-SUMMARY.md

Load-bearing facts:
- on_start TODAY (dialog.tcl:143-226): steps 1-3 (collect_state -> resolve per target_mode with the three error boxes -> validate_state vs atom_count), step 3.5 pick_bridge::deactivate (16-14 gap-1), step 4 start_game, step 5 set_difficulty, step 6 raise_tab, step 7 start_round. 18-10 patches the start_game call to 5 args with the per_mat toggle gating (C2) — DRIFT-RECHECK: read the LANDED on_start first; the extraction must preserve its exact shape (5-arg call, gating, error boxes verbatim).
- The extraction: `proc ::biochemeleon::_prepare_setup {}` -> returns [list $state $molid] on success, empty list on failure (the error boxes are shown INSIDE, exactly as today). on_start becomes: prep -> empty? return : catch deactivate -> start_game -> set_difficulty -> raise_tab -> start_round. BEHAVIOR-PRESERVING: same boxes, same order, same abort points (v1's _prepare_and_start precedent was verified as a safe extraction in 08-04).
- on_export_game (W3, integration research): prep (shared) -> catch {pick_bridge::deactivate} -> set gs [start_game ...5-arg...] (failure -> warning box "Could not generate the game: $gs" + return, no partial state) -> STASH `set ::biochemeleon::game_tab::game_state $gs` (C4 — Cleanup admits it via _game_stash) -> tk_getSaveFile -defaultextension ".bcmz" -title "Generate & export puzzle" -filetypes [list {{bioCHEMeleon game} {.bcmz}} {{All files} {*}}] -parent $w -> cancel: game::cleanup $gs + clear the stash ([dict create]) + return (C5) -> catch around build+write: save_game $gs $fname puzzle $validated_state (the validate_state output from prep) — failure -> warning box "Export failed: $err" + KEEP the stash (retry possible) -> success: info box "Saved puzzle to:\n%s\n\nYour model still has the generated hiders. Press Cleanup to restore your scene." (v1-verbatim-able) -> STAY on Setup (no raise_tab, no start_round, no countdown).
- Button: build_actions row becomes reset/random/save/load/EXPORT/start — ttk::button $f.export -text "Generate & export" -command {::biochemeleon::on_export_game}, packed between $f.load and $f.start (spec.md order; ASCII "..." matches the existing button style).
- HEADLESS-TESTABLE: the generate-only invariant (state idle, timer_elapsed 0, on_pick no-op) is assertable in the 20-11 smoke; this plan's own verify is a text-mode load-gate (all procs defined, no Tk executed) + a grep-level behavior audit.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract _prepare_setup from on_start (behavior-preserving) </name>
  <files>vmd/gui/dialog.tcl</files>
  <action>
DRIFT-RECHECK first: read the LANDED on_start (post-18-10: 5-arg start_game + per_mat toggle gating) — the extraction must carry its exact current body. Create `proc ::biochemeleon::_prepare_setup {}` containing TODAY's steps 1-3 verbatim (collect_state catch-box; target_mode resolution with the three branches + boxes; validate_state catch-box) returning [list $state $molid] or {} on any abort. Rewire on_start to call it (prep -> if empty return; then steps 3.5-7 unchanged). NOTHING else changes: same box texts, same variable names, same abort semantics. Tcl 8.5 (catch, braced expr); no new Tk widgets.
Commit: `refactor(20-07): extract shared _prepare_setup from on_start (behavior-preserving, post-18 5-arg shape)`.
  </action>
  <verify>
Text-mode load-gate probe (tmp/p20-07/gate1.tcl, fake ::tk_version pattern from 17.2-12 if needed — or plain source in -dispdev text): source the GUI chain -> `_prepare_setup` + `on_start` defined, 0 ERROR)/bad switch, Exiting normally. Diff-audit: the three error box message strings in dialog.tcl are byte-identical pre/post (git diff shows them moved, not edited).
  </verify>
  <done>on_start routes through the shared helper with zero behavior change, proven by load-gate + diff audit.</done>
</task>

<task type="auto">
  <name>Task 2: on_export_game + Setup-tab Generate & export button</name>
  <files>vmd/gui/dialog.tcl, vmd/gui/setup_tab.tcl</files>
  <action>
1. `proc ::biochemeleon::on_export_game {}` in dialog.tcl per the W3 state machine (context above): shared prep -> deactivate -> start_game (5-arg landed shape) -> stash -> dialog -> cancel-cleanup / catch-write / success box / stay on Setup. The stashed gs enables on_cleanup (landed 19-10) and on_restart replay (documented, acceptable — C6: same-count replay of the exported round).
2. setup_tab.tcl build_actions: add $f.export between load and start with the exact literal `-text "Generate & export"` / `-command {::biochemeleon::on_export_game}`; update the pack order + the proc's header comment (BTN-05).
3. Header comments: on_export_game documents the generate-only invariant (start_game without start_round; state stays idle; picks no-op via the on_pick state gate) and the C4/C5 stash semantics.
Commit: `feat(20-07): on_export_game + Setup-tab Generate & export button (BTN-05)`.
  </action>
  <verify>
Text-mode load-gate probe: source the GUI chain -> on_export_game defined; grep `-text "Generate & export"` in setup_tab.tcl == 1; grep `-command {::biochemeleon::on_export_game}` == 1; the pack order places $f.export between $f.load and $f.start (read the pack line). Grab-set grep over vmd/gui/ still 0. 8.6-idiom grep still 0.
  </verify>
  <done>BTN-05 is wired: generate-only export with v1 cancel/success semantics, the button in spec order, and the stash making Cleanup work post-export.</done>
</task>

</tasks>

<verification>
- Load-gate probe green (all procs defined, 0 ERROR)/bad switch, Exiting normally).
- `grep -rn "grab set" vmd/gui/` == 0; 8.6-idiom grep == 0.
- on_start's error-box strings byte-identical (git diff audit); the 5-arg start_game call + toggle gating preserved verbatim.
- The generate-only invariant is left to 20-11's smoke (asserted there headlessly).
</verification>

<success_criteria>
The Setup tab can generate + export a shareable .bcmz without starting the game, through one shared prepare with Start — v1's educator UX on the post-18 seam.
</success_criteria>

<output>
After completion, create `.planning/phases/20-persistence-combined-pdb-bcm-json/20-07-SUMMARY.md` (extraction diff summary, handler contract, C4/C5/C6 notes for the 20-14 checkpoint).
</output>
