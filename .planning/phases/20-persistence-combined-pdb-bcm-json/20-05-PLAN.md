---
phase: 20-persistence-combined-pdb-bcm-json
plan: 05
type: execute
wave: 2
depends_on: ["20-02", "20-03", "20-04"]
files_modified: [vmd/lib/game.tcl]
autonomous: true

must_haves:
  truths:
    - "save_game on a live round writes a .bcmz containing exactly game.pdb + game.bcm, where the sidecar dict carries: registry (every record {index rep status} + the resid_block), timer_elapsed, reveal/hint counts, found_colorid, found_visible, hider_count, original_pdb (the PRE-game snapshot's filename — never the combined temp path), viewer (viewpoint + the FULL live rep list incl. hider pairs), and the setup dict verbatim"
    - "kind=puzzle forces started 0 / timer 0 / counters 0 via build_bcm_dict (the v1 discriminator — kind is the only byte difference from a checkpoint of the same state)"
    - "The live game molecule is untouched by save_game (beta restored to -999, no mol delete, no rep changes) — the round continues live after a save"
  artifacts:
    - path: "vmd/lib/game.tcl"
      provides: "game::save_game {gs path {kind checkpoint} {setup {}}} — the save-side composition: registry snapshot + game_logic reads + persistence build + persistence_mol write/zip"
      contains: "proc ::biochemeleon::game::save_game"
  key_links:
    - from: "vmd/lib/game.tcl save_game"
      to: "vmd/lib/persistence.tcl build_bcm_dict (20-02)"
      via: "assembles the live dict and delegates schema + forcing"
      pattern: "build_bcm_dict"
    - from: "vmd/lib/game.tcl save_game"
      to: "vmd/lib/registry.tcl records_snapshot/resid_block (20-03)"
      via: "bulk registry read at save time"
      pattern: "records_snapshot"
    - from: "vmd/lib/game.tcl save_game"
      to: "vmd/lib/persistence_mol.tcl (20-04)"
      via: "write_game_pdb + bcmz_write into a unique $env(TEMP) staging dir"
      pattern: "write_game_pdb|bcmz_write"
---

<objective>
Land the save-side orchestration in the composition root: game::save_game turns a live game_state + a target path into a shareable .bcmz (SC1). One proc, one concern — the GUI handler (20-09) only adds dialogs, guards, and the timer rebase.

Purpose: everything capture-shaped (registry, counters, colors, viewer, setup, original path) is read HERE through the landed seams, so the headless smoke (20-11) can prove the full save half without Tk.
Output: game::save_game in vmd/lib/game.tcl, probe-verified headless.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-persistence-combined-pdb-bcm-json/20-RESEARCH-v1-carryover-format.md
@.planning/phases/20-persistence-combined-pdb-bcm-json/20-RESEARCH-integration-gui.md
@vmd/lib/game.tcl
@.planning/phases/19-in-game-actions/19-02-SUMMARY.md
@.planning/phases/19-in-game-actions/19-04-SUMMARY.md
@.planning/phases/18-materials-exploration/18-07-SUMMARY.md

Load-bearing seams (drift-recheck ALL at execution — they are Phase 18/19 contracts):
- gs shape: {game_molid hider_count snapshot per_rep} today; 18-07 adds per_mat (5-key). Read per-key with dict exists (never assume arity).
- original_pdb = `dict get $gs snapshot filename` — the PRE-mutate backup::snapshot (game.tcl builds gs with the pre-game snapshot). NEVER backup::snapshot of the LIVE game molecule for this field (its filename is the combined temp path).
- viewer viewpoint + reps = backup::snapshot of the LIVE game molecule (viewpoint + reps keys only; its filename/molid are ignored). Reps include the 2N hider pairs — the loader filters them (20-06); the save side captures FULL (v1-carryover §3.4 example shows both a scene rep and a GAM rep in viewer.reps).
- Counters: game_logic::reveal_count / hint_count 0-arg getters (P-19-02 landed). timer_elapsed: game_logic::timer_elapsed (frozen final if won — the lib allows a won save; the GUI blocks it, pinned decision). state via game_logic::state -> started = 1 when playing (won can't reach here through the GUI).
- found_colorid = ::biochemeleon::hiders::found_colorid (P-19-04 namespace var; info-exists default 7). found_visible = ::biochemeleon::hiders::found_visible (P-19-04 var; info-exists default 1) — defensive reads, drift-recheck the exact landed var names in 19-04-SUMMARY.
- resid_block = registry::resid_block (20-03); records = registry::records_snapshot (20-03).
- Staging: unique dir under $::env(TEMP) (clock clicks), members game.pdb + game.bcm, bcmz_write handles the atomic rename, staging dir always deleted (catch-wrapped).
- game.tcl references other namespaces at CALL time only; add NO source lines (20-10 owns the entry).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drift-recheck the Phase 18/19 capture seams against the landed code</name>
  <files>.planning/phases/20-persistence-combined-pdb-bcm-json/20-05-SUMMARY.md</files>
  <action>
BEFORE writing save_game, read the LANDED files + SUMMARYs and record the verified contract in the SUMMARY (this plan's drift-recheck deliverable — the integration research flags every one of these as a hypothesis with a pinned source):
1. game_logic: confirm reveal_count/hint_count getter names + 0-arg form, timer_elapsed semantics, and that round_reset did not gain new behavior (19-02-SUMMARY + the landed vmd/lib/game_logic.tcl).
2. hiders: confirm the found_colorid / found_visible namespace var names + defaults, and whether getters exist (19-04-SUMMARY + landed vmd/lib/hiders.tcl); if getters exist, prefer them.
3. gs shape: confirm the landed 5-key game_state from 18-07-SUMMARY (per_mat key name + resolved shape) — save_game must NOT fail on either 4-key or 5-key gs (defensive dict exists).
4. Record any deltas in the SUMMARY with file:line evidence; if a seam landed differently than pinned, ADAPT save_game's reads to the landed reality (the contract is the landed code, not this plan's guess) and flag the delta.
  </action>
  <verify>
The SUMMARY contains a seam-verification table with file:line evidence for each of the four groups and states either "matches pin" or the delta + the adaptation made.
  </verify>
  <done>All capture seams verified against the landed code; deltas adapted and recorded before any orchestration code was written.</done>
</task>

<task type="auto">
  <name>Task 2: game::save_game — the save-side composition</name>
  <files>vmd/lib/game.tcl</files>
  <action>
Add to game.tcl (after cleanup/restart, before set_callbacks; additive — no landed proc edited):
`save_game {gs path {kind "checkpoint"} {setup {}}}` -> the built bcm dict.
1. Guard: `[dict size $gs] == 0` -> error "no game round to save" (the handler pre-guards with an info box; this is the caller-bug surface).
2. set game_molid / snapshot from gs (dict get; snapshot must exist — error "game state has no snapshot" otherwise).
3. Capture: elapsed = game_logic::timer_elapsed; reveal/hint via the landed getters; state = game_logic::state; started = expr {$state eq "playing" || $state eq "won"}; found_colorid/found_visible via the verified seam (info-exists defaults 7/1); records = registry::records_snapshot -> hiders list {index rep status} (dict for walk, sorted by index ascending — lsort -integer -index 0 over the pair list); resid_block -> {resid index} list sorted by resid ascending; viewer = backup::snapshot $game_molid (use viewpoint + reps keys); original_pdb = dict get $snapshot filename.
4. hider_count = dict get $gs hider_count (the P9 effective total — NOT registry::count_hiders, which equals it but the gs value is the round's own record).
5. bcm = persistence::build_bcm_dict $kind [dict create ...all of the above...]; setup passed VERBATIM (the caller supplies the validated setup dict — the GUI's collect_state->validate_state output; default {} = degraded-legal).
6. Staging + write: unique dir [file join $::env(TEMP) bchm_save_[clock clicks]]; persistence_mol::write_game_pdb $game_molid $stage/game.pdb; persistence::write_sidecar $stage/game.bcm $bcm; persistence_mol::bcmz_write $stage [list game.pdb game.bcm] $path; file delete -force $stage (catch-wrapped, always).
7. Return $bcm. The molecule/registry/timers are UNTOUCHED (assert in the probe: state still playing, timer_epoch unchanged, beta -999 restored, numreps unchanged).
Whole body catch-shaped so a failure mid-write never leaves a half-written .bcmz (bcmz_write's .part discipline covers the final file; the staging dir cleanup is the only extra).
Commit: `feat(20-05): game::save_game — save-side .bcmz composition (checkpoint + puzzle kinds)`.
  </action>
  <verify>
Headless probe (tmp/p20-05/probe_save.tcl, sequential, from repo root): start_game a 2-tier round (e.g. `start_game 3 [dict create VDW 2 Lines 2] 0` on 1k8p), registry::mark_found + hiders::mark_found_visual on 2 indices, advance the timer (begin_play happened at start via start_round? NO — headless probe drives the pure machine: round_reset/begin_countdown/tick x4/begin_play, sleep-free, then a couple of clock seconds naturally), call save_game with a small validated setup dict -> assert: (a) the .bcmz exists with PK magic; (b) extract via persistence_mol::bcmz_read -> parse the sidecar (persistence::read_sidecar + parse_bcm_dict) -> kind/magic/version correct; every found index has status found + the right rep; resid_block matches registry::resid_block (or empty on a simple round); timer_elapsed >= 0 int; reveal/hint present; original_pdb ENDS WITH the demo filename (1k8p.pdb) NOT biochemeleon_game.pdb; viewer.reps length == live numreps; setup round-trips. (c) live molecule untouched: beta -999, numreps unchanged, state still playing. (d) kind=puzzle variant: started/timer/counters all 0.
  </verify>
  <done>save_game produces a schema-valid .bcmz for both kinds with every capture field correct, and the live round provably survives the save.</done>
</task>

</tasks>

<verification>
- Probe green: PASS markers, 0 ERROR)/0 bad switch, Exiting normally, sequential run.
- `tclsh vmd/lib/game.tcl` loads clean (call-time references only).
- 8.6-idiom grep on game.tcl -> 0; no new `source` lines added (grep -c "^source" unchanged).
- The drift-recheck table exists in the SUMMARY with file:line evidence.
</verification>

<success_criteria>
A live round can be captured to a schema-valid, container-correct .bcmz headlessly — the full SC1 minus the GUI dialog — with the original-PDB provenance and live-molecule integrity proven.
</success_criteria>

<output>
After completion, create `.planning/phases/20-persistence-combined-pdb-bcm-json/20-05-SUMMARY.md` (seam-verification table + probe evidence + the save_game contract as landed).
</output>
