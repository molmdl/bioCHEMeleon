---
phase: 21
plan: 07
type: execute
wave: 4
depends_on: ["21-03", "21-05"]
files_modified: [vmd/gui/fetch_dialog.tcl, vmd/smoke/phase21_fetchdialog_smoke.tcl, vmd/biochemeleon.tcl]
autonomous: true
must_haves:
  truths:
    - "ensure_demo {demo_id w on_ready on_fail} is the ONE fetch orchestration entry: cache hit -> on_ready <molid>; miss -> progress UI + fetch::start_download, then an after-0 STEP CHAIN (load wet -> strip_and_cache -> promote/reload -> cleanup) with the Tk event loop pumped between steps (SC4's chunking clause applied in the pipeline)"
    - "Every step is catch-guarded: any failure destroys the progress UI, cleans .part files, and fires on_fail exactly once with a classified user-readable message — no partial cache, no raw Tcl error leaking"
    - "Re-entrancy: fetch_active flag (set on start, cleared on every exit path) — a second ensure_demo while active fails fast"
    - "Cancel: the progress window's Cancel closes the download channel, cleans the .part, clears the flag, and fires on_fail with a canceled message (v1's cancelable-dialog concept in Tcl form) — owning verification: the smoke's deterministic cancel leg drives the same _cancelled_cleanup path the button binds to and asserts the full contract headlessly"
    - "GUI rules hold: no grab set (modeless transient), ::tk_version qualifier, pipeline procs run headless (Tk widgets guarded) so the smoke can prove the full chain in text mode"
  artifacts:
    - path: "vmd/gui/fetch_dialog.tcl"
      provides: "namespace ::biochemeleon::fetch_dialog — ensure_demo state machine, progress toplevel (ttk::progressbar indeterminate + status label + Cancel), fetch_active guard"
    - path: "vmd/smoke/phase21_fetchdialog_smoke.tcl"
      provides: "headless smoke proving the full chain: error contract, cache-hit path, live-or-seeded fetch->strip->cache->load (network-tolerant)"
    - path: "vmd/biochemeleon.tcl"
      provides: "entry source line for gui/fetch_dialog.tcl (after demos.tcl, before gui/dialog.tcl)"
  key_links:
    - from: "vmd/gui/fetch_dialog.tcl ensure_demo"
      to: "vmd/lib/fetch.tcl start_download / integrity_gate"
      via: "non-blocking download with exactly-one-callback; parse + integrity per 21-03's shared gate"
      pattern: "fetch::(start_download|integrity_gate|cleanup_part)"
    - from: "vmd/gui/fetch_dialog.tcl step chain"
      to: "vmd/lib/demos.tcl strip_and_cache / load_cached_demo"
      via: "the after-0 steps call the 21-05 mol primitives"
      pattern: "demos::(strip_and_cache|load_cached_demo)"
    - from: "vmd/gui/dialog.tcl (21-08)"
      to: "vmd/gui/fetch_dialog.tcl ensure_demo / fetch_active"
      via: "on_start's fetched branch + re-entrancy guard"
      pattern: "fetch_dialog::(ensure_demo|fetch_active)"
---

<objective>
Build `vmd/gui/fetch_dialog.tcl` — the async fetch orchestration + progress UI module that turns a cache-missed fetched demo into a loaded molid without blocking the Tk event loop — plus its headless smoke and the entry source line.

Purpose: SC1/SC2's "fetch on demand" UX and SC4's >200ms discipline land here, BETWEEN the mol primitives (21-05) and the on_start restructure (21-08) — the phase's core new machinery (v1's worker-thread replacement per carryover verdict #13).
Output: `vmd/gui/fetch_dialog.tcl` + smoke + one entry source line.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/21-large-fetched-demos-attribution/21-RESEARCH-vmd-fetch-mechanics.md (RQ4: fileevent pipeline; probeF2 shape)
@.planning/phases/21-large-fetched-demos-attribution/21-RESEARCH-v1-carryover.md (§1: v1 drain/continuation concepts, verdict #13/16/17)
@vmd/lib/fetch.tcl
@vmd/lib/demos.tcl
@vmd/gui/dialog.tcl (for the $w toplevel convention + tk_messageBox idiom)
@vmd/biochemeleon.tcl

Key facts (do NOT re-derive — probe-verified where noted):
- **State machine (the v1 drain concept in Tcl form):** ensure_demo -> guard fetch_active -> is_cached ? `after 0 [list on_ready [demos::load_cached_demo $demo_id]]` (uniform async shape even on hit) : show_progress -> `fetch::start_download $url $wet [list ::biochemeleon::fetch_dialog::_download_done $demo_id $on_ready $on_fail]` -> _download_done ok: `fetch::integrity_gate $wout $wet <min>` (catch -> _fail) -> `after 0 [list _step_load_wet ...]` -> `after 0 [list _step_strip ...]` -> `after 0 [list _step_finish ...]` -> close progress -> on_ready molid. Wet path = `[fetch::cache_dir]/<demo_id>_wet.part.pdb`; min bytes per source: memprotmd 7000000 / sasbdb 300000 (same constants as demos.tcl — reference them, do not restate divergent values; simplest: call demos::fetch_and_cache's constants via a tiny shared proc OR duplicate the two integers with a comment pointing at demos.tcl — pick the shared-proc form ONLY if demos.tcl exposes one; otherwise the duplicated constants MUST carry the same comment anchor).
- **after-0 stepping is the SC4 deliverable** (mechanics RQ4: `after 0` chains verified headless; each VMD C call — mol new 95k atoms ~1.6s, writepdb ~0.9s — is atomic but steps interleave with the event loop so the progress window repaints; the research's "show a status label instead" for the atomic steps = update the label BETWEEN steps: "Downloading… / Stripping water and salt… / Caching… / Loading…").
- **Status label updates between steps** double as the progress surface (curl gives no progress events through this pipeline — v1's per-block % has no Tcl equivalent; an indeterminate ttk::progressbar + step labels is the approved shape).
- **Cancel:** button -> set `_cancelled 1` -> `catch {close $_active_ch}` (aborts curl) -> cleanup wet part -> clear flag -> destroy window -> on_fail "Fetch canceled." (the _download_done error path from the closed channel is suppressed when _cancelled).
- **Error discipline:** exactly one of on_ready/on_fail fires per ensure_demo call; on_fail receives a ONE-LINE classified message (from fetch::classify_curl_error or the step's catch); all .part cleanup via fetch::cleanup_part; `mol delete` any live wet mol (catch-guarded).
- **Headless honesty:** Tk widgets must be guarded (`info exists ::tk_version` — use the GLOBAL qualifier, 13-02 lesson) so the whole pipeline runs in text mode (the smoke proves it); the progress window simply doesn't build headless.
- **No grab set** (modeless — v1's QProgressDialog was modeless; AGENTS.md modeless gate).
- Entry source order (biochemeleon.tcl): gui/fetch_dialog.tcl goes AFTER the lib block (demos.tcl present) and BEFORE gui/dialog.tcl. ONE line + comment.
- Smoke harness (exact commands):
```
mkdir -p tmp/biochemeleon-vmd && cp -r vmd tmp/biochemeleon-vmd/ && { cp -r tmp/p21probe tmp/biochemeleon-vmd/ 2>/dev/null || true; }
timeout 600 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/smoke/phase21_fetchdialog_smoke.tcl -eofexit < /dev/null' 2>&1 | tail -40
```
- **Probe-seed staging convention (ONE rule across 21-05/21-07/21-09):** the harness copies the probe tree INTO staging (the `cp -r tmp/p21probe tmp/biochemeleon-vmd/` line above), so smoke code reads the literal staging-relative seed path `tmp/p21probe/<file>` from the staging cwd (it resolves to tmp/biochemeleon-vmd/tmp/p21probe/<file>). Smokes NEVER use `../` or repo-absolute seed paths; the copy is best-effort (`|| true` — fresh clones lack tmp/p21probe), and every seed use stays behind a `file exists` guard so absence routes to the live-network path. Census pins (19,221 / 4,123 / 2,601 / 25,974) assert whenever the seeds ARE present (the norm on this machine) — they are NOT silently network-dependent.
- Marker + x3 + full-log scan per repo contract. vwait-bounded waits (`after` safety timers, 60 s for live legs).
</context>

<tasks>

<task type="auto">
  <name>Task 1: fetch_dialog.tcl — state machine + progress UI + cancel</name>
  <files>vmd/gui/fetch_dialog.tcl, vmd/biochemeleon.tcl</files>
  <action>
Implement per the context facts: namespace ::biochemeleon::fetch_dialog; `fetch_active {}` reader; `ensure_demo {demo_id w on_ready on_fail}`; private _download_done/_step_load_wet/_step_strip/_step_finish/_fail/_cancelled_cleanup; `show_progress {w demo_id}` (guarded transient toplevel, indeterminate progressbar started, status label, Cancel button -> _cancelled_cleanup; NO grab); `set_status {text}` (no-op headless). Header comment: the state-machine diagram, the one-callback contract, the after-0/SC4 note, the cancel semantics, and the no-grab rule. Entry: add the source line per the context facts.
Commit: `feat(21-07): fetch_dialog.tcl — async fetch orchestration + progress UI (after-0 step chain)`
  </action>
  <verify>Headless source of biochemeleon.tcl (full entry chain) clean with Tk absent — one warn, procs defined; `grep -c "grab set" vmd/gui/fetch_dialog.tcl` -> 0; 8.6-idiom gate zero on both files.</verify>
  <done>Orchestration module + entry wiring in place; headless-source clean.</done>
</task>

<task type="auto">
  <name>Task 2: phase21_fetchdialog_smoke.tcl — full-chain proof (flake-proof)</name>
  <files>vmd/smoke/phase21_fetchdialog_smoke.tcl</files>
  <action>
Headless smoke (repo contract):
1. Deterministic legs (always run): (a) ensure_demo with an UNKNOWN id -> on_fail fired exactly once, message contains "unknown demo", fetch_active back to 0; (b) cache-hit leg: stage a small valid PDB at `<cache>/3gp6_dry.pdb` -> ensure_demo 3gp6 -> on_ready fired with a live molid (after-0 path exercised via vwait), progress UI never required; (c) bogus-URL leg: temporarily point a TEST-ONLY manifest-style call at https://no-such-host.invalid/x.pdb via fetch::start_download + integrity path -> on_fail contains "internet connection", no .part left, fetch_active 0; (d) CANCEL leg (the Cancel contract's owning verification, fully deterministic/offline): synthesize an in-flight state (set fetch_active 1, seed a wet .part at `<cache>/3gp6_wet.part.pdb`, no live channel needed), then invoke `fetch_dialog::_cancelled_cleanup` — the exact proc the Cancel button binds to (same cleanup/close code path; the binding itself is grep-verified as a one-liner to this proc in Task 1). Assert: on_fail fired EXACTLY ONCE with a message containing "cancel"; the seeded .part is gone; fetch_active back to 0; the suppression flag is set so the in-flight `_download_done` cannot double-fire (deliver a simulated channel-closed error to _download_done after cleanup and assert on_fail does NOT fire a second time). Assert the one-callback invariant on every leg (flag set at most once).
2. Live-or-seeded leg (network-tolerant): seed from the staging-copied `tmp/p21probe/` dry files if present (probe-seed staging convention — harness cp's tmp/p21probe into staging), else live `ensure_demo 3gp6` (and sasdpg4 -> assert 4,123 atoms + 2,601 glycan resn atoms on success): assert on_ready + final molid atom counts (3gp6 19,221 / sasdpg4 4,123) + cache file exists + wet part gone; on network-class error print `BCHM_SMOKE_NOTE SKIPPED-NO-NETWORK (<msg>)` and continue — never hard FAIL on ENOENT/6/7/28.
3. Marker `BCHM_SMOKE_RESULT PASS=1` per the rule; cleanup staging cache; run x3.
Commit: `test(21-07): fetch-dialog chain smoke (error/cancel contracts + network-tolerant full pipeline)`
  </action>
  <verify>PASS=1 x3 (or PASS=1 + recorded SKIPPED-NO-NETWORK verdicts in all three); ZERO `ERROR)` / `bad switch` in all full logs; `Exiting normally` every run; the cancel leg's assertions (on_fail exactly once with "cancel", .part gone, fetch_active 0, no double-fire after the simulated channel-close) hold in every passing run — deterministic, no network dependence.</verify>
  <done>The GUI-destined pipeline proven headless end-to-end; failure contracts and the FULL cancel contract (on_fail-once, .part cleanup, flag reset, suppression) pinned by the dedicated leg; connectivity cannot flake the gate.</done>
</task>

</tasks>

<verification>
- phase21_fetchdialog_smoke PASS=1 x3 with zero `ERROR)` / `bad switch` in all full logs
- One-callback invariant asserted on every leg; fetch_active never leaks
- Cancel contract exercised headlessly by leg (d): on_fail exactly once with "cancel", .part cleaned, fetch_active 0, _download_done suppression holds
- No grab set; 8.6 gates zero; entry source order verified (fetch_dialog before dialog.tcl)
- Atom-count pins (19,221 / 4,123 / 2,601) recorded whenever real data was present
</verification>

<success_criteria>
- SC1/SC2's fetch-on-demand machinery exists as a testable module before the dialog restructure consumes it
- 21-08 needs only ensure_demo + fetch_active — no pipeline knowledge
</success_criteria>

<output>
After completion, create `.planning/phases/21-large-fetched-demos-attribution/21-07-SUMMARY.md`
</output>
