---
phase: 21
plan: 04
type: execute
wave: 2
depends_on: ["21-01"]
files_modified: [DATA_SOURCES.md]
autonomous: true
must_haves:
  truths:
    - "DATA_SOURCES.md §2 no longer calls 1GZM a bacteriorhodopsin — the heading reads Bovine rhodopsin (matching line 91's correct citation and the RCSB header verified 2026-09-25); the fix is drafted NOW and formally approved at the 21-10 checkpoint"
    - "A v2 VMD-port processing note records: cache location vmd/data/demos/cache/, plain-PDB cache (the evidence-backed no-gzip deviation with probe evidence + revisit conditions), the strip mechanism (VMD atomselect 'not water and not ions', NEVER solvent, SOL/NA/CL equivalence) and the strip-vs-hider-cleanup distinction (v1 Pitfall C), SASDPG4 never stripped (2,601 glycan HETATM), and the fetch transport (System32 curl + UA bioCHEMeleon/2.0, pending approval)"
    - "§5 is retitled from '## 5. PyMOL' to '## 5. Graphics systems (PyMOL / VMD)' — the retitle lives inside the same pending-approval delta — and notes VMD 1.9.3 as the v2 graphics system beside PyMOL"
    - "No historical errors are reintroduced: the 404'd DOI 10.1038/s41592-018-0220-9 and the oxy.ac.uk typo stay absent; the 'glycorprotein' published-title typo stays verbatim (citation fidelity)"
  artifacts:
    - path: "DATA_SOURCES.md"
      provides: "repo-root attribution — v1-approved citation texts (unchanged) + corrected 1GZM heading + new v2 processing-note section"
  key_links:
    - from: "DEMO_MANIFEST citation keys (21-01)"
      to: "DATA_SOURCES.md sections"
      via: "1GZM / 3GP6 / SASDPG4 cross-ref keys resolve to the §2/§3 blocks"
      pattern: "1GZM|3GP6|SASDPG4"
    - from: "vmd/data/demos/SOURCES.md (stub, unchanged)"
      to: "DATA_SOURCES.md"
      via: "the 2-line pointer stub stays the single entry point for demo sources"
      pattern: "DATA_SOURCES"
---

<objective>
Draft the Phase-21 documentation deltas in the repo-root `DATA_SOURCES.md`: the 1GZM heading fix and the v2 VMD-port processing note — all citation TEXT reuse stays verbatim (human-approved 2026-08-16); every NEW/edited line is flagged for the 21-10 consolidated approval checkpoint.

Purpose: DEMO-03 requires the SASBDB source+IDs cited in documentation and DEMO-04 requires reusing v1's attribution; the roadmap's "compress before caching" deviation and the strip mechanics must be durably recorded where a human will review them.
Output: edited `DATA_SOURCES.md` (grep-verifiable), ready for checkpoint approval.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/21-large-fetched-demos-attribution/21-RESEARCH-v1-carryover.md
@.planning/phases/21-large-fetched-demos-attribution/21-RESEARCH-sources-and-integration.md
@DATA_SOURCES.md
@vmd/data/demos/SOURCES.md

Key facts (do NOT re-derive):
- **Heading bug (sources research §1, [HUMAN-APPROVAL-NEEDED]):** `DATA_SOURCES.md:98` says "### 1GZM — bacteriorhodopsin (Very challenging)"; line 91 and the cited paper (Li et al. 2004) and the RCSB header (`STRUCTURE OF BOVINE RHODOPSIN...`) say **Bovine rhodopsin**. Fix the heading text only — the citation block beneath it is already correct and approved.
- **Wrong values that must STAY absent** (v1 verified their removal, 09-VERIFICATION.md:96): DOI `10.1038/s41592-018-0220-9` (404s) and domain `oxy.ac.uk` (typo for `ox.ac.uk`).
- **Verbatim-fidelity rule:** SASBDB's published-title typo "glycorprotein" is reproduced ON PURPOSE (citation fidelity, 09-04-SUMMARY:47) — do NOT "fix" it.
- **The v2 processing note (new section, content fixed by research):** cache `vmd/data/demos/cache/` (planner decision for roadmap's `data/demos/cache/`); plain-PDB cache — VMD 1.9.3 Windows cannot read .gz (probeB/probeB2: extension AND explicit-type failures on a valid gzip; DEVIATION from DEMO-02's "compress" wording, revisit only if VMD gains gzip-PDB reading or Tcl 8.6 zlib); strip = `atomselect "not water and not ions"` (probe-exact: 3gp6 75,789 SOL + 116 NA + 113 CL stripped -> 19,221 dry; NEVER `solvent` — includes DPP membrane lipids); strip ≠ hider-cleanup (game-time cleanup is sentinel-only `resname GAM and beta < 0` — v1 Pitfall C distinction); SASDPG4 NEVER stripped (all 2,601 HETATM are glycan); transport = Windows System32 curl with UA `bioCHEMeleon/2.0` [pending approval]; 1GZM dry actuals to be backfilled at first fetch (v1 left the same TODO at DATA_SOURCES.md:105-106).
- **DEMO-04 license gate:** the MemProtMD CC-BY 4.0 + RCSB CC0 + SASBDB texts are reused VERBATIM; the formal license re-verification is the 21-10 checkpoint ("the checkpoint approval is the formal record", v1 09-04 precedent). This plan does NOT rewrite any approved citation text.
- Docs-only plan: verification is grep-based; no headless VMD run required (no code changes).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the 1GZM heading + add the v2 processing-note section</name>
  <files>DATA_SOURCES.md</files>
  <action>
1. Line 98 heading: "bacteriorhodopsin" -> "Bovine rhodopsin" (keep the "(Very challenging)" tier suffix exactly). Nothing else in §2 changes.
2. Add a new section (after §3 SASBDB, before §4 — or as a clearly-titled subsection of §2/§3 if the file's structure favors it; pick the least disruptive insertion) titled to convey "v2 VMD port — processing notes". Content per the context facts: cache location + gitignore note; the plain-PDB cache deviation (probe evidence, decision, revisit conditions — cite the probe logs by name: tmp/p21probe/probeB.log, probeB2.log); the strip mechanism + solvent trap + strip-vs-hider-cleanup distinction; the SASDPG4 never-strip rule with the 2,601-glycan-HETATM reason; transport (curl + UA, marked pending checkpoint approval); a "1GZM dry actuals: to be measured at first fetch" backfill note (completes v1's TODO when the 21-10 capstone measures it).
3. §5 (line 197 is literally `## 5. PyMOL`): retitle the heading to `## 5. Graphics systems (PyMOL / VMD)` — the retitle is part of the SAME pending-approval delta (the 21-10 checkpoint approves this plan's whole diff anyway) — then add one line noting the v2 viewer is VMD 1.9.3 (Tcl port) alongside PyMOL 2.5.0; the existing Schrödinger sentence stays verbatim; no citation text invented (VMD's own citation is explicitly out of Phase 21 scope per the carryover research).
Mark the whole delta block with a brief HTML comment or bracketed note `[Phase-21 edits — approval pending at the 21-10 checkpoint]` so the reviewer sees exactly what is new.
Commit: `docs(21-04): 1GZM heading fix + v2 VMD processing notes (approval pending)`
  </action>
  <verify>
```
grep -n "bacteriorhodopsin" DATA_SOURCES.md            # -> no matches
grep -c "10.1038/s41592-018-0220-9" DATA_SOURCES.md    # -> 0
grep -c "oxy.ac.uk" DATA_SOURCES.md                    # -> 0 (ox.ac.uk lines remain)
grep -n "glycorprotein" DATA_SOURCES.md                # -> >= 1 (verbatim typo preserved)
grep -n "not water and not ions" DATA_SOURCES.md       # -> >= 1 (strip note present)
grep -n "vmd/data/demos/cache" DATA_SOURCES.md         # -> >= 1 (cache note present)
grep -n "cannot read" DATA_SOURCES.md                  # -> >= 1 (deviation note present)
grep -n "^## 5" DATA_SOURCES.md                        # -> '## 5. Graphics systems (PyMOL / VMD)' (§5 retitle)
grep -n "VMD 1.9.3" DATA_SOURCES.md                    # -> >= 1 (v2 graphics-system line present)
```
  </verify>
  <done>Heading corrected; §5 retitled with the VMD line; v2 processing note records cache/deviation/strip/transport/never-strip/1gzm-backfill; approved texts byte-untouched; pending-approval marker present.</done>
</task>

<task type="auto">
  <name>Task 2: Stub + cross-ref integrity check</name>
  <files>vmd/data/demos/SOURCES.md (verify only)</files>
  <action>
Verify vmd/data/demos/SOURCES.md is still the 2-line pointer to DATA_SOURCES.md (no edit expected — if it drifted, restore the 2-line stub form). Verify the three citation cross-ref keys used by the manifest (1GZM, 3GP6, SASDPG4) each resolve to a heading/section in DATA_SOURCES.md (`grep -n "1GZM\|3GP6\|SASDPG4" DATA_SOURCES.md`). Do NOT edit the pymol/ tree (v1 archive).
Commit: `docs(21-04): verify SOURCES.md stub + citation cross-refs` (only if any change was needed; otherwise record verify-only in the SUMMARY without a commit)
  </action>
  <verify>`head -3 vmd/data/demos/SOURCES.md` shows the pointer; the three keys grep to real sections.</verify>
  <done>Attribution chain intact: manifest citation keys -> DATA_SOURCES.md sections -> SOURCES.md stub entry point.</done>
</task>

</tasks>

<verification>
- All Task-1 grep gates pass; approved citation texts unchanged (diff shows only the heading word, the new section, the §5 retitle, and the §5 VMD line)
- The 404'd DOI and oxy.ac.uk typo remain absent; glycorprotein typo preserved
</verification>

<success_criteria>
- DEMO-03's "cite the source and IDs in documentation" and DEMO-04's reuse requirement are doc-satisfied, with the human approval step explicitly routed to the 21-10 checkpoint
- The no-gzip deviation is durably recorded in the repo (not just in planning docs)
</success_criteria>

<output>
After completion, create `.planning/phases/21-large-fetched-demos-attribution/21-04-SUMMARY.md`
</output>
