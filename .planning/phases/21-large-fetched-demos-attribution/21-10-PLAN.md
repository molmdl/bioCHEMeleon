---
phase: 21
plan: 10
type: execute
wave: 7
depends_on: ["21-09"]
files_modified: [vmd/tests/demo_verify.tcl]
autonomous: false
must_haves:
  truths:
    - "ONE consolidated GUI checkpoint verifies the whole phase in a single real-VMD session: tier cascades (Easy/Hard/Challenge/Very challenging, all 9 demos), demo info label, live 3gp6 fetch with responsive progress dialog -> round starts on the membrane protein, >20k warn fires for 1gzm dry (~25,974) and NOT for 3gp6 (19,221), SASDPG4 glycan intact (2,601 HETATM resn atoms), cache-hit re-Start is fast with no dialog"
    - "The SAME checkpoint formally approves the four pending items: (a) v2 reuse of v1's human-approved citation texts (DEMO-04 + the MemProtMD per-entry license re-verification gate), (b) the DATA_SOURCES.md edits (1GZM heading fix + v2 processing note + §5 VMD line), (c) the curl User-Agent `bioCHEMeleon/2.0`, (d) the plain-HTTP RCSB Tcl fallback recorded as a NON-requirement (curl-only posture)"
    - "The session obeys the standing 3-paste directive: driver auto-issues commands and auto-logs; the human only clicks (warn boxes, optional p-press) and pastes at most 3 short commands"
    - "The driver is Tk-guarded and headless-clean (one warn no-op in text mode)"
  artifacts:
    - path: "vmd/tests/demo_verify.tcl"
      provides: "the auto-driver: dv_log + auto round-1 (3gp6 fetch->round->dump), dv_round2 (1gzm warn+fetch->round->cache-hit re-Start->dump), dv_done (sasdpg4 glycan check + report + cleanup)"
  key_links:
    - from: "vmd/tests/demo_verify.tcl"
      to: "vmd/gui/dialog.tcl on_start / fetch_dialog.tcl"
      via: "apply_state a fetched-demo state + on_start — the exact user path (progress dialog, continuation, warn)"
      pattern: "on_start|ensure_demo"
    - from: "vmd/tests/demo_verify.tcl (checkpoint text)"
      to: "DATA_SOURCES.md"
      via: "the approval items reference the drafted §2/§3 blocks + v2 processing note (21-04's edits)"
      pattern: "DATA_SOURCES"
---

<objective>
Build the Phase-21 GUI auto-driver (`vmd/tests/demo_verify.tcl`) and run the ONE consolidated human-verify checkpoint: GUI confirmation of the fetch-on-demand flow + the four human-approval items.

Purpose: everything machine-checkable is already headless-green (21-09); this session closes the Tk-only surfaces (menu look, progress dialog, warn box) and the human-approval record that DEMO-04 requires (v1 precedent: the checkpoint approval IS the formal license-verification record).
Output: `vmd/tests/demo_verify.tcl` + an APPROVED checkpoint record (or precisely recorded defects for gap closure).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@vmd/tests/rep_verify.tcl (the driver TEMPLATE — Tk-guard wrap, pv_log pattern, auto-session setup; EXTEND the pattern, do not restructure)
@.planning/phases/21-large-fetched-demos-attribution/21-RESEARCH-v1-carryover.md (§5.4 approval statuses; §6 F2/F7)
@DATA_SOURCES.md (the 21-04-edited draft being approved)
@vmd/gui/setup_tab.tcl
@vmd/gui/dialog.tcl

Key facts (do NOT re-derive):
- **Driver shape (rep_verify.tcl pattern):** Tk-guarded if-wrap (headless = one warn, no-op); dv_log writes to demo_verify_log.txt + vmdcon echo; auto-session on source: log the metadata state (9-demo manifest summary, tier grouping, per-fetched-id cache status), load the extension, open the dialog, then AUTO-RUN round 1: `apply_state [validate_state [dict create target_mode demo demo_id 3gp6 hider_count 5 per_rep [dict create] lock_scene 0 difficulty_easy 1]]` -> `::biochemeleon::on_start` -> the fetched branch runs ensure_demo (progress dialog appears) -> continuation starts the round -> `after 25000` auto-dump (game atoms, sentinel count 5, timer running, per-fetched cache status).
- **dv_round2:** 1gzm state (hider_count 5) -> on_start -> the >20k warn box appears (~25,974 atoms) -> human clicks Yes -> fetch (~10 s) -> round -> `after 30000` auto-dump; THEN auto re-Start 3gp6 (now cached): log clock deltas around on_start to evidence the fast no-dialog cache hit, auto-dump again.
- **dv_done:** sasdpg4 via ensure_demo (or load via demos if cached) -> log atom count 4,123 + glycan resn census 2,601 -> print the verdict summary (rounds started, warn observed per log timestamps, cache-hit deltas, census) -> game cleanup + close any stray progress window.
- **Human actions:** watch the progress dialog during round 1 (responsive? completes?), click Yes on the 1gzm warn (and optionally No on a repeat to see the abort), glance at the demo menu cascades, optionally press `p` + click one hider to sanity-check the scene (NOT required — the pick contract is locked from Phase 16). Max 3 pastes: `source vmd/tests/demo_verify.tcl`, `dv_round2`, `dv_done`.
- **Checkpoint checklist (record per item):** (1) cascades show 4 tiers w/ all 9 demos, labels readable; (2) selecting 3gp6 shows the info label and does NOT load; (3) progress dialog appears, is responsive (can be moved), completes; (4) round starts on the membrane protein (protein + DPP bilayer visible, NO water); (5) 1gzm warn fires w/ ~25,974 wording; No aborts cleanly; (6) 3gp6 cache-hit re-Start fast + no dialog; (7) sasdpg4 glycan visible (2,601 HETATM resn atoms logged); (8) rep_verify_log-style log clean (0 ERROR)/bad switch).
- **Approval items (present in the checkpoint, all four):** (a) DATA_SOURCES.md §1/§2/§3 citation texts reused verbatim from v1's 2026-08-16 approval — including the MemProtMD CC-BY 4.0 license block (this approval = the DEMO-04 per-entry license verification record; optionally re-confirm the live site still shows the CC-BY string); (b) the 21-04 edits: 1GZM heading "Bovine rhodopsin" fix + the v2 processing note (plain-PDB cache deviation, strip mechanism, cache location, never-strip rule) + §5 VMD line; (c) UA constant `bioCHEMeleon/2.0` in fetch.tcl; (d) NO plain-HTTP RCSB Tcl fallback — curl-only posture recorded as a non-requirement (fetch_pdb stub stays for SETUP-01's future RCSB fetch mode, out of Phase-21 scope).
- Tcl 8.5; no grab; `::tk_version` qualifier; short lines; staging per the repo harness.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the demo_verify.tcl auto-driver</name>
  <files>vmd/tests/demo_verify.tcl</files>
  <action>
Per the context facts, modeled on rep_verify.tcl's structure (Tk-guard wrap, log helper, auto round-1, dv_round2, dv_done). End-of-source instructions <= 8 short lines (paste budget, warn-box behavior, what to watch). Headless: single-warn no-op.
Commit: `test(21-10): demo_verify.tcl auto-driver (fetched-demo rounds + approvals session)`
  </action>
  <verify>Staged headless source: exactly one warn line, NO errors; 8.6 + grab-set gates zero; `grep -c "dv_round2\|dv_done" vmd/tests/demo_verify.tcl` >= 2; full-log scan clean.</verify>
  <done>Driver ready: 3-paste session, auto-logged, headless-clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The complete Phase 21: on-demand fetch -> strip -> cache -> load for 1gzm/3gp6/sasdpg4 with full attribution, tier-cascaded demo menu, async progress flow, >20k warn, and the drafted DATA_SOURCES.md edits — all headless-green (21-09) and now presented for GUI confirmation + the four approvals.</what-built>
  <how-to-verify>
1. Open a real VMD GUI on Windows. Extensions -> Tk Console. `cd` to the staging root (tmp/biochemeleon-vmd — freshly staged with `mkdir -p tmp/biochemeleon-vmd && cp -r vmd tmp/biochemeleon-vmd/`).
2. Paste 1: `source vmd/tests/demo_verify.tcl` — the driver logs the metadata state, opens the dialog, and AUTO-RUNS the 3gp6 round. WATCH: the demo menu's 4 tier cascades (Easy/Hard/Challenge/Very challenging — all 9 demos); selecting-vs-loaded behavior in the log; the progress dialog appearing, staying responsive, and completing (~7-14 s first fetch); the round starting on the membrane protein (protein + DPP bilayer, no water).
3. Paste 2: `dv_round2` — the 1gzm round. CLICK Yes on the "~25,974 atoms" warn box (optionally later run it once more and click No to see the clean abort). WATCH: the fetch completes and the round starts.
4. Paste 3: `dv_done` — the driver re-Starts 3gp6 from cache (fast, no dialog — deltas logged), loads sasdpg4 (glycan census logged), prints the verdict summary, cleans up.
5. Read DATA_SOURCES.md §2/§3 + the new v2 processing note in the repo root. Answer the four approval items: (a) citation-text reuse + MemProtMD license re-verification, (b) the DATA_SOURCES.md edits (heading fix + v2 note), (c) UA bioCHEMeleon/2.0, (d) curl-only posture (no plain-HTTP fallback).
6. Record per checklist item 1-8 (context list) + the four approvals.
  </how-to-verify>
  <resume-signal>Type "approved" (all items pass) or list the failing item numbers / approval rejections.</resume-signal>
</task>

</tasks>

<verification>
- Headless: driver no-ops cleanly (one warn, zero errors); 8.6 + grab-set gates zero
- GUI checkpoint record: all 8 checklist items + 4 approval items with explicit verdicts; auto-log transcript attached; any defect precisely recorded for gap closure
</verification>

<success_criteria>
- The consolidated checkpoint is APPROVED — Phase 21's last unverified surfaces (GUI + human approvals) are closed
- The approval record doubles as DEMO-04's MemProtMD license verification for v2
</success_criteria>

<output>
After completion, create `.planning/phases/21-large-fetched-demos-attribution/21-10-SUMMARY.md`
</output>
