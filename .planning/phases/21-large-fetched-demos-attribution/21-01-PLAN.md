---
phase: 21
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [vmd/lib/setup_state.tcl, vmd/tests/test_setup_state.test]
autonomous: true
must_haves:
  truths:
    - "DEMO_MANIFEST exposes 9 demos: the 6 bundled + 1gzm/3gp6 (MemProtMD, very_challenging, strip=1) + sasdpg4 (SASBDB, challenge, strip=0), each fetched entry carrying the full v1 fetch schema (source, source_id, fetch_url, cache_name, citation, strip)"
    - "cache_name values are plain .pdb (NO .gz) — the evidence-backed no-gzip deviation is encoded in the data and commented at the manifest"
    - "TIER_LABELS maps easy/hard/challenge/very_challenging to the exact DIFF-05 display vocabulary, and tier_label() falls back to string totitle for unknown keys"
    - "bundled_demo_ids() returns exactly the 6 bundled ids and randomize_state uses it — fetched demos can never be picked offline"
    - "validate_state accepts demo_id 1gzm/3gp6/sasdpg4 (membership check scales with the manifest)"
    - "hider_count_cap pinned at large atom counts (19221 -> 50, 25974 -> 50)"
  artifacts:
    - path: "vmd/lib/setup_state.tcl"
      provides: "extended DEMO_MANIFEST (9 entries, fetch schema), TIER_LABELS, tier_label, bundled_demo_ids"
      exports: ["TIER_LABELS", "tier_label", "bundled_demo_ids"]
    - path: "vmd/tests/test_setup_state.test"
      provides: "tcltest cases for the manifest schema, tier map, bundled-only pool, new-id validation, large-count cap"
  key_links:
    - from: "vmd/lib/demos.tcl (21-05)"
      to: "vmd/lib/setup_state.tcl DEMO_MANIFEST"
      via: "load_demo branches on [dict get $meta source] (bundled vs memprotmd/sasbdb)"
      pattern: "dict get \\$meta source"
    - from: "vmd/gui/setup_tab.tcl (21-06)"
      to: "vmd/lib/setup_state.tcl TIER_LABELS"
      via: "demo sub-menu cascades labeled via tier_label"
      pattern: "TIER_LABELS|tier_label"
    - from: "vmd/lib/setup_state.tcl randomize_state"
      to: "bundled_demo_ids"
      via: "random demo pool built from bundled_demo_ids (offline-safe)"
      pattern: "bundled_demo_ids"
---

<objective>
Extend the PURE layer for fetched demos: 3 new DEMO_MANIFEST entries with the v1 fetch schema, the 4-tier TIER_LABELS model, and the bundled-only randomize helper — TDD-first.

Purpose: DEMO-02/03/04 + DIFF-05 all key off this pure data. Every later plan (mol bridge, GUI menu, dialog) consumes exactly these shapes; pinning them first with green tests is the cheapest possible contract.
Output: extended `vmd/lib/setup_state.tcl` + green `vmd/tests/test_setup_state.test`.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/21-large-fetched-demos-attribution/21-RESEARCH-v1-carryover.md
@.planning/phases/21-large-fetched-demos-attribution/21-RESEARCH-sources-and-integration.md
@vmd/lib/setup_state.tcl
@vmd/tests/test_setup_state.test

Key facts (do NOT re-derive — research-verified):
- **New entries (exact values, all probe-verified 2026-09-25):**
  - `1gzm`: category "Membrane protein", type protein, difficulty very_challenging, source memprotmd, source_id 1GZM, fetch_url `https://memprotmd.bioch.ox.ac.uk/data/memprotmd/simulations/1gzm_default_dppc/files/structures/at.pdb`, cache_name `1gzm_dry.pdb`, citation 1GZM, strip 1
  - `3gp6`: category "Membrane protein", type protein, difficulty very_challenging, source memprotmd, source_id 3GP6, fetch_url `https://memprotmd.bioch.ox.ac.uk/data/memprotmd/simulations/3gp6_default_dppc/files/structures/at.pdb`, cache_name `3gp6_dry.pdb`, citation 3GP6, strip 1
  - `sasdpg4`: category Glycoprotein, type protein, difficulty challenge, source sasbdb, source_id SASDPG4, fetch_url `https://www.sasbdb.org/media/pdb_file/SASDPG4_fit2_model1.pdb`, cache_name `SASDPG4_fit2_model1.pdb`, citation SASDPG4, strip 0
  - The `fit2_model1` suffix is LOAD-BEARING (fit1 is protein-only, silently fails DEMO-03). Append the 3 entries AFTER 4wb3 (keep existing dict order; the GUI groups by tier, not flat order).
- **EVIDENCE-BACKED DEVIATION (record here, durably, in a manifest comment):** DEMO-02 says "compress before caching". VMD 1.9.3 Windows CANNOT read gzipped PDBs (probeB/probeB2: fails by extension AND with explicit `type pdb` on a valid gzip, magic 1f8b). Decision: **strip-then-cache plain PDB** (cache_name `.pdb`, never `.gz`); sizes are tiny (3gp6 dry 1.5 MB, 1gzm dry ~2.0 MB, SASDPG4 0.4 MB — total ~4 MB, gitignored). Precedent: 17.2's Option-A deviation pattern. Revisit only if VMD gains a gzip-reading pdb plugin or Tcl 8.6 zlib.
- **TIER_LABELS (v1 vocabulary, verbatim):** `easy -> Easy`, `hard -> Hard`, `challenge -> Challenge`, `very_challenging -> Very challenging`. Tcl 8.5 dicts preserve insertion order — cascade order derives from dict keys.
- `randomize_state` already filters `source eq "bundled"` inline (setup_state.tcl:271-274). Refactor that block to call the new exported `bundled_demo_ids` proc (same behavior, now testable) — behavior-identical, suite must stay green.
- tier_label: `dict exists` guard -> mapped label; else `string totitle $tier` (v1's .title() fallback).
- Tcl 8.5 only (no lmap/try/throw); brace ALL expr; one `variable` per line; purity: NO mol/atomselect/tk in this file.
- TDD harness (exact commands):
```
mkdir -p tmp/biochemeleon-vmd && cp -r vmd tmp/biochemeleon-vmd/
timeout 300 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/tests/test_setup_state.test -eofexit < /dev/null' 2>&1 | grep -E "BCHM_TEST_RESULT|ERROR\)|bad switch"
```
Parse the `BCHM_TEST_RESULT Total=.. Passed=.. Failed=..` marker, NEVER $?. Model new tests on the existing file's structure; end-of-file marker block stays last.
</context>

<feature>
  <name>Fetched-demo manifest entries + 4-tier metadata (pure layer)</name>
  <files>vmd/lib/setup_state.tcl, vmd/tests/test_setup_state.test</files>
  <behavior>
RED test cases (all before implementation):
- manifest_nine: `[dict size $::biochemeleon::setup_state::DEMO_MANIFEST]` == 9; keys contain 1gzm, 3gp6, sasdpg4.
- fetched_schema_1gzm / _3gp6 / _sasdpg4: each entry's dict equals the exact key/value set above (source, source_id, fetch_url, cache_name, citation, strip, category, type, difficulty). Assert fetch_url strings EXACTLY (they are traceable against DATA_SOURCES.md; sasdpg4's URL contains the load-bearing `SASDPG4_fit2_model1.pdb`).
- no_gzip_deviation: none of the 9 cache_name values contains `.gz` (the deviation is structural, not accidental).
- strip_flags: 1gzm strip==1, 3gp6 strip==1, sasdpg4 strip==0.
- tier_labels_values: TIER_LABELS has exactly 4 keys with the exact display values in insertion order easy, hard, challenge, very_challenging.
- tier_label_mapped: tier_label easy -> "Easy"; tier_label very_challenging -> "Very challenging".
- tier_label_fallback: tier_label "extreme" -> "Extreme" (string totitle), tier_label "" -> "".
- bundled_demo_ids: returns exactly {1znf 1xdn 5e54 1k8p 2qbz 4wb3} (current manifest insertion order of the bundled six).
- randomize_excludes_fetched: for seeds 1..30, `randomize_state $seed` -> demo_id is ALWAYS in bundled_demo_ids (never 1gzm/3gp6/sasdpg4) — the offline-safety regression.
- validate_accepts_fetched: `validate_state [dict create demo_id 1gzm]` -> demo_id 1gzm round-trips; same for 3gp6 and sasdpg4.
- cap_large: hider_count_cap 19221 == 50; hider_count_cap 25974 == 50; hider_count_cap 117898 == 50.
  </behavior>
  <implementation>
Once RED fails for the right reasons, in `vmd/lib/setup_state.tcl`:
- Append the 3 entries to DEMO_MANIFEST (dict create continuation lines, same style as the existing 6) with a comment block above them carrying: the fetch-schema explanation (per-entry keys), the fit2_model1 load-bearing warning, and THE NO-GZIP DEVIATION record (probe evidence, decision, revisit conditions).
- `variable TIER_LABELS [dict create easy Easy hard Hard challenge Challenge very_challenging "Very challenging"]` near DEMO_MANIFEST.
- `proc ::biochemeleon::setup_state::tier_label {tier}` — mapped-or-totitle.
- `proc ::biochemeleon::setup_state::bundled_demo_ids {}` — foreach over DEMO_MANIFEST keys collecting source eq "bundled" (exact logic moved out of randomize_state; randomize_state now calls it).
- Extend `namespace export` with TIER_LABELS tier_label bundled_demo_ids.
REFACTOR only if obvious; all prior 47 tests must stay green.
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: RED — extend the setup-state suite with fetched-manifest + tier cases</name>
  <files>vmd/tests/test_setup_state.test</files>
  <action>
Add every case from <behavior> to vmd/tests/test_setup_state.test, modeled on the file's existing test style (fully-qualified calls, catch+string match for errors where applicable). Run the staged harness — the suite MUST fail (missing TIER_LABELS/tier_label/bundled_demo_ids + manifest size 6 != 9).
Commit: `test(21-01): add failing fetched-manifest and tier-metadata cases`
  </action>
  <verify>Staged run prints BCHM_TEST_RESULT with Failed > 0; full log has the failures rooted in the missing symbols/manifest size, not suite syntax (`bad switch` count == 0 unrelated to intent).</verify>
  <done>Suite covers manifest schema (exact URLs/cache names/strip flags), no-gzip structural assert, tier map + fallback, bundled-only pool across 30 seeds, new-id validation, large-count cap — and fails for the right reason.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement manifest entries, TIER_LABELS, tier_label, bundled_demo_ids</name>
  <files>vmd/lib/setup_state.tcl</files>
  <action>
Implement per <implementation> (entries with deviation comment, TIER_LABELS, both procs, export list, randomize_state refactor onto bundled_demo_ids). Re-run the staged suite.
Commit: `feat(21-01): fetched-demo manifest entries + 4-tier TIER_LABELS (pure layer)`
  </action>
  <verify>
```
tclsh vmd/lib/setup_state.tcl   # loads clean, pure
timeout 300 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/tests/test_setup_state.test -eofexit < /dev/null' 2>&1 | grep -E "BCHM_TEST_RESULT|ERROR\)|bad switch"
```
BCHM_TEST_RESULT Failed=0 (all prior 47 + new cases); ZERO `ERROR)` / `bad switch` in the full log; `grep -rnE "\blmap\b|\btry\b|\bthrow\b|\btailcall\b|\bcoroutine\b|\byield\b|\bfinally\b" vmd/lib/setup_state.tcl` -> zero; `grep -nE "mol |atomselect|tk" vmd/lib/setup_state.tcl` -> only comment mentions, no commands.
  </verify>
  <done>Manifest has 9 entries with the exact fetch schema; TIER_LABELS/tier_label/bundled_demo_ids exported; randomize_state uses bundled_demo_ids; deviation recorded in a manifest comment; suite fully green.</done>
</task>

</tasks>

<verification>
- BCHM_TEST_RESULT Failed=0 staged, zero `ERROR)` / `bad switch` in the full log
- Purity + 8.6-idiom gates zero on setup_state.tcl
- `grep -c "pdb.gz\|\.pdb\.gz" vmd/lib/setup_state.tcl` -> 0 (deviation holds structurally)
- The deviation comment cites the probe evidence (VMD cannot read .gz) and the revisit condition
</verification>

<success_criteria>
- The fetch schema (source/source_id/fetch_url/cache_name/citation/strip) and tier vocabulary are pinned by green tests BEFORE any mol-bridge or GUI work consumes them
- Plans 21-05/21-06 can consume the exact manifest shapes documented here without re-deriving decisions
</success_criteria>

<output>
After completion, create `.planning/phases/21-large-fetched-demos-attribution/21-01-SUMMARY.md`
</output>
