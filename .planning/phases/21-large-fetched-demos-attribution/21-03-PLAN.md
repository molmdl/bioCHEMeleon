---
phase: 21
plan: 03
type: tdd
wave: 2
depends_on: ["21-02"]
files_modified: [vmd/lib/fetch.tcl, vmd/tests/test_fetch.test, vmd/smoke/phase21_async_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "Cache layout is deterministic and script-relative: vmd/data/demos/cache/ (the planner decision for the roadmap's `data/demos/cache/` — the viewer tree has no root data/; sibling of the bundled demos; gitignored via .gitignore cache rules, probe-verified)"
    - "cache_path_for maps manifest demo ids to their cache files; is_cached is existence-only (v1 semantics — a corrupt cache self-heals via load failure -> re-fetch)"
    - "Promotion is atomic: write to <name>.part then file rename -force; cleanup is idempotent and silent on missing files"
    - "start_download runs curl NON-BLOCKING via open |curl + fconfigure -blocking 0 + fileevent readable (probeF2-verified shape), keeps the event loop alive, and delivers exactly one on_done callback: {ok <wout>} or {error <classified message>}"
  artifacts:
    - path: "vmd/lib/fetch.tcl"
      provides: "cache layout (script_dir capture, cache_dir, cache_path_for, is_cached, promote_part, cleanup_part) + async engine (start_download/_read) added to ::biochemeleon::fetch"
      exports: ["cache_dir", "cache_path_for", "is_cached", "promote_part", "cleanup_part", "start_download"]
    - path: "vmd/tests/test_fetch.test"
      provides: "cache-layout + promotion test cases appended to the 21-02 suite"
    - path: "vmd/smoke/phase21_async_smoke.tcl"
      provides: "headless async smoke: vwait-driven download with deterministic bogus-host error path + network-tolerant success path"
  key_links:
    - from: "vmd/lib/fetch.tcl cache_path_for"
      to: "DEMO_MANIFEST cache_name"
      via: "[dict get $meta cache_name] joined under cache_dir; errors on bundled ids (they load from data/demos/, not the cache)"
      pattern: "cache_name"
    - from: "vmd/gui/fetch_dialog.tcl (21-07)"
      to: "vmd/lib/fetch.tcl start_download"
      via: "the GUI fetch flow drives downloads through the non-blocking engine"
      pattern: "fetch::start_download"
    - from: "vmd/lib/demos.tcl (21-05)"
      to: "vmd/lib/fetch.tcl cache helpers"
      via: "load_cached_demo / strip_and_cache resolve paths via cache_dir/cache_path_for and promote via promote_part"
      pattern: "fetch::(cache_path_for|promote_part|cleanup_part)"
---

<objective>
Complete `vmd/lib/fetch.tcl`: the cache-layout helpers (WHERE files live, atomic promotion) and the non-blocking download engine (the v2 replacement for v1's worker thread) — TDD for the deterministic parts, headless smoke for the async engine.

Purpose: the whole phase writes into exactly one cache directory and reads back through exactly one path-resolution proc; the GUI fetch (21-07/21-08) must never block the Tk event loop. Pin both contracts now.
Output: completed `vmd/lib/fetch.tcl`, extended test suite, `vmd/smoke/phase21_async_smoke.tcl`.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/21-large-fetched-demos-attribution/21-RESEARCH-vmd-fetch-mechanics.md
@vmd/lib/fetch.tcl

Key facts (do NOT re-derive — probe-verified):
- **CACHE LOCATION DECISION (planner, closing mechanics-research open Q1):** `vmd/data/demos/cache/`. Roadmap SC1 says `data/demos/cache/`; the viewer tree has NO root `data/` — the bundled demos live at `vmd/data/demos/`, so the faithful script-relative reading from `vmd/lib/` is `[file join $script_dir .. data demos cache]`. Gitignored (`.gitignore` `cache` + `**/cache/**`, probeE verified clean git status with files present). Windows-visible (repo under /mnt/c). This plan records the decision; ALL later plans use `fetch::cache_dir` — never a second derivation.
- **script_dir capture (the 14-02 lesson):** `[info script]` is DYNAMIC — capture `variable script_dir [file dirname [info script]]` inside `namespace eval` (runs at source time) exactly like demos.tcl:26. Proc bodies reference the frozen variable.
- **Naming (mechanics RQ5):** cache file = `<cache_name>` from the manifest (`1gzm_dry.pdb`, `3gp6_dry.pdb`, `SASDPG4_fit2_model1.pdb`). Wet download = `<cache_dir>/<demo_id>_wet.part.pdb`. Strip output = `<cache_dir>/<cache_name>.part`. Promote = `file rename -force` (probeE-verified on Windows VMD). is_cached = `file exists` ONLY.
- **Non-blocking shape (probeF2.tcl, verified headless):**
```tcl
open |[list curl -sS -L --connect-timeout 15 --max-time 120 -A bioCHEMeleon/2.0 -o $outfile -w {%{http_code} %{size_download}} $url] ch
fconfigure $ch -blocking 0 -buffering none
fileevent $ch readable [list ::biochemeleon::fetch::_read $ch $on_done]
```
  `_read`: on eof -> `fileevent $ch readable {}` -> `catch {close $ch} cerr` (nonzero curl exit lands here) -> dispatch `uplevel #0 [list $on_done error <classified>]` or `$on_done ok <accumulated -w output>`; else `append result [read $ch]`. The `-w` string is what accumulates on the pipe (the body goes to -o). NO vwait in the library — vwait is the caller's/business of the GUI loop (headless smokes may vwait; probeD verified `after 0` + vwait chains work in text mode).
- **Deterministic offline error path for smokes/tests:** a URL on a guaranteed-nonexistent host (RFC 2606 reserved TLD, e.g. `https://no-such-host.invalid/x.pdb`) -> curl exit 6 (DNS) -> classified "internet connection" message -> `on_done error`. This makes the async error path testable with ZERO network dependence.
- Tcl 8.5: single-flight state (`variable _active_ch`, `variable _result`) guarded with `info exists` (namespace re-source note); brace ALL expr; catch patterns per AGENTS.md.
- TDD harness (exact commands):
```
mkdir -p tmp/biochemeleon-vmd && cp -r vmd tmp/biochemeleon-vmd/
timeout 300 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/tests/test_fetch.test -eofexit < /dev/null' 2>&1 | grep -E "BCHM_TEST_RESULT|ERROR\)|bad switch"
```
Smoke harness: same staging; run `vmd/smoke/phase21_async_smoke.tcl` 3x; grep `BCHM_SMOKE_RESULT` (PASS=1 or the SKIPPED-NO-NETWORK note rule from 21-02), scan FULL logs for `ERROR)` AND `bad switch`.
</context>

<feature>
  <name>fetch.tcl cache layout + atomic promotion + async download engine</name>
  <files>vmd/lib/fetch.tcl, vmd/tests/test_fetch.test</files>
  <behavior>
New namespace surface (appended to 21-02's fetch.tcl):
- `variable script_dir [file dirname [info script]]` (source-time freeze, comment cites 14-02).
- `cache_dir {}` -> `[file normalize [file join $script_dir .. data demos cache]]`. Does NOT create it (creation is ensure_cache_dir's job: `file mkdir` is idempotent — wrap in the proc `ensure_cache_dir {}` returning the dir).
- `cache_path_for {demo_id}` -> cache_dir + `<cache_name>` from DEMO_MANIFEST (fully-qualified `variable ::biochemeleon::setup_state::DEMO_MANIFEST`). Unknown id -> error "unknown demo: <id>". Bundled id -> error "bundled demo <id> loads from data/demos/, not the cache" (guards misuse).
- `is_cached {demo_id}` -> 1/0 (file exists on cache_path_for; existence-only).
- `promote_part {part final}` -> file rename -force; returns final. Error propagates if part missing (caller's bug).
- `cleanup_part {path}` -> catch {file delete -force $path}; idempotent, silent.

Async engine:
- `start_download {url outfile on_done}` -> single-flight guard (`_active_ch` exists -> error "download already in progress"); ensure parent dir of outfile; open |curl per the shape; on open catch -> `uplevel #0 [list $on_done error [classify_curl_error $err $::errorCode $url]]` and return. Stores ch + accumulates `_result`.
- `_read {ch on_done}` -> per the shape above; on ok path runs the SAME post-download gate as curl_get? NO — keep the engine transport-only: on_done receives `{ok <wout>}`; the CALLER (21-05/21-07) runs parse_curl_w/integrity (single gate implementation lives in a shared proc `integrity_gate {wout outfile min_bytes}` -> bytes or error, ALSO used by curl_get — refactor curl_get to call it so blocking and async share one gate).

RED test cases (append to test_fetch.test; deterministic, no network):
- cache_dir_shape: cache_dir ends with `vmd/data/demos/cache` (string match `*vmd/data/demos/cache`) and is absolute (file pathtype == absolute after normalize).
- ensure_cache_dir_idempotent: call twice -> no error; dir exists; (cleanup deletes it in a later test or leaves it — staging tree only).
- cache_path_for_ok: cache_path_for 3gp6 -> <cache_dir>/3gp6_dry.pdb (tail match); sasdpg4 -> .../SASDPG4_fit2_model1.pdb; 1gzm -> .../1gzm_dry.pdb.
- cache_path_for_bundled: cache_path_for 1znf -> error containing "not the cache".
- cache_path_for_unknown: cache_path_for nope -> error "unknown demo".
- is_cached_roundtrip: write a dummy file at cache_path_for 3gp6 -> is_cached 3gp6 == 1; file delete -> 0.
- promote_part_atomic: write part file with content, promote to final -> final exists with content, part gone; re-promote over an EXISTING final succeeds (rename -force) with new content.
- cleanup_part_idempotent: cleanup_part on existing + missing paths -> no error.
- start_download_error_path: `vwait`-driven: start_download https://no-such-host.invalid/x.pdb <tmp>/x.pdb on_done -> inside on_done set a flag with the result; `vwait ::testflag` (bounded by an `after 20000` safety timer that fails the test); assert result is {error <msg>} with msg containing "internet connection"; no stale _active_ch (a second start_download immediately after does NOT error "already in progress").
- integrity_gate_shared: integrity_gate "200 4" <file-with-"HEADER x" and 4 bytes> 10 -> error "Download incomplete"; integrity_gate "200 12" <same> 10 -> returns 12; integrity_gate "404 0" ... -> withdrawn error. (File fixtures under the staging temp dir.)
  </behavior>
  <implementation>
- Refactor curl_get's post-download validation into `integrity_gate {wout outfile min_bytes}` (parse_curl_w + code check + size check + first_line_ok) and call it from curl_get — ONE gate, two transports.
- Append the cache-layout procs + async engine per <behavior>. Header comment gains the CACHE LOCATION DECISION record (with the roadmap-reading rationale) and the atomic-promotion rule.
- start_download reuses curl_argv (single call shape).
REFACTOR only if obvious; 21-02's cases must stay green.
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: RED — cache-layout + async-contract cases in test_fetch.test</name>
  <files>vmd/tests/test_fetch.test</files>
  <action>
Append the <behavior> cases (cache dir shape, path resolution incl. bundled/unknown guards, is_cached roundtrip, atomic promote, idempotent cleanup, vwait-driven bogus-host error path with a safety-timeout `after`, shared integrity gate). Run staged — new cases fail (missing procs), 21-02 cases still green.
Commit: `test(21-03): add failing cache-layout and async-contract cases`
  </action>
  <verify>Staged BCHM_TEST_RESULT Failed > 0 with only the new cases failing; full log otherwise clean.</verify>
  <done>Cache + async contracts pinned; suite red for the right reason.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement cache layout, shared integrity gate, async engine</name>
  <files>vmd/lib/fetch.tcl</files>
  <action>
Implement per <implementation> (integrity_gate refactor + cache procs + start_download/_read + decision-record comments). Re-run staged suite.
Commit: `feat(21-03): fetch.tcl cache layout + atomic promotion + non-blocking download`
  </action>
  <verify>Staged BCHM_TEST_RESULT Failed=0 (21-02 + new cases); ZERO `ERROR)`/`bad switch` in full log; purity gate zero (`grep -nE "mol |atomselect|tk_version|toplevel" vmd/lib/fetch.tcl`); 8.6-idiom gate zero.</verify>
  <done>fetch.tcl complete: transport + cache + async, one shared integrity gate, suite green.</done>
</task>

<task type="auto">
  <name>Task 3: phase21_async_smoke.tcl — headless non-blocking proof (flake-proof)</name>
  <files>vmd/smoke/phase21_async_smoke.tcl</files>
  <action>
Headless smoke per the repo contract (BCHM_SMOKE_RESULT marker, full-log scan discipline):
1. Source setup_state + fetch. Deterministic leg: bogus-host start_download under vwait -> on_done {error ...internet connection...} -> assert; immediate second start_download succeeds in arming (no single-flight leak) then errors the same way -> assert. These ALWAYS run.
2. Network-tolerant leg: start_download the real SASBDB URL (min 300000) to a staging temp file -> on ok: parse_curl_w -> code 200 + integrity_gate passes -> assert bytes >= 300000 + first line TITLE -> `BCHM_SMOKE_NOTE LIVE-FETCH-OK <bytes>`. On error classified network-class (ENOENT/6/7/28 fragments): print `BCHM_SMOKE_NOTE SKIPPED-NO-NETWORK (<msg>)` and continue — NEVER a hard FAIL on network-class errors. Any other failure -> FAIL the smoke.
3. Marker block: `BCHM_SMOKE_RESULT PASS=1` only when the deterministic legs + (live leg OR honest skip) all passed. `after`-bounded vwait waits (30 s safety timers). Clean up temp files.
Run 3x sequential on fresh staging.
Commit: `test(21-03): phase21 async download smoke (deterministic error path + network-tolerant live leg)`
  </action>
  <verify>
```
timeout 600 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/smoke/phase21_async_smoke.tcl -eofexit < /dev/null' 2>&1 | tail -30
```
PASS=1 on 3 consecutive runs (or PASS=1 with the SKIPPED-NO-NETWORK note recorded); full-log scan: ZERO `ERROR)` / `bad switch`; `Exiting normally` every run.</verify>
  <done>Non-blocking engine proven headless: event-loop-driven callback contract verified, error path deterministic, live leg honest about network state — the gate cannot flake on connectivity.</done>
</task>

</tasks>

<verification>
- BCHM_TEST_RESULT Failed=0 (21-02 + 21-03 cases); zero `ERROR)` / `bad switch` across suite + smoke logs
- phase21_async_smoke PASS=1 x3 (or PASS=1 + recorded SKIPPED-NO-NETWORK verdict in all three)
- Purity + 8.6 gates zero on fetch.tcl
- Cache-location decision + atomic-promotion rule recorded in fetch.tcl's header
</verification>

<success_criteria>
- Every later plan resolves cache paths ONLY via fetch::cache_path_for (one derivation, one decision record)
- The GUI fetch flow (21-07) can rely on start_download's exactly-one-callback contract without re-testing transport
</success_criteria>

<output>
After completion, create `.planning/phases/21-large-fetched-demos-attribution/21-03-SUMMARY.md`
</output>
