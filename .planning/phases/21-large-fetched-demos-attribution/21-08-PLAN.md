---
phase: 21
plan: 08
type: execute
wave: 5
depends_on: ["21-06", "21-07"]
files_modified: [vmd/gui/dialog.tcl, vmd/smoke/phase21_dialog_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "on_start's demo branch is source-aware: bundled -> sync load_demo; fetched -> cache hit loads sync; cache miss -> fetch_dialog::ensure_demo with a stashed state and RETURN — the remaining steps run in the continuation (_continue_start) when the fetch completes"
    - "Steps 3-7 of on_start live in ONE shared proc `::biochemeleon::_continue_start {state molid}` used by ALL paths (loaded / bundled / fetch-completion) — single semantics, no duplicated fan-out"
    - "The >20k-atom warning fires before start_game on EVERY path: `demos::atom_count > 20000` -> tk_messageBox yes/no ('~N atoms… continue?'); No -> plain return (the established abort shape). 1gzm dry (~25,974) trips it; 3gp6 dry (19,221) does not"
    - "Re-entrancy: a fetch in progress (fetch_dialog::fetch_active) makes Start show an info box and return — no second action can re-enter the continuation"
    - "Sync paths (loaded/bundled demos) behave exactly as today for small molecules — zero regression on the existing smokes"
  artifacts:
    - path: "vmd/gui/dialog.tcl"
      provides: "restructured on_start (source-aware demo branch + continuation split), _continue_start, >20k warn, fetch re-entrancy guard"
    - path: "vmd/smoke/phase21_dialog_smoke.tcl"
      provides: "headless load-gate + definition smoke for the restructured dialog; regression runs of the existing dialog-related smokes"
  key_links:
    - from: "vmd/gui/dialog.tcl on_start (fetched branch)"
      to: "vmd/gui/fetch_dialog.tcl ensure_demo"
      via: "cache-miss path hands off to the async orchestrator with the collected state stashed in the continuation"
      pattern: "fetch_dialog::ensure_demo"
    - from: "vmd/gui/dialog.tcl _continue_start"
      to: "game.tcl start_game (steps 4-7 unchanged)"
      via: "validate_state -> pick_bridge deactivate -> start_game -> set_difficulty -> raise_tab -> start_round"
      pattern: "start_game"
    - from: "vmd/gui/dialog.tcl _continue_start"
      to: "demos::atom_count"
      via: ">20000 warn check immediately after the target molid exists"
      pattern: "atom_count"
---

<objective>
Restructure `vmd/gui/dialog.tcl::on_start` for fetched demos: continuation split, >20k warn, re-entrancy guard — the last structural piece of the fetch-on-Start flow.

Purpose: v1 solved cache-miss Starts with a worker + QTimer re-entry (carryover verdict #16); v2's Tcl form is the ensure_demo callback into `_continue_start`. The >20k warn is v2-ONLY new work (v1 had none — carryover verdict #21) and completes SC4.
Output: modified `vmd/gui/dialog.tcl` + load-gate smoke + regression runs.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/21-large-fetched-demos-attribution/21-RESEARCH-v1-carryover.md (§1.1 flow; §6 F3 finalize-None guard)
@.planning/phases/21-large-fetched-demos-attribution/21-RESEARCH-sources-and-integration.md (§3.4 dialog seams)
@vmd/gui/dialog.tcl
@vmd/gui/fetch_dialog.tcl
@vmd/gui/setup_tab.tcl (select_demo fetched semantics from 21-06)

Key facts (do NOT re-derive):
- **Current on_start (dialog.tcl:143-226):** steps 1 collect_state / 2 resolve target (loaded :153-167, demo :168-173, fetch stub :174-184) / 3 validate_state with atom_count / 3.5 pick_bridge deactivate / 4 start_game / 5-7 difficulty + raise + start_round. Abort = plain `return` after a tk_messageBox (established shape).
- **The split:** everything from step 3 onward moves VERBATIM into `::biochemeleon::_continue_start {state molid}` (plus the new warn step). on_start keeps step 1 + target resolution: loaded -> same checks -> `_continue_start $state $selobj`; demo+bundled -> load_demo catch -> `_continue_start`; demo+fetched -> `fetch_dialog::fetch_active` guard -> is_cached ? load_cached_demo + `_continue_start` : `fetch_dialog::ensure_demo $did $w [list ::biochemeleon::_continue_start $state] [list ::biochemeleon::_fetch_failed $w]` then `return`. The fetch-stub branch (mode eq fetch) stays as-is (RCSB fetch mode out of Phase-21 scope).
- **>20k warn (inside _continue_start, FIRST thing after entry, before validate):** `set natoms [::biochemeleon::demos::atom_count $molid]; if {$natoms > 20000 && ![tk_messageBox -type yesno -icon warning -parent $w -title bioCHEMeleon -message "This molecule has ~$natoms atoms. Hider generation and rendering may be slow. Start anyway?"]} { return }`. Constant `20000` as a namespace variable `::biochemeleon::LARGE_ATOM_WARN` (one definition, one use). `w` must be passed into _continue_start (add a $w param — it needs the parent for message boxes anyway).
- **F3 lesson (finalize-None guard):** every continuation failure path surfaces a readable tk_messageBox and cleans state — _fetch_failed {w msg} shows the message; ensure_demo's one-callback contract guarantees it fires once. The stashed `$state` is a plain proc arg (no pending-flags globals needed — Tcl closures-by-list beat v1's stash).
- **Re-entrancy:** at on_start top (after collect_state): `if {[::biochemeleon::fetch_dialog::fetch_active]} { tk_messageBox ... "A demo download is already in progress." ; return }`. (The Export-button guard from v1 verdict #17 is N/A — BTN-05 Generate&export is Phase 20 scope, no button exists yet; record as a note.)
- **Double-load wart (record, don't fix):** bundled demos still load at select time AND at Start (harmless at 555-3,779 atoms — the fetched half is already eliminated by 21-06). Record as an accepted wart in the SUMMARY.
- GUI rules: Tcl 8.5, no grab, `::tk_version` qualifier, catch patterns, braced expr.
- Smoke harness (exact commands):
```
mkdir -p tmp/biochemeleon-vmd && cp -r vmd tmp/biochemeleon-vmd/
timeout 300 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/smoke/phase21_dialog_smoke.tcl -eofexit < /dev/null' 2>&1 | grep -E "BCHM_SMOKE_RESULT|ERROR\)|bad switch"
```
Regression runs (same staged tree): `vmd/smoke/phase16_entry_smoke.tcl` and `vmd/smoke/phase16_gametab_smoke.tcl` MUST stay PASS; if their on_start-shape assertions reference the old inline structure, update those assertions MINIMALLY to the new proc names (the 17.1-07 smoke-adaptation precedent — document every change).
</context>

<tasks>

<task type="auto">
  <name>Task 1: _continue_start extraction + source-aware demo branch + re-entrancy</name>
  <files>vmd/gui/dialog.tcl</files>
  <action>
1. Extract steps 3-7 into `_continue_start {state molid w}` (bodies verbatim; add the LARGE_ATOM_WARN definition + the warn as the first step).
2. Rewrite the demo branch per the context facts (bundled sync / fetched cache-hit sync / fetched miss -> ensure_demo + return). Add `_fetch_failed {w msg}` (readable box, no partial state).
3. Add the re-entrancy guard at on_start top per the context facts.
4. Update the header block-comment (the numbered flow chart at dialog.tcl:110-141) to describe the new shape.
Commit: `feat(21-08): on_start continuation split + fetched-demo branch + >20k warn`
  </action>
  <verify>Headless entry source clean (Tk absent -> the established single-warn no-op); `grep -n "_continue_start" vmd/gui/dialog.tcl` shows definition + >= 3 call sites (loaded, bundled, continuation callback); `grep -c "grab set" vmd/gui/dialog.tcl` -> 0; 8.6-idiom gate zero.</verify>
  <done>Single continuation proc; fetched miss hands off to ensure_demo; warn + re-entrancy in place; sync paths call the same continuation.</done>
</task>

<task type="auto">
  <name>Task 2: phase21_dialog_smoke.tcl + regression sweep</name>
  <files>vmd/smoke/phase21_dialog_smoke.tcl</files>
  <action>
Headless smoke (repo contract):
1. Load-gate + definition run: source the full entry chain headless; assert `::biochemeleon::_continue_start`, `_fetch_failed`, `::biochemeleon::LARGE_ATOM_WARN` == 20000, and on_start are defined; assert `fetch_dialog::ensure_demo` is referenced by on_start (grep-level via `info body` string match or file grep in the smoke).
2. GUI-shape greps: `tk_messageBox` present in _continue_start's warn; `return` abort shape after the warn; no grab set.
3. Regression: run phase16_entry_smoke + phase16_gametab_smoke + phase14_gui_smoke in the same staged tree — all PASS (adapt shape-assertions minimally if needed, documented).
4. Marker + x3 runs + full-log scan.
Commit: `test(21-08): dialog restructure load-gate smoke + dialog regression sweep`
  </action>
  <verify>PASS=1 x3 on the new smoke; the three regression smokes PASS; ZERO `ERROR)` / `bad switch` across all full logs; grab-set + 8.6 gates zero on dialog.tcl.</verify>
  <done>Restructured dialog load-gates clean; existing entry/gametab/gui smokes stay green (or minimally, documentedly adapted).</done>
</task>

</tasks>

<verification>
- phase21_dialog_smoke PASS=1 x3; regression smokes PASS; zero `ERROR)` / `bad switch` in all full logs
- on_start: fetched cache-miss returns after ensure_demo (no sync game start); _continue_start is the single steps-3-7 home
- LARGE_ATOM_WARN == 20000 wired before start_game on every path
- Re-entrancy guard present; no grab; 8.6 gates zero
</verification>

<success_criteria>
- The full fetch-on-Start UX chain is structurally complete end-to-end (select -> Start -> fetch -> continuation -> round)
- SC4's warn deliverable is in the code path; behavioral confirmation (dialog appearance, Yes/No flow) routes to the 21-10 GUI checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/21-large-fetched-demos-attribution/21-08-SUMMARY.md`
</output>
