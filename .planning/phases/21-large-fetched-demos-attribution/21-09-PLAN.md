---
phase: 21
plan: 09
type: execute
wave: 6
depends_on: ["21-05", "21-06", "21-07", "21-08"]
files_modified: [vmd/smoke/phase21_capstone_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "The full phase pipeline is proven headless end-to-end through PUBLIC surfaces: manifest/tier metadata -> fetch (live or cache-seeded) -> strip -> cache -> load -> start_game (sphere round AND a Cartoon+VDW mixed round on a large demo) -> cleanup restore"
    - "The v1 F7 alt-conf bug class is checked on a large demo: a game round generates and cleans up on 3gp6_dry (19,221 atoms, native alt-confs) without error — sphere + cartoon tiers"
    - "SC4's hider cap is proven at large atom counts through the real flow (cap(19221)==50 drives the round's hider count)"
    - "Generation time on the large round is measured and within the documented budget (< 30 s per vmd/AGENTS.md)"
    - "FULL-SUITE gate (17.2-11 mechanism: AGENT-ORCHESTRATED sequential one-VMD-run-per-file — suites through a staged suite_driver.tcl wrapper at the staging root, smokes via -eofexit, per-file logs under gate-logs/, marker + ERROR)/bad-switch + Exiting-normally checks per log): every tcltest suite Failed=0 and every smoke PASS (with ONLY reds explicitly documented as pre-existing in the latest phase capstone summary allowed, listed by name), phase21 smokes all green with the SKIPPED-NO-NETWORK allowance; the gate runs ONCE per execution — capstone x3 reruns do NOT re-run it (the capstone's recorder leg only re-prints the recorded verdict)"
  artifacts:
    - path: "vmd/smoke/phase21_capstone_smoke.tcl"
      provides: "the phase capstone: metadata gate + pipeline gate + large-demo game rounds + a gate-verdict recorder leg (the full-suite gate itself is agent-orchestrated per the 17.2-11 mechanism — the capstone never spawns subprocess VMD runs)"
  key_links:
    - from: "vmd/smoke/phase21_capstone_smoke.tcl"
      to: "the full entry chain (biochemeleon.tcl sources)"
      via: "loads the same module set the GUI uses, then drives game::start_game on cached large demos"
      pattern: "start_game"
    - from: "vmd/smoke/phase21_capstone_smoke.tcl"
      to: "fetch::cache_dir seeded cache"
      via: "offline determinism: seeds the staging cache from the staging-copied tmp/p21probe dry files when present (probe-seed staging convention; network-tolerant verdict otherwise)"
      pattern: "cache_dir"
---

<objective>
Write and run the Phase-21 capstone smoke: the whole fetched-demo story proven through public surfaces headlessly (including game rounds on large, alt-conf-heavy molecules), then the full-suite sequential regression gate.

Purpose: SC1-SC4's machine-checkable half closes here; the human-verifiable half (GUI look, dialogs, citations) is the 21-10 checkpoint. The 3gp6 cartoon round retires the v1 F7 concern (research open Q7) in-phase.
Output: `vmd/smoke/phase21_capstone_smoke.tcl` green x3 + a recorded full-suite verdict.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/21-large-fetched-demos-attribution/21-RESEARCH-v1-carryover.md (§6 F7 alt-conf; §2.2 census targets)
@.planning/phases/17.2-cartoon-newcartoon-generators/17.2-11-SUMMARY.md (full-suite gate pattern + the documented pre-existing reds)
@vmd/smoke/phase17_capstone_smoke.tcl (the capstone template: structure, marker block, sequential-gate discipline)
@vmd/biochemeleon.tcl
@vmd/lib/demos.tcl
@vmd/lib/fetch.tcl

Key facts (do NOT re-derive):
- **Source list:** load the full entry chain exactly as phase16_entry_smoke does (everything the GUI loads: setup_state, demos->fetch, registry, backup, mutation, game_logic, generators, rep_tiers, splice, hiders, game, pick_bridge, gui files Tk-guarded). Headless = one warn, no dialog.
- **Pins:** 3gp6 dry = 19,221 atoms / 0 water / 0 ions; 1gzm dry = 25,974 (verify at first measurement — the arithmetic estimate; assert >= 25000 && <= 27000 until measured, then pin exact); sasdpg4 = 4,123 atoms with 2,601 atoms across resnames {NAG MAN BMA NAN GLB AFL NGA GLA}; cap(19221) == 50; cap(25974) == 50.
- **Offline determinism:** seed the STAGING cache from `tmp/p21probe/1gzm_dry.pdb` / `3gp6_dry.pdb` when present (staging-relative — the harness cp's tmp/p21probe into staging per the probe-seed staging convention; copy into `fetch::cache_dir` under the manifest cache names); SASDPG4: seed from the staged `tmp/p21probe/sasdpg4.pdb` (probeE's SASDPG4_fit2_model1.pdb download — 4,123 atoms, verified) if present, else live-fetch. If NEITHER cache nor network: record `BCHM_SMOKE_NOTE SKIPPED-NO-NETWORK` for the live legs and still pass the deterministic legs (metadata + fixture pipeline + regression gate). Never hard-FAIL on ENOENT/6/7/28-class errors.
- **Game rounds (only when the cache is populated):** 
  - Sphere round on 3gp6: `start_game $m 50 [dict create] 0` (cap-driven) -> assert game atoms = 19221+50, sentinel count 50 via `resname GAM and beta < 0`, registry count 50, then cleanup restores 19,221 atoms + 0 sentinels + game molid dead.
  - Cartoon+VDW round on 3gp6: `start_game $m 4 [dict create Cartoon 2 VDW 2] 0` -> asserts: generation completes with 4 hiders (2 residue-splice + 2 simple), per-tier registry counts, cleanup restore. This is the F7 analogue check (v1: native alt-confs broke cartoon anchors; v2 splice handles altloc — prove it on this molecule). Any `ERROR)` here is a REAL finding, not a flake.
  - Round on sasdpg4 (if available): sphere round with hider_count 10 (small, fast; note cap(4123) == 50 — 4123/50 = 82 clamped) -> assert + cleanup.
  - Timing: wrap the 50-hider sphere round's start_game in `clock milliseconds` -> log `BCHM_SMOKE_NOTE GENERATE-TIME <ms>`; assert < 30000 (documented budget).
- **Single-frame rule (17.2-04):** dry caches are writepdb outputs (single frame) — safe for residue splices; do NOT add multi-frame pins.
- **Full-suite gate (17.2-11 pattern — EXPLICIT mechanism):** AGENT-ORCHESTRATED sequential runs, exactly how 17.2-11 actually executed its gate — NOT one VMD script spawning 40+ subprocess VMD runs. The AGENT loops over files from bash, one VMD headless invocation per file, each with its own log — and the gate runs in the SAME staging tree the capstone runs in (tmp/biochemeleon-vmd; sequential discipline makes shared staging safe, and it keeps every gate path staging-relative so the recorder can read the verdict — no cross-staging `../` paths, the exact bug class the probe-seed convention fixes):
  - Per-file logs go under `gate-logs/` (staging-relative: tmp/biochemeleon-vmd/gate-logs/).
  - Suites (every `vmd/tests/*.test`) run through the staged one-purpose driver `suite_driver.tcl` at the staging root — `source <suite-name>; exit`, suite name sed-swapped per run — because tcltest suites have no explicit `exit` and VMD's text console HANGS past stdin EOF over the WSL->Windows pipe (17.2-11's Rule-3 finding: the suite completed 47/47 but the command timed out in the console). NEVER `-e <suite>` directly.
  - Smokes (every `vmd/smoke/*.tcl`) run via `-e <smoke> -eofexit < /dev/null` (they carry explicit exits).
  - Per-log checks: suites `BCHM_TEST_RESULT Failed=0`; smokes `PASS=1` (or the recorded SKIPPED-NO-NETWORK allowance for the phase21 network smokes); zero `ERROR)` AND zero `bad switch` per log; `Exiting normally` present.
  - EXCEPTIONS: the 2 pre-existing reds documented in 17.2-11-SUMMARY.md (phase17_dispatch_smoke, phase17_licorice_smoke) are listed by name as `BCHM_SMOKE_NOTE PRE-EXISTING-RED <name>` and skipped — anything else red fails the gate.
  - The agent writes `GATE-VERDICT.txt` at the STAGING ROOT (suites X/X Failed=0; smokes Y/Z PASS + named allowances) and copies the per-log verdict TABLE into the SUMMARY (17.2-11 precedent — the table lives in the SUMMARY, not in the smoke).
  - **Once-only semantics:** the gate runs ONCE per execution, AFTER the capstone's own legs are green (capstone PASS x3). Capstone x3 reruns (and any later rerun) do NOT re-run the gate — the capstone's gate-verdict recorder leg (Task 2) only RE-PRINTS the recorded verdict when `GATE-VERDICT.txt` exists at the staging root, else prints `BCHM_SMOKE_NOTE GATE-VERDICT NOT-RUN-THIS-RUN` (informational, never affects PASS).
- Smoke harness (exact commands):
```
mkdir -p tmp/biochemeleon-vmd && cp -r vmd tmp/biochemeleon-vmd/ && { cp -r tmp/p21probe tmp/biochemeleon-vmd/ 2>/dev/null || true; }
timeout 1800 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/smoke/phase21_capstone_smoke.tcl -eofexit < /dev/null' 2>&1 | tail -60
```
- **Probe-seed staging convention (ONE rule across 21-05/21-07/21-09):** the harness copies the probe tree INTO staging (the `cp -r tmp/p21probe tmp/biochemeleon-vmd/` line above), so smoke code reads the literal staging-relative seed path `tmp/p21probe/<file>` from the staging cwd (it resolves to tmp/biochemeleon-vmd/tmp/p21probe/<file>). Smokes NEVER use `../` or repo-absolute seed paths; the copy is best-effort (`|| true` — fresh clones lack tmp/p21probe), and every seed use stays behind a `file exists` guard so absence routes to the live-network path. Census pins (19,221 / 4,123 / 2,601 / 25,974) assert whenever the seeds ARE present (the norm on this machine) — they are NOT silently network-dependent.
- Marker `BCHM_SMOKE_RESULT PASS=1` (or PASS=1 with SKIPPED notes), x3 for the capstone's own legs; the full-suite gate is run ONCE by the AGENT (mechanism above — staged suite_driver.tcl, per-file logs) and is NEVER executed inside the capstone script.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capstone smoke — metadata, pipeline, large-demo game rounds</name>
  <files>vmd/smoke/phase21_capstone_smoke.tcl</files>
  <action>
Build per the context facts, modeled on phase17_capstone_smoke.tcl's structure: (A) metadata gate (9 entries, schema, tier grouping 3/3/1/2, TIER_LABELS, bundled_demo_ids, cap pins, randomize-fetched-exclusion); (B) deterministic pipeline gate (fixture strip census + cache contracts — may invoke the phase21_fetch_smoke logic or re-assert its core pins); (C) large-demo rounds with cache seeding + SKIPPED-NO-NETWORK tolerance (sphere 50-hider round w/ timing, Cartoon+VDW F7-check round, sasdpg4 sphere round + glycan census); (D) marker block. Every start_game/cleanup in catch with readable failure notes.
Commit: `test(21-09): phase21 capstone — metadata + pipeline + large-demo game rounds`
  </action>
  <verify>
```
timeout 1800 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/smoke/phase21_capstone_smoke.tcl -eofexit < /dev/null' 2>&1 | tail -60
```
Capstone legs PASS (x3 for legs A-C); the F7 cartoon round generates + cleans on 3gp6 with zero `ERROR)`; GENERATE-TIME logged and < 30000 ms; SKIPPED verdicts recorded honestly when offline.</verify>
  <done>SC1-SC4's machine-checkable assertions all green headlessly; alt-conf cartoon concern retired on real large-demo data (or the cache/network absence honestly recorded).</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite sequential regression gate (agent-orchestrated, 17.2-11 mechanism)</name>
  <files>vmd/smoke/phase21_capstone_smoke.tcl (gate-verdict recorder leg only — the gate loop itself is agent-run from bash; suite_driver.tcl is staging-only, never committed)</files>
  <action>
Two parts:

A. Recorder leg (the ONLY edit to the capstone file): append a gate-verdict recorder after leg D — try to read `GATE-VERDICT.txt` at the staging root (`[file join [pwd] GATE-VERDICT.txt]`, catch-guarded); if present print `BCHM_SMOKE_NOTE GATE-VERDICT <file content>`; if absent print `BCHM_SMOKE_NOTE GATE-VERDICT NOT-RUN-THIS-RUN`. Informational either way — it never affects the capstone's PASS. The capstone file therefore contains ONLY legs A-D plus this recorder — it NEVER spawns subprocess VMD runs (a self-exec'ing capstone would be an unproven mechanism and would contradict the 17.2-11 precedent it cites).

B. Agent-orchestrated gate (run from bash, the 17.2-11 pattern exactly — read 17.2-11-SUMMARY.md first for the documented pre-existing reds' names):
1. Staging: fresh-staged tmp/biochemeleon-vmd per the harness block (vmd + the tmp/p21probe seed copies, probe-seed staging convention) — the gate runs in THIS SAME staging tree the capstone runs in; do NOT create a sibling gate staging (cross-staging `../` paths are the exact bug class the seed convention forbids).
2. Write the one-purpose `suite_driver.tcl` at the staging root: `source <suite-name>; exit`, suite name sed-swapped per run (suites hang past stdin EOF without it — 17.2-11's Rule-3 finding).
3. SEQUENTIALLY, one VMD headless invocation per file with its own per-file log under `gate-logs/` (staging-relative): every `vmd/tests/*.test` via the driver (`cd tmp/biochemeleon-vmd && vmd -dispdev text -e suite_driver.tcl -eofexit < /dev/null` with the suite name sed-swapped into the driver), every `vmd/smoke/*.tcl` via `-e <smoke> -eofexit < /dev/null`.
4. Per-log checks per the context facts (marker / zero `ERROR)` / zero `bad switch` / `Exiting normally`; the 2 documented pre-existing reds named-and-skipped; phase21 network smokes with the SKIPPED-NO-NETWORK allowance). Anything else red fails the gate.
5. Write `GATE-VERDICT.txt` at the staging root (suites X/X Failed=0; smokes Y/Z PASS + named allowances); copy the per-log verdict TABLE into the SUMMARY (17.2-11 precedent).
6. Final capstone run in the same staging: the recorder leg now prints `GATE-VERDICT` with the recorded verdict — demonstrating the once-only semantics (no gate re-run).
Commit: `test(21-09): capstone gate-verdict recorder leg + agent-orchestrated full-suite gate`
  </action>
  <verify>The SUMMARY's gate table reports suites Failed=0 across the board and every non-exempt smoke PASS=1 (or SKIPPED-NO-NETWORK for phase21 network smokes / PRE-EXISTING-RED for the documented names); every non-exempt per-file log is clean (marker + zero ERROR)/bad switch + Exiting normally); the final capstone run prints `GATE-VERDICT` with the recorded verdict and still PASSes — proving the gate did NOT re-run on the capstone rerun.</verify>
  <done>The repo's regression baseline is re-proven WITH Phase-21 changes in place, via the same agent-orchestrated sequential mechanism 17.2-11 used (staged suite_driver.tcl, per-file logs, verdict table in the SUMMARY); the capstone itself stays a legs-A-D-plus-recorder script and the once-only gate semantics are demonstrated.</done>
</task>

</tasks>

<verification>
- Capstone PASS x3 (legs A-C), zero `ERROR)` / `bad switch` in the capstone's own logs
- The F7 cartoon round on 3gp6: generated 4 hiders, cleaned up, no errors — logged with atom pins
- GENERATE-TIME < 30000 ms recorded
- Full-suite gate (agent-orchestrated, once per execution): all suites green; smoke greens with only the documented exemptions; verdict table in the SUMMARY + GATE-VERDICT.txt written; the capstone rerun prints the recorded verdict WITHOUT re-running the gate
</verification>

<success_criteria>
- The phase is HEADLESS-GREEN end-to-end; only the consolidated GUI checkpoint (21-10) remains
- Any large-demo game-loop failure surfaces HERE as a named finding, not at the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/21-large-fetched-demos-attribution/21-09-SUMMARY.md`
</output>
