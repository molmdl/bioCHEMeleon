---
phase: 21
plan: 06
type: execute
wave: 2
depends_on: ["21-01"]
files_modified: [vmd/gui/setup_tab.tcl, vmd/smoke/phase21_gui_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "The demo picker shows 4 tier cascades in TIER_LABELS order (Easy -> Hard -> Challenge -> Very challenging), containing all 9 demos, each entry labeled `\"$category — $id\"` with a [list]-built -command (Pitfall 7)"
    - "Selecting a FETCHED demo (1gzm/3gp6/sasdpg4) sets demo_id + mode + page and shows a tier/source/cache-status info label but does NOT call load_demo — fetch-on-Start is the design (kills the fetched half of the double-load wart)"
    - "Selecting a BUNDLED demo keeps the current behavior byte-for-byte (load + update_cap)"
    - "The stale Phase-14 fetch note is replaced with accurate text"
    - "Tk-only surface: no grab set, ::tk_version global qualifier, Tcl 8.5 idioms, headless source is a clean no-op"
  artifacts:
    - path: "vmd/gui/setup_tab.tcl"
      provides: "tier-cascade demo menu, fetched-aware select_demo, demo info label, updated fetch note"
    - path: "vmd/smoke/phase21_gui_smoke.tcl"
      provides: "headless load-gate smoke: definition run (procs/exports present), GUI-shape greps, existing-gate regressions"
  key_links:
    - from: "vmd/gui/setup_tab.tcl demo cascades"
      to: "TIER_LABELS / tier_label / DEMO_MANIFEST"
      via: "cascades built by grouping manifest ids per difficulty in TIER_LABELS key order"
      pattern: "tier_label|TIER_LABELS|DEMO_MANIFEST"
    - from: "vmd/gui/setup_tab.tcl select_demo"
      to: "DEMO_MANIFEST source field"
      via: "source eq bundled branch decides load-now vs fetch-on-Start"
      pattern: "dict get .* source"
    - from: "vmd/gui/dialog.tcl (21-08)"
      to: "vmd/gui/setup_tab.tcl select_demo"
      via: "on_start's fetched branch relies on select_demo NOT having loaded the demo"
      pattern: "select_demo"
---

<objective>
Surface the difficulty tiers (DIFF-05) and the fetched demos in the Setup tab's demo picker: tier cascades, fetched-aware select_demo (no select-time load), an info label, and an accurate fetch note — with a headless load-gate smoke.

Purpose: DIFF-05's user-facing surface + the select-time semantics that 21-08's fetch-on-Start flow depends on. Visual blend/menu look is confirmed at the 21-10 checkpoint (Tk cannot load headless).
Output: modified `vmd/gui/setup_tab.tcl` + `vmd/smoke/phase21_gui_smoke.tcl`.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/21-large-fetched-demos-attribution/21-RESEARCH-v1-carryover.md (§4: tier model + display format)
@.planning/phases/21-large-fetched-demos-attribution/21-RESEARCH-sources-and-integration.md (§3.3: setup_tab seams)
@vmd/gui/setup_tab.tcl
@vmd/lib/setup_state.tcl

Key facts (do NOT re-derive):
- **Seams:** demo menubutton + flat menu at setup_tab.tcl:153-162; select_demo at :404-417 (sets the ns var via its fully-qualified path — the parameter shadows it, Pitfall 7); stale note at :149; update_cap no-ops when current_molid is empty (:502) — that guard is what makes the no-load fetched branch safe.
- **Menu shape decision (planner, closing sources-research open Q9): TIER CASCADES** — 4 sub-menus labeled via TIER_LABELS (satisfies SETUP-01's literal "sub-menu" AND DIFF-05's labels). Entries inside: `"$category — $id"` (v1 display format minus the tier suffix — the cascade IS the tier). Group ids by iterating `dict keys $DEMO_MANIFEST` (manifest order) into per-tier lists, then emit cascades in `dict keys $TIER_LABELS` order (easy -> hard -> challenge -> very_challenging; Tcl 8.5 dicts preserve insertion order). Defensive: a manifest entry with an unknown difficulty goes under a 5th "Other" cascade (tier_label fallback) with a `vmdcon -warn` at build time. Empty tier -> skip its cascade.
- **Radio label:** the third radiobutton's text changes "Bundled demo" -> "Demo" (the set is now 6 bundled + 3 fetched; the old label would lie). Value stays "demo" (setup-state untouched).
- **select_demo fetched branch:** read source via the fully-qualified `::biochemeleon::setup_state::DEMO_MANIFEST`; bundled -> exactly the current body; fetched -> set `::biochemeleon::setup_tab::demo_id` + `set mode "demo"` + `switch_page`, set `current_molid` to "" (so update_cap no-ops — read its guard), NO load_demo call, then update the info label: `"<tier_label> — <category> — <cached | downloads on first Start (needs internet)>"` using `::biochemeleon::fetch::is_cached` — WAIT: 21-05 (demos.tcl) lands in wave 3 AFTER this plan (wave 2); call `::biochemeleon::fetch::is_cached` through a defensive catch with a fallback text so the GUI builds even pre-21-05 (the entry sources fetch.tcl via demos.tcl once 21-05 lands; until then the catch path shows "downloads on first Start"). Keep it to ONE defensive catch with a clear comment.
- **Info label:** new `ttk::label $pd.info` under the demo menubutton; initial text set in build_target_group from the current demo_id (default 1znf is bundled); updated by select_demo in both branches (bundled: tier + category).
- **Fetch note (:149):** replace "Phase 14: bundled demos only; fetch at Start (Phase 16+)" with: "RCSB PDB-code fetch: not yet implemented. Use the Demo tab for the 3 large fetched demos (1gzm / 3gp6 / sasdpg4)."
- GUI rules: Tcl 8.5; no `grab set`; `::tk_version` global qualifier (13-02 lesson); short lines; headless source = Tk-guarded no-op.
- Load-gate smoke harness (exact commands; the 14-03/17.2-12 pattern):
```
mkdir -p tmp/biochemeleon-vmd && cp -r vmd tmp/biochemeleon-vmd/
timeout 300 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/smoke/phase21_gui_smoke.tcl -eofexit < /dev/null' 2>&1 | grep -E "BCHM_SMOKE_RESULT|ERROR\)|bad switch"
```
Definition run: set a fake `::tk_version` BEFORE sourcing the gui file so proc definitions execute while widget calls stay guarded (17.2-12 precedent) — assert the new/renamed procs exist and the old flat-menu build proc is gone.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tier-cascade demo menu + radio label + info label + fetch note</name>
  <files>vmd/gui/setup_tab.tcl</files>
  <action>
1. In build_target_group: rename the demo radiobutton text to "Demo" (value unchanged).
2. Replace the flat per-key menu loop (:158-161) with the tier-cascade builder per the context facts (group by difficulty, cascade per tier in TIER_LABELS order, `"$category — $id"` entry labels, `[list ::biochemeleon::setup_tab::select_demo $did]` commands, unknown-difficulty fallback + vmdcon warn, empty-tier skip).
3. Add `ttk::label $pd.info` (packed under the menubutton) + a small `update_demo_info {}` proc that renders the tier/category/cache-status line (defensive catch around fetch::is_cached per context).
4. Update the fetch note text per the context facts.
Commit: `feat(21-06): tier-cascade demo menu + demo info label (DIFF-05)`
  </action>
  <verify>Headless source of setup_tab.tcl (fake ::tk_version definition run in Task 2's smoke; here at least `tclsh`-level syntax check via a source-with-stub under tclsh is NOT possible for Tk commands — rely on the smoke's definition run): no syntax errors; `grep -c "add cascade" vmd/gui/setup_tab.tcl` >= 1; `grep -c "Bundled demo" vmd/gui/setup_tab.tcl` -> 0.</verify>
  <done>Menu built from tiers; info label + accurate note in place; Tk rules respected (no grab, ::tk_version, 8.5 idioms).</done>
</task>

<task type="auto">
  <name>Task 2: Fetched-aware select_demo (no select-time load)</name>
  <files>vmd/gui/setup_tab.tcl</files>
  <action>
Branch select_demo per the context facts: bundled keeps the exact current body (load_demo catch + current_molid + update_cap); fetched sets demo_id/mode/page + current_molid "" + update_demo_info and RETURNS (comment: fetch-on-Start design, 21-08 consumes this; avoids the fetched double-load). Keep the fully-qualified ns-var set (Pitfall 7).
Commit: `feat(21-06): select_demo fetched branch — no select-time load (fetch-on-Start)`
  </action>
  <verify>`grep -n "load_demo" vmd/gui/setup_tab.tcl` shows the call only inside the bundled branch (no call on the fetched path); 8.6-idiom + grab-set gates zero on the file.</verify>
  <done>Fetched selection never touches the mol surface; bundled selection byte-identical to today.</done>
</task>

<task type="auto">
  <name>Task 3: phase21_gui_smoke.tcl — load-gate + definition run + GUI-shape greps</name>
  <files>vmd/smoke/phase21_gui_smoke.tcl</files>
  <action>
Headless smoke per the repo contract:
1. Load-gate: source setup_state.tcl + demos.tcl + setup_tab.tcl with Tk ABSENT -> exactly one warn, no dialog attempted, no errors (the 14-03 pattern).
2. Definition run: with a fake `::tk_version` set before sourcing (17.2-12 precedent), assert `::biochemeleon::setup_tab::update_demo_info` and `select_demo` are defined; assert TIER_LABELS/tier_label resolve and the manifest's 9 ids group 3/3/1/2 across easy/hard/challenge/very_challenging by replicating the grouping in the smoke (pure loop) — pins the cascade data.
3. GUI-shape greps on setup_tab.tcl: `add cascade` >= 1; `"Bundled demo"` == 0; `select_demo` referenced with [list]-built commands; `grab set` == 0.
4. Marker `BCHM_SMOKE_RESULT PASS=1` + full-log scan for `ERROR)` / `bad switch`. Run x3.
Commit: `test(21-06): phase21 gui load-gate smoke (tier menu definitions + shape greps)`
  </action>
  <verify>
```
timeout 300 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/smoke/phase21_gui_smoke.tcl -eofexit < /dev/null' 2>&1 | tail -30
```
PASS=1 x3; ZERO `ERROR)` / `bad switch` in all full logs; 8.6-idiom + grab-set gates zero on setup_tab.tcl.</verify>
  <done>GUI surface load-gates clean headlessly; menu data + select_demo semantics pinned as far as text mode allows (visual verdict routed to the 21-10 checkpoint).</done>
</task>

</tasks>

<verification>
- phase21_gui_smoke PASS=1 x3, zero `ERROR)` / `bad switch` in full logs
- 8.6-idiom gate zero; `grep -c "grab set" vmd/gui/setup_tab.tcl` -> 0
- The demo page carries 4 tier cascades (all 9 demos reachable), info label, accurate note
- select_demo: fetched -> no mol calls; bundled -> unchanged behavior
</verification>

<success_criteria>
- DIFF-05's tiered metadata is structurally surfaced (GUI confirmation at the 21-10 checkpoint)
- The select-time contract 21-08 depends on (fetched demos not pre-loaded) is in place
</success_criteria>

<output>
After completion, create `.planning/phases/21-large-fetched-demos-attribution/21-06-SUMMARY.md`
</output>
