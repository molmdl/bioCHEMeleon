---
phase: 21
plan: 05
type: execute
wave: 3
depends_on: ["21-01", "21-02", "21-03"]
files_modified: [vmd/lib/demos.tcl, vmd/smoke/phase21_fetch_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "load_demo branches on manifest source: bundled -> existing path; fetched -> cache HIT loads the dry file via `mol new <cache_path> type pdb` (explicit type — never extension dispatch, v1 Pitfall F1), cache MISS -> a recognizable 'fetch required' error (the GUI owns the fetch)"
    - "strip_and_cache reproduces the probe-verified census: 3gp6 wet 95,239 -> dry 19,221 (75,789 SOL + 116 NA + 113 CL stripped via `atomselect \"not water and not ions\"`; NEVER `solvent` — it includes DPP membrane lipids)"
    - "SASDPG4 is NEVER stripped or rewritten (strip=0 -> the downloaded file promotes byte-identical to the cache; all 2,601 HETATM glycan atoms survive)"
    - "fetch_and_cache (blocking orchestrator) runs the probeE pipeline: curl_get wet.part -> integrity gate -> strip (or as-is) -> atomic promote -> wet cleanup; every failure path cleans the .part and throws a classified, user-readable error"
    - "The wet file NEVER becomes the game target (strip-before-load invariant, carried from v1)"
  artifacts:
    - path: "vmd/lib/demos.tcl"
      provides: "fetched-demo mol bridge: load_demo source branch, load_cached_demo, strip_and_cache, fetch_and_cache; sources fetch.tcl; export list grown"
      exports: ["load_cached_demo", "strip_and_cache", "fetch_and_cache"]
    - path: "vmd/smoke/phase21_fetch_smoke.tcl"
      provides: "headless smoke: fixture strip census (deterministic), cache-hit/miss contracts, live 3gp6 pipeline with SKIPPED-NO-NETWORK tolerance, leftover-probe-artifact cleanup"
  key_links:
    - from: "vmd/lib/demos.tcl"
      to: "vmd/lib/fetch.tcl"
      via: "sources fetch.tcl (same lib/ dir); paths via cache_path_for, download via curl_get, promotion via promote_part/cleanup_part"
      pattern: "fetch::(cache_path_for|curl_get|promote_part|cleanup_part|is_cached)"
    - from: "vmd/lib/demos.tcl load_demo"
      to: "DEMO_MANIFEST source field"
      via: "[dict get $meta source] eq bundled vs memprotmd/sasbdb branch (the 14-02 stub's explicit Phase-21 pointer)"
      pattern: "dict get \\$meta source"
    - from: "vmd/gui/fetch_dialog.tcl (21-07)"
      to: "vmd/lib/demos.tcl strip_and_cache / load_cached_demo"
      via: "the async GUI pipeline calls the same strip/load primitives"
      pattern: "demos::(strip_and_cache|load_cached_demo)"
---

<objective>
Wire the fetched-demo mol bridge into `vmd/lib/demos.tcl`: source-aware load_demo, cache-hit loader, the probe-verified strip+cache pipeline, and the blocking fetch orchestrator — plus the phase's network-flake-proof fetch smoke.

Purpose: this is the VMD-side core of DEMO-02/03 (fetch -> strip -> cache -> load). The GUI (21-07/21-08) and the smokes both consume exactly these primitives.
Output: extended `vmd/lib/demos.tcl` + `vmd/smoke/phase21_fetch_smoke.tcl` (PASS=1 x3, network-tolerant).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/21-large-fetched-demos-attribution/21-RESEARCH-vmd-fetch-mechanics.md
@.planning/phases/21-large-fetched-demos-attribution/21-RESEARCH-v1-carryover.md
@vmd/lib/demos.tcl
@vmd/lib/fetch.tcl

Key facts (do NOT re-derive — probe-verified):
- **Strip selector (THE one):** `atomselect $m "not water and not ions"` — probe-exact on real MemProtMD files (3gp6: water=75,789 SOL, ions=229 NA+CL, dry=19,221; 1gzm: dry=25,974). Equivalence to explicit `not resname SOL and not resname NA and not resname CL` is probe-verified. **NEVER `solvent`** (probe: 93,518 = water+ions+DPP lipid — would delete the membrane the demo exists to show). Comment the trap + the v1 Pitfall F6/C distinctions (strip is a data transform, NOT the hider-cleanup sentinel filter; explicit resn/keyword strip cannot drop structural ions like ZN/CA/MG by construction).
- **writepdb quirks (probeC):** beta round-trips; segid truncates to 3 chars on READ (GAME->GAM) — irrelevant here (sentinels are re-set after load by the game); writepdb output = CRYST1 + ATOM/TER + END, single frame (good: the 17.2-04 multi-frame raciness rule wants single-frame molecules — note it).
- **Strip gate:** `strip==1 && source eq "memprotmd"` (v1's double gate, demos.py:476). For strip=0 (sasdpg4): `file rename -force` the wet .part directly to the cache name (byte-identical, zero VMD rewrite risk to the 2,601 HETATM glycan atoms).
- **fetch_and_cache (blocking; smoke/console path):** ensure_cache_dir -> wet = `<cache_dir>/<demo_id>_wet.part.pdb` -> `fetch::curl_get $fetch_url $wet <min_bytes>` (min bytes per source: memprotmd 7000000, sasbdb 300000 — constants here with a comment citing DATA_SOURCES byte counts) -> if strip: `mol new $wet type pdb` (catch -> cleanup + classified error), `atomselect ... "not water and not ions"`, `$sel writepdb <cache_name>.part`, `$sel delete`, sanity `molinfo get numatoms` on the wet mol BEFORE delete (log it), `mol delete`, promote dry part via `fetch::promote_part`, delete wet via `fetch::cleanup_part`; else: promote wet part directly (wet.part IS the cache). Return the final cache path.
- **load_demo branch (replace the 14-02 reject at demos.tcl:65-70):** bundled -> unchanged; fetched -> `fetch::is_cached` ? load cache file : `return -code error "demo not cached: fetch required: <id>"`. `load_cached_demo {demo_id}` = the cache-hit half (mol new type pdb -> molid; missing -> same error) so the GUI can call it directly.
- **fetch_pdb (RCSB fetch mode) stays a STUB** — SETUP-01's RCSB-code fetch is out of Phase-21 scope (carryover verdict #23); update its error message to say Phase-21 scope is the 3 manifest demos.
- **Leftover probe artifact:** `vmd/data/demos/cache/3gp6_dry.pdb` exists from probeE (gitignored; `rm` is denied for agents). The smoke prelude does `file delete -force [fetch::cache_dir]`'s contents — actually `file delete -force $cachedir` then re-mkdir (clean-slate per run; Tcl file delete, not rm).
- **Smoke network tolerance (verdict 11):** live 3gp6 leg marks `BCHM_SMOKE_NOTE SKIPPED-NO-NETWORK` on network-class errors (ENOENT/6/7/28 fragments per fetch::classify_curl_error wording), never hard FAIL; a pre-seeded cache (seeded from the staging-copied `tmp/p21probe/3gp6_dry.pdb` — see the probe-seed staging convention in the harness note) lets the cache-hit + census legs run offline deterministically.
- demos.tcl must stay Tk-free (GUI progress lives in 21-07).
- Smoke harness (exact commands):
```
mkdir -p tmp/biochemeleon-vmd && cp -r vmd tmp/biochemeleon-vmd/ && { cp -r tmp/p21probe tmp/biochemeleon-vmd/ 2>/dev/null || true; }
timeout 600 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/smoke/phase21_fetch_smoke.tcl -eofexit < /dev/null' 2>&1 | tail -40
```
- **Probe-seed staging convention (ONE rule across 21-05/21-07/21-09):** the harness copies the probe tree INTO staging (the `cp -r tmp/p21probe tmp/biochemeleon-vmd/` line above), so smoke code reads the literal staging-relative seed path `tmp/p21probe/<file>` from the staging cwd (it resolves to tmp/biochemeleon-vmd/tmp/p21probe/<file>). Smokes NEVER use `../` or repo-absolute seed paths; the copy is best-effort (`|| true` — fresh clones lack tmp/p21probe), and every seed use stays behind a `file exists` guard so absence routes to the live-network path. Census pins (19,221 / 4,123 / 2,601 / 25,974) assert whenever the seeds ARE present (the norm on this machine) — they are NOT silently network-dependent.
`BCHM_SMOKE_RESULT PASS=1` marker, NEVER $?; x3 sequential runs; scan FULL logs for `ERROR)` AND `bad switch`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: load_demo source branch + load_cached_demo</name>
  <files>vmd/lib/demos.tcl</files>
  <action>
1. Add `source [file join [file dirname [info script]] fetch.tcl]` next to the setup_state source line (comment: cache/fetch primitives live in fetch.tcl, 21-02/21-03).
2. `load_cached_demo {demo_id}`: path = `fetch::cache_path_for` (its bundled/unknown guards apply); `file exists` else the "fetch required" error; `mol new $path type pdb` in catch (v1 Pitfall F1: ALWAYS explicit type on cache/temp files) -> molid.
3. Replace load_demo's non-bundled reject: bundled -> existing path; fetched -> `if {[fetch::is_cached $demo_id]} { load_cached_demo $demo_id } { return -code error "demo not cached: fetch required: $demo_id" }`. Keep the docstring contract (errors via -code error; GUI catches).
4. Grow `namespace export` with load_cached_demo strip_and_cache fetch_and_cache.
Commit: `feat(21-05): demos.tcl fetched-demo cache-hit path + source-aware load_demo`
  </action>
  <verify>Staged quick check: source demos.tcl headless clean; `load_demo 3gp6` with no cache -> error containing "fetch required"; copy any small staged demo PDB to `<cache>/3gp6_dry.pdb` -> `load_demo 3gp6` returns a live molid. Phase-14 behavior preserved: all 6 bundled demos still load (run the existing phase14_mol_smoke in Task 3).</verify>
  <done>Cache-hit path loads with explicit type; miss signals fetch-required; bundled path byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: strip_and_cache + fetch_and_cache (the probeE pipeline)</name>
  <files>vmd/lib/demos.tcl</files>
  <action>
Implement both procs per the context facts (selector, gate, atomic promotion, wet cleanup, classified errors, min-bytes constants with DATA_SOURCES citation). Every failure path: `fetch::cleanup_part` the .part files, `mol delete` any loaded wet mol (catch-guarded), then `return -code error`. Docstring records the strip-before-load invariant + the strip-vs-hider-cleanup distinction.
Commit: `feat(21-05): strip_and_cache + fetch_and_cache (probe-verified strip, atomic cache promotion)`
  </action>
  <verify>Staged: with a staged wet fixture (Task 3 covers it properly) — here at minimum `tclsh` syntax-clean and the 8.6/purity gates zero on demos.tcl (`grep -rnE "\blmap\b|\btry\b|\bthrow\b|\btailcall\b|\bcoroutine\b|\byield\b|\bfinally\b" vmd/lib/demos.tcl` -> zero; `grep -nE "tk_version|toplevel|ttk" vmd/lib/demos.tcl` -> zero).</verify>
  <done>Blocking pipeline implemented; all failure paths clean up and throw user-readable errors.</done>
</task>

<task type="auto">
  <name>Task 3: phase21_fetch_smoke.tcl — fixture census + contracts + live pipeline</name>
  <files>vmd/smoke/phase21_fetch_smoke.tcl</files>
  <action>
Headless smoke (repo contract; BCHM_SMOKE_RESULT marker, full-log scan):
1. Prelude: `file delete -force [fetch::cache_dir]` (removes the leftover probe artifact + stale cache; Tcl delete, NOT rm) then `fetch::ensure_cache_dir`.
2. Fixture strip census (deterministic, offline): format ~8 ATOM lines with the standard PDB template (3 protein MET atoms, 2 SOL, 1 NA, 1 CL, 1 DPP; cols per the 78-col format used in mutation.tcl) -> write as a fake wet file named `<cache>/3gp6_wet.part.pdb` -> `mol new` it -> `strip_and_cache 3gp6 <molid>` -> `mol delete` -> reload the promoted `3gp6_dry.pdb` -> assert EXACTLY 4 atoms (3 MET + 1 DPP), `atomselect water` count 0, `atomselect ions` count 0, wet part deleted, dry part never left behind. Also assert the DPP survived (the solvent-trap discriminator).
3. Contract legs: `load_demo 3gp6` cache-hit -> live molid; `file delete` the cache file -> `load_demo 3gp6` -> error contains "fetch required"; `load_cached_demo sasdpg4` with no cache -> "fetch required".
4. Live leg (network-tolerant): if `tmp/p21probe/3gp6_dry.pdb` exists (staging-relative — present because the harness cp'd tmp/p21probe into staging per the probe-seed staging convention), seed the cache from it and assert census offline: 19,221 atoms, water 0, ions 0. Else attempt `fetch_and_cache 3gp6` live: on success assert 19,221 atoms + 0 water + 0 ions + cache file exists + wet part gone, then `fetch_and_cache sasdpg4` -> assert 4,123 atoms and atomselect `resname NAG MAN BMA NAN GLB AFL NGA GLA` count 2601 (glycan intact — the never-strip proof); on network-class error print `BCHM_SMOKE_NOTE SKIPPED-NO-NETWORK (<msg>)` and continue. Any other live error -> FAIL.
5. Regression: source-order run of the existing phase14_mol_smoke expectations still holds (run `vmd/smoke/phase14_mol_smoke.tcl` once in the same staged tree; its PASS must be unchanged).
6. Marker: `BCHM_SMOKE_RESULT PASS=1` when the deterministic legs pass AND (live legs passed OR honest skips recorded). Run x3 sequential.
Commit: `test(21-05): phase21 fetch/strip/cache smoke (fixture census + contracts + network-tolerant live leg)`
  </action>
  <verify>
```
timeout 600 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/smoke/phase21_fetch_smoke.tcl -eofexit < /dev/null' 2>&1 | tail -40
```
PASS=1 x3 (or PASS=1 + recorded SKIPPED-NO-NETWORK verdicts in all three); ZERO `ERROR)` / `bad switch` in all full logs; `Exiting normally` every run; phase14_mol_smoke still PASS.</verify>
  <done>The strip census matches the probe numbers on both fixture and real data paths; cache contracts pinned; SASDPG4 never-strip proven when live data available; gate is connectivity-flake-proof.</done>
</task>

</tasks>

<verification>
- phase21_fetch_smoke PASS=1 x3 with zero `ERROR)` / `bad switch` in all full logs
- phase14_mol_smoke regression still PASS (bundled path untouched)
- 8.6-idiom + Tk-free gates zero on demos.tcl
- The exact dry census (19,221 for 3gp6; 4,123 with 2,601 glycan for sasdpg4) asserted whenever real data was present, with verdicts recorded in the SUMMARY
</verification>

<success_criteria>
- The VMD-side pipeline of SC1/SC2 (fetch -> strip -> cache -> load) is proven through the real mol surface
- 21-07/21-08 can compose the GUI flow purely from load_cached_demo / strip_and_cache / fetch primitives without new mol code
</success_criteria>

<output>
After completion, create `.planning/phases/21-large-fetched-demos-attribution/21-05-SUMMARY.md`
</output>
