---
phase: 06-hint-reveal
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - biochemeleon/game.py
  - tests/test_game_controller.py
autonomous: true

must_haves:
  truths:
    - "hint() colors the residues around a random hidden hider orange (neighbors, NOT the hider itself, NOT any GAME atoms), increments _hint_count, fires on_counts_changed(hint_count, reveal_count), logs a hint message"
    - "reveal_one() picks a random hidden hider, marks it found + green via _mark_found, increments _reveal_count by 1, fires on_counts_changed + on_remaining_changed, logs 'Revealed one! N remaining', checks win"
    - "reveal_all() marks ALL hidden hiders found + green via _mark_found loop, increments _reveal_count by N (one per hider revealed), fires on_counts_changed + on_remaining_changed(0), logs 'Revealed all N hiders', calls win()"
    - "_mark_found(hider_id) is the shared helper: registry.mark_found + cmd.color('green', 'obj and id N') — no log, no win-check (callers do those); on_pick is refactored to call it"
    - "_reveal_count and _hint_count are zeroed in __init__ and reset to 0 in start() alongside the registry reset"
    - "on_counts_changed is the 4th set_callbacks param (default no-op lambda); existing 3-arg calls still work (backward compat)"
    - "hint/reveal_one/reveal_all guard with 'if not self._started: return' and 'if not hidden: return' (no IndexError on empty)"
  artifacts:
    - path: "biochemeleon/game.py"
      provides: "GameController.hint / reveal_one / reveal_all / _mark_found + _reveal_count / _hint_count attrs + on_counts_changed callback"
      contains: "def hint"
      min_lines: 180
    - path: "tests/test_game_controller.py"
      provides: "WSL unit tests for hint/reveal_one/reveal_all/_mark_found/counters/4th-callback (10 new tests)"
      contains: "TestGameControllerHintReveal"
      min_lines: 280
  key_links:
    - from: "biochemeleon/game.py GameController.hint"
      to: "cmd.color('orange', sele with 'around' and 'not segi GAME') + on_counts_changed"
      via: "random.choice(hidden) -> byres around selection -> cmd.color -> _hint_count++ -> callback"
      pattern: "cmd\\.color|on_counts_changed|around"
    - from: "biochemeleon/game.py GameController.reveal_one"
      to: "_mark_found -> registry.mark_found + cmd.color('green') + on_counts_changed + on_remaining_changed + win check"
      via: "random.choice(hidden) -> _mark_found -> _reveal_count++ -> callbacks -> win()"
      pattern: "_mark_found|_reveal_count|on_counts_changed"
    - from: "biochemeleon/game.py GameController.reveal_all"
      to: "_mark_found loop + _reveal_count += N + on_remaining_changed(0) + win()"
      via: "for rec in hidden: _mark_found -> _reveal_count += len(hidden) -> win()"
      pattern: "_mark_found|_reveal_count|win\\(\\)"
    - from: "biochemeleon/game.py GameController.on_pick"
      to: "_mark_found (refactored from inline mark_found + cmd.color)"
      via: "self._mark_found(picked_id) replaces inline registry.mark_found + cmd.color"
      pattern: "self\\._mark_found"
    - from: "biochemeleon/game.py GameController.set_callbacks"
      to: "self._on_counts_changed = on_counts_changed or (lambda h, r: None)"
      via: "4th positional-or-keyword param appended after on_win"
      pattern: "on_counts_changed"
---

<objective>
TDD the GameController Phase-6 extensions: `hint()`, `reveal_one()`,
`reveal_all()`, `_mark_found()` (shared helper), `_reveal_count`/`_hint_count`
counter attrs (reset in `start()`), and `on_counts_changed` as the 4th
`set_callbacks` param (default no-op, backward-compatible).

Purpose: This is the hint/reveal LOGIC (GAME-05 hint neighbors, GAME-06
reveal-one, GAME-07 reveal-all, DIFF-01 reveal counter). The GUI (plan 06-02)
calls these controller methods after showing confirm dialogs. The controller
owns the coloring + mark_found + counter logic; the GUI owns the Qt dialogs.
The callback interface (`on_counts_changed`) keeps the controller WSL-testable
(mock cmd + mock callbacks + manually-populated registry) WITHOUT calling
`start()` (which needs real cmd — same pattern as the existing Phase-4 tests).

Output: `biochemeleon/game.py` extended (141 → ~210 lines) +
`tests/test_game_controller.py` extended (217 → ~360 lines) following
RED → GREEN → REFACTOR.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-hint-reveal/06-RESEARCH.md
@AGENTS.md

# GameController to extend (Phase 4 proven; start/cleanup/abort UNCHANGED):
@biochemeleon/game.py

# Registry hint/reveal reads/writes (mark_found raises KeyError on unregistered):
@biochemeleon/registry.py

# Mock-cmd test pattern (stub pymol/pymol.Qt via sys.modules):
@tests/test_game_controller.py
</context>

<feature>
  <name>GameController.hint + reveal_one + reveal_all + _mark_found + counters + on_counts_changed</name>
  <files>biochemeleon/game.py, tests/test_game_controller.py</files>
  <behavior>
    `hint()` — GAME-05: color the residues around a random hidden hider orange.
    Guards: `if not self._started: return`; `hidden = [r for r in self.registry.all() if r.status == HIDER_STATUS_HIDDEN]`; `if not hidden: return`.
    Pick `rec = random.choice(hidden)`. Build selection:
    `sele = "(byres (%s and id %d around %s)) and not segi GAME" % (self.target_obj, rec.id, HINT_RADIUS)`.
    Gate: `if cmd.count_atoms(sele) > 0: cmd.color(HINT_COLOR, sele)`.
    Increment `self._hint_count += 1`. Fire `self._on_counts_changed(self._hint_count, self._reveal_count)`.
    Log `self._on_log("Hint: highlighted neighbors of one hider.")`.
    NO confirm dialog (Hint is help, not give-up). NO mark_found (status stays hidden).

    `reveal_one()` — GAME-06 + DIFF-01: mark one random hidden hider found.
    Guards: `if not self._started: return`; `hidden = [...]`; `if not hidden: return`.
    Pick `rec = random.choice(hidden)`. Call `self._mark_found(rec.id)`.
    `self._reveal_count += 1`. Fire `self._on_counts_changed(self._hint_count, self._reveal_count)`.
    `remaining = self._remaining()`. Log `"Revealed one! %d remaining" % remaining`.
    Fire `self._on_remaining_changed(remaining)`. If `remaining == 0: self.win()`.
    NO confirm dialog here (the GUI shows it before calling).

    `reveal_all()` — GAME-07 + DIFF-01: mark all hidden hiders found.
    Guards: same. `for rec in hidden: self._mark_found(rec.id)`.
    `self._reveal_count += len(hidden)` (count hiders revealed, NOT +1 for the action).
    Fire `self._on_counts_changed(self._hint_count, self._reveal_count)`.
    Fire `self._on_remaining_changed(0)`. Log `"Revealed all %d hiders. Game over." % len(hidden)`.
    Call `self.win()` (all found → win fires).

    `_mark_found(self, hider_id)` — shared helper (refactor of on_pick's inline logic):
    `self.registry.mark_found(self.target_obj, hider_id)` +
    `cmd.color('green', "%s and id %s" % (self.target_obj, hider_id))`.
    Does NOT log (caller logs) and does NOT fire win (caller checks remaining + calls win).
    Used by: `on_pick` (refactored), `reveal_one`, `reveal_all`.

    Counters: `self._reveal_count = 0` and `self._hint_count = 0` in `__init__`.
    Both reset in `start()` alongside `self.registry = registry.HiderRegistry()`.

    `set_callbacks(self, on_log=None, on_remaining_changed=None, on_win=None, on_counts_changed=None)` —
    4th positional-or-keyword param appended after `on_win`. Default no-op lambda
    `(lambda h, r: None)`. Existing 3-arg calls still work (4th defaults to no-op).

    Module constants: `HINT_RADIUS = 5.0` (CA-CA ~3.8 Å, captures adjacent residues),
    `HINT_COLOR = 'orange'` (distinct from green=found + blend colors).

    Cases (testable with mock cmd + populated registry, WITHOUT start()):
      - hint() with 3 hidden -> _hint_count==1; on_counts_changed called with (1, 0);
        cmd.color called with ('orange', sele) where sele contains 'around' and 'not segi GAME';
        no record status changed (all still hidden); on_log called
      - hint() before start (_started=False) -> no-op: _hint_count stays 0, cmd.color NOT called
      - hint() with all found (no hidden) -> no-op: _hint_count stays 0
      - reveal_one() with 3 hidden -> exactly 1 found (status); _reveal_count==1;
        on_counts_changed called with (0, 1); on_remaining_changed called with 2;
        cmd.color called with ('green', '1ubq and id N'); on_log called; win NOT fired (2 remaining)
      - reveal_one() with 1 hidden -> found; _reveal_count==1; win fires (on_win called)
      - reveal_all() with 3 hidden -> all 3 found; _reveal_count==3;
        on_remaining_changed called with 0; win fires (on_win called once)
      - reveal_all() before start -> no-op
      - _mark_found(id) directly -> registry.get(...).status == 'found';
        cmd.color called once with ('green', '1ubq and id N')
      - fresh GameController has _reveal_count==0 and _hint_count==0
      - set_callbacks() with no args -> _on_counts_changed is a no-op (returns None)
      - set_callbacks(on_counts_changed=cb) -> _on_counts_changed(h, r) calls cb(h, r)
  </behavior>
  <implementation>
    Extend the existing `GameController` class. DO NOT change `start`'s signature
    or hider-spec dispatch logic — only ADD the two counter-reset lines inside
    it. DO NOT change `cleanup`/`abort_on_error`/`reconstruct_registry` (Phase 3
    proven). DO NOT change `win` (Phase 4 proven). The `on_pick` refactor (calling
    `_mark_found`) is the REFACTOR task — no behavior change.

    Add `import random` at the top (after `import time`, before `from pymol import cmd`).
    Add module constants `HINT_RADIUS = 5.0` and `HINT_COLOR = 'orange'` after
    the imports, before the class.

    In `__init__`, ADD (do NOT remove existing attrs):
    ```python
    self._reveal_count = 0
    self._hint_count = 0
    self._on_counts_changed = lambda h, r: None
    ```

    In `start()`, ADD these two lines right after `self.registry = registry.HiderRegistry()`:
    ```python
    self._reveal_count = 0
    self._hint_count = 0
    ```

    Update `set_callbacks` to add the 4th param:
    ```python
    def set_callbacks(self, on_log=None, on_remaining_changed=None,
                      on_win=None, on_counts_changed=None):
        self._on_log = on_log or (lambda msg: None)
        self._on_remaining_changed = on_remaining_changed or (lambda r: None)
        self._on_win = on_win or (lambda elapsed: None)
        self._on_counts_changed = on_counts_changed or (lambda h, r: None)
    ```

    Add `_mark_found` (placed AFTER `on_pick`, BEFORE `win`):
    ```python
    def _mark_found(self, hider_id):
        """Shared mark+color helper. Does NOT log or fire win (callers do those)."""
        self.registry.mark_found(self.target_obj, hider_id)
        cmd.color('green', "%s and id %s" % (self.target_obj, hider_id))
    ```

    Add `hint` (placed AFTER `win`, BEFORE `reconstruct_registry`):
    ```python
    def hint(self):
        """Color the residues around a random hidden hider orange (GAME-05)."""
        if not self._started:
            return
        hidden = [r for r in self.registry.all()
                  if r.status == registry.HIDER_STATUS_HIDDEN]
        if not hidden:
            return
        rec = random.choice(hidden)
        sele = "(byres (%s and id %d around %s)) and not segi GAME" % (
            self.target_obj, rec.id, HINT_RADIUS)
        if cmd.count_atoms(sele) > 0:
            cmd.color(HINT_COLOR, sele)
        self._hint_count += 1
        self._on_counts_changed(self._hint_count, self._reveal_count)
        self._on_log("Hint: highlighted neighbors of one hider.")
    ```

    Add `reveal_one` (placed after `hint`):
    ```python
    def reveal_one(self):
        """Mark one random hidden hider found + green; count the reveal (GAME-06)."""
        if not self._started:
            return
        hidden = [r for r in self.registry.all()
                  if r.status == registry.HIDER_STATUS_HIDDEN]
        if not hidden:
            return
        rec = random.choice(hidden)
        self._mark_found(rec.id)
        self._reveal_count += 1
        self._on_counts_changed(self._hint_count, self._reveal_count)
        remaining = self._remaining()
        self._on_log("Revealed one! %d remaining" % remaining)
        self._on_remaining_changed(remaining)
        if remaining == 0:
            self.win()
    ```

    Add `reveal_all` (placed after `reveal_one`):
    ```python
    def reveal_all(self):
        """Mark all hidden hiders found + green; count reveals (GAME-07)."""
        if not self._started:
            return
        hidden = [r for r in self.registry.all()
                  if r.status == registry.HIDER_STATUS_HIDDEN]
        if not hidden:
            return
        for rec in hidden:
            self._mark_found(rec.id)
        self._reveal_count += len(hidden)
        self._on_counts_changed(self._hint_count, self._reveal_count)
        self._on_remaining_changed(0)
        self._on_log("Revealed all %d hiders. Game over." % len(hidden))
        self.win()
    ```

    REFACTOR `on_pick` — replace the inline `mark_found + cmd.color` with a
    call to `self._mark_found(picked_id)`. The two lines:
    ```python
    self.registry.mark_found(self.target_obj, picked_id)
    cmd.color('green', "%s and id %s" % (self.target_obj, picked_id))
    ```
    become:
    ```python
    self._mark_found(picked_id)
    ```
    Everything else in on_pick stays the same (the rec None check, the
    already-found check, the remaining + log + callback + win-check).
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>RED: write failing tests for hint/reveal/counters (TestGameControllerHintReveal)</name>
  <files>tests/test_game_controller.py</files>
  <action>
    APPEND a new test class `TestGameControllerHintReveal(unittest.TestCase)` to
    the EXISTING `tests/test_game_controller.py` (DO NOT modify the existing
    `TestGameControllerOnPick` class — its tests must still pass unchanged).

    The file already has the pymol/pymol.Qt stub pattern at the top (lines 1-33)
    and imports `GameController` + `HIDER_STATUS_HIDDEN`/`HIDER_STATUS_FOUND`.
    Use those imports; do NOT re-import.

    The new class's `setUp` builds a fresh `GameController('1ubq')` with
    `self.gc._started = True` (hint/reveal guard on _started; the existing
    on_pick tests don't set it because on_pick has no such guard — but hint/
    reveal do). Reset mock cmd history: `game.cmd.reset_mock()`.

    Tests to write (10 total):

    1. `test_hint_colors_neighbors`: register 3 hidden (ids 100, 101, 102);
       mock callbacks (`counts = MagicMock()`; `gc.set_callbacks(on_counts_changed=counts)`).
       Call `gc.hint()`. Assert: `gc._hint_count == 1`; `counts.assert_called_once_with(1, 0)`;
       `game.cmd.color.assert_called_once()` — first arg is `'orange'`, second arg
       contains `'around'` and `'not segi GAME'` (use `assertIn('around', sele_arg)`
       and `assertIn('not segi GAME', sele_arg)` on `game.cmd.color.call_args[0][1]`);
       all 3 records still `HIDER_STATUS_HIDDEN` (no mark_found); `game.cmd.count_atoms`
       was called (the gate before cmd.color).

    2. `test_hint_no_started`: `gc2 = GameController('1ubq')` (do NOT set _started);
       register 1 hidden; call `gc2.hint()`. Assert: `gc2._hint_count == 0`;
       `game.cmd.color.assert_not_called()` (no coloring before start).

    3. `test_hint_no_hidden`: register 1 hider with `status=HIDER_STATUS_FOUND`;
       call `gc.hint()`. Assert: `gc._hint_count == 0` (no hidden → no-op);
       `game.cmd.color.assert_not_called()`.

    4. `test_reveal_one`: register 3 hidden (ids 100, 101, 102); mock callbacks
       (`counts = MagicMock()`, `rem = MagicMock()`, `win_cb = MagicMock()`;
       `gc.set_callbacks(on_remaining_changed=rem, on_win=win_cb, on_counts_changed=counts)`).
       Call `gc.reveal_one()`. Assert: exactly 1 of the 3 is now `HIDER_STATUS_FOUND`
       (count found == 1); `gc._reveal_count == 1`; `counts.assert_called_once_with(0, 1)`;
       `rem.assert_called_once()` with the remaining count (2); `game.cmd.color.assert_called_once()`
       with `('green', ...)` (first arg is `'green'`); `win_cb.assert_not_called()` (2 remaining).

    5. `test_reveal_one_last`: register 1 hidden (id 100); `gc._start_time = 1000.0`;
       mock win_cb; `gc.set_callbacks(on_win=win_cb)`. Call `gc.reveal_one()`.
       Assert: record 100 status == `'found'`; `gc._reveal_count == 1`; `win_cb.assert_called_once()`.

    6. `test_reveal_all`: register 3 hidden (ids 100, 101, 102); mock callbacks
       (`counts`, `rem`, `win_cb`); `gc._start_time = 1000.0`;
       `gc.set_callbacks(on_remaining_changed=rem, on_win=win_cb, on_counts_changed=counts)`.
       Call `gc.reveal_all()`. Assert: all 3 status == `'found'`;
       `gc._reveal_count == 3` (one per hider revealed); `counts.assert_called_once_with(0, 3)`;
       `rem.assert_called_once_with(0)`; `win_cb.assert_called_once()`.

    7. `test_reveal_all_no_started`: `gc2 = GameController('1ubq')` (no _started);
       register 1 hidden; call `gc2.reveal_all()`. Assert: `gc2._reveal_count == 0`;
       record still hidden; `game.cmd.color.assert_not_called()`.

    8. `test_mark_found_helper`: register 1 hidden (id 100); `game.cmd.reset_mock()`;
       call `gc._mark_found(100)`. Assert: `gc.registry.get('1ubq', 100).status == HIDER_STATUS_FOUND`;
       `game.cmd.color.assert_called_once_with('green', '1ubq and id 100')`.

    9. `test_counters_init_zero`: `gc2 = GameController('1ubq')`. Assert:
       `gc2._reveal_count == 0` and `gc2._hint_count == 0`.

    10. `test_set_callbacks_4th_param`: `gc2 = GameController('1ubq')`;
        `gc2.set_callbacks()` (all None) → `self.assertIsNone(gc2._on_counts_changed(0, 0))`
        (no-op default). Then `cb = MagicMock()`; `gc3 = GameController('1ubq')`;
        `gc3.set_callbacks(on_counts_changed=cb)`; `gc3._on_counts_changed(1, 2)`;
        `cb.assert_called_once_with(1, 2)`.

    Run — the new tests MUST fail (AttributeError: GameController has no
    hint / reveal_one / reveal_all / _mark_found; no _reveal_count / _hint_count
    attrs; no _on_counts_changed attr). The EXISTING 8 tests (TestGameControllerOnPick)
    MUST still pass. Confirm RED (new tests fail, existing tests pass).

    Commit RED: `git add tests/test_game_controller.py && git commit -m "test(06-01): add failing tests for hint/reveal_one/reveal_all/_mark_found/counters"`
  </action>
  <verify>
    `python3.6 -m unittest tests.test_game_controller -v` — the 10 NEW tests FAIL
    with AttributeError (methods/attrs absent); the 8 EXISTING tests (TestGameControllerOnPick)
    PASS. Confirm the failure is the expected RED state, not a test-logic or import error.
  </verify>
  <done>
    tests/test_game_controller.py has the new TestGameControllerHintReveal class with
    10 test methods; new tests fail (AttributeError); existing 8 tests pass; RED commit landed.
  </done>
</task>

<task type="auto">
  <name>GREEN: implement hint/reveal_one/reveal_all/_mark_found + counters + 4th callback</name>
  <files>biochemeleon/game.py</files>
  <action>
    Extend `biochemeleon/game.py` EXACTLY as in the
    `<feature><implementation>` block above:
    - Add `import random` at the top (after `import time`, before `from pymol import cmd`).
    - Add module constants `HINT_RADIUS = 5.0` and `HINT_COLOR = 'orange'` after
      the imports, before the `class GameController:` line.
    - In `__init__`, ADD: `self._reveal_count = 0`, `self._hint_count = 0`,
      `self._on_counts_changed = lambda h, r: None` (do NOT remove existing attrs).
    - In `start()`, ADD `self._reveal_count = 0` and `self._hint_count = 0` right
      after `self.registry = registry.HiderRegistry()` (the existing reset line).
    - Update `set_callbacks` to add the 4th param `on_counts_changed=None` and
      `self._on_counts_changed = on_counts_changed or (lambda h, r: None)`.
    - Add `_mark_found(self, hider_id)` method (placed AFTER `on_pick`, BEFORE `win`).
    - Add `hint(self)` method (placed AFTER `win`, BEFORE `reconstruct_registry`).
    - Add `reveal_one(self)` method (placed after `hint`).
    - Add `reveal_all(self)` method (placed after `reveal_one`).

    DO NOT modify `cleanup`, `abort_on_error`, `reconstruct_registry`, or `win`
    (Phase 3/4 proven). DO NOT refactor `on_pick` yet (that's the REFACTOR task).
    DO NOT import wizard/gui_game/generators (the caller wires those).

    Run the tests — ALL 18 tests (8 existing + 10 new) MUST pass.

    Commit GREEN: `git add biochemeleon/game.py && git commit -m "feat(06-01): add GameController.hint/reveal_one/reveal_all/_mark_found + counters + on_counts_changed callback"`
  </action>
  <verify>
    Full WSL gate suite (all MUST be green):
    - `python3.6 -m py_compile biochemeleon/*.py` → clean
    - `python3.6 -m unittest tests.test_game_controller -v` → 18 pass (8 existing + 10 new)
    - `python3.6 -m unittest tests.test_setup_state -v` → 90 pass (no regression)
    - `python3.6 -m unittest tests.test_registry -v` → 54 pass (no regression)
    - `python3.6 -m unittest tests.test_generators -v` → pass (no regression)
    - Phase-3/4 methods intact (start/cleanup/abort/reconstruct/win/on_pick UNCHANGED
      except set_callbacks signature + start counter-reset lines):
      `rg -n "def start|def cleanup|def abort_on_error|def reconstruct_registry|def win|def on_pick" biochemeleon/game.py` → 6
    - New methods present:
      `rg -n "def hint|def reveal_one|def reveal_all|def _mark_found" biochemeleon/game.py` → 4
    - Counter attrs present:
      `rg -n "_reveal_count|_hint_count" biochemeleon/game.py` → >=4 (init + start reset + increment lines)
    - 4th callback present:
      `rg -n "on_counts_changed" biochemeleon/game.py` → >=2 (set_callbacks param + _mark calls)
    - Hint selection uses 'around' and 'not segi GAME':
      `rg -n "around" biochemeleon/game.py` → 1 (the hint selection)
      `rg -n "not segi GAME" biochemeleon/game.py` → 1 (the hint selection)
    - id selector lowercase in cmd.color (NOT UPPERCASE ID):
      `rg -n "and id " biochemeleon/game.py` → >=2 (_mark_found + hint selection)
    - Pitfall-1 gate (ZERO across package):
      `rg -n "import Tkinter|import tkinter|from tkinter|import Pmw|from Pmw|app\.root|grab_set|mainloop|Toplevel|menuBar\.addmenuitem|from PyQt5 import|import PyQt5" biochemeleon/` → 0
    - exec_ gate: `rg -n "\.exec_\(\)" biochemeleon/` → 0 (game.py uses no Qt)
    - No walrus := in game.py: `rg -n ":=" biochemeleon/game.py` → 0
  </verify>
  <done>
    game.py extended (141 → ~210 lines) with hint/reveal_one/reveal_all/_mark_found
    + HINT_RADIUS/HINT_COLOR constants + _reveal_count/_hint_count attrs (init +
    start reset) + on_counts_changed 4th callback; start/cleanup/abort/reconstruct/
    win UNCHANGED; on_pick NOT yet refactored (REFACTOR task); 18 tests pass;
    144+ prior tests green; all WSL gates clean; GREEN commit landed.
  </done>
</task>

<task type="auto">
  <name>REFACTOR: on_pick calls _mark_found + final gates</name>
  <files>biochemeleon/game.py</files>
  <action>
    REFACTOR `on_pick` to call `self._mark_found(picked_id)` instead of the
    inline `self.registry.mark_found(self.target_obj, picked_id)` +
    `cmd.color('green', "%s and id %s" % (self.target_obj, picked_id))`.
    Replace those two lines with one:
    ```python
    self._mark_found(picked_id)
    ```
    Everything else in `on_pick` stays the same (the rec None → "Miss!" check,
    the already-found → "Already found!" check, the remaining + log +
    on_remaining_changed + win-check). This is a pure DRY refactor — NO
    behavior change. The existing `test_found`/`test_already_found`/
    `test_found_not_last` tests must still pass unchanged.

    Do NOT add features (no GUI, no per-rep remaining — that's gui_game's job).
    Do NOT change any other method.

    Run ALL tests — 18 must still pass.

    If the refactor is clean (no additional changes needed), commit:
    `git add biochemeleon/game.py && git commit -m "refactor(06-01): on_pick calls _mark_found helper (DRY with reveal paths)"`
    If a docstring tweak is needed, re-run all gates and commit with the same message.

    Final confirmation: dependency direction holds — game.py imports only
    `pymol.cmd` + stdlib (`time`, `random`) + sibling `.backup`/`.mutation`/`.registry`.
    It does NOT import `wizard`/`gui_game`/`generators`.
  </action>
  <verify>
    - `python3.6 -m unittest tests.test_game_controller -v` → 18 pass (including
      the existing test_found which asserts `cmd.color.assert_called_once_with('green', "1ubq and id 100")`
      — the refactored _mark_found must produce the same cmd.color call)
    - `python3.6 -m py_compile biochemeleon/*.py` → clean
    - on_pick calls _mark_found:
      `rg -n "self\._mark_found" biochemeleon/game.py` → >=3 (def + on_pick + reveal_one + reveal_all)
    - No inline mark_found + cmd.color in on_pick (the two-line block is gone):
      the `rg -n "and id " biochemeleon/game.py` count stays >=2 (hint selection
      + _mark_found) but on_pick no longer has its own `cmd.color` line
    - `rg -n "from \. import wizard|from \. import gui_game|from \. import generators" biochemeleon/game.py` → 0
    - Pitfall-1 gate still 0; exec_ gate still 0
  </verify>
  <done>
    on_pick refactored to call _mark_found (DRY); all 18 tests pass; no behavior
    change; dependency direction intact; REFACTOR commit landed. Ready for plan
    06-02 (GUI buttons call these controller methods) and 06-03 (smoke + checkpoint).
  </done>
</task>

</tasks>

<verification>
GameController Phase-6 logic is WSL-verified when:
- hint/reveal_one/reveal_all/_mark_found exist and pass 10 new unit tests
- hint colors neighbors orange (not the hider, not GAME atoms); reveal marks
  found + green + increments counters; reveal-all marks all + win fires
- _mark_found is the shared helper; on_pick refactored to call it (existing
  tests unchanged)
- _reveal_count/_hint_count zeroed in __init__ + reset in start()
- on_counts_changed is the 4th callback (default no-op, backward compat)
- start/cleanup/abort_on_error/reconstruct_registry/win UNCHANGED
- 144+ prior tests green (no regression); py_compile clean; Pitfall-1=0; exec_=0
</verification>

<success_criteria>
- GameController.hint() implements GAME-05 (color neighbors orange via byres around selection)
- GameController.reveal_one() implements GAME-06 (mark random hidden found + green + count)
- GameController.reveal_all() implements GAME-07 (mark all hidden found + green + count + win)
- _reveal_count + _hint_count implement DIFF-01 (track usage, reset per round)
- on_counts_changed callback enables the GUI reveal-counter label (plan 06-02)
- All logic is WSL-unit-tested with mocked cmd; ready for GUI wiring + smoke
</success_criteria>

<output>
After completion, create `.planning/phases/06-hint-reveal/06-01-SUMMARY.md`
</output>
