---
phase: 17.2-cartoon-newcartoon-generators
plan: 03
subsystem: rep-tiers
tags: [vmd, tcl-8.5, tcltest, rep-tiers, seam-widening, cartoon, newcartoon, trace, tube, residue-splice, game-reps]

# Dependency graph
requires:
  - phase: 17.1-rep-setup-infrastructure-simple-rep-generators
    provides: rep_tiers.tcl pure module (17.1-01 seam: IMPLEMENTED_TIERS 6-name subset, TIER_KINDS free|bonded, 8 exported procs) + its 32-case tcltest suite (seed pins 173/41, drop-overflow clamp, quick-008 randomize semantics)
provides:
  - "Widened seam: IMPLEMENTED_TIERS == full GAME_REPS (10 names, order identical) -- one splice, four consumers"
  - "Third tier kind 'residue' (Cartoon/NewCartoon/Trace/Tube) in TIER_KINDS -- game.tcl 17.2-09 routes it to the shared GAM-splice generator"
  - "Bare single-word style_args for all four residue tiers ({Cartoon}/{NewCartoon}/{Trace}/{Tube}) -- no modstyle params"
  - "resolve_per_rep KEEPS explicit residue entries; lock-scene detection with a Cartoon-family scene derives a full residue round; randomize domain widened to all 10 GAME_REPS"
  - "49-case suite (was 32) as the single source of truth for the dispatch contract, pinned BEFORE the 17.2-09 dispatch consumes it"
affects: [17.2-09-dispatch (routes kind residue), 17.2-11-capstone/known-red rewrites, 17.2-01/17.2-04 splice generator (consumes the contract), phase 19 difficulty]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Seam-widening as a dedicated TDD plan: pin the contract by inverting the old suite's narrow-seam cases IN PLACE (names keep intent, exclusions re-targeted) + a new per-tier section, before any consumer lands"
    - "Per-tier test granularity (one concept per test) so a future regression names the exact tier"
    - "Full-domain randomize proof by 20-seed key-union sweep (a single draw cannot prove a domain)"
    - "Draw-dependent seed pins must be RE-PROBED when the randomize domain changes (17.2-03 widened 6 -> 10 tiers; all draws changed, sum properties re-verified)"

key-files:
  created: []
  modified:
    - vmd/lib/rep_tiers.tcl
    - vmd/tests/test_rep_tiers.test

key-decisions:
  - "IMPLEMENTED_TIERS kept as a SEPARATE constant even though it now equals GAME_REPS (consumers reference it; the seam note documents they may diverge again if GAME_REPS gains a rep without a generator)"
  - "Residue tiers pinned PER TIER (12 query cases) instead of 3 aggregate loops -- satisfies the plan's Failed>=12 RED gate honestly and gives per-tier regression naming"
  - "resolve_randomize_full_domain strengthened beyond the plan's single seeded case: added a seeds-1..20 key-union sweep == GAME_REPS, because one draw cannot prove the widened domain"
  - "resolve_lockscene_cartoon carries seed 41 (plan case omitted the seed; single-rep distribution is draw-dependent -- c=_randint 0 3 can yield 1 or 2); probed: seed 41 -> exactly {Cartoon 3}, and draws are name-independent so the 17.1-01 Licorice pin transfers"

patterns-established:
  - "Inverted-in-place convention: old narrow-seam tests keep their names' intent with re-targeted exclusions (Ribbons/Bonds) + a one-line 'inverted by 17.2-03' comment; widened expectations live in the new section"
  - "Suite header documents every seed pin WITH its domain (re-probe on binary OR domain change)"

# Metrics
duration: 88 min
completed: 2026-09-05
---

# Phase 17.2 Plan 03: Seam Widening (Residue Tier Family) Summary

**IMPLEMENTED_TIERS widened to the full 10-name GAME_REPS with a third tier kind "residue" for Cartoon/NewCartoon/Trace/Tube -- constants-only change, 49/49 suite green, dispatch contract pinned before 17.2-09 consumes it.**

## Performance

- **Duration:** 88 min
- **Started:** 2026-09-05T16:27:56Z
- **Completed:** 2026-09-05T17:56:04Z
- **Tasks:** 2 (RED + GREEN; REFACTOR not needed -- constants and comments only)
- **Files modified:** 2

## Accomplishments
- `vmd/lib/rep_tiers.tcl`: IMPLEMENTED_TIERS = `{Lines VDW Licorice CPK Cartoon NewCartoon Trace Tube Points DynamicBonds}` (== GAME_REPS, order included); TIER_KINDS gains the four `residue` entries; header seam note rewritten (tier = {name, kind in free|bonded|residue, style_args}; one splice, four consumers; NEVER `mol ssrecalc` in the residue flow). **ZERO proc-body/signature changes** -- style_args/resolve/tiers/effective_total were already constant-driven, exactly as the plan predicted.
- `vmd/tests/test_rep_tiers.test`: 32 -> 49 cases. The 17.1-01 narrow-seam pins inverted in place (exclusions re-targeted to Ribbons/Bonds), new 17.2 section pins the four residue tiers per tier + resolve keep/lock-scene/randomize-domain/ordering/effective-total/scene-detection contracts.
- Suite count transition documented: **rep_tiers 32 (17.1-01) -> 49 (17.2-03)**; full-suite gate **147 -> 164** cases, all green.

## Task Commits

Each task was committed atomically on `exec/17.2-03`:

1. **Task 1: RED -- invert + add seam tests** - `ebd9310` (test)
2. **Task 2: GREEN -- widen the constants** - `ddda863` (feat)

_REFACTOR: none (no cleanup warranted; constants-only change)._

## Verification Evidence (all gates from the plan)

- **RED (Task 1):** staged headless VMD run -- `BCHM_TEST_RESULT Total=49 Passed=33 Failed=16` (>= 12 gate OK); the 16 failures are exactly the widened-contract cases (12 per-tier query inversions + resolve_keeps_residue_per_rep + resolve_lockscene_cartoon + resolve_l0_residue_only_kept_verbatim + resolve_randomize_full_domain); **0 `ERROR)`, 0 `bad switch`** in the full log.
- **GREEN (Task 2):** `BCHM_TEST_RESULT Total=49 Passed=49 Failed=0`, **0 `ERROR)`, 0 `bad switch`**, `Info) Exiting normally`.
- **Full-suite gate (sequential, fresh staging tmp/biochemeleon-vmd):** setup_state 47/47, registry 27/27, generators 26/26, rep_tiers 49/49, game_logic 15/15 -> **164/164, Failed=0**.
- **Tcl 8.6 gate:** zero matches on both files.
- **Purity gate:** 0 code matches for mol/atomselect/tk in rep_tiers.tcl (1 grep hit = the plan-MANDATED header comment "NEVER call `mol ssrecalc`" -- comment-only; grep over comment-stripped source = 0).
- **Proc-signature gate:** `git diff` shows header comments + 2 constants + 1 docstring only; all 8 `proc ::biochemeleon::rep_tiers::` signatures byte-identical.
- **IMPLEMENTED_TIERS == GAME_REPS:** both constants verified char-identical (order included).
- **tclsh load-clean gate:** `tclsh` is NOT available in WSL (matches the 17.1-03 finding) -- the load-clean property is proven by the staged suite run itself (test_rep_tiers.test `source`s rep_tiers.tcl; a load error would abort before the marker).

## KNOWN RED (owned by 17.2-11, NOT touched here -- smoke-file ownership)

- `vmd/smoke/phase17_capstone_smoke.tcl` section C (under-generation case `start_game $m3 3 {Cartoon 2 VDW 1} 0` asserted Cartoon DROPPED) now FAILS post-widening -- captured evidence: `BCHM_SMOKE_RESULT PASS=0 FAIL=undergen_per_rep_size:exp=1 got=2 (per_rep=VDW 1 Cartoon 2),undergen_eff_total:exp=1 got=3 (P9 effective total, not the requested 3),rep_count_c:exp=3 got=5`; 0 `ERROR)` (assertion failure, not a crash). 17.2-11 rewrites the case with a non-GAME_REPS style (Ribbons).

## Discoveries for 17.2-09 / 17.2-11 (beyond the plan's known-red note)

- **`vmd/smoke/phase17_dispatch_smoke.tcl` line 343 is now DRAW-DEPENDENT-FLAKY:** its bare 2-arg randomize round (`start_game $m2 5`, no seed) draws over the WIDENED 10-tier domain; any draw containing a residue tier fails the round's `reg2_count`/`rep_count2`/user3-stamp asserts because game.tcl (17.1-06) hits its defensive `else` (warn "no generator yet -- skipped", tier_counts 0) for kind "residue" until 17.2-09 adds the residue route. Verified one run PASS=1 (all-simple draw); probability of a residue-contaminated draw is high (~40% for a 1-rep subset). This smoke is the first thing to re-check after 17.2-09 lands.
- All other 15 smokes use explicit simple-tier per_rep dicts (`[dict create VDW n]` etc.) -> resolve keeps them verbatim -> structurally unaffected by the widening (verified by call-site grep: the dispatch smoke is the ONLY bare 2-arg start_game).
- game.tcl's residue-kind reachability is the exact seam this plan pins: kind "residue" now RESOLVES into per_rep (kept verbatim / lock-scene derivation / randomize domain) but the 17.1-06 dispatch skips it with a warning -- by design until 17.2-09.

## Files Created/Modified
- `vmd/lib/rep_tiers.tcl` -- widened seam constants + rewritten seam note (+28/-10 lines, constants and comments only)
- `vmd/tests/test_rep_tiers.test` -- inverted narrow-seam cases + 17 new residue-family cases (+186/-40 -> 49 cases)

## Decisions Made
See `key-decisions` frontmatter. Highlights:
- Kept IMPLEMENTED_TIERS as a separate constant despite equality with GAME_REPS (consumer-facing seam; divergence documented).
- Per-tier test granularity (12 cases) to satisfy the Failed >= 12 RED gate with one-concept-per-test semantics.
- resolve_lockscene_cartoon pinned with probed seed 41 (the plan's seedless form is non-deterministic: `_randint 0 3` inclusive can yield 1 or 2; suite Pitfall-4 rule requires an explicit seed).
- resolve_randomize_full_domain = plan's seeded case (seed 42, sum==3, probed {Tube 2 Cartoon 1}) PLUS a seeds-1..20 union sweep == all 10 GAME_REPS.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Plan's `tiers_from_per_rep_residue` expected value contradicted GAME_REPS order**
- **Found during:** Task 1 (RED)
- **Issue:** The plan's behavior bullet expected `{{1 Cartoon 2} {2 VDW 1}}` claiming "Cartoon precedes VDW in GAME_REPS" -- false: GAME_REPS (setup_state.tcl:10) is `{Lines VDW Licorice CPK Cartoon ...}`, VDW (pos 2) precedes Cartoon (pos 5), and the suite's own pinned precedent (`tiers_from_per_rep_order_and_codes`: `{VDW 2 Lines 3}` -> `{{1 Lines 3} {2 VDW 2}}`) agrees.
- **Fix:** Expected value corrected to `{{1 VDW 1} {2 Cartoon 2}}` -- the bullet's intent ("residue tiers ordered in their GAME_REPS position, between CPK and Points") is preserved; correction noted in a test comment.
- **Files modified:** vmd/tests/test_rep_tiers.test
- **Verification:** GREEN 49/49 (this case passes under both narrow and widened constants -- it pins the ordering contract, not the seam width)
- **Committed in:** ebd9310 (Task 1 commit)

**2. [Rule 1 - Bug] Plan's `scene_detection_cartoon_unchanged` selection trips the sentinel filter**
- **Found during:** Task 1 (RED)
- **Issue:** The plan's literal record `{Cartoon {protein and not resname GAM} ...}` contains the sentinel substring "resname GAM", so scene_reps_to_per_rep's defense-in-depth filter (rep_tiers.tcl:109: skip any selection containing "resname GAM") SKIPS it -- the case failed in RED for the wrong reason (the guard, not the seam width).
- **Fix:** Selection corrected to plain `{protein}` (a real user Cartoon scene rep would not carry the game's sentinel substring); intent ("a user scene styled Cartoon is detected -- GAME_REPS-filtered, was always true") preserved; correction noted in a test comment.
- **Files modified:** vmd/tests/test_rep_tiers.test
- **Verification:** RED re-run: Failed 17 -> 16 with exactly the widened-contract failure set; GREEN 49/49.
- **Committed in:** ebd9310 (Task 1 commit)

**3. [Rule 3 - Blocking] Plan's `resolve_lockscene_cartoon` case omitted the mandatory seed**
- **Found during:** Task 1 (RED), before writing the case
- **Issue:** `resolve_per_rep 3 {} [dict create Cartoon 1] 1` (no seed) randomizes over the single-rep allowed list with the GLOBAL PRNG: `c = _randint 0 3` (inclusive) can yield 1 or 2 -> the result is non-deterministic ({Cartoon 1}, {Cartoon 2}, or {Cartoon 3}). The suite's own header (Pitfall 4) mandates an explicit seed for every randomized call.
- **Fix:** Seed 41 added (probed headlessly via randomize_per_rep's DI parameter: `randomize_per_rep 3 {Cartoon} 41` -> exactly `{Cartoon 3}`; draws are name-independent so the 17.1-01 Licorice pin transfers); deviation documented in the test comment.
- **Files modified:** vmd/tests/test_rep_tiers.test
- **Verification:** GREEN 49/49; probe script retained at tmp/biochemeleon-vmd/probe17203.tcl (gitignored).
- **Committed in:** ebd9310 (Task 1 commit)

**4. [Rule 1 - Bug] RED-failure-count design gap: the plan's 12 aggregate bullets yield only ~6-7 failing tests**
- **Found during:** Task 1 (RED), test design
- **Issue:** The plan's verify gate demands `Failed >= 12`, but aggregating 4 tiers per test means only ~6 of the 12 bullets can fail against the narrow seam (the unchanged-behavior bullets -- exclusions, ordering, effective_total, scene detection, DynamicBonds -- pass in RED by design).
- **Fix:** The four residue tiers pinned PER TIER for implemented/tier_kind/style_args (12 guaranteed RED failures) + 4 resolve cases -> RED Failed=16 >= 12 honestly. Also follows the TDD guidance "one concept per test" and gives per-tier regression naming. `resolve_randomize_full_domain` additionally gained the 20-seed union sweep so the full-domain claim is actually provable by a test.
- **Files modified:** vmd/tests/test_rep_tiers.test
- **Verification:** RED Failed=16 (>= 12); GREEN 49/49.
- **Committed in:** ebd9310 (Task 1 commit)

---

**Total deviations:** 4 auto-fixed (2 plan-expectation bugs corrected, 1 blocking determinism gap, 1 RED-gate design gap). **Impact on plan:** All corrections preserve the plan's stated intent; the seam contract itself is implemented exactly as specified. No scope creep (only the two plan-owned files touched).

## Issues Encountered
- Bash-tool default timeout (120s) killed the first headless-VMD suite run; re-ran with explicit tool timeouts (each staged VMD run takes ~2 min on the /mnt/c mount). Not a code issue.
- `tclsh` unavailable in WSL (known, 17.1-03) -> the plan's `tclsh vmd/lib/rep_tiers.tcl` load-clean check was satisfied by the staged suite run (documented above).
- The worktree's `tmp/` staging had to be created fresh (`cp -r vmd tmp/biochemeleon-vmd/`) -- expected for a fresh worktree.

## User Setup Required
None - no external service configuration required.

## STATE.md note (worktree protocol: orchestrator owns STATE/ROADMAP)
This plan was executed on isolated worktree branch `exec/17.2-03` (worktree `tmp/exec-17.2-03`); every commit verified via `git symbolic-ref --short HEAD` = `exec/17.2-03` immediately before each commit. For STATE.md's next update: Phase 17.2 plan 03 complete (commits ebd9310 + ddda863 pending the orchestrator's merge of exec/17.2-03); decisions worth carrying: (a) the four key-decisions in the frontmatter above, (b) the Discoveries section (dispatch-smoke flaky until 17.2-09; capstone section C known-red until 17.2-11), (c) rep_tiers suite 32 -> 49, full-suite gate 147 -> 164. Velocity: 88 min.

## Next Phase Readiness
- The seam contract is widened and pinned by 49 green tests BEFORE the dispatch consumes it: tier_kind "residue", bare style_args, GAME_REPS-order codes (VDW=1, Cartoon=2 for {Cartoon 2 VDW 1}), lock-scene residue derivation, full-domain randomize.
- Ready for 17.2-01/17.2-04 (splice generator -- consumes the residue kind contract) and 17.2-09 (dispatch routing: game.tcl's `else` branch is the exact integration point; its defensive warning must be replaced by the residue-splice route).
- 17.2-11 must rewrite capstone section C (evidence captured above) and should re-verify the dispatch smoke's randomize round (flaky until 17.2-09).

---
*Phase: 17.2-cartoon-newcartoon-generators*
*Completed: 2026-09-05*
