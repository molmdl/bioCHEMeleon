---
phase: 17.2
plan: 04
type: execute
wave: 2
depends_on: ["17.1-05", "17.2-01"]
files_modified: [vmd/lib/mutation.tcl, vmd/smoke/phase17_splice_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "make_residue_hiders {molid count {occupied_hiders} {resid_start}} returns structured residue records {chain resid atoms} — one fake GAM residue per hider: the anchor's N/CA/C/O/(CB) coords rigid-translated by exactly 1.0 Å perpendicular to the local peptide-bond vector (C_anchor → N_next), the ANCHOR's chain (never G), resid 9001+k from the disjoint block, mid-chain anchors only, pairwise CA separation >= 5.0, under-generation tolerated"
    - "write_combined_pdb appends residue records AFTER the simple records with a continuing serial; every residue atom line is the 78-col probe-pinned layout with beta -999.0 on CA ONLY, segid GAME on all atoms, element always emitted — NO CONECT, no TER, no renumbering"
    - "tag_sentinels is NOT run on residue-containing rounds: mutate gains a 3rd arg and dispatches to tag_sentinels_mixed, which stamps beta -999 + segid GAME + user ordinals on {simple GAM atoms + residue CAs} and beta 0.00 + segid GAME on residue N/C/O/CB — computed from index sets derived from orig_n + the record layout (never a selector that would over-match)"
    - "After mutate+reload: resname GAM and beta < 0 returns exactly ONE index per hider (the CA), the CA reads structure == T (load-time STRIDE — no ssrecalc anywhere), chain == the anchor's chain, and both junctions are bonded (fake N to prev C and fake C to next N within the C–N cutoff)"
    - "The splice smoke carries the mechanism controls: POSITIVE (1.0 Å splice's window render exceeds the unspliced control — in-path), NEGATIVE-A (chain-G splice renders 0 GAME-only bits — fragment homogeneity), NEGATIVE-B (2.0 Å splice DEGRADES the window below the control — the half-bonded pathology is headlessly detectable)"
  artifacts:
    - path: "vmd/lib/mutation.tcl"
      provides: "make_residue_hiders (mol bridge), write_combined_pdb residue-records arg, tag_sentinels_mixed, mutate 3-arg dispatch (sources splice.tcl)"
      exports: ["make_residue_hiders", "tag_sentinels_mixed", "make_placeholder_hiders", "make_bonded_hiders", "write_combined_pdb", "tag_sentinels", "fetch_hider_indices", "mutate"]
    - path: "vmd/smoke/phase17_splice_smoke.tcl"
      provides: "headless mechanism proof + positive/negative Tachyon controls + mixed-round tagging + restore round-trip; establishes the 17.2 render-diff harness the four tier smokes copy"
  key_links:
    - from: "vmd/lib/mutation.tcl (make_residue_hiders)"
      to: "vmd/lib/splice.tcl"
      via: "splice::select_anchors + splice::displacement + splice::assemble_record + splice::resid_block (mutation sources splice.tcl after generators.tcl)"
      pattern: "splice::(select_anchors|displacement|assemble_record|resid_block|atom_record)"
    - from: "vmd/lib/mutation.tcl (mutate)"
      to: "tag_sentinels_mixed"
      via: "residue_records non-empty → tag_sentinels_mixed (NEVER tag_sentinels — it would beta-stamp all 5 fake atoms and break the CA-only sentinel); residue_records empty → tag_sentinels (byte-unchanged path)"
      pattern: "tag_sentinels_mixed"
---

<objective>
Add the residue-splice writer/bridge to `vmd/lib/mutation.tcl` — make_residue_hiders, residue records in the combined-PDB flow, CA-only mixed tagging — plus the mechanism smoke with the research-mandated positive and negative Tachyon controls.

Purpose: this is the ONE splice mechanism all four trace-family reps consume (17.2-05..08 render it per rep). The writer reuses the probe-proven `writepdb → drop END → append records → END` flow (append-at-end == mid-file splice, research probe6). The tagging must be CA-only on residue atoms (Pitfall C6: reusing tag_sentinels as-is would beta-stamp all 5 fake atoms → 5 fetch indices → 5× counts → broken bookkeeping).
Output: extended `vmd/lib/mutation.tcl` + `vmd/smoke/phase17_splice_smoke.tcl` (PASS=1).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/17.2-cartoon-newcartoon-generators/17.2-RESEARCH-cartoon-stride.md
@.planning/phases/17.2-cartoon-newcartoon-generators/17.2-01-PLAN.md
@.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-05-PLAN.md
@vmd/lib/mutation.tcl
@vmd/smoke/phase17_points_smoke.tcl

Key facts (do NOT re-derive — probe-verified in 17.2-RESEARCH):
- **Splice constants (17.2-01, consume via splice.tcl):** SPLICE_DISPLACEMENT 1.0 (perpendicular to the peptide bond — NEVER along it: 0.62 Å cap; hard envelope 1.43 documented), RESID_BASE 9001 (disjoint block; real demo resids ≤ ~500), MIN_ANCHOR_SEP 5.0. **MIN_OCC_SEP is NOT a splice.tcl constant** — occupied clearance is a BRIDGE concern (17.2-01's `splice::select_anchors` consumes pre-computed ok descriptors and never sees occupied positions), so THIS bridge defines `variable MIN_OCC_SEP 3.0` locally in mutation.tcl (occupied-hider clearance for candidate anchors; used in step 2's occupied-clearance check below).
- **make_residue_hiders flow (mol bridge; runs on the ORIGINAL pre-mutate molecule — it has NO GAM atoms; `not resname GAM` is future-proofing):**
  1. Candidates: `set casel [atomselect $molid {protein and not resname GAM and name CA}]` → batched SINGLE-KEYWORD gets (get index / get resid / get chain / get x / get y / get z — multi-keyword gets return nested lists, research Pitfall 5). num == 0 → delete + `error "make_residue_hiders: no protein anchors"`. real_max = max of the resid list (foreach loop). Cap count at num candidates (under-generation, v1 parity). `$casel delete`.
  2. Per candidate (chain $ch, resid $rid): skip if chain blank/empty (validated territory; comment why). Residue atom presence: for each name in {N CA C O CB}: `atomselect $molid "chain $ch and resid $rid and name $nm"` → num ≥ 1? read x/y/z + element + index (lindex 0 each); delete. Require N+CA+C+O present (else ok=0). MID-CHAIN: `atomselect $molid "name C and within 1.7 of index <n_idx>"` num ≥ 1 (prev C; the anchor's own C is ~2.4 Å from its N — excluded by 1.7) AND `atomselect $molid "name N and within 1.7 of index <c_idx>"` num ≥ 1 (next N; capture first result's x/y/z for the displacement). Occupied clearance: min distance from ANY present-atom coord to any occupied_hiders position ≥ MIN_OCC_SEP 3.0 (else ok=0). Descriptor: `[list $ch:$rid $ok $cax $cay $caz]`.
  3. `set chosen [::biochemeleon::splice::select_anchors $descriptors $count]` (NO seed — global PRNG stream convention). Chosen may be < count (sep rejections) — return the actual; warn via `catch {vmdcon -warn ...}` when under-generated (v1 parity, non-blocking).
  4. Per chosen descriptor (acceptance order, ordinal k): refetch the anchor's present atoms in N/CA/C/O/CB order (atoms_data); anchor's C coords from atoms_data; n_next coords via the `within 1.7 of index <c_idx>` N-selection; `set d [::biochemeleon::splice::displacement $c_pt $n_next_pt]`; `set resid [lindex [::biochemeleon::splice::resid_block 1 $start_k $real_max] 0]` where start_k = (resid_start arg, {} → RESID_BASE) + k; `lappend recs [::biochemeleon::splice::assemble_record $atoms_data $ch $resid $d]`. Return recs.
- **write_combined_pdb {molid hider_records out_path {residue_records {}}}** — add the 4th optional arg. After the existing simple-record loop: `foreach r $residue_records { lassign $r chain resid atoms; foreach a $atoms { lassign $a nm el x y z; puts $fh [::biochemeleon::splice::atom_record $serial $nm $el $chain $resid $x $y $z]; incr serial } }`. Return orig_n (unchanged). 3-arg behavior byte-identical.
- **tag_sentinels_mixed {molid n_simple residue_records orig_n}** — NEW. Index-set walk (writepdb preserves index order; fake atoms occupy orig_n.. in record order: n_simple single-atom records, then each residue's atoms in N-first/CA-second order — assemble_record validates that order): simple indices → beta set; per residue atom: name eq "CA" → beta set, else → other set; every fake index → segid set. Stamp: beta-set selection → `set beta $HID_BETA` + `set segid $HID_SEGID` + `set user` batched ordinals (0..k−1 over the beta set, file order); other-set selection (guard non-empty) → `set beta 0.00` + `set segid $HID_SEGID`; delete every selection. Return the beta-set index list. Comment MUST document WHY selector-based tagging is forbidden (tag_sentinels would beta-stamp residue N/C/O/CB → 5 fetch indices/hider; Pitfall C6) and why index computation is safe (writepdb order == index order, Phase-15 established; assemble_record guarantees the atom order).
- **mutate {molid hider_records {residue_records {}}}** — add the 3rd optional arg (backward compatible): write_combined_pdb with $residue_records (capture orig_n); mol delete + mol new unchanged; `if {[llength $residue_records] > 0} { tag_sentinels_mixed $new_m [llength $hider_records] $residue_records $orig_n } else { tag_sentinels $new_m }`. Doc comment: NEVER tag_sentinels on residue rounds (Pitfall C6); NEVER `mol ssrecalc` anywhere (Pitfall C2 — load-time STRIDE already assigns T; ssrecalc wipes manual ss).
- **Source line:** `source [file join [file dirname [info script]] splice.tcl]` after the generators.tcl line (pure re-init harmless). Do NOT source registry (unchanged rule).
- Keep make_placeholder_hiders / make_bonded_hiders / _hider_record / tag_sentinels / fetch_hider_indices BYTE-IDENTICAL (simple-tier regression surface — phase15/16 smokes).
- Export line: add make_residue_hiders tag_sentinels_mixed. Tcl 8.5; brace expr; `$sel delete` after EVERY atomselect; one variable per line; no 8.6 idioms.
- Smoke harness: 17.1-08 conventions — source order: setup_state, registry, rep_tiers, generators, game_logic, demos, backup, mutation, hiders, game; failures list + `_bail` + per-step catch + `BCHM_SMOKE_RESULT PASS=1 FAIL=none` marker + `exit`; registry sourced EXACTLY ONCE; full-log `ERROR)`/`bad switch` scan. Tachyon technique (17.1-08 template): `axes location off` first; probe rep added LAST (highest index — deleting it never renumbers earlier reps), deleted after renders; baseline-zero render (selection `index 999999` → 0 primitives) proves the harness; parse with `regexp -all -- {\mFCylinder\y}` / `{\mTriStrip\y}` / `{\mSTri\y}` / `{\mSphere\y}` — render once, inspect the .dat head, then pin. **This smoke ESTABLISHES the 17.2 render helpers; the four tier smokes (17.2-05..08) copy them.**
- Staging: `mkdir -p tmp/biochemeleon-vmd && cp -r vmd tmp/biochemeleon-vmd/` then `timeout 300 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/smoke/phase17_splice_smoke.tcl -eofexit < /dev/null' 2>&1 | grep -E "BCHM_SMOKE_RESULT|ERROR\)|bad switch"` — parse the marker, scan the FULL log (false-PASS guard).
</context>

<tasks>

<task type="auto">
  <name>Task 1: make_residue_hiders + residue records in the write/tag/mutate flow</name>
  <files>vmd/lib/mutation.tcl</files>
  <action>
In `vmd/lib/mutation.tcl`:
1. Add the splice.tcl source line (after generators.tcl) + update the header comment (17.2: residue-splice tier — ONE splice serves Cartoon/NewCartoon/Trace/Tube; NEVER mol ssrecalc — see splice.tcl's decision record).
2. `make_residue_hiders {molid count {occupied_hiders {}} {resid_start {}}}` per the context flow (candidates → splice::select_anchors → per-anchor displacement/assemble → records). NO seed args anywhere (global PRNG stream). Add to the namespace export line.
3. `write_combined_pdb {molid hider_records out_path {residue_records {}}}` — 4th arg + the residue loop (splice::atom_record; serial continues). Existing 3-arg behavior byte-identical.
4. `tag_sentinels_mixed {molid n_simple residue_records orig_n}` — NEW, index-set-based per the context, with the Pitfall-C6 rationale comment.
5. `mutate {molid hider_records {residue_records {}}}` — 3-arg dispatch per the context; doc comment updated (Pitfall C6 + never-ssrecalc note).
6. Keep make_placeholder_hiders / make_bonded_hiders / _hider_record / tag_sentinels / fetch_hider_indices byte-identical.
Tcl 8.5; brace expr; every atomselect deleted; no 8.6 idioms.
  </action>
  <verify>`tclsh vmd/lib/mutation.tcl` loads clean; 8.6 gate zero on the file; `grep -n "splice::" vmd/lib/mutation.tcl` shows the 5 consumed procs; `grep -c "mol ssrecalc" vmd/lib/mutation.tcl` → 0; `grep -cE "^source .*(splice|generators|setup_state)" vmd/lib/mutation.tcl` → 3 and `grep -c "^source" vmd/lib/mutation.tcl` → 3 total (NO registry source); `grep -c "%6.2f" vmd/lib/mutation.tcl` → 0.</verify>
  <done>mutation.tcl builds/writes/tags residue records with CA-only beta, anchor chains, disjoint resids, and the 1.0 Å ⊥ displacement via the pure layer; simple-tier paths untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Splice mechanism smoke with positive + negative Tachyon controls</name>
  <files>vmd/smoke/phase17_splice_smoke.tcl</files>
  <action>
Write `vmd/smoke/phase17_splice_smoke.tcl` (harness per context; the 17.2 render-helper origin). Demo 1znf (424 atoms):
1. `set recs [mutation::make_residue_hiders $m0 2]` → assert 2 records; per record: `[llength $r]` == 3; chain is a real chain (NOT "G", NOT blank); resid ∈ {9001 9002}; atoms ≥ 4, first name N, second name CA, all names ⊆ {N CA C O CB}; each fake atom ~1.0 Å from its anchor counterpart (fetch the anchor residue's same-named atom coords, compare distance within [0.95, 1.05] — rigid-translation proof); fake N–CA distance == anchor's N–CA distance (eps 1e-6, geometry preserved).
2. `mutation::write_combined_pdb $m0 [list] $outpdb $recs` → read the file: the LAST 10 ATOM lines each length 78; CA lines (2nd of each 5-group) `[string range $l 60 65]` eq "-999.0", other fake lines eq "  0.00"; segid `[string range $l 72 75]` eq "GAME"; element `[string range $l 76 77]` non-blank; chain col == the record's chain; ZERO "CONECT" lines; exactly one END.
3. `set gm [mutation::mutate $m0b [list] $recs]` → atoms 434; `resname GAM and beta < 0` num == 2 (CA-only sentinel); those indices' name == CA; `resname GAM` num == 10; protein count == real_protein + 10; CA structure == T (string compare); CA resids {9001 9002}; every fake atom's chain == its record's chain and segid GAME; BOTH junctions bonded: for each fake residue, its N index has ≥ 1 bonded C and its C index has ≥ 1 bonded N (via `getbonds` + name checks on the bonded indices, or `within 2.0` name selections — assert ≥ 1 each).
4. POSITIVE CONTROL (in-path): anchor of record 1 = the nearest real CA to the fake CA (`atomselect $m "name CA and within 2.0 of <fake CA coords>"` → its resid a). Probe rep (added last): style Cartoon, selection `resid <a-2> to <a+2>` → render Tachyon spliced_win.dat. Fresh-load the original demo → same probe style/selection → control_win.dat. Assert spliced FCyl count > control FCyl count (the fake CA is IN the traced path — SX10-style evidence).
5. NEGATIVE-A (chain-G, Pitfall C8): fresh load; rebuild record 1 with chain "G" via `splice::assemble_record $atoms "G" 9003 $d` (same displacement recomputed) → write + mutate on a FRESH original → `resname GAM` num == 5 (splice landed) BUT probe render (style Cartoon, selection `segid GAME`) FCyl == 0 (separate fragment, never traced).
6. NEGATIVE-B (over-displacement, Pitfall C1): fresh load; rebuild record 1 with a 2.0 Å displacement (recompute d, scale each component ×2.0 in the smoke) → assemble/write/mutate → probe rep style Cartoon selection `resid <a-2> to <a+2>` → render → assert FCyl count < the step-4 control count (DEGRADED below baseline — the half-bonded pathology detectable headlessly).
7. Mixed-round tagging: fresh load; `set srecs [mutation::make_bonded_hiders $m2 2]` + `set rrecs2 [mutation::make_residue_hiders $m2 2]` → `mutate $m2 $srecs $rrecs2` → atoms 436; `resname GAM and beta < 0` num == 4 (2 simple + 2 CA); the 2 simple atoms' beta == -999.0 (rescue preserved); residue N/C/O/CB beta ≈ 0.0; user read-back over the beta set == {0.0 1.0 2.0 3.0}; segid GAME on all 12 fake atoms.
8. Restore round-trip: `backup::snapshot` before a fresh make/mutate → after mutate `molinfo get filename` ends in biochemeleon_game.pdb; `backup::restore $snap <live game molid>` (2-arg, LIVE molid) → atom count 424 (no residue leak).
9. Delete probe reps; marker `BCHM_SMOKE_RESULT PASS=1 FAIL=none` (or _bail FAIL list); `exit`.
  </action>
  <verify>Staged run: `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log; the pinned .dat regexes confirmed against the baseline-zero render (0 primitives); positive control strictly >, both negatives at their failure signatures.</verify>
  <done>Splice mechanism proven end-to-end: rigid 1.0 Å ⊥ translation, CA-only sentinel round-trip, junction bonding, in-path positive control, chain-G + 2.0 Å negative controls, mixed tagging, clean restore.</done>
</task>

</tasks>

<verification>
- `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log
- Tcl 8.6 gate zero on both touched files
- `grep -c "mol ssrecalc" vmd/lib/mutation.tcl` → 0; simple-tier procs byte-identical (`git diff` scope check)
- No CONECT emitted; no TER inserted; residue lines 78 cols with CA-only beta (smoke-asserted)
</verification>

<success_criteria>
- Any tier (17.2-05..08) and the dispatch (17.2-09) can request residue hiders and get anchor-mimicking, junction-bonded, CA-only-sentinel GAM residue records through the SAME combined-PDB flow
- The research's failure modes (chain-G fragment, ≥1.4 Å half-bonded degradation) are regression-guarded headlessly by the negative controls
</success_criteria>

<output>
After completion, create `.planning/phases/17.2-cartoon-newcartoon-generators/17.2-04-SUMMARY.md`
</output>
