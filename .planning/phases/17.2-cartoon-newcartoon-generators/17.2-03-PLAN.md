---
phase: 17.2
plan: 03
type: tdd
wave: 1
depends_on: ["17.1-01"]
files_modified: [vmd/lib/rep_tiers.tcl, vmd/tests/test_rep_tiers.test]
autonomous: true
must_haves:
  truths:
    - "IMPLEMENTED_TIERS widens to the FULL GAME_REPS list {Lines VDW Licorice CPK Cartoon NewCartoon Trace Tube Points DynamicBonds} — the 17.2 seam contract: one splice, four consumers"
    - "tier_kind returns 'residue' for Cartoon/NewCartoon/Trace/Tube (the third kind beside free|bonded — the dispatch routes them to the residue-splice generator) and keeps free|bonded for the six simple tiers"
    - "style_args returns the bare single-word list for all four residue tiers ({Cartoon}/{NewCartoon}/{Trace}/{Tube}) — they take no modstyle params"
    - "resolve_per_rep now KEEPS explicit {Cartoon n}/{NewCartoon n}/{Trace n}/{Tube n} entries (previously dropped as unimplemented) and lock-scene detection with a Cartoon-family scene derives a full residue round (detected {Cartoon 1} + empty per_rep + hc=3 → exactly {Cartoon 3})"
    - "The 17.1-01 test cases that asserted Cartoon is NOT implemented are INVERTED in the same plan (the seam contract changes; the suite stays the single source of truth)"
  artifacts:
    - path: "vmd/lib/rep_tiers.tcl"
      provides: "widened seam: IMPLEMENTED_TIERS == all 10 GAME_REPS; TIER_KINDS gains residue entries; procs/signatures otherwise unchanged"
      exports: ["implemented", "tier_kind", "style_args", "scene_reps_to_per_rep", "filter_per_rep_to_scene", "resolve_per_rep", "tiers_from_per_rep", "effective_total"]
    - path: "vmd/tests/test_rep_tiers.test"
      provides: "updated + new tcltest cases: the four residue tiers implemented/kind/style_args/resolvable; pre-existing simple-tier cases still green"
  key_links:
    - from: "vmd/lib/game.tcl (17.2-09)"
      to: "vmd/lib/rep_tiers.tcl"
      via: "tier_kind Cartoon → 'residue' routes to make_residue_hiders; tiers_from_per_rep orders Cartoon/NewCartoon/Trace/Tube in GAME_REPS position"
      pattern: "rep_tiers::(tier_kind|tiers_from_per_rep|resolve_per_rep)"
---

<objective>
Widen the 17.1 `IMPLEMENTED_TIERS` seam to the full residue-splice tier family — the dedicated seam-contract plan, TDD-first.

Purpose: 17.1-01 built rep_tiers.tcl with the seam deliberately narrow (`{Lines VDW Licorice CPK Points DynamicBonds}`, cartoon tiers ABSENT). 17.2's ONE splice mechanism serves Cartoon + NewCartoon + Trace + Tube — widening the seam (constants + the third tier kind "residue") is a pure-layer contract change that everything downstream (game.tcl routing, lock-scene rounds, capstone bookkeeping) consumes. Doing it as its own early TDD plan keeps the contract pinned before any dispatch code runs.
Output: widened `vmd/lib/rep_tiers.tcl` + updated `vmd/tests/test_rep_tiers.test`, all green.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/17.2-cartoon-newcartoon-generators/17.2-RESEARCH-cartoon-stride.md
@.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-01-PLAN.md
@vmd/lib/rep_tiers.tcl
@vmd/tests/test_rep_tiers.test

Key facts (do NOT re-derive):
- 17.1-01's contract (now being widened): IMPLEMENTED_TIERS = `{Lines VDW Licorice CPK Points DynamicBonds}`; TIER_KINDS maps those to free|bonded; `tier_kind` returns "" for Cartoon/NewCartoon/Trace/Tube; `style_args` returns `[list $rep]` for implemented non-DynamicBonds tiers and `{DynamicBonds 1.6}` for DynamicBonds. The 17.1-01 test file contains cases that PIN the narrow seam: `implemented Cartoon → 0`, `tier_kind Cartoon → ""`, `style_args Cartoon → {}`, resolve dropping `{Cartoon 2}`, lock-scene detected-{Cartoon} falling back to randomize. THIS PLAN INVERTS those cases (same file, same harness).
- GAME_REPS (setup_state.tcl:10, UNCHANGED): `{Lines VDW Licorice CPK Cartoon NewCartoon Trace Tube Points DynamicBonds}` — GAME_REPS position decides tier ordering and 1-based codes; Cartoon/NewCartoon/Trace/Tube sit BETWEEN CPK and Points.
- The residue kind: "residue" = one fake GAM residue per hider via the shared splice (17.2-01/17.2-04). rep_tiers does NOT know about generators — it only returns the kind string; game.tcl (17.2-09) routes on it.
- style_args for the four: NO modstyle params (bare `Cartoon` etc. — viability research §6.6: Cartoon 0, NewCartoon 0, Trace 0, Tube 0-or-2 params; the game uses the bare form). The EXISTING style_args code path (`[list $rep]` for implemented non-DynamicBonds) already produces this once the tiers are in IMPLEMENTED_TIERS — NO proc-body change expected (verify by test).
- Header comment must be updated: the seam note ("17.2 adds residue-splice kinds here") becomes the widened reality — IMPLEMENTED_TIERS == GAME_REPS; a tier = {name, kind ∈ free|bonded|residue, style_args}.
- KNOWN RED elsewhere (owned by 17.2-11, do NOT touch): `vmd/smoke/phase17_capstone_smoke.tcl` section C asserts `{Cartoon 2 VDW 1}` is DROPPED (17.1-13's under-generation case) — after this plan it GENERATES (Cartoon implemented). The smoke goes red until 17.2-11 rewrites that case with a non-GAME_REPS style (Ribbons). Record this in the SUMMARY; touch only rep_tiers.tcl + test_rep_tiers.test.
- Tcl 8.5 only; dicts built by iterating GAME_REPS (order-stable); `lsearch -exact`; brace all expr.
- tcltest harness (headless VMD, per 17.1-01): `timeout 300 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/tests/test_rep_tiers.test -eofexit < /dev/null' 2>&1 | grep -E "BCHM_TEST_RESULT|ERROR\)|bad switch"`.
</context>

<feature>
  <name>Seam widening: full residue tier family in rep_tiers.tcl</name>
  <files>vmd/lib/rep_tiers.tcl, vmd/tests/test_rep_tiers.test</files>
  <behavior>
Constant changes:
- `IMPLEMENTED_TIERS` = `{Lines VDW Licorice CPK Cartoon NewCartoon Trace Tube Points DynamicBonds}` (full GAME_REPS order — now EQUAL to GAME_REPS; keep the separate constant: consumers reference it, and the seam note documents they may diverge again if a future rep is added to GAME_REPS without a generator).
- `TIER_KINDS` gains: Cartoon→residue, NewCartoon→residue, Trace→residue, Tube→residue (the six 17.1 entries unchanged).

Proc behavior (no signature changes):
- `implemented` → 1 for all 10 GAME_REPS; 0 for anything else (Ribbons/Bonds → 0).
- `tier_kind` → free (VDW) | bonded (Lines/Licorice/CPK/Points/DynamicBonds) | residue (Cartoon/NewCartoon/Trace/Tube) | "" (unknown).
- `style_args` → `{DynamicBonds 1.6}` | single-word lists (`{Cartoon}` `{NewCartoon}` `{Trace}` `{Tube}` for the residue tiers) | {} unknown.
- `resolve_per_rep` / `filter_per_rep_to_scene` / `tiers_from_per_rep` / `effective_total` / `scene_reps_to_per_rep`: NO body changes expected — they operate on GAME_REPS membership (already includes the four) + IMPLEMENTED_TIERS (now widened). Their BEHAVIOR on residue-tier inputs changes only through the constants. Verify via tests.

RED test cases (update the inverted 17.1-01 cases IN PLACE + add new ones in a `# ---- Phase 17.2: residue-splice tier family ----` section):
- implemented_residue_tiers: implemented Cartoon / NewCartoon / Trace / Tube → 1 each (INVERTS 17.1-01's `implemented Cartoon → 0` case).
- implemented_excluded_unchanged: Ribbons → 0; Bonds → 0 (curation exclusions are NOT generators).
- tier_kind_residue: tier_kind Cartoon / NewCartoon / Trace / Tube → "residue" each (INVERTS the `tier_kind Cartoon → ""` case).
- tier_kind_simple_unchanged: VDW → free; Lines/Licorice/CPK/Points/DynamicBonds → bonded; Ribbons → "".
- style_args_residue: style_args Cartoon → exactly `{Cartoon}`; NewCartoon → `{NewCartoon}`; Trace → `{Trace}`; Tube → `{Tube}` (INVERTS `style_args Cartoon → {}`).
- style_args_dynamicbonds_unchanged: → exactly `{DynamicBonds 1.6}`.
- resolve_keeps_residue_per_rep: resolve_per_rep 4 [dict create Cartoon 2 Tube 2] {} 0 → dict keys exactly {Cartoon Tube} with counts {2 2} (INVERTS the `{Cartoon 2}`-dropped case).
- resolve_lockscene_cartoon: resolve_per_rep 3 {} [dict create Cartoon 1] 1 → exactly {Cartoon 3} (INVERTS the "detected all-unimplemented → randomize fallback" case).
- resolve_randomize_full_domain: resolve_per_rep 3 {} {} 0 42 (seeded) → keys ⊆ GAME_REPS (all 10 possible — INVERTS the ⊆-6 case), sum == 3.
- tiers_from_per_rep_residue: tiers_from_per_rep [dict create Cartoon 2 VDW 1] → `{{1 Cartoon 2} {2 VDW 1}}` (Cartoon precedes VDW in GAME_REPS — the ordering contract).
- effective_total_residue: effective_total [dict create Cartoon 2 VDW 1] → 3 (residue hiders count ONE per hider, not per atom — the registry/dispatch bookkeeping unit is the HIDER).
- scene_detection_cartoon_unchanged: scene_reps_to_per_rep with a `{Cartoon {protein and not resname GAM} Element Opaque}` record → detected contains Cartoon (was always true — GAME_REPS-filtered; pins it).
   </behavior>
  <implementation>
- rep_tiers.tcl: update the two constants; update the header seam note (widened 17.2: IMPLEMENTED_TIERS == GAME_REPS; residue kind = one GAM-residue splice serving Cartoon/NewCartoon/Trace/Tube — ONE mechanism, FOUR consumers; kind "residue" is routed by game.tcl to the splice generator; NEVER call mol ssrecalc in that flow — see splice.tcl's decision record). NO proc-body changes unless a test proves otherwise (style_args/resolve are constant-driven).
- test_rep_tiers.test: invert the pinned narrow-seam cases in place (keep the test names' intent, update expected values, add a one-line comment "inverted by 17.2-03 seam widening") + append the new section.
REFACTOR only if an obvious cleanup exists; tests must stay green.
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: RED — invert + add seam tests</name>
  <files>vmd/tests/test_rep_tiers.test</files>
  <action>
Update the 17.1-01 cases that pin the narrow seam (implemented Cartoon→0, tier_kind Cartoon→"", style_args Cartoon→{}, resolve {Cartoon 2} dropped, lock-scene Cartoon fallback, randomize ⊆ 6) to their widened expectations, and append the new <behavior> cases. Run the staged suite — the updated/new cases MUST fail against the current constants (implemented Cartoon still returns 0) while untouched cases stay green.
Commit: `test(17.2-03): seam-widening expectations for the residue tier family`
  </action>
  <verify>Staged run: BCHM_TEST_RESULT shows Failed ≥ 12 (the inverted + new cases); zero `bad switch` in the full log.</verify>
  <done>The suite now specifies the WIDENED contract and fails against the narrow-seam implementation.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — widen the constants</name>
  <files>vmd/lib/rep_tiers.tcl</files>
  <action>
Update IMPLEMENTED_TIERS to the full 10-name list + add the four residue entries to TIER_KINDS + update the header seam note per <implementation>. Re-run the staged suite. If any NON-constant-dependent test fails (e.g. a style_args or resolve behavior gap), fix the proc minimally — but expect ZERO proc-body changes.
Commit: `feat(17.2-03): widen IMPLEMENTED_TIERS seam to the residue-splice tier family`
  </action>
  <verify>
```
tclsh vmd/lib/rep_tiers.tcl   # loads clean (pure layer)
timeout 300 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/tests/test_rep_tiers.test -eofexit < /dev/null' 2>&1 | grep -E "BCHM_TEST_RESULT|ERROR\)|bad switch"
```
BCHM_TEST_RESULT Failed=0 (full suite); zero `ERROR)` / `bad switch`; 8.6 gate zero on rep_tiers.tcl; `grep -n "residue" vmd/lib/rep_tiers.tcl` shows the four TIER_KINDS entries; `git diff vmd/lib/rep_tiers.tcl` shows no proc signature changes.
  </verify>
  <done>The seam is widened: all 10 GAME_REPS implemented, residue kind pinned, bare style_args for the four, resolve/lock-scene/randomize accept residue tiers — all green.</done>
</task>

</tasks>

<verification>
- BCHM_TEST_RESULT Failed=0 (staged headless-VMD run), zero `ERROR)` / `bad switch` in the full log
- Tcl 8.6 gate zero on both files
- rep_tiers.tcl still PURE (no mol/atomselect/tk); no proc signature changes
- IMPLEMENTED_TIERS == GAME_REPS content (order included); TIER_KINDS has 10 entries (1 free, 5 bonded, 4 residue)
- KNOWN RED documented in the SUMMARY: phase17_capstone_smoke.tcl section C (Cartoon-dropped assert) — fixed by 17.2-11, NOT here (smoke-file ownership)
</verification>

<success_criteria>
- The seam contract is widened and pinned by tests BEFORE the dispatch (17.2-09) consumes it: tier_kind "residue", bare style_args, GAME_REPS-order codes, lock-scene derivation over the full family
- The 17.1 test suite remains the single source of truth for the dispatch contract (inverted cases, not deleted coverage)
</success_criteria>

<output>
After completion, create `.planning/phases/17.2-cartoon-newcartoon-generators/17.2-03-SUMMARY.md`
</output>
