---
phase: 17.2-cartoon-newcartoon-generators
plan: 02
subsystem: registry
tags: [tcl, tcltest, vmd, registry, dict, pure-layer, pick-fallback, resid-block, tdd]

# Dependency graph
requires:
  - phase: 17.1-rep-setup-infrastructure-simple-generators
    provides: "registry per-record rep API precedent (17.1-02 set_rep/assign_reps — status-preserving stamps, atomic validation, exact mark_found error wording) + the staged headless-VMD tcltest runner pattern"
  - phase: 15-mutation-safety-hider-registry
    provides: "the _records dict (idx -> {rep status}), reconstruct_from_sentinels DI shape, reset, and the error-message convention"
provides:
  - "registry::register_resid_block {mapping} — ATOMIC validate-every-idx-then-REPLACE-WHOLESALE resid -> hider-index registration (error 'hider <idx> not registered'; empty mapping = no-op, block unchanged)"
  - "registry::hider_for_resid {resid} — registered hider index or '' for unknown (never errors) — the pure half of the multi-atom pick fallback (click on fake GAM N/C/O/CB resolves to the registered CA)"
  - "reset extended to clear BOTH _records and _resid_block (one added line; observable _records behavior unchanged)"
  - "reconstruct_from_sentinels BYTE-UNCHANGED (P8/17.1-02 pin held, diff-verified) — block survives reconstruct; only reset and register_resid_block mutate _resid_block"
affects: [17.2-09-dispatch-integration, 17.2-cartoon-generators, phase-19-game-tab, 17.2-03-and-later-cartoon-plans]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Atomic validate-then-replace wholesale map registration (assign_reps' atomicity pattern generalized to a second map)"
    - "Two independent namespace maps over one record set (_records idx-keyed, _resid_block resid-keyed) with a pinned single-mutator lifecycle (reset + register only)"
    - "Tcl 8.5 dict discipline: dict exists guard + dict get (no 3-arg default form)"

key-files:
  created: []
  modified:
    - vmd/lib/registry.tcl
    - vmd/tests/test_registry.test

key-decisions:
  - "Empty-mapping semantics implemented per the prose contract: register_resid_block [dict create] is a NO-OP (block left UNCHANGED, not cleared) — early-return guard after the validate loop; reset remains the only clearer"
  - "register_empty_noop pins the no-op contract beyond the plan's literal sequence (adds the unchanged-pin: a prior entry survives an empty-mapping call) so the guard is regression-guarded, not just implemented"
  - "Comment hygiene: avoided the standalone tokens mol/atomselect/tk in new registry comments (one reword: 'read by game.tcl at pick time' not 'mol-side') so automated purity greps stay zero for the phase verifier"

patterns-established:
  - "resid-block lifecycle: register_resid_block WHOLESALE-REPLACES (one call per round, never merged) / reset CLEARS / reconstruct_from_sentinels NEVER touches — the dispatch (17.2-09) always re-registers after reconstruct in residue rounds"
  - "Harmless 9001 collision documented in-proc: simple-tier single-atom hiders share resid 9001 (RESID_BASE) but on_pick consults is_hider FIRST, so the fallback only fires for fake-residue N/C/O/CB; real resids << 9001"

# Metrics
duration: 51 min
completed: 2026-09-05
---

# Phase 17.2 Plan 02: Registry Resid-Block Lookup API Summary

**Pure resid-block API on registry.tcl — register_resid_block (atomic validate-then-wholesale-replace, 'hider <idx> not registered') + hider_for_resid (idx or '') — TDD RED→GREEN, 37/37 headless-VMD green, reconstruct_from_sentinels byte-unchanged (P8 pin held)**

## Performance

- **Duration:** 51 min
- **Started:** 2026-09-05T17:08:59Z
- **Completed:** 2026-09-05T17:59:53Z
- **Tasks:** 2 (RED + GREEN; no refactor needed)
- **Files modified:** 2

## Accomplishments

- `register_resid_block {mapping}`: ATOMIC validate-every-idx-then-replace-wholesale; error `hider <idx> not registered` (exact mark_found wording) BEFORE any state change; empty mapping = no-op (block unchanged); never merged with a prior round's block
- `hider_for_resid {resid}`: registered hider index or `""` for unknown — never errors (the miss path is a normal "Miss!" pick); Tcl 8.5 `dict exists` + `dict get` (no 3-arg default)
- `reset` extended with ONE added line: clears `_resid_block` alongside `_records` (stale block cannot survive cleanup into the next round)
- `reconstruct_from_sentinels` BYTE-UNCHANGED — verified by extracting the proc from HEAD and diffing (empty diff); block survives reconstruct (pinned by test `block_survives_reconstruct`)
- 10 new tcltest cases pin the full contract: lookup round-trip, wholesale replace, atomic no-partial error, empty no-op (+ unchanged pin), empty-registry validate-loop-zero-times, unknown-lookup, reset-clears, reconstruct-survives, orthogonality to assign_reps, mixed-round 9001-collision shape

## Task Commits

Each TDD phase committed atomically on branch `exec/17.2-02`:

1. **Task 1: RED — failing registry resid-block tests** - `9bdb578` (test) — Total=37 Passed=27 Failed=10 (all 10 new cases, `invalid command name`), all 27 pre-existing green
2. **Task 2: GREEN — implement register_resid_block + hider_for_resid** - `34db7b0` (feat) — Total=37 Passed=37 Failed=0

**Plan metadata:** (docs commit follows this SUMMARY)

_Note: TDD RED→GREEN produced 2 commits; REFACTOR skipped (no changes needed — implementation already matches file conventions)._

## Files Created/Modified

- `vmd/lib/registry.tcl` — `_resid_block` namespace var + both procs exported; Phase 17.2 section appended after assign_reps (fallback purpose, harmless-9001-collision rationale, and replace/reset-clears/reconstruct-untouched lifecycle documented in-proc); reset gains the one clear line (+76/−2)
- `vmd/tests/test_registry.test` — new `# ---- Phase 17.2 additions: resid-block lookup ----` section before the marker block (+111)

## Verification Evidence

- RED (staged headless VMD, `tmp/biochemeleon-vmd`): `BCHM_TEST_RESULT Total=37 Passed=27 Failed=10 Skipped=0`; failing tests are EXACTLY the 10 new cases (`register_and_lookup`, `register_replace_wholesale`, `register_unregistered_atomic`, `register_empty_noop`, `register_nonexistent_records_ok_when_replaced_later`, `lookup_unknown`, `lookup_after_reset`, `block_survives_reconstruct`, `block_orthogonal_to_assign_reps`, `mixed_round_shape`); full-log scan: 0 `ERROR)`, 0 `bad switch`
- GREEN (same runner): `BCHM_TEST_RESULT Total=37 Passed=37 Failed=0 Skipped=0`; 0 `ERROR)`, 0 `bad switch`, 0 `invalid command name`, `Exiting normally` x1
- reconstruct_from_sentinels byte-unchanged: awk-extracted proc from HEAD vs worktree — diff empty (`RECONSTRUCT_BYTE_UNCHANGED=yes`); `git diff -U0` hunks touch only the namespace-eval block, reset, and the appended section
- Purity: `grep -c "^source" vmd/lib/registry.tcl` → 0; `\bmol\b|atomselect|\btk\b` token scan → 0
- Tcl 8.6-idiom gate (`\blmap\b|\btry\b|\bthrow\b|\btailcall\b|\bcoroutine\b|\byield\b|\bfinally\b` over vmd/lib/ + vmd/gui/) → 0 matches
- Regression insurance beyond the plan gate (reset is a shared proc): all 5 pure-layer suites green on the staged tree — setup_state 47/47, registry 37/37, generators 26/26, rep_tiers 32/32, game_logic 15/15 (157/157), 0 `ERROR)`/`bad switch`/FAILED verdicts in every full log

## Decisions Made

- Empty-mapping no-op implemented as an early-return guard AFTER the validate loop (prose contract: "empty mapping → no-op (block unchanged)"; the block is NOT cleared by an empty call — reset is the only clearer)
- `register_empty_noop` written to pin that contract beyond the plan's literal sequence: after the literal steps (no error; 9001 unregistered) it registers `{9001 5}` then calls with an empty mapping and asserts `hider_for_resid 9001` still returns 5 — the guard is regression-guarded, not just implemented. Same treatment for `register_nonexistent_records_ok_when_replaced_later` (empty-registry empty-mapping no-op + a later real registration works), honoring the case name. Test COUNT unchanged (10 new cases as planned)
- New comments avoid the standalone tokens mol/atomselect/tk (one reword during GREEN) so future automated purity greps stay zero; tclsh is unavailable in this WSL env (17.1-03 precedent) — load-clean proven by the sourced staged run (the suite sources registry.tcl before all 37 cases)
- NO other procs touched; `reconstruct_from_sentinels` byte-identity is the P8/17.1-02 pin, verified by extraction-diff, not eyeball

## Deviations from Plan

None - plan executed exactly as written. (The two test-body strengthenings above stay within the plan's specified case list and prose contract — recorded as decisions, not deviations.)

## Issues Encountered

- `tclsh` not present in this WSL env (plan's GREEN verify lists `tclsh vmd/lib/registry.tcl` as the load-clean check) — resolved per the 17.1-03 precedent: the headless-VMD suite sources registry.tcl at the top, so 37/37 green proves the module loads clean. Root AGENTS.md's "tclsh available" note does not hold in the current environment.
- A benign WSL artifact (`bash: tcsetattr: Inappropriate ioctl for device`) appears in the game_logic suite log tail instead of `Exiting normally`; the suite's marker + tcltest summary printed, all 15 cases passed, 0 errors — not a defect.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- game.tcl (17.2-09) can now: register the fake-resid→CA map ONCE per round after the index split (`register_resid_block`), and in `on_pick`'s `_resolve_pick` call `hider_for_resid $resid` when `is_hider` fails — the mol-side resid read stays in game.tcl (registry stays pure)
- Mixed rounds: the dispatch's resid_start offset produces disjoint blocks; the registry stores whatever mapping it is given (one 9001+k → CA idx mapping per round, replaced wholesale)
- Downstream plans in this phase (17.2-03+) build the splice generator on 17.2-01's RESID_BASE; the in-proc 9001-collision comment is the canonical reference for the mixed-round pick story
- STATE.md NOT modified (worktree protocol — orchestrator owns state updates); position/decisions/lessons recorded here instead

---
*Phase: 17.2-cartoon-newcartoon-generators*
*Completed: 2026-09-05*
