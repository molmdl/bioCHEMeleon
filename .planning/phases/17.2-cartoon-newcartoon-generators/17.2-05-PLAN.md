---
phase: 17.2
plan: 05
type: execute
wave: 3
depends_on: ["17.1-08", "17.2-04"]
files_modified: [vmd/smoke/phase17_cartoon_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "A Cartoon splice renders CONNECTED under the Cartoon rep: the GAME-only exclusive render draws FCylinder coil-tube bits (> 0, NO TriStrip — a T-ss fake residue draws as a tube), and the anchor-window render EXCEEDS the unspliced control's (the fake CA is IN the traced path)"
    - "The hider rep for the Cartoon tier is created with the bare style `Cartoon`, Element coloring, and the user3-conjoined sentinel selections; read-back string-compares to `Cartoon` exactly"
    - "The game's VDW hider sphere coexists with the cartoon bump: user3 stamping + add_hider_reps on the spliced molecule select exactly the 3 fake CAs"
    - "The splice lands with the load-time STRIDE verdict: fake CA structure == T (turn) — the Option-A loop-tube look, never ssrecalc"
  artifacts:
    - path: "vmd/smoke/phase17_cartoon_smoke.tcl"
      provides: "Cartoon-tier end-to-end proof: splice → mutate → CA-only sentinels → structure T → stamp/add Cartoon pair → Tachyon GAME-only + window path-inclusion renders → found-marking → clean restore"
  key_links:
    - from: "vmd/smoke/phase17_cartoon_smoke.tcl"
      to: "vmd/lib/mutation.tcl"
      via: "make_residue_hiders + mutate (the 17.2-04 splice flow, driven directly — the dispatch is 17.2-09)"
      pattern: "mutation::(make_residue_hiders|mutate)"
    - from: "vmd/smoke/phase17_cartoon_smoke.tcl"
      to: "render Tachyon"
      via: "the 17.2-04 render-diff helpers: GAME-only exclusive + window vs control, FCylinder/TriStrip token parse"
      pattern: "render Tachyon"
---

<objective>
Prove the Cartoon tier end-to-end headlessly — the flagship residue-splice consumer.

Purpose: Cartoon is the SS-dependent rep of the family; the research's Option-A decision says a spliced GAM gets STRIDE `T` and renders as a smooth coil/turn tube — connected, in-path, bump-visible. This smoke pins: GAME-only bits render (FCylinder, no TriStrip), the window render proves path inclusion vs the unspliced control, the sentinel/registry surface stays CA-only, and found-marking partitions correctly. Drives mutation + hiders directly (the dispatch lands in 17.2-09; 17.1-04's tiers-smoke precedent).
Output: `vmd/smoke/phase17_cartoon_smoke.tcl` PASS=1.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/17.2-cartoon-newcartoon-generators/17.2-RESEARCH-cartoon-stride.md
@vmd/smoke/phase17_splice_smoke.tcl
@vmd/smoke/phase17_points_smoke.tcl

Key facts (do NOT re-derive):
- Probe-evidence targets (17.2-RESEARCH §2.3/§4.3): GAME-only Cartoon on ONE spliced residue → 6 FCylinder, 0 TriStrip (coil/turn tube; ss=T). Window `resid <a-2> to <a+2>` on the spliced molecule renders MORE than the same window on the unspliced control (the fake CA reroutes the path; probe5b/probe6 5-unit vs 4-unit counts).
- Anchor-window derivation recipe (no anchor bookkeeping needed): the fake CA is ~1.0 Å from its anchor → `atomselect $m "name CA and within 2.0 of <fake CA x y z>"` → its resid IS the anchor's (next-nearest real CA ≥ ~2.8 Å away). Shared by all four tier smokes.
- Splice facts (17.2-04 proven): CA-only sentinel (`resname GAM and beta < 0` == 1/hider, name == CA), fake atoms chain == anchor's chain, segid GAME, structure == T, both junctions bonded, resids 9001+k.
- Render helpers: copy from `vmd/smoke/phase17_splice_smoke.tcl` (axes location off; probe rep added LAST; baseline-zero `index 999999` → 0 primitives; `regexp -all -- {\mFCylinder\y}` / `{\mTriStrip\y}` / `{\mSphere\y}` / `{\mSTri\y}`; delete probe rep after renders).
- Hider-rep contracts (17.1-04): `hiders::stamp_tier_codes {molid tier_of}` BEFORE `hiders::add_hider_reps {molid tier_specs}` (production order — a rep added before the user3 stamp caches an empty selection); specs `{code styleArgs...}`; read-back via the COMBINED-braces molinfo form + `mol repindex` with -1 guard; NEVER `mol showrep off`; NEVER write beta.
- Harness + staging: 17.1-08 conventions (failures list + `_bail` + per-step catch + `BCHM_SMOKE_RESULT` marker + `exit`; registry sourced EXACTLY ONCE; `mkdir -p tmp/biochemeleon-vmd && cp -r vmd tmp/biochemeleon-vmd/`; `timeout 300 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/smoke/phase17_cartoon_smoke.tcl -eofexit < /dev/null' 2>&1 | grep -E "BCHM_SMOKE_RESULT|ERROR\)|bad switch"` — parse the marker, scan the FULL log).
- THIS PLAN EDITS NO LIB FILES (defects → SUMMARY for a gap plan).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write + run the Cartoon-tier end-to-end smoke</name>
  <files>vmd/smoke/phase17_cartoon_smoke.tcl</files>
  <action>
Copy the 17.2-04 harness + render helpers. Demo 1znf (424 atoms), driven directly (NOT through start_game):
1. Splice: `set recs [mutation::make_residue_hiders $m0 3]` → assert 3 records; capture the 3 fake CA coords + resids {9001 9002 9003}; `set gm [mutation::mutate $m0b [list] $recs]` → atoms 424+15; `resname GAM and beta < 0` num == 3, all names CA, resids {9001 9002 9003}, structure == T on all three; `resname GAM` num == 15; segid GAME on all fake atoms.
2. Junction bonds: per fake residue, its N has ≥ 1 bonded C and its C has ≥ 1 bonded N (getbonds + name checks).
3. Hider reps: `hiders::stamp_tier_codes $gm [dict create 1 <the 3 CA indices>]` → user3 ≈ 1.0 (numeric); `hiders::add_hider_reps $gm {{1 Cartoon}}` → numreps == pre_start + 2; read-back via tier_reps + repindex (COMBINED-braces): style == `Cartoon` EXACTLY, color `Element` / `ColorID 7`, selections == `resname GAM and beta < 0 and user2 < 1 and user3 1` / the `user2 > 0` variant.
4. Tachyon renders (probe rep last; baseline-zero first):
   a. GAME-only exclusive: selection `segid GAME`, style `Cartoon`, color `Element` → FCylinder count ≥ 3 (one or more coil-tube bits per residue — research measured 6/residue; assert ≥ 3, not exact) AND TriStrip count == 0 (a T fake never draws sheet ribbon).
   b. Window path-inclusion: for record 1, derive the anchor resid a (the within-2.0-of-fake-CA recipe); probe rep selection `resid <a-2> to <a+2>` style Cartoon → render spliced_win.dat; fresh-load the original demo → same probe → control_win.dat; assert spliced FCyl > control FCyl (IN the traced path).
   c. No salmon check: parse all Color R G B triples from the GAME-only render — none within eps 0.01 of (1.0 0.6 0.6).
   Delete the probe rep after renders.
5. Found-marking: `hiders::mark_found_visual $gm <ca idx 2>` → user2(ca2) > 0 numeric; `resname GAM and beta < 0 and user2 > 0 and user3 1` num == 1; the `< 1` variant num == 2; CA 1/3 user2 still ≤ 0.
6. Cleanup/restore: `mol delete $gm`; reload the original demo → 424 atoms (no residue leak); marker + `exit`.
  </action>
  <verify>Staged run: `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log; GAME-only render has FCyl ≥ 3 and TriStrip == 0; window spliced > control.</verify>
  <done>Cartoon tier proven: splice lands with structure T, CA-only sentinels, connected GAME-only tube bits, in-path window evidence, exact Cartoon read-back, per-tier found partitions, clean restore.</done>
</task>

</tasks>

<verification>
- `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log
- GAME-only Cartoon render: FCyl ≥ 3 + TriStrip == 0 + no salmon
- Window render: spliced > unspliced control (path inclusion)
- Tcl 8.6 gate zero; no lib files touched
</verification>

<success_criteria>
- ROADMAP SC1's headless half for Cartoon: the splice renders CONNECTED inside the user's Cartoon rep (bits + path inclusion), with the Option-A turn-tube look (structure T, no ssrecalc)
</success_criteria>

<output>
After completion, create `.planning/phases/17.2-cartoon-newcartoon-generators/17.2-05-SUMMARY.md`
</output>
