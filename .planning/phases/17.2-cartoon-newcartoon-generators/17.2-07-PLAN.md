---
phase: 17.2
plan: 07
type: execute
wave: 3
depends_on: ["17.1-08", "17.2-04"]
files_modified: [vmd/smoke/phase17_trace_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "A Trace splice is PATH-INCLUDED but draws NO GAME-only geometry: a hiders-only Trace render has 0 FCylinders (the both-endpoints rule — a single fake CA has no CA→CA segment entirely inside the GAME selection) while the anchor-window render EXCEEDS the unspliced control's (the trace builder reroutes through the fake CA)"
    - "The hider rep for the Trace tier is created with the bare style `Trace`, Element coloring, and the user3-conjoined sentinel selections; read-back string-compares to `Trace` exactly"
    - "Trace is SS-INDEPENDENT (UG node65) — the smoke asserts NO structure dependence: the fake CA's structure == T is recorded but the render assertion is purely geometric (path inclusion)"
    - "Trace is a SIBLING CONSUMER of the same splice, not a sidestep (research §5) — one make_residue_hiders call serves it identically to Cartoon/NewCartoon/Tube"
  artifacts:
    - path: "vmd/smoke/phase17_trace_smoke.tcl"
      provides: "Trace-tier end-to-end proof (the both-endpoints 0-bits case + window path inclusion — the family's SS-independent control)"
  key_links:
    - from: "vmd/smoke/phase17_trace_smoke.tcl"
      to: "vmd/lib/mutation.tcl"
      via: "make_residue_hiders + mutate (the 17.2-04 splice flow, driven directly — the dispatch is 17.2-09)"
      pattern: "mutation::(make_residue_hiders|mutate)"
    - from: "vmd/smoke/phase17_trace_smoke.tcl"
      to: "render Tachyon"
      via: "the 17.2-04 render-diff helpers; the 0-GAME-only-bits + window>control pair is the Trace signature"
      pattern: "render Tachyon"
---

<objective>
Prove the Trace tier end-to-end headlessly — the SS-independent CA-trace consumer.

Purpose: Trace draws CA→CA cylinder segments; a hiders-only render over ONE fake residue has ZERO primitives (both trace endpoints must be selected — 17.1 viability P/§3), but the fake CA IS in the fragment's CA path, so the anchor-window render changes vs the unspliced control. This asymmetric signature (0 GAME-only bits + window > control) is the Trace-specific evidence the family's GUI checkpoint relies on. Drives mutation + hiders directly (dispatch = 17.2-09).
Output: `vmd/smoke/phase17_trace_smoke.tcl` PASS=1.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/17.2-cartoon-newcartoon-generators/17.2-RESEARCH-cartoon-stride.md
@vmd/smoke/phase17_splice_smoke.tcl
@vmd/smoke/phase17_cartoon_smoke.tcl

Key facts (do NOT re-derive):
- Probe evidence (17.2-RESEARCH §2.3, probe6): window `resid <a-2> to <a+2>` under Trace on the spliced molecule → 8 FCyl (5-unit path) vs a 4-unit control's fewer — the fake CA reroutes the trace. GAME-only Trace → 0 primitives expected (both-endpoints rule, 17.1-RESEARCH viability §3: trace_gam = 0, trace_protein = 4).
- The both-endpoints rule is the EXPECTED behavior here — the assertion is 0 GAME-only FCyl (a POSITIVE assertion of the rule, unlike the Cartoon/Tube ≥ 1 bits cases).
- Trace param form: bare `Trace` (0 args; read-back `Trace` exactly).
- SS-independence: Trace never consults `structure` — the smoke still records the fake CA's structure == T (the load-time STRIDE fact) but the render assertions are purely geometric.
- Anchor-window derivation + splice facts + hider-rep contracts + render helpers + harness/staging: IDENTICAL to 17.2-05 (copy that smoke; per-tier deltas: style `Trace`; GAME-only assert == 0 FCyl; window assert spliced FCyl > control FCyl).
- THIS PLAN EDITS NO LIB FILES (defects → SUMMARY for a gap plan).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write + run the Trace-tier end-to-end smoke</name>
  <files>vmd/smoke/phase17_trace_smoke.tcl</files>
  <action>
Copy the 17.2-05 harness/render helpers; adapt to Trace. Demo 1znf (424 atoms), driven directly:
1. Splice: `set recs [mutation::make_residue_hiders $m0 3]` → `mutate $m0b [list] $recs` → gm atoms 439; `resname GAM and beta < 0` num == 3 (names CA, resids {9001 9002 9003}, structure == T recorded); `resname GAM` num == 15; segid GAME; junction bonds ≥ 1 each side.
2. Hider reps: stamp_tier_codes (code 1 = the 3 CA indices) → add_hider_reps {{1 Trace}} → numreps == pre_start + 2; read-back style == `Trace` EXACTLY, Element / ColorID 7, selections `resname GAM and beta < 0 and user2 < 1 and user3 1` (+ the found variant).
3. Tachyon renders (probe rep last; baseline-zero first):
   a. GAME-only exclusive (selection `segid GAME`, style Trace, color Element): FCylinder count == 0 (both-endpoints rule — a single fake CA forms no in-selection CA→CA segment; the POSITIVE assertion of the rule) AND Sphere count == 0 (Trace draws no dots).
   b. Window path-inclusion: anchor resid a via the recipe; probe `resid <a-2> to <a+2>` style Trace → spliced_win.dat vs fresh-original control_win.dat → assert spliced FCyl > control FCyl (the trace reroutes through the fake CA — probe6's 8-vs-4 evidence class).
   c. No salmon in any parsed Color.
   Delete the probe rep after renders.
4. Found-marking: mark_found_visual on CA 3 → user2 > 0; found-selection num 1 / hidden 2 (user3 1 conjunct).
5. Cleanup/restore: mol delete gm; reload original → 424 atoms; marker + exit.
  </action>
  <verify>Staged run: `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log; GAME-only render exactly 0 primitives; window spliced > control.</verify>
  <done>Trace tier proven: hiders-only renders nothing (both-endpoints rule pinned), the fake CA is in the traced path (window evidence), exact read-back, per-tier partitions, clean restore.</done>
</task>

</tasks>

<verification>
- `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log
- GAME-only Trace render: 0 FCyl + 0 Sphere (the both-endpoints signature)
- Window render: spliced > unspliced control (path inclusion)
- Tcl 8.6 gate zero; no lib files touched
</verification>

<success_criteria>
- The SS-independent sibling consumer is proven: the SAME splice serves Trace with zero extra machinery (research §5 — no sidestep needed), with the both-endpoints render rule pinned headlessly
</success_criteria>

<output>
After completion, create `.planning/phases/17.2-cartoon-newcartoon-generators/17.2-07-SUMMARY.md`
</output>
