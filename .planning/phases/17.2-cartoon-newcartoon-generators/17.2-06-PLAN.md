---
phase: 17.2
plan: 06
type: execute
wave: 3
depends_on: ["17.1-08", "17.2-04"]
files_modified: [vmd/smoke/phase17_newcartoon_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "A NewCartoon splice renders CONNECTED under the NewCartoon rep: the GAME-only exclusive render draws TriStrip mesh primitives (> 0 — the ribbon mesh covers the fake residue) and the anchor-window render EXCEEDS the unspliced control's (the fake CA is IN the traced path)"
    - "The hider rep for the NewCartoon tier is created with the bare style `NewCartoon`, Element coloring, and the user3-conjoined sentinel selections; read-back string-compares to `NewCartoon` exactly"
    - "The NewCartoon B-spline caveat is recorded but non-blocking: the mesh need not pass exactly through Cα (UG node70) — path INCLUSION is the assertion, not geometric exactness"
    - "The splice lands with structure == T on the fake CA (Option-A turn-tube; the mesh draws the turn as smooth ribbon geometry — never ssrecalc)"
  artifacts:
    - path: "vmd/smoke/phase17_newcartoon_smoke.tcl"
      provides: "NewCartoon-tier end-to-end proof (TriStrip token render + path inclusion + sentinel/registry surface + found-marking + restore)"
  key_links:
    - from: "vmd/smoke/phase17_newcartoon_smoke.tcl"
      to: "vmd/lib/mutation.tcl"
      via: "make_residue_hiders + mutate (the 17.2-04 splice flow, driven directly — the dispatch is 17.2-09)"
      pattern: "mutation::(make_residue_hiders|mutate)"
    - from: "vmd/smoke/phase17_newcartoon_smoke.tcl"
      to: "render Tachyon"
      via: "the 17.2-04 render-diff helpers with the TriStrip token (the NewCartoon primitive)"
      pattern: "TriStrip"
---

<objective>
Prove the NewCartoon tier end-to-end headlessly — the mesh-rendering residue-splice consumer.

Purpose: NewCartoon draws smooth B-spline ribbons (TriStrip primitives in Tachyon exports) — research measured 14 TriStrip per GAME-only fake residue and 70 in a 5-unit window. This smoke pins the TriStrip rendering (GAME-only > 0), path inclusion (window > control), the CA-only sentinel surface, exact `NewCartoon` read-back, and found-marking. Drives mutation + hiders directly (dispatch = 17.2-09).
Output: `vmd/smoke/phase17_newcartoon_smoke.tcl` PASS=1.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/17.2-cartoon-newcartoon-generators/17.2-RESEARCH-cartoon-stride.md
@vmd/smoke/phase17_splice_smoke.tcl
@vmd/smoke/phase17_cartoon_smoke.tcl

Key facts (do NOT re-derive):
- Probe-evidence targets (17.2-RESEARCH §2.3/§4.3): GAME-only NewCartoon on ONE spliced residue → 14 TriStrip primitives (vs 0 untraced); window `resid <a-2> to <a+2>` → 70 TriStrip (5-unit) — assert GAME-only TriStrip ≥ 1 and spliced-window TriStrip > control-window TriStrip (never exact counts — B-spline geometry varies with the local path).
- Anchor-window derivation recipe: the fake CA is ~1.0 Å from its anchor → `atomselect $m "name CA and within 2.0 of <fake CA coords>"` → resid = anchor. (Same recipe as 17.2-05.)
- NewCartoon param form: bare `NewCartoon` (0 args valid; read-back `NewCartoon` exactly — never hand-type variants; viability §6.6).
- Splice facts + hider-rep contracts + render helpers + harness/staging: IDENTICAL to 17.2-05 (copy that smoke; the ONLY per-tier deltas are the style name and the primitive token: TriStrip instead of FCylinder for the GAME-only assertion; the window assertion may match EITHER token — parse both, assert spliced_total(FCyl+TriStrip) > control_total).
- THIS PLAN EDITS NO LIB FILES (defects → SUMMARY for a gap plan).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write + run the NewCartoon-tier end-to-end smoke</name>
  <files>vmd/smoke/phase17_newcartoon_smoke.tcl</files>
  <action>
Copy the 17.2-05 harness/render helpers; adapt to NewCartoon. Demo 1znf (424 atoms), driven directly:
1. Splice: `set recs [mutation::make_residue_hiders $m0 3]` → `mutate $m0b [list] $recs` → gm atoms 439; `resname GAM and beta < 0` num == 3 (names CA, resids {9001 9002 9003}, structure == T); `resname GAM` num == 15; segid GAME on all fake atoms; junction bonds ≥ 1 each side.
2. Hider reps: stamp_tier_codes (code 1 = the 3 CA indices) → add_hider_reps {{1 NewCartoon}} → numreps == pre_start + 2; read-back style == `NewCartoon` EXACTLY, Element / ColorID 7, selections `resname GAM and beta < 0 and user2 < 1 and user3 1` (+ the found variant).
3. Tachyon renders (probe rep last; baseline-zero first):
   a. GAME-only exclusive (selection `segid GAME`, style NewCartoon, color Element): TriStrip count ≥ 1 (the ribbon mesh covers the fake residue — research measured 14/residue) AND FCylinder count == 0 (a T fake draws NO helix cylinders).
   b. Window path-inclusion: anchor resid a via the recipe; probe `resid <a-2> to <a+2>` style NewCartoon → spliced_win.dat vs fresh-original control_win.dat → assert spliced(TriStrip + FCyl) > control(TriStrip + FCyl) (B-spline token mix may vary; total primitives is the robust signal).
   c. No salmon in any parsed Color (eps 0.01 of 1.0 0.6 0.6).
   Delete the probe rep after renders.
4. Found-marking: mark_found_visual on CA 2 → user2 > 0; found-selection num 1 / hidden 2 (user3 1 conjunct).
5. Cleanup/restore: mol delete gm; reload original → 424 atoms; marker + exit.
  </action>
  <verify>Staged run: `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log; GAME-only TriStrip ≥ 1 and FCyl == 0; window spliced > control.</verify>
  <done>NewCartoon tier proven: mesh bits render over the fake residue, path inclusion vs control, exact read-back, per-tier partitions, clean restore.</done>
</task>

</tasks>

<verification>
- `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log
- GAME-only NewCartoon render: TriStrip ≥ 1 + FCyl == 0 + no salmon
- Window render: spliced > unspliced control (path inclusion, total-token comparison)
- Tcl 8.6 gate zero; no lib files touched
</verification>

<success_criteria>
- ROADMAP SC1's headless half for NewCartoon: the splice renders CONNECTED inside the user's NewCartoon rep (mesh bits + path inclusion)
</success_criteria>

<output>
After completion, create `.planning/phases/17.2-cartoon-newcartoon-generators/17.2-06-SUMMARY.md`
</output>
