---
phase: 17.2
plan: 12
type: execute
wave: 7
depends_on: ["17.1-14", "17.2-11"]
files_modified: [vmd/tests/rep_verify.tcl]
autonomous: false
user_setup: []
must_haves:
  truths:
    - "rep_verify.tcl gains the cartoon round: pv_round3 builds a 5-hider state (Cartoon 1 NewCartoon 1 Trace 1 Tube 1 VDW 1), applies it, calls on_start, and AUTO-LOGS the round composition (per-tier remaining_by_rep, fake resid block via hider_for_resid dumps, CA sentinel indices, per-tier rep read-backs, structure==T) to rep_verify_log.txt"
    - "The pick observer logs the fallback verdict for every click: when a pick lands on a NON-registered index, the log records the clicked atom's resid + the hider_for_resid resolution — capturing WHICH atom delivers each bump click (CA vs N/C/O/CB — the research's open pick-target question)"
    - "The human's required actions are only real clicks (press p once; find the 5 hiders — at least 2 by clicking the cartoon BUMP not the VDW sphere) plus at most 3 short pastes (pv_round3, pv_report, pv_cleanup) — the standing auto-issue/auto-log directive"
    - "ONE consolidated GUI checkpoint verifies: per-rep blend for all four residue reps (smooth tube bumps — no fork/gap/floating nub), pick-through on N/C/O/CB, SS-shape sanity (real helices/sheets undistorted — the neighbor-ripple guard), multi-hider coexistence, and cleanup restore with no residue remnants"
  artifacts:
    - path: "vmd/tests/rep_verify.tcl"
      provides: "extended auto-driver: pv_round3 cartoon helper + fallback-verdict logging in pv_observe + updated end-of-source instructions (EXTENDS 17.1-14's driver — no duplication)"
  key_links:
    - from: "vmd/tests/rep_verify.tcl (pv_round3)"
      to: "vmd/gui/setup_tab.tcl + vmd/lib/game.tcl"
      via: "apply_state with the crafted 5-hider state + on_start (the same driver pattern as 17.1-14's rounds)"
      pattern: "(apply_state|on_start)"
    - from: "vmd/tests/rep_verify.tcl (pv_observe)"
      to: "vmd/lib/registry.tcl"
      via: "is_hider + hider_for_resid verdicts logged per pick (the fallback-transparency record)"
      pattern: "hider_for_resid"
---

<objective>
Extend the 17.1-14 auto-driver (`vmd/tests/rep_verify.tcl`) with the cartoon round, then run the ONE consolidated GUI human-verify checkpoint for the residue-splice family.

Purpose: the headless smokes proved geometry, sentinels, dispatch, and both find paths — but blend GESTALT (is the bump invisible-in-plain-sight?), SS-shape sanity (real helices/sheets undistorted next to the bumps), multi-hider bump coexistence, and the actual C-side pick target on cartoon geometry (CA vs N/C/O/CB) are GUI-only. Per the standing directive the session is driver-driven and auto-logged; the human only clicks.
Output: extended `vmd/tests/rep_verify.tcl` + an APPROVED GUI checkpoint record (or precisely recorded defects for gap closure).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/17.2-cartoon-newcartoon-generators/17.2-RESEARCH-cartoon-stride.md
@.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-14-PLAN.md
@vmd/tests/rep_verify.tcl
@vmd/tests/pick_verify.tcl

Key facts (do NOT re-derive):
- rep_verify.tcl (17.1-14) ALREADY provides: the Tk-guarded if-wrap body, pv_log (file + vmdcon echo), auto-session setup (source extension → load demo → apply_state → on_start → after-4500 auto-dump), pv_state dumps, pv_round2 (lock-scene), pv_cleanup/pv_cleanup_check, pv_report, the pick observer `trace add variable ::vmd_pick_event write pv_observe`. EXTEND it — do NOT duplicate or restructure.
- pv_round3 mirrors pv_round2's shape: resolve the live target the SAME way pv_round2 does (earlier rounds mutated/restored molecules — never assume a fixed molid), build the crafted state via validate_state → `setup_tab::apply_state` → `::biochemeleon::on_start` → `after 4500` auto-dump. Round-3 state: `validate_state [dict create target_mode loaded selected_object <resolved molid> hider_count 5 per_rep [dict create Cartoon 1 NewCartoon 1 Trace 1 Tube 1 VDW 1] lock_scene 0 difficulty_easy 1] <atom_count>` — 4 residue tiers + 1 VDW in ONE round (blend + pick + coexistence in a single pass; the VDW hider anchors the game-sphere-vs-bump comparison).
- pv_observe upgrade (READ-ONLY — no game behavior change): after the existing logging, log the fallback verdict: `registry::is_hider <idx>` true → `is_hider=1 direct`; false → read the clicked atom's resid on the game molecule (atomselect index → single-keyword `get resid`, lindex 0, `$sel delete`) and log `is_hider=0 resid=<r> fallback=<registry::hider_for_resid result>`. This captures WHICH atom delivers each bump click (the research's ASSUMED open question — CA vs N/C/O/CB).
- pv_state round-3 additions: per-tier remaining_by_rep; the fake resid block (dump hider_for_resid for 9001..9005); CA sentinel indices + their `structure` values (log — expect T); per-tier rep read-backs via tier_reps + repindex; numreps; game molecule atom count (orig + 21 for the crafted round: 4 residues × 5 + 1 simple).
- Pick contract (LOCKED): first-click quirk — ONE keyboard `p` press per round arms C-side pick delivery. The driver instructs the user to press `p` once (a real keyboard act, never a paste).
- GUI checklist items this session owns (research §10.4): 1. bump blended (no fork/gap/floating nub — the PP14 pathology); 2. bump clicks register (atom logged); 3. VDW sphere coexists (cyan → green on find); 4. multi-hider coexistence (the 05-08 GUI-only bug class — MANDATORY); 5. all four reps show the bump; 6. default-demo round end-to-end; 7. cleanup restore (no remnants); 8. idle-fidelity (no neighbor-ripple on real helices/sheets); 9. magnitude tuning (1.0 Å subtle-but-findable; if too subtle record it — 1.25 is law-safe via SPLICE_DISPLACEMENT, NEVER ≥ 1.4).
- Tcl 8.5; NO grab set; `::tk_version` global qualifier; short lines (< ~70 cols, the two-paste rule); headless: the whole body no-ops (one warn line).
- Staging: `mkdir -p tmp/biochemeleon-vmd && cp -r vmd tmp/biochemeleon-vmd/`; headless check `bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/tests/rep_verify.tcl -eofexit < /dev/null' 2>&1 | grep -E "ERROR\)|bad switch"` → one warn line, no errors.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend rep_verify.tcl with the cartoon round + fallback logging</name>
  <files>vmd/tests/rep_verify.tcl</files>
  <action>
1. `pv_round3` — mirror pv_round2: resolve the live target (reuse the driver's existing resolution code), build the 5-hider crafted state via validate_state (per context), `setup_tab::apply_state`, `::biochemeleon::on_start`, `after 4500` auto-dump of the extended pv_state. Log via pv_log ONLY.
2. `pv_observe` upgrade: append the fallback-verdict lines per context (is_hider / resid / hider_for_resid — read-only).
3. pv_state additions: the resid-block dump (hider_for_resid 9001..9005), CA sentinel indices + structure values, per-tier rep read-backs, game atom count.
4. Update the end-of-source instructions (≤ 8 short lines, extending 17.1's): after the 17.1 rounds, paste `pv_round3`; press `p` ONCE; find the 5 hiders — click the cartoon BUMP (not the VDW sphere) for at least the Cartoon and Tube hiders; bumps should read as smooth tube detours; nearby real helices/sheets untouched; found ones turn green; then `pv_report` and `pv_cleanup`.
5. Keep the Tk-guard if-wrap, pv_* naming, short lines, `::tk_version` qualifier, no grab set. Headless: unchanged single-warn no-op.
  </action>
  <verify>Staged headless source: exactly one warn line, NO errors, no dialog attempted; 8.6 gate zero; `grep -c "grab set" vmd/tests/rep_verify.tcl` → 0; `grep -n "pv_round3" vmd/tests/rep_verify.tcl` shows the helper; full-log scan clean.</verify>
  <done>The auto-driver carries the cartoon round: pv_round3 + fallback-verdict logging + extended dumps; human pastes limited to pv_round3 / pv_report / pv_cleanup.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The complete 17.2 phase: ONE shared GAM-residue splice serving Cartoon/NewCartoon/Trace/Tube, dispatched through lock-scene/randomize infrastructure, exercised in a real VMD GUI via the auto-logged driver (the cartoon round with 4 residue bumps + 1 VDW sphere).</what-built>
  <how-to-verify>
1. Open a real VMD GUI on Windows. Extensions → Tk Console. `cd` to the staging root (tmp/biochemeleon-vmd — freshly staged with `cp -r vmd tmp/biochemeleon-vmd/`).
2. Paste ONE line: `source vmd/tests/rep_verify.tcl` — the driver loads everything, opens the dialog, and auto-runs round 1 (the 17.1 six-tier round) with full logging.
3. Complete the 17.1 rounds as in the 17.1-14 session, then paste: `pv_round3` — the driver starts the CARTOON round (Cartoon 1 NewCartoon 1 Trace 1 Tube 1 VDW 1) and auto-logs the round composition to rep_verify_log.txt.
4. Press `p` ONCE (arms pick delivery — the locked first-click quirk). Find all 5 hiders:
   - For the Cartoon and Tube hiders: click the BUMP on the cartoon geometry (NOT the small VDW sphere) — the fallback-path proof. The log records which atom delivered each click.
   - For NewCartoon and Trace: click the bump/segment at the fake residue.
   - For the VDW hider: click the cyan sphere (direct path).
   - While hunting, judge BLEND per residue rep: bumps read as smooth tube detours — no fork, no gap, no floating nub; no salmon/pink; nearby REAL helices/sheets undistorted (idle-fidelity).
5. After each find: the found hider turns GREEN and the Game-tab remaining label decrements with per-tier counts (easy mode). After all 5 (win box): paste `pv_report`, then `pv_cleanup`; confirm the restored molecule has no residue remnants (atom count back to original in the log).
6. Record per item: (a) blend verdict per residue rep (Cartoon/NewCartoon/Trace/Tube), (b) for each bump click — did the find register? which atom delivered (check the log's fallback lines)?, (c) SS-shape sanity (real helices/sheets render + bumps are tubes), (d) multi-hider coexistence (4 bumps + 1 sphere all visible/clickable), (e) found-green visibility per rep, (f) magnitude: are 1.0 Å bumps findable but subtle? (record if 1.25 tuning is wanted — never ≥ 1.4), (g) any ERROR/oddity in rep_verify_log.txt.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the failing items (which rep / which step).</resume-signal>
</task>

</tasks>

<verification>
- Headless: rep_verify.tcl no-ops cleanly in text mode (one warn line, zero errors); 8.6 + grab-set gates zero
- GUI checkpoint record: per-rep blend verdicts, bump-click delivery atoms (fallback evidence), SS-shape sanity, multi-hider coexistence, magnitude verdict, cleanup-restore — recorded with the auto-log transcript
</verification>

<success_criteria>
- The consolidated GUI checkpoint is APPROVED (or its defects are precisely recorded for gap closure) — the last unverified surface of 17.2
- The pick-target open question (CA vs N/C/O/CB on cartoon geometry) is answered with logged evidence
</success_criteria>

<output>
After completion, create `.planning/phases/17.2-cartoon-newcartoon-generators/17.2-12-SUMMARY.md`
</output>
