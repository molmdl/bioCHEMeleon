---
phase: 17.2-cartoon-newcartoon-generators
plan: 11
subsystem: testing
tags: [vmd, tcl, headless-smoke, capstone, full-suite-gate, under-generation, mixed-residue-round, lock-scene, residue-splice, green-gate]

# Dependency graph
requires:
  - phase: 17.1-13
    provides: the composition capstone this plan rewrites (rounds A/B kept byte-identical; round C rewritten) and the PLAN-DETAIL draw-adaptive invariant precedent
  - phase: 17.2-03
    provides: the widened seam (ALL 10 GAME_REPS implemented; tier_kind 'residue' for Cartoon/NewCartoon/Trace/Tube) that obsoleted the old under-generation case
  - phase: 17.2-09
    provides: residue-kind dispatch routing + the on_pick resid-block fallback + the collapse-loader convention for residue rounds on 1znf
  - phase: 17.2-10
    provides: the draw-adaptive assertion pattern (observed-layout invariants; strict pins only at full generation) and the state-gate drive (round_reset -> countdown -> begin_play)
provides:
  - vmd/smoke/phase17_capstone_smoke.tcl rewritten for the widened seam: under-generation via Ribbons (valid VMD style NOT in GAME_REPS -> dropped with the vmdcon -warn), NEW mixed residue round (VDW 2 + Cartoon 2), NEW lock-scene Cartoon derivation round
  - the phase's FULL-SUITE green-gate record: 6/6 tcltest suites (205 tests, Failed=0) + 29/31 smokes PASS=1 on fresh staging, zero ERROR)/bad switch in all 44 logs, 8.6 + grab-set + ssrecalc decision gates zero
  - two PRE-EXISTING draw-dependent smoke flakes characterized and recorded for gap closure (phase17_dispatch_smoke step-8, phase17_licorice_smoke P-pins) -- zero lib changes this phase
affects: [17.2-12 (GUI checkpoint -- the headless surface is declared green), gap-closure plan (the two recorded smoke flakes), phase 18+ (capstone as the composition-root regression baseline)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "suite_driver.tcl wrapper: tcltest suites have no explicit exit and VMD's text console hangs past stdin EOF over the WSL->Windows pipe -- source the suite then `exit` (the smokes' explicit-exit pattern, applied to suites)"
    - "observed-layout invariants with strict full-generation pins: request-side pins assert unconditionally; layout pins assert when the draw generated in full (17.2-10 pattern, now at capstone level)"
    - "resid-block zip assertion: hider_for_resid (9001+i) == the i-th residue CA in file order -- draw-independent at any generated count"

key-files:
  created: []
  modified:
    - vmd/smoke/phase17_capstone_smoke.tcl

key-decisions:
  - "Under-generation vehicle moved Cartoon -> Ribbons (per plan): Ribbons is a VALID VMD style excluded from GAME_REPS (viability 5b), so {Ribbons 2 VDW 1} exercises the exact pre-17.2 drop path (resolve clean-step drop + game.tcl tier_kind '' vmdcon -warn) now that Cartoon implements"
  - "Rounds D/E assert draw-adaptive invariants with the plan's exact pins as the strict full-generation special case -- the plan's own must-have truths specify {Cartoon n}; the task action's literal {Cartoon 3} is unreachable draw-independently (randomize_per_rep's quick-008 subset contract over a single allowed tier: c = randint(0, n) can underspend)"
  - "The two red smokes were NOT edited: both pre-existing and draw-dependent, zero lib changes this phase (git diff a7f28a9..HEAD -- vmd/lib/ is empty), and the plan's file scope is the capstone only -- recorded for a gap-closure plan instead"

patterns-established:
  - "Capstone green-gate shape: every suite via suite_driver.tcl, every smoke via -eofexit, one command each, per-file log, BCHM marker + ERROR)/bad-switch scan + Exiting-normaliy check per log"
  - "Lock-scene residue derivation assertions: per_rep keys == detected set exactly; P9 chain hider_count == effective_total == n; CA-only sentinels; resid 9001+k; numreps pre+2; restore-style snapshot fidelity"

# Metrics
duration: 45min
completed: 2026-09-07
---

# Phase 17.2 Plan 11: Capstone + Full-Suite Green Gate Summary

**The capstone now proves the widened seam end-to-end — Ribbons-drop under-generation, a mixed VDW+Cartoon residue round with resid-block zip and a public-surface fallback find, and a lock-scene Cartoon derivation with snapshot-faithful restore (PASS x4) — and the full-suite gate records 205/205 suite tests plus 29/31 smokes green with the two reds characterized as pre-existing draw-dependent flakes.**

## Performance

- **Duration:** ~45 min (44 headless VMD runs total)
- **Started:** 2026-09-06T23:57+08:00 (approx, after a7f28a9)
- **Completed:** 2026-09-07T00:41+08:00
- **Tasks:** 2
- **Files modified:** 1 (`vmd/smoke/phase17_capstone_smoke.tcl`, +608/−70)

## Accomplishments
- Round C rewritten for the widened seam: `start_game $m3 3 [dict create Ribbons 2 VDW 1] 0` -> the log carries `Warning) bioCHEMeleon: rep 'Ribbons' has no generator yet -- dropped from this round` (a Warning) line, never ERROR)); per_rep == {VDW 1}, P9 hider_count == 1, 425 atoms, hider {424}, user3 1.0, registry 1, remaining_by_rep {VDW 1}, cleanup restores the original. Rounds A/B byte-identical.
- NEW round D (mixed residue): `start_game $m4 4 [dict create VDW 2 Cartoon 2] 0` on collapsed 1znf -> request-side pins unconditional (per_rep {VDW 2 Cartoon 2} GAME_REPS-ordered, P9 hider_count == 4, tier table {1 VDW 2}{2 Cartoon 2}); observed-layout invariants (chain classifier: G sentinels {424 425} + nR CAs, resid zip 9001+i -> i-th CA, atoms formula 424+2+4nR+cb); ONE public-surface fallback find (drive to playing, on_pick residue-1's C = CA+1 -> the fallback resolves the CA: status found, user2(CA) > 0, user2(C) == 0, remaining 3, rbr {VDW 2 Cartoon 1}, "Found one! 3 remaining"); cleanup clears registry AND the resid block, restores 424 atoms.
- NEW round E (lock-scene Cartoon): pre-style rep 0 Cartoon + `start_game $m5 3 {} 1` -> per_rep keys == {Cartoon} EXACTLY (the derivation proof onto the detected residue tier), P9 chain, ALL sentinels CA with user3 1.0, CA structures in the load-time-STRIDE {T C} family (recorded), resid zip, numreps pre+2, the Cartoon pair's read-back (style Cartoon / Element / ColorID 7 / "user3 1" selections), remaining_by_rep {Cartoon n'}; cleanup restores rep-0 style Cartoon (snapshot fidelity) + 424 atoms.
- **Capstone PASS x4** on fresh staging (3 standalone consecutive + 1 gate run), zero ERROR)/bad switch, `Exiting normally` every run. The three runs exercised ALL draw branches: full generation (436/439), a GLY-anchor 4-atom record (D cb=1 -> 435 atoms), a 2-draw lock-scene (434), and an under-generated 3-draw (E drawn 3 -> res 2 -> 434) — the draw-adaptive invariants held on every branch.
- FULL-SUITE GATE executed sequentially on the same staging (see the table below); 8.6-idiom, grab-set, and ssrecalc decision gates all zero.

## FULL-SUITE GATE TABLE (fresh staging tmp/cap172-gate, sequential)

tcltest suites (via suite_driver.tcl — see Deviations; each: zero ERROR)/bad switch, clean exit):

| Suite | Total | Passed | Failed | Skipped |
| --- | --- | --- | --- | --- |
| test_setup_state | 47 | 47 | 0 | 0 |
| test_registry | 37 | 37 | 0 | 0 |
| test_generators | 26 | 26 | 0 | 0 |
| test_rep_tiers | 49 | 49 | 0 | 0 |
| test_game_logic | 15 | 15 | 0 | 0 |
| test_splice | 31 | 31 | 0 | 0 |
| **Total** | **205** | **205** | **0** | **0** |

Smokes (31; every log: zero ERROR), zero "bad switch", `Exiting normally` present):

| Smoke | Result | Notes |
| --- | --- | --- |
| phase13_smoke | PASS=1 | |
| phase14_gui_smoke | PASS=1 | Tk load-gate skips cleanly in text mode |
| phase14_mol_smoke | PASS=1 | |
| phase15_smoke | PASS=1 | |
| phase15_backup_smoke | PASS=1 | |
| phase15_game_smoke | PASS=1 | |
| phase15_mutation_smoke | PASS=1 | |
| phase16_entry_smoke | PASS=1 | |
| phase16_gametab_smoke | PASS=1 | |
| phase16_hiders_smoke | PASS=1 | |
| phase16_onpick_smoke | PASS=1 | |
| phase16_pick_smoke | PASS=1 | |
| phase16_placement_smoke | PASS=1 | |
| phase16_restart_smoke | PASS=1 | |
| phase16_smoke | PASS=1 | |
| phase17_tiers_smoke | PASS=1 | |
| phase17_bonded_smoke | PASS=1 | |
| phase17_dispatch_smoke | PASS=0 | **PRE-EXISTING draw-dependent flake** (see Issues); 3/3 draws red this session |
| phase17_points_smoke | PASS=1 | |
| phase17_lines_smoke | PASS=1 | |
| phase17_licorice_smoke | PASS=0 | **PRE-EXISTING P-pin defect** (see Issues); 2/3 draws red, 1/3 green |
| phase17_cpk_smoke | PASS=1 | |
| phase17_dynbonds_smoke | PASS=1 | |
| phase17_capstone_smoke | PASS=1 | this plan's rewrite; PASS x4 total |
| phase17_splice_smoke | PASS=1 | |
| phase17_cartoon_smoke | PASS=1 | |
| phase17_newcartoon_smoke | PASS=1 | |
| phase17_trace_smoke | PASS=1 | |
| phase17_tube_smoke | PASS=1 | |
| phase17_residue_dispatch_smoke | PASS=1 | |
| phase17_e2e_smoke | PASS=1 | |
| **Total** | **29/31 PASS=1** | 2 pre-existing draw-dependent flakes, recorded below |

Cross-log sweep: 44 logs (6 suites + 31 smokes + 3 capstone rerun logs + 4 flake-characterization reruns), **ERROR) count 0 and "bad switch" count 0 in every log; every log carries `Exiting normally`**.

Code gates (on the repo, not staging):
- `grep -rnE "\blmap\b|\btry\b|\bthrow\b|\btailcall\b|\bcoroutine\b|\byield\b|\bfinally\b" vmd/lib/ vmd/gui/` -> **0**
- `grep -rnE "grab set" vmd/gui/` -> **0**
- `grep -rn "mol ssrecalc" vmd/lib/ | grep -v "^\S*:\s*#"` -> **0** (zero live call sites); splice.tcl lines 17-24 carry the Option-A decision block ("Accepted for 17.2 = **Option A**" + the NEVER-call prohibition)

## Task Commits

1. **Task 1: Capstone smoke — seam-rewritten under-generation + mixed residue + lock-scene Cartoon rounds** - `238b368` (test)
2. **Task 2: Full-suite green gate + ss-decision recording check** - gate-only (no code change; the results table lives in this SUMMARY; no fix commit — nothing red was caused by this phase)

**Plan metadata:** (docs commit follows this summary)

## Files Created/Modified
- `vmd/smoke/phase17_capstone_smoke.tcl` - the 17.2 capstone: header rewritten for the five-round shape (A/B kept byte-identical, C rewritten to Ribbons, D/E added with the collapse loader), observed-layout + strict-pin assertion structure, recording callbacks + state-gate drive for the public-surface fallback find

## Decisions Made
- **Ribbons as the under-generation vehicle (per plan).** The 17.1-13 case `{Cartoon 2 VDW 1}` now GENERATES (Cartoon implements since 17.2-03); `{Ribbons 2 VDW 1}` reproduces the identical post-resolve state (per_rep {VDW 1}, P9 total 1) through the real drop path: resolve_per_rep's clean step drops the non-GAME_REPS key, game.tcl's warning loop fires on tier_kind "" — a vmdcon -warn, never an ERROR).
- **Draw-adaptive invariants for D/E, strict pins as the full-generation special case.** The plan's must-have truths specify `{Cartoon n}`; the task action's literal `{Cartoon 3}` is not draw-independent — with a single allowed tier, randomize_per_rep draws c = randint(0, n) and can underspend (the capstone's own PLAN-DETAIL note documented this exact conflict for round B in 17.1-13). D's exact pins (436 atoms, {424 425 427 432}, 9001->427/9002->432, fallback C 428, remaining 3, rbr {VDW 2 Cartoon 1}) ARE asserted whenever generation is full (nR == 2 with all-CB records) — run 1 proved them; runs 2-3 proved the invariant branches (GLY 4-atom record, E under-gen) stay green.
- **No edits to the two red smokes.** Both failures are pre-existing and draw-dependent; this phase's diff touches zero lib files (`git diff a7f28a9..HEAD -- vmd/lib/` empty), and the plan's file scope is the capstone only. Characterized and recorded for a gap-closure plan (below).
- **Suites run through a VMD-headless driver wrapper.** tclsh is unavailable in this WSL session (17.2-09 precedent) and the suites lack an explicit `exit` — the first suite run COMPLETED (47/47 in the log) but VMD's text console hung past stdin EOF over the WSL->Windows pipe and the command timed out. A one-purpose driver (`source` the suite by name, then `exit`) makes every suite log terminate cleanly. Staging-only file (`tmp/cap172-gate/suite_driver.tcl`), no repo change.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Suite runs hung after completion (no explicit exit in the suites)**
- **Found during:** Task 2 (first suite run, test_setup_state)
- **Issue:** The .test suites end after tcltest reporting; under `vmd -e ... -eofexit < /dev/null` VMD entered its text console and hung (the suite itself had already printed `BCHM_TEST_RESULT Total=47 Passed=47 Failed=0`)
- **Fix:** `tmp/cap172-gate/suite_driver.tcl` sources the suite by name then calls `exit`; the suite name is sed-swapped per run. Every suite log now ends with `Exiting normally`
- **Files modified:** none in the repo (staging-only driver under gitignored tmp/)
- **Verification:** all 6 suites re-run through the driver: Failed=0 + clean exit + zero ERROR)/bad switch each
- **Committed in:** n/a (no repo change)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** The driver is the suites' missing explicit-exit; no scope creep, no repo changes beyond the plan's one file. The draw-adaptive D/E assertion structure is NOT a deviation — it implements the plan's own must-have truths (`{Cartoon n}`) and follows the two precedents the brief cites (17.1-13 PLAN-DETAIL, 17.2-10 draw-adaptive pattern).

## Issues Encountered (pre-existing, NOT edited — gap-closure candidates)

**1. phase17_dispatch_smoke step-8 flake (known since 17.2-03/17.2-09, report-only)**
- **Failing asserts:** `reg2_count:exp=5 got=0` / `rbr3_sum:exp=5 got=0` / `idxs2_count:exp=5 got=0` (rerun with got=1 — draw-dependent)
- **Mechanism (from the log):** the bare 2-arg randomize draw on 1k8p (DNA) included residue tier 'Tube' -> `Warning) bioCHEMeleon: residue tier 'Tube' could not generate -- dropped from this round (make_residue_hiders: no protein anchors)` -> 0 (or 1) generated vs the smoke's pinned 5
- **Status:** 3/3 draws red this session; all-simple draws green (17.2-09 recorded 2/3). Pre-existing: zero lib changes this phase. **Gap-closure candidate:** the drop-ungeneratable-tiers + effective-total-recompute policy + the 17.2-10 draw-adaptive rewrite of step 8 (17.2-09's Next-Phase-Readiness already flagged exactly this).

**2. phase17_licorice_smoke wrong P pins (NEW characterization — pre-existing defect)**
- **Failing asserts:** `mimic_rad_556:exp=1.55 got=1.7999999523162842` (rerun: mimic_rad_555) + `hider_color:0.5 0.5 0.2 not within 0.01 of an element color`
- **Mechanism:** the smoke pins element VDW radii {C 1.70 N 1.55 O 1.52 S 1.80 P 1.55} and a P color of tan {0.5 0.5 0.31}; when a 1k8p backbone-P anchor is drawn, VMD's actual element table reads P radius 1.80 and renders P at {0.5 0.5 0.2} — both pins are wrong for P
- **Status:** draw-dependent (2/3 red, 1/3 green this session — non-P draws pass). The smoke's own comment (lines 164-168) anticipated it: "P tan is the pinned approximate value from colordefs.dat ... re-pin from a render if 1k8p anchors draw a P hider and the eps-0.01 compare trips." Pre-existing: zero lib changes this phase. **Gap-closure candidate:** re-pin P radius 1.80 + the render-observed P color {0.5 0.5 0.2} (or assert the P-color family), per the smoke's own re-pin method.

- `vmd/tests/pick_verify.tcl` / `rep_verify.tcl` are Tk-gated GUI driver scripts (no .test suffix, no BCHM_TEST_RESULT) — not part of the headless gate; their load-gate skip is exercised inside phase16_pick_smoke (PASS=1).

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- The 17.2 headless surface is declared green at composition-root level: ROADMAP SC1 (per-tier generators), SC2 (lock-scene derivation, now onto a residue tier), SC3 (playable mixed round) all proven; the phase is ready for the single consolidated GUI checkpoint (17.2-12, C-side pick path).
- The two recorded flakes are the only reds in the suite; both are smoke-side (assertions), not engine-side, and both have concrete gap-closure recipes above.
- The capstone is the standing composition-root regression baseline: five rounds (mixed simple, lock-scene simple, under-generation, mixed residue, lock-scene residue) in one file, draw-adaptive where generation is random, strict where the plan pins.

---
*Phase: 17.2-cartoon-newcartoon-generators*
*Completed: 2026-09-07*
