---
phase: 17.2
plan: 02
type: tdd
wave: 1
depends_on: ["17.1-02"]
files_modified: [vmd/lib/registry.tcl, vmd/tests/test_registry.test]
autonomous: true
must_haves:
  truths:
    - "registry::register_resid_block {mapping} stores a resid → hider-index map ATOMICALLY: validates every idx is a registered hider first (error 'hider <idx> not registered' on any miss), then REPLACES the block wholesale (one call per round, never merged)"
    - "registry::hider_for_resid {resid} returns the registered hider index for a fake-residue resid (9001+k) or '' for unknown — the pure half of the multi-atom pick fallback (a click on a fake GAM residue's N/C/O/CB resolves to the registered CA)"
    - "registry::reset clears BOTH _records and _resid_block (cleanup must not leak a stale block across rounds)"
    - "reconstruct_from_sentinels does NOT touch _resid_block (only reset and register_resid_block mutate it — pinned contract; the dispatch always re-registers after reconstruct in residue rounds)"
    - "registry stays PURE: no mol/atomselect/tk, sources NOTHING — the mol-side resid read lives in game.tcl (17.2-09)"
  artifacts:
    - path: "vmd/lib/registry.tcl"
      provides: "pure resid-block API: register_resid_block + hider_for_resid (+ _resid_block namespace var; reset extended)"
      exports: ["register_resid_block", "hider_for_resid"]
    - path: "vmd/tests/test_registry.test"
      provides: "new tcltest cases for the resid-block API; all pre-existing cases still green"
  key_links:
    - from: "vmd/lib/game.tcl (17.2-09)"
      to: "vmd/lib/registry.tcl"
      via: "start_game registers the fake-resid→CA map after the index split; on_pick's _resolve_pick calls hider_for_resid when is_hider fails"
      pattern: "registry::(register_resid_block|hider_for_resid)"
---

<objective>
Add the pure resid-block lookup API (`register_resid_block` / `hider_for_resid`) to the registry, TDD-first.

Purpose: the cartoon splice registers ONE atom per hider (the fake CA, beta -999-only), but a real click on the cartoon bump can land on the residue's N/C/O/CB. The pick contract therefore needs a SECOND, resid-keyed lookup: clicked index → resid → registered hider index (the v2 analog of v1's `get_altconf_by_resv` dual lookup, keyed `resid ∈ registered block`). The map + lookup are pure → registry.tcl, TDD. game.tcl injects the mol-side resid read at pick time (17.2-09) — registry stays pure.
Output: extended `vmd/lib/registry.tcl` + `vmd/tests/test_registry.test`, all green.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/17.2-cartoon-newcartoon-generators/17.2-RESEARCH-cartoon-stride.md
@vmd/lib/registry.tcl
@vmd/tests/test_registry.test

Key facts (do NOT re-derive):
- registry.tcl is PURE (stdlib only; NO mol/atomselect/tk; sources NOTHING). Do not add any source line or viewer call.
- `_records` is a dict: index → `{rep status}`. Error-message convention: `error "hider $idx not registered"` (mark_found; 17.1-02's set_rep/assign_reps reuse it) — reuse verbatim.
- 17.1-02's PINNED contract: reconstruct_from_sentinels is UNCHANGED (clears + single-rep stamps; P8: never a reconstruct loop). This plan must NOT modify reconstruct_from_sentinels. `reset` (15-01) currently does `set _records [dict create]` — this plan EXTENDS reset with one added line (clear _resid_block); observable _records behavior unchanged, so phase15/16 smokes stay green.
- The resid domain: fake residue resids are 9001+k (17.2-01 RESID_BASE; single-atom simple-tier hiders all share resid 9001 — the collision is HARMLESS because on_pick consults is_hider FIRST (a single-atom hider click is registered) and the fallback only fires for non-registered atoms, whose only resid-block members are fake-residue N/C/O/CB. Real demo atoms have resid ≪ 9001. Document this in the proc comment.)
- Mixed rounds: TWO residue tiers in one round (e.g. Cartoon + Tube) get disjoint blocks via the dispatch's resid_start offset (17.2-09) — the registry just stores what it is given (one 9001+k → CA idx mapping per round, replaced wholesale).
- Tcl 8.5: `dict get` has NO 3-arg default form; use `dict exists` + `dict get`; `dict for` for iteration; brace all expr.
- tcltest runner (headless VMD): `timeout 300 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/tests/test_registry.test -eofexit < /dev/null' 2>&1 | grep -E "BCHM_TEST_RESULT|ERROR\)|bad switch"` — parse the marker, never $?; scan the full log.
</context>

<feature>
  <name>Registry resid-block API (register_resid_block + hider_for_resid)</name>
  <files>vmd/lib/registry.tcl, vmd/tests/test_registry.test</files>
  <behavior>
Namespace var: `_resid_block` (dict resid → idx, init `[dict create]`).

1. `register_resid_block {mapping}`:
   - mapping = dict resid → hider index (the dispatch builds it: fake residue k (resid 9001+k) → its registered CA index).
   - ATOMIC validate-then-replace: first `dict for` — EVERY idx must be a registered hider (`dict exists $_records $idx`), else `error "hider <idx> not registered"` BEFORE touching anything (no partial state).
   - All valid → `set _resid_block $mapping` (WHOLESALE REPLACE — never merged with a prior block; one call per round).
   - empty mapping → no-op (block unchanged).
2. `hider_for_resid {resid}`:
   - `_resid_block` has resid → return the idx.
   - else → return "" (NEVER an error — the miss path is a normal "Miss!" pick).
3. `reset` — extended: after the existing `set _records [dict create]`, add `set _resid_block [dict create]` (stale block must not survive cleanup into the next round).
4. `reconstruct_from_sentinels` — BYTE-UNCHANGED (P8/17.1-02 pin). Consequence pinned by test: a registered block SURVIVES a reconstruct call (the dispatch always re-registers after reconstruct in residue rounds; only reset and register_resid_block mutate the block).

RED test cases (append to test_registry.test in a `# ---- Phase 17.2: resid-block lookup ----` section before the marker block):
- register_and_lookup: reconstruct {5 6}; register_resid_block {9001 5 9002 6} → hider_for_resid 9001 == 5 AND hider_for_resid 9002 == 6.
- register_replace_wholesale: reconstruct {5 6}; register {9001 5}; register {9002 6} → hider_for_resid 9001 == "" AND hider_for_resid 9002 == 6 (replaced, not merged).
- register_unregistered_atomic: reconstruct {5}; catch register_resid_block {9001 5 9002 999} → error text exactly `hider 999 not registered` AND hider_for_resid 9001 == "" (validate-before-apply, no partial).
- register_empty_noop: reconstruct {5}; register_resid_block [dict create] → no error; hider_for_resid 9001 == "".
- register_nonexistent_records_ok_when_replaced_later: register on an EMPTY registry with empty mapping → fine (no-op); the validate loop runs zero times.
- lookup_unknown: hider_for_resid 42 → "" (string-empty, no error).
- lookup_after_reset: reconstruct {5}; register {9001 5}; reset → hider_for_resid 9001 == "".
- block_survives_reconstruct: reconstruct {5}; register {9001 5}; reconstruct_from_sentinels <fake fetch returning {5}> → hider_for_resid 9001 == 5 (pinned: only reset/register mutate the block).
- block_orthogonal_to_assign_reps: reconstruct {5}; register {9001 5}; assign_reps {5 Cartoon} → hider_for_resid 9001 == 5 AND remaining_by_rep == {Cartoon 1} (the two maps are independent).
- mixed_round_shape: reconstruct {7 8 9}; register {9001 8 9002 9} (hider 7 = a simple-tier atom, unregistered-in-block) → hider_for_resid 9001 == 8 (the harmless 9001 collision case documented in context).
   </behavior>
  <implementation>
In `vmd/lib/registry.tcl`:
- Add `variable _resid_block [dict create]` in the namespace eval (one `variable` per line — 14-04 lesson).
- Add `register_resid_block` and `hider_for_resid` to the `namespace export` line. New section header `# ---- Phase 17.2: resid-block lookup (multi-atom pick fallback, pure half) ----` near assign_reps. Proc comments must state: (a) the fallback purpose (click on fake GAM N/C/O/CB → resolve the registered CA; v2 analog of v1's get_altconf_by_resv); (b) the harmless 9001 collision rationale (is_hider consulted first; real resids ≪ 9001); (c) wholesale-replace + reset-clears + reconstruct-untouched lifecycle.
- register_resid_block: validate loop then `set _resid_block $mapping`. hider_for_resid: `dict exists` guard → `dict get`, else `return ""`.
- reset: add the ONE clear line + comment.
- NO other proc changes (reconstruct_from_sentinels byte-identical — verify with git diff).
Brace all expr; no 8.6 idioms.
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: RED — failing registry resid-block tests</name>
  <files>vmd/tests/test_registry.test</files>
  <action>
Append the RED cases from <behavior> as a new section before the marker block. Run the staged suite — the new tests MUST fail (invalid command name register_resid_block/hider_for_resid) while ALL pre-existing tests stay green.
Commit: `test(17.2-02): add failing registry resid-block lookup tests`
  </action>
  <verify>Staged run: BCHM_TEST_RESULT shows Failed ≥ 10 (the new cases) with all failures being the new cases; zero `bad switch` in the full log.</verify>
  <done>New failing cases exist and the pre-existing suite is untouched-green.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement register_resid_block + hider_for_resid</name>
  <files>vmd/lib/registry.tcl</files>
  <action>
Implement per <implementation> (atomic validate-then-replace; wholesale replace; reset clears; reconstruct untouched). Re-run the staged suite.
Commit: `feat(17.2-02): registry resid-block lookup API (multi-atom pick fallback, pure half)`
  </action>
  <verify>
```
tclsh vmd/lib/registry.tcl   # loads clean (pure layer)
timeout 300 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/tests/test_registry.test -eofexit < /dev/null' 2>&1 | grep -E "BCHM_TEST_RESULT|ERROR\)|bad switch"
```
BCHM_TEST_RESULT Failed=0 (ALL cases — old + new); zero `ERROR)` / `bad switch`; 8.6 gate zero on registry.tcl; `git diff vmd/lib/registry.tcl` shows reconstruct_from_sentinels byte-unchanged; registry.tcl still sources NOTHING (`grep -c "^source" vmd/lib/registry.tcl` → 0).
  </verify>
  <done>Registry has the resid-block API with the exact error contract + wholesale-replace lifecycle; the whole suite (old + new) is green and the module is still pure.</done>
</task>

</tasks>

<verification>
- BCHM_TEST_RESULT Failed=0 on the FULL suite (pre-existing + new), zero `ERROR)` / `bad switch` in the full log
- registry.tcl purity intact: no source lines, no mol/atomselect/tk
- reconstruct_from_sentinels byte-unchanged (P8/17.1-02 guard: `git diff` shows no changes to that proc)
- reset now clears _resid_block (the ONE behavioral extension, comment-documented)
</verification>

<success_criteria>
- game.tcl (17.2-09) can register a fake-resid→CA map once per round and resolve bump clicks on N/C/O/CB through hider_for_resid, with the lifecycle (replace/reset-clear/reconstruct-untouched) pinned by tests
</success_criteria>

<output>
After completion, create `.planning/phases/17.2-cartoon-newcartoon-generators/17.2-02-SUMMARY.md`
</output>
