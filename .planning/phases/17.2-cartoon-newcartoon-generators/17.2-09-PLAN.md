---
phase: 17.2
plan: 09
type: execute
wave: 4
depends_on: ["17.1-06", "17.2-02", "17.2-03", "17.2-04"]
files_modified: [vmd/lib/game.tcl, vmd/smoke/phase17_residue_dispatch_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "start_game's per-tier loop routes tier_kind 'residue' to make_residue_hiders (residue records accumulated SEPARATELY from simple records), runs ONE mutate call carrying BOTH record lists, and slices the fetch-hider index list by per-tier INDEX counts (a residue tier contributes 1 index per hider — its CA — not per record)"
    - "The dispatch registers the fake-resid→CA map ONCE via registry::register_resid_block after reconstruct_from_sentinels: residue tier k's record resids (9001+j, offset by prior residue records via make_residue_hiders' resid_start) zip with its index slice in file order"
    - "game::on_pick resolves multi-atom clicks: if is_hider fails, it reads the clicked atom's resid on the game molecule and consults registry::hider_for_resid — a hit re-targets the scoring to the registered CA (the v2 analog of v1's get_altconf_by_resv dual lookup); a miss stays a Miss!"
    - "Mixed rounds are correct end-to-end: VDW 2 + Cartoon 2 on 1znf → 436 atoms, 12 fake atoms (2 simple + 10 residue), exactly 4 beta<0 indices ({simple pair} + {CA pair}), user3 stamps 1|2, remaining_by_rep {VDW 2 Cartoon 2}, and finds register through BOTH the direct path (CA click) and the fallback path (N/C/O/CB click)"
    - "NEVER mol ssrecalc in the dispatch (one-line prohibition comment at the residue branch — Pitfall C2); NEVER tag_sentinels on residue rounds (mutate's 3-arg dispatch owns that)"
  artifacts:
    - path: "vmd/lib/game.tcl"
      provides: "residue-kind routing in start_game + _resolve_pick fallback in on_pick; game_state shape UNCHANGED (the registry holds the resid block)"
      exports: ["start_game", "cleanup", "restart", "on_pick", "set_callbacks"]
    - path: "vmd/smoke/phase17_residue_dispatch_smoke.tcl"
      provides: "mixed VDW+Cartoon round through the real dispatch: bookkeeping, resid-block lookups, fallback find via a direct on_pick call on a residue C atom, per-tier partitions, cleanup restore"
  key_links:
    - from: "vmd/lib/game.tcl (start_game)"
      to: "vmd/lib/mutation.tcl"
      via: "tier_kind 'residue' → mutation::make_residue_hiders; ONE mutation::mutate $molid $simple_records $residue_records"
      pattern: "mutation::(make_residue_hiders|mutate)"
    - from: "vmd/lib/game.tcl (start_game)"
      to: "vmd/lib/registry.tcl"
      via: "registry::register_resid_block $resid_map after reconstruct_from_sentinels + assign_reps"
      pattern: "registry::register_resid_block"
    - from: "vmd/lib/game.tcl (_resolve_pick)"
      to: "vmd/lib/registry.tcl"
      via: "atomselect resid read (game.tcl is the mol layer) → registry::hider_for_resid"
      pattern: "registry::hider_for_resid"
---

<objective>
Extend `game.tcl` into the residue-splice dispatch — residue-kind routing, index-count slicing, resid-block registration — and add the multi-atom pick fallback to `on_pick`, plus its headless dispatch smoke.

Purpose: this plan turns the widened seam (17.2-03) and the splice writer (17.2-04) into playable rounds. The 17.1-06 dispatch already handles per-tier placement + slicing; the residue kind adds three deltas: (1) a third generator branch accumulating residue records separately, (2) index-count slicing (records ≠ indices for residue tiers), (3) the resid→CA map registered for the pick fallback — because only the fake CA carries the beta sentinel, a real click on the cartoon bump can land on N/C/O/CB.
Output: extended `vmd/lib/game.tcl` + `vmd/smoke/phase17_residue_dispatch_smoke.tcl` PASS=1.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/17.2-cartoon-newcartoon-generators/17.2-RESEARCH-cartoon-stride.md
@.planning/phases/17.1-rep-setup-infrastructure-simple-rep-generators/17.1-06-PLAN.md
@vmd/lib/game.tcl
@vmd/lib/mutation.tcl
@vmd/lib/registry.tcl
@vmd/lib/rep_tiers.tcl
@vmd/smoke/phase17_dispatch_smoke.tcl

Key facts (do NOT re-derive):
- Pinned upstream APIs (landed in 17.1 + 17.2 waves 1-2; consume EXACTLY these):
  - `rep_tiers::tier_kind {rep}` → free | bonded | **residue** (Cartoon/NewCartoon/Trace/Tube — 17.2-03); `style_args` → bare single-word lists for the four; `tiers_from_per_rep` GAME_REPS-ordered; `effective_total` counts HIDERS (a residue hider = 1).
  - `mutation::make_residue_hiders {molid count {occupied_hiders {}} {resid_start {}}}` → `{chain resid atoms}` records (17.2-04); `mutate {molid hider_records {residue_records {}}}` → mixed tagging when residue_records non-empty; `fetch_hider_indices {molid}` UNCHANGED (`resname GAM and beta < 0` — returns the CA indices for residue hiders, file order == record order).
  - `registry::register_resid_block {mapping}` (atomic validate-then-REPLACE; errors `hider <idx> not registered`) + `hider_for_resid {resid}` → idx or "" (17.2-02).
- 17.1-06's dispatch shape to EXTEND (keep the guard/snapshot/resolve/warn/loop order): guard → snapshot → detected/resolve → tier table → per-tier placement loop → ONE mutate → backup::apply → stamp_tier_codes → add_hider_reps → registry reconstruct + assign_reps.
- **Slicing fix (records ≠ indices):** 17.1-06 slices the fetch list by cumulative per-tier RECORD counts. Residue tiers contribute 5 records per hider but 1 index per hider. Track a parallel per-tier ACTUAL INDEX-COUNT list instead: simple tier → llength(recs); residue tier → llength(rrecs) (1 CA per record). The fetch list order == file order == record order, and each tier's beta<0 indices are contiguous in that order — cumulative INDEX counts slice correctly.
- **resid_start threading:** residue tiers must not collide on the 9001 block when TWO residue tiers run in one round: track `resid_used` (sum of residue records so far); pass resid_start = RESID_BASE + resid_used (read `::biochemeleon::splice::RESID_BASE` fully-qualified at call time — game.tcl sources NOTHING; splice.tcl is loaded because mutation sources it). The harmless 9001 collision with simple-tier hiders is documented in 17.2-02 (is_hider is consulted first; real resids ≪ 9001).
- **resid_map build:** after the index split, for each residue tier: zip its record resids (each record's `[lindex $r 1]`, in record order) with its index slice (in file order) → one flat mapping; `registry::register_resid_block $resid_map` ONCE, AFTER reconstruct_from_sentinels (validation needs _records populated) and after assign_reps. Empty residue tiers → skip the call.
- **on_pick fallback (research §7.1):** restructure the scoring body: resolve the pick FIRST via a private `game::_resolve_pick {idx}` → (a) is_hider idx → idx; (b) else atomselect the game molecule `index $idx` → single-keyword `get resid` (lindex 0) → `registry::hider_for_resid` → resolved idx or "". Miss ("") → the existing miss log. The three-way guard (already-found / hidden) then runs on the RESOLVED index; mark_found_visual also on the resolved index (1 green sphere at the CA — research §7.2's CA-only design; the 5-sphere polish is explicitly NOT built). The whole body stays inside the existing catch with `rc == 1` handling.
- P9: game_state hider_count = effective_total (residue hiders count 1 — effective_total already correct). P8: ONE reconstruct + ONE assign_reps (register_resid_block is a THIRD call, additive — never a reconstruct loop).
- game_state shape UNCHANGED ({game_molid hider_count snapshot per_rep}) — the registry holds the resid block; on_pick reads it via hider_for_resid.
- Order is NON-NEGOTIABLE (17.1-06): stamp_tier_codes BEFORE add_hider_reps; backup::apply before both; reconstruct before assign_reps/register.
- DI rule (lesson a): `[list apply {{molid} {...}} $game_molid]` command-prefix VALUE — never bare `[apply ...]`.
- Tcl 8.5; game.tcl sources NOTHING (call-time resolution); one variable per line; brace expr; every atomselect deleted; NO entry-file changes (splice.tcl is sourced by mutation; rep_tiers already sourced by 17.1-06).
- Smoke harness per 17.1-06/17.2-04 conventions (source order includes splice via mutation; registry EXACTLY ONCE; marker + exit + full-log scan).
- The 16-13 active-game guard stays byte-identical; cleanup/restart unchanged.
- THIS PLAN'S SMOKE: game_logic sourced with NO callbacks registered → on_pick's `catch {{*}$cb ...}` paths are harmless no-ops (16-11 precedent); the win path (finish_win) runs headless.
</context>

<tasks>

<task type="auto">
  <name>Task 1: start_game residue-kind routing + index-count slicing + resid-block registration</name>
  <files>vmd/lib/game.tcl</files>
  <action>
In `vmd/lib/game.tcl` (keep the 16-13 guard + steps a-f of 17.1-06's shape; extend the loop):
1. Per-tier placement loop: `set kind [::biochemeleon::rep_tiers::tier_kind $style]`; branch:
   - "free" → make_placeholder_hiders (unchanged);
   - "bonded" → make_bonded_hiders (unchanged);
   - "residue" → `set rrecs [::biochemeleon::mutation::make_residue_hiders $molid $count $occ_hiders $resid_start]` where `$resid_start` = RESID_BASE + resid_used (track `set resid_used 0` before the loop; after each residue tier `incr resid_used [llength $rrecs]`; RESID_BASE via the fully-qualified `::biochemeleon::splice::RESID_BASE` read with a one-line comment — NEVER `mol ssrecalc` in this flow, Pitfall C2). Accumulate rrecs into `$residue_records` (separate list) and `$occ_hiders` gains each record's CA position (atoms[1]'s x y z). Simple kinds keep appending to `$records` and `$occ_hiders` as in 17.1-06.
   - Track per-tier ACTUAL INDEX counts: `lappend tier_idx_counts [llength $recs]` (simple) / `[llength $rrecs]` (residue); ALSO keep each residue tier's record list (for the resid zip) in a parallel `residue_tiers` list.
2. ONE mutate: `set game_molid [::biochemeleon::mutation::mutate $molid $records $residue_records]`.
3. backup::apply + fetch (fetch_hider_indices once — same selector as the DI lambda) + split: slice `$idxs` by CUMULATIVE `tier_idx_counts` (replacing 17.1-06's record-count slicing).
4. After assign_reps: build `$resid_map` — for each residue tier (parallel lists: its records, its index slice): `set rid [lindex [lindex $r 1] ...]` zip with the slice index at the same position; `if {[llength $resid_map] > 0} { ::biochemeleon::registry::register_resid_block $resid_map }`.
5. stamp_tier_codes / add_hider_reps / assign_reps per 17.1-06 (stamp BEFORE add; specs from style_args — the four tiers yield bare single-word specs).
6. Update the proc doc comment (residue kind; index-count slicing; resid-block registration; game_state shape unchanged).
7. cleanup/restart/set_callbacks UNCHANGED. Tcl 8.5; brace expr; no 8.6 idioms.
  </action>
  <verify>`grep -n "make_residue_hiders" vmd/lib/game.tcl` shows the residue branch; `grep -c "register_resid_block" vmd/lib/game.tcl` → 1; `grep -c "reconstruct_from_sentinels" vmd/lib/game.tcl` → 1 (P8); `grep -c "mol ssrecalc" vmd/lib/game.tcl` → 0; 8.6 gate zero; `grep -c "list apply" vmd/lib/game.tcl` unchanged (DI prefix form intact); the 16-13 guard block byte-identical (git diff scope).</verify>
  <done>start_game routes residue tiers through make_residue_hiders with resid_start offsetting, ONE mixed mutate, index-count slicing, and one register_resid_block call; simple-tier behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: on_pick resid-block fallback (_resolve_pick)</name>
  <files>vmd/lib/game.tcl</files>
  <action>
1. NEW private proc `::biochemeleon::game::_resolve_pick {idx}` → the registered hider index or "":
   - `if {[::biochemeleon::registry::is_hider $idx]} { return $idx }`
   - stash guard: current_state empty → return "".
   - `set sel [atomselect [dict get $current_state game_molid] "index $idx"]`; `set rid [lindex [$sel get resid] 0]`; `$sel delete` (single-keyword get).
   - `set resolved [::biochemeleon::registry::hider_for_resid $rid]`; return $resolved ("" on miss).
   Comment: the v2 analog of v1's get_altconf_by_resv dual lookup — only the fake CA carries the beta sentinel, so a cartoon-bump click can deliver N/C/O/CB; the resid block resolves it to the registered CA (research §7.1).
2. Restructure `on_pick`'s scoring body (INSIDE the existing catch, rc==1 handling unchanged): after the stash guard, `set hit_idx [::biochemeleon::game::_resolve_pick $idx]`; if "" → the miss log path (unchanged wording); the already-found check → `status_of $hit_idx`; the found flow → mark_found_visual on $hit_idx + registry::mark_found $hit_idx + remaining + win check (all on hit_idx). State gate FIRST (unchanged). The direct-hit path (is_hider true) must behave byte-compatibly with Phase 16 (miss indices that are real atoms still miss — resid ≪ 9001 never in the block).
3. Doc comments updated (the fallback + CA-only marking rationale).
  </action>
  <verify>`grep -n "_resolve_pick" vmd/lib/game.tcl` shows the proc + the on_pick call; `grep -c "hider_for_resid" vmd/lib/game.tcl` → 1; 8.6 gate zero; the three-way guard order preserved (state gate → stash → resolve → already-found → found flow); `grep -c "atomselect" vmd/lib/game.tcl` grew by exactly the _resolve_pick selection (every selection deleted — `grep -c '\$sel delete'` grew by 1).</verify>
  <done>on_pick resolves bump clicks on fake-residue N/C/O/CB to the registered CA and scores it; misses stay misses; the Phase-16 direct path is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Mixed VDW+Cartoon dispatch smoke (direct + fallback finds)</name>
  <files>vmd/smoke/phase17_residue_dispatch_smoke.tcl</files>
  <action>
Harness per context (17.1-06 dispatch-smoke shape). Demo 1znf (424 atoms), capture pre_start numreps:
1. Mixed round: `set gs [game::start_game $molid 4 [dict create VDW 2 Cartoon 2] 0]` → gm atoms 436 (2 simple + 10 residue); `dict get $gs hider_count` == 4; per_rep == {VDW 2 Cartoon 2}.
2. Index layout: `mutation::fetch_hider_indices $gm` == {424 425 427 432} (VDW records first: 424-425; Cartoon residue 1: 426=N 427=CA 428=C 429=O 430=CB; residue 2: 431-435, CA 432) — derive the expectation from rep_tiers::tiers_from_per_rep ({1 VDW 2} {2 Cartoon 2}) + the 5-atoms-per-record layout, then assert; user3 ≈ 1.0 on {424 425}, ≈ 2.0 on {427 432}.
3. Resid block: `registry::hider_for_resid 9001` == 427; `hider_for_resid 9002` == 432; `hider_for_resid 9999` == ""; `registry::count_hiders` == 4; `remaining_by_rep` has VDW 2 + Cartoon 2.
4. Fake-residue facts: `resname GAM` num == 12; the 4 beta<0 indices' names all CA; CA structure == T; fake chains != "G"; segid GAME on all 12; numreps == pre_start + 4 (2 pairs); rep read-back via tier_reps + repindex: VDW pair style `VDW` + selections `... user3 1`, Cartoon pair style `Cartoon` + `... user3 2`.
5. Find-through-BOTH-paths: `game::on_pick 428` (residue-1's C atom — NOT registered) → fallback resolves 427: user2(427) > 0, registry remaining 3, remaining_by_rep {VDW 2 Cartoon 1}; `game::on_pick 424` (direct VDW) → remaining 2; `game::on_pick 425` → remaining 1; `game::on_pick 433` (residue-2's C atom — NOT registered) → fallback resolves 432 → remaining 0 → WIN: game_logic state == won, finish_win fired (timer frozen — timer_elapsed returns the frozen value), win line logged via a list-appending callback registered with set_callbacks BEFORE the finds; assert the log contains a "found" line for the fallback find and the win line mentions 4 hiders.
6. Double-find guard through the fallback: `game::on_pick 426` (residue-1's N — its CA 427 already found) → "Already found!" log line, remaining still 0.
7. Cleanup: `game::cleanup $gs` → registry count 0 AND `hider_for_resid 9001` == "" (reset cleared the block); gm dead; restored original 424 atoms.
8. Marker + exit.
  </action>
  <verify>Staged run: `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log.</verify>
  <done>Mixed residue+simple dispatch, resid-block registration, fallback + direct find paths, double-find guard, win flow, and block-clearing cleanup all proven headlessly through the public surface.</done>
</task>

</tasks>

<verification>
- `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log
- Tcl 8.6 gate zero on both touched files
- P8/P9 honored (one reconstruct; effective total); DI prefix form intact; the 16-13 guard + cleanup byte-identical
- `grep -c "mol ssrecalc" vmd/lib/game.tcl` → 0; the residue branch carries the prohibition comment
- No entry-file changes (biochemeleon.tcl untouched)
</verification>

<success_criteria>
- ROADMAP SC3's headless half: a game round with cartoon-blend hiders runs through the REAL dispatch — generate (mixed VDW+Cartoon) → click-to-find (direct CA + fallback N/C/O/CB) → win
- The resid-block fallback is registered, consulted, and lifecycle-cleaned (reset clears) with the simple-tier Phase-16 behavior regression-safe
</success_criteria>

<output>
After completion, create `.planning/phases/17.2-cartoon-newcartoon-generators/17.2-09-SUMMARY.md`
</output>
