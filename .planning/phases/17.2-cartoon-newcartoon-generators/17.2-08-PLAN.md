---
phase: 17.2
plan: 08
type: execute
wave: 3
depends_on: ["17.1-08", "17.2-04"]
files_modified: [vmd/smoke/phase17_tube_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "A Tube splice renders CONNECTED under the Tube rep: the GAME-only exclusive render draws FCylinder spline segments (> 0 — the smooth CA spline covers the fake residue) and the anchor-window render EXCEEDS the unspliced control's (the fake CA is IN the spline path)"
    - "The hider rep for the Tube tier is created with the bare style `Tube`, Element coloring, and the user3-conjoined sentinel selections; read-back string-compares to `Tube` exactly"
    - "Tube is SS-INDEPENDENT (UG node66) — the smooth spline through ALL CA positions includes the fake CA without any ss involvement (the Option-A 'no sidestep needed' evidence for the SS-independent half of the family)"
    - "The four residue tiers are proven to consume the IDENTICAL splice: this smoke's splice/registry/rep asserts are structurally the same as 17.2-05/06/07 with only the style name and primitive expectation differing"
  artifacts:
    - path: "vmd/smoke/phase17_tube_smoke.tcl"
      provides: "Tube-tier end-to-end proof (spline bits + path inclusion + sentinel/registry surface + found-marking + restore) — completes the four-rep family"
  key_links:
    - from: "vmd/smoke/phase17_tube_smoke.tcl"
      to: "vmd/lib/mutation.tcl"
      via: "make_residue_hiders + mutate (the 17.2-04 splice flow, driven directly — the dispatch is 17.2-09)"
      pattern: "mutation::(make_residue_hiders|mutate)"
    - from: "vmd/smoke/phase17_tube_smoke.tcl"
      to: "render Tachyon"
      via: "the 17.2-04 render-diff helpers; Tube = FCylinder spline segments"
      pattern: "render Tachyon"
---

<objective>
Prove the Tube tier end-to-end headlessly — the smooth-spline residue-splice consumer, completing the four-rep family.

Purpose: Tube draws a smooth spline through ALL protein CAs; research measured 6 FCyl per GAME-only fake residue and 30 in a 5-unit window (probe5b/probe6). This is the last of the four sibling consumers — same splice, same sentinel surface, only the style name differs. Drives mutation + hiders directly (dispatch = 17.2-09).
Output: `vmd/smoke/phase17_tube_smoke.tcl` PASS=1.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/17.2-cartoon-newcartoon-generators/17.2-RESEARCH-cartoon-stride.md
@vmd/smoke/phase17_splice_smoke.tcl
@vmd/smoke/phase17_cartoon_smoke.tcl

Key facts (do NOT re-derive):
- Probe-evidence targets (17.2-RESEARCH §2.3/§4.3): GAME-only Tube on ONE spliced residue → 6 FCylinder (the spline covers it); window `resid <a-2> to <a+2>` → 30 FCyl (5-unit) — assert GAME-only FCyl ≥ 1 and spliced-window FCyl > control-window FCyl.
- Tube param form: bare `Tube` (0 or 2 params valid; the game uses bare — read-back `Tube` exactly).
- SS-independence: Tube never consults `structure`; the fake CA's structure == T is recorded (the load-time STRIDE fact) but the render assertions are purely geometric.
- Anchor-window derivation + splice facts + hider-rep contracts + render helpers + harness/staging: IDENTICAL to 17.2-05 (copy that smoke; per-tier deltas: style `Tube`; GAME-only assert FCyl ≥ 1 + TriStrip == 0; window assert spliced FCyl > control FCyl).
- THIS PLAN EDITS NO LIB FILES (defects → SUMMARY for a gap plan).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write + run the Tube-tier end-to-end smoke</name>
  <files>vmd/smoke/phase17_tube_smoke.tcl</files>
  <action>
Copy the 17.2-05 harness/render helpers; adapt to Tube. Demo 1znf (424 atoms), driven directly:
1. Splice: `set recs [mutation::make_residue_hiders $m0 3]` → `mutate $m0b [list] $recs` → gm atoms 439; `resname GAM and beta < 0` num == 3 (names CA, resids {9001 9002 9003}, structure == T recorded); `resname GAM` num == 15; segid GAME; junction bonds ≥ 1 each side.
2. Hider reps: stamp_tier_codes (code 1 = the 3 CA indices) → add_hider_reps {{1 Tube}} → numreps == pre_start + 2; read-back style == `Tube` EXACTLY, Element / ColorID 7, selections `resname GAM and beta < 0 and user2 < 1 and user3 1` (+ the found variant).
3. Tachyon renders (probe rep last; baseline-zero first):
   a. GAME-only exclusive (selection `segid GAME`, style Tube, color Element): FCylinder count ≥ 3 (spline segments over the fake residues — research measured 6/residue) AND TriStrip count == 0 (Tube draws no mesh).
   b. Window path-inclusion: anchor resid a via the recipe; probe `resid <a-2> to <a+2>` style Tube → spliced_win.dat vs fresh-original control_win.dat → assert spliced FCyl > control FCyl (the spline passes through the fake CA — probe6's 30-vs-control evidence class).
   c. No salmon in any parsed Color.
   Delete the probe rep after renders.
4. Found-marking: mark_found_visual on CA 1 → user2 > 0; found-selection num 1 / hidden 2 (user3 1 conjunct).
5. Cleanup/restore: mol delete gm; reload original → 424 atoms; marker + exit.
  </action>
  <verify>Staged run: `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log; GAME-only render FCyl ≥ 3 and TriStrip == 0; window spliced > control.</verify>
  <done>Tube tier proven: spline bits render over the fake residues, path inclusion vs control, exact read-back, per-tier partitions, clean restore — the four-rep family complete on ONE splice.</done>
</task>

</tasks>

<verification>
- `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log
- GAME-only Tube render: FCyl ≥ 3 + TriStrip == 0 + no salmon
- Window render: spliced > unspliced control (path inclusion)
- Tcl 8.6 gate zero; no lib files touched
</verification>

<success_criteria>
- All four trace-family reps (Cartoon/NewCartoon/Trace/Tube) are proven to render the SAME splice connected — HIDER-05's "one shared splice mechanism, four consumers" shape, headlessly
</success_criteria>

<output>
After completion, create `.planning/phases/17.2-cartoon-newcartoon-generators/17.2-08-SUMMARY.md`
</output>
