---
phase: 17.2
plan: 10
type: execute
wave: 5
depends_on: ["17.2-09"]
files_modified: [vmd/smoke/phase17_e2e_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "A cartoon-only round (Cartoon 2 + Trace 1) through start_game is PLAYABLE end-to-end headlessly: generate → find all 3 (one direct CA click, two fallback N/C/O/CB clicks) → win (state won, timer frozen, win line logged, win_cb fired with elapsed + 3) → cleanup restores the original"
    - "A randomize round (2-arg start_game) with an arbitrary IMPLEMENTED_TIERS distribution produces the EXACT predicted atom count: orig + (simple hiders × 1) + (residue hiders × 5) — the record layout is deterministic given the distribution"
    - "The win flow is driven purely through the public surface (start_game + on_pick) with no test hooks into game.tcl/registry internals"
    - "Every round leaves no residue leak: cleanup → registry 0, block cleared, game molid dead, restored molecule atom count == original"
  artifacts:
    - path: "vmd/smoke/phase17_e2e_smoke.tcl"
      provides: "the phase's SC3 acceptance smoke: playable cartoon-blend rounds through the Phase-16 core loop (start_game → on_pick → win), plus the randomize-atom-count invariant"
  key_links:
    - from: "vmd/smoke/phase17_e2e_smoke.tcl"
      to: "vmd/lib/game.tcl"
      via: "start_game + on_pick ONLY (the public surface; on_pick called directly with atom indices — 16-11 capstone precedent)"
      pattern: "game::(start_game|on_pick|cleanup)"
---

<objective>
Prove ROADMAP SC3 headlessly: a game round with cartoon-blend hiders is playable end-to-end (generate → click-to-find → win) through the public surface.

Purpose: 17.2-09 proved the dispatch bookkeeping and both find paths on a mixed round; this plan proves the PLAYER story — a cartoon round won through the Phase-16 core loop (countdown/timer state machine, three-way pick guard, win box data, cleanup), plus the randomize-round atom-count invariant that follows from the record layout. Headless only (picks fired by direct on_pick calls — the C-side pick path is the GUI checkpoint's, 17.2-12).
Output: `vmd/smoke/phase17_e2e_smoke.tcl` PASS=1.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/17.2-cartoon-newcartoon-generators/17.2-RESEARCH-cartoon-stride.md
@vmd/smoke/phase17_residue_dispatch_smoke.tcl
@vmd/smoke/phase16_restart_smoke.tcl
@vmd/lib/game.tcl

Key facts (do NOT re-derive):
- Public-surface contracts: `game::start_game {molid hider_count {per_rep {}} {lock_scene 0}}`; `game::on_pick {idx}` (direct calls are the 16-11 headless precedent — text mode cannot fire real picks); `game::set_callbacks {log_cb remaining_cb win_cb}` (command prefixes; list-appending lambdas in the smoke); `game::cleanup {game_state}` → restored molid.
- Index layout math (for the find plan + atom-count invariant): records concatenate in GAME_REPS order; simple records = 1 atom (chain G, resid 9001); residue records = 5 atoms (chain = anchor's, resid 9001+k), atom order N/CA/C/O/CB → CA = block start + 1. Fake atoms start at orig_n. For 1znf (424) with per_rep {Cartoon 2 Trace 1}: game molecule 424 + 3×5 = 439; fake atoms 424..438; CAs at 425, 430, 435 (block starts 424, 429, 434); fetch indices {425 430 435}; residue N/C/O/CB indices are everything else in 424..438.
- Find plan for the win: pick CA 425 DIRECT (is_hider path), then 429 (residue-2's N — fallback → 430), then 436 (residue-3's C — fallback → 435); remaining 3→2→1→0; the LAST find triggers finish_win + win_cb(elapsed, 3).
- Win assertions: game_logic::state == "won"; timer_elapsed returns the FROZEN value (stays constant across two reads); the win log line (via a list-appending log_cb) contains "found all 3"; win_cb received 2 args (elapsed, 3) — capture via a list-appending win_cb.
- Randomize invariant: for ANY distribution from a 2-arg round, atoms == orig + Σ(simple tiers' counts × 1) + Σ(residue tiers' counts × 5), where the tier kind comes from rep_tiers::tier_kind and the counts from `dict get $gs per_rep`. Assert against the ACTUAL game molecule.
- game_logic sourced (the state machine) with NO GUI; callbacks registered via set_callbacks before the finds. game::start_game's countdown is NOT started by on_pick — state gate: on_pick is a no-op unless state == "playing" → the smoke must call `::biochemeleon::game_logic::begin_play` (the VERIFIED proc name — game_logic.tcl exports begin_play and it is the transition the Game tab's GO button drives; it requires the countdown finished at GO and calls timer_start) after start_game, BEFORE the finds.
- Harness per 17.2-09 conventions (source order incl. game_logic BEFORE game; registry EXACTLY ONCE; marker + exit + full-log scan; staging `cp -r vmd tmp/biochemeleon-vmd/`).
- THIS PLAN EDITS NO LIB FILES (defects → SUMMARY for a gap plan).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write + run the end-to-end playable-round smoke</name>
  <files>vmd/smoke/phase17_e2e_smoke.tcl</files>
  <action>
Harness per context. Demo 1znf (424 atoms). Register list-appending callbacks via set_callbacks BEFORE any round (log lines / remaining pulls / win args captured into smoke lists).

A. CARTOON ROUND (the SC3 story):
1. `set gs [game::start_game $molid 3 [dict create Cartoon 2 Trace 1] 0]` → gm atoms 439; hider_count 3; remaining_by_rep {Cartoon 2 Trace 1}; fetch indices {425 430 435} (derive + assert); registry count 3.
2. Transition to playing (the exact game_logic proc the Game tab calls — inspect and pin); assert state == "playing".
3. Find all three: `game::on_pick 425` (direct CA) → remaining 2; `game::on_pick 429` (fallback → 430) → remaining 1 + user2(430) > 0 + user2(429) == 0 (the FALLBACK marks the CA, never the clicked atom); `game::on_pick 436` (fallback → 435) → remaining 0 → WIN: state == "won"; timer frozen (two timer_elapsed reads equal); log list contains a found line + a win line with "3"; win_cb captured exactly one {elapsed 3} pair.
4. Post-win pick guard: `game::on_pick 424` (unfound-residue atom) → NO state change, NO extra log (the state gate + already-found guard hold after a win).
5. `game::cleanup $gs` → registry 0; hider_for_resid 9001 == ""; gm dead; restored original 424 atoms; state reset (a fresh start_game works — covered in B).

B. RANDOMIZE INVARIANT ROUND:
6. Fresh load 1znf; `set gs2 [game::start_game $m2 5]` (2-arg) → per_rep from `dict get $gs2 per_rep`; compute the predicted atom count: 424 + Σ over per_rep (tier_kind free|bonded → count × 1; residue → count × 5); assert `molinfo <gm2> get numatoms` == predicted EXACTLY; assert every remaining_by_rep key ∈ GAME_REPS; registry count == 5.
7. One find through whichever path applies (if the distribution has residue tiers, pick a residue N index → fallback; else pick a registered index) → remaining 4. (Distribution-dependent — derive the index from the per_rep layout at runtime, do not hard-code.)
8. `game::cleanup $gs2` → registry 0; block cleared; restored 424 atoms.
9. Marker + exit.
  </action>
  <verify>Staged run: `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log; the win assertions (state won, frozen timer, win_cb {elapsed 3}) all pass.</verify>
  <done>SC3 proven headlessly: cartoon-blend rounds are playable end-to-end through start_game + on_pick (direct + fallback finds → win → clean restore), and the randomize atom-count invariant pins the record layout.</done>
</task>

</tasks>

<verification>
- `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log
- Tcl 8.6 gate zero; no lib files touched
- The win flow, state gate, and double-count guards all exercised through the public surface only
</verification>

<success_criteria>
- ROADMAP SC3 (headless half): "A game round with cartoon-blend hiders is playable end-to-end (generate → click-to-find → win) using the Phase 16 core loop" — proven without touching lib internals
</success_criteria>

<output>
After completion, create `.planning/phases/17.2-cartoon-newcartoon-generators/17.2-10-SUMMARY.md`
</output>
