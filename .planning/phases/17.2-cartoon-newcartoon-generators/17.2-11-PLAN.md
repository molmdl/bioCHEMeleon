---
phase: 17.2
plan: 11
type: execute
wave: 6
depends_on: ["17.1-13", "17.2-10"]
files_modified: [vmd/smoke/phase17_capstone_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "The 17.1-13 capstone's under-generation case is REWRITTEN for the widened seam: {Ribbons 2 VDW 1} (Ribbons = valid VMD style, NOT in GAME_REPS) → Ribbons dropped with the vmdcon warning, registry count 1, remaining_by_rep {VDW 1} — the old {Cartoon 2 VDW 1} case now GENERATES (Cartoon is implemented) and was asserting the pre-17.2 seam"
    - "NEW mixed residue round: VDW 2 + Cartoon 2 through start_game → exact bookkeeping (436 atoms, 4 beta<0 indices, remaining_by_rep {VDW 2 Cartoon 2}, resid block 9001→427 / 9002→432, one fallback find)"
    - "NEW lock-scene CARTOON round: scene pre-styled Cartoon + lock_scene 1 → ALL hiders derive onto the detected residue tier (remaining_by_rep == {Cartoon n}, resids 9001+k, CA-only sentinels, numreps pre+2, snapshot-faithful restore)"
    - "FULL-SUITE green gate: every tcltest suite (incl. the new test_splice) Failed=0 and every smoke PASS=1 on fresh staging, zero ERROR)/bad switch anywhere, 8.6 + grab-set gates zero"
    - "The ss decision is durably recorded in code: splice.tcl's header carries the Option-A decision block and `grep -rn 'mol ssrecalc' vmd/lib/` finds ZERO non-comment call sites"
  artifacts:
    - path: "vmd/smoke/phase17_capstone_smoke.tcl"
      provides: "the 17.2 capstone: updated under-generation case + mixed residue round + lock-scene Cartoon round + the full-suite green-gate log"
  key_links:
    - from: "vmd/smoke/phase17_capstone_smoke.tcl"
      to: "vmd/lib/{game,rep_tiers,registry,mutation}.tcl"
      via: "mixed + lock-scene residue rounds through start_game; under-generation via a non-GAME_REPS style"
      pattern: "start_game"
---

<objective>
Capstone: rewrite the widened-seam-broken under-generation case, add the mixed + lock-scene Cartoon rounds, and run the FULL-SUITE green gate (17.1 suite stays green + 8.6 gate + the ss-decision recording check).

Purpose: the per-tier smokes (17.2-05..08) prove single reps and 17.2-09/10 prove the dispatch; the capstone proves the phase's actual acceptance shape — residue tiers MIXING with simple tiers in one round, the lock-scene derivation landing on a residue tier, and under-generation tolerance — then declares the whole headless surface green before the GUI session. This plan also owns the ONE 17.1 smoke broken by the seam widening (17.2-03's KNOWN RED).
Output: updated `vmd/smoke/phase17_capstone_smoke.tcl` PASS=1 + the full-suite results table in the SUMMARY.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/17.2-cartoon-newcartoon-generators/17.2-RESEARCH-cartoon-stride.md
@vmd/smoke/phase17_capstone_smoke.tcl
@vmd/smoke/phase17_residue_dispatch_smoke.tcl

Key facts (do NOT re-derive):
- 17.1-13's capstone (this file) has sections: A mixed simple round {VDW 2 Lines 2 Licorice 1} (KEEP — still valid), B lock-scene Licorice (KEEP), C under-generation `{Cartoon 2 VDW 1}` → Cartoon dropped (BROKEN by 17.2-03 — Cartoon now implements + generates). Rewrite C with `{Ribbons 2 VDW 1}`: Ribbons is a VALID VMD style but NOT in GAME_REPS → resolve_per_rep drops it (non-GAME_REPS keys), game.tcl's warning loop fires (tier_kind "" → vmdcon -warn), registry count == 1, remaining_by_rep == {VDW 1}, game_state hider_count == 1 (P9 effective total).
- Lock-scene Cartoon recipe (17.1-13's B pattern + residue): fresh load → `mol modstyle 0 $m Cartoon` (canonical spelling from GAME_REPS) → `start_game $m n {} 1` → scene_reps_to_per_rep detects {Cartoon 1} → resolve restricts to detected ∩ IMPLEMENTED_TIERS = {Cartoon} → ALL n hiders on Cartoon → make_residue_hiders × n → remaining_by_rep == {Cartoon n}; resids {9001..9000+n}; CA-only sentinels (n beta<0 indices, names CA); numreps == pre + 2; cleanup → the restored original's rep-0 style still reads `Cartoon` (snapshot fidelity).
- Mixed residue round: per 17.2-09's smoke (that smoke OWNS the deep asserts; the capstone re-proves the composition at capstone level: atoms, remaining_by_rep, resid lookups, ONE fallback find).
- Full-suite gate = run EVERY suite/smoke staged, one command each, grep each log for its marker AND `ERROR)`/`bad switch`:
  - tcltest (6): test_setup_state, test_registry, test_generators, test_rep_tiers, test_game_logic, **test_splice (NEW — 17.2-01)** → each `BCHM_TEST_RESULT ... Failed=0`.
  - smokes (31): phase13_smoke, phase14_gui_smoke, phase14_mol_smoke, phase15_smoke, phase15_backup_smoke, phase15_game_smoke, phase15_mutation_smoke, phase16_entry_smoke, phase16_gametab_smoke, phase16_hiders_smoke, phase16_onpick_smoke, phase16_pick_smoke, phase16_placement_smoke, phase16_restart_smoke, phase16_smoke, phase17_tiers_smoke, phase17_bonded_smoke, phase17_dispatch_smoke, phase17_points_smoke, phase17_lines_smoke, phase17_licorice_smoke, phase17_cpk_smoke, phase17_dynbonds_smoke, phase17_capstone_smoke, phase17_splice_smoke, phase17_cartoon_smoke, phase17_newcartoon_smoke, phase17_trace_smoke, phase17_tube_smoke, phase17_residue_dispatch_smoke, phase17_e2e_smoke → each `BCHM_SMOKE_RESULT PASS=1 FAIL=none`.
  - Gates: `grep -rnE "\blmap\b|\btry\b|\bthrow\b|\btailcall\b|\bcoroutine\b|\byield\b|\bfinally\b" vmd/lib/ vmd/gui/` → zero; `grep -rnE "grab set" vmd/gui/` → zero.
  - Decision-record gate: `grep -n "ssrecalc" vmd/lib/splice.tcl` matches the prohibition comment (Option A recorded); `grep -rn "mol ssrecalc" vmd/lib/ | grep -v "^\S*:\s*#"` → ZERO (no live call sites anywhere in the lib layer).
- If ANY suite/smoke is red: diagnose; fix ONLY within this plan's file scope (phase17_capstone_smoke.tcl); otherwise record the defect in the SUMMARY for a gap-closure plan (do NOT touch other files).
- Harness/staging conventions per 17.2-04; staging `mkdir -p tmp/biochemeleon-vmd && cp -r vmd tmp/biochemeleon-vmd/`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capstone smoke — seam repair + mixed residue + lock-scene Cartoon rounds</name>
  <files>vmd/smoke/phase17_capstone_smoke.tcl</files>
  <action>
Edit `vmd/smoke/phase17_capstone_smoke.tcl` (17.1-13's file):
1. Section C (under-generation): change the round to `start_game $m3 3 [dict create Ribbons 2 VDW 1] 0`; update the asserts: the log contains the Ribbons drop warning (a vmdcon -warn, NOT an `ERROR)`); registry count == 1; remaining_by_rep == {VDW 1}; game_state hider_count == 1; cleanup. Add a comment: the 17.1 case used Cartoon-then-unimplemented; Cartoon implements in 17.2, so the unimplemented-style case moved to Ribbons (valid style, excluded from GAME_REPS — viability §5b).
2. NEW section D (mixed residue): fresh load 1znf → `start_game $m4 4 [dict create VDW 2 Cartoon 2] 0` → assert: gm atoms 436; beta<0 indices == 4 (2 simple + 2 CA); remaining_by_rep {VDW 2 Cartoon 2}; `registry::hider_for_resid 9001` == the first Cartoon CA and 9002 == the second (derive from the tier table order); ONE fallback find: `game::on_pick <residue-1 C index>` (index 428 per the layout — derive, don't blind-hard-code) → remaining 3, remaining_by_rep {VDW 2 Cartoon 1}; transition to playing first (state gate — same proc as 17.2-10 pins); cleanup → registry 0 + block cleared + restored 424.
3. NEW section E (lock-scene Cartoon): fresh load → `mol modstyle 0 $m5 Cartoon` → `start_game $m5 3 {} 1` → assert: remaining_by_rep == {Cartoon 3} (ALL hiders on the detected residue tier); beta<0 num == 3 (names CA); resids {9001 9002 9003}; numreps == pre_start + 2; cleanup → the restored original's rep-0 style reads `Cartoon` (snapshot fidelity); registry 0.
4. Keep sections A/B byte-identical (mixed simple + lock-scene Licorice still valid). Marker + exit.
  </action>
  <verify>Staged run: `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch` in the full log (the Ribbons warning is a warn line, not an error).</verify>
  <done>Capstone green: under-generation repaired for the widened seam, mixed residue bookkeeping + lock-scene Cartoon derivation + snapshot-faithful restore proven at capstone level.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite green gate + ss-decision recording check</name>
  <files>.planning/phases/17.2-cartoon-newcartoon-generators/17.2-11-SUMMARY.md</files>
  <action>
Run EVERY suite + smoke staged (the full list in context — 6 tcltest + 31 smokes), one command per file, and record a results table in the SUMMARY. Then the gates:
- 8.6 idiom gate zero on vmd/lib/ + vmd/gui/; grab-set gate zero on vmd/gui/.
- Decision-record gate: splice.tcl header contains the Option-A ssrecalc prohibition; ZERO non-comment `mol ssrecalc` matches under vmd/lib/.
If ANY run is red: diagnose; fix ONLY within phase17_capstone_smoke.tcl scope; otherwise record the defect (repo + failing assert) in the SUMMARY for a gap-closure plan.
  </action>
  <verify>The recorded table shows every suite Failed=0 and every smoke PASS=1; both grep gates zero; the ssrecalc decision-record gate passes.</verify>
  <done>The entire 17.1+17.2 headless surface is green on fresh staging and the ss decision is durably recorded in code.</done>
</task>

</tasks>

<verification>
- Capstone smoke PASS=1 (repaired C + mixed residue D + lock-scene Cartoon E), zero `ERROR)` / `bad switch`
- Full-suite table: all tcltest Failed=0; all smokes PASS=1; 8.6 + grab-set gates zero
- `grep -rn "mol ssrecalc" vmd/lib/ | grep -v "#" → 0` (the decision enforced in code)
</verification>

<success_criteria>
- The 17.1 regression baseline STAYS GREEN under the widened seam (the one broken case repaired, everything else untouched)
- ROADMAP SC1/SC2/SC3 are proven at composition-root level; the phase is headless-green and ready for the single consolidated GUI checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/17.2-cartoon-newcartoon-generators/17.2-11-SUMMARY.md`
</output>
