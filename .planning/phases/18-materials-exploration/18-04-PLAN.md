---
phase: 18-materials-exploration
plan: 04
type: execute
wave: 2
depends_on: ["18-01"]
files_modified: [vmd/lib/materials.tcl, vmd/tests/test_materials.test]
autonomous: true

must_haves:
  truths:
    - "ensure_gameblend creates GameBlend from a value-copy of Opaque (opacity 0.85, specular 0.65) exactly once per session and is idempotent on repeat calls"
    - "NO stock material is ever mutated — the only material add/change in Phase 18 lives in this plan's ensure_gameblend (single ownership directive)"
    - "resolve_per_mat maps every ACTIVE tier to a curated material: per_mat value if valid, else DEFAULT_MATERIAL; unknown values degrade to default + a dropped entry for the caller's warn loop"
    - "resolve_per_mat output is GAME_REPS-ordered (the runtime derivation choke-point discipline, mirroring resolve_per_rep)"
    - "Empty per_mat resolves to empty → callers do NOTHING (byte-identical blending-off path)"
  artifacts:
    - path: "vmd/lib/materials.tcl"
      provides: "GameBlend recipe + ensure_gameblend + resolve_per_mat + valid_mat_value; sources NOTHING (standalone like backup.tcl)"
      contains: "ensure_gameblend"
    - path: "vmd/tests/test_materials.test"
      provides: "Pure resolution cases + headless GameBlend creation/idempotency/blast-radius cases"
      contains: "BCHM_TEST_RESULT"
  key_links:
    - from: "materials.tcl resolve_per_mat"
      to: "setup_state GAME_MATERIALS / DEFAULT_MATERIAL"
      via: "call-time fully-qualified references (sources NOTHING convention)"
      pattern: "::biochemeleon::setup_state::GAME_MATERIALS"
    - from: "ensure_gameblend"
      to: "material add GameBlend copy Opaque"
      via: "value-copy then opacity/specular changes on the COPY only"
      pattern: "material add GameBlend copy Opaque"
---

<objective>
Create vmd/lib/materials.tcl — the phase's material module: the SINGLE GameBlend creation recipe (idempotent ensure_gameblend), the pure per_mat resolver (resolve_per_mat), and the material-value validator. Plus its tcltest suite. This plan owns ALL GameBlend creation for Phase 18 (user directive: exactly one plan creates/initializes GameBlend) — every other plan CALLS these procs and never inlines `material add`.

Purpose: MATERIAL-01's `material add GameBlend` + `material change opacity` recipe, made idempotent and blast-radius-safe (stock names are never touched — the materials research's Pitfall 1). resolve_per_mat is the runtime derivation choke point (17.1-14 locked decision: derivation lives INSIDE start_game's call chain so console + GUI + restart share one semantics).
Output: materials.tcl + test_materials.test, both green headless.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-materials-exploration/18-RESEARCH-vmd-materials.md
@.planning/phases/18-materials-exploration/18-RESEARCH-codebase-integration.md
@vmd/lib/backup.tcl
@vmd/lib/rep_tiers.tcl

Load-bearing research: materials research "GameBlend recipe" block (the exact 4-line recipe — idempotent create, copy Opaque, opacity 0.85, specular 0.65, NEVER touch stock names) + §1.2 of the integration research (resolution rule: per_mat value if present and known, else DEFAULT_MATERIAL; unknown names dropped with warn — the warn is the CALLER's job, resolve stays pure). Standalone convention: backup.tcl:9 "STANDALONE — sources NOTHING". resolve_per_rep's GAME_REPS-order output discipline: rep_tiers.tcl resolve_per_rep. Settings order for assertions: amb spec dif shin mirr opac outl outlw transmode (materials research B13 — spec = index 1, opac = index 5).
</context>

<tasks>

<task type="auto">
  <name>Task 1: materials.tcl — GameBlend recipe + resolve_per_mat + validator</name>
  <files>vmd/lib/materials.tcl</files>
  <action>
1. Header: `# vmd/lib/materials.tcl` — mol bridge, STANDALONE — sources NOTHING (like backup.tcl). References setup_state constants (GAME_MATERIALS, DEFAULT_MATERIAL, GAME_REPS) via fully-qualified `::biochemeleon::setup_state::*` names at CALL time. Include the MATERIAL-02 pedagogy-hypothesis note and the curated-set rationale (difficulty ladder; Glass1/Ghost gated pending the 18-15 pick-through verdict).
2. Namespace `::biochemeleon::materials` with:
   - `variable GAMEBLEND_NAME "GameBlend"`, `variable GB_OPACITY 0.85`, `variable GB_SPECULAR 0.65`.
   - `proc ensure_gameblend {}`: idempotent — `if {[lsearch -exact [material list] $GAMEBLEND_NAME] == -1} { material add $GAMEBLEND_NAME copy Opaque; material change opacity $GAMEBLEND_NAME $GB_OPACITY; material change specular $GAMEBLEND_NAME $GB_SPECULAR }`; return 1. NEVER calls material change on any stock name. Tcl-validate the constants are numeric in [0,1] before applying (silent-garbage directive — VMD would store `zzz` as 0.0 and 1.5 unclamped; assert with `string is double` + range check, error loudly on violation since these are compile-time constants, not user input).
   - `proc valid_mat_value {mat}`: returns 1 iff mat ∈ GAME_MATERIALS (the Tcl-side gate against VMD's silent garbage acceptance).
   - `proc resolve_per_mat {per_mat tiers}`: returns a 2-element list `{resolved dropped}`.
     - Shape-guard per_mat (catch `dict size` — non-dict → treat as empty).
     - If empty → return `[list [dict create] [list]]`.
     - For each ACTIVE tier in $tiers ({code style count} triples, GAME_REPS order): `set mat ""`; if per_mat has key $style AND `valid_mat_value $mat` → use it; else if per_mat has the key but the value is invalid → append `{$style $rawval unknown-material}` to dropped and use DEFAULT_MATERIAL; else (no entry) → DEFAULT_MATERIAL (silent default fill — a missing pick is normal, not a warning). `dict set resolved $style $mat`.
     - per_mat keys NOT in GAME_REPS (typo'd style names): append `{$key $val unknown-rep}` to dropped (mirrors the warn-loop precedent at game.tcl:204-212).
     - Output resolved is built in $tiers order = GAME_REPS order (tiers_from_per_rep guarantees it — assert nothing, rely on the caller contract like resolve_per_rep does).
     - PURE: no vmdcon, no material commands, no mol — warnings and creation are the caller's (start_game, 18-07).
   - Tcl 8.5 only: dict, lassign, brace every expr; no lmap/try/dict-get-default.
  </action>
  <verify>
Static: grep materials.tcl — `sources NOTHING` header present; `material add $GAMEBLEND_NAME copy Opaque` present exactly once; NO `material change` on a stock name (grep `material change` lines all reference $GAMEBLEND_NAME); no `source ` statements; no top-level VMD calls (proc defs only). 8.6-idiom grep = 0.
  </verify>
  <done>materials.tcl exists with the single GameBlend recipe, the pure resolver, and the validator — nothing else in the phase will create or mutate materials.</done>
</task>

<task type="auto">
  <name>Task 2: test_materials.test — resolution + GameBlend creation suite</name>
  <files>vmd/tests/test_materials.test</files>
  <action>
New tcltest suite following the repo marker pattern (BCHM_TEST_RESULT at the end; source-by-[pwd] like test_rep_tiers.tcl:29 — source setup_state.tcl THEN materials.tcl; explicit `exit` handling per the suite_driver.tcl convention):
Pure cases:
1. `resolve_empty_is_noop`: resolve_per_mat {} {} → {{} {}}; also resolve {} with tiers → {} resolved, no dropped.
2. `resolve_valid_value_used`: tiers {{1 VDW 2}} + per_mat {VDW Glass3} → resolved {VDW Glass3}.
3. `resolve_default_fill`: tiers {{1 VDW 1} {2 Lines 2}} + per_mat {VDW Glass3} → resolved has Lines→Translucent (DEFAULT_MATERIAL) and VDW→Glass3; dropped empty (missing pick is silent).
4. `resolve_unknown_value_degrades`: per_mat {VDW NotAMaterial} → VDW→Translucent + dropped contains {VDW NotAMaterial unknown-material}.
5. `resolve_output_game_reps_order`: tiers in GAME_REPS order {Lines, VDW} → resolved keys emit Lines before VDW regardless of per_mat construction order.
6. `resolve_ignores_inactive_valid_keys`: per_mat {Tube Glass3} with no Tube tier → Tube absent from resolved, dropped empty (lock-scene normal case — silent).
7. `resolve_drops_unknown_rep_keys`: per_mat {Ribbon Glass3} → dropped contains {Ribbon Glass3 unknown-rep}.
8. `resolve_shape_garbage`: per_mat "junk" (non-dict) → {{} {}} no error.
9. `valid_mat_value_gate`: Translucent → 1; Glass1 → 0 (GATED OUT of the shipped set); GameBlend → 1; Opaque → 0 (not curated).
Headless VMD cases (material commands work in -dispdev text — probe-verified):
10. `ensure_creates_with_recipe`: catch-guarded cleanup of any pre-existing GameBlend is NOT allowed to use material delete while reps exist — instead assert from a fresh session: before ensure, GameBlend absent from [material list]; after ensure_gameblend, present; `material settings GameBlend` lindex 5 (opacity) == 0.85 and lindex 1 (specular) == 0.65 (eps 1e-6).
11. `ensure_idempotent`: capture settings, call ensure_gameblend again, settings eq (the 9-value list string-eq, C7 round-trip discipline).
12. `stock_opaque_untouched`: capture `material settings Opaque` BEFORE ensure, compare AFTER — 9-value eq (blast-radius guard, research Pitfall 1).
Suite ends with the BCHM_TEST_RESULT marker; add an explicit `exit` so it works under both standalone runs and suite_driver.tcl.
  </action>
  <verify>
Headless suite run from fresh staging: `bash -ic 'cd <staging> && vmd -dispdev text -e vmd/tests/test_materials.test -eofexit < /dev/null'` → BCHM_TEST_RESULT Failed=0; full-log scan 0 `ERROR)` / 0 `bad switch`; `Exiting normally`. 8.6-idiom gate 0. SEQUENTIAL VMD runs only.
  </verify>
  <done>test_materials.test green: pure resolution contract pinned (default fill, degrade, order, gating) + GameBlend creation proven idempotent with stock materials untouched.</done>
</task>

</tasks>

<verification>
- test_materials.test BCHM_TEST_RESULT Failed=0; full-log 0 `ERROR)` / 0 `bad switch`; `Exiting normally`.
- materials.tcl: zero `source` statements, zero stock-name `material change`, GameBlend recipe present once.
- 8.6-idiom grep = 0.
</verification>

<success_criteria>
GameBlend has exactly one creator (idempotent, recipe-pinned, stock-safe) and per_mat has exactly one runtime resolver (pure, GAME_REPS-ordered, warning-class degrade) — both proven by a new green suite.
</success_criteria>

<output>
After completion, create `.planning/phases/18-materials-exploration/18-04-SUMMARY.md`
</output>
