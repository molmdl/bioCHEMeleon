---
phase: 18-materials-exploration
plan: 12
type: execute
wave: 6
depends_on: ["18-11"]
files_modified: [vmd/smoke/phase17_capstone_smoke.tcl]
autonomous: true

must_haves:
  truths:
    - "The capstone (composition-root regression baseline) gains an explicit per_mat blending round: mixed tiers, resolved stash, per-pair read-back, bookkeeping unchanged"
    - "A blending-off regression round (4-arg call) proves gs per_mat == {} and hider materials Opaque through the SAME composition root"
    - "A lock-scene + blending interplay round proves derived tiers get DEFAULT_MATERIAL / per_mat values while inactive per_mat keys are silently ignored (resolved keys == the derived tier set)"
    - "All pre-existing capstone rounds stay byte-identical (zero lib changes — smoke-only file)"
  artifacts:
    - path: "vmd/smoke/phase17_capstone_smoke.tcl"
      provides: "Rounds F (blending), G-off (off regression), G-lock (lock-scene blending interplay)"
      contains: "per_mat"
  key_links:
    - from: "capstone round F"
      to: "game.tcl 5-arg seam (18-07)"
      via: "start_game with explicit per_mat + resolved-stash asserts"
      pattern: "per_mat"
    - from: "capstone round G-lock"
      to: "DEFAULT_MATERIAL fill"
      via: "lock_scene=1 + per_mat → resolved keys == derived tiers only"
      pattern: "DEFAULT_MATERIAL|Translucent"
---

<objective>
Extend the 17.x capstone — the standing composition-root regression baseline — with material rounds: an explicit per_mat round, a blending-off regression, and the lock-scene × blending interplay (the trickiest resolution case: derived tiers + user picks + inactive keys).

Purpose: ROADMAP SC3's composition-root insurance — every future phase runs this capstone; if the material seam ever regresses, the capstone catches it at the dispatch level. Follows 17.1-13/17.2-11's draw-adaptive invariant discipline (never pin random draws).
Output: capstone smoke extended, PASS=1 ×3, existing rounds byte-identical.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-materials-exploration/18-RESEARCH-vmd-materials.md
@.planning/phases/18-materials-exploration/18-RESEARCH-codebase-integration.md
@vmd/smoke/phase17_capstone_smoke.tcl

Load-bearing research: integration research §5.3 (capstone row: "One blending round (explicit per_mat) + the blending-off regression round; lock-scene + blending interplay (derived tier gets DEFAULT_MATERIAL)") and §5.4 (gate checklist). 17.1-13/17.2-11 precedents: invariant-based asserts (quick-008 sum≤hc, never ==), observed-layout invariants with plan pins as the strict full-generation case, cleanup snapshot-fidelity asserts. Lock-scene detection reads STYLES only (rep_tiers.tcl) — materials ride alongside.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Round F (explicit per_mat mixed round) + Round G-off (blending-off regression)</name>
  <files>vmd/smoke/phase17_capstone_smoke.tcl</files>
  <action>
1. Round F (explicit blending, mixed simple tiers on 1znf): `start_game $m 3 {VDW 2 Lines 1} 0 {VDW GameBlend Lines Glass3}` — asserts: gs 5 keys + stashed per_mat == {Lines Glass3 VDW GameBlend} (GAME_REPS-ordered); per-pair material read-back (VDW pair GameBlend, Lines pair Glass3) via tier_reps + repindex; registry bookkeeping UNCHANGED ({VDW 2 Lines 1} — materials never touch counts, STATE constraint 3); one find per tier; cleanup restore incl. material-name fidelity + Opaque pristine.
2. Round G-off: `start_game $m2 2 {VDW 2} 0` (4-arg) → gs per_mat == {}, both hider reps read Opaque — the composition root's off-default through the SAME public surface.
3. Do NOT touch the existing rounds A-E (byte-identical) and ZERO lib changes (17.2-11 discipline — this plan is smoke-only).
  </action>
  <verify>
Headless ×3 (fresh staging, SEQUENTIAL): PASS=1 each; full-log 0 `ERROR)` / 0 `bad switch`; `Exiting normally`; the pre-existing rounds' assert output unchanged. 8.6 gate 0.
  </verify>
  <done>Capstone proves blending + off-default at the composition root without perturbing the standing rounds.</done>
</task>

<task type="auto">
  <name>Task 2: Round G-lock (lock-scene × blending interplay)</name>
  <files>vmd/smoke/phase17_capstone_smoke.tcl</files>
  <action>
1. Pre-style rep 0 of a fresh 1znf to Licorice (the 17.1-13 round-B shape), then `start_game $m3 <n> {} 1 {Licorice Glass3 VDW GameBlend}` where n is the round-B-class count — per_mat carries entries for BOTH a tier that will derive (Licorice) and one that will NOT (VDW inactive in lock-scene).
2. Draw-adaptive asserts (17.1-13 invariant discipline): gs per_mat keys == {Licorice} EXACTLY (VDW was inactive → silently ignored; Licorice kept its explicit Glass3); the Licorice pair reads Glass3; hider_count == effective_total == registry count (P9 chain, n ∈ [1, count] NOT ==); cleanup snapshot-fidelity (rep-0 STILL Licorice + material name fidelity).
3. Also assert the derived-tier default-fill path: a SECOND lock-scene round with per_mat {} + blending implicitly OFF is already covered by G-off — instead, cover default-fill via Round F variant? NO — default-fill for active tiers lacking entries is proven in test_materials.test (pure) and 18-09 Round A is explicit; the lock-scene DEFAULT_MATERIAL case: run G-lock with per_mat {VDW GameBlend} ONLY (no Licorice entry) → resolved == {Licorice Translucent} (derived Licorice gets DEFAULT_MATERIAL Translucent; VDW dropped as inactive) and the Licorice pair reads Translucent. Assert that shape.
  </action>
  <verify>
Headless ×3 (sequential): PASS=1 each; full-log 0 `ERROR)` / 0 `bad switch`; `Exiting normally`. 8.6 gate 0.
  </verify>
  <done>Lock-scene × blending resolution proven both ways (explicit pick survives; derived tier default-fills; inactive keys silently ignored) — draw-adaptively.</done>
</task>

</tasks>

<verification>
- Capstone PASS=1 ×3 with rounds A-G all green; full logs 0 `ERROR)` / 0 `bad switch`; `Exiting normally`.
- Existing rounds byte-identical (git diff shows additions only).
- 8.6-idiom grep = 0.
</verification>

<success_criteria>
The standing composition-root regression now covers the material dimension: on, off, and lock-scene interplay — any future seam regression is caught by the phase gate.
</success_criteria>

<output>
After completion, create `.planning/phases/18-materials-exploration/18-12-SUMMARY.md`
</output>
