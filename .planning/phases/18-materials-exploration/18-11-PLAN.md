---
phase: 18-materials-exploration
plan: 11
type: execute
wave: 5
depends_on: ["18-09"]
files_modified: [vmd/smoke/phase18_multimat_smoke.tcl]
autonomous: true

must_haves:
  truths:
    - "One round can carry DIFFERENT materials per tier (per-hider material variety) with per-pair read-back proving each tier got its own material"
    - "A single Tachyon render shows DISTINCT opacity tokens side by side (Glass2 0.68 + Translucent 0.8 classes) — material variety is render-proven, not just read-back-proven"
    - "Materials apply cleanly to the residue (Cartoon) tier class — the seam is orthogonal to tier kind, with Stride ERRORs classified as known noise"
    - "Cleanup restores material-name fidelity for both rounds"
  artifacts:
    - path: "vmd/smoke/phase18_multimat_smoke.tcl"
      provides: "Multi-material coexistence round + residue-class materials round + Stride-noise classification"
      contains: "BCHM_SMOKE_RESULT"
  key_links:
    - from: "multimat render"
      to: "distinct Opacity tokens"
      via: "anchored regexps for 0.68 and 0.8 coexisting in one .dat"
      pattern: "Opacity 0\\\\.68(\\\\s|\\$)"
---

<objective>
Extend the headless proof to the two cases the core smoke (18-09) doesn't cover: (1) MULTI-material coexistence — different tiers with different materials in one round, proven by distinct Opacity/Phong tokens in ONE render; (2) the residue (Cartoon) tier class — materials on residue-spliced hiders, with the Stride-noise classification the 17.2 suite established.

Purpose: closes SC1's coverage matrix (tier class × material variety) and proves the seam's per-tier mat_specs parallelism survives residue slicing (game.tcl step 14's zip walk).
Output: phase18_multimat_smoke.tcl, PASS=1 ×3.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-materials-exploration/18-RESEARCH-vmd-materials.md
@.planning/phases/18-materials-exploration/18-RESEARCH-codebase-integration.md
@vmd/smoke/phase18_materials_smoke.tcl
@vmd/smoke/phase17_splice_smoke.tcl

Load-bearing research: materials research C8 evidence (one scene held Opacity 0.1/0.15/0.68/0.8/1.0 simultaneously — distinct-value coexistence is assertable) + inventory values (Glass2 opac 0.68 spec 0.22; Translucent opac 0.80 spec 0.6) + Pitfall 6 (Stride ERROR noise: classify lines containing "Stride" as known noise, zero-tolerance for OTHER classes); integration research §5.2 (_parse_dat + both-endpoints), §5.3 (the two-round smoke spec), 17.2-04's frame-pinning rule (residue rounds need the single-frame collapse loader — _load_demo_1f from phase17_splice_smoke/17.2-10). Token anchor discipline from 18-09 (prefix trap).
</context>

<tasks>

<task type="auto">
  <name>Task 1: multi-material coexistence round (VDW/Glass2 + Lines/Translucent)</name>
  <files>vmd/smoke/phase18_multimat_smoke.tcl</files>
  <action>
New smoke file (copy 18-09's harness skeleton: same source list, marker, log discipline):
1. Round M on 1znf: `start_game $m 4 {VDW 2 Lines 2} 0 {VDW Glass2 Lines Translucent}` (per_mat carries BOTH tiers). Asserts: gs 5 keys; stashed per_mat == {Lines Translucent VDW Glass3?} — NO: resolved is {Lines Translucent VDW Glass2} (GAME_REPS-ordered dict: Lines before VDW); per-pair read-back via tier_reps + repindex: VDW hidden+found == Glass2, Lines hidden+found == Translucent; scene rep 0 + Opaque settings untouched (blast-radius guards as in 18-09).
2. Tachyon coexistence proof: emptied-reps pattern, ONE hiders-only probe rep over all 4 hiders (or two probe reps — one per tier; simplest: single probe rep over the GAM selection — the .dat carries each primitive's own material tokens): render → assert `regexp -- {Opacity 0\.68(\s|$)}` AND `regexp -- {Opacity 0\.8(\s|$)}` (both present; the anchored 0.8 does NOT match 0.85 or 0.88) AND `regexp -- {Phong Plastic 0\.22(\s|$)}` AND `regexp -- {Phong Plastic 0\.6(\s|$)}` (distinct speculars). Baseline (pre-round Opaque render of the same scene): NONE of the four tokens.
3. One find per tier (two on_pick calls on VDW + Lines hider indices) → remaining_by_rep {VDW 1 Lines 1} + found reps keep their materials. Cleanup fidelity (registry 0, molid dead, restored material names, Opaque pristine).
  </action>
  <verify>
Headless ×3 (sequential): PASS=1 each; full-log 0 `ERROR)` / 0 `bad switch` / 0 `out of range`; `Exiting normally`. 8.6 gate 0.
  </verify>
  <done>Per-tier material variety proven at read-back AND render level in a single round.</done>
</task>

<task type="auto">
  <name>Task 2: residue-class materials round (Cartoon/GameBlend + VDW/Glass3) + Stride-noise rules</name>
  <files>vmd/smoke/phase18_multimat_smoke.tcl</files>
  <action>
1. Round R: load 1znf via the single-frame collapse loader (_load_demo_1f pattern from phase17_splice_smoke.tcl — residue splicing REQUIRES single-frame molecules, 17.2-04 rule). `start_game $m 3 {Cartoon 2 VDW 1} 0 {Cartoon GameBlend VDW Glass3}`. Asserts: Cartoon pair (hidden+found) reads GameBlend, VDW pair reads Glass3 — the residue slicing walk (game.tcl step 14) must not disturb mat_specs' code→material parallelism; gs per_mat == {Cartoon GameBlend VDW Glass3} (GAME_REPS order: Cartoon before VDW); hider_count 3; one VDW find registers (fallback path not needed — click the sphere-class hider via on_pick index).
2. Stride-noise classification (Pitfall 6): the log scanner must classify ERROR lines whose text contains `Stride` (e.g. "Unable to find Stride output file" / "Call to Stride program failed") as KNOWN NOISE — assert ZERO non-Stride `ERROR)` lines and 0 `bad switch`. Record the classification rule in the smoke header.
3. Cleanup fidelity: restored material names + Opaque pristine + registry cleared.
  </action>
  <verify>
Headless ×3 (sequential): PASS=1 each; scanner reports 0 non-Stride `ERROR)` / 0 `bad switch`; `Exiting normally`. 8.6 gate 0.
  </verify>
  <done>Materials proven on the residue tier class with draw-adaptive assertions (no pinned layout counts) and the Stride-noise convention inherited.</done>
</task>

</tasks>

<verification>
- phase18_multimat_smoke PASS=1 ×3, logs clean (Stride-classified), `Exiting normally` ×3.
- Distinct-token coexistence asserted with ANCHORED regexps; baseline absence asserted.
- 8.6-idiom grep = 0.
</verification>

<success_criteria>
The material seam is proven for material VARIETY (many materials, one round) and for the residue tier class — SC1's coverage matrix is complete headlessly.
</success_criteria>

<output>
After completion, create `.planning/phases/18-materials-exploration/18-11-SUMMARY.md`
</output>
