---
phase: 18-materials-exploration
plan: 14
type: execute
wave: 7
depends_on: ["18-12", "18-13"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "ALL 7 tcltest suites green (the 6 standing + NEW test_materials) with Failed=0"
    - "ALL 33 headless smokes run sequentially: every NEW/updated smoke PASS=1; the 2 PRE-EXISTING reds fail EXACTLY as recorded (same shapes, recipes already documented — do NOT fix or re-diagnose)"
    - "Every log: 0 `ERROR)` (Stride-classified where applicable), 0 `bad switch`, `Exiting normally`; the 2 phase-18 smokes also 0 `out of range`"
    - "Static gates zero: 8.6-idiom, grab-set"
  artifacts:
    - path: ".planning/phases/18-materials-exploration/18-14-SUMMARY.md"
      provides: "Gate record: suite totals, smoke PASS list, gate outputs, pre-existing-red confirmation"
      contains: "FULL-SUITE"
  key_links:
    - from: "gate run"
      to: "every Phase-18 artifact"
      via: "fresh staging tmp/cap18-gate, sequential VMD runs, marker parse + full-log scans"
      pattern: "BCHM_(TEST|SMOKE)_RESULT"
---

<objective>
The phase's FULL-SUITE green gate (17.1-13 / 17.2-11 pattern): every suite + every smoke against a fresh staging copy, sequential (shared $env(TEMP) forbids parallel VMD), markers parsed from logs (never $?), full-log false-PASS scans, static gates, and the 2 known pre-existing reds confirmed unchanged.

Purpose: HEADLESS-GREEN declaration for Phase 18 — after this, only the GUI checkpoint remains.
Output: gate record in the SUMMARY (no code files touched).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-materials-exploration/18-RESEARCH-codebase-integration.md

Load-bearing research: integration research §5.4 (the verbatim gate checklist: suites under headless VMD via the staging-only suite_driver.tcl wrapper — suites lack explicit exit and VMD's console hangs past stdin EOF; PASS=1 ×3 for smokes; 8.6-idiom + grab-set greps; SEQUENTIAL VMD runs; capstone/full-suite gate pattern; expect the 2 known pre-existing reds with their recorded recipes — do NOT re-diagnose). The 17.2-11 gate numbers: 6 suites 205/205, 29/31 smokes; this gate adds test_materials + 2 phase-18 smokes and the 18-05/18-07/18-12 updates.
</context>

<tasks>

<task type="auto">
  <name>Task 1: FULL-SUITE gate run + record</name>
  <files>.planning/phases/18-materials-exploration/18-14-SUMMARY.md</files>
  <action>
1. Stage a FRESH copy of the repo's vmd/ tree to `tmp/cap18-gate/` (Windows-visible path).
2. Suites (sequential, one VMD process at a time): test_setup_state, test_registry, test_generators, test_rep_tiers, test_game_logic, test_splice, test_materials — via the staging-only suite_driver.tcl wrapper (or explicit-exit driver). Parse BCHM_TEST_RESULT from each log: record Total/Failed; require Failed=0 everywhere.
3. Smokes (sequential): all 33 files in vmd/smoke/ (31 standing incl. the updated phase14_mol/onpick/capstone + NEW phase18_materials_smoke + phase18_multimat_smoke). Each new/updated smoke needs PASS=1 ×3 (independent PRNG draws); the untouched standing smokes ×1 (their ×3 baselines are recorded from prior gates). Full-log scan EVERY log: 0 `ERROR)` (Stride-classified for the residue/strand-running smokes per the 17.2 convention), 0 `bad switch`, `Exiting normally`; the 2 phase-18 smokes additionally 0 `out of range`.
4. Static gates: 8.6-idiom grep (lib/+gui/) = 0; grab-set grep (gui/) = 0.
5. Pre-existing-red discipline: phase17_dispatch_smoke (step-8 draw-dependent reg2_count) and phase17_licorice_smoke (stale P pins) are EXPECTED red with recorded recipes — confirm the failure SHAPE matches the 17.2-11 records (not a new failure mode); do NOT fix, do NOT re-diagnose; record "pre-existing, unchanged" in the SUMMARY.
6. Record everything in 18-14-SUMMARY.md: per-suite totals, the 33-smoke PASS/expected-red table, gate outputs, sequential-run confirmation.
  </action>
  <verify>
The SUMMARY's gate table shows: 7/7 suites Failed=0; 31/33 smokes PASS (2 expected pre-existing reds with matching shapes); all scanned logs clean; static gates zero. Any OTHER red = a Phase-18 regression → STOP and gap-close before proceeding to 18-15.
  </verify>
  <done>Phase 18 is HEADLESS-GREEN: full gate recorded with zero new regressions and the 2 known reds confirmed unchanged.</done>
</task>

</tasks>

<verification>
- 7 suites Failed=0 (incl. test_materials).
- 33 smokes: 31 PASS + 2 expected pre-existing reds (shapes match the recorded recipes).
- Every log: 0 `ERROR)` / 0 `bad switch` / `Exiting normally`; static gates 0.
</verification>

<success_criteria>
The full-suite gate declares Phase 18 headless-green with evidence recorded — the GUI checkpoint is the only remaining plan.
</success_criteria>

<output>
After completion, create `.planning/phases/18-materials-exploration/18-14-SUMMARY.md`
</output>
