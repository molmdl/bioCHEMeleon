---
phase: 18-materials-exploration
plan: 06
type: execute
wave: 2
depends_on: ["18-01"]
files_modified: [vmd/gui/setup_tab.tcl]
autonomous: true

must_haves:
  truths:
    - "The Hiders group has a 'Use material blending' checkbutton (off by default) and a 4th per-rep grid column with a material menubutton per GAME_REPS row"
    - "Each menubutton offers (default) + the 5 curated GAME_MATERIALS in difficulty order; picking sets rep_mat($rep) (empty = follow DEFAULT_MATERIAL)"
    - "Picker enabled ⇔ mat_blend == 1 AND rep_sel($rep) == 1 — the grey-out coupling runs outside _loading (on_rep_toggled precedent)"
    - "New namespace vars follow the one-variable-per-line + bare-array discipline (14-04 traps)"
  artifacts:
    - path: "vmd/gui/setup_tab.tcl"
      provides: "mat_blend var, rep_mat array, toggle checkbutton, per-rep material menubuttons, on_mat_picked + on_mat_changed + sync_mat_label"
      contains: "mat_blend"
  key_links:
    - from: "menubutton menu build"
      to: "setup_state GAME_MATERIALS"
      via: "foreach over the curated list at build time"
      pattern: "GAME_MATERIALS"
    - from: "on_rep_toggled tail"
      to: "on_mat_changed"
      via: "rep-toggle changes the enable condition, so it re-runs the coupling"
      pattern: "on_mat_changed"
---

<objective>
Build the Setup-tab material widgets: the "Use material blending" toggle in the Hiders group's top row and a per-rep material picker (menubutton + menu — the proven Tk 8.5.6 idiom; ttk::combobox is UNVERIFIED in this build and is banned) in the per-rep grid's 4th column, plus the grey-out coupling. Headless load-gate only — real widget behavior is the 18-15 GUI checkpoint.

Purpose: MATERIAL-02's Setup-tab surface. The picker is per-rep (material is a hider property, orthogonal to the 10-rep tier system — rep choice × material choice are independent axes).
Output: setup_tab.tcl widgets + coupling, headless-loadable with zero error lines.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-materials-exploration/18-RESEARCH-vmd-materials.md
@.planning/phases/18-materials-exploration/18-RESEARCH-codebase-integration.md
@vmd/gui/setup_tab.tcl
@vmd/lib/setup_state.tcl

Load-bearing research: integration research §3.1-§3.4 — menubutton+menu idiom (setup_tab.tcl:137-144 loaded-mol picker is the template), ttk::combobox DO NOT plan on it, group placement (build_hiders_group 174-209), the grey-out rules (§3.3: picker enabled ⇔ toggle AND rep checked; enable/disable OUTSIDE _loading), and the trap table §3.4 (one `variable` per line; bare array declaration; fully-qualified -variable paths; no grab; 8.6-idiom comment hygiene). The menubutton label is cosmetic (configure -text), the VALUE lives in rep_mat($rep) — collect (18-08) reads the value, not the label.
</context>

<tasks>

<task type="auto">
  <name>Task 1: vars + toggle + per-rep material menubuttons</name>
  <files>vmd/gui/setup_tab.tcl</files>
  <action>
1. Namespace vars block (27-46): add `variable mat_blend 0` (scalar, off default) and `variable rep_mat` (BARE — undefined so `set rep_mat($k) v` creates the array; the 14-04 trap). ONE variable per line. Update the block comment.
2. build_hiders_group top row (178-188): after `$top.lock`, add `ttk::checkbutton $top.matb -text "Use material blending" -variable ::biochemeleon::setup_tab::mat_blend -command {::biochemeleon::setup_tab::on_mat_changed}`; pack after lock. Keep the modeless discipline — no grab anywhere.
3. Per-rep grid (191-207): add a 4th column per row —
   `menubutton $pr.m$row -text "(default)" -state disabled -relief raised` (initial state disabled; blending off default) with a child `menu $pr.m$row.m -tearoff 0` containing: an entry "(default)" whose command is `[list ::biochemeleon::setup_tab::on_mat_picked $rep ""]`, then one entry per material in `$::biochemeleon::setup_state::GAME_MATERIALS` order with command `[list ::biochemeleon::setup_tab::on_mat_picked $rep $mat]`. `grid $pr.m$row -row $row -column 3 -sticky w -padx 4`. Mirror the existing loaded-mol picker idiom (137-144). Use plain `menubutton`/`menu` (Tk 8.5 core, proven in this file) — NOT ttk::combobox (zero vmd-ref occurrences, unverifiable headlessly).
4. New procs (place near on_rep_toggled 443-482):
   - `sync_mat_label {rep}`: resolves the row index via `lsearch -exact $::biochemeleon::setup_state::GAME_REPS $rep`, guards `![info exists w] || $w eq ""` + `winfo exists` (on_rep_toggled pattern), then `configure -text` to `[expr {$rep_mat($rep) eq "" ? "(default)" : $rep_mat($rep)}]` — guard the rep_mat element with info exists (array element may not exist yet).
   - `on_mat_picked {rep mat}`: `set rep_mat($rep) $mat` (empty string = default), `sync_mat_label $rep`, then `on_mat_changed`.
5. Comment hygiene: reword any comment that would trip the 8.6-idiom grep literals.
  </action>
  <verify>
Headless load-gate from fresh staging: source setup_state.tcl then setup_tab.tcl under `vmd -dispdev text` → the established single warn line pattern, 0 `ERROR)` / 0 `bad switch`, `Exiting normally`. Static: grep for `mat_blend`, `rep_mat`, `GAME_MATERIALS` (menu build), `on_mat_picked`; grep `grab set` over vmd/gui/ = 0; 8.6-idiom grep = 0. Tk rendering CANNOT be verified headlessly — that is 18-15's checkpoint.
  </verify>
  <done>Widgets + vars + picker menu exist and the file loads headless-clean; values are stored in rep_mat($rep) with empty = default.</done>
</task>

<task type="auto">
  <name>Task 2: grey-out coupling — on_mat_changed + on_rep_toggled integration</name>
  <files>vmd/gui/setup_tab.tcl</files>
  <action>
1. New proc `on_mat_changed {}`: loops all GAME_REPS rows; for each, resolves the menubutton path `$w.nb.setup.hiders.perrep.m$row`; guards winfo exists; reconfigures `-state` to `normal` iff (`mat_blend` truthy AND `rep_sel($rep)` exists-and-truthy) else `disabled`. NO _loading check in this proc — the enable/disable reconfigure runs OUTSIDE the guard exactly like on_rep_toggled's spinbox enable/disable (apply_state, 18-08, needs it to fire during its guarded pass).
2. Append a `::biochemeleon::setup_tab::on_mat_changed` call to the TAIL of on_rep_toggled (after recompute_per_rep_maxes) — toggling a rep changes the picker-enable condition too. During apply_state's per-rep loop this fires per rep; on_mat_changed's own loop makes it idempotent and cheap.
3. The checkbutton -command from Task 1 already routes to on_mat_changed (toggle flip re-evaluates all rows).
  </action>
  <verify>
Headless load-gate again (file still loads clean — new proc definitions parse). Static: grep `on_mat_changed` (defined + called from checkbutton + on_rep_toggled tail); confirm NO `_loading` guard inside on_mat_changed. grab-set gate 0; 8.6-idiom gate 0.
  </verify>
  <done>Pickers grey out unless blending is on AND the rep is checked; coupling fires from both the toggle and rep toggles; the _loading discipline is preserved for apply_state (18-08).</done>
</task>

</tasks>

<verification>
- Headless source of setup_tab.tcl: 0 `ERROR)` / 0 `bad switch`, `Exiting normally`.
- grab-set grep vmd/gui/ = 0; 8.6-idiom grep = 0.
- Grep: menubutton column exists; on_mat_changed called from ≥2 sites (checkbutton + on_rep_toggled tail).
</verification>

<success_criteria>
The Setup tab's material surface exists (toggle + curated per-rep pickers) with the repo's Tk 8.5.6 idioms and grey-out coupling, headless-load-clean, ready for 18-08's collect/apply wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/18-materials-exploration/18-06-SUMMARY.md`
</output>
