---
phase: 18-materials-exploration
plan: 03
type: execute
wave: 2
depends_on: ["18-01"]
files_modified: [vmd/lib/setup_state.tcl, vmd/tests/test_setup_state.test]
autonomous: true

must_haves:
  truths:
    - "validate_state coerces material_blending through _to_bool (same as lock_scene)"
    - "validate_state drops per_mat entries whose key is not a GAME_REPS name and whose value is not a GAME_MATERIALS name — VMD-style garbage can never survive into validated state"
    - "per_mat dict-shape garbage (non-dict) is caught and yields {} (mirror of the per_rep guard)"
    - "per_mat entry order is PRESERVED from input (validate_state's own-order discipline, 17.1-01 lesson); GAME_REPS ordering is resolve_per_mat's job (18-04), not validate's"
  artifacts:
    - path: "vmd/lib/setup_state.tcl"
      provides: "validate_state material_blending bool coercion + per_mat cleaning block"
      contains: "mat_clean"
    - path: "vmd/tests/test_setup_state.test"
      provides: "Validation cases: bad keys, unknown materials, order preservation, coercion, shape garbage"
      contains: "per_mat"
  key_links:
    - from: "validate_state per_mat block"
      to: "GAME_MATERIALS / GAME_REPS constants"
      via: "lsearch -exact membership drops"
      pattern: "GAME_MATERIALS \\$mat"
---

<objective>
validate_state learns the two new keys: bool-coerce material_blending and clean per_mat (drop non-GAME_REPS keys, drop non-GAME_MATERIALS values, preserve input order). This is the state-layer half of the silent-garbage defense (user directive: wherever per_mat values are accepted, Tcl-validate them — VMD silently stores garbage).

Purpose: the GUI and .bcm loader both funnel through validate_state; cleaning here means an unknown material name typed into a hand-edited .bcm degrades to a clean default instead of reaching mol modmaterial.
Output: vmd/lib/setup_state.tcl validate_state block + test cases; all prior tests stay green.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-materials-exploration/18-RESEARCH-vmd-materials.md
@.planning/phases/18-materials-exploration/18-RESEARCH-codebase-integration.md
@vmd/lib/setup_state.tcl
@vmd/tests/test_setup_state.test

Load-bearing research: integration research §2.2 (the exact validate block — follow it verbatim including the input-order `dict for` loop and the catch-based shape guard) + §1.2's note that GAME_REPS-order output is the RUNTIME resolver's job (18-04 resolve_per_mat), NOT validate's (17.1-01 lesson: validate_state's per_rep is input-order — keep the new dict's semantics consistent with its sibling). The backward-compat property (validate starts from a fresh DEFAULTS copy) means missing keys already yield defaults — no version bump.
</context>

<tasks>

<task type="auto">
  <name>Task 1: validate_state — material_blending coercion + per_mat cleaning block</name>
  <files>vmd/lib/setup_state.tcl</files>
  <action>
1. In validate_state, AFTER the per_rep clamp block (`dict set result per_rep $clamped`) and BEFORE the lock_source block, insert (research §2.2 verbatim shape):
   - material_blending: `if {[dict exists $state material_blending]} { dict set result material_blending [_to_bool [dict get $state material_blending]] }` (same coercion as lock_scene).
   - per_mat: shape-guard + clean loop:
     ```tcl
     set mat_clean [dict create]
     if {[dict exists $state per_mat] && ![catch {dict size [dict get $state per_mat]}]} {
         dict for {rep mat} [dict get $state per_mat] {
             if {[lsearch -exact $GAME_REPS $rep] < 0} { continue }
             if {[lsearch -exact $GAME_MATERIALS $mat] < 0} { continue }
             dict set mat_clean $rep $mat
         }
     }
     dict set result per_mat $mat_clean
     ```
2. Add a block comment: per_mat is input-order-preserved (17.1-01 own-order discipline, sibling of per_rep); GAME_REPS-order canonicalization happens in materials::resolve_per_mat (the runtime derivation choke point, 18-04). Note the silent-garbage rationale: VMD's material layer accepts garbage silently, so garbage is dropped HERE.
3. Update the validate_state header comment (the deterministic-clamp description) to mention the two new keys. Comment hygiene for the 8.6-idiom gate.
  </action>
  <verify>
Headless suite run (fresh staging, sequential): BCHM_TEST_RESULT Failed=0 — all pre-existing + 18-01's cases + Task 2's new cases. Full-log 0 `ERROR)` / 0 `bad switch`; 8.6-idiom gate 0.
  </verify>
  <done>validate_state bool-coerces material_blending and cleans per_mat exactly as specced; missing keys still yield the 18-01 defaults.</done>
</task>

<task type="auto">
  <name>Task 2: test_setup_state.test — validation cases</name>
  <files>vmd/tests/test_setup_state.test</files>
  <action>
Add tcltest cases:
1. `per_mat_drops_unknown_material`: input `{VDW Glass3 Lines NotAMaterial}` → output has VDW→Glass3, Lines absent (unknown value dropped, key not emitted).
2. `per_mat_drops_non_game_rep_keys`: input key `Ribbon` (valid VMD style, NOT in GAME_REPS) with any value → dropped.
3. `per_mat_preserves_input_order`: input `{VDW Glass3 Lines Translucent}` → output keys in the SAME order (VDW then Lines) — pin the own-order discipline so a future refactor to GAME_REPS-order here is a conscious choice.
4. `per_mat_valid_multi_preserved`: input `{Cartoon EdgyGlass VDW GameBlend}` → both survive with values intact.
5. `material_blending_bool_coercion`: truthy inputs "yes"/1/true → 1 AND "no" → 1; falsy inputs 0/false/False/"" → 0 — mirror _to_bool's EXACT established behavior (its falsy set is only empty-string/0/false/False, setup_state.tcl:63-66; any other non-empty string including "no" coerces to 1). Pin the "no" → 1 quirk deliberately and note it in the test's comment so material_blending and lock_scene share one documented semantics.
6. `per_mat_shape_garbage_yields_empty`: per_mat set to a non-dict (e.g. the string "junk") → validate returns per_mat {} without error.
7. `per_mat_and_per_rep_independent`: a per_rep-clamped input (overflow per_rep) does not disturb a valid per_mat, and vice versa.
  </action>
  <verify>
Same headless suite gate as Task 1: Failed=0, full-log 0 `ERROR)` / 0 `bad switch`, 8.6-idiom gate 0.
  </verify>
  <done>7 new validation cases green; the silent-garbage defense is pinned by test at the state layer.</done>
</task>

</tasks>

<verification>
- BCHM_TEST_RESULT Failed=0 (all suites cases including 18-01's), full-log 0 `ERROR)` / 0 `bad switch`, `Exiting normally`.
- 8.6-idiom grep vmd/lib/ = 0.
- Grep confirm: `_to_bool [dict get $state material_blending]` and the `mat_clean` rebuild present in validate_state.
</verification>

<success_criteria>
Validated state can never contain a material name outside the curated set or a rep key outside GAME_REPS — the GUI and .bcm paths are garbage-proof before any VMD command runs.
</success_criteria>

<output>
After completion, create `.planning/phases/18-materials-exploration/18-03-SUMMARY.md`
</output>
