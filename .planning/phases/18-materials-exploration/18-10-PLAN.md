---
phase: 18-materials-exploration
plan: 10
type: execute
wave: 4
depends_on: ["18-07", "18-08"]
files_modified: [vmd/gui/dialog.tcl]
autonomous: true

must_haves:
  truths:
    - "on_start calls the 5-arg start_game: per_mat threads from the collected state ONLY when material_blending is on; toggle off → per_mat {} → no blending"
    - "The toggle gating happens at the GUI boundary; start_game's empty-{}-means-off rule keeps console/smoke callers unchanged"
    - "dialog.tcl loads headless-clean (the entry sources it; 0 ERROR lines)"
  artifacts:
    - path: "vmd/gui/dialog.tcl"
      provides: "on_start 5-arg call with toggle gating + comment update"
      contains: "material_blending"
  key_links:
    - from: "dialog.tcl on_start"
      to: "game::start_game 5th arg"
      via: "set pm {}; if blending on → pm = per_mat"
      pattern: "material_blending"
---

<objective>
Thread the GUI's material state into the engine: on_start (dialog.tcl step 4) becomes the 5-arg call, gated on the material_blending toggle collected by setup_tab (18-08). GUI-only file — headless load-gate verification; the real click-path is 18-15's checkpoint.

Purpose: completes MATERIAL-02's GUI→engine path so a GUI-started round blends exactly like a console round with the same per_mat (one derivation semantics — 17.1-14's locked decision).
Output: dialog.tcl 5-arg threading, headless-clean.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-materials-exploration/18-RESEARCH-codebase-integration.md
@vmd/gui/dialog.tcl
@vmd/gui/setup_tab.tcl

Load-bearing research: integration research §3.5 (the exact on_start snippet: `set pm {}`; `if {[dict get $state material_blending]} { set pm [dict get $state per_mat] }`; 5-arg call) and §1.4 (caller table — on_start 209-212). The state dict comes from collect_state which ALWAYS emits both keys post-18-08, but keep a dict-exists defensive guard mirroring restart's pattern (454-463). Comment hygiene for the 8.6 gate.
</context>

<tasks>

<task type="auto">
  <name>Task 1: on_start 5-arg threading + toggle gating</name>
  <files>vmd/gui/dialog.tcl</files>
  <action>
1. In on_start (step 4, 209-212), before the start_game call build the gated arg:
   ```tcl
   # 18-10: material blending is gated AT THE GUI BOUNDARY -- toggle off
   # passes per_mat {} (start_game's empty-means-off rule keeps console
   # + smoke callers byte-identical).
   set pm [dict create]
   if {[dict exists $state material_blending] && [dict get $state material_blending]
           && [dict exists $state per_mat]} {
       set pm [dict get $state per_mat]
   }
   ```
   (line-continuate per the file's style; keep the catch wrapper around start_game unchanged).
2. Extend the call to 5 args: `::biochemeleon::game::start_game $molid [dict get $state hider_count] [dict get $state per_rep] [dict get $state lock_scene] $pm`.
3. Update the step-4 comment (the 17.1-14 note) to mention the 5th arg + toggle gating.
  </action>
  <verify>
Headless: source the ENTRY (biochemeleon.tcl) from fresh staging under `vmd -dispdev text` → 0 `ERROR)` / 0 `bad switch`, `Exiting normally` (this gates ONLY 18-10's own edit: the entry sources dialog + setup_tab and must still load clean — NOTE the staged tree does NOT source materials.tcl or fire ensure_gameblend, because that entry wiring is 18-09's deliverable and 18-09 is a same-wave parallel sibling in a separate worktree; the entry-sources-materials/ensure-fires proof lives in 18-09's verify). Static: grep dialog.tcl for `material_blending` + the 5-arg call with `$pm`. 8.6-idiom gate 0; grab-set gate 0.
  </verify>
  <done>GUI Start threads per_mat only when the toggle is on; the file loads clean in the full entry chain.</done>
</task>

</tasks>

<verification>
- Entry-level headless load: 0 `ERROR)` / 0 `bad switch`, `Exiting normally`.
- 8.6-idiom + grab-set greps = 0.
- Grep: 5-arg start_game call with $pm; defensive dict-exists guards.
</verification>

<success_criteria>
The GUI's Start button produces exactly the same material-blended round a console caller would — same resolution, same degrade, same stash.
</success_criteria>

<output>
After completion, create `.planning/phases/18-materials-exploration/18-10-SUMMARY.md`
</output>
