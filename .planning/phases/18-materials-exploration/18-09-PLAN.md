---
phase: 18-materials-exploration
plan: 09
type: execute
wave: 4
depends_on: ["18-07"]
files_modified: [vmd/biochemeleon.tcl, vmd/smoke/phase18_materials_smoke.tcl]
autonomous: true

must_haves:
  truths:
    - "The entry sources materials.tcl (after hiders.tcl, before game.tcl) and calls ensure_gameblend once, catch-guarded — GameBlend exists in every real session"
    - "A 5-arg start_game round with per_mat {VDW GameBlend} produces hider reps (hidden AND found) reading back material == GameBlend while scene reps and stock Opaque stay untouched"
    - "Tachyon render-proof BEYOND read-back: the hiders-only render's .dat carries `Opacity 0.85` and `Phong Plastic 0.65` tokens; the Opaque baseline carries neither"
    - "A 4-arg (blending-off) round produces hider material == Opaque and gs per_mat == {} — byte-identical pre-18 observable behavior"
    - "Cleanup restores the original with its pre-round material NAME and Opaque's settings pristine (no leakage)"
  artifacts:
    - path: "vmd/biochemeleon.tcl"
      provides: "materials.tcl source line + ensure_gameblend call + order-comment update"
      contains: "materials.tcl"
    - path: "vmd/smoke/phase18_materials_smoke.tcl"
      provides: "The phase's core headless proof: GameBlend VDW round, blending-off round, Tachyon token proof, cleanup fidelity"
      contains: "BCHM_SMOKE_RESULT"
  key_links:
    - from: "phase18_materials_smoke round A"
      to: "Tachyon .dat material tokens"
      via: "regexp-anchored token asserts on the hiders-only render"
      pattern: "Opacity 0\\\\.85(\\\\s|\\$)"
    - from: "game_state"
      to: "resolved per_mat stash"
      via: "5-key dict assert in the smoke"
      pattern: "per_mat"
---

<objective>
Two deliverables: (1) wire materials.tcl into the entry (source + one catch-guarded ensure_gameblend call), (2) build the phase's CORE headless smoke — the composition seam proven end-to-end with the Tachyon token render-proof, on the VDW (simple free) tier class.

Purpose: SC1's headless half. molinfo read-back proves assignment; the Tachyon `Opacity`/`Phong Plastic` tokens prove it BEYOND read-back (the repo's scene-diff discipline extended to materials — materials research Layer 2). Round B pins the blending-off default.
Output: entry wiring + phase18_materials_smoke.tcl, PASS=1 ×3.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-materials-exploration/18-RESEARCH-vmd-materials.md
@.planning/phases/18-materials-exploration/18-RESEARCH-codebase-integration.md
@vmd/biochemeleon.tcl
@vmd/smoke/phase17_points_smoke.tcl
@vmd/smoke/phase17_licorice_smoke.tcl

Load-bearing research: materials research "Headless test strategy" Layer 2 (token mapping: `Opacity <v>` = opacity verbatim shortest form; `Phong Plastic <v>` = specular; pin PRESENCE + distinct-value, never absolute counts — scene-scale dependent; `Opacity 0.8` vs `Opacity 0.85` is a PREFIX TRAP → anchor with `(\s|$)`), Pitfall 4 (molinfo combined braces; scan logs for `out of range` too), restore/rollback block; integration research §1 (seam), §5.2-5.3 (render machinery inherited from 17.1-08/10/12: _parse_dat, emptied-reps pattern, axes location off, probe rep LAST, baseline-zero proof), §5.4 gate checklist. Smoke template: phase17_points_smoke.tcl (structure) + phase17_licorice_smoke.tcl (_parse_dat 124-148, restored-style assert 582-610).
</context>

<tasks>

<task type="auto">
  <name>Task 1: entry wiring + smoke rounds A/B + cleanup fidelity</name>
  <files>vmd/biochemeleon.tcl, vmd/smoke/phase18_materials_smoke.tcl</files>
  <action>
1. biochemeleon.tcl: add `source [file join $_dir lib materials.tcl]` between hiders.tcl and game.tcl (mol-bridge block; materials sources NOTHING and game references it at CALL time — update the long "Order matters" comment to include materials). AFTER the source block (top level), add ONE call: `if {[catch {::biochemeleon::materials::ensure_gameblend} _gberr]} { catch {vmdcon -warn "bioCHEMeleon: GameBlend init failed: $_gberr"} }` — a failure warns and never aborts the extension load (warning-class discipline).
2. phase18_materials_smoke.tcl (NEW; copy the phase17_points_smoke.tcl structure + licorice's _parse_dat helpers):
   - Header documenting the phase contract. Source the full lib chain the capstone uses PLUS materials.tcl (setup_state, materials, registry, generators, game_logic, rep_tiers, backup, mutation, hiders, game — the 17.x smoke source-list convention; NO GUI).
   - Round A (GameBlend VDW end-to-end on 1znf): `set gs [start_game $m 3 {VDW 3} 0 {VDW GameBlend}]` (5-arg). Asserts: `dict size $gs` == 5 with key set {game_molid hider_count snapshot per_rep per_mat}; stashed per_mat == {VDW GameBlend} (resolved, GAME_REPS-ordered trivially); `material settings GameBlend` lindex 5 == 0.85 and lindex 1 == 0.65 (recipe pin, eps 1e-6); resolve hider rep indices via tier_reps + `mol repindex` (-1 guards) and assert hidden AND found reps read back `GameBlend` via `molinfo get "{material $idx}"` (combined braces); scene rep 0 material UNCHANGED (capture pre-round); `material settings Opaque` eq its pre-round 9-value capture (blast-radius guard). One find via game::on_pick on a hider index → remaining {VDW 2} + the found rep STILL reads GameBlend.
   - Round B (blending-off regression): `start_game $m2 2 {VDW 2} 0` (4-arg) → gs per_mat == {} and BOTH new hider reps read back `Opaque`.
   - Cleanup fidelity (per round): registry::count_hiders 0, game molid dead, original atom count + numreps restored, restored scene rep-0 material NAME == pre-round name (mirror licorice's restored-style assert), Opaque settings STILL eq the pristine capture.
  </action>
  <verify>
Headless ×3 (fresh staging, SEQUENTIAL — shared $env(TEMP) PDB): `BCHM_SMOKE_RESULT PASS=1` every run; full-log 0 `ERROR)` / 0 `bad switch` / 0 `out of range`; `Exiting normally` every run. 8.6-idiom gate 0. Entry-load gate: source the ENTRY (biochemeleon.tcl) from fresh staging under `vmd -dispdev text` → 0 `ERROR)` / 0 `bad switch`, `Exiting normally` — this is where the entry-sources-materials + ensure_gameblend-fires proof is proven at runtime (the wired entry loads clean and the catch-guarded ensure call does not abort the load; 18-10, a same-wave parallel sibling, deliberately does NOT own this gate — its worktree lacks this wiring).
  </verify>
  <done>Entry wired; Round A proves the material seam end-to-end with isolation + blast-radius guards; Round B proves off-by-default; cleanup restores material-name fidelity.</done>
</task>

<task type="auto">
  <name>Task 2: Tachyon token render-proof (hiders-only vs Opaque baseline)</name>
  <files>vmd/smoke/phase18_materials_smoke.tcl</files>
  <action>
1. In Round A, add the render sub-proof (17.1-08 template): `axes location off`; empty all scene reps via `mol modselect $i $m {index 999999}` (mol showrep off is IGNORED in text mode); add ONE probe rep LAST (deleted LAST) selecting the 3 hiders with GameBlend; `render Tachyon <staging>/p18_a.dat`.
   - Assert `regexp -- {Opacity 0\.85(\s|$)}` matches the .dat (the ANCHORED form — `Opacity 0.8` must NOT match `Opacity 0.85`; the leading-dash `regexp --` lesson from 15-orchestrator) and `regexp -- {Phong Plastic 0\.65(\s|$)}` matches (specular token — research token mapping).
   - Baseline: before Round A (or a separate molecule/rep state), render the SAME scene selection with Opaque → assert NEITHER `Opacity 0.85` NOR `Phong Plastic 0.65` appears in the baseline .dat (both-endpoints rule: presence-in-blended + absence-in-baseline isolates the material signal).
   - Missing-file-parses-as-failure guard (licorice harness rule): a missing .dat is an assert failure, never a pass.
2. First-render inspection discipline: before pinning, run ONE inspection render and confirm the tokens appear exactly as the research documents (Opacity 0.85 / Phong Plastic 0.65 in the hiders-only render); adjust pins ONLY if the inspection shows a different token shape, and record the finding in the SUMMARY (17.1-08 plan-prescribed-first-render precedent).
3. Keep absolute token counts UNPINNED (scene-scale dependent — pin presence/absence only).
  </action>
  <verify>
Same ×3 gate as Task 1; additionally grep the smoke for the anchored regexps (`Opacity 0\\.85` and `Phong Plastic 0\\.65`) and the baseline absence asserts. Full-log scans unchanged.
  </verify>
  <done>The material assignment is proven at the RENDER level (Tachyon tokens), not just read-back — with the 0.8/0.85 prefix trap defused by anchoring.</done>
</task>

</tasks>

<verification>
- phase18_materials_smoke PASS=1 ×3, full-log 0 `ERROR)` / 0 `bad switch` / 0 `out of range`, `Exiting normally` ×3.
- biochemeleon.tcl: materials.tcl sourced between hiders and game; exactly ONE ensure_gameblend call, catch-guarded.
- Entry-level headless load: 0 `ERROR)` / 0 `bad switch`, `Exiting normally` (proves the wired entry loads and ensure_gameblend fires catch-guarded at runtime).
- 8.6-idiom grep = 0.
</verification>

<success_criteria>
SC1's headless half is done: a material-blended round is generated, played (find registers), rendered-proven, and cleaned up without leaking material state — and turning the feature off reproduces pre-18 behavior exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/18-materials-exploration/18-09-SUMMARY.md`
</output>
