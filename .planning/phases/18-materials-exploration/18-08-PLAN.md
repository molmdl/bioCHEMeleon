---
phase: 18-materials-exploration
plan: 08
type: execute
wave: 3
depends_on: ["18-06"]
files_modified: [vmd/gui/setup_tab.tcl]
autonomous: true

must_haves:
  truths:
    - "collect_state emits material_blending (bool) and per_mat (GAME_REPS-ordered dict of checked reps with non-empty picked materials)"
    - "apply_state populates mat_blend + rep_mat($rep) from the state dict (missing keys → defaults via _dget), syncs the picker labels, and runs the grey-out coupling"
    - "The save-warning diff reporter covers material_blending and summarizes per_mat"
    - "_loading discipline preserved: value clamping guarded, enable/disable + label sync unguarded (the on_rep_toggled precedent)"
  artifacts:
    - path: "vmd/gui/setup_tab.tcl"
      provides: "collect_state material keys, apply_state per_mat pass, do_save diff additions"
      contains: "material_blending"
  key_links:
    - from: "collect_state"
      to: "on_start (dialog.tcl, 18-10)"
      via: "state dict keys material_blending + per_mat read by on_start's gating"
      pattern: "material_blending \\[expr \\{\\!\\!\\$mat_blend\\}\\]"
    - from: "apply_state per_mat pass"
      to: "sync_mat_label + on_mat_changed (18-06)"
      via: "label sync + coupling re-run inside the guarded catch block"
      pattern: "sync_mat_label"
---

<objective>
Thread material state through the Setup tab's data plane: collect_state emits the two new keys, apply_state populates the widgets from them (the _loading-guarded pass), and the do_save diff reporter stays honest about blending changes. GUI-only file — headless load-gate verification; behavioral proof comes from the 18-13 driver round and the 18-15 checkpoint.

Purpose: closes the GUI's state loop (form ↔ validated state ↔ .bcm) so on_start (18-10) can gate on material_blending and pass per_mat through.
Output: setup_tab.tcl collect/apply/diff wiring, headless-clean.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-materials-exploration/18-RESEARCH-codebase-integration.md
@vmd/gui/setup_tab.tcl

Load-bearing research: integration research §3.5 (collect/apply/on_start threading — collect emits `material_blending [expr {!!$mat_blend}]` and a GAME_REPS-ordered per_mat built only from checked reps with non-empty values, mirroring the per_rep build at 266-272; apply drives the pickers AFTER the per_rep pass then re-runs the coupling; do_save's diff list at 641-657 gains the new keys) and §3.3 (the _loading discipline: enable/disable OUTSIDE the guard). `_dget` (setup_tab.tcl:56-59) is the only dict-get-with-default in tcl 8.5 — use it for the new keys.
</context>

<tasks>

<task type="auto">
  <name>Task 1: collect_state + apply_state material passes</name>
  <files>vmd/gui/setup_tab.tcl</files>
  <action>
1. collect_state (251-285): add `variable mat_blend` and `variable rep_mat` to the link block (one per line); build per_mat mirroring the per_rep build (266-272): `set per_mat [dict create]` — CAREFUL: per_rep is already declared in this proc; name the local `per_mat`; `foreach rep $::biochemeleon::setup_state::GAME_REPS { if {[info exists rep_sel($rep)] && $rep_sel($rep)} { if {[info exists rep_mat($rep)] && $rep_mat($rep) ne ""} { dict set per_mat $rep $rep_mat($rep) } } }` (only checked reps with a non-empty pick; unchecked/empty = default → absent). Add to the returned dict: `material_blending [expr {!!$mat_blend}]` and `per_mat $per_mat` — after pdb_pool, matching DEFAULTS order.
2. apply_state (297-345): inside the guarded catch block, after the per_rep pass and its on_rep_toggled loop:
   - `set mat_blend [_dget $state material_blending 0]` (int coerce: `[expr {[string is integer -strict [set _mb [_dget $state material_blending 0]]] ? $_mb : 0}]` — keep it simple: _dget then truthiness via the same style as lock_scene's set).
   - per_mat pass: `set pm [_dget $state per_mat [dict create]]` with a catch-based shape guard (non-dict → empty); `foreach rep $::biochemeleon::setup_state::GAME_REPS { set rep_mat($rep) [expr {[dict exists $pm $rep] ? [dict get $pm $rep] : ""}]; ::biochemeleon::setup_tab::sync_mat_label $rep }`.
   - Coupling: the per-rep on_rep_toggled loop already fires on_mat_changed (18-06 tail); after the per_mat pass, call `::biochemeleon::setup_tab::on_mat_changed` once more to converge states.
   - All of this sits INSIDE the existing catch (value-setting is guarded — the _loading discipline); sync_mat_label/on_mat_changed reconfigure unguarded internally per the established split.
3. Comment hygiene (8.6-idiom gate).
  </action>
  <verify>
Headless load-gate (source setup_state + setup_tab under `vmd -dispdev text`): 0 `ERROR)` / 0 `bad switch`, `Exiting normally`. Static: grep collect_state for `material_blending [expr` + the per_mat build; grep apply_state for the per_mat pass + sync_mat_label. grab-set gate 0; 8.6-idiom gate 0. Behavior is GUI-only — proven by 18-13/18-15.
  </verify>
  <done>collect_state emits both keys in DEFAULTS order; apply_state round-trips them into the widgets; _loading discipline intact.</done>
</task>

<task type="auto">
  <name>Task 2: do_save diff reporter — material lines</name>
  <files>vmd/gui/setup_tab.tcl</files>
  <action>
do_save's diff reporter (641-657): add material_blending to the scalar-diff key list so toggling blending flags the save-warning; add a per_mat summary line (e.g. `material picks: VDW=Glass3 Lines=(default) ...` built from rep_mat in GAME_REPS order, or a compact count + changes) so a picker change that isn't saved is surfaced like the other unsaved changes. Keep the existing diff keys and behavior byte-identical otherwise.
  </action>
  <verify>
Headless load-gate clean again. Static: grep do_save's diff section for material_blending + per_mat. 8.6 + grab-set gates 0.
  </verify>
  <done>The save-warning is honest about blending/picker changes.</done>
</task>

</tasks>

<verification>
- Headless source: 0 `ERROR)` / 0 `bad switch`, `Exiting normally`.
- grab-set grep = 0; 8.6-idiom grep = 0.
- Grep: collect emits both keys; apply reads both via _dget + guards; diff reporter mentions both.
</verification>

<success_criteria>
The Setup form's material state flows collect → validate → apply → collect losslessly, matching the DEFAULTS key order the save/load and game threads expect.
</success_criteria>

<output>
After completion, create `.planning/phases/18-materials-exploration/18-08-SUMMARY.md`
</output>
