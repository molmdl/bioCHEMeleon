---
phase: 18-materials-exploration
plan: 13
type: execute
wave: 5
depends_on: ["18-10"]
files_modified: [vmd/tests/rep_verify.tcl]
autonomous: true

must_haves:
  truths:
    - "REQUIRES Phase 17.2 closed — 17.2-12 checkpoint approved — before execution (the live 17.2-12 session reads this exact file)"
    - "pv_round4 crafts a material-blending state through the REAL path (validate_state → apply_state → on_start) with material keys surviving validation"
    - "pv_state auto-logs material=<name> for hidden AND found reps of every tier (read-back evidence without human parsing)"
    - "Session budget preserved: ≤3 short pastes (pv_round4, pv_report, pv_cleanup); instructions lead with the first-click p-press quirk"
  artifacts:
    - path: "vmd/tests/rep_verify.tcl"
      provides: "pv_round4 materials round, pv_round3→pv_round4 handoff, pv_instructions round-4 step, pv_state material logging"
      contains: "pv_round4"
  key_links:
    - from: "pv_round4 crafted state"
      to: "validate_state material keys (18-01/18-03)"
      via: "material_blending 1 + per_mat dict in the crafted state"
      pattern: "material_blending 1"
    - from: "pv_state tier dump"
      to: "molinfo {material $r} read-back"
      via: "the dump already reads mat — now logs it"
      pattern: "material=\\$mat"
---

<objective>
Extend the rep_verify GUI auto-driver for the materials round: pv_round4 (a 3-hider round spanning the curated set — GameBlend, Translucent, Glass3), material read-back logging in pv_state, and the handoff/instructions updates that keep the session at ≤3 short pastes.

REQUIRES Phase 17.2 closed — 17.2-12 checkpoint approved — before execution. The pending 17.2-12 GUI session is reading this exact file; editing it earlier would corrupt the live session.

Purpose: the 18-15 checkpoint's driver half — the auto-issued commands and auto-logged evidence that let ONE human session validate the materials feature (STATE standing directive: GUI sessions simple, driver auto-issues).
Output: rep_verify.tcl pv_round4 + material logging, headless definition-gate green.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-materials-exploration/18-RESEARCH-vmd-materials.md
@.planning/phases/18-materials-exploration/18-RESEARCH-codebase-integration.md
@vmd/tests/rep_verify.tcl

Load-bearing research: integration research §6 (pv_round4 extension shape: copy pv_round3's skeleton 1:1 — fresh 1znf target via load_demo, crafted state via validate_state+apply_state+on_start, catch-unset ::pv_gs before start, ::pv_rounds 4, after 4500 pv_state; pv_state gain: add material=$mat to the hidden line ~265 and found line ~272 — the dump ALREADY reads mat via the 4-keyword molinfo form but never logs it; handoff: pv_round3's closing log routes to pv_round4; instructions gain the round-4 step). Standing rules: ≤3 pastes (pv_round4/pv_report/pv_cleanup), instructions LEAD with the p-press quirk (pv_instructions:515-517), driver auto-issues everything else. 17.2-12's definition-gate pattern (fake ::tk_version + ::pv_probe knob) for the headless gate.
</context>

<tasks>

<task type="auto">
  <name>Task 1: pv_round4 + handoff + instructions updates</name>
  <files>vmd/tests/rep_verify.tcl</files>
  <action>
1. New proc `pv_round4 {}` — copy pv_round3's skeleton (375-423) 1:1 with these changes:
   - Fresh 1znf target via `::biochemeleon::demos::load_demo 1znf` (never reuse a mutated molid); log the molid + expected game atoms (orig + 3).
   - Crafted state WITH the new keys: `dict create target_mode loaded selected_object $m4 hider_count 3 per_rep [dict create VDW 1 Lines 1 CPK 1] material_blending 1 per_mat [dict create VDW Glass3 Lines Translucent CPK GameBlend] lock_scene 0 difficulty_easy 1 demo_id 1znf` → validate_state (the material keys MUST survive — this is the real-path proof that GAME_MATERIALS accepts all three) → apply_state → `catch {unset ::pv_gs}` → `::biochemeleon::on_start` → `set ::pv_rounds 4` → `after 4500 pv_state`.
   - Log guidance: press p ONCE (the quirk), find the 3 blended hiders — click THROUGH the transparency (Glass3 is half-transparent; picks should still land — that is the pick-through evidence), found turns green THROUGH the material.
2. Handoff: pv_round3's closing log lines (currently "Then paste: pv_report, then pv_cleanup") now route "paste pv_round4 (MATERIALS round) first — then pv_report + pv_cleanup". pv_instructions gains a round-4 line between the round-3 step and the final report step (keep it ≤ 9 short lines total — renumber if needed).
3. Comment hygiene (8.6-idiom gate greps this file's comments too? — the gate greps lib/+gui/ only, but keep the file's style consistent).
  </action>
  <verify>
Static: grep rep_verify.tcl for `pv_round4` (proc + handoff + instructions), `material_blending 1`, `Glass3`. Headless definition-gate (17.2-12 pattern): fake `::tk_version` + `::pv_probe` knob run in text mode → all pv_* procs defined including pv_round4, entry resolves, 0 `ERROR)` / 0 `bad switch`, `Exiting normally`.
  </verify>
  <done>pv_round4 exists, the driver routes rounds 1→2→3→4→report→cleanup, and the materials state survives the real validate→apply→on_start path (definition-gate proven headlessly).</done>
</task>

<task type="auto">
  <name>Task 2: pv_state material logging</name>
  <files>vmd/tests/rep_verify.tcl</files>
  <action>
In pv_state's per-tier dump (the loop that reads `foreach {sty selx col mat} [molinfo ... {material $r}]` around lines 264-272): add `material=$mat` to BOTH the hidden-rep and found-rep log lines. This makes material read-back AUTO-LOGGED evidence — the 18-15 session's verdicts cite log lines, not human memory. Keep the rest of the dump byte-identical (17.2-12's session-1 log format must stay comparable).
  </action>
  <verify>
Headless definition-gate again (proc still parses; the pv_probe knob run logs the definition list). Static: grep for `material=\$mat` in the two log lines.
  </verify>
  <done>Every tier dump line now carries its material read-back — the checkpoint's evidence is automatic.</done>
</task>

</tasks>

<verification>
- Definition-gate headless run: all pv_* procs defined (incl. pv_round4), 0 `ERROR)` / 0 `bad switch`, `Exiting normally`.
- Static: handoff chain pv_round2→pv_round3→pv_round4→pv_report/pv_cleanup; instructions ≤ 9 lines and LEAD with the p-press quirk.
- grab-set gate 0.
</verification>

<success_criteria>
The GUI driver can walk a human through the materials round with 3 pastes and auto-logged material evidence — pending only the 17.2-12 gate opening.
</success_criteria>

<output>
After completion, create `.planning/phases/18-materials-exploration/18-13-SUMMARY.md`
</output>
