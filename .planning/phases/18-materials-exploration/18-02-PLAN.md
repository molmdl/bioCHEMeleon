---
phase: 18-materials-exploration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [vmd/lib/hiders.tcl]
autonomous: true

must_haves:
  truths:
    - "add_hider_reps accepts an optional 3rd arg mat_specs ({code materialName} pairs) and assigns the material to BOTH the hidden and found rep of each matching tier pair"
    - "A material read-back mismatch on a material-carrying pair is a HARD error (P-1 discipline) — a silent modmaterial no-op can never pass unnoticed"
    - "1-arg and 2-arg calls are byte-compatible with pre-18 behavior (no mat_specs → zero material application)"
    - "tier_materials tracks code → material in parallel to tier_styles, reset per call"
  artifacts:
    - path: "vmd/lib/hiders.tcl"
      provides: "mat_specs application, tier_materials dict, extended P-1 read-back"
      contains: "mat_specs"
  key_links:
    - from: "vmd/lib/game.tcl step 12 (18-07)"
      to: "vmd/lib/hiders.tcl add_hider_reps 3rd arg"
      via: "add_hider_reps $game_molid $specs $mat_specs"
      pattern: "mat_specs"
    - from: "hiders.tcl read-back"
      to: "molinfo combined-braces 4-keyword form"
      via: "rmat already read in the existing loop — extend the compare"
      pattern: "rmat ne \\$mat"
---

<objective>
Give hiders.tcl the material-application half of the seam: an optional 3rd `mat_specs` arg on add_hider_reps that runs `mol modmaterial` on both reps of each tier pair, validated by the existing P-1 read-back loop. Self-contained (wave 1, parallel with 18-01 — hiders.tcl references no new setup_state constants).

Purpose: MATERIAL-01's `mol modmaterial` per-rep assignment lives in the "visual half" module (hiders.tcl), per the integration research §1.2 — game.tcl delegates ALL rep manipulation to the mol bridges. The found transition must stay COLOR-only (ColorID 7 green); material is layered on top, not swapped.
Output: vmd/lib/hiders.tcl with byte-compatible 3-arg extension + a headless probe proving application and isolation.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-materials-exploration/18-RESEARCH-vmd-materials.md
@.planning/phases/18-materials-exploration/18-RESEARCH-codebase-integration.md
@vmd/lib/hiders.tcl

Load-bearing research sections: integration research §1.2 points 1-3 (seam mechanics, BOTH reps, P-1 read-back extension, tier_materials); materials research `mol modmaterial` row (per-rep isolated; INVALID name = silent no-op — hence the read-back is the only reliable gate) and Pitfall 4 (molinfo combined-braces syntax; out-of-range scans). P-1 read-back precedent is already in hiders.tcl lines 171-177.
</context>

<tasks>

<task type="auto">
  <name>Task 1: add_hider_reps mat_specs arg + application + tier_materials + read-back</name>
  <files>vmd/lib/hiders.tcl</files>
  <action>
1. Widen the signature: `proc ::biochemeleon::hiders::add_hider_reps {molid {tier_specs {{1 VDW}}} {mat_specs {}}}` — additive, all existing callers (1-arg/2-arg) byte-compatible (research: verified additive-defaults pattern from 17.1-06's 2→4-arg widening).
2. Inside the per-spec loop (after `set sargs ...`), resolve this tier's material: `set mat ""` then `foreach ms $mat_specs { if {[lindex $ms 0] eq $code} { set mat [lindex $ms 1] } }` (a code appears at most once — mat_specs is built 1:1 with tier_specs by game.tcl). Empty mat = no material application for this pair.
3. In the hidden/found rep block, AFTER the existing `mol modselect` calls (both reps fully defined first): if `$mat ne ""` → `mol modmaterial $hidx $molid $mat` and `mol modmaterial $fidx $molid $mat`. Do NOT touch modcolor — found stays ColorID 7 green; the material multiplies the style's blend, it does not replace the found transition.
4. Extend the P-1 read-back loop (lines ~171-177): the loop already reads `rmat` via the combined-braces 4-keyword molinfo form and ignores it. When `$mat ne ""`, add `$rmat ne $mat` to the mismatch condition and extend the error message with `mat='$mat' got_mat='$rmat'`. Rationale: `mol modmaterial` with an invalid name is a SILENT no-op (research C4) — read-back string-compare is the only reliable gate, exactly like the existing style/selection compares.
5. Add `variable tier_materials` alongside tier_styles; reset it to `[dict create]` at the top of the proc (the same reset-per-call contract as tier_reps/tier_styles — stale entries would break the next round); after the read-back block: `dict set tier_materials $code $mat` (empty string when no material — deterministic shape, order-parallel with tier_styles).
6. Update the proc's header comment: mat_specs contract ({code materialName} pairs parallel to tier_specs), the BOTH-reps rule, the found-transition-stays-color-only rule, and the byte-compat note. Comment hygiene: avoid 8.6-idiom grep literals.
  </action>
  <verify>
Static: grep hiders.tcl for `mat_specs` (present in signature + application + doc), `tier_materials` (declared + reset + set), and `rmat ne $mat` (read-back extension). 8.6-idiom grep vmd/lib/ = 0.
Headless probe (gitignored `tmp/p18-02-probe/` staging copy): a small script that (a) sources setup_state.tcl + hiders.tcl, (b) `mol new` the bundled 1znf demo PDB directly (NO mutation — this probe never writes the shared $env(TEMP) PDB), (c) stamps user3 via `hiders::stamp_tier_codes $m [dict create 1 <3 real indices>]`, (d) calls `add_hider_reps $m {{1 VDW}} {{1 Translucent}}` (Translucent = STOCK, exists without materials.tcl — GameBlend does not exist yet, 18-04 owns it), (e) asserts BOTH new reps read back material == Translucent via `molinfo $m get "{material $idx}"` (combined braces — Pitfall 4), (f) asserts scene rep 0 still reads Opaque (isolation), (g) calls the 2-arg form on a fresh mol and asserts both reps read Opaque (byte-compat). Run headless: log has 0 `ERROR)`, 0 `bad switch`, 0 `out of range`, `Exiting normally`.
  </verify>
  <done>3-arg call applies the material to both pair reps with hard-error read-back validation; 1/2-arg behavior unchanged; tier_materials dict populated; isolation proven (scene rep untouched).</done>
</task>

<task type="auto">
  <name>Task 2: byte-compat regression — phase16_hiders_smoke stays green</name>
  <files>vmd/lib/hiders.tcl</files>
  <action>
Run `vmd/smoke/phase16_hiders_smoke.tcl` (the 1-arg add_hider_reps consumer, user3-stamped) headless from fresh staging. It must stay PASS=1 with ZERO edits — this is the byte-compat guarantee for every pre-18 caller (phase16_hiders_smoke, game.tcl pre-18 call sites, 17.x smokes). If it fails, the extension leaked behavior into the 2-arg path — fix hiders.tcl, not the smoke.
  </action>
  <verify>
`bash -ic 'cd <staging> && vmd -dispdev text -e vmd/smoke/phase16_hiders_smoke.tcl -eofexit < /dev/null'` → `BCHM_SMOKE_RESULT PASS=1`; full-log scan 0 `ERROR)` / 0 `bad switch`; `Exiting normally`. Sequential VMD runs only.
  </verify>
  <done>phase16_hiders_smoke PASS=1 unmodified — the additive arg is proven invisible to legacy callers.</done>
</task>

</tasks>

<verification>
- Probe asserts: material applied to BOTH pair reps, scene rep isolated, 2-arg unchanged, read-back error fires on mismatch (optional negative: apply a bogus material name, expect the P-1 error text).
- phase16_hiders_smoke PASS=1, full-log clean.
- 8.6-idiom gate over vmd/lib/ = 0.
</verification>

<success_criteria>
The hiders mol bridge accepts per-tier materials with the same rigor as styles (P-1 read-back, hard error on silent no-op) while remaining byte-compatible for every existing caller.
</success_criteria>

<output>
After completion, create `.planning/phases/18-materials-exploration/18-02-SUMMARY.md`
</output>
