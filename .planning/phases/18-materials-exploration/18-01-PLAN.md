---
phase: 18-materials-exploration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [vmd/lib/setup_state.tcl, vmd/tests/test_setup_state.test]
autonomous: true

must_haves:
  truths:
    - "validate_state / randomize_state output dicts carry material_blending (default 0) and per_mat (default {}) as their last two keys in DEFAULTS key order"
    - "Old dicts without the new keys validate to material_blending 0 / per_mat {} — backward compat with NO version bump"
    - "The shipped curated material set is exactly {GameBlend Translucent Glass2 EdgyGlass Glass3}; Glass1/Ghost are NOT in it (gated pending the 18-15 pick-through verdict)"
  artifacts:
    - path: "vmd/lib/setup_state.tcl"
      provides: "GAME_MATERIALS + DEFAULT_MATERIAL constants, DEFAULTS material_blending/per_mat keys, randomize_state keys, namespace exports"
      contains: "GAME_MATERIALS"
    - path: "vmd/tests/test_setup_state.test"
      provides: "Suite cases for new keys, backward compat, randomize shape, curated-set membership"
      contains: "material_blending"
  key_links:
    - from: "vmd/lib/setup_state.tcl DEFAULTS key order"
      to: "vmd/lib/demos.tcl load_setup rebuild list (extended by 18-05)"
      via: "new keys appended at END so the rebuild-list extension is a two-token append"
      pattern: "material_blending 0"
---

<objective>
Setup-state schema for material blending: add the curated GAME_MATERIALS list + DEFAULT_MATERIAL constant, the two new DEFAULTS keys (material_blending, per_mat), and the randomize_state keys — everything the rest of Phase 18 reads. PURE layer only; validate_state's per_mat cleaning is 18-03, NOT here.

Purpose: MATERIAL-02's curated material set lives in the state layer so GUI, save/load, and the runtime resolver (18-04) share one vocabulary. Appending keys at the END of DEFAULTS preserves the order-stable `eq` discipline every round-trip depends on.
Output: vmd/lib/setup_state.tcl schema keys + test_setup_state.test cases, all 47 pre-existing tests still green.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-materials-exploration/18-RESEARCH-vmd-materials.md
@.planning/phases/18-materials-exploration/18-RESEARCH-codebase-integration.md
@vmd/lib/setup_state.tcl
@vmd/tests/test_setup_state.test

Load-bearing research sections: integration research §2.1 (exact DEFAULTS additions + GAME_MATERIALS membership rationale) and §2.3 (randomize_state); materials research "Recommended curated material set" table (difficulty ladder + dropped-with-reasons list). Order-stability discipline: STATE.md constraint 10; the 8.6-idiom gate greps COMMENTS too (STATE.md constraint 9).
</context>

<tasks>

<task type="auto">
  <name>Task 1: setup_state.tcl — GAME_MATERIALS + DEFAULT_MATERIAL + DEFAULTS keys + randomize_state</name>
  <files>vmd/lib/setup_state.tcl</files>
  <action>
1. Add ONE new constant block next to GAME_REPS (line ~10), with a comment stating the curation rationale:
   - `variable GAME_MATERIALS {GameBlend Translucent Glass2 EdgyGlass Glass3}` — difficulty order (opacity ladder 0.85 → 0.80 → 0.68 → 0.62 → 0.50 per the materials research inventory table). All names are space-free single tokens (verified against vmd-ref/scripts/materials.dat) which keeps the .bcm 2-token `per_mat_entry` parse safe. Comment MUST note: Glass1/Ghost are GATED OUT of the shipped default set pending the Phase 18 GUI checkpoint's pick-through verdict (transparent-but-shown reps may not receive atom picks at opacity ≤ 0.15) — do not add them in this phase. Transparent/BlownGlass/GlassBubble/AO*/RTChrome stay out (dropped-with-reasons in the materials research).
   - `variable DEFAULT_MATERIAL "Translucent"` — the runtime default-fill for active tiers lacking a per_mat entry. Translucent (opacity 0.80) is chosen over GameBlend because it is STOCK (always exists in every VMD session — zero dependency on GameBlend creation landing) and is the mildest rung.
   - Add a `# MATERIAL-02 PEDAGOGY HYPOTHESIS (not a validated fact):` comment noting that materials can leave subtle light/edge artifacts and that spotting them is a gameplay-validation hypothesis pending the 18-15 checkpoint.
2. Append to DEFAULTS (AFTER pdb_pool, at the END — order-stable `eq` discipline, minimizes diff):
   `material_blending 0 \` and `per_mat [dict create]`. Update the key-count comment above DEFAULTS (11 → 13 keys).
3. randomize_state: add `material_blending 0` and `per_mat [dict create]` to the returned `dict create` (lines 281-292). Do NOT randomize blending — Randomize stays non-surprising (research §2.3 recommendation); the keys exist so the returned dict stays validate-shaped for every apply_state/eq round-trip.
4. Add GAME_MATERIALS and DEFAULT_MATERIAL to the `namespace export` list.
5. Comment hygiene: reword any comment that would trip the 8.6-idiom grep (no `lmap|try|throw|tailcall|coroutine|yield|finally` literals anywhere, comments included).
  </action>
  <verify>
Headless suite run from a fresh Windows-visible staging copy (e.g. `tmp/p18-01/`, copy of the vmd/ tree): `bash -ic 'cd tmp/p18-01 && vmd -dispdev text -e vmd/tests/test_setup_state.test -eofexit < /dev/null'` → parse `BCHM_TEST_RESULT` (NOT $?): all existing 47 tests + any new ones, Failed=0. Full-log scan: 0 `ERROR)` AND 0 `bad switch`. Static gate: 8.6-idiom grep over vmd/lib/ = 0 matches. VMD runs are SEQUENTIAL — never two VMD processes at once (shared $env(TEMP)).
  </verify>
  <done>DEFAULTS carries material_blending 0 + per_mat {} as the last two keys; GAME_MATERIALS is exactly the 5-name curated set; randomize_state returns both new keys; all pre-existing suite tests stay green.</done>
</task>

<task type="auto">
  <name>Task 2: test_setup_state.test — new-key cases</name>
  <files>vmd/tests/test_setup_state.test</files>
  <action>
Add tcltest cases (mirror the file's existing style; keep names descriptive):
1. `defaults_carry_new_keys`: `validate_state [dict create]` result contains material_blending == 0 and per_mat == {} and the dict's LAST two keys (key order check via `dict keys`) are material_blending then per_mat.
2. `old_dict_backward_compat`: a v1-era-shaped dict WITHOUT the new keys (e.g. only format/target_mode/hider_count/per_rep) validates to material_blending 0 / per_mat {} — no version bump, no migration.
3. `randomize_returns_new_keys`: `randomize_state <seed> <atom_count> 0 [dict create]` result contains material_blending == 0 and per_mat == {} (NOT randomized — pinned decision).
4. `game_materials_membership`: GAME_MATERIALS == exactly {GameBlend Translucent Glass2 EdgyGlass Glass3} (exact list compare); assert Glass1 and Ghost are NOT members (the gate).
5. `default_material_value`: DEFAULT_MATERIAL == "Translucent".
6. `exports_present`: the namespace export list includes GAME_MATERIALS and DEFAULT_MATERIAL.
7. Update the TWO pre-existing tests that pin the 11-key DEFAULTS shape — they WILL fail after step 2's extension unless their pins move with it:
   - `validate_state_fills_missing_keys` (test_setup_state.test ~lines 75-81): change `-result {11 5e54}` to `-result {13 5e54}`.
   - `randomize_state_has_all_defaults_keys` (~lines 223-230): extend the literal expected key list with `material_blending per_mat` (11 → 13 keys) so its `llength` == check matches.
Do not add validate_state per_mat-cleaning cases here — that is 18-03.
  </action>
  <verify>
Same headless suite command as Task 1: BCHM_TEST_RESULT Failed=0 (all pre-existing + 6 new), full-log 0 `ERROR)` / 0 `bad switch`, 8.6-idiom gate 0.
  </verify>
  <done>New cases green alongside all 47 pre-existing cases; the curated set + DEFAULT_MATERIAL are pinned by test.</done>
</task>

</tasks>

<verification>
- BCHM_TEST_RESULT Failed=0 (47 pre-existing + new cases) from the headless suite run.
- Full-log scan 0 `ERROR)` / 0 `bad switch`; `Exiting normally` in the log.
- `grep -rnE "\blmap\b|\btry\b|\bthrow\b|\btailcall\b|\bcoroutine\b|\byield\b|\bfinally\b" vmd/lib/ vmd/gui/` → 0 matches.
- GAME_MATERIALS literal in setup_state.tcl is exactly `{GameBlend Translucent Glass2 EdgyGlass Glass3}`.
</verification>

<success_criteria>
The state schema speaks materials: any validate_state/randomize_state consumer now sees material_blending + per_mat in DEFAULTS order, old inputs silently get the defaults, and the curated set + default material are constants with tests pinning them.
</success_criteria>

<output>
After completion, create `.planning/phases/18-materials-exploration/18-01-SUMMARY.md`
</output>
