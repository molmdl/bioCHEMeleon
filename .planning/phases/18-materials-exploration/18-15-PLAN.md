---
phase: 18-materials-exploration
plan: 15
type: execute
wave: 8
depends_on: ["18-13", "18-14"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "REQUIRES Phase 17.2 closed — 17.2-12 checkpoint approved — before execution (this is the phase's terminal GUI session)"
    - "A material-blended round is playable end-to-end in the real GUI: toggle on → pickers → Start → click-to-find THROUGH transparent materials → green found-transition visible → win"
    - "The pick-through question is ANSWERED: does clicking a transparent-but-shown hider (Glass3, opacity 0.50) hit the hider or pass through? — this verdict gates any future Glass1/Ghost inclusion"
    - "The pedagogy verdicts are RECORDED as MATERIAL-02's gameplay-validation-hypothesis evidence: visual distinctness, depth-sorting artifacts, transmode exposure, lighting-artifact notes"
    - "Widget behavior human-verified: toggle flips, pickers grey/ungrey with rep selection"
  artifacts:
    - path: ".planning/phases/18-materials-exploration/18-15-SUMMARY.md"
      provides: "Checkpoint verdicts (a)-(g) + the Glass1/Ghost gate decision + any defects for gap closure"
      contains: "VERDICT"
  key_links:
    - from: "human session"
      to: "rep_verify pv_round4 (18-13)"
      via: "driver auto-issues; 3 pastes max; material read-backs auto-logged"
      pattern: "pv_round4"
---

<objective>
The phase's single GUI human-verify checkpoint (LAST plan, autonomous:false): play a material-blended round in real VMD via the rep_verify driver's new pv_round4, verify the pick contract survives transparency, and answer the pedagogy/gating question list that turns MATERIAL-02's hypothesis into recorded evidence.

REQUIRES Phase 17.2 closed — 17.2-12 checkpoint approved — before execution.

Purpose: Tk cannot load headless; this checkpoint is the ONLY proof of the visual/interactive half of SC1-SC3 (17.1-14/17.2-12 precedent).
Output: checkpoint verdicts + the Glass1/Ghost gate decision, recorded in the SUMMARY.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-materials-exploration/18-RESEARCH-vmd-materials.md
@.planning/phases/18-materials-exploration/18-RESEARCH-codebase-integration.md
@vmd/tests/rep_verify.tcl

Load-bearing research: materials research "Layer 3 — GUI checkpoint" (the 5-item must-verify list: pick-through CRITICAL, visual distinctness, depth-sorting artifacts, transmode, lighting) + the curated-set gating rows (Glass1/Ghost gated on the pick-through answer); integration research §6 (driver mechanics + the 5-question human list + the execution-timing constraint). vmd/AGENTS.md FIRST-CLICK QUIRK block. 17.1-14/17.2-12 checkpoint precedents.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: GUI checkpoint — materials round + pedagogy/gating questions</name>
  <what-built>
Complete material blending (MATERIAL-01 + MATERIAL-02): Setup-tab toggle + per-rep curated pickers (GameBlend/Translucent/Glass2/EdgyGlass/Glass3), GUI→engine 5-arg threading, material-carrying hider reps with render-level proof, .bcm persistence, and the rep_verify driver's pv_round4 materials round (3 hiders: VDW/Glass3, Lines/Translucent, CPK/GameBlend — the full curated range in one round).
  </what-built>
  <how-to-verify>
1. Stage the current vmd/ tree to a fresh Windows-visible dir and launch VMD with the GUI (vmd.exe, NOT -dispdev text).
2. cd to the staging root in the VMD console and paste: source vmd/tests/rep_verify.tcl — the driver auto-sources the extension, loads 1k8p, opens the dialog, applies the round-1 state, and starts round 1 (the 17.2-12 session flow, rounds 1-3 as previously approved).
3. IMPORTANT — FIRST-CLICK QUIRK: press p ONCE on the VMD display BEFORE clicking, EACH round. Clicks before a p-press land in label mode and never count.
4. Play rounds 1-3 as in 17.2-12 (6-tier round; lock-scene round; cartoon round — press p, find hiders, found turns green).
5. After round 3's win box, paste: pv_round4 — the MATERIALS round auto-starts (3 hiders: Glass3, Translucent, GameBlend). Press p ONCE. Click each blended hider — click THROUGH the transparency where it looks faint. Watch: does the click HIT (green + remaining drops) or PASS THROUGH to the geometry behind?
6. On the Setup tab (between rounds): flip "Use material blending" off/on and confirm the per-rep material pickers grey out/ungrey (also toggle a rep checkbox with blending on — a picker should enable only for checked reps). 10 seconds, no paste.
7. Paste pv_report, then pv_cleanup.
8. Answer these in your verdict (a)-(g):
   (a) Pick-through: clicking the transparent-but-shown Glass3 hider — hit or pass-through? (Gates any future Glass1/Ghost inclusion.)
   (b) Visual distinctness: does each material read as clearly "off" vs the Opaque scene at game scale (Lines/VDW/CPK tiers)?
   (c) Depth-sorting/draw-order artifacts with multiple translucent reps — pedagogically interesting or game-breaking?
   (d) transmode: worth exposing to users? (Shipped set is all transmode 0; BlownGlass/GlassBubble ship 1 and are excluded — optional one-liner to eyeball it on a spare rep: mol modmaterial 0 <molid> BlownGlass; default answer "no".)
   (e) Lighting: do light 1 on / light 2 on toggles make material artifacts more/less noticeable? (Pedagogy-hypothesis evidence for MATERIAL-02 — lighting is NOT managed in game state.)
   (f) Found-green: does the ColorID 7 found transition stay visible THROUGH each material?
   (g) Any new pick regressions vs the 17.2-12 baseline (first-click behavior, find registration, win box)?
  </how-to-verify>
  <resume-signal>Type "approved" (optionally with the (a)-(g) answers) or list the issues found — issues become gap-closure plans.</resume-signal>
</task>

</tasks>

<verification>
- The driver log (rep_verify_log.txt) shows round 4's material=$mat read-backs for all 3 tiers, 0 error-class lines, picks registered (PICK lines), win/cleanup evidence.
- The SUMMARY records verdicts (a)-(g) + the Glass1/Ghost gate decision (add to GAME_MATERIALS behind hard difficulty vs stay gated).
- Any defect found → gap-closure plan BEFORE phase verification.
</verification>

<success_criteria>
SC1-SC3's interactive half is human-verified: material blending is visually distinct, playable, pick-compatible, and the pedagogy hypothesis has its first evidence — with the Glass1/Ghost gate decided on evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/18-materials-exploration/18-15-SUMMARY.md`
</output>
