---
phase: 18-materials-exploration
plan: 07
type: execute
wave: 3
depends_on: ["18-02", "18-04"]
files_modified: [vmd/lib/game.tcl, vmd/smoke/phase16_onpick_smoke.tcl]
autonomous: true

must_haves:
  truths:
    - "start_game accepts an additive 5th arg per_mat ({} = blending OFF for the round — byte-identical pre-18 behavior for every existing caller)"
    - "With non-empty per_mat: resolve_per_mat runs inside start_game (single derivation choke point), dropped entries degrade via catch-guarded vmdcon -warn (NEVER an ERROR) line, never an abort), ensure_gameblend runs once before application, and mat_specs {code mat} pairs ride INTO add_hider_reps"
    - "game_state gains an additive 5th key per_mat holding the RESOLVED dict (mirror of 17.1-06's resolved per_rep precedent); restart passes it through defensively"
    - "The byte-frozen invariants are untouched: 16-13 guard, ONE mutate, ONE 1-arg reconstruct, stamp_tier_codes BEFORE add_hider_reps, hider reps land LAST"
  artifacts:
    - path: "vmd/lib/game.tcl"
      provides: "5-arg signature, resolution + warn + ensure + mat_specs build in the step-12 block, 5-key game_state, restart pass-through, header contract update"
      contains: "per_mat"
    - path: "vmd/smoke/phase16_onpick_smoke.tcl"
      provides: "gs shape 4→5 key regression update"
      contains: "per_mat"
  key_links:
    - from: "game.tcl step 12"
      to: "materials::resolve_per_mat"
      via: "lassign [resolve_per_mat $per_mat $tiers] per_mat_resolved mat_drops"
      pattern: "resolve_per_mat"
    - from: "game.tcl step 12"
      to: "hiders::add_hider_reps 3rd arg (18-02)"
      via: "add_hider_reps $game_molid $specs $mat_specs"
      pattern: "add_hider_reps \\$game_molid \\$specs \\$mat_specs"
    - from: "game_state stash"
      to: "resolved per_mat"
      via: "dict create ... per_mat $per_mat_resolved"
      pattern: "per_mat \\$per_mat_resolved"
---

<objective>
Wire the composition root: start_game's 5-arg seam — resolve the raw per_mat inside start_game (validation, default-fill, active-tier restriction, warning-class degrade), ensure GameBlend exists, build mat_specs, pass them into add_hider_reps, stash the RESOLVED per_mat as game_state's 5th key, and thread restart. Update the phase16 onpick smoke's gs-shape regression (4→5 keys, the 17.1-07 precedent class).

Purpose: MATERIAL-01's runtime half — the one place where material state becomes per-rep application, shared by console, GUI, and restart (17.1-14's one-derivation-semantics decision).
Output: game.tcl 5-arg seam + onpick smoke green ×3 + restart/game regression smokes green.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-materials-exploration/18-RESEARCH-vmd-materials.md
@.planning/phases/18-materials-exploration/18-RESEARCH-codebase-integration.md
@vmd/lib/game.tcl
@vmd/lib/materials.tcl
@vmd/lib/hiders.tcl
@vmd/smoke/phase16_onpick_smoke.tcl

Load-bearing research: integration research §1.1 (step table: signature line 147, step 12 at 376-381, gs stash 407-409, restart 452-466, header 56-146), §1.2 (the resolution rule + why-not alternatives), §2.6 (5-key stash + the onpick 4→5 regression). Warning-class degrade precedent: game.tcl:204-212 (`catch {vmdcon -warn ...}` in a loop). BYTE-FROZEN invariants: STATE.md constraint 6. Materials research: modmaterial silent no-op on invalid names → the P-1 read-back in hiders (18-02) is the enforcement layer; resolve/warn is the input-hygiene layer.
</context>

<tasks>

<task type="auto">
  <name>Task 1: game.tcl — 5-arg seam + resolution + 5-key stash + restart</name>
  <files>vmd/lib/game.tcl</files>
  <action>
1. Signature: `proc ::biochemeleon::game::start_game {molid hider_count {per_rep {}} {lock_scene 0} {per_mat {}}}` — additive; every existing caller (console, smokes, GUI pre-18) is byte-compatible.
2. In the step-12 block (376-381), BEFORE the existing `set specs [list]` loop, add the material resolution:
   - Shape-guard the raw per_mat exactly like the warn loop guards per_rep (catch `dict size` → treat as empty).
   - `lassign [::biochemeleon::materials::resolve_per_mat $per_mat $tiers] per_mat_resolved mat_drops`
   - Warn loop over $mat_drops mirroring game.tcl:204-212: `foreach d $mat_drops { lassign $d dkey dmat dreason; catch {vmdcon -warn "bioCHEMeleon: material '$dmat' for rep '$dkey' $dreason -- using default"} }` — adjust message per reason (unknown-material → "using default"; unknown-rep → "dropped"). NEVER an `ERROR)` line, never a raise (STATE.md constraint 2).
   - `set mat_specs [list]`; `if {[dict size $per_mat_resolved] > 0} { ::biochemeleon::materials::ensure_gameblend; foreach t $tiers { lassign $t code style count; lappend mat_specs [list $code [dict get $per_mat_resolved $style]] } }` — ensure_gameblend is idempotent and covers the only non-stock curated name; stock names need nothing.
   - Change the call to `::biochemeleon::hiders::add_hider_reps $game_molid $specs $mat_specs` ({} mat_specs = pre-18 behavior — 18-02 proved byte-compat).
3. game_state (407-409): add the 5th key — `set gs [dict create game_molid $game_molid hider_count $eff_total snapshot $snapshot per_rep $per_rep per_mat $per_mat_resolved]` (RESOLVED dict stashed — the 17.1-06 resolved-per_rep precedent; idempotent on restart re-resolution).
4. restart (452-466): add the defensive per_mat read mirroring the per_rep/lock_scene reads (`if {[dict exists $game_state per_mat]} { set per_mat [dict get $game_state per_mat] } else { set per_mat [dict create] }`) and thread it as the 5th arg to start_game.
5. Header comment (56-146 contract): document the new step 12.5 (material resolution + ensure + mat_specs) and the 4-key→5-key game_state note (the 38-43 / 145-146 lines). Do NOT touch the byte-frozen blocks: 16-13 guard (149-191), ONE mutate, ONE 1-arg reconstruct_from_sentinels, stamp_tier_codes BEFORE add_hider_reps, hider reps LAST.
6. Comment hygiene (8.6-idiom gate greps comments).
  </action>
  <verify>
Static: grep game.tcl — `per_mat` in the signature; `resolve_per_mat` called once; `ensure_gameblend` called once; `add_hider_reps $game_molid $specs $mat_specs` present; `per_mat $per_mat_resolved` in the gs dict; restart threads per_mat. The guard/mutate/reconstruct/stamp ordering lines are byte-identical (diff the regions). 8.6-idiom gate 0.
Regression smokes (fresh staging, SEQUENTIAL runs): phase16_onpick_smoke (updated in Task 2) PASS=1 ×3; phase16_restart_smoke PASS=1 (restart pass-through regression — it uses per-key dict reads so needed no edit, prove it); phase15_game_smoke PASS=1. Full-log each: 0 `ERROR)` / 0 `bad switch`, `Exiting normally`.
  </verify>
  <done>The seam resolves, degrades, ensures, applies, and stashes exactly as specced with byte-frozen invariants intact; restart is symmetric.</done>
</task>

<task type="auto">
  <name>Task 2: phase16_onpick_smoke — gs shape 4→5 keys</name>
  <files>vmd/smoke/phase16_onpick_smoke.tcl</files>
  <action>
1. Locate the gs shape/stash asserts (updated 3→4 keys by 17.1-07 — the identical update class). Extend to 5 keys: assert `dict size $gs` == 5 and the key set is {game_molid hider_count snapshot per_rep per_mat}; for the smoke's 4-arg rounds assert the stashed per_mat == {} (empty dict) — the blending-off default made observable.
2. No other asserts change (per-key dict reads elsewhere are unaffected — research §2.6).
3. Re-run headless ×3: PASS=1 each, full-log 0 `ERROR)` / 0 `bad switch`, `Exiting normally`.
  </action>
  <verify>
`bash -ic 'cd <staging> && vmd -dispdev text -e vmd/smoke/phase16_onpick_smoke.tcl -eofexit < /dev/null'` ×3 sequential → PASS=1 every run, clean logs.
  </verify>
  <done>onpick smoke green with the 5-key shape pinned and the empty-per_mat default asserted.</done>
</task>

</tasks>

<verification>
- phase16_onpick_smoke PASS=1 ×3; phase16_restart_smoke + phase15_game_smoke PASS=1; full logs 0 `ERROR)` / 0 `bad switch`; `Exiting normally`.
- game.tcl: 5-arg signature, single resolve call, single ensure call, 3-arg add_hider_reps, 5-key gs, restart pass-through, header updated; byte-frozen blocks untouched.
- 8.6-idiom grep = 0.
</verification>

<success_criteria>
A 5-arg start_game call with per_mat now produces material-carrying hider reps through the standard composition pipeline, with warning-class degrade for garbage and byte-identical behavior when per_mat is empty.
</success_criteria>

<output>
After completion, create `.planning/phases/18-materials-exploration/18-07-SUMMARY.md`
</output>
