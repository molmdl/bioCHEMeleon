---
phase: 18-materials-exploration
plan: 05
type: execute
wave: 3
depends_on: ["18-01", "18-03"]
files_modified: [vmd/lib/demos.tcl, vmd/smoke/phase14_mol_smoke.tcl]
autonomous: true

must_haves:
  truths:
    - "save_setup emits per_mat as per_mat_count N + per_mat_entry <rep> <mat> lines (mirroring per_rep); material_blending rides the existing scalar branch with zero changes"
    - "load_setup parses per_mat_count/per_mat_entry (defensively: rep = first token, mat = rest of line) and rebuilds the loaded dict with material_blending + per_mat appended in DEFAULTS order"
    - "An OLD .bcm without the new lines loads unchanged — defaults fill via validate_state, no version bump"
    - "Save → load → validate round-trip is order-stable eq=1 WITH the new keys"
  artifacts:
    - path: "vmd/lib/demos.tcl"
      provides: "per_mat save/load line family + rebuild-list extension"
      contains: "per_mat_entry"
    - path: "vmd/smoke/phase14_mol_smoke.tcl"
      provides: "Check 7 round-trip with new keys + old-.bcm backward-compat sub-case"
      contains: "per_mat"
  key_links:
    - from: "demos.tcl load_setup rebuild list"
      to: "setup_state DEFAULTS key order (18-01)"
      via: "append material_blending per_mat to the 11-key literal"
      pattern: "material_blending per_mat"
---

<objective>
Extend the .bcm save/load format for material state: per_mat gets its own line family (mirroring per_rep) and material_blending rides the scalar branch; load_setup's DEFAULTS-order rebuild list gains the two new keys. Update the phase14 mol smoke to prove the round-trip AND old-.bcm backward compatibility.

Purpose: MATERIAL-02's curated set must survive Save/Load like every other setup field — and a user's pre-Phase-18 .bcm files must keep loading byte-compatibly (research §2.4).
Output: demos.tcl save/load + phase14_mol_smoke regression, PASS=1 ×3.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-materials-exploration/18-RESEARCH-codebase-integration.md
@vmd/lib/demos.tcl
@vmd/lib/setup_state.tcl
@vmd/smoke/phase14_mol_smoke.tcl

Load-bearing research: integration research §2.4 (exact save/load changes: per_mat line family, defensive 2-token parse hardened to rep + join[lrange], rebuild-list extension IN DEFAULTS ORDER, backward-compat via dict-exists guard + validate fill). The order-stable eq discipline is STATE.md constraint 10. Space-free material names keep the 2-token parse safe (integration research §2.1).
</context>

<tasks>

<task type="auto">
  <name>Task 1: demos.tcl — per_mat save/load lines + rebuild-list extension</name>
  <files>vmd/lib/demos.tcl</files>
  <action>
1. save_setup (116-133): add an `elseif {$k eq "per_mat"}` branch mirroring per_rep: `puts $fh "per_mat_count [dict size $v]"` + `dict for {rep mat} $v { puts $fh "per_mat_entry $rep $mat" }`. material_blending needs NO branch — it falls into the scalar `else { puts $fh "$k $v" }` (verify by reading: the branch order is per_rep → pdb_pool → else; insert per_mat between pdb_pool and else). Update the proc's header comment (the line-format description).
2. load_setup (142-181):
   - Init `set per_mat [dict create]` next to the existing `set per_rep`/`set pdb_pool` inits (near line 148).
   - Add switch cases (158-169): `"per_mat_count" { }` (no-op, mirrors per_rep_count) and `"per_mat_entry"` with the DEFENSIVE parse: `set rv [split $val " "]; dict set per_mat [lindex $rv 0] [join [lrange $rv 1 end] " "]` — hardens against any future spaced material name (research §2.4).
   - After the while loop, next to `dict set tmp per_rep $per_rep`: `dict set tmp per_mat $per_mat`.
   - Extend the DEFAULTS-order rebuild list (line 176): `{format target_mode selected_object pdb_code demo_id hider_count lock_scene per_rep difficulty_easy lock_source pdb_pool material_blending per_mat}` — the two new tokens appended in DEFAULTS order (18-01 appended them at the END of DEFAULTS).
   - The dict-exists guard in the rebuild loop (line 177) already makes old files load unchanged; validate_state at line 180 canonicalizes/fills.
3. Comment hygiene for the 8.6-idiom gate (it greps comments too).
  </action>
  <verify>
Static: grep demos.tcl for `per_mat_count` and `per_mat_entry` (save + load both) and the extended rebuild list ending in `material_blending per_mat`. 8.6-idiom gate = 0. Headless load-gate: source demos.tcl (after setup_state) under `vmd -dispdev text` → 0 `ERROR)` / 0 `bad switch`.
  </verify>
  <done>save_setup emits per_mat lines; load_setup parses them defensively and rebuilds in DEFAULTS order; old files still load.</done>
</task>

<task type="auto">
  <name>Task 2: phase14_mol_smoke — round-trip + backward-compat regression</name>
  <files>vmd/smoke/phase14_mol_smoke.tcl</files>
  <action>
1. Check 7 (save/load round-trip, lines 119-137): the round-trip input is a validate-shaped dict, which now (post 18-01/18-03) carries material_blending + per_mat. Extend the existing round-trip so the saved file contains the new line families: build the input with `material_blending 1` + `per_mat [dict create VDW Glass3 Lines Translucent]` (validate_state it first so the shape is canonical — REQUIRES 18-03's per_mat passthrough: pre-18-03 validate_state builds its result from a fresh DEFAULTS copy + explicit dict-exists overrides only, so it silently DROPS per_mat input values and the per_mat_count 2 / per_mat_entry asserts below can never pass; this is why this plan runs in wave 3, after 18-03), save, load, assert `expr {$loaded eq $original}` (eq operator, NOT dict eq) → eq=1 proves order-stable round-trip WITH the new keys. Also assert the saved FILE contains `per_mat_count 2` and `per_mat_entry VDW Glass3` lines (read the file back).
2. NEW sub-case (backward compat): take the saved file, strip every `material_blending`/`per_mat*` line (a small while-gets filter writing a tmp copy), load the stripped file → succeeds, and the loaded state has material_blending == 0 + per_mat == {} (validate-filled defaults). This pins "old .bcm loads unchanged".
3. Keep every other check byte-identical. Full-log scan discipline applies.
  </action>
  <verify>
Headless smoke run ×3 (fresh staging, sequential runs, independent draws): `BCHM_SMOKE_RESULT PASS=1` every run; full-log 0 `ERROR)` / 0 `bad switch`; `Exiting normally`. 8.6-idiom gate 0.
  </verify>
  <done>Round-trip eq=1 with the new keys; stripped old-format file loads to clean defaults; phase14 smoke green ×3.</done>
</task>

</tasks>

<verification>
- phase14_mol_smoke PASS=1 ×3, full-log 0 `ERROR)` / 0 `bad switch`, `Exiting normally` each run.
- demos.tcl: per_mat line family present in both save and load; rebuild list ends `material_blending per_mat`.
- 8.6-idiom grep = 0.
</verification>

<success_criteria>
Material state persists in .bcm files with the same order-stable rigor as every other setup field, and pre-18 files load untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/18-materials-exploration/18-05-SUMMARY.md`
</output>
