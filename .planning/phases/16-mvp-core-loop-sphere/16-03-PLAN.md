---
phase: 16
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified: [vmd/lib/game_logic.tcl, vmd/tests/test_game_logic.test]
autonomous: true
must_haves:
  truths:
    - "State machine enforces idle -> countdown -> playing -> won with errors on illegal transitions"
    - "countdown_tick returns {label done} pairs 3,2,1 then GO!/1 and errors if ticked after done"
    - "Timer starts ONLY at begin_play (GO), is a drift-free clock-seconds epoch delta, and freezes on finish_win"
    - "finish_win errors on a second call (double-win prevention) and freezes timer_elapsed_final"
    - "format_mmss produces M:SS (0 -> 0:00, 65 -> 1:05, 600 -> 10:00)"
    - "log model formats Miss! / Already found! / Found one! N remaining lines and returns them from log_append"
    - "Module is PURE (no mol, no tk, no after) — clock seconds is stdlib tcl"
  artifacts:
    - path: "vmd/lib/game_logic.tcl"
      provides: "::biochemeleon::game_logic state machine + timer + log model (pure)"
      exports: ["round_reset", "begin_countdown", "countdown_tick", "begin_play", "timer_start", "timer_elapsed", "timer_stop", "finish_win", "state", "format_mmss", "log_reset", "log_append", "log_lines"]
    - path: "vmd/tests/test_game_logic.test"
      provides: "~15-case tcltest suite with BCHM_TEST_RESULT marker"
  key_links:
    - from: "vmd/lib/game.tcl (Plan 16-08 on_pick)"
      to: "vmd/lib/game_logic.tcl"
      via: "state gate (only score while playing) + log_append + finish_win + timer_elapsed on win"
      pattern: "game_logic::(state|log_append|finish_win|timer_elapsed)"
    - from: "vmd/gui/game_tab.tcl (Plan 16-09 countdown/tick)"
      to: "vmd/lib/game_logic.tcl"
      via: "round_reset/begin_countdown -> countdown_tick per after-step -> begin_play at GO -> timer_elapsed per tick"
      pattern: "game_logic::(round_reset|begin_countdown|countdown_tick|begin_play|timer_elapsed)"
---

<objective>
Create the PURE game-loop logic `vmd/lib/game_logic.tcl`: the explicit `idle -> countdown -> playing -> won` state machine, drift-free timer, and rolling-log model — TDD-style.

Purpose: GAME-01/02/03 logic core + LOOP-03 win-condition data. v1's implicit `_started` flag becomes an EXPLICIT machine because VMD pick callbacks fire asynchronously and must be gated by `state == playing` (research gametab SS1 row 1 and SS6.10). The `after` scheduling stays in the GUI (Plan 16-09); this module is the pure, tcltest-able decision layer.
Output: `vmd/lib/game_logic.tcl` + `vmd/tests/test_game_logic.test` — all green.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/16-mvp-core-loop-sphere/16-RESEARCH-gametab.md
@vmd/lib/registry.tcl
@vmd/tests/test_registry.test
@.planning/phases/04-mvp-core-loop-sphere/04-04-SUMMARY.md

Key facts (do NOT re-derive):
- Research gametab doc SS3.1 is the authoritative proc table — port it. SS3.4 lists the concrete test cases.
- Timer math (v1 Q11 lesson): drift-free ABSOLUTE-time delta, NOT a tick counter. `timer_epoch [clock seconds]` at `timer_start` (called ONLY from `begin_play` — the 04-04 lesson "timer measures play time, not countdown time"); each read computes `[expr {[clock seconds] - $timer_epoch}]`.
- TEST DETERMINISM: `clock seconds` is not injectable by default. Give `timer_start`, `timer_elapsed`, and `finish_win` each an OPTIONAL trailing `now` arg (empty means `[clock seconds]`). Tests inject fixed values (e.g. epoch=1000, now=1065 gives elapsed 65). Production callers never pass it.
- `finish_win` must ERROR if state is not "playing" (prevents double-win from duplicate pick callbacks — research SS6.10).
- `countdown_tick` returns a 2-element list {label done}: labels "3","2","1" with done=0, then "GO!" with done=1 on the 4th tick. Ticking after done raises an error. State stays "countdown" until `begin_play` (the GUI calls begin_play in the done branch).
- Purity: NO `after` (scheduling is the GUI's job), NO `mol`/`tk`. `clock` IS stdlib tcl — allowed.
- Log model formats (v1 game.py:170/173/187 + gui_game countdown): kind "countdown" -> msg verbatim ("Get ready...", "3", "2", "1", "GO!"); kind "miss" -> "Miss!"; kind "already" -> "Already found!"; kind "found" -> "Found one! <msg> remaining" (msg = remaining count); kind "win" -> msg verbatim (caller pre-formats with format_mmss). `log_append` formats, appends (newest last), and RETURNS the line; `log_lines` returns the list. The Tk text widget is a VIEW (Plan 16-09); this list is the authoritative model.
- Namespace: `::biochemeleon::game_logic`. State vars: `state`, `countdown_steps`, `timer_epoch`, `timer_elapsed_final`, `log_lines`. One `variable` declaration per line (the name-VALUE pair trap, setup_tab.tcl:246-250).
- Re-source note for the header comment: the namespace eval re-inits state to idle/empty on re-source (acceptable — the entry sources it exactly once).
- tclsh NOT in WSL: stage + headless VMD + parse the `BCHM_TEST_RESULT` marker; scan the full output for `ERROR)` and `bad switch` (false-PASS guard). Tcl 8.5 only; braced expr; illegal transitions via `error` (registry mark_found precedent).
</context>

<feature>
  <name>Pure game-loop state machine + timer + log model</name>
  <files>vmd/lib/game_logic.tcl, vmd/tests/test_game_logic.test</files>
  <behavior>
    - `round_reset` -> state idle; countdown_steps 0; timer_epoch 0; timer_elapsed_final 0; log_lines empty
    - `begin_countdown` -> errors unless state == idle; sets state countdown, countdown_steps 3
    - `countdown_tick` -> errors unless state == countdown AND countdown_steps > 0; decrements; returns {label done}: steps 3/2/1 give {"3" 0} {"2" 0} {"1" 0}, then {"GO!" 1} with countdown_steps set to 0
    - `begin_play` -> errors unless state == countdown AND countdown_steps == 0 (the GO tick fired); state playing; calls timer_start (no injected now in production)
    - `timer_start {{now {}}}` -> timer_epoch = now, or [clock seconds] when now is empty
    - `timer_elapsed {{now {}}}` -> playing: now - epoch; won: timer_elapsed_final; idle/countdown: 0
    - `finish_win {{now {}}}` -> errors unless state == playing; state won; timer_elapsed_final = elapsed(now)
    - `timer_stop` -> documented no-op in the model (the GUI cancels its own after-ids; exported for API symmetry)
    - `state {}` -> returns the current state string (the getter Plan 16-08 gates on)
    - `format_mmss {secs}` -> "M:SS" with int guard (0 -> "0:00", 65 -> "1:05", 600 -> "10:00", 3725 -> "62:05")
    - `log_reset` / `log_append {kind msg}` / `log_lines {}` -> per the format table in context; log_append returns the formatted line
  </behavior>
  <implementation>
    One namespace eval block with the 5 state vars + `namespace export` list, then one proc per contract. Illegal transitions call `error` with a clear message (registry mark_found precedent). All `expr` braced. Header comment: purpose, purity statement, the {label done} contract, the optional-now testing rule, the log format table.
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: RED — write the failing game_logic suite</name>
  <files>vmd/tests/test_game_logic.test</files>
  <action>
Copy the harness structure of `vmd/tests/test_registry.test` (source path `vmd lib game_logic.tcl`, marker tail reading numTests before cleanupTests). Write 15 tests (research SS3.4 list):
1. `round_reset_initializes` — round_reset gives state "idle", timer_elapsed 0, log_lines empty
2. `begin_countdown_from_idle` — begin_countdown gives state "countdown"
3. `begin_countdown_twice_errors` — second call errors (catch + assert nonzero)
4. `countdown_sequence` — after begin_countdown, 4 ticks return exactly {"3" 0} {"2" 0} {"1" 0} {"GO!" 1}
5. `countdown_tick_after_done_errors` — a 5th tick errors
6. `begin_play_from_idle_errors` — begin_play without countdown errors
7. `begin_play_after_go` — full countdown then begin_play gives state "playing"
8. `timer_elapsed_zero_before_start` — after begin_countdown only, timer_elapsed == 0
9. `timer_elapsed_injected_now` — begin_play (which calls timer_start with clock seconds), then round via injected now: use timer_start 1000 explicitly after begin_play, then timer_elapsed 1065 gives 65
10. `finish_win_freezes_elapsed` — playing with timer_start 1000, finish_win 1100 gives state "won" and timer_elapsed (any now, e.g. 9999) == 100
11. `finish_win_twice_errors` — second finish_win errors
12. `format_mmss_cases` — 0 -> "0:00", 65 -> "1:05", 600 -> "10:00", 3725 -> "62:05"
13. `log_append_found_format` — log_append found 3 returns "Found one! 3 remaining"
14. `log_append_miss_and_already` — "Miss!" and "Already found!" exact lines
15. `log_reset_clears` — append lines, log_reset, log_lines is empty
Every test body starts with `round_reset` (isolation — no residual state between cases).
  </action>
  <verify>Staged headless run of the test file — suite FAILS (module missing / unknown procs), proving RED.</verify>
  <done>RED state confirmed: 15 failing cases define the contract.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement game_logic.tcl</name>
  <files>vmd/lib/game_logic.tcl</files>
  <action>
Implement per the behavior table. Minimal code to pass. `timer_stop` is a one-line documented no-op (`return`) — the GUI cancels its own after-ids; the model has nothing to stop. Keep procs tiny; brace every expr; use `error` for illegal transitions.
  </action>
  <verify>Staged run gives `BCHM_TEST_RESULT Total=15 Passed=15 Failed=0 Skipped=0`.</verify>
  <done>All 15 game_logic tests green.</done>
</task>

<task type="auto">
  <name>Task 3: REFACTOR + gates</name>
  <files>vmd/lib/game_logic.tcl, vmd/tests/test_game_logic.test</files>
  <action>
Only obvious cleanups (naming, comment clarity) — otherwise skip code changes. Gates:
1. Purity: `grep -nE "\bmol\b|\batomselect\b|\btk\b|\bafter\b|vwait" vmd/lib/game_logic.tcl` — must return ZERO matches.
2. 8.6 gate on both files: `grep -rnE "\blmap\b|\btry\b|\bthrow\b|\btailcall\b|\bcoroutine\b|\byield\b|\bfinally\b" vmd/lib/game_logic.tcl vmd/tests/test_game_logic.test` — ZERO matches.
3. Suite still fully green.
Commits: `test(16-03): add failing game_logic suite (state machine + timer + log model)`, then `feat(16-03): implement game_logic.tcl pure layer`, then a refactor commit only if code changed.
  </action>
  <verify>Gates return zero matches; suite green (15/15).</verify>
  <done>Pure, 8.5-clean, 15/15 green, atomic commits made.</done>
</task>

</tasks>

<verification>
- `BCHM_TEST_RESULT Total=15 Passed=15 Failed=0 Skipped=0` from the staged headless run
- Zero `ERROR)` / `bad switch` lines in the full output
- Purity + 8.6 gates zero matches
</verification>

<success_criteria>
- game_logic.tcl exists with the full exported surface; illegal transitions error; timer is drift-free and injectable for tests
- Plans 16-08 (on_pick state gate + win) and 16-09 (after-chains) can consume it without modification
</success_criteria>

<output>
After completion, create `.planning/phases/16-mvp-core-loop-sphere/16-03-SUMMARY.md`
</output>
