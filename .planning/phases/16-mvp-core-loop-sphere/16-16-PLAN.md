---
phase: 16
plan: 16
type: execute
wave: 3
depends_on: [16-14, 16-15]
files_modified: [vmd/tests/pick_verify.tcl, .planning/phases/16-mvp-core-loop-sphere/16-VERIFICATION.md]
autonomous: false
gap_closure: true
must_haves:
  truths:
    - "THE A/B VERDICT IS RECORDED: do finds require `mouse callback on` (off-click finds y/n, on-click finds y/n) + whether the no-arg `mouse callback` query form works — this decides 16-17's branch"
    - "exact PICK event values are captured paste-safely (ev args triple, 0-based atom index, game molid, shift present) — the session-1 capture artifact is closed"
    - "the freeze question is answered: reproduced or clean exit (open question c — recorded as observation, never speculation)"
    - "pv_cleanup restore is verified via the real console path: mouse restored to saved rotate/-1, labels back to baseline, active=0"
    - "double-Start re-check passes IN THE GUI (16-13/16-14's fix): Start mid-round and after-win both yield a fresh round with atom count = original + N, single sentinel generation, no Segments: 3"
  artifacts:
    - path: "vmd/tests/pick_verify.tcl"
      provides: "re-verify driver with STEP 10 (guard re-check + freeze probe) and the conditional STEP 4C restore"
      min_lines: 540
    - path: ".planning/phases/16-mvp-core-loop-sphere/16-VERIFICATION.md"
      provides: "dated re-verify session record (5 checklist verdicts + exact PICK values + transcript)"
  key_links:
    - from: "vmd/tests/pick_verify.tcl STEP 10"
      to: "the 16-13 guard"
      via: "pv_state atom/sentinel assertions after double-Start (555+N atoms, HIDER 555..554+N)"
      pattern: "STEP 10"
    - from: "the re-verify record"
      to: "16-17's branch decision"
      via: "the recorded ab/callback-query verdict fields"
      pattern: "mouse callback|ab_off|ab_on"
---

<objective>
Run the ONE GUI human-verify re-verify session that closes both blocking gaps' remaining evidence: the pick-contract caveats (Truth 3 — callback A/B, exact PICK values, freeze question, pv_cleanup restore) AND the double-Start guard re-check (Truth 7's GUI half), all in a single sitting using the staged driver.

Purpose: VERIFICATION.md's recommendation — "schedule the re-verify session for gap 2; gap 2's code consequence (if any) should be planned together with its A/B outcome". The guard code lands first (16-13..16-15) so the session can re-check it in the same sitting (checklist item 5).
Output: extended `vmd/tests/pick_verify.tcl` + a dated re-verify record section in `16-VERIFICATION.md` carrying every verdict 16-17 needs.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/tests/pick_verify.tcl
@.planning/phases/16-mvp-core-loop-sphere/16-VERIFICATION.md
@vmd/lib/pick_bridge.tcl

Key facts:
- The driver is READY for checklist items 1-4 (two-paste `pv_observe` observer, STEP 4 callback A/B, `pv_cleanup`/`pv_cleanup_check`, freeze-probeable step sequence) but item 5 (double-Start guard re-check) does NOT exist yet — this plan adds STEP 10.
- DRIVER BUG to fix while extending (found planning this session): STEP 4's part C says "Restore VMD's default state: mouse callback off". If the A/B CONFIRMS the hypothesis (B finds, A does not), that static `off` would kill delivery for the REST of the session (steps 5-9 need the game-required state). Part C must become a CONDITIONAL restore instruction.
- Two-paste lesson: every printed line < ~70 cols; no bracketed substitutions in paste instructions (a terminal wrap inside `[info exists ...]` silently broke session-1's observer — the step-2 INCONCLUSIVE root cause).
- Headless safety: the whole driver body lives inside `if {[info exists ::tk_version]} {...}` (a top-level `return` is NOT sufficient under `vmd -e` — each top-level command evaluates independently). `::tk_version` global qualifier (13-02 lesson).
- The human runs the real VMD GUI on Windows; OpenCode prepares/stages the driver, then reads the pasted transcript. Tk cannot load in `-dispdev text` — none of this session is headless-verifiable.
- `rm` is denied — stage with `cp -r vmd tmp/biochemeleon-vmd/` (overwrite).
- VERIFICATION.md commit ownership: the orchestrator bundles it at roll-up — do NOT git-commit it in this plan (commit only the driver + summary).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the re-verify driver (STEP 10 + conditional 4C) and stage it</name>
  <files>vmd/tests/pick_verify.tcl</files>
  <action>
Edit `vmd/tests/pick_verify.tcl` (keep every printed line < ~70 cols; EXPECTED comment next to each new step):

1. Retitle the step banners: replace each `STEP N of 9` with `STEP N of 10` (9 banner occurrences).
2. Fix STEP 4 part C to a CONDITIONAL restore (session-integrity bug): replace the `C. Restore VMD's default state:  mouse callback off` text with:
   `C. Restore the state the A/B just proved necessary: if B found and` / `A did not, paste:  mouse callback on   (the game needs it for` / `steps 5-9). Otherwise paste:  mouse callback off  (VMD default).`
3. Add STEP 10 after STEP 9 (guard re-check — checklist item 5 — plus the freeze probe, open question c):
   - 10a (mid-round double-Start): "On the Setup tab press Start AGAIN while the current round is playing (after GO)." Then: "The Game tab log restarts the countdown (3-2-1-GO) -- that is the guarded auto-restart." Then: "After GO, paste:  pv_state". EXPECTED comment: a NEW game molid; atom count = 555 + 3 = 558; HIDER indices exactly `555 556 557` (single generation); registry remaining = 3; the VMD main log's combined reload shows 558 atoms / Segments: 2 (NEVER 561 / Segments: 3 / HIDER 555-559).
   - Freeze probe (between 10a and 10b, bridge inactive after the NEXT win — see 10b ordering note): "With the pick bridge inactive (after a win), click a few atoms in the viewer (labels appear), then paste:  pv_state. Record: does VMD stay responsive? (session-1 froze here once -- open question c; record what you see, repro or clean)."
   - 10b (after-win double-Start — the exact session-1 defect): "Find all 3 hiders to win, wait for the win box, then press Start on the Setup tab once more. After GO, paste:  pv_state." EXPECTED comment: identical single-generation expectations (558 atoms, HIDER 555 556 557, remaining 3) — session 1 produced 561 atoms / Segments: 3 / HIDER 555-559 here; the guard must make that impossible.
4. Update the END banner to list the FIVE re-verify answers to record: (1) A/B verdict per state, (2) exact PICK values, (3) freeze outcome, (4) pv_cleanup restore result, (5) guard re-check result (10a + 10b).
5. Stage: `cp -r vmd tmp/biochemeleon-vmd/` (overwrite; rm denied).

Keep ALL existing steps/behavior byte-stable otherwise (the driver is session-1-recorded evidence).
  </action>
  <verify>
1. Headless guard run (must stay a clean no-op): `timeout 120 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/tests/pick_verify.tcl -eofexit < /dev/null' > tmp/pv_headless.txt 2>&1; grep -c "GUI-only" tmp/pv_headless.txt` → ≥1 warning AND `grep -c "ERROR)" tmp/pv_headless.txt` → 0 AND `grep -c "bad switch" tmp/pv_headless.txt` → 0.
2. `grep -c "STEP 10" vmd/tests/pick_verify.tcl` → ≥2 (banner + references); `grep -c "of 10" vmd/tests/pick_verify.tcl` → 10 banners; `grep -c "mouse callback on" vmd/tests/pick_verify.tcl` → present in the conditional 4C text.
3. `grep -c "pv_state" vmd/tests/pick_verify.tcl` → the STEP 10 prompts include pv_state dumps.
  </verify>
  <done>The driver carries all 5 re-verify checklist items (A/B, exact PICK capture, freeze probe, pv_cleanup restore, guard re-check 10a+10b); staged to the staging root; headless guard still clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: GUI re-verify session (pick caveats + guard re-check, one sitting)</name>
  <what-built>
The double-Start auto-restart guard (16-13 game.tcl choke point + 16-14 on_start bridge teardown and Game-tab view reset), its regression smoke (16-15, PASS=1), and the extended re-verify driver — everything the two blocking gaps need, ready for one ~15-minute GUI session.
  </what-built>
  <how-to-verify>
Stage first (OpenCode, already done in Task 1): `cp -r vmd tmp/biochemeleon-vmd/`.
Then in a REAL VMD GUI on Windows (not -dispdev text):
1. Open VMD → menu Extensions → Tk Console. In the console: `cd tmp/biochemeleon-vmd` then `source vmd/tests/pick_verify.tcl`. Follow its printed prompts IN ORDER.
2. STEP 1 — press Start (demo 1k8p, hider count 3): CONFIRM countdown 3-2-1-GO, timer starts, Mouse menu shows Pick -> Pick Atom. Then paste `pv_state` (it also stashes the round's game_state).
3. STEP 2 — exact PICK values: paste the TWO-PASTE observer (the `pv_observe` proc block first, then the short `trace add variable ::vmd_pick_event write pv_observe` line); click a hider sphere. RECORD the exact two `PICK ...` lines per click (ev args triple, atom=<0-based index>, mol=<game molid>, shift=0/1; hold Shift for one click).
4. STEP 3 — paste `mouse mode pick 0`, click: event fire? label? Then paste `mouse mode pick` (the hotkey-p 1-arg form), click: does it re-arm finds? What submode does it resolve to? Restore with `mouse mode pick 2`.
5. STEP 4 — THE A/B (decides 16-17): paste `mouse callback off` + `pv_callback_state`; click ONE UNFOUND hider → find or not? Then `mouse callback on` + `pv_callback_state`; click ANOTHER UNFOUND hider → find or not? RECORD both outcomes AND whether the no-arg `mouse callback` query printed a state. Then apply part C's conditional restore.
6. STEPS 5-8 — quick confirmations as printed (rotate toggle + finds-after-repick; label hygiene + held-label detectability; phantom never fires; hidden-rep unpickable then findable after re-show).
7. STEP 9 — find all 3 hiders: found spheres green, remaining 0, timer stops, win box WITH time. Paste `pv_state` (frozen timer, state=won). FREEZE PROBE: with the bridge inactive, click a few atoms (labels appear), paste `pv_state` again — VMD responsive or frozen? Then paste `pv_cleanup` + `pv_cleanup_check` — RECORD: mouse restored to rotate/-1, labels back to baseline, active=0.
8. STEP 10a — press Start on the Setup tab DURING the new round (mid-play): the log restarts the countdown; after GO paste `pv_state`. EXPECT: new game molid, 558 atoms, HIDER indices exactly 555 556 557, remaining 3 — NEVER 561/Segments 3/555-559.
9. STEP 10b — find all 3 to win, then press Start once more: after GO paste `pv_state`. EXPECT: same single-generation values (this was the exact session-1 defect).
10. Paste the FULL console transcript plus your answers where the driver's END banner says (the executor records them into 16-VERIFICATION.md).
  </how-to-verify>
  <resume-signal>Answer in one line (then paste the transcript or say where it is):
REVERIFY: ab_off_finds=<y/n> ab_on_finds=<y/n> callback_query=<worked/failed> pick_values=<captured: <values> | not captured: <what happened>> freeze=<repro | clean> cleanup_restore=<ok | failed: <what>> guard_midround=<pass | fail: <observed>> guard_afterwin=<pass | fail: <observed>>
</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the re-verify session</name>
  <files>.planning/phases/16-mvp-core-loop-sphere/16-VERIFICATION.md</files>
  <action>
Append a clearly dated section to `16-VERIFICATION.md` (after the existing content, before nothing — it is the last record):

`# Re-verify session (plan 16-16, <today's date>)` containing:
1. The five checklist verdicts, verbatim from the resume signal: A/B per state (off-finds / on-finds), callback query form worked or not, exact PICK values (the ev triple + atom/mol/shift), freeze outcome (repro or clean — recorded as OBSERVATION, never speculation), pv_cleanup restore result, guard re-check 10a + 10b results.
2. The pasted console transcript (or a reference to where it is stored).
3. A one-line disposition: which 16-17 branch the A/B selects — (a) `mouse callback on/off` pairing required, or (b) callback irrelevant (non-requirement recorded).

Do NOT git-commit 16-VERIFICATION.md (the orchestrator bundles it at roll-up). Commit the driver changes + the plan summary normally.

If either guard re-check (10a/10b) FAILED: record the observed values exactly, still write the disposition line, and flag the failure prominently at the TOP of the section — the pick-bridge work (16-17) proceeds, but the guard failure must be surfaced to the orchestrator in this plan's summary.
  </action>
  <verify>
1. `grep -c "Re-verify session" .planning/phases/16-mvp-core-loop-sphere/16-VERIFICATION.md` → ≥1 with today's date on the heading line.
2. The section contains all five verdict fields (`grep -c "ab_off_finds\|ab_on_finds\|freeze\|cleanup_restore\|guard_afterwin" ...` → matches present) plus the exact PICK values or their absence explanation.
3. `git status --porcelain .planning/phases/16-mvp-core-loop-sphere/16-VERIFICATION.md` may show modified (expected) but the plan's commits do NOT include it.
  </verify>
  <done>The re-verify record is on disk with every verdict 16-17 needs; Truth 3's and Truth 7's evidence is complete; any guard failure is flagged for the orchestrator.</done>
</task>

</tasks>

<verification>
- pick_verify.tcl: STEP 10 + conditional 4C present; staged; headless guard clean (zero ERROR)/bad switch).
- 16-VERIFICATION.md: dated re-verify section with the 5 verdicts + exact PICK values + transcript + the 16-17 branch disposition.
- The checkpoint's resume signal captured: A/B verdict, callback query status, exact PICK values, freeze outcome, cleanup restore, guard 10a/10b results.
</verification>

<success_criteria>
- VERIFICATION checklist items 1-5 all have recorded answers in one sitting.
- Truth 3 (pick-contract lock): the callback hypothesis is decisively confirmed or refuted; exact PICK values captured; freeze question answered; restore path exercised via pv_cleanup.
- Truth 7 (loop integrity): the double-Start guard is HUMAN-VERIFIED in the GUI (no stacked generations, mid-round and after-win).
</success_criteria>

<output>
After completion, create `.planning/phases/16-mvp-core-loop-sphere/16-16-SUMMARY.md`
</output>
