---
phase: 16
plan: 14
type: execute
wave: 2
depends_on: [16-13]
files_modified: [vmd/gui/dialog.tcl, vmd/gui/game_tab.tcl]
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Start pressed mid-round re-arms the pick bridge on the NEW game molecule — without the teardown, activate's idempotence would leave the bridge bound to the DEAD old molid and every pick on the new molecule would be silently dropped"
    - "the Game tab view resets for a new round (timer 0:00, Mouse: Rotate until GO) — the previous round's frozen timer and pick-mode indicators cannot leak into the countdown window"
    - "a failed Start (validation abort at steps 1-3) leaves a running round untouched — the teardown happens only once a new round is committed"
    - "a dialog closed mid-round composes safely with the guard: the surviving stash is consumed by 16-13's guard on the next Start (on_close needs NO game::cleanup call)"
  artifacts:
    - path: "vmd/gui/dialog.tcl"
      provides: "on_start tears down the previous round's pick bridge immediately before game::start_game"
      contains: "pick_bridge::deactivate"
    - path: "vmd/gui/game_tab.tcl"
      provides: "start_round resets the previous round's view state (timer_text / mode_text / mouse_mode) after stop_all_timers"
      contains: "set timer_text"
  key_links:
    - from: "vmd/gui/dialog.tcl on_start"
      to: "pick_bridge::deactivate"
      via: "catch-guarded teardown between the validate step and start_game"
      pattern: "pick_bridge::deactivate"
    - from: "vmd/gui/game_tab.tcl start_round"
      to: "widget-bound view vars"
      via: "reset sets after stop_all_timers (programmatic var sets fire no -command)"
      pattern: "set timer_text .0:00."
---

<objective>
Close VERIFICATION gap 1's GUI half: integrate the 16-13 guarded `start_game` with the dialog/Game-tab layer so a double-Start (mid-round or after-win) yields a fresh, PLAYABLE round — the pick bridge re-arms on the NEW game molecule and the Game tab view resets.

Purpose: the guard alone makes stacked generations impossible, but without a bridge teardown a mid-round double-Start would leave `pick_bridge::activate`'s idempotence guard bound to the DEAD old game_molid — `_on_event`'s molid filter would silently drop every pick on the new molecule (game unplayable, zero errors). The view reset closes the cosmetic leak the observed session showed (frozen "5:14" timer + "Mouse: Pick" panel during the new countdown while the bridge was down).
Output: `vmd/gui/dialog.tcl` (on_start teardown) + `vmd/gui/game_tab.tcl` (start_round view reset); headless regression via the existing entry/gametab smokes; GUI re-check lands in 16-16's checkpoint.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/gui/dialog.tcl
@vmd/gui/game_tab.tcl
@vmd/lib/pick_bridge.tcl
@.planning/phases/16-mvp-core-loop-sphere/16-VERIFICATION.md

Key facts:
- `pick_bridge::activate` (pick_bridge.tcl:77-120) early-returns when `active` is 1 (idempotence guard, probe3: duplicate traces double-fire). After a win, `on_win` already deactivated the bridge — but a Start pressed MID-round leaves it active on the old molid.
- `_on_event` (pick_bridge.tcl:134-161) filters `if {$vmd_pick_mol ne $active_mol} { return }` — a bridge bound to the dead old molid drops every pick silently.
- `on_start` (dialog.tcl:134-202) is the BTN-07 fan-in: steps 1-3 (collect_state / resolve target / validate_state) can ABORT with a tk_messageBox; step 4 calls `game::start_game` (now guarded by 16-13); steps 5-7 (set_difficulty / raise_tab / start_round) consume the RETURNED game_state — already correct: `start_round $gs` flows the guard's possibly-remapped game_state (16-13 may start on the restored original) into game_tab's stash. NO change needed to how `gs` flows.
- `game_tab::start_round` (game_tab.tcl:151-203) already calls `stop_all_timers` FIRST (defensive restart parity — cancels a round-1 countdown chain when Start is pressed mid-countdown), stashes game_state, registers callbacks, resets game_logic, clears the log, pulls remaining, fires countdown.
- `game_tab` view vars that LEAK between rounds: `timer_text` (stays at the old round's frozen value until the first tick at GO+1s), `mode_text`/`mouse_mode` (stay at the old round's "Mouse: Pick" while the bridge is down during the countdown).
- Programmatic `variable` sets do NOT fire `-command` (documented in countdown_step's GO branch) — resetting `mouse_mode` to "rotate" issues no viewer command.
- on_close (dialog.tcl:92-111) stops timers + deactivates the bridge but does NOT call game::cleanup — CORRECT for this gap: game.tcl is never re-sourced mid-session, so `current_state` survives the close and 16-13's guard consumes it on the next Start ("dialog closed mid-round" path). Do NOT add a cleanup call to on_close (that is Phase 19 scope).
- Tcl 8.5 only; one-per-line `variable` declarations (the 14-04 lesson); Tk never loads headless — GUI behavior verification is 16-16's checkpoint.
</context>

<tasks>

<task type="auto">
  <name>Task 1: on_start tears down the previous round's pick bridge</name>
  <files>vmd/gui/dialog.tcl</files>
  <action>
In `::biochemeleon::on_start`, insert a bridge teardown BETWEEN step 3 (the validate_state clamp) and step 4 (the `game::start_game` call):

```tcl
    # 3.5 (16-14, gap-1 GUI half): tear down the PREVIOUS round's pick bridge
    # BEFORE starting the new one. pick_bridge::activate is idempotent (it
    # early-returns when already active), so without this a Start pressed
    # mid-round would leave the bridge bound to the DEAD old game_molid --
    # _on_event's molid filter would then silently drop every pick on the
    # new molecule (game unplayable, no error). Idempotent no-op when no
    # round is live (fresh session, or the round already won -- on_win
    # deactivated it). Placed AFTER the abort paths (steps 1-3) so a failed
    # validation leaves a running game untouched; catch-guarded like every
    # other step.
    catch {::biochemeleon::pick_bridge::deactivate}
```

Also update `on_start`'s header doc-comment (the numbered flow list "1. collect_state ... 7. start_round") to include the new step so the documented flow matches the code — renumber as 8 steps or insert as "3.5" consistently with how the file's comment style reads best.

Do NOT:
- Do not move the teardown before step 1 (an abort at collect_state must not disturb a live round).
- Do not change how `gs` from start_game flows into `game_tab::start_round` (the returned — possibly remapped — game_state is already the stash source).
- Do not add `game::cleanup` to `on_close` (see context: the surviving stash is consumed by the 16-13 guard on the next Start; a cleanup-on-close is Phase 19 scope).
  </action>
  <verify>
1. `grep -n "pick_bridge::deactivate" vmd/gui/dialog.tcl` → ≥2 sites: the pre-existing on_close call AND the new on_start call.
2. Confirm ordering: the on_start call's line number is greater than the validate_state step's and less than the `game::start_game` call's (`grep -n "validate_state\|pick_bridge::deactivate\|game::start_game" vmd/gui/dialog.tcl`).
3. `grep -rnE "\blmap\b|\btry\b|\bthrow\b|\btailcall\b|\bcoroutine\b|\byield\b|\bfinally\b" vmd/gui/dialog.tcl` → zero (8.5 gate); `grep -rnE "grab set" vmd/gui/` → zero (modeless gate).
  </verify>
  <done>on_start tears down the old round's bridge exactly once, after the abort paths and before start_game; a failed validation leaves a live round untouched.</done>
</task>

<task type="auto">
  <name>Task 2: start_round resets the previous round's view state</name>
  <files>vmd/gui/game_tab.tcl</files>
  <action>
In `::biochemeleon::game_tab::start_round`, add `variable timer_text`, `variable mode_text`, `variable mouse_mode` to the proc's declaration block (ONE PER LINE — the 14-04 lesson), then insert a view reset immediately AFTER the `stop_all_timers` call (step 1) and BEFORE the game_state stash (step 2):

```tcl
    # 1.5 (16-14, gap-1 GUI half): reset the PREVIOUS round's view state so
    # it cannot leak into the new round's countdown window: the frozen timer
    # and the pick mouse-mode indicators (the bridge is DOWN until GO --
    # on_start deactivated it; the panel must reflect reality). Programmatic
    # variable sets do NOT fire -command (no viewer call happens -- see the
    # countdown_step GO-branch note).
    set timer_text "0:00"
    set mode_text "Mouse: Rotate"
    set mouse_mode "rotate"
```

Rationale to encode in the comment: the observed double-Start session left the frozen "5:14" timer and "Mouse: Pick" panel showing during the new countdown while the bridge was down; `remain_text` needs no reset (update_remaining runs later in start_round) and the log is already cleared by step 5.

Do NOT:
- Do not touch the countdown/tick/win/after-discipline machinery (all 7 rules stay intact; stop_all_timers-first already cancels a round-1 countdown chain on mid-countdown double-Start).
- Do not issue any `mouse mode`/viewer command here (the tab never does; bridge save/restore semantics belong to pick_bridge).
- Do not initialize the vars at namespace scope (build owns their first set; the load-gate smoke asserts they are UNSET pre-build — start_round only ever runs after build in practice, and this plan does not change that contract).
  </action>
  <verify>
1. `grep -n "set timer_text\|set mode_text\|set mouse_mode" vmd/gui/game_tab.tcl` → the reset sets appear inside start_round (line range after the proc's `stop_all_timers` line, before the `set ::biochemeleon::game_tab::game_state` stash line), in addition to build's initializations and on_win/countdown_step's existing sets.
2. `grep -rnE "\blmap\b|\btry\b|\bthrow\b|\btailcall\b|\bcoroutine\b|\byield\b|\bfinally\b" vmd/gui/game_tab.tcl` → zero; `grep -rnE "grab set" vmd/gui/` → zero.
3. Headless regression (these smokes source the touched files — a parse error fails them):
   `mkdir -p tmp/gap14 && cp -r vmd tmp/gap14/ && timeout 300 bash -ic 'cd tmp/gap14 && vmd -dispdev text -e vmd/smoke/phase16_entry_smoke.tcl -eofexit < /dev/null' > tmp/gap14_entry.txt 2>&1; grep BCHM_SMOKE_RESULT tmp/gap14_entry.txt` → `BCHM_SMOKE_RESULT PASS=1 FAIL=none`
   `timeout 300 bash -ic 'cd tmp/gap14 && vmd -dispdev text -e vmd/smoke/phase16_gametab_smoke.tcl -eofexit < /dev/null' > tmp/gap14_gametab.txt 2>&1; grep BCHM_SMOKE_RESULT tmp/gap14_gametab.txt` → `BCHM_SMOKE_RESULT PASS=1 FAIL=none`
4. Full-log false-PASS scan on both outputs: `grep -c "ERROR)" tmp/gap14_entry.txt tmp/gap14_gametab.txt` → 0 0 AND `grep -c "bad switch" tmp/gap14_entry.txt tmp/gap14_gametab.txt` → 0 0 (switch-parse errors lack the ERROR prefix).
  </verify>
  <done>start_round resets timer/mode view state after cancelling timers; entry + gametab load-gate smokes stay green on fresh staging with zero ERROR)/bad-switch lines; the entry smoke's Phase-15 regression (which now runs against the 16-13 guard) also stays green.</done>
</task>

</tasks>

<verification>
- dialog.tcl: on_start contains the catch-guarded `pick_bridge::deactivate` between validate_state and start_game (structural greps).
- game_tab.tcl: start_round resets `timer_text`/`mode_text`/`mouse_mode` after `stop_all_timers` (structural grep).
- Gates: 8.5-idioms zero; modeless (grab set) zero on vmd/gui/.
- Regression: phase16_entry_smoke + phase16_gametab_smoke PASS=1 on fresh staging, full-log scan clean.
- GUI-level double-Start re-check (real button presses) is 16-16 checkpoint item 5 — NOT verified here (Tk never loads headless).
</verification>

<success_criteria>
- A double-Start now yields a fresh PLAYABLE round at the GUI layer: bridge down during the old round's teardown, re-armed on the NEW game molecule at GO (16-16 verifies the real-click delivery).
- No stale round state leaks into the new round's countdown window.
- The dialog-closed-mid-round path composes with the guard without an on_close change.
</success_criteria>

<output>
After completion, create `.planning/phases/16-mvp-core-loop-sphere/16-14-SUMMARY.md`
</output>
