---
phase: 16
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified: [vmd/lib/registry.tcl, vmd/tests/test_registry.test]
autonomous: true
must_haves:
  truths:
    - "status_of {idx} returns 'hidden' for un-found hiders, 'found' after mark_found, '' for unregistered indices"
    - "count_remaining counts ONLY status=='hidden' records and decrements on each mark_found"
    - "remaining_by_rep groups hidden records by their rep field; records with rep=='' are skipped"
    - "reconstruct_from_sentinels accepts an OPTIONAL rep arg (default '') without breaking any existing 1-arg call"
    - "Existing Phase-15 registry tests stay green (backward compatibility)"
  artifacts:
    - path: "vmd/lib/registry.tcl"
      provides: "status_of / count_remaining / remaining_by_rep + optional rep arg on reconstruct_from_sentinels (all PURE)"
      exports: ["reconstruct_from_sentinels", "is_hider", "mark_found", "count_hiders", "reset", "status_of", "count_remaining", "remaining_by_rep"]
    - path: "vmd/tests/test_registry.test"
      provides: "extended suite (~16 cases) covering the new found-state surface"
  key_links:
    - from: "vmd/lib/game.tcl (Plan 16-08 on_pick)"
      to: "vmd/lib/registry.tcl"
      via: "status_of guard BEFORE mark_found (the silent-overwrite caller guard)"
      pattern: "registry::status_of"
    - from: "vmd/gui/game_tab.tcl (Plan 16-09 update_remaining)"
      to: "vmd/lib/registry.tcl"
      via: "count_remaining + remaining_by_rep pull model"
      pattern: "registry::(count_remaining|remaining_by_rep)"
---

<objective>
Extend the PURE registry with the Phase-16 found-state surface: `status_of`, `count_remaining`, `remaining_by_rep`, and an optional `rep` argument on `reconstruct_from_sentinels` — TDD-style.

Purpose: LOOP-02 ("found-status tracking — a single source of truth") and GAME-03 ("remaining hiders count — total + per-rep in easy mode"). 15-RESEARCH-registry-game.md explicitly deferred these to Phase 16. The registry record gains a real `rep` value ("VDW" for the sphere tier) so per-rep remaining is derivable forever.
Output: extended `vmd/lib/registry.tcl` + extended `vmd/tests/test_registry.test` — all cases (old + new) green.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/16-mvp-core-loop-sphere/16-RESEARCH-sphere.md
@vmd/lib/registry.tcl
@vmd/tests/test_registry.test
@.planning/phases/15-mutation-safety-hider-registry/15-01-SUMMARY.md

Key facts (do NOT re-derive):
- Research §6 gives probe-verified reference code for all three new procs and the rep-arg signature. `dict incr` works under tcl 8.5.6 (probe16_dictincr).
- FROZEN: record shape stays a 2-key dict `rep status`; `_records` stays a dict keyed by atom `index`; `HIDER_STATUS_HIDDEN`/`HIDER_STATUS_FOUND` constants unchanged. Only ADD — never change existing proc signatures EXCEPT `reconstruct_from_sentinels` gains an OPTIONAL 2nd arg `{rep ""}` (backward compatible: every existing 1-arg call and test keeps working).
- v1 ports: `remaining` == v1 game.py:113-116 (`_remaining` = count of hidden); `remaining_by_rep` == v1 registry.py:274-295 (skips `rep None` records exactly like we skip `rep ""`).
- `mark_found` is a silent idempotent overwrite (probe F19) — the three-way guard (miss/already/mark) lives in the CALLER (Plan 16-08), NOT here. Do not add guard logic to mark_found.
- Purity: this module must stay 100% stdlib (no mol/atomselect/tk/after) — the purity gate greps it.
- tclsh NOT in WSL: tcltest runs under headless VMD via staging; parse `BCHM_TEST_RESULT` marker, scan full output for `ERROR)` and `bad switch` (false-PASS guard).
- Tcl 8.5 only; one `variable` declaration per line; braced `expr`; `eq` operator for string compare.
</context>

<feature>
  <name>Registry found-state surface (Phase-16 deferrals)</name>
  <files>vmd/lib/registry.tcl, vmd/tests/test_registry.test</files>
  <behavior>
    - `status_of {idx}` → "hidden" | "found" | "" (absent idx → "")
    - `count_remaining {}` → integer count of records with status == HIDER_STATUS_HIDDEN
    - `remaining_by_rep {}` → dict {rep count} over hidden records only; records with rep == "" skipped; reps absent from records never appear (no zero-fill of GAME_REPS here — the GUI's format_remaining handles GAME_REPS ordering)
    - `reconstruct_from_sentinels {fetch_hider_ids {rep ""}}` → unchanged behavior for 1-arg calls (rep ""); 2-arg calls stamp every record with the given rep
    - `mark_found` → status flips to found; `count_remaining` decrements; `remaining_by_rep` count for that rep decrements
    - double `mark_found` on the same idx → idempotent (count stays, no error)
    - `reset` → count_remaining 0, remaining_by_rep empty
  </behavior>
  <implementation>
    Add the three procs per research §6 reference code (namespace-qualified constant references, `dict for` + `dict incr`). Change the reconstruct signature to `{fetch_hider_ids {rep ""}}` and stamp `rep $rep` in the record dict. Extend the `namespace export` line with the three new names. Update the header comment (record shape now documents the rep field's Phase-16 meaning: the GAME_REPS name of the tier that owns the hider, "VDW" for Phase 16).
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: RED — append the failing found-state tests</name>
  <files>vmd/tests/test_registry.test</files>
  <action>
Append BEFORE the marker block (keep the existing 9 cases untouched — they are the regression gate). New cases (use the existing `[list apply {{} { return {...} }}]` DI shape):
1. `reconstruct_with_rep_arg` — reconstruct with ids {5 6} and rep "VDW" → `dict get [dict get ... records]`... assert via new API: `status_of 5` == "hidden" and remaining_by_rep == `[dict create VDW 2]`.
2. `status_of_unregistered_is_empty` — `status_of 999` == "".
3. `status_of_after_mark_found` — mark_found 5 → `status_of 5` == "found".
4. `count_remaining_counts_hidden_only` — 3 hiders, mark 1 → count_remaining == 2.
5. `count_remaining_after_reset` — reset → 0.
6. `remaining_by_rep_groups_and_sums` — ids {1 2 3} rep "VDW", mark 1 → remaining_by_rep == {VDW 2}; sum of values == count_remaining.
7. `remaining_by_rep_skips_empty_rep` — reconstruct WITHOUT rep arg (rep "") → remaining_by_rep == empty dict even though count_remaining > 0.
8. `mark_found_idempotent` — mark_found 5 twice → count_remaining unchanged, is_hider 5 still 1, no error.
9. `one_arg_reconstruct_still_works` — the original 1-arg call form → records exist, remaining_by_rep empty (backward-compat proof).
  </action>
  <verify>Staged headless run of `vmd/tests/test_registry.test` → FAILS on the new cases (unknown procs), old 9 still pass.</verify>
  <done>RED state: new cases fail because status_of/count_remaining/remaining_by_rep/rep-arg do not exist.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement the registry extensions</name>
  <files>vmd/lib/registry.tcl</files>
  <action>
Implement per the <implementation> block: `status_of`, `count_remaining`, `remaining_by_rep`, the optional `{rep ""}` arg on `reconstruct_from_sentinels` (stamp `rep $rep` into the record dict), and the extended `namespace export`. Follow the file's existing style exactly (full-path constant refs `$::biochemeleon::registry::HIDER_STATUS_HIDDEN` inside procs that don't `variable` them, or declare `variable` — match neighboring procs).
  </action>
  <verify>Staged run → `BCHM_TEST_RESULT Total=18 Passed=18 Failed=0 Skipped=0` (9 old + 9 new).</verify>
  <done>All old and new registry tests green.</done>
</task>

<task type="auto">
  <name>Task 3: REFACTOR + gates + regression</name>
  <files>vmd/lib/registry.tcl, vmd/tests/test_registry.test</files>
  <action>
Only obvious cleanups. Gates:
1. Purity: `grep -nE "\bmol\b|\batomselect\b|\btk\b|\bafter\b" vmd/lib/registry.tcl` → ZERO matches.
2. 8.6 gate: `grep -rnE "\blmap\b|\btry\b|\bthrow\b|\btailcall\b|\bcoroutine\b|\byield\b|\bfinally\b" vmd/lib/registry.tcl vmd/tests/test_registry.test` → ZERO matches.
3. Full suite green again (Total=18 Failed=0).
Commits: `test(16-02): add failing registry found-state tests (status_of/count_remaining/remaining_by_rep/rep arg)`, `feat(16-02): implement registry found-state surface`, optional `refactor(16-02): ...`.
  </action>
  <verify>Gates zero matches; Total=18 Passed=18 Failed=0.</verify>
  <done>Pure, 8.5-clean, 18/18 green, atomic commits made.</done>
</task>

</tasks>

<verification>
- `BCHM_TEST_RESULT Total=18 Passed=18 Failed=0 Skipped=0` from the staged headless run
- Zero `ERROR)` / `bad switch` lines in the full output
- Purity + 8.6 gates zero matches
- No existing Phase-15 test was modified or deleted (git diff shows only appends in the test file)
</verification>

<success_criteria>
- Registry exposes status_of / count_remaining / remaining_by_rep / optional rep arg — all pure
- LOOP-02 data model ready: status is the single source of truth, read by on_pick (16-08) and the GUI (16-09)
- GAME-03 data ready: total remaining + per-rep remaining derivable
</success_criteria>

<output>
After completion, create `.planning/phases/16-mvp-core-loop-sphere/16-02-SUMMARY.md`
</output>
