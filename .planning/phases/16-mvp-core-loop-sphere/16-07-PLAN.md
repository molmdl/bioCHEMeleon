---
phase: 16
plan: 07
type: execute
wave: 2
depends_on: [16-01]
files_modified: [vmd/lib/mutation.tcl, vmd/smoke/phase16_placement_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "make_placeholder_hiders returns hider coordinates UNIFORM-RANDOM inside the molecule's bounding box (measure minmax), not the old center+jitter diagonal"
    - "The {molid count} signature and {name x y z} record shape (names G01..GNN zero-padded) are UNCHANGED — the frozen Phase-15 contract"
    - "All Phase-15 behavior holds: mutate/rebuild/sentinels/registry/cleanup untouched by the swap"
    - "Hider coords are within the real-atom bbox (with epsilon) and not all identical"
  artifacts:
    - path: "vmd/lib/mutation.tcl"
      provides: "body-swapped make_placeholder_hiders (measure minmax + generators::sphere_positions); signature + record shape frozen"
      contains: "generators::sphere_positions"
    - path: "vmd/smoke/phase16_placement_smoke.tcl"
      provides: "headless smoke proving real placement: bbox containment, count, names, registry + sentinel regression"
  key_links:
    - from: "vmd/lib/mutation.tcl"
      to: "vmd/lib/generators.tcl"
      via: "source line + sphere_positions call inside make_placeholder_hiders"
      pattern: "generators::sphere_positions"
    - from: "vmd/lib/game.tcl start_game"
      to: "vmd/lib/mutation.tcl"
      via: "unchanged call site make_placeholder_hiders $molid $hider_count"
      pattern: "make_placeholder_hiders"
---

<objective>
Body-swap `mutation::make_placeholder_hiders` from center+jitter placeholders to REAL sphere placement: `measure minmax` bounding box + the pure sampler from Plan 16-01.

Purpose: HIDER-03. Phase 15 froze this exact swap point ("Phase 16 replaces make_placeholder_hiders with real sphere placement; the signature, game_state shape, and DI injection line stay identical" — 15-05). Everything downstream (start_game, registry, cleanup) must keep working unchanged.
Output: modified `vmd/lib/mutation.tcl` + `vmd/smoke/phase16_placement_smoke.tcl` (PASS=1).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/16-mvp-core-loop-sphere/16-RESEARCH-sphere.md
@vmd/lib/mutation.tcl
@vmd/lib/generators.tcl
@vmd/smoke/phase15_smoke.tcl

Key facts (do NOT re-derive):
- Research SS3.3 is the exact swap (reference body given). `measure minmax $sel` returns `{{xmin ymin zmin} {xmax ymax zmax}}` (probe-verified). `measure center` is FLAT — but placement needs minmax anyway.
- FROZEN contract (15-05): `{molid count}` signature, `{name x y z}` record shape, names `G%02d` zero-padded. Only the coordinate math changes.
- Port strategy 1:1 from v1 (04-01-SUMMARY): uniform random in bbox, NO min-distance, NO overlap avoidance, NO clamping/inset, overlapping a real atom is harmless. Do NOT invent surface-projection.
- NO seed in the production call path — `sphere_positions $mm $count` (global PRNG stream continues; same convention as randomize_state). Reseeding per call would correlate placements with prior rand() consumers.
- mutation.tcl already sources setup_state.tcl at top — add `source [file join [file dirname [info script]] generators.tcl]` the same way (pure re-init is harmless; the entry also sources it in 16-10).
- `measure minmax` on a bad molid raises naturally — game.tcl already propagates (15-05: caller aborts). Keep the existing catch-defensiveness only if you keep any molinfo call; do not swallow measure errors.
- Pitfall 4 (research SS8): never `lassign [molinfo $m get center] x y z` (nested) — irrelevant here but do not mix forms.
- Smoke conventions: phase15_smoke.tcl pattern (failures/_bail/catch-per-step/marker/exit; registry sourced ONCE; every atomselect deleted). Staging + headless run + parse `BCHM_SMOKE_RESULT`, scan full log for `ERROR)`/`bad switch`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Body-swap make_placeholder_hiders</name>
  <files>vmd/lib/mutation.tcl</files>
  <action>
1. Add the generators.tcl source line next to the existing setup_state.tcl source at the top (same `[file dirname [info script]]` pattern), with a comment.
2. Replace ONLY the body of `make_placeholder_hiders` with the research SS3.3 reference: `set all [atomselect $molid "all"]`; `set mm [measure minmax $all]`; `$all delete`; `set pts [::biochemeleon::generators::sphere_positions $mm $count]`; build records with `format "G%02d"` names. Keep the docstring updated: the proc now does real uniform-bbox placement (Phase 16); signature + record shape unchanged.
3. Update the namespace-eval comment block if it references the old jitter behavior.
Do NOT touch write_combined_pdb / tag_sentinels / fetch_hider_indices / mutate.
  </action>
  <verify>`grep -n "generators::sphere_positions" vmd/lib/mutation.tcl` -> exactly 1 call site; `grep -n "0.5 \\* \\$i\\|0.3 \\* \\$i\\|0.2 \\* \\$i" vmd/lib/mutation.tcl` -> ZERO (old jitter gone); 8.6 gate zero matches.</verify>
  <done>Swap complete; signature/record shape preserved; old jitter math removed.</done>
</task>

<task type="auto">
  <name>Task 2: Write + run the placement smoke</name>
  <files>vmd/smoke/phase16_placement_smoke.tcl</files>
  <action>
Model on phase15_smoke.tcl. Steps:
1. Source lib files in dep order (setup_state, registry, demos, backup, mutation, game).
2. `demos::load_demo 1k8p` (555 atoms). Compute the ORIGINAL bbox BEFORE start_game: `atomselect $molid all` -> `measure minmax` -> `lassign` lo/hi -> delete sel. (The original's real-atom extents == the game molecule's real-atom extents — hiders are appended, real atoms untouched.)
3. `set gs [game::start_game $molid 5]`; `set gm [dict get $gs game_molid]`.
4. Assert 560 atoms; sentinels `resname GAM and beta < 0` -> 5 indices {555 556 557 558 559}.
5. Read hider coords: `atomselect $gm "resname GAM and beta < 0"` -> `get {x y z}` (5 atoms — small list, safe). Assert EVERY hider coordinate within [min-0.001, max+0.001] per axis (epsilon for float formatting).
6. Assert the 5 points are NOT all identical (uniform-random smoke — compare at least one coord pair differs).
7. Assert names: `get name` -> {G01 G02 G03 G04 G05}.
8. Registry regression: count_hiders == 5; is_hider 555 true, is_hider 0 false.
9. `game::cleanup $gs` -> 555 atoms, registry 0, game_molid deleted (catch-molinfo nonzero).
Marker + exit.
  </action>
  <verify>Staged run: `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)`/`bad switch` in full log.</verify>
  <done>Real placement proven: bbox containment, count, names, sentinel + registry + cleanup regression all green.</done>
</task>

</tasks>

<verification>
- `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch`
- Old jitter math absent; generators::sphere_positions called exactly once
- make_placeholder_hiders signature/record shape unchanged (grep the proc line)
- 8.6 gate zero matches on both files
</verification>

<success_criteria>
- start_game now produces genuinely blended, uniformly-placed VDW sphere candidates (blending visual is 16-12's GUI check; the geometry is proven here)
- Zero downstream changes required — game.tcl's call site works unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/16-mvp-core-loop-sphere/16-07-SUMMARY.md`
</output>
