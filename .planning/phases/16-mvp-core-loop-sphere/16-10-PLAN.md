---
phase: 16
plan: 10
type: execute
wave: 3
depends_on: [16-06, 16-07, 16-08, 16-09]
files_modified: [vmd/biochemeleon.tcl, vmd/gui/dialog.tcl, vmd/gui/setup_tab.tcl, vmd/smoke/phase16_entry_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "Entry sources the new modules in dependency order: generators + game_logic in the pure block; hiders + pick_bridge in the mol block; registry still sourced EXACTLY ONCE"
    - "dialog.tcl sources game_tab.tcl at TOP LEVEL and builds the real Game tab (placeholder removed)"
    - "on_start implements the BTN-07 flow: collect_state -> resolve target -> validate/clamp -> start_game -> stash -> raise_tab -> start_round, with tk_messageBox aborts and NO partial game state"
    - "on_close stops all game timers and deactivates the pick bridge BEFORE destroying the dialog"
    - "Start button exists in the Setup Actions group (BTN-07)"
  artifacts:
    - path: "vmd/biochemeleon.tcl"
      provides: "extended source order (generators, game_logic, hiders, pick_bridge)"
      contains: "game_logic.tcl"
    - path: "vmd/gui/dialog.tcl"
      provides: "game_tab source + build + on_start proc + extended on_close"
      contains: "proc ::biochemeleon::on_start"
    - path: "vmd/gui/setup_tab.tcl"
      provides: "Start button in build_actions"
      contains: "Start"
    - path: "vmd/smoke/phase16_entry_smoke.tcl"
      provides: "headless load gate: full entry sources clean, all namespaces/procs exist, Phase-15 regression (start/cleanup) still green"
  key_links:
    - from: "vmd/gui/setup_tab.tcl Start button"
      to: "vmd/gui/dialog.tcl on_start"
      via: "-command {::biochemeleon::on_start}"
      pattern: "on_start"
    - from: "vmd/gui/dialog.tcl on_start"
      to: "vmd/lib/game.tcl + vmd/gui/game_tab.tcl"
      via: "start_game -> stash game_state -> set_difficulty -> raise_tab -> start_round"
      pattern: "(game::start_game|game_tab::start_round)"
    - from: "vmd/gui/dialog.tcl on_close"
      to: "vmd/lib/pick_bridge.tcl + vmd/gui/game_tab.tcl"
      via: "pick_bridge::deactivate + game_tab::stop_all_timers before destroy"
      pattern: "(pick_bridge::deactivate|game_tab::stop_all_timers)"
---

<objective>
Wire the full entry: source the four new modules, build the real Game tab, add the Start button (BTN-07), implement `::biochemeleon::on_start` (the v1 `_on_start` fan-in), and extend on_close — plus the entry load-gate smoke.

Purpose: this is the integration plan that makes the loop REACHABLE from the GUI: Start -> generate per setup -> Game tab -> 3-2-1 -> timer -> clicks -> win. BTN-07 is the last unwired requirement.
Output: modified entry/dialog/setup_tab + `vmd/smoke/phase16_entry_smoke.tcl` (PASS=1).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/16-mvp-core-loop-sphere/16-RESEARCH-gametab.md
@.planning/phases/16-mvp-core-loop-sphere/16-RESEARCH-sphere.md
@vmd/biochemeleon.tcl
@vmd/gui/dialog.tcl
@vmd/gui/setup_tab.tcl

Key facts (do NOT re-derive):
- Source order (research gametab SS2.1 + 15-04): pure block after registry.tcl gains `generators.tcl` then `game_logic.tcl`; mol block gains `hiders.tcl` (after mutation.tcl) and `pick_bridge.tcl` (after game.tcl), both BEFORE gui/dialog.tcl. registry.tcl stays sourced EXACTLY ONCE (re-sourcing wipes _records). mutation.tcl also sources generators itself — harmless pure re-init (existing demos/mutation pattern).
- dialog.tcl: source game_tab.tcl at the TOP LEVEL next to the setup_tab source line (the `[info script]` lesson — NEVER inside a proc body, dialog.tcl:14-19). Replace the placeholder label in open_dialog with `::biochemeleon::game_tab::build $nb.game` (eager build — research SS2.1).
- on_start lives at dialog scope `::biochemeleon::on_start` (research SS7.5 recommendation — it needs setup_tab state + game_tab + game.tcl; dialog scope avoids cross-tab reach-ins). Flow (v1 __init__._on_start :242-261 parity + research SS2.4):
  1. `set state [::biochemeleon::setup_tab::collect_state]`
  2. Resolve target molid: mode "loaded" -> selected_object must be a live molecule (`catch {molinfo $sel get numatoms}`) else abort; mode "demo" -> `set molid [::biochemeleon::demos::load_demo [dict get $state demo_id]]` (catch -> abort); mode "fetch" -> `::biochemeleon::demos::fetch_pdb` is a STUB that errors (Phase 21) -> catch and abort with the stub's message.
  3. Clamp: `set state [::biochemeleon::setup_state::validate_state $state [::biochemeleon::demos::atom_count $molid]]` (the do_save precedent).
  4. `set gs [::biochemeleon::game::start_game $molid [dict get $state hider_count]]` (catch -> abort: no partial game state).
  5. Stash: `set ::biochemeleon::game_tab::game_state $gs` is done inside start_round; here set difficulty: `::biochemeleon::game_tab::set_difficulty [dict get $state difficulty_easy]`.
  6. `::biochemeleon::game_tab::raise_tab`
  7. `::biochemeleon::game_tab::start_round $gs`
  - EVERY abort path: `tk_messageBox -parent $::biochemeleon::w -icon warning -title "bioCHEMeleon" -message <reason>` then `return` (v1 QMessageBox.warning parity — no partial state, dialog stays open).
- setup_tab.tcl build_actions: add `ttk::button $f.start -text "Start" -command {::biochemeleon::on_start}` packed with the others (BTN-07).
- on_close (dialog.tcl:77-88): BEFORE destroy, add catch-guarded `::biochemeleon::game_tab::stop_all_timers` and `::biochemeleon::pick_bridge::deactivate` (after-ids outlive widgets; a leaked trace + pick mouse mode outlives the dialog otherwise — research pick Pitfall 13).
- Headless smoke = LOAD GATE (the established 15-04 pattern): set `::biochemeleon::loaded 0` via namespace eval BEFORE sourcing the entry so the re-source guard lets the body run; then assert namespaces/procs exist; then run the Phase-15 start/cleanup regression through the public API (registry/generators/game_logic are now sourced by the entry — proves the new order).
- Tk is ABSENT headless: open_dialog is never called; only sourcing + proc existence is asserted.
- Tcl 8.5; braced expr; `::tk_version` global qualifier inside procs (never bare).
- Staging + headless + marker parse + full-log scan (`ERROR)` AND `bad switch`).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Entry source order + dialog game-tab build + on_close</name>
  <files>vmd/biochemeleon.tcl, vmd/gui/dialog.tcl</files>
  <action>
1. biochemeleon.tcl: add the 4 source lines per the order above, updating the order-matters comment block (setup_state -> registry -> generators -> game_logic (pure) -> demos -> backup -> mutation -> hiders -> game -> pick_bridge -> dialog).
2. dialog.tcl: add the top-level `source [file join [file dirname [info script]] game_tab.tcl]` next to the setup_tab line; replace the placeholder block in open_dialog with the `::biochemeleon::game_tab::build $nb.game` call; extend on_close with the catch-guarded stop_all_timers + pick_bridge::deactivate before destroy (update the proc comment).
Do NOT define on_start yet (Task 2 owns it).
  </action>
  <verify>`grep -n "game_tab.tcl" vmd/gui/dialog.tcl` -> 1 top-level source; `grep -n "placeholder" vmd/gui/dialog.tcl` -> 0 matches in code (comments ok); entry source order greps: generators/game_logic/hiders/pick_bridge lines present; 8.6 gate zero on both files.</verify>
  <done>Entry + dialog wired for the Game tab; close path cleans timers + pick state.</done>
</task>

<task type="auto">
  <name>Task 2: Start button + on_start (BTN-07 fan-in)</name>
  <files>vmd/gui/setup_tab.tcl, vmd/gui/dialog.tcl</files>
  <action>
1. setup_tab.tcl build_actions: add the Start button per context; update the group comment (BTN-01..04 + BTN-07).
2. dialog.tcl: define `proc ::biochemeleon::on_start {}` implementing the 7-step flow with catch-guarded target resolution and tk_messageBox aborts (spec in context). Commit comment: `feat(16-10): on_start BTN-07 fan-in (resolve->start_game->tab->countdown)`.
  </action>
  <verify>`grep -n "f.start" vmd/gui/setup_tab.tcl` -> 1; `grep -n "proc ::biochemeleon::on_start" vmd/gui/dialog.tcl` -> 1; abort paths present (`grep -c "tk_messageBox" vmd/gui/dialog.tcl` >= 4); 8.6 gate zero.</verify>
  <done>BTN-07 wired end-to-end at the code level (GUI rendering verified in 16-12).</done>
</task>

<task type="auto">
  <name>Task 3: Entry load-gate smoke + Phase-15 regression</name>
  <files>vmd/smoke/phase16_entry_smoke.tcl</files>
  <action>
Model on 15-04's load-gate pattern. Steps:
1. `namespace eval ::biochemeleon { variable loaded 0 }` (bypass the re-source guard), then `source [file join [pwd] vmd biochemeleon.tcl]` — must source clean under -dispdev text (all guards behave; no Tk calls fire).
2. Assert existence: `::biochemeleon::generators::sphere_positions`, `::biochemeleon::game_logic::begin_play`, `::biochemeleon::hiders::add_hider_reps`, `::biochemeleon::pick_bridge::activate`, `::biochemeleon::game::on_pick`, `::biochemeleon::on_start`, all 11 `::biochemeleon::game_tab::*` procs, `::biochemeleon::setup_tab::collect_state`.
3. Registry sourced exactly once: after sourcing the entry, `registry::reconstruct_from_sentinels [list apply {{} {return {1}}}}]`-style check then verify a second reconstruct+count works (indirect — the real guard is the source-order grep; assert `info procs` and that no duplicate namespace init broke state).
4. Phase-15 regression through the public API: `demos::load_demo 1k8p` -> `game::start_game $m 5` -> 560 atoms, 5 sentinels, count_hiders 5, numreps grew by 2 -> `game::cleanup` -> 555 atoms, count_hiders 0, game_molid deleted.
5. Assert the entry defines the Start wiring path: `info procs ::biochemeleon::on_start` non-empty.
Marker + exit.
  </action>
  <verify>Staged run: `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)`/`bad switch` in full log.</verify>
  <done>Entry loads clean with all Phase-16 modules; Phase-15 pipeline still green through the public API.</done>
</task>

</tasks>

<verification>
- `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch`
- 8.6 + modeless (`grab set`) gates zero matches on all touched gui files
- registry.tcl sourced exactly once in the entry (grep)
- on_start aborts every failure path with a message box and leaves no partial state
</verification>

<success_criteria>
- The loop is reachable: Start button -> on_start -> start_game (real spheres) -> Game tab -> countdown (pick armed at GO)
- Closing the dialog mid-game cannot leak timers, the pick trace, or the user's mouse mode
- All prior phases' behavior regression-verified through the entry
</success_criteria>

<output>
After completion, create `.planning/phases/16-mvp-core-loop-sphere/16-10-SUMMARY.md`
</output>
