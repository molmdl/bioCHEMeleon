---
phase: 16
plan: 04
type: tdd
wave: 1
depends_on: []
files_modified: [vmd/lib/setup_state.tcl, vmd/tests/test_setup_state.test]
autonomous: true
must_haves:
  truths:
    - "format_remaining in hard mode (or easy with no per-rep counts) returns exactly 'Remaining: N'"
    - "format_remaining in easy mode with counts returns 'Remaining: N  (Rep: n, ...)' in GAME_REPS order, >0 counts only, exactly two spaces before the paren"
    - "format_remaining is PURE and added to the namespace export"
    - "All existing setup_state tests stay green (no behavior change to existing procs)"
  artifacts:
    - path: "vmd/lib/setup_state.tcl"
      provides: "::biochemeleon::setup_state::format_remaining {total counts_by_rep easy_mode} (pure, exported)"
      contains: "proc ::biochemeleon::setup_state::format_remaining"
    - path: "vmd/tests/test_setup_state.test"
      provides: "extended suite with format_remaining cases (hard / easy-with-counts / easy-all-zero)"
  key_links:
    - from: "vmd/gui/game_tab.tcl (Plan 16-09 update_remaining)"
      to: "vmd/lib/setup_state.tcl"
      via: "format_remaining [registry::count_remaining] [registry::remaining_by_rep] $easy_mode -> remain_text"
      pattern: "setup_state::format_remaining"
---

<objective>
Port v1's pure `format_remaining` (setup_state.py:418-447) to `vmd/lib/setup_state.tcl` — TDD-style.

Purpose: GAME-03's display string ("Remaining: 3" or "Remaining: 3  (VDW: 3)" in easy mode). v1 keeps this formatter in the pure layer; so does v2 — the Game tab (Plan 16-09) is a thin view that calls it.
Output: extended `vmd/lib/setup_state.tcl` + extended `vmd/tests/test_setup_state.test` — all green.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/16-mvp-core-loop-sphere/16-RESEARCH-gametab.md
@vmd/lib/setup_state.tcl
@vmd/tests/test_setup_state.test

Key facts (do NOT re-derive):
- v1 source of truth: `pymol/biochemeleon/setup_state.py:418-447` — read it and port VERBATIM in logic. Format per research gametab SS1.2 row 9: `Remaining: %d` or `Remaining: %d  (Rep: n, ...)` — GAME_REPS order, non-zero counts only, EXACTLY TWO SPACES before the opening paren, comma-space between entries.
- Signature: `format_remaining {total counts_by_rep easy_mode}` where counts_by_rep is a dict {rep count} (from `registry::remaining_by_rep`, Plan 16-02). Only reps present in GAME_REPS are shown (skip unknown keys defensively — dict-safe iteration over GAME_REPS, not over the dict).
- easy_mode semantics: falsy (0/false/"") OR empty counts_by_rep -> total-only string (this is the "easy all-zero" case — v1 falls back to total-only when no per-rep data exists).
- Add `format_remaining` to the existing `namespace export` line.
- Purity: no mol/tk/after. GAME_REPS is already a namespace variable of this module.
- tclsh NOT in WSL: stage + headless VMD + `BCHM_TEST_RESULT` marker; scan full output for `ERROR)` / `bad switch`. Tcl 8.5 only; braced expr; one variable per line.
- IMPORTANT: do NOT touch any existing proc. This plan appends one proc + export entry + tests only (the 41 existing cases are the regression gate).
</context>

<feature>
  <name>Pure remaining-count formatter</name>
  <files>vmd/lib/setup_state.tcl, vmd/tests/test_setup_state.test</files>
  <behavior>
    - `format_remaining 3 [dict create] 0` -> "Remaining: 3"
    - `format_remaining 3 [dict create] 1` -> "Remaining: 3" (easy but no counts -> total-only)
    - `format_remaining 5 [dict create VDW 5] 1` -> "Remaining: 5  (VDW: 5)"
    - `format_remaining 7 [dict create Lines 3 VDW 4] 1` -> "Remaining: 7  (Lines: 3, VDW: 4)" (GAME_REPS order: Lines precedes VDW)
    - counts of 0 are omitted; reps not in GAME_REPS are omitted; hard mode ignores counts entirely
  </behavior>
  <implementation>
    One proc appended near the end of setup_state.tcl (after randomize_state), plus the export-list extension. Build the paren part with foreach over GAME_REPS + dict exists/dict get, lappend parts, join with ", ". Header comment notes the v1 source line and the two-space rule.
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: RED — append the failing format_remaining tests</name>
  <files>vmd/tests/test_setup_state.test</files>
  <action>
Append BEFORE the marker block (keep all existing cases untouched). 5 cases:
1. `format_remaining_hard_mode` — (3, empty dict, 0) -> exact "Remaining: 3"
2. `format_remaining_easy_no_counts` — (3, empty dict, 1) -> "Remaining: 3"
3. `format_remaining_easy_single_rep` — (5, {VDW 5}, 1) -> "Remaining: 5  (VDW: 5)" (two spaces — build the expected string carefully)
4. `format_remaining_easy_multi_rep_game_reps_order` — (7, {VDW 4 Lines 3}, 1) -> "Remaining: 7  (Lines: 3, VDW: 4)" — dict insertion order VDW-first must NOT leak; GAME_REPS order wins
5. `format_remaining_ignores_zero_and_unknown` — (4, {VDW 0 Lines 4 Bogus 9}, 1) -> "Remaining: 4  (Lines: 4)"
  </action>
  <verify>Staged run of `vmd/tests/test_setup_state.test` -> new cases FAIL (proc missing), existing cases still pass.</verify>
  <done>RED state confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement format_remaining</name>
  <files>vmd/lib/setup_state.tcl</files>
  <action>
Implement the proc per <implementation>; add it to `namespace export`. Port v1 setup_state.py:418-447 logic (read that file first).
  </action>
  <verify>Staged run -> `BCHM_TEST_RESULT` with Failed=0 (Total = existing count + 5).</verify>
  <done>All setup_state tests green including the 5 new ones.</done>
</task>

<task type="auto">
  <name>Task 3: Gates + regression</name>
  <files>vmd/lib/setup_state.tcl, vmd/tests/test_setup_state.test</files>
  <action>
Gates:
1. Purity: `grep -nE "\bmol\b|\batomselect\b|\btk\b|\bafter\b" vmd/lib/setup_state.tcl` -> ZERO matches (the file was already pure; keep it so).
2. 8.6 gate on both files -> ZERO matches.
3. Full suite green.
Commits: `test(16-04): add failing format_remaining tests (GAME-03 display string)`, then `feat(16-04): implement format_remaining (pure remaining-count formatter)`.
  </action>
  <verify>Gates zero matches; full suite Failed=0.</verify>
  <done>Formatter shipped pure and green; existing behavior untouched.</done>
</task>

</tasks>

<verification>
- `BCHM_TEST_RESULT ... Failed=0` from the staged headless run (Total = prior total + 5)
- Zero `ERROR)` / `bad switch` lines in the full output
- Purity + 8.6 gates zero matches
- git diff on setup_state.tcl shows ONLY the new proc + export-list change
</verification>

<success_criteria>
- format_remaining available and exported; exact-string behavior proven (two-space rule, GAME_REPS order)
- Ready for Plan 16-09's update_remaining to consume
</success_criteria>

<output>
After completion, create `.planning/phases/16-mvp-core-loop-sphere/16-04-SUMMARY.md`
</output>
