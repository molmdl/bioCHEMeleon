---
phase: 16
plan: 12
type: execute
wave: 5
depends_on: [16-11]
files_modified: [vmd/tests/pick_verify.tcl, vmd/AGENTS.md, .planning/phases/16-mvp-core-loop-sphere/16-VERIFICATION.md, .planning/STATE.md]
autonomous: false
must_haves:
  truths:
    - "THE PICK CONTRACT IS LOCKED: a real mouse click in a real VMD GUI session fires the ::vmd_pick_event trace with the correct vmd_pick_atom (0-based index) and vmd_pick_mol (game molid) — or the documented fallback (labelpoll / mouse callback on) is activated"
    - "A complete round is played with a real mouse: Start -> countdown -> pick mode -> clicks find hiders (green) -> remaining decrements -> timer stops -> win message with time -> cleanup restores mouse mode"
    - "Rotate/Pick toggle works mid-game (hotkey r + panel radios); hidden reps never contain unfound unpickable hiders"
    - "vmd/AGENTS.md picking section reflects the LOCKED contract (MEDIUM flag removed); STATE.md carries the lesson"
  artifacts:
    - path: "vmd/tests/pick_verify.tcl"
      provides: "staged GUI verification script driving research SS3 steps 1-9 with vmdcon -info prompts"
    - path: ".planning/phases/16-mvp-core-loop-sphere/16-VERIFICATION.md"
      provides: "human-recorded answers for all 9 checkpoint questions + keep-vs-delete decisions"
    - path: "vmd/AGENTS.md"
      provides: "updated Picking mechanism section (locked contract, no MEDIUM flag)"
    - path: ".planning/STATE.md"
      provides: "Phase-16 pick-contract decision lesson in Accumulated Context"
  key_links:
    - from: "vmd/tests/pick_verify.tcl"
      to: "the real VMD GUI session"
      via: "human performs clicks/keys; script prints step prompts + reads live state (label list, molinfo, mouse globals)"
      pattern: "vmdcon -info"
---

<objective>
Lock the VMD pick-callback contract and verify the complete playable loop in a REAL VMD GUI session — the phase's one human-verify checkpoint (ROADMAP SC2/SC5).

Purpose: everything headless-provable is already green (16-11). This session proves what text mode cannot: VMD's C-side pick firing, visual blending, the modal-win render timing, and the toggle UX. Its verdict either confirms the trace mechanism as primary or activates a researched fallback.
Output: `vmd/tests/pick_verify.tcl` + human-verified `16-VERIFICATION.md` + locked docs.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/16-mvp-core-loop-sphere/16-RESEARCH-pick.md
@.planning/phases/16-mvp-core-loop-sphere/16-RESEARCH-sphere.md
@vmd/lib/pick_bridge.tcl

Key facts:
- Research pick SS3 is the checkpoint spec (steps 1-9, the keep-vs-delete decision table). The lock step is #2: the trace must fire on a real click with exact values (vmd_pick_atom = 0-based index, vmd_pick_mol = game molid, shift-state present).
- The script is STAGED and run from Windows-visible storage. GUI VMD cannot run from WSL — the HUMAN runs `vmd` on Windows (or `bash -ic "vmd -e ..."` from a /mnt/c cwd opens the Windows GUI). Instruct the human precisely; OpenCode prepares the script + reads results from the console log the human pastes into 16-VERIFICATION.md.
- Tk console: the human opens it via VMD menu (Extensions -> Tk Console) for the trace/phantom steps.
- NEVER claim headless results validate the C-side pick — that is exactly what this session decides.
- The checkpoint task below is the ONLY human-verify task in Phase 16 (constraint: exactly one, in the final plan).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the GUI verification script</name>
  <files>vmd/tests/pick_verify.tcl</files>
  <action>
Implement research SS3 steps 1-9 as a runnable script with `vmdcon -info` prompts and state dumps the human can read/copy:
- STEP 1 prompt: load 1k8p via `demos::load_demo 1k8p`, `source vmd/biochemeleon.tcl` (path relative to staging root), print "open the dialog, set hider count 3, press Start" and dump `[::biochemeleon::game_tab::game_state]` + mouse globals after Start.
- STEP 2 (THE LOCK): print the exact trace snippet for the human to paste into the Tk console (the `apply {{args} {...}}` one-liner from research SS3 step 2 — vmdcon -info "PICK ev=$args atom=$vmd_pick_atom mol=$vmd_pick_mol shift=..."), then prompt: "click a hider sphere; record whether the trace fired and the EXACT values".
- STEP 3: print `mouse mode pick 0` + prompt (query-mode fire?).
- STEP 4: print `mouse callback off` + prompt (still fires?).
- STEP 5: prompt "press r, drag, click an atom" (no pick, view rotates?) then "toggle Pick atoms on the panel, click" (finds again?).
- STEP 6: prompt about label accumulation (clean view? `label list Atoms` shows the click labels?).
- STEP 7: print the phantom snippet (`proc phantom_cb {args} {...}; catch {lappend ::vmd_pick_atom_callbacks phantom_cb}`) + prompt (PHANTOM FIRED ever appears?).
- STEP 8: print `mol showrep <gm> <found_rep_idx> off` instructions + prompt (click where the hider was -> no find?), then on again.
- STEP 9: prompt "find all 3 hiders" — dump timer/remaining/win box appearance; after Cleanup dump mouse mode + label count.
Keep every step's expected observation in a comment. No headless execution of this file.
  </action>
  <verify>`bash -n`-equivalent: staged `vmd -dispdev text` sourcing the file WITHOUT executing steps (guard the whole body behind `if {![info exists ::tk_version]} { vmdcon -warn "GUI only"; return }` style check at top) — headless run prints the GUI-only warning and exits clean; `grep -c "STEP" vmd/tests/pick_verify.tcl` >= 9.</verify>
  <done>pick_verify.tcl ready to stage; headless-safe (no-op without Tk).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: GUI session — lock the pick contract + play a full round</name>
  <what-built>
The complete Phase-16 MVP loop: Start button (generates blended VDW sphere hiders via PDB-rebuild), Game tab with 3-2-1 countdown, ticking timer, rolling info log, remaining counter, click-to-find via the PickBridge (trace on ::vmd_pick_event, mouse mode pick 2), found hiders turn green, win message with time, Rotate/Pick toggle, mouse-mode restore on cleanup. All headless layers are green (16-11 capstone).
  </what-built>
  <how-to-verify>
Stage first (OpenCode): `mkdir -p tmp/biochemeleon-vmd && cp -r vmd tmp/biochemeleon-vmd/`.
Then run a REAL VMD GUI session on Windows (not -dispdev text) and perform, in order:
1. Open a VMD GUI; cd to `tmp/biochemeleon-vmd`; in the Tk console: `source vmd/tests/pick_verify.tcl` and follow its prompts.
2. STEP 1 — press Start (demo 1k8p, 3 hiders): CONFIRM the mouse switches to pick mode (VMD Mouse menu shows Pick -> Pick Atom; clicking an atom creates a label), the Game tab activates, 3-2-1 counts down in the log, the timer starts at GO.
3. STEP 2 — THE LOCK: paste the printed trace snippet into the Tk console; click a hider sphere. RECORD: did the trace fire? Exact `vmd_pick_atom` (expect 0-based index 555-557), `vmd_pick_mol` (expect the game molid), shift-state presence.
4. STEP 3 — `mouse mode pick 0` then click: does the event still fire? Restore `mouse mode pick 2` (or the panel radio).
5. STEP 4 — with `mouse callback off`: repeat step 2's click. Still fires?
6. STEP 5 — press `r`, drag-rotate, click an atom: no pick + view rotates? Toggle "Pick atoms" on the panel: clicking finds again?
7. STEP 6 — after a few finds: labels accumulated on clicked atoms? Did the game's auto-delete keep the view clean?
8. STEP 7 — paste the phantom snippet; click atoms: does "PHANTOM FIRED" ever appear?
9. STEP 8 — hide a rep containing an unfound hider (`mol showrep`), click where it was: no find? Restore: find works?
10. STEP 9 — find all 3: found spheres turn green, remaining hits 0, timer stops, "You win!" message shows the time; after Cleanup the mouse mode is restored (was rotate) and labels are clean.
Paste the full console output + your answers into `.planning/phases/16-mvp-core-loop-sphere/16-VERIFICATION.md` (create it from the step list above with PASS/FAIL + observed values per step).
  </how-to-verify>
  <resume-signal>Type "approved" (all steps pass; trace fired in step 2) or describe the failing steps / observed values.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post-lock doc updates (keep-vs-delete + lessons)</name>
  <files>vmd/AGENTS.md, .planning/STATE.md, .planning/phases/16-mvp-core-loop-sphere/16-VERIFICATION.md</files>
  <action>
Apply research SS3 "After the lock — keep vs delete" to the VERIFIED outcome:
- If step 2 fired (expected): KEEP trace as primary; KEEP labelpoll dormant; DELETE any positional-(molid atom) assumption comments; confirm `vmd_pick_mol` naming. Update `vmd/AGENTS.md` section "Picking mechanism": replace the MEDIUM-confidence guidance (callbacks-list + `mouse mode pick 0` + human-verify-needed) with the LOCKED contract (trace ::vmd_pick_event + {args} + globals vmd_pick_atom/vmd_pick_mol + `mouse mode pick 2`; forbidden forms; label hygiene; PickBridge is vmd/lib/pick_bridge.tcl). Remove the "GUI human-verify checkpoint needed" flag.
- If step 2 did NOT fire but step 6 showed labels: flip `pick_bridge::mechanism` default to labelpoll + note the after-poll loop as follow-up; update AGENTS.md accordingly.
- If step 4 required `mouse callback on`: add it to pick_bridge activate/deactivate.
- If step 7 fired "PHANTOM FIRED" (unexpected): record the discovery in VERIFICATION.md and keep the shim; do NOT gate on it.
- Record in `.planning/STATE.md` Accumulated Context (one decision entry): the locked pick contract + the checkpoint verdict + any fallback activated. Update STATE.md Current Position (Phase 16 complete pending verification roll-up).
Commits: `feat(16-12): lock pick contract via GUI human-verify` + `docs(16): update AGENTS.md/STATE with locked pick contract`.
  </action>
  <verify>`grep -n "MEDIUM" vmd/AGENTS.md` -> no picking-mechanism MEDIUM flag remains; VERIFICATION.md contains all 9 step verdicts; STATE.md contains the new decision entry; pick_bridge.tcl matches the locked outcome (mechanism default / callback-on if required).</verify>
  <done>Pick contract locked in code + docs; Phase 16's GUI-verify obligation discharged.</done>
</task>

</tasks>

<verification>
- 16-VERIFICATION.md records all 9 steps with observed values and a final verdict
- vmd/AGENTS.md picking section states the LOCKED contract without the MEDIUM flag
- Any fallback (labelpoll flip / mouse callback on) is both documented and reflected in pick_bridge.tcl
- STATE.md carries the decision for future phases (17.1/17.2 generators build on this loop)
</verification>

<success_criteria>
- ROADMAP SC2 (pick identifies registered hiders, contract locked via real GUI session) and SC5 (pick-vs-rotate control) are HUMAN-VERIFIED
- All 5 phase success criteria now have both headless proof (16-11) and GUI proof (this session)
</success_criteria>

<output>
After completion, create `.planning/phases/16-mvp-core-loop-sphere/16-12-SUMMARY.md`
</output>
