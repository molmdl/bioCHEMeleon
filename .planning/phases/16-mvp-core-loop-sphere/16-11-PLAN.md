---
phase: 16
plan: 11
type: execute
wave: 4
depends_on: [16-10]
files_modified: [vmd/smoke/phase16_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "One headless smoke drives the COMPLETE game loop through the public composition surface: start_game (real sphere placement) -> add hider reps -> countdown ticks -> begin_play -> pick_bridge activate -> simulated picks (miss / hit / already-found / winning hit) -> win state + frozen timer + win callback -> deactivate (mouse restored) -> cleanup (registry 0, game_molid deleted)"
    - "The pick chain is exercised through pick_bridge's REAL _on_event (simulated fire), not by calling game::on_pick directly"
    - "All Phase-16 phase criteria reachable headlessly are asserted (SC1/SC3/SC4 logic layers + SC2's delivery machinery); the GUI-only remainder is explicitly deferred to 16-12"
    - "Full-log false-PASS scan (ERROR) + bad switch) is clean"
  artifacts:
    - path: "vmd/smoke/phase16_smoke.tcl"
      provides: "Phase-16 EXIT GATE: end-to-end loop smoke with rigorous assertions; BCHM_SMOKE_RESULT marker"
  key_links:
    - from: "vmd/smoke/phase16_smoke.tcl"
      to: "all Phase-16 modules"
      via: "public-surface composition: game::start_game/on_pick/cleanup + pick_bridge::activate/_on_event-sim/deactivate + game_logic + registry + hiders"
      pattern: "(game::start_game|pick_bridge::activate|game_logic::finish_win)"
---

<objective>
Write and run the Phase-16 headless capstone: one smoke proving the entire core loop works end-to-end (minus the physical mouse click, which is 16-12's GUI checkpoint).

Purpose: the Phase-15 capstone proved the mutation pipeline; this proves the LOOP. After this passes, the only unproven claim left in the phase is VMD's C-side pick firing + visual behavior — exactly what the human-verify session locks.
Output: `vmd/smoke/phase16_smoke.tcl` (PASS=1 first or second run).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/16-mvp-core-loop-sphere/16-RESEARCH-pick.md
@vmd/smoke/phase15_smoke.tcl

Conventions (copy phase15_smoke.tcl exactly): sources the lib files in dependency order directly (mirrors the entry, NOT the entry itself): setup_state, registry, generators, game_logic, demos, backup, mutation, hiders, game, pick_bridge (registry sourced ONCE). `[pwd]`-based paths; `set failures [list]` + `_bail` + per-step catch; `BCHM_SMOKE_RESULT PASS=1 FAIL=none` / `PASS=0 FAIL=...`; `exit`; every atomselect deleted; Tcl 8.5 only; braced expr.
- Simulated pick protocol (research pick SS4 item 3): with the bridge active, `set ::vmd_pick_atom <idx>; set ::vmd_pick_mol $gm; set ::vmd_pick_event 1` fires the REAL trace -> _on_event -> game::on_pick. This validates the tcl mechanics ONLY — never claim it validates VMD's C-side firing (GUI-verify's job, 16-12).
- Mouse-mode snapshot: record `vmd_mouse_mode`/`vmd_mouse_submode` before activate; after deactivate assert restored (fresh session default rotate/-1).
- Timer: game_logic timer_start uses real `clock seconds` — sleep is forbidden; assert elapsed >= 0 and frozen (two timer_elapsed reads equal after won) rather than exact values.
- Do NOT stage copies of prior smokes' assertions — this smoke composes the loop; module-level behavior is already proven by 16-05/06/07/08 smokes.
- Run + scan: `timeout 300 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/smoke/phase16_smoke.tcl -eofexit < /dev/null' 2>&1` -> grep marker; scan FULL output for `ERROR)` and `bad switch` (false-PASS guard).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Phase-16 capstone smoke</name>
  <files>vmd/smoke/phase16_smoke.tcl</files>
  <action>
Stages (each in catch + _bail):
1. SETUP: `demos::load_demo 1k8p` (555 atoms); register recording callbacks (`game::set_callbacks`); snapshot mouse-mode globals; add a VDW rep on the original (so backup::apply carries a rep forward — mirrors real usage), record pre-start numreps.
2. GENERATE (SC1 logic): `game::start_game $m 5` -> 560 atoms; 5 sentinels at 555-559 (segid GAME); hider coords within the ORIGINAL bbox (+/- epsilon); numreps == pre-start + 2; hidden rep read-back (style VDW, color Element, sel exact string) + found rep read-back (ColorID 7).
3. COUNTDOWN->PLAY: `game_logic::round_reset` -> `begin_countdown` -> 4x `countdown_tick` (assert the {"3" 0}.."GO!" 1 sequence) -> `begin_play` -> state "playing"; timer_elapsed == 0-ish.
4. PICK ARMED: `pick_bridge::activate [dict get $gs game_molid]` -> assert `vmd_mouse_mode` == "labelatom", submode 2; assert active flag.
5. MISS: simulate pick on index 0 -> log ends "Miss!"; count_remaining 5; no registry change.
6. HIT + ALREADY: simulate 555 -> status found, remaining 4, user2 > 0, log "Found one! 4 remaining"; simulate 555 again -> "Already found!", remaining still 4.
7. WIN (LOOP-03/GAME-02): simulate 556, 557, 558, 559 -> after the last: state "won"; WINS callback fired EXACTLY once with {elapsed hider_count==5}; elapsed >= 0 and two timer_elapsed reads identical (frozen); win log line "You found all 5 hiders in".
8. INERT AFTER WIN: simulate 0 and 555 -> no new log lines, WINS still 1 (state gate + finish_win guard).
9. DEACTIVATE: `pick_bridge::deactivate` -> trace gone (simulate fire -> no delivery), mouse mode restored to the pre-activate snapshot, click labels cleaned.
10. CLEANUP (Phase-15 regression through the loop's exit): `game::cleanup $gs` -> 555 atoms, count_hiders 0, game_molid deleted (catch-molinfo nonzero), restored numreps == pre-start.
11. GATES: grep our own lib files for `mouse mode 4` (zero) and `_on_event {args}` (present) — belt-and-suspenders assertions inside the smoke.
  </action>
  <verify>Run: `BCHM_SMOKE_RESULT PASS=1 FAIL=none` + zero `ERROR)`/`bad switch` in the FULL log.</verify>
  <done>Capstone green: the complete loop works headlessly through the public surface.</done>
</task>

<task type="auto">
  <name>Task 2: Regression sweep + gates</name>
  <files>vmd/smoke/phase16_smoke.tcl</files>
  <action>
1. Re-run ALL prior smokes + tests against fresh staging (the 15-05 verifier lesson): test_registry.test, test_setup_state.test, test_generators.test, test_game_logic.test, phase16_hiders_smoke.tcl, phase16_pick_smoke.tcl, phase16_placement_smoke.tcl, phase16_onpick_smoke.tcl, phase16_gametab_smoke.tcl, phase16_entry_smoke.tcl, phase15_smoke.tcl — all markers green.
2. Gates: 8.6 grep over vmd/lib/ + vmd/gui/ zero matches; `grab set` grep over vmd/gui/ zero matches.
3. Fix anything red (in the OWNING file, minimal diff), re-run until green.
  </action>
  <verify>All markers: `BCHM_TEST_RESULT ... Failed=0` x4 + `BCHM_SMOKE_RESULT PASS=1` x7; gates zero matches.</verify>
  <done>Whole-repo test/smoke suite green; Phase 16 is headless-complete.</done>
</task>

</tasks>

<verification>
- `BCHM_SMOKE_RESULT PASS=1 FAIL=none` for phase16_smoke.tcl, zero `ERROR)` / `bad switch`
- All 4 tcltest suites + all prior smokes green on fresh staging
- 8.6 + modeless gates zero matches
</verification>

<success_criteria>
- The full loop (generate -> countdown -> play -> find -> win -> cleanup) is proven end-to-end headlessly
- The ONLY remaining unverified claims are GUI-only (real click firing, visuals, modal timing) — handed to 16-12
</success_criteria>

<output>
After completion, create `.planning/phases/16-mvp-core-loop-sphere/16-11-SUMMARY.md`
</output>
