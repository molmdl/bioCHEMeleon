---
phase: 16
plan: 15
type: execute
wave: 2
depends_on: [16-13]
files_modified: [vmd/smoke/phase16_restart_smoke.tcl]
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "double-start mid-round yields a fresh single-generation round honoring the NEW settings (count), with the old game molecule deleted (no leak)"
    - "double-start after a won round — the exact observed 561-atom defect — yields atom count = original + N, exactly N sentinels at the top indices, registry count N: no stacked generations"
    - "Start on a DIFFERENT live molecule cleans the old round and builds the new one; the old round's restored original survives loaded (cleanup ran, mutate consumed only the new target)"
  artifacts:
    - path: "vmd/smoke/phase16_restart_smoke.tcl"
      provides: "headless regression gate for the 16-13 active-game guard ($failures capstone pattern; BCHM_SMOKE_RESULT marker)"
      min_lines: 150
  key_links:
    - from: "vmd/smoke/phase16_restart_smoke.tcl"
      to: "game::start_game"
      via: "repeated direct invocation through the public surface (2nd/3rd/4th start with an active stash — never on_pick, never the bridge)"
      pattern: "game::start_game"
    - from: "vmd/smoke/phase16_restart_smoke.tcl"
      to: "registry::count_hiders"
      via: "per-round registry assertions (rebuilt per round, never accumulated across generations)"
      pattern: "count_hiders"
---

<objective>
Prove the 16-13 active-game guard headlessly with a dedicated regression smoke: double-start mid-round, double-start after-win (the exact observed defect), and different-target restart — each asserting single-generation invariants (atom count = original + N, exactly N sentinels, registry N, old game molecule deleted, viewpoint/reps still applied).

Purpose: VERIFICATION gap 1's "verifiable headlessly (regression assert in a new smoke)" half. The GUI re-check lands in 16-16; this smoke is the always-runnable gate that makes stacked generations a tested-impossible state.
Output: `vmd/smoke/phase16_restart_smoke.tcl` (PASS=1) + regression sweep of the start_game-touching smokes.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/lib/game.tcl
@vmd/smoke/phase16_smoke.tcl
@vmd/smoke/phase15_smoke.tcl
@.planning/phases/16-mvp-core-loop-sphere/16-11-SUMMARY.md
@.planning/phases/15-mutation-safety-hider-registry/15-05-SUMMARY.md

Key facts:
- HARNESS PATTERN (16-11 capstone, authoritative): assertions accumulate into a `$failures` list; the closing marker prints `BCHM_SMOKE_RESULT PASS=1 FAIL=none` ONLY when `$failures` is empty (a bare marker without a real failure list is the false-PASS anti-pattern); every stage in catch + a `_bail` escape so a mid-script error cannot silently skip asserts (VMD `-e` catches top-level errors and CONTINUES).
- LIFT VERBATIM from `vmd/smoke/phase15_smoke.tcl`: the recursive `_flat`/`_vp_maxdiff` viewpoint flattener (nested 4x4 matrices; naive abs() errors; string-eq drifts) and the viewpoint capture form — backup::snapshot's EXACT positional combined-braces get `{rotate_matrix center_matrix scale_matrix global_matrix}`.
- Source list (dep order, mirrors the entry's lib block, registry sourced EXACTLY ONCE): `setup_state → registry → generators → demos → backup → mutation → hiders → game`. generators IS required (start_game → mutation::make_placeholder_hiders → generators::sphere_positions since 16-07); hiders IS required (start_game → add_hider_reps since 16-08). pick_bridge + game_logic are NOT needed — the guard path never touches them (the game.tcl layer cannot distinguish won vs mid-round: both keep current_state; state machine is GUI-side).
- Demo data resolves from `<root>/vmd/data/demos/` via demos.tcl's source-time `script_dir` — staging the whole `vmd/` tree (cp -r) suffices (the proven 15-05 recipe).
- Sentinel selector is ALWAYS `resname GAM and beta < 0` (never `beta -999` exact); every atomselect gets `$sel delete`.
- "Segments: 2" is a VMD load-log observation, not a molinfo query — the in-smoke AUTHORITATIVE stacking proof is atom count + exact sentinel index set + registry count (a stacked generation would show 2N sentinels / +N atoms). The runner may eyeball Segments in the log; do not parse it as an assertion.
- Tcl 8.5 only (braced expr, foreach+lappend, catch); `regexp --` if any regexp (leading-dash patterns parse as switches); `[pwd]`-relative sourcing; marker printed BEFORE any `exit`; `bash -ic` exit codes are unreliable — the marker is authoritative, never `$?`.
- `rm` is denied by permission rules — stage into a FRESH dir (or cp -r overwrite), never rm.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write phase16_restart_smoke.tcl</name>
  <files>vmd/smoke/phase16_restart_smoke.tcl</files>
  <action>
Write the smoke mirroring `vmd/smoke/phase16_smoke.tcl`'s harness shape ($failures + per-stage catch + _bail + marker + exit), with `_assert {expr} {tag}` (lappend tag on failure) and `_fail_all`-style bail on caught stage errors. Header comment: what the guard defect was (VERIFICATION gap 1, observed 561-atom/Segments-3/555-559 stacking), what each stage proves, and the note that the game.tcl layer cannot distinguish won vs mid-round (faithful after-win simulation = registry remaining 0 with all records found).

STAGE 0 — setup:
- `demos::load_demo 1k8p` → assert 555 atoms (`orig1`).
- Add one VDW rep (numreps 1→2) + mutate the viewpoint (`rotate x by 30; rotate y by 45; scale to 0.8; translate by 0.5 0.5 0.5`) so the SC4-forward/restore assertions are non-vacuous (15-05 stage pattern).
- Record `saved_numreps` and `vp_orig` with backup::snapshot's EXACT positional combined-braces get form.

STAGE A — double-start MID-ROUND with NEW-count semantics (fresh session → guard fires):
- `gs1 = game::start_game $orig1 3` → assert: `dict exists` all three keys (shape frozen); game_molid atoms == 558; sentinels (`resname GAM and beta < 0`) exactly 3 with sorted index list eq `{555 556 557}`; `registry::count_hiders` == 3; numreps == saved_numreps + 2; `_vp_maxdiff vp_orig <current>` < 1e-4.
- `gs2 = game::start_game [dict get $gs1 game_molid] 2` — target = the OLD GAME MOLECULE (the exact observed defect path), NEW count 2. Assert: `gs2.game_molid != gs1.game_molid`; gs2 atoms == 557 (555+2); sentinels exactly 2, sorted eq `{555 556}`; `count_hiders` == 2 (registry REBUILT — never 5); gs1.game_molid DEAD (`catch {molinfo ...}` nonzero — the 15-05 leak-guard pattern); gs2 numreps == saved_numreps + 2; vp < 1e-4.

STAGE B — double-start AFTER WIN (the exact GUI defect: remaining 0, all found):
- Mark gs2's hiders found: `registry::mark_found` on each sentinel index → assert `registry::count_remaining` == 0 (the controller's won-state precondition; the smoke does not source game_logic — note why in a comment).
- `gs3 = game::start_game [dict get $gs2 game_molid] 3` (Start after win, same target, count 3). Assert: gs3.game_molid != gs2.game_molid; gs3 atoms == 558; sentinels exactly 3 sorted eq `{555 556 557}`; `count_hiders` == 3; gs2.game_molid DEAD; gs3 numreps == saved_numreps + 2.

STAGE C — different-target restart (live-target branch of the guard):
- `orig2 = demos::load_demo 1znf` → assert 424 atoms, LIVE.
- `gs4 = game::start_game $orig2 2` with gs3 still active. Assert: gs4.game_molid != gs3.game_molid; gs4 atoms == 426; sentinels exactly 2 sorted eq `{424 425}`; `count_hiders` == 2; gs3.game_molid DEAD; the round-3 restored 1k8p original SURVIVES loaded — scan `[molinfo list]` for a molecule with numatoms == 555 and 0 sentinels (the guard's cleanup restored it; stage C's start consumed only 1znf) → assert at least one exists.

END: print `BCHM_SMOKE_RESULT PASS=[...] FAIL=[...]` (PASS=1 only when $failures empty; FAIL=none or the comma-joined tags), then `exit`.

Conventions: Tcl 8.5 only; braced expr; sorted-list compare for sentinel indices (`lsort -integer` + list eq); every atomselect `$sel delete`; catch-guard `dict get` on game_state keys (tag the failure, never raise past a stage); no `regexp` with leading-dash patterns (use `regexp --` if needed); keep log lines short.
  </action>
  <verify>
1. `grep -rnE "\blmap\b|\btry\b|\bthrow\b|\btailcall\b|\bcoroutine\b|\byield\b|\bfinally\b" vmd/smoke/phase16_restart_smoke.tcl` → zero (8.5 gate).
2. `grep -c "BCHM_SMOKE_RESULT" vmd/smoke/phase16_restart_smoke.tcl` → ≥1 and the marker line derives from `$failures` (`grep -n "failures" vmd/smoke/phase16_restart_smoke.tcl` shows the list feeding the marker).
3. `grep -c "game::start_game" vmd/smoke/phase16_restart_smoke.tcl` → 4 (stages A×2, B, C).
  </verify>
  <done>The smoke exists, follows the $failures capstone pattern, and drives all three double-start scenarios through the public game surface only.</done>
</task>

<task type="auto">
  <name>Task 2: Stage, run, and regression-sweep</name>
  <files>vmd/smoke/phase16_restart_smoke.tcl</files>
  <action>
Run the new smoke + the start_game-touching regression smokes on fresh staging (cp -r overwrite; NEVER rm — denied):
1. `mkdir -p tmp/restart16 && cp -r vmd tmp/restart16/`
2. New smoke: `timeout 300 bash -ic 'cd tmp/restart16 && vmd -dispdev text -e vmd/smoke/phase16_restart_smoke.tcl -eofexit < /dev/null' > tmp/restart16_out.txt 2>&1; grep BCHM_SMOKE_RESULT tmp/restart16_out.txt` → expect `BCHM_SMOKE_RESULT PASS=1 FAIL=none`.
   - If FAIL tags appear: fix the SMOKE first (assertion/setup bugs) or the guard in game.tcl (real defects — report them in the summary); re-run to PASS=1.
3. Full-log false-PASS scan: `grep -c "ERROR)" tmp/restart16_out.txt` → 0 AND `grep -c "bad switch" tmp/restart16_out.txt` → 0. Also eyeball the atom-count narrative (555 → 558 → 557 → 558 → 426) and the Segments fields in the load log (expect Segments: 2 on every combined reload — never 3).
4. Regression sweep (the guard must be a NO-OP for fresh-session starts; existing smokes never double-start except via cleanup/restart which empty the stash first) — same staging, one VMD run each, expect `BCHM_SMOKE_RESULT PASS=1 FAIL=none` + zero `ERROR)`/`bad switch` in each log:
   - `vmd/smoke/phase15_game_smoke.tcl` (start/cleanup/restart round-trip)
   - `vmd/smoke/phase15_smoke.tcl` (Phase-15 capstone)
   - `vmd/smoke/phase16_onpick_smoke.tcl` (start_game + scoring)
   - `vmd/smoke/phase16_smoke.tcl` (Phase-16 capstone)
5. Fix anything red in the OWNING file (the established 16-11 Task-2 rule); a red regression that traces to the 16-13 guard is a REAL defect — fix game.tcl, re-run everything.
  </action>
  <verify>
`grep BCHM_SMOKE_RESULT` on all five outputs → PASS=1 FAIL=none each; `grep -c "ERROR)"` and `grep -c "bad switch"` on all five logs → 0 each; the restart smoke's log shows the 555→558→557→558→426 atom narrative with single-generation sentinel sets.
  </verify>
  <done>The guard is proven headlessly (3 scenarios, single-generation invariants, leak guards, viewpoint/reps intact) and no start_game-touching smoke regressed.</done>
</task>

</tasks>

<verification>
- phase16_restart_smoke.tcl PASS=1 FAIL=none on fresh staging, zero `ERROR)`/`bad switch` in the full log.
- Regression: phase15_game_smoke, phase15_smoke, phase16_onpick_smoke, phase16_smoke all PASS=1 (guard is a no-op without an active stash).
- The three gap scenarios each assert: atom count = original + N, exactly N sentinels at the top indices, registry N, old game molid deleted, numreps = saved + 2, viewpoint < 1e-4.
</verification>

<success_criteria>
- VERIFICATION gap 1's headless half is closed: the observed stacking defect (561 atoms / Segments: 3 / two sentinel generations) is now a TESTED-IMPOSSIBLE state, re-runnable by any future verifier.
- Phase-15/16 smoke suite remains green with the guard merged.
</success_criteria>

<output>
After completion, create `.planning/phases/16-mvp-core-loop-sphere/16-15-SUMMARY.md`
</output>
