---
phase: 16
plan: 08
type: execute
wave: 2
depends_on: [16-02, 16-03, 16-05]
files_modified: [vmd/lib/game.tcl, vmd/smoke/phase16_onpick_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "start_game adds the two hider reps (hiders::add_hider_reps) between backup::apply and registry reconstruct — ordering otherwise unchanged"
    - "on_pick {idx} implements the v1 three-way guard: unregistered -> 'Miss!' log only; already found -> 'Already found!' log only; hidden -> mark_found + mark_found_visual + found log"
    - "on_pick is gated on game_logic state == playing (no scoring during countdown/idle/won)"
    - "Last find (count_remaining == 0) triggers finish_win exactly once and fires the win callback with (elapsed, hider_count)"
    - "game_state dict shape stays FROZEN {game_molid hider_count snapshot}; the current game_state is stashed in a game.tcl namespace var, NOT threaded through pick_bridge"
  artifacts:
    - path: "vmd/lib/game.tcl"
      provides: "start_game + hider-rep step, on_pick {idx}, set_callbacks {log_cb remaining_cb win_cb}, current-state stash"
      exports: ["start_game", "cleanup", "restart", "on_pick", "set_callbacks"]
    - path: "vmd/smoke/phase16_onpick_smoke.tcl"
      provides: "headless smoke: miss/hit/already-found/win/state-gate/double-win-prevention with recording callbacks; PASS=1"
  key_links:
    - from: "vmd/lib/pick_bridge.tcl (Plan 16-06)"
      to: "vmd/lib/game.tcl"
      via: "_on_event forwards game::on_pick $index (single arg)"
      pattern: "game::on_pick"
    - from: "vmd/lib/game.tcl on_pick"
      to: "vmd/lib/registry.tcl + vmd/lib/hiders.tcl + vmd/lib/game_logic.tcl"
      via: "status_of guard -> mark_found + mark_found_visual + log_append -> count_remaining -> finish_win"
      pattern: "(registry::mark_found|hiders::mark_found_visual|game_logic::finish_win)"
---

<objective>
Extend `vmd/lib/game.tcl` (the composition root) with the click-scoring controller: the hider-rep step in start_game, `on_pick {idx}` (v1 game.py on_pick 1:1 logic), and the callback interface — plus its headless smoke.

Purpose: LOOP-01 (click-to-find marks found), LOOP-02 (registry status is the single source of truth — the caller-side guard), LOOP-03 (win condition), GAME-01 data (log lines). The GUI (16-09) registers callbacks; PickBridge (16-06) delivers indices.
Output: modified `vmd/lib/game.tcl` + `vmd/smoke/phase16_onpick_smoke.tcl` (PASS=1).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/16-mvp-core-loop-sphere/16-RESEARCH-sphere.md
@.planning/phases/16-mvp-core-loop-sphere/16-RESEARCH-gametab.md
@vmd/lib/game.tcl
@.planning/phases/04-mvp-core-loop-sphere/04-03-SUMMARY.md

Key facts (do NOT re-derive):
- v1 logic to port (04-03, game.py:118-190): `on_pick`: registry lookup -> miss ("Miss!", no harm) -> already found ("Already found!") -> else mark_found + recolor + "Found one! %d remaining" + remaining callback + win check. Registry is the single source of truth.
- Pitfall 5 (research SS8): `mark_found` silently overwrites — the THREE-WAY GUARD MUST LIVE HERE (caller-side), checking `registry::status_of $idx` BEFORE mark_found. Check `registry::is_hider` FIRST (unregistered -> miss, and status_of of an unregistered index is "" not an error).
- State gate (research gametab SS6.10): a stray pick outside `playing` must be a no-op. Gate: `if {[::biochemeleon::game_logic::state] ne "playing"} { return }` at the top of on_pick.
- game_state shape FROZEN (15-05): `{game_molid hider_count snapshot}`. on_pick takes ONE arg (the index — the PickBridge contract from 16-06); game.tcl stashes the current game_state in a namespace var set by start_game (`variable current_state`) and cleared by cleanup (`variable current_state [dict create]`). Do NOT change the dict shape; do NOT thread game_state through pick_bridge.
- start_game ordering (NON-NEGOTIABLE, 15-04): snapshot -> make_placeholder_hiders -> mutate -> backup::apply -> **NEW: hiders::add_hider_reps $game_molid** -> registry reconstruct (the DI line is UNTOUCHED: `[list apply {lambda} $game_molid]`). The hider-rep step goes after apply (base numreps deterministic) and before/after reconstruct — either works; put it right after backup::apply per research SS5.5.
- Win flow: after mark_found, `set rem [registry::count_remaining]`; `log_append found $rem`; if rem == 0: `game_logic::finish_win` (freezes elapsed; errors on double), log the win line (`log_append win "You found all [registry::count_hiders] hiders in [game_logic::format_mmss $elapsed]!"` — elapsed = `game_logic::timer_elapsed` AFTER finish_win, which returns the frozen value), then invoke win_cb with (elapsed, hider_count). PickBridge deactivation is the GUI's job (on_win) — game.tcl does NOT touch pick_bridge.
- Callback interface (v1 set_callbacks game.py:94-111): `set_callbacks {log_cb remaining_cb win_cb}` stores command prefixes in ns vars `_cb_log/_cb_remaining/_cb_win` (default no-op-safe: init to empty lists; invocation is `catch {{*}$cb $args}` so unset callbacks are harmless). log_cb receives ONE arg (the formatted line from game_logic::log_append's return). remaining_cb receives ZERO args (pull model — the GUI reads the registry itself). win_cb receives TWO args (elapsed_seconds hider_count).
- game.tcl still sources NOTHING (references hiders/game_logic at CALL time — call-time resolution).
- Smoke conventions: phase15_smoke.tcl pattern. The smoke registers RECORDING callbacks (`lappend ::LOG_LOG $line` etc.) to prove invocation.
- Staging + headless + `BCHM_SMOKE_RESULT` parse + full-log `ERROR)`/`bad switch` scan.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend game.tcl (hider-rep step + on_pick + callbacks)</name>
  <files>vmd/lib/game.tcl</files>
  <action>
1. Extend the namespace export: `start_game cleanup restart on_pick set_callbacks`.
2. Namespace vars: `current_state [dict create]`, `_cb_log [list]`, `_cb_remaining [list]`, `_cb_win [list]` (one variable per line).
3. In start_game: after `backup::apply` and BEFORE the reconstruct block, insert `::biochemeleon::hiders::add_hider_reps $game_molid` with a comment (research SS5.5). At the END of start_game (after building the return dict), stash: `set current_state $gs` (build the dict into $gs first, stash, then return it). Update the header + proc comments.
4. In cleanup: at the end, `set current_state [dict create]` (round over).
5. New proc `set_callbacks {log_cb remaining_cb win_cb}` — store the three prefixes.
6. New proc `on_pick {idx}`:
   - Gate: `if {[::biochemeleon::game_logic::state] ne "playing"} { return }`
   - Guard the stash: `if {[dict size $current_state] == 0} { return }`
   - `if {![::biochemeleon::registry::is_hider $idx]} { catch {{*}$_cb_log [::biochemeleon::game_logic::log_append miss ""]}; return }`
   - `if {[::biochemeleon::registry::status_of $idx] eq $::biochemeleon::registry::HIDER_STATUS_FOUND} { catch {{*}$_cb_log [::biochemeleon::game_logic::log_append already ""]}; return }`
   - Hidden: `::biochemeleon::hiders::mark_found_visual [dict get $current_state game_molid] $idx`; `::biochemeleon::registry::mark_found $idx`; `set rem [::biochemeleon::registry::count_remaining]`; `catch {{*}$_cb_log [::biochemeleon::game_logic::log_append found $rem]}`; `catch {{*}$_cb_remaining}`.
   - Win: `if {$rem == 0}` -> `::biochemeleon::game_logic::finish_win`; `set elapsed [::biochemeleon::game_logic::timer_elapsed]`; `catch {{*}$_cb_log [::biochemeleon::game_logic::log_append win "You found all [::biochemeleon::registry::count_hiders] hiders in [::biochemeleon::game_logic::format_mmss $elapsed]!"]}`; `catch {{*}$_cb_win $elapsed [dict get $current_state hider_count]}`.
   - Wrap the whole body in `catch` + `vmdcon -err` (a throwing on_pick inside the pick trace would block VMD's write — same rule as the PickBridge handler).
Keep cleanup/restart semantics otherwise identical.
  </action>
  <verify>`grep -n "add_hider_reps" vmd/lib/game.tcl` -> exactly 1; `grep -n "proc ::biochemeleon::game::on_pick {idx}" vmd/lib/game.tcl` -> 1; 8.6 gate zero; the DI reconstruct line byte-unchanged (git diff shows no change to that block).</verify>
  <done>game.tcl extended with the controller surface; frozen contracts intact.</done>
</task>

<task type="auto">
  <name>Task 2: Write + run the on_pick smoke</name>
  <files>vmd/smoke/phase16_onpick_smoke.tcl</files>
  <action>
Model on phase15_smoke.tcl. Source lib files in dep order INCLUDING game_logic and hiders (setup_state, registry, generators, game_logic, demos, backup, mutation, hiders, game). Steps:
1. `demos::load_demo 1k8p` -> `set gs [game::start_game $m 5]` (assert 560 atoms, 5 sentinels, 7 reps on gm — 5 from snapshot-apply if any + 2 hider reps... assert `numreps >= 2` more defensively: read numreps and assert it grew by 2 over the pre-start value).
2. Register recording callbacks: `game::set_callbacks {lappend ::LOG_LOG} {lappend ::REM_TICKS} {lappend ::WINS}`.
3. State gate: with game_logic state idle, `game::on_pick 555` -> assert nothing logged, registry unchanged.
4. Drive to playing: `game_logic::round_reset; game_logic::begin_countdown`; 4x `game_logic::countdown_tick`; `game_logic::begin_play` (timer_start uses real clock — fine).
5. Miss: `game::on_pick 0` -> LOG_LOG ends with "Miss!", count_remaining still 5.
6. Hit: `game::on_pick 555` -> status_of 555 == "found", count_remaining 4, user2 of 555 > 0 (read-back), log line "Found one! 4 remaining".
7. Already-found: `game::on_pick 555` again -> log "Already found!", count_remaining still 4.
8. Win: pick 556..559 -> after the last, game_logic::state == "won", WINS has exactly 1 entry with 2 elements (elapsed >= 0, hider_count 5), win log line contains "You found all 5 hiders in".
9. Double-win prevention: `game::on_pick 0` after win -> no new log line, WINS still length 1 (state gate + finish_win guard).
10. `game::cleanup $gs` -> registry 0; current_state empty (on_pick with a stale idx after cleanup -> no-op, no error).
Marker + exit.
  </action>
  <verify>Staged run: `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)`/`bad switch` in full log.</verify>
  <done>Full scoring + win flow proven headlessly with recording callbacks.</done>
</task>

</tasks>

<verification>
- `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch`
- game_state dict shape unchanged; DI reconstruct line unchanged; start_game ordering comment updated
- 8.6 gate zero matches on both files
</verification>

<success_criteria>
- on_pick implements the exact v1 three-way scoring with the caller-side guard
- Win path fires exactly once with (elapsed, hider_count); stray picks are inert
- The GUI (16-09) and PickBridge (16-06) contracts are both satisfied without modification
</success_criteria>

<output>
After completion, create `.planning/phases/16-mvp-core-loop-sphere/16-08-SUMMARY.md`
</output>
