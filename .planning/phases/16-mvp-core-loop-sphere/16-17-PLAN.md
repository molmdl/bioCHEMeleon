---
phase: 16
plan: 17
type: execute
wave: 4
depends_on: [16-16]
files_modified: [vmd/lib/pick_bridge.tcl, vmd/smoke/phase16_pick_smoke.tcl, vmd/AGENTS.md, .planning/STATE.md]
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "the pick contract is CLEANLY locked: the mouse-callback caveat is resolved per the recorded A/B verdict — either the on/off pairing is in the bridge and smoke-proven, or the verified non-requirement is recorded and the mechanism is untouched"
    - "the bridge's saved-state discipline is preserved: any new saved state follows the activate-snapshot / deactivate-restore pattern with a safe fallback (the non-atomic mode-set philosophy)"
    - "vmd/AGENTS.md and STATE.md carry the resolved verdict — no 'pending re-verify' caveat remains in the picking section"
  artifacts:
    - path: "vmd/lib/pick_bridge.tcl"
      provides: "A/B verdict applied: mouse callback pairing (branch a) or dated non-requirement comment (branch b)"
      contains: "mouse callback"
    - path: "vmd/AGENTS.md"
      provides: "picking-section caveat resolved with the re-verify verdict"
    - path: ".planning/STATE.md"
      provides: "dated decision entry closing the pick-contract caveat"
  key_links:
    - from: "vmd/lib/pick_bridge.tcl activate/deactivate"
      to: "mouse callback"
      via: "the on/off pairing per the A/B verdict (branch a) or the recorded non-requirement (branch b)"
      pattern: "mouse callback"
    - from: "vmd/AGENTS.md picking section"
      to: "the 16-16 re-verify record"
      via: "the resolved verdict replacing the pending-caveat wording"
      pattern: "re-verify|A/B"
---

<objective>
Apply the recorded A/B verdict (16-16) to `pick_bridge.tcl` — BOTH branches fully specified — and close the pick-contract caveat in the docs, so Truth 3 ("the pick contract is LOCKED") holds without caveats.

Purpose: VERIFICATION.md's keep-vs-delete table deliberately did NOT add `mouse callback on` to the bridge ("hypothesis unconfirmed... the A/B test decides FIRST, then the mechanism is touched"). The verdict now exists; this plan executes its consequence.
Output: `vmd/lib/pick_bridge.tcl` (pairing or recorded non-requirement), `vmd/smoke/phase16_pick_smoke.tcl` (branch a only), `vmd/AGENTS.md` + `.planning/STATE.md` (verdict recorded), regression green.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-mvp-core-loop-sphere/16-VERIFICATION.md
@vmd/lib/pick_bridge.tcl
@vmd/smoke/phase16_pick_smoke.tcl
@vmd/AGENTS.md

Key facts:
- READ FIRST: the "Re-verify session (plan 16-16)" section at the end of 16-VERIFICATION.md. Its disposition line selects the branch:
  - BRANCH (a) — finds require `mouse callback on` (ab_off_finds=n AND ab_on_finds=y): add the pairing.
  - BRANCH (b) — finds fired in BOTH states (ab_off_finds=y): no functional change; record the verified non-requirement.
- UG 9.3.23 documents ONLY `callback on/off` (no query form). The 16-16 record says whether the no-arg `mouse callback` query actually worked in 1.9.3 — this decides branch (a)'s save/restore sub-design:
  - Query works → proper snapshot/restore: `variable saved_callback` + snapshot in activate + restore in deactivate (same philosophy as saved_mode/saved_submode, including a safe fallback).
  - Query does not work → set-on at activate / set-off at deactivate, with a header comment documenting the no-query limitation + the default-off assumption (www.tcl never enables it; fresh-session default is off).
- pick_bridge structure to respect: `activate` snapshots saved_mode/saved_submode (steps 1-2), engages `mouse mode pick 2` (step 3), registers the trace (step 4); `deactivate` removes the trace, cancels labelpoll, cleans labels, restores the mode with a rotate fallback, clears active/active_mol. The phantom shim (lappend to ::vmd_pick_atom_callbacks) stays a no-op gesture — never read/branch on it.
- The existing smoke (`phase16_pick_smoke.tcl`, 9 steps, PASS=1) proves tcl-side machinery only — real-click firing stays GUI-only evidence (16-16's record).
- `regexp --` for any leading-dash pattern; Tcl 8.5 only; one-per-line `variable` declarations; the 8.6-idiom gate must stay zero.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the A/B verdict to pick_bridge.tcl (+ smoke when branch a)</name>
  <files>vmd/lib/pick_bridge.tcl, vmd/smoke/phase16_pick_smoke.tcl</files>
  <action>
Read the 16-16 re-verify record's disposition line, then execute EXACTLY ONE branch:

BRANCH (a) — finds require `mouse callback on`:
1. `activate`: after the saved_mode/saved_submode snapshot and alongside the `mouse mode pick 2` engagement:
   - If the 16-16 record says the query form WORKED: add `variable saved_callback` (one-per-line in the namespace block) + snapshot it with the recorded working query form + `catch {mouse callback on}`.
   - If the query form did NOT work: just `catch {mouse callback on}` in activate.
2. `deactivate`: after the mode restore, restore the callback state:
   - Query works: `catch {mouse callback $saved_callback}` with a fallback `catch {mouse callback off}` (the non-atomic mode-set philosophy: a failed restore must not leave a hybrid state); clear the var.
   - Query does not work: `catch {mouse callback off}` + a header comment documenting the no-query limitation (UG 9.3.23 on/off only) and the default-off assumption.
3. Update the header MECHANISM VERDICT section: the callback caveat is RESOLVED — one dated line with the A/B verdict (off-finds/on-finds) and the pairing rationale.
4. Update `vmd/smoke/phase16_pick_smoke.tcl`'s activate/deactivate steps:
   - Query works: set `mouse callback off` before activate → after activate assert the callback state reads ON (via the query) → after deactivate assert it reads OFF/restored.
   - Query does not work: structural assertions instead — read pick_bridge.tcl's text and assert `mouse callback on` appears within activate's body and the restore/off appears within deactivate's (string-first line-order check, the entry-smoke structural pattern), plus the existing no-error activation asserts.
   - Re-run the smoke (Task 2).

BRANCH (b) — callback irrelevant (finds fired in BOTH states):
1. NO functional change to activate/deactivate/set_view_mode (byte-stable behavior).
2. Add ONE dated comment to the header's MECHANISM VERDICT section (and mirror it at the SS3 keep-vs-delete callback row if present): `mouse callback A/B resolved <date>: finds fire in BOTH callback states (16-VERIFICATION.md re-verify record); the session-1 "User Pick:" correlation was VMD's console echo of the callback, not a delivery prerequisite -- callback on/off deliberately NOT added.` Keep it under the same < ~70-col line discipline as the rest of the header.
3. `vmd/smoke/phase16_pick_smoke.tcl` untouched.

EITHER BRANCH — never: read/branch on ::vmd_pick_atom_callbacks; change the mechanism default; touch the {args} signature, the molid/index filters, the label hygiene, or the rc==1 error classifier.
  </action>
  <verify>
1. Branch (a): `grep -n "mouse callback" vmd/lib/pick_bridge.tcl` → the on-set in activate AND the restore/off in deactivate (plus the header verdict line); branch (b): the dated non-requirement comment present and NO functional `mouse callback` commands in activate/deactivate bodies (`grep -n "mouse callback" vmd/lib/pick_bridge.tcl` shows comment-only matches).
2. 8.5 gate zero on touched vmd/ files: `grep -rnE "\blmap\b|\btry\b|\bthrow\b|\btailcall\b|\bcoroutine\b|\byield\b|\bfinally\b" vmd/lib/pick_bridge.tcl vmd/smoke/phase16_pick_smoke.tcl` → zero.
3. Branch (a) only: the smoke gains the callback assertions (`grep -c "callback" vmd/smoke/phase16_pick_smoke.tcl` ≥ the new asserts).
  </verify>
  <done>The verdict's code consequence is applied exactly once, in the bridge, with the saved-state discipline intact (branch a) or the mechanism proven-untouched with the non-requirement recorded (branch b).</done>
</task>

<task type="auto">
  <name>Task 2: Docs close-out + regression sweep</name>
  <files>vmd/AGENTS.md, .planning/STATE.md</files>
  <action>
1. `vmd/AGENTS.md` — Picking mechanism section: replace the "pending re-verify / NOT added to pick_bridge yet" caveat bullets with the resolved verdict:
   - Branch (a): document that the bridge now pairs `mouse callback on` at activate with restore/off at deactivate (and the no-query-form limitation if that sub-case applied), per the A/B verdict + date.
   - Branch (b): record that the A/B found finds fire in both callback states — the correlation was the console echo; callback on/off deliberately not used; the caveat is CLOSED.
   Keep the CONFIRMED facts (trace/{args}/globals, forbidden forms, hidden-rep rule, label premise) and the "text-mode smoke is not C-side proof" warning untouched.
   ALSO in `vmd/AGENTS.md` — Phase-19 note (~line 109): the sentence "`start_game` has no active-game guard (double-Start … 561-atom combined PDB … `Segments: 3`)" is now FALSE after 16-13 — rewrite it to state that `start_game` auto-restarts via the active-game guard (landed 16-13, headless-proven 16-15), so double-Start can no longer stack generations. Keep the rest of the note (no Cleanup/Restart buttons — Phase 19 scope; Setup-Reset fields-only mismatch) unchanged — those facts remain true.
2. `.planning/STATE.md` — one dated decision entry in Accumulated Context: the re-verify session outcome (A/B verdict, exact PICK values captured or why not, freeze observation, cleanup restore, guard re-check PASS) and the resulting pick_bridge disposition. Do NOT rewrite Current Position (the phase verifier owns the roll-up).
3. Regression on fresh staging (`mkdir -p tmp/gap17 && cp -r vmd tmp/gap17/`):
   - `timeout 300 bash -ic 'cd tmp/gap17 && vmd -dispdev text -e vmd/smoke/phase16_pick_smoke.tcl -eofexit < /dev/null' > tmp/gap17_pick.txt 2>&1; grep BCHM_SMOKE_RESULT tmp/gap17_pick.txt` → PASS=1 FAIL=none.
   - `timeout 300 bash -ic 'cd tmp/gap17 && vmd -dispdev text -e vmd/smoke/phase16_smoke.tcl -eofexit < /dev/null' > tmp/gap17_cap.txt 2>&1; grep BCHM_SMOKE_RESULT tmp/gap17_cap.txt` → PASS=1 FAIL=none (the capstone drives activate/deactivate — the pairing must not break the loop composition).
   - Full-log scan on both: `grep -c "ERROR)"` → 0 AND `grep -c "bad switch"` → 0.
   - Branch (b): the pick smoke must be byte-identical in behavior (it was untouched) — the run is a pure regression check.
4. Commits: branch (a) `feat(16-17): pair mouse callback on/off in pick_bridge (A/B verdict)` + `docs(16-17): close the pick-contract callback caveat`; branch (b) `docs(16-17): record verified mouse-callback non-requirement (A/B verdict)`.
  </action>
  <verify>
1. `grep -n "pending re-verify\|NOT added yet\|NOT added to pick_bridge" vmd/AGENTS.md` → zero matches in the picking section (caveat closed; wording may survive only inside historical decision records in .planning/, not in AGENTS.md).
2. `grep -n "no active-game guard" vmd/AGENTS.md` → zero matches (the stale Phase-19 guard sentence is rewritten; the "no Cleanup/Restart buttons" and Reset-mismatch facts remain).
2. Both regression runs PASS=1 FAIL=none with zero `ERROR)`/`bad switch`.
3. `.planning/STATE.md` contains the new dated decision entry (`grep -n "16-17\|re-verify" .planning/STATE.md` → present).
  </verify>
  <done>The callback caveat is closed in code-or-record per the verdict; the pick smoke and capstone stay green; AGENTS.md/STATE.md carry the resolved contract for Phase 17+.</done>
</task>

</tasks>

<verification>
- The 16-16 disposition line selects exactly one branch; its consequence is fully applied (pairing + smoke asserts, or dated non-requirement comment).
- phase16_pick_smoke.tcl + phase16_smoke.tcl PASS=1 on fresh staging, full-log scan clean.
- AGENTS.md picking section has no pending-caveat wording; STATE.md has the decision entry.
- The trace mechanism, {args} signature, filters, label hygiene, rc==1 classifier, and phantom shim are untouched.
</verification>

<success_criteria>
- Truth 3 (SC2-lock) is cleanly closed: the pick contract is locked WITHOUT the callback caveat — either the delivery prerequisite is managed by the bridge, or it is proven unnecessary.
- Phase 17+ inherits a documented, regression-guarded pick contract with no open hypotheses.
</success_criteria>

<output>
After completion, create `.planning/phases/16-mvp-core-loop-sphere/16-17-SUMMARY.md`
</output>
