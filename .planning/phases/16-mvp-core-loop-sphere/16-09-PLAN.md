---
phase: 16
plan: 09
type: execute
wave: 2
depends_on: [16-02, 16-03, 16-04, 16-06]
files_modified: [vmd/gui/game_tab.tcl, vmd/smoke/phase16_gametab_smoke.tcl]
autonomous: true
must_haves:
  truths:
    - "Game tab builds eagerly with: status labels (timer/remaining/mouse-mode via -textvariable), a core-Tk read-only text log + PLAIN scrollbar, and Rotate/Pick ttk::radiobuttons in a Mouse-mode labelframe"
    - "Countdown is a chained ONE-SHOT after 1000 with ID TRACKING (after cancel before re-arm); the timer is a SELF-RESCHEDULING after 1000 tick that re-arms ONLY while state == playing"
    - "Timer epoch starts at GO (begin_play), never at Start-button press; elapsed is the drift-free game_logic delta"
    - "Every after callback guards winfo exists + catch (a closed dialog must never raise bad window path name); stop_all_timers cancels both ids"
    - "on_win: cancel tick -> deactivate pick bridge exactly once -> mode label back to Rotate -> after 100 -> tk_messageBox (parented to the dialog) -> win line in the log"
    - "The Game tab never calls `mouse mode` directly — all mouse switching goes through pick_bridge (set_view_mode/activate/deactivate)"
  artifacts:
    - path: "vmd/gui/game_tab.tcl"
      provides: "::biochemeleon::game_tab (build/start_round/countdown_step/tick/on_log_line/update_remaining/on_win/set_mouse_mode/set_difficulty/stop_all_timers/raise_tab); source-time safe headless"
      exports: ["build", "start_round", "stop_all_timers", "raise_tab", "set_difficulty"]
    - path: "vmd/smoke/phase16_gametab_smoke.tcl"
      provides: "headless load-gate smoke: sources clean under -dispdev text, all procs exist, pure-layer round-trip sanity"
  key_links:
    - from: "vmd/gui/game_tab.tcl"
      to: "vmd/lib/game_logic.tcl"
      via: "round_reset/begin_countdown -> countdown_tick per after step -> begin_play at GO -> timer_elapsed per tick"
      pattern: "game_logic::(round_reset|begin_countdown|countdown_tick|begin_play|timer_elapsed|format_mmss|log_append)"
    - from: "vmd/gui/game_tab.tcl"
      to: "vmd/lib/pick_bridge.tcl"
      via: "radios -> set_view_mode; GO branch -> activate game_molid; on_win -> deactivate"
      pattern: "pick_bridge::(set_view_mode|activate|deactivate)"
    - from: "vmd/gui/game_tab.tcl update_remaining"
      to: "vmd/lib/registry.tcl + vmd/lib/setup_state.tcl"
      via: "format_remaining [count_remaining] [remaining_by_rep] $easy_mode -> remain_text"
      pattern: "setup_state::format_remaining"
---

<objective>
Create `vmd/gui/game_tab.tcl` — the player-facing Game tab (GAME-01 rolling log, GAME-02 timer, GAME-03 remaining, 3-2-1 countdown, win message, pick-vs-rotate control) — plus its headless load-gate smoke.

Purpose: the VIEW layer of the MVP loop. All decision logic lives in game_logic/registry/game.tcl (Plans 16-02/03/08); this module owns only Tk widgets, `after` scheduling, and callback registration.
Output: `vmd/gui/game_tab.tcl` + `vmd/smoke/phase16_gametab_smoke.tcl` (PASS=1).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/16-mvp-core-loop-sphere/16-RESEARCH-gametab.md
@vmd/gui/setup_tab.tcl
@vmd/gui/dialog.tcl

Key facts (do NOT re-derive):
- Research gametab SS2.1-2.3 is the build spec (widget tree + proc list); SS4 is the after-discipline rule list; SS6 the pitfalls. Follow them.
- Widget tree: `$nb.game` frame gets (1) status frame: ttk::label .timer ("0:00") / .remain ("Remaining: -") / .mode ("Mouse: Rotate") each bound with `-textvariable ::biochemeleon::game_tab::<name>`; (2) "Info log:" label; (3) log frame: CORE `text` widget (`-state disabled -wrap word -height 10`, `-yscrollcommand {.logf.sb set}`) + PLAIN `scrollbar` (`-command {.logf.log yview}`) — NOT ttk::scrollbar (never exercised in VMD's Tk; plain is the ecosystem-verified idiom); (4) "Mouse mode" ttk::labelframe with two ttk::radiobuttons "Rotate" (value rotate) / "Pick atoms" (value pick) sharing `-variable ::biochemeleon::game_tab::mouse_mode`, `-command {::biochemeleon::game_tab::set_mouse_mode}`. Layout: status top fill-x, logf top fill-both-expand, mouse bottom anchor-w.
- File shape: ONLY `namespace eval` + proc definitions — zero widget commands execute at source time (headless-safe, same as setup_tab.tcl; no tk_version guard needed at source).
- after discipline (research SS4, all 7 rules): track `after_tick`/`after_countdown` ns vars; `catch {after cancel $id}` before EVERY re-arm; every callback FIRST guards `if {![winfo exists $::biochemeleon::game_tab::w]} { return }` and wraps widget writes in catch; re-arm conditionally (tick only while `[game_logic::state] eq "playing"`; countdown only while not done); schedule by VALUE with fully-qualified literals (`set after_tick [after 1000 ::biochemeleon::game_tab::tick]`); reschedule at the END of tick; NEVER `update`, NEVER `vwait`.
- countdown_step: call `game_logic::countdown_tick` -> destructure {label done}; `log_append countdown $label` + on_log_line; if !done: cancel old id, `set after_countdown [after 1000 ::biochemeleon::game_tab::countdown_step]`; if done: `log_append countdown "GO!"` + on_log_line, `game_logic::begin_play`, `pick_bridge::activate [dict get $game_state game_molid]`, set mode_text "Mouse: Pick" + mouse_mode var pick, start the tick loop.
- start_round {game_state}: `stop_all_timers` FIRST (defensive, v1 gui_game.py:285 "Restart mid-game"); stash game_state ns var; register callbacks: `game::set_callbacks [list ::biochemeleon::game_tab::on_log_line] [list ::biochemeleon::game_tab::update_remaining] [list ::biochemeleon::game_tab::on_win]` (the 16-08 signature: log_cb gets 1 arg, remaining_cb 0 args, win_cb 2 args); `game_logic::round_reset; game_logic::begin_countdown`; clear the text widget; log "Get ready..." (log_append countdown + insert); call countdown_step DIRECTLY (no delay for "3" — v1 parity).
- on_log_line {line}: the ONLY proc that touches the text widget: winfo-exists guard -> `state normal` -> `insert end "$line\n"` -> `state disabled` -> `see end` (Pitfall 6.8: never leave state normal).
- update_remaining: pull model — `set total [registry::count_remaining]`; `set byrep [registry::remaining_by_rep]`; `set remain_text [setup_state::format_remaining $total $byrep $easy_mode]`.
- on_win {elapsed n_hiders}: cancel tick id; set mode_text "Mouse: Rotate" + mouse_mode rotate; `pick_bridge::deactivate` (exactly once); win line already logged by game.tcl (16-08) — do NOT double-log; then `after 100` a guarded message box: winfo-exists check -> `tk_messageBox -parent $w -icon info -title "You win!" -message "You found all $n_hiders hiders in [game_logic::format_mmss $elapsed]!"` (research SS1.3 Bug A 100ms render delay + Bug B parent = the top-level).
- set_mouse_mode {mode}: set mode_text ("Mouse: Pick"/"Mouse: Rotate") + call `pick_bridge::set_view_mode $mode` inside catch.
- set_difficulty {easy}: stores easy_mode ns var (default 1) — called by on_start (16-10) BEFORE start_round.
- raise_tab: `$w.nb select $w.nb.game` (ttk::notebook select — NEVER `raise` on the child).
- stop_all_timers: catch-cancel both ids. Also called from dialog on_close (16-10).
- Namespace vars (ONE PER LINE — the name-VALUE trap): w, nb_path (or read from w), timer_text, remain_text, mode_text, mouse_mode, easy_mode, game_state, after_tick, after_countdown.
- The game_state ns var here is the game_tab's own stash for start_round/countdown_step — it does NOT replace game.tcl's `current_state`.
- Smoke = LOAD GATE only (Tk does not load headless; after-callbacks never fire under -e): source game_tab.tcl under `-dispdev text`, assert every proc exists (`info procs`), assert zero widget commands ran (no error = pass), assert the pure-layer round trip it depends on (game_logic full countdown sequence + format_remaining + registry remaining) still works. Model on `vmd/smoke/phase14_gui_smoke.tcl` (loading-layer-only precedent).
- Tcl 8.5: brace expr; catch patterns; `eq` for strings.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write vmd/gui/game_tab.tcl</name>
  <files>vmd/gui/game_tab.tcl</files>
  <action>
Implement per the context spec: header comment (purpose, view-only statement, the after-discipline rules, the headless-safety note), namespace eval with one-per-line vars, then the 11 procs (build, start_round, countdown_step, tick, on_log_line, update_remaining, on_win, set_mouse_mode, set_difficulty, stop_all_timers, raise_tab). Widget paths under `$nb.game` per the tree above. Mirror setup_tab.tcl's code style (proc comments, `variable` declarations at proc tops).
  </action>
  <verify>`tclsh`-free syntax gate: staged `bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/gui/game_tab.tcl -eofexit < /dev/null'` exits without a tcl error (sourcing defines procs only); `grep -nE "\blmap\b|\btry\b|\bthrow\b|\btailcall\b|\bcoroutine\b|\byield\b|\bfinally\b" vmd/gui/game_tab.tcl` -> ZERO; `grep -n "mouse mode" vmd/gui/game_tab.tcl` -> ZERO (the tab never touches VMD mouse commands directly — only pick_bridge does); `grep -cn "vwait" vmd/gui/game_tab.tcl` -> 0.</verify>
  <done>game_tab.tcl complete; headless-source-safe; mouse-mode discipline enforced via pick_bridge only.</done>
</task>

<task type="auto">
  <name>Task 2: Write + run the load-gate smoke</name>
  <files>vmd/smoke/phase16_gametab_smoke.tcl</files>
  <action>
Model on phase14_gui_smoke.tcl. Steps:
1. Source the pure deps the tab calls at runtime (setup_state, registry, game_logic) + game_tab.tcl itself — sourcing must not error under -dispdev text.
2. Assert proc existence for all 11 game_tab procs (`llength [info procs ::biochemeleon::game_tab::*]` >= 11 and each named one exists).
3. Pure-layer sanity (the tab's runtime dependencies): full game_logic round trip (round_reset -> begin_countdown -> 4 ticks -> begin_play -> finish_win) returns won with frozen elapsed; `setup_state::format_remaining 3 [dict create VDW 3] 1` == "Remaining: 3  (VDW: 3)"; registry reconstruct+count_remaining round trip.
4. Assert the textvariables initialize only at build (they may be unset headless — assert `info exists` is 0 for timer_text BEFORE any build call, proving no widget code ran at source).
5. Assert game_tab contains no direct `mouse mode` / `vwait` / `grab set` strings (read the file in tcl and string match, or grep in the runner).
Marker (use BCHM_SMOKE_RESULT for consistency) + exit.
  </action>
  <verify>Staged run: `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)`/`bad switch` in full log.</verify>
  <done>Load gate green; widget rendering explicitly deferred to the 16-12 GUI checkpoint.</done>
</task>

</tasks>

<verification>
- `BCHM_SMOKE_RESULT PASS=1 FAIL=none`, zero `ERROR)` / `bad switch`
- 8.6 gate + `vwait`/`mouse mode`/`grab set` greps zero matches on game_tab.tcl
- Every `after` in the file is assigned to a tracked var and preceded (at its re-arm site) by a catch-cancel
</verification>

<success_criteria>
- The Game tab view is complete and wired to game_logic/registry/pick_bridge/game contracts without owning any decision logic
- After-discipline hazards (stray callbacks after close, double chains, countdown-time leakage, drift) are structurally prevented
</success_criteria>

<output>
After completion, create `.planning/phases/16-mvp-core-loop-sphere/16-09-SUMMARY.md`
</output>
