---
phase: 11-alt-conf-cartoon-hider
plan: 03
type: execute
wave: 2
depends_on: ["11-02"]
files_modified:
  - tests/test_persistence.py
autonomous: true

must_haves:
  truths:
    - "build_bcm_dict carries the 3 alt-conf fields (is_altconf, endpoint_resvs, alt_tag) through the registry.to_dict() passthrough"
    - "parse_bcm_dict accepts a sidecar with the 3 new optional fields (NO version bump — version stays 1)"
    - "apply_bcm_dict → reconcile_with_bcm restores the 3 alt-conf fields on a sentinel-rebuilt registry"
    - "A Phase 8 sidecar WITHOUT the 3 new fields still round-trips (backward compatible — defaults applied)"
  artifacts:
    - path: "tests/test_persistence.py"
      provides: "WSL round-trip tests proving alt-conf fields survive build→parse→apply + backward-compat with Phase 8 sidecars"
      contains: "class TestBcmAltconfRoundtrip"
  key_links:
    - from: "tests/test_persistence.py:TestBcmAltconfRoundtrip"
      to: "biochemeleon/persistence.py:build_bcm_dict + apply_bcm_dict"
      via: "MockController.registry with alt-conf records → build → parse → apply on fresh sentinel-rebuilt registry"
      pattern: "is_altconf"
---

<objective>
Add WSL round-trip tests proving the 3 alt-conf registry fields (from Plan 02) survive the full `.bcm` persistence chain (build_bcm_dict → parse_bcm_dict → apply_bcm_dict) with NO version bump, AND that Phase 8 sidecars without the new fields still load gracefully (backward compatible).

Purpose: persistence.py is a PASS-THROUGH layer for the new fields (build_bcm_dict calls `controller.registry.to_dict()`; apply_bcm_dict calls `reconcile_with_bcm` — both extended in Plan 02 to carry `is_altconf`/`endpoint_resvs`/`alt_tag`). This plan proves the END-TO-END chain preserves the fields, closing research Open Risk 3 (`.bcm` round-trip of alt-conf metadata) at the pure WSL tier before the cmd-coupled smoke (Plan 07) verifies `.pse` `alt` survival. No production code change is expected in persistence.py — if a change is needed, it is a defensive read/default (documented in the task).
Output: Extended `tests/test_persistence.py` (WSL green). persistence.py unchanged (or a 1-line defensive tweak if the test reveals a gap).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-alt-conf-cartoon-hider/11-RESEARCH.md

@.planning/phases/11-alt-conf-cartoon-hider/11-02-SUMMARY.md

@biochemeleon/persistence.py
@biochemeleon/registry.py
@tests/test_persistence.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add alt-conf .bcm round-trip tests (build → parse → apply)</name>
  <files>tests/test_persistence.py</files>
  <action>
Add a `TestBcmAltconfRoundtrip` class to `tests/test_persistence.py`. REUSE the existing `MockController` helper (lines 43-60) and `_sample_setup()` (lines 63-71) — do NOT duplicate them. The `MockController` has a `.registry = HiderRegistry()`; you will register alt-conf records onto it. Import nothing new beyond what the file already imports (HiderRegistry is already imported).

Tests to add:
1. `test_build_carries_altconf_fields`: create a `MockController`; `reg = mc.registry; reg.register('1ubq', 100, 'cartoon', is_altconf=True, endpoint_resvs=(2,4), alt_tag='B')`; `d = build_bcm_dict(mc, _sample_setup(), kind='checkpoint')`; assert `d['registry']['hiders'][0]['is_altconf'] is True`, `d['registry']['hiders'][0]['endpoint_resvs'] == [2,4]` (list — JSON form), `d['registry']['hiders'][0]['alt_tag'] == 'B'`; AND `d['version'] == 1` (NO version bump — research §8).
2. `test_build_omits_defaults`: register a NON-alt-conf record `reg.register('1ubq', 101, 'spheres')`; `d = build_bcm_dict(...)`; assert the hider dict for id 101 does NOT contain keys `is_altconf`, `endpoint_resvs`, `alt_tag` (compact sidecar; backward-compatible with Phase 8).
3. `test_parse_accepts_altconf_fields`: build a dict with an alt-conf hider (via build_bcm_dict), `raw = json.dumps(d)`; `d2 = parse_bcm_dict(raw)`; assert `d2['registry']['hiders'][0]['is_altconf'] is True` and `d2['version'] == 1` (parse does NOT reject the new fields).
4. `test_apply_restores_altconf_on_sentinel_rebuild`: build a `MockController` with an alt-conf record; `d = build_bcm_dict(...)`. Create a SECOND `MockController` (`mc2`), manually sentinel-rebuild its registry: `mc2.registry.register('1ubq', 100, rep=None)` (simulating `reconstruct_from_sentinels` — rep=None, defaults). Then `mm = apply_bcm_dict(mc2, d)`. Assert `mc2.registry.get('1ubq', 100).is_altconf is True`, `mc2.registry.get('1ubq', 100).endpoint_resvs == (2,4)` (TUPLE — list→tuple coercion from Plan 02), `mc2.registry.get('1ubq', 100).alt_tag == 'B'`, and `mc2.registry.get('1ubq', 100).rep == 'cartoon'` (reconcile restores rep too). `mm` is a `ReconcileMismatches` with empty mismatch lists (perfect match).
5. `test_apply_restores_altconf_mixed_registry`: register BOTH an alt-conf (id 100, cartoon, is_altconf=True, endpoint_resvs=(2,4)) AND a sphere (id 101, spheres) on mc; build; apply on mc2 with both sentinel-rebuilt (id 100 rep=None, id 101 rep=None). Assert id 100 gets is_altconf=True/endpoint_resvs=(2,4)/alt_tag='B' AND id 101 gets is_altconf=False/endpoint_resvs=None/alt_tag='' (defaults — the sphere was never alt-conf).
6. `test_altconf_endpoint_resvs_json_list_roundtrip`: build with endpoint_resvs=(2,4); `raw = json.dumps(d)`; `d2 = parse_bcm_dict(raw)`; the parsed endpoint_resvs is a list `[2,4]` (JSON has no tuples); then `apply_bcm_dict` coerces it back to a tuple on the record (assert `isinstance(mc2.registry.get('1ubq',100).endpoint_resvs, tuple)`).

If ALL tests pass with persistence.py UNCHANGED (expected — it is pass-through), commit only the test file. If ANY test reveals persistence.py needs a defensive read (e.g. apply_bcm_dict does not pass the fields because reconcile_with_bcm in Plan 02 wasn't wired), DO NOT fix it here — that is a Plan 02 regression; report it in the SUMMARY and fail the task. (Expected: Plan 02's reconcile_with_bcm handles it, so persistence.py needs NO change.)

Commit: `test(11-03): add alt-conf .bcm round-trip tests`.
  </action>
  <verify>python3.6 -m unittest tests.test_persistence -v  (new TestBcmAltconfRoundtrip + existing tests green); python3.6 -m py_compile biochemeleon/persistence.py</verify>
  <done>6 new round-trip tests green proving build→parse→apply preserves is_altconf/endpoint_resvs/alt_tag with list→tuple coercion; version stays 1; persistence.py unchanged (pass-through confirmed).</done>
</task>

<task type="auto">
  <name>Task 2: Backward-compat tests (Phase 8 sidecar without alt-conf fields) + full WSL gate</name>
  <files>tests/test_persistence.py</files>
  <action>
Add a `TestBcmAltconfBackwardCompat` class proving a Phase 8 sidecar (NO alt-conf fields) still round-trips and degrades to defaults:
1. `test_phase8_sidecar_loads_with_defaults`: hand-construct a `.bcm`-shaped dict (magic=BCM_MAGIC, version=1) with a hider dict that has ONLY `object/id/rep/status` (NO is_altconf/endpoint_resvs/alt_tag) — mimicking a Phase 8 sidecar from before Phase 11. `raw = json.dumps(d)`; `d2 = parse_bcm_dict(raw)` (accepts — no version bump); build `mc2` with a sentinel-rebuilt record for that id; `apply_bcm_dict(mc2, d2)`; assert the record has `is_altconf is False`, `endpoint_resvs is None`, `alt_tag == ''` (defaults — backward compatible). This proves a Phase 8 save made BEFORE Phase 11 still loads on Phase 11 code.
2. `test_phase11_sidecar_loads_on_phase8_reconcile`: conversely, a sidecar WITH the alt-conf fields applied to a reconcile that defaults missing fields — already covered by Task 1; here just assert `reconcile_with_bcm` does NOT raise when the fields are present AND when absent (no KeyError on `.get`).
3. `test_no_version_bump`: build_bcm_dict always emits `version == 1` (assert for both alt-conf and non-alt-conf controllers); parse_bcm_dict rejects `version > 1` (existing test — do not duplicate, just confirm still green). The contract: Phase 11 adds OPTIONAL fields, NOT a schema version bump (research §8).

Then run the FULL WSL gate (all 4 test modules + py_compile + grep gates) to confirm no regression across the pure layer.

Commit: `test(11-03): add backward-compat tests for phase 8 sidecars on phase 11`.
  </action>
  <verify>python3.6 -m unittest tests.test_setup_state tests.test_registry tests.test_generators tests.test_persistence -v  (ALL green — no regression); python3.6 -m py_compile biochemeleon/*.py; grep -rnE "import Tkinter|import tkinter|from tkinter|import Pmw|from Pmw|app\.root|grab_set|mainloop|Toplevel|menuBar\.addmenuitem|from PyQt5 import|import PyQt5" biochemeleon/  (0); grep -rnE "\.exec_\(\)" biochemeleon/  (only QFileDialog/QMessageBox)</verify>
  <done>Backward-compat tests green (Phase 8 sidecar → Phase 11 defaults; no version bump); full WSL gate green across all 4 pure test modules; persistence.py still PURE (no pymol import).</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest tests.test_persistence -v` — new alt-conf round-trip + backward-compat tests green; existing Phase 8 persistence tests still green.
- Full WSL gate (all 4 test modules) — no regression.
- py_compile all; Pitfall-1=0; exec_ gate intact.
- persistence.py unchanged (pass-through) OR a documented defensive tweak; stays PURE.
</verification>

<success_criteria>
- The 3 alt-conf fields round-trip through build_bcm_dict → parse_bcm_dict → apply_bcm_dict (with list→tuple coercion for endpoint_resvs).
- `.bcm` version stays 1 (NO schema bump — optional fields only).
- Phase 8 sidecars (without the new fields) load on Phase 11 code with defaults (backward compatible).
- All WSL gates green; persistence.py PURE.
</success_criteria>

<output>
After completion, create `.planning/phases/11-alt-conf-cartoon-hider/11-03-SUMMARY.md`. Note: this plan ran in a parallel worktree (Wave 2, disjoint files from Plan 04) — commit on the isolated branch.
</output>
