---
phase: 11-alt-conf-cartoon-hider
plan: 07
type: execute
wave: 5
depends_on: ["11-01", "11-02", "11-03", "11-04", "11-05", "11-06"]
files_modified:
  - smoke/phase11_smoke.py
autonomous: true

must_haves:
  truths:
    - "phase11_smoke.py is pure pymol.cmd.* (NO Qt) and runs headlessly via cmd.exe /c run-conda-pymol.bat -cq"
    - "Single cartoon alt-conf hider: GAME atoms render in rep cartoon, backbone-only (no sidechain), alt='B' set, anchor is a middle CA, sentinel returns 1 atom"
    - "Ribbon alt-conf hider renders in rep ribbon (NOT hardcoded cartoon)"
    - "Multi-hider (2 disjoint mid-chain segments): registry len 2, all_states on, >=2 states, NO coord corruption of 1st after 2nd insert"
    - "Scoring truth table: middle CA scores, non-anchor middle atom scores (get_altconf_by_resv), endpoint misses, real-trace (alt='') misses"
    - "Cleanup (backup.restore): verify_intact True + count back to orig + no GAME atoms"
    - "New Game (cleanup -> re-start): no residual alt-conf state"
    - "Mixed-rep (sphere+stick+cartoon+ribbon): all 4 reps in registry + visible"
    - ".bcm round-trip: is_altconf/endpoint_resvs/alt_tag survive build->write->read->import_state->reconcile"
    - ".pse alt survival: save .pse -> load -> iterate alt on segi GAME == 'B' (or import_state fallback re-applied)"
  artifacts:
    - path: "smoke/phase11_smoke.py"
      provides: "Phase 11 headless integration smoke (pure pymol.cmd.*, ~12 sections, ~40 checks)"
      contains: "phase11_smoke"
  key_links:
    - from: "smoke/phase11_smoke.py"
      to: "biochemeleon/game.py:GameController.start + on_pick (Plan 05)"
      via: "gc.start(4-tuple hider_specs) -> gc.on_pick(aid, alt, resv) directly (NOT PickWizard)"
      pattern: "gc\\.on_pick"
    - from: "smoke/phase11_smoke.py"
      to: "Windows PyMOL headless runner"
      via: "cmd.exe /c C:\\src\\run-conda-pymol.bat -cq smoke\\phase11_smoke.py"
      pattern: "run-conda-pymol"
---

<objective>
Write and run the Phase 11 headless integration smoke — the cmd-coupled verification that the 4-call alt-conf construction, the scoring gate, cleanup, multi-hider, mixed-rep, and `.bcm`/`.pse` round-trip all work at the runtime tier. This is the NECESSARY (but not sufficient — GUI is Plan 08) verification: headless smoke catches mechanism regressions, but the 05-08 attempt proved headless is STRUCTURALLY BLIND to GUI-only failure modes (auto-zoom, multi-state display, retroactive coord corruption), so the GUI diagnostic (Plan 08) is also MANDATORY.

Purpose: Close every smoke-checkable Open Risk (research §14): backbone-only rendering (Risk 2), `.pse` alt survival (Risk 3), multi-state all_states (Risk 5), `cmd.create` state args / backup sourcing (Risk 9), `backbone` selector atom set (Risk 10), `get_altconf_by_resv` disjointness (Risk 8). Plus all 6 success criteria that are headless-checkable.
Output: `smoke/phase11_smoke.py` (pure pymol.cmd.*, ~12 sections, ~40 checks), staged to `tmp/bioCHEMeleon/`, run headlessly, ALL PASSED + committed.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-alt-conf-cartoon-hider/11-RESEARCH.md

@.planning/phases/11-alt-conf-cartoon-hider/11-01-SUMMARY.md
@.planning/phases/11-alt-conf-cartoon-hider/11-05-SUMMARY.md
@.planning/phases/11-alt-conf-cartoon-hider/11-06-SUMMARY.md

@smoke/phase8_smoke.py
@smoke/phase5_smoke.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write smoke/phase11_smoke.py (pure pymol.cmd.*, ~12 sections, ~40 checks)</name>
  <files>smoke/phase11_smoke.py</files>
  <action>
Create `smoke/phase11_smoke.py` modeled on `smoke/phase8_smoke.py` (header comment + `RESULTS = []` + `def check(name, cond)` + `from pymol import cmd` + `from biochemeleon import game, registry, backup, mutation, persistence, generators` + section comments + final SUMMARY `sys.exit(1)`). Pure `pymol.cmd.*` ONLY — NO Qt (AGENTS.md: headless path cannot run Qt). Use `cmd.fetch("1ubq", async_=0)` as the demo (1ubq: 76 residues, 1 chain — enough for 2 disjoint mid-chain 3-residue segments; it's the Phase 5/3 reference). Use hygienic `space={'stored': ...}` on EVERY `cmd.iterate`/`cmd.alter`; `ID` UPPERCASE in iterate (never lowercase `id`); `b < 0` selector (never `b -999`).

Sections (write ALL — each `check(...)` records pass/fail):
- **A. SETUP**: `cmd.fetch("1ubq", async_=0)`; `mutation.collapse_to_single_state(obj)`; `orig_count = cmd.count_atoms(obj)`; check orig_count > 0.
- **B. ALT-CONF CONSTRUCTION (single cartoon hider)**: build `cas_by_chain` via `cmd.iterate("%s and polymer and name CA" % obj, "stored.append((chain, resv, ID))", space={'stored': cas})` → dict. `segments = generators.pick_segments(cas_by_chain, 1)` → assert len 1. `disp = generators.generate_middle_displacement(1, seed=42, magnitude=1.5)[0]`. `chain, start_resi, end_resi = segments[0]`. `hider_specs = [((chain, start_resi, end_resi, disp), 'cartoon')]`. `gc = game.GameController(obj); gc.start(hider_specs)`. Checks: `gc._backup_name is not None`; `len(gc.registry.all()) == 1`; `rec = gc.registry.all()[0]`; `rec.is_altconf is True`; `rec.endpoint_resvs == (start_resi, end_resi)`; `rec.alt_tag == 'B'`; `cmd.count_atoms("%s and segi GAME and rep cartoon" % obj) > 0` (success crit 1 — renders in cartoon); `cmd.count_atoms("%s and segi GAME and backbone") == cmd.count_atoms("%s and segi GAME" % obj)` (Open Risk 2 + 10 — backbone-only, no sidechain); iterate alt on GAME atoms: `cmd.iterate("%s and segi GAME" % obj, "stored.append(alt)", space={'stored': alts})` → all `'B'` (Open Risk — alt set); `mutation.fetch_all_hider_ids(obj)` returns exactly 1 `(obj, id)` (the anchor middle CA, b<0); the anchor resv == `start_resi + 1` (MIDDLE, not endpoint — iterate resv on the anchor `id <aid> and segi GAME`).
- **C. CONNECTED RENDERING**: check `cmd.count_atoms("%s and segi GAME and rep cartoon" % obj) > 0`. (Headless cannot assert "connected tube visual" — that's Plan 08 GUI; here assert the atoms ARE in the cartoon rep so the tube has geometry to draw.)
- **D. RIBBON**: `cmd.delete(obj); cmd.fetch("1ubq", async_=0); collapse;` rebuild cas_by_chain; `seg = generators.pick_segments(cas_by_chain, 1)[0]`; `disp = generators.generate_middle_displacement(1, seed=42)[0]`; `hider_specs = [((seg[0], seg[1], seg[2], disp), 'ribbon')]`; `gc2 = game.GameController(obj); gc2.start(hider_specs)`; check `cmd.count_atoms("%s and segi GAME and rep ribbon" % obj) > 0` (success crit 2 — ribbon, NOT hardcoded cartoon); check `cmd.count_atoms("%s and segi GAME and rep cartoon" % obj) == 0` (ribbon hiders do NOT show in cartoon). `gc2.cleanup()`.
- **E. MULTI-HIDER (success crit 3)**: `cmd.delete(obj); cmd.fetch("1ubq", async_=0); collapse;` rebuild cas_by_chain; `segments = generators.pick_segments(cas_by_chain, 2)` (1ubq 76 residues → 2 disjoint mid-chain 3-residue segments); assert len 2 + the two `[start,end]` ranges DISJOINT (Open Risk 8); `disps = generators.generate_middle_displacement(2, seed=42)`; `hider_specs = [((s[0], s[1], s[2], d), 'cartoon') for s, d in zip(segments, disps)]`; `gc3 = game.GameController(obj); gc3.start(hider_specs)`; checks: `len(gc3.registry.all()) == 2`; both records is_altconf True; `cmd.get_setting_string("all_states") == "on"` (Open Risk 5); `cmd.count_states(obj) >= 2` (Open Risk 5 — multi-state); NO coord corruption (Open Risk 4 Part B): capture 1st anchor coords BEFORE the loop is done — actually capture 1st anchor coords after start, then... (the 2nd insert already happened inside start, so capture 1st anchor coords via `cmd.iterate_state` and assert they are displaced from the original middle CA by the displacement vector, NOT corrupted to 0/garbage). Concretely: `a1 = gc3.registry.all()[0]; a2 = gc3.registry.all()[1]`; iterate_state(1, `obj and id <a1.id> and segi GAME`) coords; iterate_state(1, `obj and chain <chain1> and resi <a1 middle> and name CA and not segi GAME`) (original) coords; assert the GAME one differs by ~disp1 (displaced), the original is unchanged. (This proves the 1st alt-conf was NOT corrupted by the 2nd insert.)
- **F. SCORING (success crit 4, the truth table)**: use `gc3` (2 hiders). For hider 0: `rec0 = gc3.registry.all()[0]`; `rv1, rv2 = rec0.endpoint_resvs`; `mid_resv = rv1 + 1`. Simulate clicks via `gc3.on_pick(aid, alt, resv)` DIRECTLY (NOT PickWizard — headless can't mouse-pick):
  - `test_score_anchor_middle_ca`: get anchor alt-conf CA: `cmd.iterate("%s and id %d and segi GAME" % (obj, rec0.id), "stored.append((alt, resv))", space={'stored': p})` → `('B', mid_resv)`; `gc3.on_pick(rec0.id, alt='B', resv=mid_resv)` → check `rec0.status == 'found'` + `cmd.color` was called (color the middle range). (Reset rec0 to hidden between sub-tests via `rec0.status = HIDER_STATUS_HIDDEN` for isolation, or use a fresh gc.)
  - `test_score_non_anchor_middle_atom`: pick a non-CA middle atom id: `n_ids = cmd.identify("%s and segi GAME and resi %d and name N" % (obj, mid_resv), mode=0)`; `gc3.on_pick(n_ids[0], alt='B', resv=mid_resv)` → registry.get(n_id) None → get_altconf_by_resv → SCORE; check rec0 found.
  - `test_miss_endpoint`: `ep_ids = cmd.identify("%s and segi GAME and resi %d and name CA" % (obj, rv1), mode=0)`; `gc3.on_pick(ep_ids[0], alt='B', resv=rv1)` → Miss (endpoint not strictly between); check rec0 stays hidden + on_log "Miss!".
  - `test_miss_real_trace`: the original middle CA shares rec0.id; `gc3.on_pick(rec0.id, alt='', resv=mid_resv)` → Miss (alt != 'B'); check stays hidden.
  Use a fresh `gc` per sub-test (or reset) to avoid state bleed. Capture on_log via `gc3.set_callbacks(on_log=...)` to assert "Miss!"/"Found".
- **G. CLEANUP (success crit 5)**: `ok = gc3.cleanup()`; check `ok is True`; `backup.verify_intact` NOT re-called (AGENTS.md — cleanup already restored+discarded); `cmd.count_atoms(obj) == orig_count` (back to orig — success crit 5); `cmd.count_atoms("%s and segi GAME" % obj) == 0` (no GAME atoms). NOTE: cleanup uses `backup.restore` (delete+create two-step) — NO `mutation.cleanup_hiders` call (research §7 — backup.restore is canonical for alt-conf).
- **H. NEW GAME (success crit 6)**: after G cleanup, start a fresh `gc4 = game.GameController(obj); gc4.start([((seg, disp), 'cartoon')])`; check `cmd.count_atoms("%s and segi GAME" % obj) > 0` (new hider inserted, no residual corruption); `gc4.cleanup()`; check count back to orig. (Proves cleanup -> re-start works without residual alt-conf state.)
- **I. MIXED-REP (success crit 4)**: `cmd.delete(obj); cmd.fetch("1ubq", async_=0); collapse;` build a mixed hider_specs: 1 sphere (`generators.generate_sphere_positions(extent, 1, seed=42)[0]`), 1 stick (`generators.generate_line_stick_offsets(1, seed=42)[0]` + a neighbor CA id), 1 cartoon (pick_segments 1 + disp), 1 ribbon (pick_segments another + disp). `gc5 = game.GameController(obj); gc5.start(hider_specs)`; checks: `len(gc5.registry.all()) == 4`; `counts_by_rep` has spheres>=1, sticks>=1, cartoon>=1, ribbon>=1; `cmd.count_atoms("%s and segi GAME and rep spheres" % obj) > 0` etc. for each rep. `gc5.cleanup()`.
- **J. .BCM ROUND-TRIP (Open Risk 3 + 5)**: start a gc with 2 cartoon alt-conf hiders (like E); `state = _sample_setup()` (build a minimal setup dict — mirror phase8_smoke); `bcm = persistence.build_bcm_dict(gc, state, kind='checkpoint')`; check `bcm['registry']['hiders'][0]['is_altconf'] is True` + `endpoint_resvs` present + `version == 1`; `raw = json.dumps(bcm)`; `bcm2 = persistence.parse_bcm_dict(raw)`; write `.bcmz` via `persistence.write_bcmz(tmp_path, bcm, pse_path)` after `cmd.save(pse_path, obj)`; read via `persistence.read_bcmz(tmp_path)`; clear session; `cmd.load(pse_path, partial=1)`; `resolve_target`; `gc_imp = game.GameController(obj); gc_imp.import_state(bcm2)`; check the imported registry records have `is_altconf=True`, `endpoint_resvs == (rv1, rv2)` (tuple), `alt_tag == 'B'` (Open Risk 3 + 5 — round-trip preserved).
- **K. .PSE ALT SURVIVAL (Open Risk 3 — load-bearing MEDIUM)**: after J, `cmd.iterate("%s and segi GAME" % obj, "stored.append(alt)", space={'stored': alt_check})`; check all `alt_check` entries == `'B'`. If ANY is `''` (alt did NOT survive .pse), the import_state fallback (Plan 05) should have re-applied it — so this check asserts alt == 'B' AFTER import_state (fallback covers the gap). Document in a comment: "if this FAILS, alt did not survive .pse AND the import_state fallback did not re-apply — investigate Open Risk 3."
- **L. SUMMARY**: `n_pass = sum(1 for _, ok in RESULTS if ok)`; `n_fail = len(RESULTS) - n_pass`; print "Phase 11 smoke: %d/%d PASSED" % (n_pass, len(RESULTS)); `if n_fail: sys.exit(1)`.

Write the file with clear section comments. NO Qt imports. Keep it under ~280 lines (mirror phase8_smoke.py's density).

Commit: `test(11-07): add phase11 headless smoke (construction+scoring+cleanup+multi-hider+mixed-rep+.bcm+.pse)`.
  </action>
  <verify>python3.6 -m py_compile smoke/phase11_smoke.py; grep -nE "from pymol.Qt|import PyQt5|QtWidgets" smoke/phase11_smoke.py  (0 — pure pymol.cmd.*); grep -nE "space=\{\}" smoke/phase11_smoke.py  (>= 5 — every iterate); grep -nE "space=None" smoke/phase11_smoke.py  (0); grep -nE "\bID\b" smoke/phase11_smoke.py  (>= 1 — uppercase ID in iterate); grep -nE "b -999" smoke/phase11_smoke.py  (0 — selector is b < 0)</verify>
  <done>smoke/phase11_smoke.py written: 12 sections (A-L), pure pymol.cmd.*, hygienic space={} + ID uppercase + b<0; py_compile clean; no Qt.</done>
</task>

<task type="auto">
  <name>Task 2: Stage to Windows path + run headlessly + assert ALL PASSED</name>
  <files>smoke/phase11_smoke.py</files>
  <action>
Stage the package + smoke to the Windows-facing path and run headlessly (AGENTS.md headless pattern). Run these commands from the repo root:
```bash
bash wsl2win_cp.sh
mkdir -p tmp/bioCHEMeleon/smoke
cp smoke/phase11_smoke.py tmp/bioCHEMeleon/smoke/
cd tmp/bioCHEMeleon && timeout 180 cmd.exe /c "C:\\src\\run-conda-pymol.bat -cq smoke\\phase11_smoke.py" 2>&1 | tail -60
```
Then check the exit code (`echo $?` immediately after — 0 = clean, nonzero = crash) and grep the output for "Phase 11 smoke:" + "PASSED" + any "FAIL:". 

If ANY check FAILS or the run crashes: do NOT mark done. Diagnose via the FAIL output (mirror the Phase 3 03-15 / Phase 5 05-04 smoke-debug pattern — the failure is almost always a Rule-1 runtime bug in the new code, e.g. a selector typo, a state-arg surprise, or an iterate symbol). Fix the bug in the SOURCE file (`biochemeleon/*.py` or `smoke/phase11_smoke.py`), re-stage (`bash wsl2win_cp.sh` re-copies), and re-run. Iterate until ALL PASSED + exit 0. Record each fix as a `fix(11-07):` commit (or `fix(11-0X):` if the bug is in an earlier plan's source — note which plan in the commit message). Common anticipated fixes (from research Open Risks): backbone selector atom set (Risk 10), `cmd.create` state args (Risk 9), `target_state` semantics (may need -1 vs 0 swap), alt not surviving .pse (Risk 3 — import_state fallback should cover it; if not, strengthen the fallback).

If the run is clean (ALL PASSED, exit 0): commit the final smoke state (if changed during debug) and record the PASS count in the SUMMARY.

CRITICAL: headless ALL PASSED is NECESSARY but NOT SUFFICIENT (research §10 + the 05-08 lesson: 4 fix cycles each passed headless but failed in GUI). The GUI diagnostic + human-verify is Plan 08 — do NOT claim phase success here; the smoke proves the MECHANISM, the GUI proves the INTEGRATION.
  </action>
  <verify>cd tmp/bioCHEMeleon && timeout 180 cmd.exe /c "C:\\src\\run-conda-pymol.bat -cq smoke\\phase11_smoke.py" 2>&1 | tail -60  (output ends with "Phase 11 smoke: N/N PASSED", no FAIL: lines, exit 0); grep -c "FAIL:" <output>  (0)</verify>
  <done>phase11_smoke.py runs headlessly via cmd.exe /c run-conda-pymol.bat -cq; ALL checks PASSED (N/N), exit 0, no FAIL: lines; any runtime bugs discovered during the run are fixed in source + re-run until clean; commits record fixes.</done>
</task>

</tasks>

<verification>
- `python3.6 -m py_compile smoke/phase11_smoke.py` — compiles.
- `grep` gates on the smoke: no Qt, space={} on every iterate, space=None=0, ID uppercase, b<0 (not b -999).
- Headless run: `cd tmp/bioCHEMeleon && timeout 180 cmd.exe /c "C:\\src\\run-conda-pymol.bat -cq smoke\\phase11_smoke.py" 2>&1 | tail -60` → "Phase 11 smoke: N/N PASSED", exit 0.
- All 6 success criteria that are headless-checkable are covered (1-render-in-cartoon, 2-ribbon, 3-multi-hider, 4-mixed-rep, 5-cleanup, 6-new-game) + Open Risks 2/3/5/8/9/10.
- GUI-only failure modes (auto-zoom, connected visual, multi-state display, retroactive coord corruption) are NOT covered headlessly — deferred to Plan 08.
</verification>

<success_criteria>
- phase11_smoke.py is pure pymol.cmd.*, runs headlessly, ALL PASSED, exit 0.
- The 4-call alt-conf construction works at runtime (cartoon + ribbon render, backbone-only, alt='B', anchor middle CA).
- Multi-hider works (2 disjoint segments, all_states on, >=2 states, no coord corruption).
- Scoring truth table passes at runtime (middle CA scores, non-anchor scores, endpoint/real-trace miss).
- Cleanup + new-game work (backup.restore, count back to orig).
- Mixed-rep works (4 reps in one game).
- .bcm + .pse round-trip preserves alt-conf metadata.
- GUI-only modes deferred to Plan 08.
</success_criteria>

<output>
After completion, create `.planning/phases/11-alt-conf-cartoon-hider/11-07-SUMMARY.md` (include the final PASS count + any fix commits). Single-plan wave.
</output>
