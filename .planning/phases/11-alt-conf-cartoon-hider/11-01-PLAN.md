---
phase: 11-alt-conf-cartoon-hider
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - biochemeleon/generators.py
  - tests/test_generators.py
autonomous: true

must_haves:
  truths:
    - "pick_segments returns disjoint mid-chain segments (no overlapping resi ranges) for a multi-chain CA map"
    - "pick_segments returns [] for an empty CA map and skips chains with fewer than segment_size residues"
    - "generate_middle_displacement returns one [dx,dy,dz] vector per requested hider, each a unit vector scaled by magnitude"
    - "generate_middle_displacement with the same seed is deterministic (reproducible displacements)"
  artifacts:
    - path: "biochemeleon/generators.py"
      provides: "pick_segments + generate_middle_displacement (PURE, no pymol import)"
      contains: "def pick_segments"
    - path: "tests/test_generators.py"
      provides: "WSL unit tests for both new functions"
      contains: "class TestPickSegments"
  key_links:
    - from: "biochemeleon/generators.py:pick_segments"
      to: "biochemeleon/mutation.py:insert_altconf_cartoon_hider (Plan 04)"
      via: "(chain, start_resi, end_resi) tuple consumed by the dispatcher"
      pattern: "return.*chain.*start_resi.*end_resi"
    - from: "biochemeleon/generators.py:generate_middle_displacement"
      to: "biochemeleon/mutation.py:insert_altconf_cartoon_hider (Plan 04) alter_state"
      via: "[dx,dy,dz] rigid-translation vector applied to middle backbone atoms"
      pattern: "def generate_middle_displacement"
---

<objective>
Add the two PURE generator functions Phase 11's alt-conf cartoon/ribbon hider needs: `pick_segments` (disjoint mid-chain ≥3-residue segment picker) and `generate_middle_displacement` (rigid unit-vector displacement per hider).

Purpose: These are the pure, WSL-testable geometry/selection decisions that feed the cmd-coupled `insert_altconf_cartoon_hider` (Plan 04). Per USER REQUIREMENT 1 ("only generate the BACKBONE") and 2 ("≥3 residues: keep the two ends' positions, slightly move the middle"), `pick_segments` selects mid-chain backbone segments and `generate_middle_displacement` produces the middle-residue offset vector. Keeping them pure (stdlib `random` only, NO `from pymol`) preserves the WSL-unit-testable convention of generators.py/registry.py/setup_state.py.
Output: Extended `biochemeleon/generators.py` + extended `tests/test_generators.py` (WSL green).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-alt-conf-cartoon-hider/11-RESEARCH.md

@biochemeleon/generators.py
@tests/test_generators.py
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED+GREEN): TDD pick_segments — disjoint mid-chain segment picker</name>
  <files>biochemeleon/generators.py, tests/test_generators.py</files>
  <action>
RED first: add a `TestPickSegments` class to `tests/test_generators.py` (follow the existing sys.modules pymol/pymol.Qt MagicMock stub pattern at the top of the file — DO NOT remove it). Import `pick_segments` from `biochemeleon.generators` in the existing import block. Write failing tests for these behaviors (run `python3.6 -m unittest tests.test_generators -v` — they MUST fail with ImportError/AttributeError first):

1. `test_empty_returns_empty`: `pick_segments({})` → `[]`.
2. `test_single_chain_three_residues`: `cas_by_chain = {'A': [(1, 101), (2, 102), (3, 103)]}` (list of `(resi, ca_id)` tuples, the SAME shape `_prepare_and_start` builds), `count=1`, `segment_size=3` → returns `[( 'A', 1, 3 )]` (the whole chain is one mid-chain segment when it is exactly segment_size; start_resi=min resi, end_resi=max resi).
3. `test_single_chain_five_residues_picks_mid_segment`: `cas_by_chain = {'A': [(1, 101),(2, 102),(3, 103),(4, 104),(5, 105)]}`, `count=1`, `segment_size=3` → returns `[('A', 2, 4)]` (the MIDDLE 3-residue window: start = resi 2, end = resi 4; NOT a terminal). Assert start_resi == 2 and end_resi == 4 (the mid-chain window, not the N or C terminus — this is the whole point of Phase 11 replacing terminal-extension).
4. `test_disjoint_segments_multi_count`: `cas_by_chain = {'A': [(1,101),(2,102),(3,103),(4,104),(5,105),(6,106),(7,107)]}`, `count=2`, `segment_size=3` → returns 2 tuples whose `[start_resi, end_resi]` ranges DO NOT overlap (Bug 1 fix — assert `end of first < start of second` or `start of first > end of second`; no shared resi). Both must be mid-chain (not the N-term resi 1 nor the C-term resi 7 as a segment endpoint that touches the terminus).
5. `test_skips_short_chains`: `cas_by_chain = {'A': [(1,101),(2,102)], 'B': [(1,201),(2,202),(3,203),(4,204)]}`, `count=2`, `segment_size=3` → chain A (only 2 residues < segment_size 3) is skipped; only chain B yields a segment. Assert len == 1 and the result is from chain B.
6. `test_longest_chain_first`: with 2 chains of unequal length, `count=2`, the longer chain's segment comes first (mirrors `pick_terminal_residues`'s longest-first ordering for determinism).
7. `test_count_cap`: `count` larger than available disjoint segments → returns only as many as fit (no overlap forced). Assert len <= count and all disjoint.

Signature (EXACT — Plan 04 + _prepare_and_start depend on this contract):
```python
def pick_segments(cas_by_chain, count, segment_size=3):
    """Mid-chain segment picker. Returns [(chain, start_resi, end_resi), ...].
    DISJOINT segments (Bug 1 fix — ranges non-overlapping). Skips chains with
    fewer than segment_size residues. Longer chains first. Mid-chain (NOT
    terminal): the segment window is centered on the chain interior so
    endpoints are not the N/C terminus when the chain is longer than segment_size.
    """
```
`cas_by_chain` is `{chain: [(resi, ca_id), ...]}` (the EXISTING shape `_prepare_and_start` builds at line 183-185 of __init__.py — DO NOT change that shape). Returns a list of `(chain, start_resi, end_resi)` 3-tuples where start_resi/end_resi are ints (resi values, NOT ca_ids — ca_ids are not needed for alt-conf construction which copies by `chain and resi N-M`).

Implementation guidance for GREEN (after RED fails): iterate chains sorted longest-first; for each chain with `len(residues) >= segment_size`, pick mid-chain windows. To guarantee disjointness across multiple picks on the SAME chain, advance the window start past the previous segment's end (or pick non-overlapping windows spaced across the chain). Keep it simple: for one segment per chain, pick the centered window `start = resi[mid_index]` where `mid_index = (len - segment_size) // 2`. For `count` segments, prefer one-per-chain first (longest chains), then if more are requested than chains, pick additional disjoint windows on the longest chain (advancing past prior windows). Hard contract: ranges MUST be non-overlapping (assert this in a test). Use `random` ONLY if you need to jitter window placement; deterministic centered windows are fine and more testable — PREFER deterministic (no RNG) so tests are stable.

Commit RED: `test(11-01): add failing tests for pick_segments`. Then implement to GREEN: `feat(11-01): implement pick_segments`. Run `python3.6 -m unittest tests.test_generators -v` — ALL (new + existing 21) MUST pass.
  </action>
  <verify>python3.6 -m unittest tests.test_generators -v  (ALL pass: new TestPickSegments + existing sphere/line-stick/terminal tests); python3.6 -m py_compile biochemeleon/generators.py</verify>
  <done>pick_segments returns disjoint mid-chain (chain, start_resi, end_resi) tuples, skips short chains, longest-first, count-capped; 7+ new tests green; existing 21 generator tests still green.</done>
</task>

<task type="auto">
  <name>Task 2 (RED+GREEN): TDD generate_middle_displacement — rigid unit vectors</name>
  <files>biochemeleon/generators.py, tests/test_generators.py</files>
  <action>
RED first: add a `TestGenerateMiddleDisplacement` class to `tests/test_generators.py`. Import `generate_middle_displacement`. Write failing tests:

1. `test_count`: `generate_middle_displacement(5, seed=42)` → len == 5, each element is a 3-element list `[dx, dy, dz]`.
2. `test_magnitude`: `generate_middle_displacement(1, seed=42, magnitude=1.5)` → the single vector has Euclidean norm == 1.5 (assert `abs(sqrt(dx*dx+dy*dy+dz*dz) - 1.5) < 1e-9`). This is the RIGID unit-vector × magnitude contract (research §4: 1.5 Å default, 2.0 Å ceiling).
3. `test_magnitude_default`: default `magnitude=1.5` → norm == 1.5.
4. `test_deterministic_seed`: `generate_middle_displacement(3, seed=7) == generate_middle_displacement(3, seed=7)` (same seed → identical vectors).
5. `test_different_seed_differs`: `generate_middle_displacement(3, seed=1) != generate_middle_displacement(3, seed=2)` (different seeds → different vectors, almost surely).
6. `test_zero_count`: `generate_middle_displacement(0, seed=42)` → `[]`.
7. `test_rigid_per_hider`: each returned vector is ONE unit vector (the SAME offset is applied to ALL middle atoms of that one hider — rigid translation, Pitfall 15). The function returns one vector PER HIDER (not per atom); the caller applies the same vector to all middle atoms. Assert each vector is a single `[dx,dy,dz]` (the contract is one vector per hider).

Signature (EXACT — Plan 04 / _prepare_and_start depend on this):
```python
def generate_middle_displacement(n, seed=None, magnitude=1.5):
    """Pure RNG. Returns [[dx, dy, dz], ...] — one random unit vector per hider
    times magnitude. Same offset for all middle atoms of one hider (rigid
    translation — Pitfall 15: displace ALL middle atoms, NOT just CA)."""
```
Implementation for GREEN: use `random.Random(seed)`; for each of `n`, generate a random unit vector (e.g. sample 3 standard-normal components via `rng.gauss(0, 1)`, normalize to unit length, then scale by `magnitude`). Normalizing avoids directional bias (better than uniform-in-cube). Return a list of `[dx, dy, dz]` lists. This is the SAME `random.Random(seed)` pattern as `generate_sphere_positions` / `generate_line_stick_offsets` — mirror their style. NO `from pymol`, NO numpy (stdlib only).

Commit RED: `test(11-01): add failing tests for generate_middle_displacement`. Then GREEN: `feat(11-01): implement generate_middle_displacement`. Run the full WSL gate at the end of this task.
  </action>
  <verify>python3.6 -m unittest tests.test_generators tests.test_setup_state tests.test_registry tests.test_persistence -v  (no regression); python3.6 -m py_compile biochemeleon/*.py; grep -rnE "import Tkinter|import tkinter|from tkinter|import Pmw|from Pmw|app\.root|grab_set|mainloop|Toplevel|menuBar\.addmenuitem|from PyQt5 import|import PyQt5" biochemeleon/  (MUST be 0); grep -rnE "\.exec_\(\)" biochemeleon/  (hits only on QFileDialog/QMessageBox, never main dialog)</verify>
  <done>generate_middle_displacement returns n unit vectors × magnitude, deterministic by seed, rigid-per-hider; ALL WSL gates green (py_compile + full test suite + Pitfall-1=0 + exec_ gate); generators.py stays PURE (no `from pymol`).</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest tests.test_generators -v` — new + existing tests green.
- Full WSL gate: `python3.6 -m unittest tests.test_setup_state tests.test_registry tests.test_generators tests.test_persistence -v` (no regression in sibling pure layers).
- `python3.6 -m py_compile biochemeleon/*.py`.
- Pitfall-1 grep: 0 matches. exec_ grep: only QFileDialog/QMessageBox.
- Purity: `grep -nE "from pymol|import pymol" biochemeleon/generators.py` → 0 matches (generators.py stays pure).
</verification>

<success_criteria>
- `pick_segments` and `generate_middle_displacement` exist, pure (stdlib `random` only), WSL-tested.
- `pick_segments` returns disjoint mid-chain `(chain, start_resi, end_resi)` tuples — the contract `insert_altconf_cartoon_hider` (Plan 04) and `_prepare_and_start` (Plan 06) consume.
- `generate_middle_displacement` returns rigid unit-vector × magnitude displacements — the contract `insert_altconf_cartoon_hider` applies to middle backbone atoms via `alter_state`.
- All WSL gates green; no regression in existing pure-layer tests.
</success_criteria>

<output>
After completion, create `.planning/phases/11-alt-conf-cartoon-hider/11-01-SUMMARY.md` using the summary template. Note: this plan ran in a parallel worktree (Wave 1, disjoint files from Plan 02) — commit on the isolated branch.
</output>
