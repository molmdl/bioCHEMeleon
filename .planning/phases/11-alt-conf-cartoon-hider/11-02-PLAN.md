---
phase: 11-alt-conf-cartoon-hider
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - biochemeleon/registry.py
  - tests/test_registry.py
autonomous: true

must_haves:
  truths:
    - "HiderRecord carries is_altconf (bool), endpoint_resvs ((rv1,rv2) or None), and alt_tag (str) with backward-compatible defaults"
    - "register() accepts the 3 new optional fields and existing callers passing only (object,id,rep,...) still work"
    - "get_altconf_by_resv(object, resv) returns the first alt-conf record with rv1 < resv < rv2, or None"
    - "to_dict emits the 3 new fields only when non-default; from_dict reads them with defaults"
    - "reconstruct_from_sentinels and reconcile_with_bcm default the 3 new fields (alt-conf lost on .pse reload, restored from .bcm)"
  artifacts:
    - path: "biochemeleon/registry.py"
      provides: "HiderRecord + HiderRegistry extended for alt-conf (3 fields + get_altconf_by_resv + serialization)"
      contains: "def get_altconf_by_resv"
    - path: "tests/test_registry.py"
      provides: "WSL unit tests for the 3 new fields, get_altconf_by_resv, and serialization round-trip"
      contains: "class TestAltconfFields"
  key_links:
    - from: "biochemeleon/registry.py:register (is_altconf, endpoint_resvs, alt_tag)"
      to: "biochemeleon/game.py:on_pick alt/resv gate (Plan 05)"
      via: "on_pick reads rec.is_altconf + rec.endpoint_resvs + rec.alt_tag to gate scoring"
      pattern: "is_altconf"
    - from: "biochemeleon/registry.py:to_dict/reconcile_with_bcm"
      to: "biochemeleon/persistence.py:build_bcm_dict/apply_bcm_dict (Plan 03)"
      via: ".bcm sidecar carries the 3 optional fields; reconcile restores them"
      pattern: "endpoint_resvs"
---

<objective>
Extend the PURE HiderRegistry data model with the 3 alt-conf fields (`is_altconf`, `endpoint_resvs`, `alt_tag`) and a new `get_altconf_by_resv` lookup, plus serialization/reconstruction/reconciliation that carry the fields through the `.bcm` sidecar. This is the registry foundation Phase 11's scoring (Plan 05) and persistence (Plan 03) build on.

Purpose: Alt-conf atoms SHARE ids with originals (research Pitfall 10), so the registry keyed by `(object, id)` cannot distinguish a real-trace click from an alt-conf hider click by id alone. The scoring solution (Plan 05) reads `alt` + `resv` at pick time and gates on `alt == rec.alt_tag AND rv1 < resv < rv2`. That requires the registry to STORE `is_altconf`, `endpoint_resvs`, and `alt_tag` per record. `get_altconf_by_resv` handles non-anchor middle-atom clicks (USER REQUIREMENT 3: click ANY middle atom). The `.bcm` round-trip (NO version bump) preserves these across save/load.
Output: Extended `biochemeleon/registry.py` + extended `tests/test_registry.py` (WSL green). registry.py stays PURE (no `from pymol`).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-alt-conf-cartoon-hider/11-RESEARCH.md

@biochemeleon/registry.py
@tests/test_registry.py
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED+GREEN): TDD 3 new HiderRecord fields + register + get_altconf_by_resv</name>
  <files>biochemeleon/registry.py, tests/test_registry.py</files>
  <action>
RED first: add tests to `tests/test_registry.py` (keep the existing sys.modules pymol/pymol.Qt MagicMock stub at the top — DO NOT remove it). Write failing tests in a new `TestAltconfFields` class + extend `TestHiderRegistryCore`/queries as needed:

1. `test_record_defaults`: `HiderRecord(100, '1ubq', 'cartoon')` → `is_altconf == False`, `endpoint_resvs is None`, `alt_tag == ''` (backward-compatible defaults — existing records behave identically).
2. `test_record_altconf_fields`: `HiderRecord(100, '1ubq', 'cartoon', is_altconf=True, endpoint_resvs=(2,4), alt_tag='B')` → fields set as passed.
3. `test_register_altconf_fields`: `reg.register('1ubq', 100, 'cartoon', is_altconf=True, endpoint_resvs=(2,4), alt_tag='B')` → returned record has the 3 fields; `reg.get('1ubq', 100).is_altconf is True`, `.endpoint_resvs == (2,4)`, `.alt_tag == 'B'`.
4. `test_register_backward_compat`: `reg.register('1ubq', 101, 'spheres')` (NO new fields) → `rec.is_altconf is False`, `rec.endpoint_resvs is None`, `rec.alt_tag == ''` (existing Phase 3/4/5 callers unaffected).
5. `test_get_altconf_by_resv_hit`: register an alt-conf record `(100, '1ubq', 'cartoon', endpoint_resvs=(2,4))`; `reg.get_altconf_by_resv('1ubq', 3)` → returns that record (3 is strictly between 2 and 4).
6. `test_get_altconf_by_resv_endpoint_miss`: `reg.get_altconf_by_resv('1ubq', 2)` → None (endpoint, NOT strictly between); same for resv=4.
7. `test_get_altconf_by_resv_non_altconf_skipped`: register a non-altconf record `(102, '1ubq', 'spheres')` (is_altconf=False, endpoint_resvs=None); `reg.get_altconf_by_resv('1ubq', 5)` → None (non-altconf records are skipped by the resv lookup).
8. `test_get_altconf_by_resv_first_match`: register two alt-conf records with overlapping-but-disjoint ranges (e.g. (2,4) and (6,8)); `get_altconf_by_resv('1ubq', 3)` → the first registered matching record (O(N) first-match; research §6).
9. `test_get_altconf_by_resv_wrong_object`: `reg.get_altconf_by_resv('other', 3)` → None (object filter).

Implementation for GREEN:
- `HiderRecord.__slots__`: add `'is_altconf'`, `'endpoint_resvs'`, `'alt_tag'` AFTER the existing 5 slots. Keep `__slots__` ordering stable (existing 5 first, then the 3 new).
- `HiderRecord.__init__`: add params `is_altconf=False, endpoint_resvs=None, alt_tag=''` with those defaults; assign them. Do NOT validate `alt_tag` (any string; default `''`). `endpoint_resvs` is a 2-tuple of ints or None (store as-is; do NOT coerce — the caller passes ints from `pick_segments`). Keep the existing `rep` validation unchanged.
- `HiderRegistry.register`: add the same 3 kwargs with defaults; pass them through to `HiderRecord(...)`. The duplicate-key `KeyError` guard is UNCHANGED.
- `HiderRegistry.get`: UNCHANGED (keyed by `(object, id)` — the anchor id).
- NEW `HiderRegistry.get_altconf_by_resv(self, object, resv)`: iterate `self._records.values()` in insertion order; return the FIRST record where `r.object == object and r.is_altconf and r.endpoint_resvs is not None and r.endpoint_resvs[0] < resv < r.endpoint_resvs[1]`; else None. Pure O(N). NO `from pymol`.

Commit RED: `test(11-02): add failing tests for alt-conf registry fields`. Then GREEN: `feat(11-02): add alt-conf fields + get_altconf_by_resv`. Run `python3.6 -m unittest tests.test_registry -v` — ALL (new + existing 54) MUST pass.
  </action>
  <verify>python3.6 -m unittest tests.test_registry -v  (ALL pass: new + existing 54); python3.6 -m py_compile biochemeleon/registry.py</verify>
  <done>HiderRecord has 3 new slots with defaults; register accepts them; get_altconf_by_resv does strict-between lookup skipping non-altconf; existing 54 registry tests still green.</done>
</task>

<task type="auto">
  <name>Task 2 (RED+GREEN): TDD serialization extension — to_dict/from_dict/reconstruct/reconcile carry the 3 fields</name>
  <files>biochemeleon/registry.py, tests/test_registry.py</files>
  <action>
RED first: add tests in a new `TestAltconfSerialization` class:

1. `test_to_dict_omits_defaults`: `HiderRecord(100,'1ubq','spheres')` (defaults) → `to_dict()` does NOT contain keys `is_altconf`, `endpoint_resvs`, `alt_tag` (compact sidecar; backward-compatible with Phase 8 sidecars).
2. `test_to_dict_includes_altconf`: `HiderRecord(100,'1ubq','cartoon', is_altconf=True, endpoint_resvs=(2,4), alt_tag='B')` → `to_dict()` contains `'is_altconf': True`, `'endpoint_resvs': [2,4]` (serialized as a LIST for JSON), `'alt_tag': 'B'`.
3. `test_from_dict_altconf_roundtrip`: build a registry, `to_dict()`, then `HiderRegistry.from_dict(d)` → the reconstructed record has the 3 fields. Round-trip preserves is_altconf/endpoint_resvs/alt_tag.
4. `test_from_dict_defaults_when_absent`: a hider dict WITHOUT the 3 new keys (a Phase 8 sidecar) → `from_dict` reconstructs with defaults (is_altconf=False, endpoint_resvs=None, alt_tag=''). BACKWARD COMPATIBLE — Phase 8 sidecars load unchanged.
5. `test_reconstruct_from_sentinels_defaults`: `reconstruct_from_sentinels(iter)` → rebuilt records have `is_altconf=False, endpoint_resvs=None, alt_tag=''` (sentinel carries no alt-conf info; Plan 05/06 reconcile from .bcm restores them).
6. `test_reconcile_with_bcm_restores_altconf`: sentinel-rebuild a record (rep=None, defaults), then `reconcile_with_bcm([{object,id,rep,'is_altconf':True,'endpoint_resvs':[2,4],'alt_tag':'B',...}])` → the record's `is_altconf/endpoint_resvs/alt_tag` are restored alongside rep/status. Assert `rec.is_altconf is True`, `rec.endpoint_resvs == (2,4)` (tuple, NOT list — coerce list→tuple on reconcile), `rec.alt_tag == 'B'`.
7. `test_reconcile_defaults_when_bcm_lacks_fields`: reconcile a sentinel record with a .bcm hider dict that LACKS the 3 fields → record keeps defaults (is_altconf=False, endpoint_resvs=None, alt_tag=''). BACKWARD COMPATIBLE.
8. `test_reconcile_endpoint_resvs_list_to_tuple`: the .bcm JSON stores endpoint_resvs as `[2,4]` (list); reconcile stores it as a TUPLE `(2,4)` on the record (so `rv1 < resv < rv2` comparison works — lists would fail `<` in py3). Assert `isinstance(rec.endpoint_resvs, tuple)`.

Implementation for GREEN:
- `HiderRecord.to_dict`: after the existing fields, add `if self.is_altconf: d['is_altconf'] = True`; `if self.endpoint_resvs is not None: d['endpoint_resvs'] = list(self.endpoint_resvs)`; `if self.alt_tag: d['alt_tag'] = self.alt_tag`. (Omit when default — compact + backward-compatible.)
- `HiderRegistry.from_dict`: in the register call, read `h.get('is_altconf', False)`, `h.get('endpoint_resvs')` (coerce list→tuple if it's a list: `tuple(h['endpoint_resvs'])` when present), `h.get('alt_tag', '')`. Pass to `register`.
- `reconstruct_from_sentinels`: the `HiderRecord(aid, obj, rep=None, status=HIDER_STATUS_HIDDEN)` call already uses the new defaults (is_altconf=False, endpoint_resvs=None, alt_tag='') — NO change needed, but verify the defaults hold.
- `reconcile_with_bcm`: in the per-record reconcile loop, after setting `rec.rep`/`rec.status`/`rec.pos`, ALSO set: `rec.is_altconf = bool(h.get('is_altconf', False))`; if `'endpoint_resvs' in h and h['endpoint_resvs'] is not None: rec.endpoint_resvs = tuple(h['endpoint_resvs'])` (coerce list→tuple); `rec.alt_tag = h.get('alt_tag', '')`. Keep the existing mismatch handling (missing_from_bcm/missing_from_pse/bad_rep) UNCHANGED.

Commit RED: `test(11-02): add failing tests for alt-conf serialization`. Then GREEN: `feat(11-02): carry alt-conf fields through to_dict/from_dict/reconcile`. Then REFACTOR if obvious (e.g. extract a `_set_altconf_fields(rec, h)` helper used by both from_dict and reconcile) — only if tests stay green. Run the FULL WSL gate.
  </action>
  <verify>python3.6 -m unittest tests.test_setup_state tests.test_registry tests.test_generators tests.test_persistence -v  (no regression — ALL green); python3.6 -m py_compile biochemeleon/*.py; grep -rnE "import Tkinter|import tkinter|from tkinter|import Pmw|from Pmw|app\.root|grab_set|mainloop|Toplevel|menuBar\.addmenuitem|from PyQt5 import|import PyQt5" biochemeleon/  (0); grep -rnE "\.exec_\(\)" biochemeleon/  (only QFileDialog/QMessageBox); grep -nE "from pymol|import pymol" biochemeleon/registry.py  (0 — registry stays PURE)</verify>
  <done>to_dict omits defaults + includes alt-conf when set; from_dict/reconcile restore the 3 fields (list→tuple coercion for endpoint_resvs); backward-compatible with Phase 8 sidecars (absent fields → defaults); reconstruct_from_sentinels defaults them; ALL WSL gates green; registry.py PURE.</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest tests.test_registry -v` — new + existing 54 green.
- Full WSL gate (all 4 test modules) — no regression.
- py_compile all; Pitfall-1=0; exec_ gate intact; registry.py purity (no `from pymol`).
</verification>

<success_criteria>
- HiderRecord/HiderRegistry carry `is_altconf`, `endpoint_resvs`, `alt_tag` with backward-compatible defaults.
- `get_altconf_by_resv(object, resv)` does strict-between lookup, skipping non-altconf records.
- `.bcm` round-trip (to_dict → from_dict, reconcile_with_bcm) preserves the 3 fields with list→tuple coercion; Phase 8 sidecars degrade gracefully (absent → defaults).
- registry.py stays PURE (stdlib + GAME_REPS only); WSL green.
</success_criteria>

<output>
After completion, create `.planning/phases/11-alt-conf-cartoon-hider/11-02-SUMMARY.md`. Note: this plan ran in a parallel worktree (Wave 1, disjoint files from Plan 01) — commit on the isolated branch.
</output>
