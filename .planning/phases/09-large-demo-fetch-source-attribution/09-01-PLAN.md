---
phase: 09-large-demo-fetch-source-attribution
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - biochemeleon/setup_state.py
  - tests/test_setup_state.py
autonomous: true

must_haves:
  truths:
    - "DEMO_MANIFEST has 9 entries; each carries source/source_id/fetch_url/cache_name/citation/strip"
    - "1gzm + 3gp6 are 'very_challenging' MemProtMD demos; sasdpg4 is 'challenge' SASBDB demo"
    - "4wb3 difficulty is 'hard' (not 'mixed')"
    - "TIER_LABELS maps the 4 identifier-safe tier values to the SC4 display labels (Easy/Hard/Challenge/Very challenging)"
    - "randomize_state never picks a fetched demo (source != 'bundled') for demo_id — offline-safe"
    - "strip_resn_from_pdb removes ATOM lines whose resn is in a strip set, preserving non-ATOM lines + padding-agnostic via .strip()"
  artifacts:
    - path: "biochemeleon/setup_state.py"
      provides: "Extended DEMO_MANIFEST (9 entries, 6 new fields) + TIER_LABELS + STRIP_RESN_MEMPROTMD + strip_resn_from_pdb + randomize fetched-exclusion"
      contains: "TIER_LABELS"
    - path: "tests/test_setup_state.py"
      provides: "Unit tests for manifest schema, tier labels, 4wb3->hard, randomize exclusion, strip_resn_from_pdb"
      contains: "TestTierLabels"
  key_links:
    - from: "biochemeleon/demos.py (09-02)"
      to: "biochemeleon/setup_state.DEMO_MANIFEST"
      via: "imports DEMO_MANIFEST + strip_resn_from_pdb + STRIP_RESN_MEMPROTMD"
      pattern: "from .setup_state import .* strip_resn_from_pdb"
    - from: "biochemeleon/gui_setup.py (09-03)"
      to: "biochemeleon/setup_state.TIER_LABELS"
      via: "demo_combo display string maps difficulty -> display label"
      pattern: "TIER_LABELS\\.get\\(meta\\['difficulty'\\]"
---

<objective>
Build the Phase 9 pure-layer foundation: extend `DEMO_MANIFEST` to 9 entries with the new fetch-source schema (`source`/`source_id`/`fetch_url`/`cache_name`/`citation`/`strip`), add the `TIER_LABELS` display map for DIFF-05, fix the 4wb3 'mixed'→'hard' mapping, make `randomize_state` offline-safe (exclude fetched demos), and add the pure `strip_resn_from_pdb` helper that the MemProtMD fetch worker uses to strip SOL/NA/CL by residue-name line-filtering (deterministic, avoids loading the 95k-atom wet file into PyMOL).

Purpose: This is the foundation every other Phase 9 plan depends on — 09-02 imports the extended manifest + strip helper; 09-03 imports TIER_LABELS. Pure layer = WSL-unit-testable, no PyMOL needed.
Output: Extended `setup_state.py` (still pure: stdlib only) + new unit tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-large-demo-fetch-source-attribution/09-RESEARCH-pipeline.md
@.planning/phases/09-large-demo-fetch-source-attribution/09-RESEARCH-memprotmd.md
@.planning/phases/09-large-demo-fetch-source-attribution/09-RESEARCH-sasbdb.md
@biochemeleon/setup_state.py
@tests/test_setup_state.py
</context>

<feature>
  <name>Phase 9 pure-layer foundation — manifest schema, tier labels, randomize exclusion, strip helper</name>
  <files>biochemeleon/setup_state.py, tests/test_setup_state.py</files>
  <behavior>
    The pure setup_state module gains a uniform 9-entry DEMO_MANIFEST, a TIER_LABELS display map, an offline-safe randomize_state, and a pure PDB-line-filter strip helper. Testable behaviors (each `expect(fn(input)).toBe(output)`):

    Manifest schema (every entry, all 9):
      - entry has keys: category, type, difficulty, source, source_id, fetch_url, cache_name, citation, strip
      - 6 bundled entries: source=='bundled', fetch_url is None, strip is False, cache_name == <existing .pdb filename>
      - 1gzm: source=='memprotmd', source_id=='1GZM', difficulty=='very_challenging', strip==True, cache_name=='1gzm.pdb.gz', fetch_url startswith 'https://memprotmd.bioch.ox.ac.uk/data/memprotmd/simulations/1gzm_default_dppc/files/structures/at.pdb'
      - 3gp6: source=='memprotmd', source_id=='3GP6', difficulty=='very_challenging', strip==True, cache_name=='3gp6.pdb.gz', fetch_url startswith 'https://memprotmd.bioch.ox.ac.uk/data/memprotmd/simulations/3gp6_default_dppc/files/structures/at.pdb'
      - sasdpg4: source=='sasbdb', source_id=='SASDPG4', difficulty=='challenge', strip==False, cache_name=='SASDPG4_fit2_model1.pdb.gz', fetch_url == 'https://www.sasbdb.org/media/pdb_file/SASDPG4_fit2_model1.pdb'
      - 4wb3 difficulty == 'hard' (NOT 'mixed')

    TIER_LABELS (4 identifier-safe -> 4 display):
      - {'easy':'Easy', 'hard':'Hard', 'challenge':'Challenge', 'very_challenging':'Very challenging'}
      - every manifest entry's difficulty is a key in TIER_LABELS

    randomize_state offline-safety:
      - for any seed, when target_mode resolves to 'demo', demo_id is chosen ONLY from entries whose meta.get('source','bundled') == 'bundled' (never 1gzm/3gp6/sasdpg4)
      - lock_source path still works (preserves a fetched demo_id if the user explicitly locked it — only the non-lock-source random pick excludes fetched)

    strip_resn_from_pdb(text, strip_set) pure helper:
      - input: PDB text + a set of residue names to strip (e.g. {'SOL','NA','CL'})
      - removes ATOM lines whose `line[17:20].strip()` is in strip_set (padding-agnostic: 'NA ', ' NA', 'NA' all strip)
      - preserves ALL non-ATOM lines (TITLE, CRYST1, MODEL, TER, ENDMDL, END, REMARK, HETATM, etc.)
      - empty text -> empty string; no ATOM lines -> unchanged
      - synthetic input with ATOM SOL/NA/CL/DPP/MET + TITLE/CRYST1/MODEL/TER/ENDMDL -> SOL/NA/CL removed, DPP/MET kept, all non-ATOM preserved
  </behavior>
  <implementation>
    Extend `biochemeleon/setup_state.py` (keep it PURE: stdlib only — `import random`, `import copy`; NO `from pymol`, NO `from pymol.Qt`):

    1. DEMO_MANIFEST — extend all 6 existing entries with the 6 new fields (source='bundled', source_id=<UPPERCASE pdb id>, fetch_url=None, cache_name=<the existing 'file' value>, citation=<UPPERCASE pdb id>, strip=False). Rename the 'file' key to 'cache_name' across all entries (the on-disk filename; `source` tells the loader which dir — bundled uses data/demos/, fetched uses tmp/phase9-demos/cache/). Change 4wb3 'difficulty' from 'mixed' to 'hard' (09-RESEARCH-pipeline.md:422 resolves the Phase 2 deferral). Add 3 new entries IN TIER ORDER (easy→hard→challenge→very_challenging) so the combo shows a natural progression (09-RESEARCH-pipeline.md:458-459):
       - '1gzm': category='Membrane protein', type='protein', difficulty='very_challenging', source='memprotmd', source_id='1GZM', fetch_url='https://memprotmd.bioch.ox.ac.uk/data/memprotmd/simulations/1gzm_default_dppc/files/structures/at.pdb', cache_name='1gzm.pdb.gz', citation='1GZM', strip=True  (URL from 09-RESEARCH-memprotmd.md:37-40)
       - '3gp6': category='Membrane protein', type='protein', difficulty='very_challenging', source='memprotmd', source_id='3GP6', fetch_url='https://memprotmd.bioch.ox.ac.uk/data/memprotmd/simulations/3gp6_default_dppc/files/structures/at.pdb', cache_name='3gp6.pdb.gz', citation='3GP6', strip=True
       - 'sasdpg4': category='Glycoprotein', type='protein', difficulty='challenge', source='sasbdb', source_id='SASDPG4', fetch_url='https://www.sasbdb.org/media/pdb_file/SASDPG4_fit2_model1.pdb', cache_name='SASDPG4_fit2_model1.pdb.gz', citation='SASDPG4', strip=False  (URL + fit2_model1 hardcode from 09-RESEARCH-sasbdb.md:207-213,277-283; strip=False per 09-RESEARCH-sasbdb.md:250-257 — glycan HETATM must survive)
       IMPORTANT: the `source` vocabulary is lowercase ('bundled'/'memprotmd'/'sasbdb') — normalize the SASBDB researcher's uppercase 'SASBDB' to 'sasbdb' for consistency with the pipeline schema (09-RESEARCH-pipeline.md:411).

    2. TIER_LABELS — new module constant (09-RESEARCH-pipeline.md:397-402):
       TIER_LABELS = {'easy':'Easy', 'hard':'Hard', 'challenge':'Challenge', 'very_challenging':'Very challenging'}

    3. STRIP_RESN_MEMPROTMD — new module constant: the verified MemProtMD strip set (09-RESEARCH-memprotmd.md:222-230 — SOL water + NA sodium + CL chloride, empirically verified by wet-vs-dry diff + atom-count math 76,018 = 75,789+116+113):
       STRIP_RESN_MEMPROTMD = {'SOL', 'NA', 'CL'}

    4. strip_resn_from_pdb(text, strip_set) — new pure function. Iterate `text.splitlines()`; for lines starting with 'ATOM', compute `resn = line[17:20].strip()` (PDB cols 18-20, 0-indexed 17:20; .strip() makes it padding-agnostic per 09-RESEARCH-memprotmd.md:283-284); if `resn in strip_set` skip the line, else keep it; keep ALL non-ATOM lines unconditionally. Return `'\n'.join(kept) + '\n'`. Guard: empty/None text -> ''. This is the deterministic pure-Python strip (NOT PyMOL solvent/inorganic selectors) — rationale: MemProtMD records SOL/NA/CL as ATOM (0 HETATM), and the wet file never needs to enter PyMOL (09-RESEARCH-memprotmd.md:247-254).

    5. randomize_state — change the demo_id pick (currently `demo_id = rng.choice(list(DEMO_MANIFEST.keys()))` at setup_state.py:230) to exclude fetched demos: `bundled_ids = [did for did, m in DEMO_MANIFEST.items() if m.get('source', 'bundled') == 'bundled']; demo_id = rng.choice(bundled_ids)`. This implements Open Risk 3 recommendation (a) — randomize should pick demos that "just work" offline (09-RESEARCH-memprotmd.md:479). The lock_source branch (preserves a user-locked fetched demo_id) is UNCHANGED — only the non-lock random pick excludes fetched.

    6. validate_state — the existing `did if did in DEMO_MANIFEST else '1znf'` (line 275) already accepts the 3 new ids; no change needed, but confirm the new ids are valid keys (they are). Backward-compat: `meta.get('source','bundled')` default keeps old code paths working if any caller hasn't migrated.

    7. Module docstring (line 27): update "Citations live in biochemeleon/data/demos/SOURCES.md (Plan 02-02)" → "Citations live in repo-root DATA_SOURCES.md (DEMO-04, Phase 9)." (09-04 writes DATA_SOURCES.md; this docstring update is the ONLY setup_state.py touch 09-04 does NOT own — 09-01 owns it to keep file ownership disjoint for parallel Wave 1.)

    PURITY GATE (must stay green): `grep -rnE "from pymol|import pymol|from pymol.Qt" biochemeleon/setup_state.py` → ZERO matches. The module remains stdlib-only (random, copy) — unit-testable in WSL without PyMOL.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>RED: write failing tests for manifest schema, tier labels, 4wb3->hard, randomize exclusion, strip helper</name>
  <files>tests/test_setup_state.py</files>
  <action>
    Add new test classes to tests/test_setup_state.py (follow the existing stub pattern — pymol/pymol.Qt are MagicMock'd via sys.modules at line 13-15, so importing biochemeleon.* works in WSL). Add `TIER_LABELS, STRIP_RESN_MEMPROTMD, strip_resn_from_pdb` to the existing `from biochemeleon.setup_state import (...)` block at line 20-24.

    Test classes to add:
    - TestManifestSchemaPhase9: assert len(DEMO_MANIFEST)==9; assert every entry has keys {'category','type','difficulty','source','source_id','fetch_url','cache_name','citation','strip'}; assert the 6 bundled entries have source=='bundled' and fetch_url is None and strip is False; assert '1gzm' in DEMO_MANIFEST and '3gp6' in DEMO_MANIFEST and 'sasdpg4' in DEMO_MANIFEST.
    - TestFetchUrls: assert DEMO_MANIFEST['1gzm']['fetch_url']=='https://memprotmd.bioch.ox.ac.uk/data/memprotmd/simulations/1gzm_default_dppc/files/structures/at.pdb'; same for 3gp6; assert DEMO_MANIFEST['sasdpg4']['fetch_url']=='https://www.sasbdb.org/media/pdb_file/SASDPG4_fit2_model1.pdb' (the fit2_model1 hardcode — 09-RESEARCH-sasbdb.md:97,213).
    - TestTierLabels: assert TIER_LABELS == {'easy':'Easy','hard':'Hard','challenge':'Challenge','very_challenging':'Very challenging'}; assert every entry's difficulty is a key in TIER_LABELS.
    - TestTierAssignment: assert DEMO_MANIFEST['1gzm']['difficulty']=='very_challenging'; DEMO_MANIFEST['3gp6']['difficulty']=='very_challenging'; DEMO_MANIFEST['sasdpg4']['difficulty']=='challenge'.
    - Test4wb3MappedToHard: assert DEMO_MANIFEST['4wb3']['difficulty']=='hard' (NOT 'mixed').
    - TestStripFalseForSasbdb: assert DEMO_MANIFEST['sasdpg4']['strip']==False (glycan HETATM must survive — 09-RESEARCH-sasbdb.md:257).
    - TestRandomizeExcludesFetched: for seed in range(50): s=randomize_state(seed=seed); if s['target_mode']=='demo': assert DEMO_MANIFEST[s['demo_id']].get('source','bundled')=='bundled' (never 1gzm/3gp6/sasdpg4). Also test the lock_source path preserves a locked fetched demo_id: locked={'target_mode':'demo','demo_id':'1gzm'}; s=randomize_state(seed=7, lock_source=True, locked_state=locked); assert s['demo_id']=='1gzm' (lock_source overrides the exclusion — only the random pick excludes).
    - TestStripResnFromPdb: 
      * synthetic = "TITLE test\nCRYST1\nATOM      1  N   MET     1\nATOM      2  N   SOL     2\nATOM      3  N   NA      3\nATOM      4  N   CL      4\nATOM      5  N   DPP   505\nTER\nENDMDL\n" — strip with STRIP_RESN_MEMPROTMD -> assert MET/DPP/TER/ENDMDL/TITLE/CRYST1 kept, SOL/NA/CL removed, result has exactly the 2 protein/lipid ATOM lines (MET, DPP).
      * padding variants: "ATOM  1  N   NA      3\n" (trailing space) and "ATOM  1  N    NA     3\n" (leading space, cols 18-20 = ' NA') both stripped (the .strip() makes it padding-agnostic).
      * empty input: strip_resn_from_pdb('', STRIP_RESN_MEMPROTMD) == ''.
      * non-ATOM preserved: a HETATM line with resn HOH is NOT stripped by STRIP_RESN_MEMPROTMD (only ATOM lines are filtered — 09-RESEARCH-memprotmd.md:267-274 keeps all non-ATOM lines).

    Run the tests — they MUST fail (AttributeError on TIER_LABELS/strip_resn_from_pdb imports; assertion failures on the 9-entry count, new ids, 4wb3 'hard', fetch URLs). Commit: `test(09-01): add failing tests for manifest schema, tiers, randomize exclusion, strip helper`.
  </action>
  <verify>python3.6 -m unittest tests.test_setup_state -v 2>&1 | tail -30 — new tests FAIL (RED); existing 90 tests still pass (no regression). python3.6 -m py_compile tests/test_setup_state.py</verify>
  <done>RED confirmed: new test classes fail on missing TIER_LABELS/strip_resn_from_pdb/9-entry manifest/4wb3-hard/fetch URLs; existing tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>GREEN: extend setup_state.py manifest + TIER_LABELS + strip helper + randomize exclusion</name>
  <files>biochemeleon/setup_state.py</files>
  <action>
    Implement the <implementation> section above. Extend DEMO_MANIFEST to 9 entries (6 existing + 1gzm/3gp6/sasdpg4) with the uniform schema; rename 'file' -> 'cache_name' across all entries; change 4wb3 difficulty 'mixed'->'hard'; add TIER_LABELS, STRIP_RESN_MEMPROTMD, strip_resn_from_pdb; change randomize_state's demo_id pick to exclude fetched; update the module docstring citation line. Keep the module PURE (stdlib only — random, copy; NO pymol/Qt import).

    Run the tests — they MUST pass. Commit: `feat(09-01): extend DEMO_MANIFEST schema, TIER_LABELS, strip helper, randomize exclusion`.
  </action>
  <verify>python3.6 -m unittest tests.test_setup_state -v 2>&1 | tail -30 — ALL tests pass (GREEN; 90 existing + ~30 new). python3.6 -m py_compile biochemeleon/setup_state.py. PURITY GATE: grep -rnE "from pymol|import pymol|from pymol.Qt" biochemeleon/setup_state.py → ZERO matches (exit 1). Pitfall-1 gate package-wide: grep -rnE "import Tkinter|import tkinter|from tkinter|import Pmw|from Pmw|app\.root|grab_set|mainloop|Toplevel|menuBar\.addmenuitem|from PyQt5 import|import PyQt5" biochemeleon/ → ZERO (exit 1).</verify>
  <done>GREEN: all tests pass; setup_state.py still pure (no pymol/Qt); 9-entry manifest with uniform schema; TIER_LABELS maps 4 tiers; 4wb3='hard'; randomize excludes fetched; strip_resn_from_pdb filters SOL/NA/CL padding-agnostically.</done>
</task>

<task type="auto" tdd="true">
  <name>REFACTOR: cleanup if obvious improvements (no behavior change)</name>
  <files>biochemeleon/setup_state.py</files>
  <action>
    Review the extended setup_state.py for obvious cleanup (e.g. a manifest docstring noting the Phase 9 schema, a tier-ordering comment). NO behavior change. If no cleanup needed, skip the commit. Run tests — MUST still pass. Commit only if changed: `refactor(09-01): clean up manifest schema docs`.

    Note the AGENTS.md-documented grep false-positive pattern: docstrings containing literal tokens like `from pymol` or `.exec_()` trip the Pitfall-1/exec_ gates. Write docstrings in PROSE (e.g. "the PyMOL cmd import" not "from pymol import cmd"; "shows the dialog" not "dialog.exec_()") to avoid false positives — mirrors the 03-02/06/09/10 precedent.
  </action>
  <verify>python3.6 -m unittest tests.test_setup_state -v 2>&1 | tail -10 — all pass. py_compile. Pitfall-1 + purity gates still green.</verify>
  <done>REFACTOR complete (or skipped); tests green; gates green.</done>
</task>

</tasks>

<verification>
- python3.6 -m unittest tests.test_setup_state -v → all pass (90 existing + new Phase 9 tests, no regression).
- python3.6 -m py_compile biochemeleon/setup_state.py → no syntax error.
- PURITY: grep -rnE "from pymol|import pymol|from pymol.Qt" biochemeleon/setup_state.py → ZERO (module stays stdlib-only; WSL-unit-testable).
- Pitfall-1 gate package-wide: ZERO matches.
- DEMO_MANIFEST has 9 entries; 1gzm/3gp6/sasdpg4 present with research-verified URLs; 4wb3='hard'; TIER_LABELS maps 4 tiers; strip_resn_from_pdb filters SOL/NA/CL.
</verification>

<success_criteria>
- The pure layer carries the full Phase 9 manifest (9 demos, 4 tiers, 3 fetched sources) + the strip helper that 09-02's worker uses.
- randomize_state is offline-safe (never picks a network-required demo).
- setup_state.py remains pure (stdlib only) — the foundation for 09-02 (demos.py) and 09-03 (gui_setup.py).
</success_criteria>

<output>
After completion, create `.planning/phases/09-large-demo-fetch-source-attribution/09-01-SUMMARY.md`
</output>
