---
phase: 09-large-demo-fetch-source-attribution
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - biochemeleon/__init__.py
  - biochemeleon/gui_setup.py
autonomous: false

must_haves:
  truths:
    - "A modeless QProgressDialog (.show(), NEVER .exec_()) shows during large-demo fetch with a Cancel button; the 3D viewer stays interactive (rotatable) during download"
    - "The Cancel button aborts the in-flight download (worker exits, temp cleaned, dialog closes)"
    - "The demo sub-menu (QComboBox) shows 9 demos with tier labels: Easy/Hard/Challenge/Very challenging (SC4)"
    - "Selecting 1GZM or 3GP6 and clicking Start fetches from MemProtMD (cache miss) → progress dialog → finalize → game starts with membrane visible (no water)"
    - "Selecting SASDPG4 and clicking Start fetches from SASBDB (cache miss) → progress dialog → finalize → game starts with glycan visible"
    - "Cache-hit (second fetch of same demo) loads synchronously with no progress dialog"
    - "exec_ grep gate stays at exactly 1 (only gui_game.py:303 QMessageBox — the QProgressDialog uses .show())"
  artifacts:
    - path: "biochemeleon/__init__.py"
      provides: "PluginDialog._resolve_large_demo (QProgressDialog + QTimer drain + worker spawn + finalize) + _prepare_and_start large-demo branch"
      contains: "_resolve_large_demo"
    - path: "biochemeleon/gui_setup.py"
      provides: "demo_combo tier-label display via TIER_LABELS"
      contains: "TIER_LABELS"
  key_links:
    - from: "biochemeleon/__init__.py._prepare_and_start"
      to: "biochemeleon/demos.load_demo"
      via: "demo mode: load_demo returns None on cache miss -> _resolve_large_demo"
      pattern: "demos\\.load_demo|_resolve_large_demo"
    - from: "biochemeleon/__init__.py._resolve_large_demo"
      to: "biochemeleon/demos.download_large_demo"
      via: "threading.Thread worker + QTimer.singleShot drain -> finalize_large_demo"
      pattern: "download_large_demo|finalize_large_demo"
    - from: "biochemeleon/gui_setup.py.demo_combo"
      to: "biochemeleon/setup_state.TIER_LABELS"
      via: "display string maps difficulty -> tier label"
      pattern: "TIER_LABELS"
---

<objective>
Wire the Qt layer: (a) a modeless cancelable `QProgressDialog` in `__init__.py`'s `PluginDialog` that orchestrates the 09-02 split API (worker download + main-thread finalize), triggered when `_prepare_and_start` hits a cache-miss fetched demo; (b) the demo sub-menu tier display in `gui_setup.py` (DIFF-05/SC4) mapping `difficulty` → `TIER_LABELS` display labels. Then human-verify the full fetch UX + visual correctness in a real Windows PyMOL GUI session.

Purpose: SC1 (modeless cancelable progress dialog, viewer stays interactive), SC2 (SASBDB fetch on demand), SC4 (4-tier sub-menu). The Qt orchestration lives here (NOT in demos.py — dependency direction + Pitfall 6: the drain needs QTimer/processEvents which are Qt).
Output: Extended `__init__.py` + `gui_setup.py`; human-verify checkpoint for the GUI fetch UX + visual load.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-large-demo-fetch-source-attribution/09-RESEARCH-pipeline.md
@.planning/phases/09-large-demo-fetch-source-attribution/09-RESEARCH-memprotmd.md
@.planning/phases/09-large-demo-fetch-source-attribution/09-RESEARCH-sasbdb.md
@.planning/phases/09-large-demo-fetch-source-attribution/09-02-SUMMARY.md
@biochemeleon/__init__.py
@biochemeleon/gui_setup.py
@biochemeleon/gui_game.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: demo sub-menu tier display (gui_setup.py) — DIFF-05/SC4</name>
  <files>biochemeleon/gui_setup.py</files>
  <action>
    Modify `biochemeleon/gui_setup.py` demo_combo population (currently lines 124-128):
    - Add `TIER_LABELS` to the existing `from .setup_state import (...)` block (line 20-24).
    - Replace the demo_combo population loop (lines 125-128). Current: `self.demo_combo.addItem("{} — {} ({})".format(meta['category'], did, meta['difficulty']), did)`. New (per 09-RESEARCH-pipeline.md:446-456):
      ```
      for did, meta in DEMO_MANIFEST.items():
          tier = TIER_LABELS.get(meta['difficulty'], meta['difficulty'].title())
          self.demo_combo.addItem(
              "{category} — {id} ({tier})".format(
                  category=meta['category'], id=did, tier=tier), did)
      ```
      This produces display strings like "Membrane protein — 3gp6 (Very challenging)", "Glycoprotein — sasdpg4 (Challenge)", "Protein — 1znf (Easy)". The 09-01 manifest is tier-ordered (easy→hard→challenge→very_challenging) so the combo shows a natural progression. (9 items < 15 threshold — keep the flat QComboBox per 09-RESEARCH-pipeline.md:441-464.)
    - The `meta['difficulty']` values are now the identifier-safe keys ('easy'/'hard'/'challenge'/'very_challenging' — 09-01); TIER_LABELS maps them to the SC4 display labels. The `.title()` fallback handles any unmapped value defensively.
    - NO other changes to gui_setup.py (the _on_fetch handler, apply_state/gather_state, etc. are unaffected — demo_id is still the combo's userData).
  </action>
  <verify>python3.6 -m py_compile biochemeleon/gui_setup.py. grep -n "TIER_LABELS" biochemeleon/gui_setup.py → present (the import + the .get() call). Pitfall-1 gate: ZERO. exec_ gate: still 1 (no new .exec_() in gui_setup.py — the QProgressDialog is in __init__.py, not here).</verify>
  <done>demo_combo shows 9 demos with tier labels via TIER_LABELS; display strings match SC4 ("Easy"/"Hard"/"Challenge"/"Very challenging").</done>
</task>

<task type="auto">
  <name>Task 2: modeless cancelable QProgressDialog orchestration (__init__.py) — SC1</name>
  <files>biochemeleon/__init__.py</files>
  <action>
    Modify `biochemeleon/__init__.py` `PluginDialog`:

    1. MODIFY `_prepare_and_start` (the `elif mode == "demo":` branch, lines 136-146): after `target_obj = demos.load_demo(demo_id)` returns None (cache miss for a fetched demo), check if the demo is a fetched demo (`demos.DEMO_MANIFEST.get(demo_id, {}).get('source', 'bundled') != 'bundled'`). If so, call `target_obj = self._resolve_large_demo(demo_id)` — the new method that shows the progress dialog + spawns the worker + finalizes. If `_resolve_large_demo` returns None (fetch failed/canceled), show the existing "Demo failed" QMessageBox + return None,None,[]. If `load_demo` returns None for a BUNDLED demo (existing behavior — file missing), keep the existing QMessageBox. Per 09-RESEARCH-pipeline.md:485-487 option (b): the QTimer drain keeps the viewer interactive while the fetch runs; on 'done', finalize loads the object and _prepare_and_start continues.

    2. ADD `_resolve_large_demo(self, demo_id)` — the modeless cancelable QProgressDialog orchestration. Implement EXACTLY the Pattern 1 skeleton from 09-RESEARCH-pipeline.md:75-154:
       - `import threading, queue, os` (add to the method's local imports, mirroring the lazy-import pattern in _prepare_and_start).
       - Cache hit check first: `obj = demos.load_cached_demo(demo_id); if obj is not None: return obj` (synchronous, no dialog — but _prepare_and_start already called load_demo which calls load_cached_demo, so this is a defensive re-check; the primary cache-hit path is load_demo returning non-None).
       - Construct `QtWidgets.QProgressDialog("Downloading %s…" % demo_id, "Cancel", 0, 100, self.window())`.
       - `progress.setWindowModality(QtCore.Qt.NonModal)` (MODELESS).
       - `progress.setAutoClose(False); progress.setAutoReset(False)` (prevent auto-hide mid-pipeline — 09-RESEARCH-pipeline.md:333).
       - `progress.setMinimumDuration(500)` (show after 500ms).
       - `progress.setValue(0); progress.show()` — **`.show()`, NEVER `.exec_()`** (trips the exec_ grep gate + blocks the viewer — Pitfall A, 09-RESEARCH-pipeline.md:190-195).
       - `q = queue.Queue(); cancel = threading.Event(); progress.canceled.connect(cancel.set)`.
       - `tmp_path = demos.temp_download_path(demo_id)`.
       - `worker = threading.Thread(target=demos.download_large_demo, args=(demo_id, tmp_path, q, cancel), daemon=True); worker.start()`.
       - Define `drain()` (nested function) per 09-RESEARCH-pipeline.md:109-151: drain the queue non-blocking; on `('progress', pct)` → `progress.setValue(pct)` + `setLabelText`; on `('done',)` → `setLabelText("Loading…")`, `setRange(0,0)` (indeterminate busy), `QtWidgets.QApplication.processEvents()` (paint before blocking cmd.*), `obj = demos.finalize_large_demo(demo_id, tmp_path)`, `demos.cleanup_temp(tmp_path)`, `progress.close()`, then continue the start flow (see step 3 below — the re-entrancy), `return`; on `('error', msg)` → `progress.close()`, `demos.cleanup_temp(tmp_path)`, `QtWidgets.QMessageBox.warning(self.window(), "Fetch failed", ...)`, `return`; on `('canceled',)` → `progress.close()`, `demos.cleanup_temp(tmp_path)`, `return`. If queue empty + cancel set + worker dead → close + cleanup + return. Else `QtCore.QTimer.singleShot(100, drain)` (recursive — keeps the Qt event loop running so the 3D viewer stays smooth).
       - `QtCore.QTimer.singleShot(100, drain)`.
       - Return: because the fetch is async, `_resolve_large_demo` cannot synchronously return the object. Use the 09-RESEARCH-pipeline.md:487 option (b) approach: `_resolve_large_demo` initiates the dialog + worker, and the `drain()` 'done' branch calls a continuation that finishes `_prepare_and_start` (step 3). `_resolve_large_demo` itself returns None to signal "async in progress" — BUT distinguish from "fetch failed" by stashing state. Cleanest: have `_resolve_large_demo` return a sentinel or set `self._pending_large_demo = demo_id`, and the drain 'done' branch calls `self._continue_after_large_demo_fetch(obj)`. If the user cancels or the fetch errors, `self._pending_large_demo = None` (the QMessageBox already showed).

    3. ADD `_continue_after_large_demo_fetch(self, obj)` — the continuation that finishes _prepare_and_start's remaining steps (collapse states, build hider_specs, start game) now that the fetched object is loaded. This is a behavior-preserving extraction: take the body of _prepare_and_start AFTER the target resolution (lines 158-266: collapse_to_single_state, extent, neighbor_ids, cas_list, hider_specs, free_nterminal_valence, cleanup prior game, GameController.start) and move it into `_continue_after_large_demo_fetch(self, target_obj, state)` so BOTH the synchronous path (bundled/loaded/fetch) and the async path (large demo) can call it. _prepare_and_start's synchronous paths call it directly; the async drain calls it after finalize. Return `(controller, target_obj, warnings)`.
       IMPORTANT — re-entrancy (09-RESEARCH-pipeline.md Open Q3, :487 + :655): after the async fetch completes, `_on_start` (which called _prepare_and_start) needs to do the tab switch + countdown. Adjust `_on_start` (lines 83-94): if _prepare_and_start returns (None, None, []) due to an async large-demo fetch in progress (detect via `self._pending_large_demo is not None`), do NOT show the "failure" path — just return (the drain's continuation will call `self.tabs.setCurrentWidget(self.game_tab); self.game_tab.start_countdown(self._controller)` after the fetch + start complete). If _prepare_and_start returns (None, None, []) due to a real failure (pending is None), keep the existing behavior (QMessageBox already shown by _prepare_and_start).

    4. The QMessageBox on fetch failure uses `QMessageBox.warning` (allowed — child dialog, the exec_ gate allows QMessageBox; 09-RESEARCH-pipeline.md:137-139).

    5. CRITICAL — exec_ grep gate (Pitfall A, 09-RESEARCH-pipeline.md:190-195): the QProgressDialog MUST use `.show()`, NEVER `.exec_()`. After editing, verify `grep -rnE "\.exec_\(\)" biochemeleon/` still returns exactly 1 (gui_game.py:303 only). Write docstrings in PROSE (no literal `.exec_()` tokens — mirrors the 03-02/06/09/10 + 07-02 Rule-3 precedent for avoiding grep false-positives).

    6. CRITICAL — Pitfall 6 (09-RESEARCH-pipeline.md:197-201): the worker thread (`demos.download_large_demo`) does ONLY urllib (verified in 09-02 — no `cmd.` in its body). The `drain()` callback runs on the main thread (QTimer) and calls `demos.finalize_large_demo` (cmd.*). NO `cmd.*` in the worker. NO `processEvents` in demos.py (it's in __init__.py's drain only).

    7. Module docstring / method docstrings: document the modeless + cancelable + viewer-interactive design. PROSE only (no literal `.exec_()` / `from pymol.Qt` tokens in docstrings — grep-gate hygiene).
  </action>
  <verify>python3.6 -m py_compile biochemeleon/__init__.py. exec_ GATE: grep -rnE "\.exec_\(\)" biochemeleon/ → exactly 1 (gui_game.py:303 only — the QProgressDialog uses .show(), NOT .exec_()). Pitfall-1 gate: ZERO. Pitfall-6 static check: `python3.6 -c "import inspect, biochemeleon.demos as d; src=inspect.getsource(d.download_large_demo); assert 'cmd.' not in src"` (the worker is cmd-free; the Qt drain in __init__.py calls finalize on the main thread). python3.6 -m unittest tests.test_setup_state -v 2>&1 | tail -5 → no regression.</verify>
  <done>__init__.py has _resolve_large_demo (modeless QProgressDialog .show() + QTimer drain + worker + finalize) + _continue_after_large_demo_fetch (async continuation) + _prepare_and_start branches on fetched cache-miss; exec_ gate at 1; Pitfall 6 compliant (worker cmd-free).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full Phase 9 fetch UX: a modeless cancelable QProgressDialog that downloads large demos (MemProtMD 1GZM/3GP6 + SASBDB SASDPG4) via a worker thread, finalizes on the main thread (strip + compress + cache), and keeps the 3D viewer interactive. The demo sub-menu shows 9 demos with 4-tier labels (Easy/Hard/Challenge/Very challenging). Cache-hit loads synchronously (no dialog).
  </what-built>
  <how-to-verify>
    **Run in a real Windows PyMOL GUI session** (setenv.bat → pymol → Plugins → bioCHEMeleon). The WSL agent CANNOT run the Qt/GUI paths (AGENTS.md). Verify ALL of:

    **SC4 — demo sub-menu tiers (quick check first):**
    1. Open the Setup tab. In the demo dropdown, confirm 9 entries with tier labels: 3 "Easy" (1znf, 5e54, 1k8p), 3 "Hard" (1xdn, 2qbz, 4wb3), 1 "Challenge" (sasdpg4 — Glycoprotein), 2 "Very challenging" (1gzm, 3gp6 — Membrane protein). The labels must read EXACTLY "Easy"/"Hard"/"Challenge"/"Very challenging" (not 'mixed', not 'very_challenging').

    **SC1 — MemProtMD fetch (1GZM, the helix demo) + modeless dialog:**
    2. Select "Membrane protein — 1gzm (Very challenging)" in the demo dropdown. Set hider count low (e.g. 3) + spheres only (faster). Click Start.
    3. A progress dialog appears ("Downloading 1gzm…") with a Cancel button. **While it downloads, left-drag to rotate the molecule in the 3D viewer — it MUST stay smooth/interactive** (SC1: "modeless cancelable progress dialog" + viewer stays interactive). If the viewer freezes, the dialog is modal (bug — .exec_() was used instead of .show()).
    4. After download completes, the dialog label changes to "Loading…" (indeterminate busy), then closes. The 1gzm object loads with the DPPC membrane visible (bilayer) and NO water (the strip removed SOL/NA/CL). Visually confirm the membrane is intact (a lipid bilayer around the helical protein).
    5. The game starts (3-2-1 countdown → play). Click around to confirm hiders work (basic sanity — the full game loop is Phase 4's job; here just confirm the fetch→start flow completes).
    6. Click Cleanup. Confirm the object restores (membrane still there, hiders gone).

    **SC1 — MemProtMD fetch (3GP6, the sheets/beta-barrel demo) + cache hit:**
    7. Select "Membrane protein — 3gp6 (Very challenging)". Click Start. This fetches (cache miss first time) → progress dialog → finalize. Confirm the beta-barrel + membrane load (no water).
    8. Cleanup. Select 3gp6 AGAIN. Click Start. This should be a CACHE HIT — loads synchronously, NO progress dialog (fast). Confirms the cache works.

    **SC1 cancel test:**
    9. (If 1gzm isn't cached yet, or delete the cache file tmp/phase9-demos/cache/1gzm.pdb.gz first.) Select 1gzm, click Start, and while the progress dialog shows "Downloading…", click Cancel. Confirm: the download aborts, the dialog closes, the temp file is cleaned, and NO partial object is loaded (the Start aborts cleanly — no crash, no half-loaded object). The viewer stays interactive throughout.

    **SC2 — SASBDB glycoprotein fetch + glycan visible:**
    10. Select "Glycoprotein — sasdpg4 (Challenge)". Click Start. Progress dialog → finalize (this is a small 400KB fetch — fast). The sasdpg4 object loads.
    11. **Visually confirm the glycan is visible** — show sticks or cartoon; you should see carbohydrate residues branching off the protein (NAG/MAN/BMA glycan trees). If the glycan is absent (protein-only), the wrong file was fetched (fit1_model1 instead of fit2_model1 — DEMO-03 failure; 09-RESEARCH-sasbdb.md:97). In the PyMOL command line, run `count_atoms sasdpg4 and hetatm` — should be 2601 (glycan HETATM). `count_atoms sasdpg4` — should be 4123.
    12. Confirm no water was stripped (there was none to strip — SASBDB strip=False). The glycan HETATM must survive (CAUTION from 09-RESEARCH-sasbdb.md:259 — a `hetatm` strip would nuke glycans; confirm it didn't happen).

    **Offline fallback (optional but recommended):**
    13. (If feasible) disconnect network, delete the cache, select 1gzm, click Start. Confirm a QMessageBox "Fetch failed / Could not download…" appears and the bundled demos still work (select 1znf, click Start — should load fine offline).

    **Structural ion check (Pitfall D, 09-RESEARCH-pipeline.md:209-213):**
    14. For 1gzm and 3gp6, visually confirm no important structural ion vanished (bacteriorhodopsin/PagP have no critical catalytic ions, so this should be clean — but confirm no metal sphere that should be there is missing).

    If ALL checks pass, type "approved". If the viewer freezes during download (modal bug), the glycan is absent, the membrane is missing, or Cancel doesn't work, describe the issue.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. "viewer froze during 1gzm download", "sasdpg4 glycan absent", "Cancel didn't abort")</resume-signal>
</task>

</tasks>

<verification>
- python3.6 -m py_compile biochemeleon/__init__.py biochemeleon/gui_setup.py.
- exec_ GATE: `grep -rnE "\.exec_\(\)" biochemeleon/` → exactly 1 (gui_game.py:303; the QProgressDialog uses .show()).
- Pitfall-1 gate: ZERO.
- Pitfall-6 static: download_large_demo body has no `cmd.`.
- Human confirmed: modeless dialog (viewer rotatable during download), Cancel works, 1gzm/3gp6 load with membrane (no water), sasdpg4 loads with glycan (2601 HETATM), cache-hit is synchronous (no dialog), 9-demo sub-menu shows 4 tier labels.
</verification>

<success_criteria>
- SC1: large membrane demos fetch on demand with full membrane, stripped of water/salt, compressed+cached, with a modeless cancelable progress dialog (viewer stays interactive).
- SC2: glycoprotein demo fetches from SASBDB on demand, glycan visible, source+IDs cited (DATA_SOURCES.md from 09-04).
- SC4: demo sub-menu surfaces 4-tier difficulty metadata (Easy/Hard/Challenge/Very challenging).
- All grep gates green; no regression in existing tests.
</success_criteria>

<output>
After completion, create `.planning/phases/09-large-demo-fetch-source-attribution/09-03-SUMMARY.md`
</output>
