---
phase: 09-large-demo-fetch-source-attribution
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - biochemeleon/demos.py
  - smoke/phase9_smoke.py
autonomous: true

must_haves:
  truths:
    - "demos.py has a Qt-free split API: download_large_demo (worker, urllib), finalize_large_demo (main, cmd.load+strip+cmd.save), load_cached_demo (cache hit), cache_path_for/is_cached/_cache_dir, temp_download_path/cleanup_temp"
    - "load_demo branches on meta['source']: 'bundled' -> existing path; fetched -> load_cached_demo (cache hit) or None (cache miss, signals caller to fetch)"
    - "download_large_demo does NO cmd.* (Pitfall 6 compliant — urllib+queue+os only); posts progress/done/error/canceled to a queue"
    - "finalize_large_demo strips MemProtMD files via the pure strip_resn_from_pdb helper (line-filter SOL/NA/CL) BEFORE cmd.load, so the 95k-atom wet file never enters PyMOL; then cmd.save writes .pdb.gz cache natively"
    - "SASBDB demo (strip=False) skips the strip step — glycan HETATM preserved"
    - "cmd.load reads .pdb.gz natively (verified) and cmd.save writes .pdb.gz natively (verified)"
    - "A headless smoke test exercises the full fetch+strip+compress+cache round-trip for the SASBDB demo (4123 atoms, glycan present) + the MemProtMD strip helper + cache-hit path"
  artifacts:
    - path: "biochemeleon/demos.py"
      provides: "download_large_demo, finalize_large_demo, load_cached_demo, cache_path_for, is_cached, _cache_dir, temp_download_path, cleanup_temp + load_demo source-branching"
      contains: "download_large_demo"
    - path: "smoke/phase9_smoke.py"
      provides: "Headless smoke test for Phase 9 fetch/strip/compress/cache"
      contains: "phase9"
  key_links:
    - from: "biochemeleon/__init__.py (09-03)"
      to: "biochemeleon/demos.download_large_demo"
      via: "QTimer drain calls download_large_demo in worker thread, finalize_large_demo on main thread"
      pattern: "demos\\.download_large_demo"
    - from: "biochemeleon/demos.finalize_large_demo"
      to: "biochemeleon/setup_state.strip_resn_from_pdb"
      via: "MemProtMD strip happens in pure Python before cmd.load"
      pattern: "strip_resn_from_pdb"
---

<objective>
Build the cmd-layer split API in `demos.py` (Qt-free) that 09-03's Qt orchestration drives: a worker-thread `download_large_demo` (urllib only, no cmd.* — Pitfall 6), a main-thread `finalize_large_demo` (cmd.load + strip + cmd.save .pdb.gz cache), a `load_cached_demo` (cache hit), and cache/path helpers. Branch the existing `load_demo` on `meta['source']`. Write a headless smoke test that exercises the full round-trip for the SASBDB demo + the MemProtMD strip helper.

Purpose: SC1 (fetch + strip + compress + cache) and SC2 (SASBDB glycoprotein fetch) require this cmd layer. The split keeps `demos.py` Qt-free (the QProgressDialog + QTimer drain lives in 09-03's `__init__.py`); the worker does ONLY urllib (Pitfall 6 compliant).
Output: Extended `demos.py` + `smoke/phase9_smoke.py` (pure pymol.cmd.*, runs headlessly via run-conda-pymol.bat -cq).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-large-demo-fetch-source-attribution/09-RESEARCH-pipeline.md
@.planning/phases/09-large-demo-fetch-source-attribution/09-RESEARCH-memprotmd.md
@.planning/phases/09-large-demo-fetch-source-attribution/09-RESEARCH-sasbdb.md
@.planning/phases/09-large-demo-fetch-source-attribution/09-01-SUMMARY.md
@biochemeleon/demos.py
@biochemeleon/setup_state.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add split API to demos.py (download/finalize/load_cached/cache helpers) + branch load_demo on source</name>
  <files>biochemeleon/demos.py</files>
  <action>
    Extend `biochemeleon/demos.py`. Add `import os, threading, queue, urllib.request` at the top (stdlib only — no Qt). Add `from .setup_state import DEMO_MANIFEST, STRIP_RESN_MEMPROTMD, strip_resn_from_pdb` to the existing setup_state import (09-01 added these). Keep the module Qt-free (NO `from pymol.Qt` — Pitfall 6 / dependency direction).

    Add these functions (implementations follow 09-RESEARCH-pipeline.md:226-327 verbatim, adapted to use the 09-01 pure strip helper):

    1. `_cache_dir()` — returns `os.path.abspath(os.path.join(os.path.dirname(__file__), '..', 'tmp', 'phase9-demos', 'cache'))`. Mirrors load_demo's os.path.dirname(__file__)/data/demos pattern (09-RESEARCH-pipeline.md:296-299).

    2. `cache_path_for(demo_id)` — returns the absolute path to the cached .pdb.gz, or None for bundled/unknown. `meta = DEMO_MANIFEST.get(demo_id); if meta is None or meta.get('source') == 'bundled': return None; return os.path.join(_cache_dir(), meta['cache_name'])`.

    3. `is_cached(demo_id)` — `p = cache_path_for(demo_id); return bool(p and os.path.exists(p))`.

    4. `temp_download_path(demo_id)` — returns a temp path under tmp/phase9-demos/ for the raw download. `os.path.join(os.path.dirname(__file__), '..', 'tmp', 'phase9-demos', demo_id + '.raw')` (then to_windows_path at use). Or use tempfile — either is fine; document the choice.

    5. `cleanup_temp(path)` — `try: os.unlink(path) except OSError: pass` (idempotent).

    6. `download_large_demo(demo_id, dest_path, progress_queue, cancel_event)` — WORKER THREAD (Pitfall 6: NO cmd.*). Implementation per 09-RESEARCH-pipeline.md:231-258: `meta = DEMO_MANIFEST[demo_id]; url = meta['fetch_url']`; `req = urllib.request.Request(url, headers={'User-Agent': 'bioCHEMeleon/1.0'})`; `urllib.request.urlopen(req, timeout=60)`; read 65536-byte blocks; check `cancel_event.is_set()` between blocks (post `('canceled',)` + return); post `('progress', received*100//total)` if Content-Length known; post `('done',)` on completion; `except Exception as exc: progress_queue.put(('error', str(exc)))`. Write to `dest_path` (wb). This function uses ONLY urllib/queue/os/threading — NO `from pymol`, NO `cmd.*`.

    7. `finalize_large_demo(demo_id, downloaded_path)` — MAIN THREAD (cmd.*). Implementation per 09-RESEARCH-pipeline.md:264-292, adapted to use the 09-01 pure strip helper for MemProtMD (the MemProtMD researcher's PRIMARY recommendation: strip by explicit resn line-filtering in pure Python BEFORE cmd.load, so the 95k-atom wet file never enters PyMOL — 09-RESEARCH-memprotmd.md:247-282):
       ```
       meta = DEMO_MANIFEST[demo_id]
       obj_name = demo_id.lower()
       if meta.get('strip', False) and meta.get('source') == 'memprotmd':
           # MemProtMD: pure-Python strip SOL/NA/CL BEFORE cmd.load (wet file never enters PyMOL).
           # 09-RESEARCH-memprotmd.md:247-282 — deterministic, avoids Pitfall 12 (large wet load).
           with open(downloaded_path, 'r') as f:
               raw = f.read()
           dry = strip_resn_from_pdb(raw, STRIP_RESN_MEMPROTMD)  # 09-01 pure helper
           dry_path = downloaded_path + '.dry'
           with open(dry_path, 'w') as f:
               f.write(dry)
           load_path = dry_path
       else:
           load_path = downloaded_path  # SASBDB (strip=False) or bundled — load as-is
       win_path = to_windows_path(load_path)
       try:
           cmd.load(win_path, object=obj_name, zoom=1)  # reads .pdb / .pdb.gz via file_read magic
       except Exception:
           return None
       # Cache the loaded object as .pdb.gz (cmd.save writes gzip in one step — exporting.py:912)
       cache_dir = _cache_dir()
       os.makedirs(cache_dir, exist_ok=True)
       cache_path = os.path.join(cache_dir, meta['cache_name'])
       try:
           cmd.save(to_windows_path(cache_path), obj_name)  # exporting.py:859-914 + :986-989
       except Exception:
           pass  # cache write failure is non-fatal (object already loaded)
       # Clean the .dry temp (keep the .raw for debugging? no — cleanup_temp both)
       if meta.get('strip', False):
           cleanup_temp(dry_path)
       return obj_name
       ```
       Docstring MUST document the Pitfall C distinction (09-RESEARCH-pipeline.md:206): "This is the deliberate pre-game DEMO-02 strip (water/salt), NOT the cleanup-time segi-GAME filter (Pitfall 9). DPPC membrane is organic — survives." Note: SASBDB (strip=False) skips the strip entirely — glycan HETATM preserved (09-RESEARCH-sasbdb.md:257-259 CAUTION: a `cmd.remove hetatm` would nuke glycans — we do NOT do that).

    8. `load_cached_demo(demo_id)` — MAIN THREAD cache hit. `meta = DEMO_MANIFEST.get(demo_id); if meta is None or meta.get('source') == 'bundled': return None; p = cache_path_for(demo_id); if not os.path.exists(p): return None; obj_name = demo_id.lower(); try: cmd.load(to_windows_path(p), object=obj_name, zoom=1); return obj_name; except Exception: return None`. (09-RESEARCH-pipeline.md:312-327.)

    9. `load_demo(demo_id)` — MODIFY the existing function (demos.py:114-137) to branch on `meta.get('source', 'bundled')`: if 'bundled' → existing path (data/demos/{cache_name}); else (fetched) → `return load_cached_demo(demo_id)` (cache hit returns obj; cache miss returns None — the caller, __init__._prepare_and_start, then triggers the fetch via 09-03's _resolve_large_demo). Keep the existing bundled path UNCHANGED (just rename `meta['file']` → `meta['cache_name']` since 09-01 renamed the field). The branching per 09-RESEARCH-pipeline.md:477 + 09-RESEARCH-memprotmd.md:411-432.
       IMPORTANT: the existing line `path = os.path.join(os.path.dirname(__file__), 'data', 'demos', meta['file'])` must become `meta['cache_name']` (09-01 renamed file→cache_name for uniform schema).

    10. Module docstring: update to mention the new fetched-demo API (download_large_demo/finalize_large_demo/load_cached_demo). Write in PROSE to avoid grep false-positives (AGENTS.md-documented pattern — no literal `from pymol.Qt` / `.exec_()` in docstrings).

    PURITY: `demos.py` is cmd-coupled (has `from pymol import cmd`) but MUST NOT import `from pymol.Qt` (dependency direction: setup_state ← demos ← gui_setup/__init__; Qt lives only in the GUI layer). The worker `download_large_demo` uses NO `cmd.*` (Pitfall 6).
  </action>
  <verify>python3.6 -m py_compile biochemeleon/demos.py. grep -rnE "from pymol.Qt" biochemeleon/demos.py → ZERO (exit 1). grep -n "cmd\." biochemeleon/demos.py → present ONLY in finalize_large_demo/load_cached_demo/load_demo/fetch_pdb/get_active_reps (NOT in download_large_demo — Pitfall 6). python3.6 -m unittest tests.test_setup_state -v 2>&1 | tail -5 → existing tests still pass (no regression; demos.py import doesn't break because setup_state is pure). Pitfall-1 gate package-wide: ZERO.</verify>
  <done>demos.py has the 8 new functions + load_demo source-branching; download_large_demo is cmd-free (Pitfall 6); finalize_large_demo uses the pure strip helper for MemProtMD; SASBDB strip=False skips strip; .pdb.gz cache via cmd.save.</done>
</task>

<task type="auto">
  <name>Task 2: Write smoke/phase9_smoke.py (headless fetch+strip+compress+cache round-trip)</name>
  <files>smoke/phase9_smoke.py</files>
  <action>
    Create `smoke/phase9_smoke.py` modeled on smoke/phase8_smoke.py (pure pymol.cmd.* — NO Qt; runs headlessly via `cmd.exe /c C:\src\run-conda-pymol.bat -cq`). Structure (~6 sections, ~25 checks):

    SETUP: `from biochemeleon import demos, setup_state; from pymol import cmd`. Verify DEMO_MANIFEST has 9 entries; verify 1gzm/3gp6/sasdpg4 present; verify TIER_LABELS maps 4 tiers; verify strip_resn_from_pdb filters a synthetic SOL/NA/CL input.

    SECTION A — SASBDB fetch round-trip (the small+fast demo — 400KB, no strip, glycan-preserving): 
      - Stage the already-downloaded `tmp/phase9-demos/SASDPG4_fit2_model1.pdb` as the "downloaded_path" (simulating the worker download — no real network in the smoke; the staged file IS the real fetched content per 09-RESEARCH-sasbdb.md:382).
      - Call `demos.finalize_large_demo('sasdpg4', staged_path)` → returns 'sasdpg4'.
      - Assert `cmd.count_atoms('sasdpg4')` == 4123 (1522 protein + 2601 glycan — 09-RESEARCH-sasbdb.md:77).
      - Assert `cmd.count_atoms('sasdpg4 and hetatm')` == 2601 (glycan HETATM — DEMO-03 requirement; 09-RESEARCH-sasbdb.md:116).
      - Assert `cmd.count_atoms('sasdpg4 and resn NAG+MAN+BMA+NAN')` > 0 (standard wwPDB glycan codes present — 09-RESEARCH-sasbdb.md:107-114).
      - Assert cache file `tmp/phase9-demos/cache/SASDPG4_fit2_model1.pdb.gz` exists.
      - Assert `demos.is_cached('sasdpg4')` == True.

    SECTION B — cache-hit path (load_cached_demo): `cmd.delete('sasdpg4')`; `obj = demos.load_cached_demo('sasdpg4')` → 'sasdpg4'; assert `cmd.count_atoms('sasdpg4')` == 4123 (cache round-trips; .pdb.gz read natively — 09-RESEARCH-pipeline.md:519-520). Assert `cmd.count_atoms('sasdpg4 and hetatm')` == 2601 (glycan preserved through cache).

    SECTION C — MemProtMD strip helper (pure, no network): call `setup_state.strip_resn_from_pdb(synthetic_wet, setup_state.STRIP_RESN_MEMPROTMD)` where synthetic_wet has ATOM lines for SOL/NA/CL/DPP/MET + TITLE/CRYST1/MODEL/TER/ENDMDL. Assert SOL/NA/CL removed, DPP/MET + all non-ATOM lines preserved. (The real 3gp6 wet fetch is 7.5MB — too slow/heavy for a smoke; the pure helper test covers the strip logic; the 09-03 human-verify covers the real fetch.)

    SECTION D — load_demo branching: 
      - `demos.load_demo('1znf')` → '1znf' (bundled path unchanged; cache_name field works).
      - `demos.load_demo('sasdpg4')` → 'sasdpg4' (fetched path → load_cached_demo → cache hit from Section A).
      - `cmd.delete('1gzm')` if exists; `demos.load_demo('1gzm')` → None (cache miss — 1gzm not yet fetched; signals caller to fetch). Assert returns None.

    SECTION E — download_large_demo is cmd-free (Pitfall 6 static check): read the demos.py source; assert 'cmd.' does NOT appear inside the `def download_large_demo` function body (only urllib/queue/os/threading). (A programmatic check: `import inspect, biochemeleon.demos as d; src = inspect.getsource(d.download_large_demo); assert 'cmd.' not in src and 'from pymol' not in src`.)

    SECTION F — cache_path_for/is_cached: assert `demos.cache_path_for('1znf')` is None (bundled); `demos.cache_path_for('sasdpg4')` endswith 'SASDPG4_fit2_model1.pdb.gz'; `demos.is_cached('sasdpg4')` == True; `demos.is_cached('1gzm')` == False.

    The smoke uses the staged `tmp/phase9-demos/SASDPG4_fit2_model1.pdb` (already present, 400810 bytes — 09-RESEARCH-sasbdb.md:382). NO real network calls in the smoke (the staged file simulates the worker download output). The real MemProtMD + SASBDB network fetch is exercised at the 09-03 human-verify checkpoint (needs the GUI + real network timing).

    Header docstring: "Pure pymol.cmd.* (NO Qt) — runs headlessly via cmd.exe /c run-conda-pymol.bat -cq. Uses the STAGED SASBDB sample (tmp/phase9-demos/SASDPG4_fit2_model1.pdb) to simulate the worker download; no real network. The real fetch+progress-dialog is verified at the 09-03 human-verify checkpoint."

    After writing, stage to Windows + run headlessly:
    ```
    bash wsl2win_cp.sh
    mkdir -p tmp/bioCHEMeleon/smoke && cp smoke/phase9_smoke.py tmp/bioCHEMeleon/smoke/
    # Also stage the SASBDB sample where the smoke expects it (relative to the staged package):
    mkdir -p tmp/bioCHEMeleon/tmp/phase9-demos && cp tmp/phase9-demos/SASDPG4_fit2_model1.pdb tmp/bioCHEMeleon/tmp/phase9-demos/
    cd tmp/bioCHEMeleon && timeout 120 cmd.exe /c "C:\\src\\run-conda-pymol.bat -cq smoke\\phase9_smoke.py" 2>&1 | tail -60
    ```
    The smoke writes a summary block "PHASE9 SMOKE: N/M PASSED" + sys.exit(0/1).
  </action>
  <verify>python3.6 -m py_compile smoke/phase9_smoke.py. Headless run via cmd.exe /c run-conda-pymol.bat -cq → "PHASE9 SMOKE: N/N PASSED", exit 0. If a check fails, read the tail output, fix the bug (Rule 1), re-run. Common pitfalls: the staged path resolution (use os.path relative to the staged package dir, not the WSL repo root); the .pdb.gz cache write (cmd.save to a to_windows_path-converted path); the 4123-atom count (if off, the strip is wrong — but SASBDB strip=False so no strip runs).</verify>
  <done>smoke/phase9_smoke.py runs headlessly with all checks passing: SASBDB fetch round-trip (4123 atoms, 2601 glycan HETATM, NAG/MAN/BMA/NAN present), cache-hit round-trip, strip helper filters SOL/NA/CL, load_demo branching (bundled + fetched cache-hit + cache-miss-None), download_large_demo cmd-free (Pitfall 6), cache helpers.</done>
</task>

</tasks>

<verification>
- python3.6 -m py_compile biochemeleon/demos.py smoke/phase9_smoke.py.
- Headless smoke (cmd.exe /c run-conda-pymol.bat -cq smoke/phase9_smoke.py) → all checks pass.
- demos.py has NO `from pymol.Qt` (dependency direction intact).
- download_large_demo has NO `cmd.` in its body (Pitfall 6 compliant).
- finalize_large_demo strips MemProtMD via the pure helper (wet file never enters PyMOL); SASBDB strip=False (glycan preserved).
- .pdb.gz cache written by cmd.save (native gzip — exporting.py:912).
- Existing tests still pass (python3.6 -m unittest tests.test_setup_state).
- Pitfall-1 gate package-wide: ZERO. exec_ gate: still 1 (only gui_game.py:303 — demos.py adds NO .exec_()).
</verification>

<success_criteria>
- The cmd layer can fetch (urllib worker), strip (pure helper for MemProtMD), compress+cache (cmd.save .pdb.gz), and load-cached (cmd.load .pdb.gz) — all Qt-free, Pitfall 6 compliant.
- SASBDB demo loads with glycan intact (SC2 foundation).
- MemProtMD strip logic verified (SC1 foundation — the real network fetch is the 09-03 human-verify).
</success_criteria>

<output>
After completion, create `.planning/phases/09-large-demo-fetch-source-attribution/09-02-SUMMARY.md`
</output>
