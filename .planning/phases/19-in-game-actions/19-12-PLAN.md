---
phase: 19-in-game-actions
plan: 12
type: execute
wave: 3
depends_on: ["19-04", "19-05"]
files_modified: [vmd/smoke/phase19_actions_smoke.tcl]
autonomous: true

must_haves:
  truths:
    - "The permanent smoke proves a hint highlights one hidden hider's neighborhood with the PARENTHESIZED GAM-excluding selector WITHOUT scoring it (statuses/remaining unchanged), increments hint_count, and accumulates on a second press"
    - "The smoke proves reveal-one with a forced 1-hider pool reveals that EXACT hider byte-identically to a click-find (user2=1, stored found-rep selection untouched), counts +1, logs the v1-verbatim line, and the last-hider reveal fires the win flow exactly once with the timer frozen"
    - "The smoke proves reveal-all marks every remaining hider with +N counting and one win, and a second reveal-all in the won state is a counter/log/rep NO-OP (state gate)"
    - "The smoke proves found-mgmt toggles and recolors ONLY found reps at the state level (showrep getter round-trip; hidden reps stay shown; R7: a modselect re-assert on an off found rep keeps it off) and the color preference persists into the next round"
    - "The smoke proves the stashed-gs restart keeps hider_count + per_rep keys, builds a NEW molid, rebuilds the registry, and round_reset zeroes both counters"
    - "The smoke proves post-hint cleanup restores the original numreps + atom count with zero sentinels and an empty registry (hint reps die with the molecule)"
    - "PASS=1 x3 SEQUENTIAL with full-log scans 0 ERROR) / 0 bad switch / Exiting normally"
  artifacts:
    - path: "vmd/smoke/phase19_actions_smoke.tcl"
      provides: "the permanent headless ACTION PROOF: 6 assertion stages (hint / reveal-one+win+no-ops / reveal-all / found-mgmt+R7+color-persistence / restart replay / post-hint cleanup) over the 19-01/02/04/05 seams"
      min_lines: 380
      contains: "reveal_one"
  key_links:
    - from: "vmd/smoke/phase19_actions_smoke.tcl"
      to: "vmd/lib/game.tcl hint/reveal_one/reveal_all/restart/cleanup (19-05 explicit-gs seam)"
      via: "direct lib calls with the explicit game_state arg (confirm-free per R3)"
      pattern: "game::(hint|reveal_one|reveal_all|restart|cleanup)"
    - from: "vmd/smoke/phase19_actions_smoke.tcl"
      to: "vmd/lib/hiders.tcl set_found_visible/set_found_color/mark_found_visual (19-04)"
      via: "found-mgmt state-level round-trips + the all-but-one-found marking idiom"
      pattern: "set_found_(visible|color)"
    - from: "vmd/smoke/phase19_actions_smoke.tcl"
      to: "vmd/lib/registry.tcl hidden_indices/found_indices (19-01)"
      via: "runtime index derivation - NEVER literal hider indices"
      pattern: "registry::(hidden|found)_indices"
    - from: "vmd/smoke/phase19_actions_smoke.tcl"
      to: "vmd/lib/game_logic.tcl reveal_count/hint_count/log_lines/state (19-02)"
      via: "counter + log-model asserts; the pure state machine drive per round"
      pattern: "game_logic::(reveal_count|hint_count|log_lines)"
---

<objective>
Write the PERMANENT headless action proof `vmd/smoke/phase19_actions_smoke.tcl` — the integration §7 test-table realized as one smoke: per-action sections for hint (GAME-05), reveal-one (GAME-06), reveal-all (GAME-07), found-mgmt (GAME-08/DIFF-04), restart (GAME-10), and post-hint cleanup (BTN-06), reusing 19-05's all-but-one-found determinism idiom and 19-04's probe assertions as permanent regression asserts.

Purpose: Phase 19's actions currently have only ephemeral probe evidence (tmp/, gitignored). This plan converts it into the standing regression gate the full-suite gate (19-15) runs forever.
Output: one new smoke file, PASS=1 x3 sequential, zero edits to any other file.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-in-game-actions/19-RESEARCH-integration.md
@.planning/phases/19-in-game-actions/19-RESEARCH-mechanisms.md
@.planning/phases/19-in-game-actions/19-01-SUMMARY.md
@.planning/phases/19-in-game-actions/19-02-SUMMARY.md
@.planning/phases/19-in-game-actions/19-04-SUMMARY.md
@.planning/phases/19-in-game-actions/19-05-SUMMARY.md
@vmd/smoke/phase16_restart_smoke.tcl

Load-bearing research: integration §7 (the per-action test table — THIS plan's primary spec: exact assert targets per action + the 4 determinism tricks), §6 (R2 state gate, R4 PRNG discipline, R5 sequential runs). Mechanisms §1 A/B/C (the probed read-back forms: parenthesized within selector, showrep molid-first arg order + getter form, ColorID 3 orange / 7 green, molinfo combined-braces read-back) and §3 V-table (V1-V13). 19-05-SUMMARY carries the all-but-one-found idiom + the stub-callback + state-machine drive patterns (round_reset -> begin_countdown -> countdown_tick x4 -> begin_play); 19-04-SUMMARY carries the GAM-arithmetic discriminator + R7 evidence. phase16_restart_smoke.tcl is the STRUCTURAL template (failures list + _bail + catch-wrapped stages + BCHM_SMOKE_RESULT marker + source block + staging run header). Tcl 8.5 ONLY; brace expr; $sel delete discipline; NO mouse commands; NO Tk.
</context>

<tasks>

<task type="auto">
  <name>Task 1: smoke harness + hint/reveal-one/reveal-all stages</name>
  <files>vmd/smoke/phase19_actions_smoke.tcl</files>
  <action>
Write the new smoke modeled 1:1 on phase16_restart_smoke.tcl's harness (failures list + `_bail` appending to the GLOBAL list; every stage catch-wrapped; `BCHM_SMOKE_RESULT PASS=1 FAIL=none` ONLY when failures is empty; header documents staging + run command + the marker-never-$? rule + Tcl 8.5). Source block = the restart smoke's lib order PLUS game_logic (pure) sourced after generators: setup_state -> registry -> generators -> game_logic -> rep_tiers -> demos -> backup -> mutation -> hiders -> game. Register stub callbacks once: `proc ::p19cb_log {line} { lappend ::P_LOG $line }`, `proc ::p19cb_rem {} { incr ::P_REM }`, `proc ::p19cb_win {elapsed hc} { lappend ::P_WIN [list $elapsed $hc] }`, then `game::set_callbacks ::p19cb_log ::p19cb_rem ::p19cb_win` (mirror 19-05's probe registration form). Per round, drive the PURE state machine exactly as 19-05's probe proved: `game_logic::round_reset` -> `begin_countdown` -> `countdown_tick` x4 -> `begin_play`, then assert `state` eq playing. ALL hider indices derived at runtime via `registry::hidden_indices` / the canonical selector `resname GAM and beta < 0` — NEVER literal indices, NEVER literal PRNG draws. Record each round's rep base (`numreps` right after start_game) and assert DELTAS against it. Use load_demo 1znf + 4 hiders + explicit `per_rep [dict create VDW 4]` (the 19-04/19-05 probe round shape).

STAGE H (hint): start_game 4 VDW -> playing. `game::hint $gs` -> assert: numreps == base+1; last rep read-back via combined-braces molinfo (`{rep h} {selection h} {color h}`) equals style `VDW`, selection EXACTLY `(within 5.0 of (index N)) and not resname GAM` with N parsed from the string (`regexp -- {index ([0-9]+)} ...`) and N in hidden_indices, color `ColorID 3`; GAM-exclusion arithmetic (19-04's discriminator): num(the rep's selection) + num(`resname GAM and within 5.0 of (index N)`) == num(`within 5.0 of (index N)`); ALL 4 statuses still hidden (`found_indices` == {}), count_remaining still 4; `hint_count` == 1; `log_lines` end == `Hint: highlighted neighbors of one hider.`; ::P_REM unchanged. Second `game::hint $gs` -> numreps == base+2, hint_count == 2 (accumulate).

STAGE R1 (reveal-one, all-but-one-found): same live round. hidden = hidden_indices (4); mark the FIRST 3 found via the registry-side idiom (`registry::mark_found` + `hiders::mark_found_visual $gm $idx` for each — 19-05's probe pattern), leaving exactly 1 hidden. `game::reveal_one $gs` -> assert: the last index is now found (status_of eq HIDER_STATUS_FOUND); its `atomselect index N get user2` == 1.0; the single tier's found-rep selection read-back STILL equals the stored literal (tier_reps dict size 1 -> `lassign $pair hname fname hself fself`; `mol repindex` fname -> molinfo selection == $fself — byte-identity to a click-find); count_remaining == 0; `reveal_count` == 1 and `hint_count` STILL 2; `log_lines` end == `Revealed one! 0 remaining`; ::P_REM incremented; `state` eq won; ::P_WIN grew by EXACTLY 1; `timer_elapsed` returns the SAME value on two consecutive calls (frozen). Then R2 no-ops in the won state: `hint`, `reveal_one`, `reveal_all` each -> counters unchanged, log_lines length unchanged, numreps unchanged.

STAGE RA (reveal-all): `start_game` again (the 16-13 guard auto-cleans the won round — new molid) -> playing. `game::reveal_all $gs` -> assert: every hider user2 == 1.0 (`found_indices` length == 4, count_remaining == 0); `reveal_count` == 4 (the +N form — NOT +1); `log_lines` end == `Revealed all 4 hiders. Game over.`; state won; ::P_WIN grew by exactly 1. Second `game::reveal_all $gs` -> NO-OP via the state gate: reveal_count still 4, log length unchanged, numreps unchanged.
  </action>
  <verify>
Staging: `mkdir -p tmp/p19-12 && cp -r vmd tmp/p19-12/` then `bash -ic 'cd tmp/p19-12 && vmd -dispdev text -e vmd/smoke/phase19_actions_smoke.tcl -eofexit < /dev/null'` -> parse BCHM_SMOKE_RESULT PASS=1 from the log (NEVER $?); every stage-H/R1/RA assert tag present in the log; full-log scan 0 `ERROR)` / 0 `bad switch` + `Exiting normally`. VMD runs SEQUENTIAL (R5: shared $env(TEMP)/biochemeleon_game.pdb).
  </verify>
  <done>Harness + stages H/R1/RA written and green on fresh staging with every assert tag in the log.</done>
</task>

<task type="auto">
  <name>Task 2: found-mgmt/restart/cleanup stages + x3 sequential gate + commit</name>
  <files>vmd/smoke/phase19_actions_smoke.tcl</files>
  <action>
STAGE F (found-mgmt, fresh round): start_game 4 VDW -> playing -> `game::reveal_all $gs` (fast all-found; counters now show reveal_count 4 — fine). Asserts: `hiders::set_found_visible $gm 0` -> EVERY found rep `mol showrep $gm $fidx` (getter, molid FIRST — probe3 arg order) == 0 while EVERY hidden rep getter == 1 (the narrowed never-hide rule: only found reps toggled); `set_found_visible $gm 1` -> getters == 1. R7 permanent assert: `set_found_visible $gm 0` -> `hiders::mark_found_visual $gm <one found idx>` (idempotent re-assert on an OFF rep) -> showstate STILL 0 -> restore `set_found_visible $gm 1`. `hiders::set_found_color $gm 11` -> every found rep color read-back == `ColorID 11` and `hiders::found_colorid` == 11. Persistence: `set gs2 [game::restart $gs]` -> the NEW round's found-rep color read-back == `ColorID 11` (add_hider_reps consumed the preference; NOT reset).

STAGE RS (restart replay): fresh `start_game` (guard cleans stage F's round) -> playing -> `game::hint $gs` + `game::reveal_one $gs` (counters {1,1}, 1 found, 2 hint reps). Capture: hider_count, per_rep keys, old molid, old atom count. `set gs3 [game::restart $gs]` (the stashed-gs replay — exactly what the Restart button's handler calls; the handler-side bridge/timer ordering is the GUI checkpoint's) -> assert: gs3 is a dict with a game_molid DIFFERENT from and GREATER than the old (monotonic); old molid dead (`molinfo $old get numatoms` errors); `[dict get $gs3 hider_count]` == captured; `[dict keys [dict get $gs3 per_rep]]` == {VDW}; registry rebuilt: count_hiders == 4, count_remaining == 4, found_indices == {}; new atom count == old atom count (same-count replay); numreps == fresh base + 8 (hint reps died with the old molecule). Counters: lib restart does NOT zero them (zeroing lives in the GUI's start_round round_reset) — assert round_reset's effect DIRECTLY (integration §7 GAME-10 row): `game_logic::round_reset` -> `list [reveal_count] [hint_count]` == `0 0` and `state` eq idle.

STAGE C (post-hint cleanup): fresh `load_demo 1znf` -> record orig_atoms + orig_numreps on the FRESH target -> start_game 4 VDW -> playing -> hint x2 + reveal_one -> `set restored [game::cleanup $gs]` -> assert: molinfo $restored numreps == orig_numreps (hint reps die with the whole-molecule restore), atoms == orig_atoms, sentinel scan `resname GAM and beta < 0` num == 0, `registry::count_hiders` == 0. The smoke ENDS here (clean session).

Static hygiene on the new file: no `mouse mode` (BTN-06 locked contract), no `grab set`, no Tk commands, Tcl 8.5 only (no lmap/try/tailcall — comments included), brace expr, `$sel delete` everywhere, no literal hider indices or PRNG draws anywhere (grep your own file to prove it). Commit (single, plan granularity): `test(19-12): add phase19 actions smoke (hint/reveal/found-mgmt/restart/cleanup)`.
  </action>
  <verify>
Full gate: re-run the staging smoke THREE times SEQUENTIAL (fresh `vmd -dispdev text` process each; same staging dir) -> PASS=1 all three; every stage's assert tags in each log; full-log scan per run 0 `ERROR)` / 0 `bad switch` + `Exiting normally`. Static: `grep -cE "mouse mode|grab set|lmap|tailcall|coroutine" vmd/smoke/phase19_actions_smoke.tcl` == 0. Also re-run `vmd/tests/test_registry.test` + `vmd/tests/test_game_logic.test` on the staging (BCHM_TEST_RESULT Failed=0) as the seam-integration check.
  </verify>
  <done>All 6 stages green x3 sequential; the smoke is committed as the permanent action regression gate.</done>
</task>

</tasks>

<verification>
- phase19_actions_smoke.tcl PASS=1 x3 SEQUENTIAL on fresh staging; full-log scans clean (0 `ERROR)` / 0 `bad switch`, `Exiting normally`).
- Every integration §7 headless target asserted: hint rep+selector+no-score+count+accumulate; reveal-one exact-index byte-identity+win-once+frozen timer; reveal-all +N+idempotent no-op; found-mgmt showrep roundtrip+recolor read-back+R7+cross-round color; restart same-count/per_rep/new-molid/registry-rebuild/counter-zeroing; post-hint cleanup restore.
- No other file modified (`git status` clean except the new smoke). 8.6-idiom/mouse-mode/grab greps 0 on the new file.
- Single test(19-12) commit.
</verification>

<success_criteria>
The phase's actions have a permanent headless proof: any future regression in hint/reveal/found-mgmt/restart/cleanup lib behavior fails this smoke with a named stage tag, and the full-suite gate (19-15) can run it as one more PASS=1 line.
</success_criteria>

<output>
After completion, create `.planning/phases/19-in-game-actions/19-12-SUMMARY.md` (carry the stage-tag list + the x3 evidence + any assert-shape adjustments discovered during authoring)
</output>
