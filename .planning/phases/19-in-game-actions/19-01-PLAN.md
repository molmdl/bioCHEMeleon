---
phase: 19-in-game-actions
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [vmd/lib/registry.tcl, vmd/tests/test_registry.test]
autonomous: true

must_haves:
  truths:
    - "The composition root can enumerate WHICH registered hider indices are still hidden (the reveal-one random pool + hint candidate filter's data source)"
    - "The composition root can enumerate WHICH registered hider indices are found (found-mgmt counts/logs + smoke asserts)"
    - "Enumeration is order-stable (registry insertion order) and consistent with the existing counters"
  artifacts:
    - path: "vmd/lib/registry.tcl"
      provides: "hidden_indices {} + found_indices {} pure procs (status-keyed enumeration over _records)"
      min_lines: 260
      contains: "hidden_indices"
    - path: "vmd/tests/test_registry.test"
      provides: "tcltest cases pinning empty/mixed/all-found/insertion-order enumeration + counter consistency"
      min_lines: 360
      contains: "hidden_indices"
  key_links:
    - from: "vmd/lib/registry.tcl hidden_indices/found_indices"
      to: "HIDER_STATUS_HIDDEN / HIDER_STATUS_FOUND constants"
      via: "dict for over _records, lappend when status eq the constant (the remaining_by_rep pattern)"
      pattern: "eq \\$::biochemeleon::registry::HIDER_STATUS_(HIDDEN|FOUND)"
    - from: "vmd/tests/test_registry.test enumeration cases"
      to: "registry::count_remaining / count_hiders"
      via: "consistency asserts (llength hidden_indices == count_remaining; hidden+found == count_hiders)"
      pattern: "count_remaining"
---

<objective>
TDD the ONE new pure seam Phase 19 needs: `registry::hidden_indices` / `registry::found_indices` — status-keyed index enumeration over the registry's `_records`. These are the data sources for reveal-one's random pool (19-05), hint's candidate filter (19-05), and found-mgmt counts (19-04 probe / 19-12 smokes).

Purpose: research gap §4.1 — the registry can COUNT hidden/found but cannot ENUMERATE them; `_records` is namespace-private with no accessor. Pure layer, trivially TDD-able.
Output: 2 new exported procs + extended test_registry.test, all pre-existing cases still green.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-in-game-actions/19-RESEARCH-integration.md
@.planning/phases/19-in-game-actions/19-RESEARCH-mechanisms.md
@vmd/lib/registry.tcl
@vmd/tests/test_registry.test

Load-bearing research: integration §1.3 (registry proc inventory — NO enumeration accessor exists today), §4.1 (the exact gap + proposed shapes), §7 (test strategy). Mechanisms §1B "The missing enumeration seam" (why game.tcl cannot enumerate without it; the v1 registry.all()+filter precedent). TCL 8.5 ONLY (no lmap/dict-get-default; brace expr; one `variable` per line).
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — failing enumeration cases in test_registry.test</name>
  <files>vmd/tests/test_registry.test</files>
  <action>
Add a `# ---- Phase 19 additions: status enumeration (hidden_indices / found_indices) ----` section BEFORE the final BCHM_TEST_RESULT marker block, mirroring the file's existing style (each body self-isolates via reconstruct_from_sentinels or reset; `[list apply {{} { return {...} }}]` fake-DI prefix). Cases (exact -result pins):
1. `hidden_indices_empty_registry`: `reset` then `hidden_indices` → `{}`.
2. `hidden_indices_all_hidden`: reconstruct {5 10 15} → `hidden_indices` → `{5 10 15}`.
3. `hidden_indices_mixed`: reconstruct {5 10 15} then `mark_found 10` → `hidden_indices` → `{5 15}`.
4. `hidden_indices_all_found`: reconstruct {5 6} then mark both found → `hidden_indices` → `{}`.
5. `hidden_indices_insertion_order`: reconstruct {3 1 2} → `hidden_indices` → `{3 1 2}` (dict iteration order = insertion order; NOT sorted — this pins the order-stability contract research §4.1 requires).
6. `found_indices_mirror`: reconstruct {5 10 15} then `mark_found 10` → `list [found_indices] [hidden_indices]` → `{10 {5 15}}`.
7. `found_indices_empty_registry`: `reset` then `found_indices` → `{}`.
8. `enumeration_counter_consistency`: reconstruct {5 6 7} then `mark_found 6` → `list [expr {[llength [hidden_indices]] eq [count_remaining]}] [expr {[llength [found_indices]] + [llength [hidden_indices]] eq [count_hiders]}]` → `{1 1}` (the eq-on-int-comparison pattern already used at line ~115).
9. `enumeration_procs_exported`: `set exp [namespace eval ::biochemeleon::game_logic { namespace export }]` is WRONG namespace — use `namespace eval ::biochemeleon::registry { namespace export }`; assert `lsearch -exact` finds hidden_indices AND found_indices → `{1 1}`.
Run the suite — the new cases MUST FAIL (unknown command `hidden_indices`), all pre-existing cases stay green. Do NOT implement anything in this task.
  </action>
  <verify>
Headless suite from a FRESH Windows-visible staging copy (e.g. `tmp/p19-01/`, a copy of the vmd/ tree): `bash -ic 'cd tmp/p19-01 && vmd -dispdev text -e vmd/tests/test_registry.test -eofexit < /dev/null'` → parse the `BCHM_TEST_RESULT` marker (NEVER $?): the 9 new cases FAILED (unknown-command errors), Failed>0, and every pre-existing case Passed. Full-log scan: 0 `ERROR)`-class crashes beyond the expected tcltest error results AND 0 `bad switch`; `Exiting normally` present. VMD runs are SEQUENTIAL (shared $env(TEMP)/biochemeleon_game.pdb forbids parallel VMD — R5).
  </verify>
  <done>9 new enumeration cases exist and FAIL for the right reason (unknown command), pre-existing suite green. Commit: `test(19-01): add failing hidden/found enumeration cases`.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement hidden_indices/found_indices in registry.tcl</name>
  <files>vmd/lib/registry.tcl</files>
  <action>
1. Add the two procs AFTER `remaining_by_rep` (before the Phase 17.1 section comment), with header comments stating: purpose (the ONE new pure seam Phase 19 needs — reveal-one's random pool + hint's candidate filter + found-mgmt counts), the order-stability contract (dict for iterates _records in insertion order — the list is NOT sorted), and that they are pure readers (never mutate _records). Shape per research §4.1:
```tcl
proc ::biochemeleon::registry::hidden_indices {} {
    variable _records
    set out [list]
    dict for {k rec} $_records {
        if {[dict get $rec status] eq $::biochemeleon::registry::HIDER_STATUS_HIDDEN} {
            lappend out $k
        }
    }
    return $out
}
```
`found_indices` is identical with `HIDER_STATUS_FOUND`. Use the FULLY-QUALIFIED constant refs (`$::biochemeleon::registry::HIDER_STATUS_HIDDEN`) exactly as `count_remaining`/`remaining_by_rep` do (the file's established pattern).
2. Extend the `namespace export` line (line ~29): append ` hidden_indices found_indices`.
3. Tcl 8.5 only; no new `variable` declarations beyond `variable _records` inside the procs; comment hygiene (no 8.6-idiom words anywhere, comments included).
Do NOT touch any existing proc, `_resid_block`, or the DI contract.
  </action>
  <verify>
Same headless suite command as Task 1: `BCHM_TEST_RESULT` Failed=0 (all pre-existing + the 9 new cases green). Full-log scan 0 `ERROR)` / 0 `bad switch`, `Exiting normally`. Static gate: `grep -rnE "\blmap\b|\btry\b|\bthrow\b|\btailcall\b|\bcoroutine\b|\byield\b|\bfinally\b" vmd/lib/ vmd/gui/` → 0 matches. Also `tclsh vmd/lib/registry.tcl` loads clean (pure layer).
  </verify>
  <done>hidden_indices/found_indices are exported pure procs; empty/mixed/all-found/order/consistency cases all green alongside every pre-existing case. Commit: `feat(19-01): add registry hidden_indices/found_indices enumeration`.</done>
</task>

</tasks>

<verification>
- BCHM_TEST_RESULT Failed=0 (all pre-existing test_registry.test cases + 9 new) from the headless suite run on the fresh staging copy.
- Full-log scan 0 `ERROR)` / 0 `bad switch`; `Exiting normally` in the log.
- 8.6-idiom gate 0 matches over vmd/lib/ vmd/gui/.
- RED→GREEN commit pair exists (test(19-01) then feat(19-01)).
</verification>

<success_criteria>
The registry can enumerate hidden and found hider indices in insertion order, pinned by tcltest and consistent with count_remaining/count_hiders. 19-05's reveal/hint procs and 19-12's smoke batch can consume `registry::hidden_indices`/`found_indices` without any further registry work.
</success_criteria>

<output>
After completion, create `.planning/phases/19-in-game-actions/19-01-SUMMARY.md`
</output>
