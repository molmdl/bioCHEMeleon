---
phase: 19-in-game-actions
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified: [vmd/lib/game_logic.tcl, vmd/tests/test_game_logic.test]
autonomous: true

must_haves:
  truths:
    - "Each reveal USE increments a round-scoped counter readable via a 0-arg getter (+1 for reveal-one, +N for reveal-all); click-finds do NOT touch it"
    - "Each hint press increments its own round-scoped counter (DIFF-02 forward-compat for Phase 22's win screen)"
    - "A new round (round_reset) zeroes BOTH counters"
    - "The log model formats the three v1-verbatim action lines: revealed / revealed_all / hint"
  artifacts:
    - path: "vmd/lib/game_logic.tcl"
      provides: "reveal_count + hint_count vars, 0-arg getters, increment_reveal/increment_hint (default 1), round_reset zeroing, 3 new log_append kinds"
      min_lines: 290
      contains: "increment_reveal"
    - path: "vmd/tests/test_game_logic.test"
      provides: "tcltest cases pinning counter lifecycle, +N increment, round_reset zeroing, and the 3 new kind formats"
      min_lines: 195
      contains: "increment_reveal"
  key_links:
    - from: "vmd/lib/game_logic.tcl round_reset"
      to: "reveal_count / hint_count vars"
      via: "both zeroed in the documented whole-model reset (one variable per line - the 14-04 name-VALUE trap)"
      pattern: "set reveal_count 0"
    - from: "vmd/lib/game_logic.tcl log_append switch"
      to: "the 3 new kinds"
      via: "new switch branches BEFORE the default (unknown-kind error discipline stays)"
      pattern: "revealed_all"
---

<objective>
TDD the DIFF-01 counter layer + the three new log kinds into the PURE round model: `reveal_count`/`hint_count` vars with 0-arg getters and incrementers (default +1, explicit N for reveal-all), both zeroed by `round_reset`, and `log_append` kinds `revealed` / `revealed_all` / `hint` with v1-verbatim lines.

Purpose: v1 tracked both counters from Phase 6; DIFF-01 needs reveals, Phase 22's DIFF-02 win screen needs hints - build both now (symmetric, one round_reset) so Phase 22 needs no game_logic seam change. The log kinds give 19-05's reveal/hint procs their v1-parity lines without string formatting in game.tcl.
Output: extended game_logic.tcl + test_game_logic.test, all pre-existing cases still green.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-in-game-actions/19-RESEARCH-integration.md
@.planning/phases/19-in-game-actions/19-RESEARCH-mechanisms.md
@vmd/lib/game_logic.tcl
@vmd/tests/test_game_logic.test

Load-bearing research: integration section 1.2 (game_logic inventory - round_reset at lines 86-98 is "the natural zeroing point"), section 4.2 (gap table: vars/getters/incrementers/kinds + the one-variable-per-line 14-04 trap). Mechanisms section 1B (DIFF-01 counter placement rationale - PULL model matching remaining_cb; reveal-all adds N NOT +1, the 06-01 v1 key-decision; kind names + verbatim lines). PINNED kind names: `revealed` (NOT `revealed_one`), `revealed_all`, `hint`. Tcl 8.5 ONLY.
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED - failing counter + log-kind cases in test_game_logic.test</name>
  <files>vmd/tests/test_game_logic.test</files>
  <action>
Add a `# ---- Phase 19 additions: DIFF-01 counters + action log kinds ----` section BEFORE the final BCHM_TEST_RESULT marker block, mirroring the file's style (every body starts with `round_reset` for isolation). Cases (exact -result pins):
1. `counters_start_zero`: after round_reset, `list [reveal_count] [hint_count]` returns `0 0`.
2. `increment_reveal_default`: round_reset; `increment_reveal`; `increment_reveal`; then `reveal_count` returns `2` (default step is 1).
3. `increment_reveal_explicit_n`: round_reset; `increment_reveal 3`; then `reveal_count` returns `3` (the reveal-all +N form - v1 adds len(hidden) in ONE call, NOT +1-per-action).
4. `increment_hint_counts_presses`: round_reset; `increment_hint`; `increment_hint`; then `hint_count` returns `2`.
5. `round_reset_zeroes_counters`: round_reset; `increment_reveal 2`; `increment_hint 1`; `round_reset`; then `list [reveal_count] [hint_count]` returns `0 0`.
6. `log_append_revealed_format`: round_reset; `log_append revealed 3` returns the exact string `Revealed one! 3 remaining` (v1-verbatim, mirrors the existing found/miss tests).
7. `log_append_revealed_all_format`: round_reset; `log_append revealed_all 2` returns `Revealed all 2 hiders. Game over.` (v1-verbatim: reveal-all win line).
8. `log_append_hint_format`: round_reset; `log_append hint ""` returns `Hint: highlighted neighbors of one hider.` (v1-verbatim; the hint kind IGNORES its msg arg).
9. `log_lines_accumulate_new_kinds`: round_reset; `log_append hint ""`; `log_append revealed 2`; `log_append revealed_all 2`; then `list [llength [log_lines]] [lindex [log_lines] end]` returns `3 {Revealed all 2 hiders. Game over.}` (kinds accumulate into the same log_lines list).
10. `counter_procs_exported`: `set exp [namespace eval ::biochemeleon::game_logic { namespace export }]`, then assert lsearch -exact finds reveal_count, hint_count, increment_reveal, increment_hint; `list` of four 0/1s returns `1 1 1 1`.
Run the suite - the new cases MUST FAIL (unknown command `reveal_count` / unknown kind), all pre-existing cases stay green. Do NOT implement anything in this task.
  </action>
  <verify>
Headless suite from a FRESH Windows-visible staging copy (e.g. `tmp/p19-02/`, a copy of the vmd/ tree): `bash -ic 'cd tmp/p19-02 && vmd -dispdev text -e vmd/tests/test_game_logic.test -eofexit < /dev/null'` - parse the `BCHM_TEST_RESULT` marker (NEVER the $?): the 10 new cases FAILED (unknown command / bad kind), Failed > 0, every pre-existing case Passed. Full-log scan: 0 `ERROR)` crashes beyond the expected tcltest error results AND 0 `bad switch`; `Exiting normally` present. VMD runs are SEQUENTIAL (shared $env(TEMP)/biochemeleon_game.pdb forbids parallel VMD - R5).
  </verify>
  <done>10 new cases exist and FAIL for the right reason, pre-existing suite green. Commit: `test(19-02): add failing counter/log-kind cases`.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN - implement counters + log kinds in game_logic.tcl</name>
  <files>vmd/lib/game_logic.tcl</files>
  <action>
1. Add the two counter vars AFTER the log_lines var (one `variable` per line - the 14-04 name-VALUE trap: `variable reveal_count 0` then `variable hint_count 0` on SEPARATE lines), with a comment stating the DIFF-01/Phase-22 symmetry rationale.
2. In `round_reset`: add `variable reveal_count` and `variable hint_count` declarations, then `set reveal_count 0` and `set hint_count 0` alongside the existing five zeroing lines (round_reset stays the documented whole-model reset).
3. Add four procs (place them near the existing `state` getter, mirroring its shape - proc name == var name is the established game_logic pattern):
   - `proc ::biochemeleon::game_logic::reveal_count {}` - `variable reveal_count` + return the var (0-arg getter, state precedent).
   - `proc ::biochemeleon::game_logic::hint_count {}` - same shape for hint_count.
   - `proc ::biochemeleon::game_logic::increment_reveal {{n 1}}` - `variable reveal_count`; `incr reveal_count $n` (default 1; reveal-all passes N).
   - `proc ::biochemeleon::game_logic::increment_hint {{n 1}}` - same shape for hint_count.
   Comment the incrementers: DIFF-01 - the reveal counter counts reveal USES; click-finds never call it (v1 game.py only increments inside the reveal paths).
4. In `log_append`'s switch: add three NEW branches BEFORE the `default` error branch:
   - `revealed` - `set line "Revealed one! $msg remaining"` (msg = remaining count).
   - `revealed_all` - `set line "Revealed all $msg hiders. Game over."` (msg = count revealed).
   - `hint` - `set line "Hint: highlighted neighbors of one hider."` (msg is IGNORED; callers pass "").
   The `lappend log_lines $line` + `return $line` tail is shared (the existing structure); the `default` unknown-kind error branch stays untouched.
5. Extend the namespace export continuation (line ~79-81): append the four proc names.
6. Update the header LOG MODEL comment table (lines ~46-55): add rows for the three new kinds (kind, msg meaning, v1-verbatim line).
Tcl 8.5 only; comment hygiene (no 8.6-idiom words anywhere, comments included). Do NOT touch the state machine, timer, or existing log kinds.
  </action>
  <verify>
Same headless suite command as Task 1: `BCHM_TEST_RESULT` Failed=0 (all pre-existing + the 10 new cases green). Full-log scan 0 `ERROR)` / 0 `bad switch`, `Exiting normally`. Static gate: `grep -rnE "\blmap\b|\btry\b|\bthrow\b|\btailcall\b|\bcoroutine\b|\byield\b|\bfinally\b" vmd/lib/ vmd/gui/` returns 0 matches. Also `tclsh vmd/lib/game_logic.tcl` loads clean (pure layer).
  </verify>
  <done>reveal_count/hint_count getters + incrementers are exported; round_reset zeroes both; the three log kinds produce the v1-verbatim lines. Commit: `feat(19-02): add reveal/hint counters + action log kinds to game_logic`.</done>
</task>

</tasks>

<verification>
- BCHM_TEST_RESULT Failed=0 (all pre-existing test_game_logic.test cases + 10 new) from the headless suite run on the fresh staging copy.
- Full-log scan 0 `ERROR)` / 0 `bad switch`; `Exiting normally` in the log.
- 8.6-idiom gate 0 matches over vmd/lib/ vmd/gui/.
- RED-GREEN commit pair exists (test(19-02) then feat(19-02)).
</verification>

<success_criteria>
The pure round model owns the DIFF-01 counter layer (reveal uses counted +N per reveal-all, hints counted separately, both reset per round) and the three v1-verbatim action log lines. 19-05's reveal/hint procs and Phase 22's DIFF-02 win screen can consume them without further game_logic work.
</success_criteria>

<output>
After completion, create `.planning/phases/19-in-game-actions/19-02-SUMMARY.md`
</output>
