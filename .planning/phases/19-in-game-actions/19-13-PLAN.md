---
phase: 19-in-game-actions
plan: 13
type: execute
wave: 4
depends_on: ["19-08"]
files_modified: [vmd/gui/setup_tab.tcl]
autonomous: true

must_haves:
  truths:
    - "A player looking at the Setup tab's Actions row can read, in the panel itself, that Reset restores the setup FIELDS only and that hiders in an active game are not affected (with Cleanup model pointed to on the Game tab) — the 16-12 expectation mismatch is resolved by in-panel documentation"
    - "Reset's BEHAVIOR is byte-unchanged: do_reset still applies DEFAULTS to the widgets only — no cleanup coupling, no new confirm dialog, BTN-01's meaning preserved"
    - "The hint text is accurate post-19-08: the Cleanup model button it points to exists on the Game tab"
  artifacts:
    - path: "vmd/gui/setup_tab.tcl"
      provides: "static ttk::label hint inside build_actions (above the button row) + updated build_actions/do_reset comments documenting the fields-only semantics (integration §8.7 minimal resolution)"
      min_lines: 695
      contains: "setup fields only"
  key_links:
    - from: "build_actions hint label"
      to: "the 16-12 registered defect (Setup-Reset expectation mismatch)"
      via: "the pinned clarification copy — references the Game tab's Cleanup model button (19-08)"
      pattern: "setup fields only"
    - from: "do_reset header comment"
      to: "BTN-01 fields-only semantics"
      via: "comment states Reset never touches an active game (the 8.7 decision: NO auto-cleanup coupling)"
      pattern: "BTN-01"
---

<objective>
Resolve the 16-12 registered defect "Setup-tab Reset clears setup fields only (hiders remain — expectation mismatch)" with the integration §8.7 MINIMAL fix: a static in-panel hint label documenting Reset's fields-only semantics (plus comment updates). NO behavior change, NO cleanup coupling, NO new dialogs, NO tooltip helper library.

Purpose: the defect is an EXPECTATION mismatch — the minimal, zero-risk resolution is documentation at the point of confusion. Auto-coupling Reset to cleanup would change BTN-01's meaning (locked decision).
Output: extended setup_tab.tcl (~10 lines), headless-load-clean, verified via the standing phase14_gui_smoke load gate (justified below — no new smoke).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-in-game-actions/19-RESEARCH-integration.md
@.planning/phases/19-in-game-actions/19-08-SUMMARY.md
@vmd/gui/setup_tab.tcl
@vmd/smoke/phase14_gui_smoke.tcl

Load-bearing research: integration §8.7 (the Setup-Reset expectation mismatch — recommendation: minimal, tooltip/label semantics or a note; auto-cleanup coupling REJECTED because it changes BTN-01's meaning) and §5's setup_tab row (W3+, optional). vmd/AGENTS.md Phase-19 note (the defect registration). 19-08-SUMMARY pins the Game tab's "Cleanup model" button label — the hint text must match it verbatim. setup_tab.tcl is free of batch-A/B conflicts (19-08/09 own game_tab.tcl, 19-10 owns dialog.tcl); anchor on PROC NAMES (build_actions, do_reset), never line numbers. Tcl 8.5 only; comments stay 8.6-idiom-gate-clean; no `grab set`; no ttk::combobox.
</context>

<tasks>

<task type="auto">
  <name>Task 1: in-panel Reset-semantics hint + comment updates (behavior untouched)</name>
  <files>vmd/gui/setup_tab.tcl</files>
  <action>
1. In `build_actions`: BEFORE the existing `pack $f.reset $f.random ...` line, add a static clarification label that packs as the frame's TOP row (pack it first with `-side top` so the button row sits below it):
```tcl
    # -- Reset-semantics hint (16-12 defect resolution, Phase 19) -----------
    # The 16-12 GUI session registered: "Setup-tab Reset clears setup fields
    # only -- hiders remain (expectation mismatch)". Resolution per the
    # integration 8.7 decision: document the semantics IN THE PANEL rather
    # than change them -- auto-coupling Reset to game cleanup would change
    # BTN-01's meaning (Reset = restore DEFAULT form state). do_reset's
    # behavior is UNCHANGED by this plan.
    ttk::label $f.hint -text \
        {Reset restores the setup fields only. Hiders in an active game are not affected -- use Cleanup model (Game tab).} \
        -wraplength 360 -justify left
    pack $f.hint -side top -anchor w -pady {0 2}
```
   Then leave the existing five button creations and the existing pack line EXACTLY as they are (the buttons pack `-side left` below the hint row).
2. Update `do_reset`'s header comment: `# do_reset -- restore the DEFAULTS state to all widgets (BTN-01). Fields only -- an active game is deliberately NOT touched (the 16-12 expectation-mismatch resolution documents this in the panel; cleanup stays a separate explicit action on the Game tab).`
3. Update `build_actions`'s header comment block: add one line stating BTN-01 is documented fields-only by the in-panel hint (16-12 resolution).
4. Do NOT: touch do_reset's body, add a tk_messageBox to do_reset, add a tooltip helper, touch any other proc, change any button label. Comment hygiene: no 8.6-idiom words (comments included), no `grab set`, no lowercase `mouse mode `/`vwait` literals.
   </action>
   <verify>
Static: `grep -c "setup fields only" vmd/gui/setup_tab.tcl` == 1; `git diff vmd/gui/setup_tab.tcl` shows ONLY the label block + the two comment updates (do_reset body byte-identical); `grep -rnE "grab set" vmd/gui/` == 0; 8.6-idiom grep over vmd/lib/ vmd/gui/ == 0. Headless load-gate on a FRESH staging copy (`tmp/p19-13/`, copy of vmd/): `bash -ic 'cd tmp/p19-13 && vmd -dispdev text -e vmd/smoke/phase14_gui_smoke.tcl -eofexit < /dev/null'` -> `BCHM_SMOKE_RESULT PASS=1` (phase14_gui_smoke is the standing setup/GUI load gate and sources setup_tab.tcl; the label lives inside build_actions, which only executes under Tk — headless sourcing stays clean). Full-log scan 0 `ERROR)` / 0 `bad switch` + `Exiting normally`. VMD runs SEQUENTIAL (R5).
   </verify>
   <done>The hint label exists in build_actions with the pinned copy, do_reset is behaviorally untouched, and the standing load gate is green. Commit (single, plan granularity): `fix(19-13): clarify Setup Reset semantics with an in-panel hint (16-12 defect)`.</done>
</task>

</tasks>

<verification>
- phase14_gui_smoke.tcl PASS=1 on fresh staging (setup_tab.tcl parses + sources clean headlessly).
- do_reset body byte-unchanged (`git diff` proof); only the hint label + comments added.
- Static gates zero: 8.6-idiom (lib/+gui/), grab-set (gui/).
- No new smoke file created — JUSTIFICATION: a wording-only GUI change with zero behavior delta adds no assertable headless surface beyond the standing load gate; the label's RENDERING is batch D's GUI-checkpoint class, and 19-15's gate re-runs phase14_gui_smoke plus a static grep for the hint text.
- Single fix(19-13) commit.
</verification>

<success_criteria>
The 16-12 Setup-Reset expectation mismatch is resolved the minimal way: the panel itself now says Reset is fields-only and points to the Game tab's Cleanup model, with Reset's behavior byte-unchanged and the standing load gate green.
</success_criteria>

<output>
After completion, create `.planning/phases/19-in-game-actions/19-13-SUMMARY.md` (carry the diff shape + the load-gate evidence; note the label rendering deferral to the GUI checkpoint)
</output>
