---
phase: 19-in-game-actions
plan: 05
type: execute
wave: 2
depends_on: ["19-01", "19-02", "19-04"]
files_modified: [vmd/lib/game.tcl]
autonomous: true

must_haves:
  truths:
    - "A reveal acts byte-identically to a click-find (mark_found_visual + registry mark_found through the same shared tail); the reveal use is counted (+1 one / +N all)"
    - "Reveal-one picks RANDOMLY (seedless generators::sample) among registry hidden_indices; no literal hider indices in game.tcl"
    - "Reveal-all marks EVERY remaining hidden hider and logs one v1-verbatim all-line, then the win flow fires exactly once"
    - "A hint highlights one candidate hider's neighborhood WITHOUT marking it found (no registry touch, count_remaining unchanged)"
    - "Reveal/hint during non-playing states are no-ops (state gate BEFORE any work - defense-in-depth)"
    - "on_pick's win flow and the reveal win flow share ONE code path (_win_flow), so reveal wins look identical to click wins"
  artifacts:
    - path: "vmd/lib/game.tcl"
      provides: "public hint/reveal_one/reveal_all (explicit-gs signatures, state-gated, confirm-free) + private _score_found/_win_flow shared tail + on_pick rewiring"
      min_lines: 660
      contains: "reveal_one"
  key_links:
    - from: "vmd/lib/game.tcl reveal_one"
      to: "vmd/lib/registry.tcl hidden_indices"
      via: "the random pool = registry::hidden_indices (19-01's seam); pick via generators::sample"
      pattern: "registry::hidden_indices"
    - from: "vmd/lib/game.tcl reveal procs"
      to: "vmd/lib/game_logic.tcl increment_reveal/increment_hint/log_append"
      via: "the 19-02 counter + log-kind seam (revealed / revealed_all / hint kinds)"
      pattern: "game_logic::increment_reveal"
    - from: "vmd/lib/game.tcl hint"
      to: "vmd/lib/hiders.tcl add_hint_rep"
      via: "the mol-bridge highlight primitive (19-04); candidate filter = hidden_indices x neighbor atomselect"
      pattern: "hiders::add_hint_rep"
    - from: "vmd/lib/game.tcl hint/reveal_one/reveal_all"
      to: "game_logic::state"
      via: "state gate at proc top: anything other than playing returns silently (R2 defense-in-depth)"
      pattern: "game_logic::state"
---

<objective>
Build the GAME-06/07/05 orchestration in game.tcl: `hint` (GAME-05 - candidate filter -> random pick -> hiders::add_hint_rep -> hint_count/log), `reveal_one` (GAME-06 - hidden_indices pool -> random pick -> the SAME mark tail as a click-find -> reveal_count/log/win-check), `reveal_all` (GAME-07 - mark every hidden hider -> one log line -> win flow once), plus the `_score_found`/`_win_flow` shared-tail refactor of on_pick so reveals are byte-identical to click-finds.

Purpose: v1 game.py:244-330 parity on the composition layer. Explicit-gs signatures per the R3 no-kwargs convention; no confirms inside lib procs; no mouse commands (BTN-06 contract); PRNG seedless via generators (R4).
Output: extended game.tcl (~90 new lines + refactor), on_pick behavior byte-preserved (phase16_onpick_smoke stays green untouched), probe-verified headlessly with the all-but-one-found determinism trick.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-in-game-actions/19-RESEARCH-integration.md
@.planning/phases/19-in-game-actions/19-RESEARCH-mechanisms.md
@.planning/phases/19-in-game-actions/19-01-SUMMARY.md
@.planning/phases/19-in-game-actions/19-02-SUMMARY.md
@.planning/phases/19-in-game-actions/19-04-SUMMARY.md
@vmd/lib/game.tcl
@vmd/lib/generators.tcl
@vmd/smoke/phase16_onpick_smoke.tcl

Load-bearing research: mechanisms section 1B (the locked action models - hint candidate filter + reveal_one pick + reveal_all loop; _found_flow/_win_flow decomposition; reveal==click byte-identity), section 2 (3 D-tasks), section 3 E.1-E.2 (Tcl bridge discipline: catch rc==1 vmdcon -err; $sel delete; NO confirm dialogs in lib; no mouse commands), section 5 (GAME-05/06/07 rows with file/proc/depth), section 7 (game.tcl invariants: state gate ALWAYS FIRST; R2/R3/R4). Integration section 3.2 (the exact public signatures - explicit game_state arg; private _score_found/_win_flow), section 4 (composition gap), section 7 (stub-callback verification pattern). 19-01/02/04 SUMMARYs (the seams this plan consumes). game.tcl's on_pick (the 3-way guard 597-601 + hidden-branch 601-616 to refactor) and the callback pattern. Tcl 8.5 ONLY; brace expr; one variable per line.
</context>

<tasks>

<task type="auto">
  <name>Task 1: shared-tail refactor (_score_found/_win_flow) + on_pick rewiring - byte-compatible</name>
  <files>vmd/lib/game.tcl</files>
  <action>
1. Extract the win flow: create `proc ::biochemeleon::game::_win_flow {gs}` containing the EXACT body of on_pick's win block (the finish_win + timer_elapsed + log_append win + win_cb catch calls, lifted verbatim - reads hider_count from gs). Declare the `_cb_log`/`_cb_win` variables inside it (variable lines). NOT exported (private).
2. Extract the find tail: create `proc ::biochemeleon::game::_score_found {gs idx kind}` - PRIVATE, containing the EXACT body of on_pick's hidden-branch mark sequence (mark_found_visual + registry::mark_found) followed by: count_remaining -> log_append with $kind as the kind and the remaining count as msg -> remaining_cb catch -> `if {$rem == 0} { _win_flow $gs }`. For on_pick, $kind is `found` - the produced log line is byte-identical to today's `Found one! N remaining`. The kind parameter is the ONLY generalization (19-02's `revealed` kind slots in for reveal-one).
3. Rewire on_pick: the hidden branch's mark+log+cb+win block (lines ~601-616) becomes `_score_found $current_state $hit_idx found`. The 3-way guard (miss / already-found log / hidden branch) stays EXACTLY where it is - _score_found contains NO guard (reveal paths have their own simpler gate). The two early `return`s in the miss/already branches are untouched. NO changes to the gs shape (stays 5-key, 18-07), no confirm dialogs, no mouse commands.
4. Update the header contract comment: document the two new PRIVATE helpers (not exported) and that on_pick's find path now flows through _score_found with kind `found` (byte-identical behavior - the phase16_onpick_smoke is the regression net and stays UNTOUCHED).
Verification anchor: onpick smoke green WITHOUT any edit to it proves the refactor is byte-compatible.
  </action>
  <verify>
Headless runs on a FRESH staging copy (e.g. `tmp/p19-05/`): `bash -ic 'cd tmp/p19-05 && vmd -dispdev text -e vmd/smoke/phase16_onpick_smoke.tcl -eofexit < /dev/null'` -> BCHM_SMOKE_RESULT PASS=1, full-log scan 0 `ERROR)` / 0 `bad switch` + `Exiting normally`. Also run `vmd/smoke/phase16_restart_smoke.tcl` the same way as a guard-untouched baseline (PASS=1). Static gate: 8.6-idiom grep 0 matches over vmd/lib/ vmd/gui/. VMD runs SEQUENTIAL (R5).
  </verify>
  <done>_win_flow and _score_found exist as private helpers; on_pick delegates with kind `found`; both phase16 smokes green with zero smoke edits (byte-compat proven).</done>
</task>

<task type="auto">
  <name>Task 2: public hint / reveal_one / reveal_all + headless probe</name>
  <files>vmd/lib/game.tcl</files>
  <action>
1. Extend the namespace export: append ` hint reveal_one reveal_all` (the private helpers stay unexported). Add a header-contract paragraph: the three procs take an EXPLICIT game_state argument (the R3 no-kwargs convention - headless smokes drive them directly and the live GUI callback path confirms), gate on `game_logic::state` FIRST (anything other than `playing` returns silently - R2 defense-in-depth: a won-state reveal must never reach finish_win), are confirm-free (R3 - no dialogs inside lib procs), never issue mouse commands (BTN-06), and never touch PRNG seeds (R4 - pick via `generators::sample`, seedless in production).
2. `proc ::biochemeleon::game::hint {game_state}`: state gate; inside the established catch-block pattern (rc==1 -> `catch {vmdcon -err "bioCHEMeleon game hint: $err"}`): gs guard (`dict size $game_state == 0` -> return); `set hidden [registry::hidden_indices]`; empty -> return (silent no-op, v1 parity); candidate filter - for each hidden idx build the atomselect `(within 5.0 of (index $idx)) and not resname GAM` on `[dict get $game_state game_molid]` (PARENTHESIZED within, the 17.2-04 gotcha; not resname GAM excludes all hider atoms - the same conjunct add_hint_rep uses), keep idx if `$sel num` > 0, ALWAYS `$sel delete` (the $sel delete discipline); empty candidate list -> return silently WITHOUT counting or logging (the 06-03 sparse-hider fix shape: no hint_count increment, no log line); pick via `lindex [generators::sample $candidates 1] 0`; `hiders::add_hint_rep [dict get $game_state game_molid] $pick`; `game_logic::increment_hint`; log via the established callback catch `catch {{*}$_cb_log [game_logic::log_append hint ""]}`. NO remaining_cb (nothing remaining changed), NO registry touch, NO mark_found_visual.
3. `proc ::biochemeleon::game::reveal_one {game_state}`: state gate; catch block: gs guard; `set hidden [registry::hidden_indices]`; empty -> return BEFORE any increment (v1 game.py no-op parity; the GUI guards remaining==0 earlier anyway); pick via `lindex [generators::sample $hidden 1] 0`; `game_logic::increment_reveal` (1) IMMEDIATELY after the pick (documented choice: the use is consumed once a hider is chosen - a scoring error is reported by the catch either way, and counter/log consistency matters more than post-score incrementing); then `_score_found $game_state $pick revealed` (byte-identical find flow, kind `revealed` -> `Revealed one! N remaining`).
4. `proc ::biochemeleon::game::reveal_all {game_state}`: state gate; catch block: gs guard; `set hidden [registry::hidden_indices]`; empty -> return; `set molid [dict get $game_state game_molid]`; loop over $hidden: `hiders::mark_found_visual $molid $idx` + `registry::mark_found $idx` (mark WITHOUT per-hider logging - the per-idx log cannot happen here, v1 logs ONCE); `game_logic::increment_reveal [llength $hidden]` (the +N form); `catch {{*}$_cb_log [game_logic::log_append revealed_all [llength $hidden]]}` (`Revealed all N hiders. Game over.`); `catch {{*}$_cb_remaining}`; then `_win_flow $game_state` (remaining is 0 unconditionally - the win is the point of reveal-all; fires EXACTLY once).
5. Tcl 8.5 only; brace expr; no lmap; comments gate-clean. NO changes to start_game/cleanup/restart/on_pick beyond Task 1's rewiring. NO gs shape changes.
6. Write an ephemeral headless probe `tmp/p19-05/probe.tcl` (fresh staging copy; source the lib in the established order; stub callbacks via set_callbacks: log -> `lappend ::P_LOG`, remaining -> `incr ::P_REM`, win -> `lappend ::P_WIN`; drive the PURE state machine per round: round_reset + begin_countdown + countdown_tick x4 + begin_play):
   - Round A (hint): start_game 4 hiders VDW-only -> playing -> `hint $gs` -> asserts: numreps +1; the LAST rep's selection read-back matches the shape `(within 5.0 of (index N)) and not resname GAM` with N in the hider pool; count_remaining STILL 4 and all statuses still hidden (hint does NOT mark found); hint_count == 1; log last line == `Hint: highlighted neighbors of one hider.`; then `hint $gs` again -> numreps +2, hint_count == 2 (accumulation proof, no literal draws).
   - Round B (reveal-one, all-but-one-found): start_game again -> playing -> mark 3 of 4 found (registry::mark_found + hiders::mark_found_visual on 555 556 557) -> `reveal_one $gs` -> the pool is {558} so the pick is deterministic: status_of 558 == found; atomselect `index 558` user2 read-back == 1.0; the found-rep selection read-back STILL equals the stored tier literal (byte-identity proof - reveal looked exactly like a click-find); reveal_count == 1; log last == `Revealed one! 0 remaining`; P_REM incremented; state == won; P_WIN has exactly 2 entries (win fired once).
   - Round C (reveal-all, fresh round): start_game again -> playing -> `reveal_all $gs` -> all 4 hiders user2 == 1.0; reveal_count == 4; log last == `Revealed all 4 hiders. Game over.`; state == won; P_WIN grew by exactly 2 more.
   - R2 no-ops: with state == won, call `hint`, `reveal_one`, `reveal_all` -> hint_count/reveal_count unchanged, log length unchanged, numreps unchanged.
   - Cleanup: `cleanup` on the last gs -> the restored original's numreps equals the pre-start value (hint reps die with the molecule).
   - Print `BCHM_SMOKE_RESULT PASS=1 FAIL=none` (or FAIL=<case>); run `vmd -dispdev text -e tmp/p19-05/probe.tcl -eofexit < /dev/null`; full-log scan 0 `ERROR)` / 0 `bad switch` + `Exiting normally`.
   NOTE: the sparse-hider no-neighbor silent branch has NO headless assert (real demo loads always have protein neighbors within 5 A; `mol new atoms N` molecules have no timestep so within-selections error) - it is v1-parity code covered by code review + the GUI checkpoint. Document this in the summary.
  </action>
  <verify>
Probe run: `BCHM_SMOKE_RESULT PASS=1 FAIL=none` parsed from the log (NEVER $?); every round's asserts present in the log; full-log scan 0 `ERROR)` / 0 `bad switch` + `Exiting normally`. Re-run BOTH tcltest suites on the same staging (`test_registry.test`, `test_game_logic.test`) -> Failed=0 each (integration check of the consumed seams). 8.6-idiom gate 0 matches. VMD runs SEQUENTIAL.
  </verify>
  <done>hint/reveal_one/reveal_all are exported, state-gated, confirm-free, and drive the shared find/win tail; the probe proves byte-identity, determinism via all-but-one-found, +N counting, single win, and R2 no-ops. Commit (single, plan granularity): `feat(19-05): add hint/reveal_one/reveal_all orchestration to game`.</done>
</task>

</tasks>

<verification>
- Probe (fresh staging tmp/p19-05): BCHM_SMOKE_RESULT PASS=1; 0 `ERROR)` / 0 `bad switch`; `Exiting normally`; all round A/B/C + no-op + cleanup asserts in the log.
- phase16_onpick_smoke PASS=1 with ZERO smoke edits (byte-compat of the refactor); phase16_restart_smoke PASS=1 (guard-untouched baseline).
- test_registry.test + test_game_logic.test Failed=0 on the staging copy.
- 8.6-idiom gate 0 matches over vmd/lib/ vmd/gui/.
- Single feat(19-05) commit; game_state stays 5-key (18-07 pin untouched).
</verification>

<success_criteria>
From the composition layer: reveal-one/reveal-all act byte-identically to click-finds and share their win path, hints highlight without scoring, all three are state-gated no-ops outside a live round, counters/logs flow through the 19-01/19-02 seams, and the permanent phase19 smoke batch (19-12..15) can drive every public proc headlessly with the explicit-gs convention.
</success_criteria>

<output>
After completion, create `.planning/phases/19-in-game-actions/19-05-SUMMARY.md` (carry the probe evidence + the all-but-one-found smoke idiom for 19-12's batch)
</output>
