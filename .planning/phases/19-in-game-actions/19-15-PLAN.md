---
phase: 19-in-game-actions
plan: 15
type: execute
wave: 6
depends_on: ["19-11", "19-12", "19-13", "19-14"]
files_modified: []
autonomous: true
# Wave ordering note: waves 1-5 complete all other Phase-19 plans (batch A
# 19-01..06, batch B 19-08..11, this batch's 19-12..14) before wave 6 runs,
# so the gate sees every phase artifact. depends_on pins the direct
# consumers: 19-11's hardened load gates, 19-12's new smoke, 19-13's defect
# fix, 19-14's driver round.

must_haves:
  truths:
    - "Every tcltest suite present at gate time runs GREEN (Failed=0) via the staging-only suite_driver.tcl wrapper, with the suite list and case totals DERIVED at execution time (ls + marker parse) and recorded — never copied stale"
    - "Every smoke present at gate time runs SEQUENTIALLY on fresh staging: phase-19 new/updated smokes PASS=1 x3, standing smokes PASS=1 x1, and the 2 pre-existing reds fail EXACTLY in their recorded shapes (counted and reported, NOT fixed)"
    - "Every log: 0 `ERROR)` (Stride-classified where the 17.2 convention applies) / 0 `bad switch` / `Exiting normally`; static gates zero: 8.6-idiom, grab-set, plus the phase-19 bans (ttk::combobox, tk_chooseColor, hardcoded ColorIDs in game_tab) and the 19-13 hint-text presence; the rep_verify definition-gate is green"
    - "The verdict PHASE HEADLESS-GREEN is recorded with derived counts and the red-shape confirmations — the GUI checkpoint (19-16/17) is the only remaining work"
  artifacts:
    - path: ".planning/phases/19-in-game-actions/19-15-SUMMARY.md"
      provides: "Gate record: derived suite totals, the derived smoke table (PASS/expected-red), static gate outputs, driver definition-gate, HEADLESS-GREEN verdict"
      contains: "HEADLESS-GREEN"
  key_links:
    - from: "gate run"
      to: "every Phase-19 artifact (19-01..06, 19-08..14)"
      via: "fresh staging tmp/cap19-gate, sequential VMD runs, marker parse + full-log scans"
      pattern: "BCHM_(TEST|SMOKE)_RESULT"
---

<objective>
The phase's FULL-SUITE green gate (17.1-13 / 17.2-11 / 18-14 pattern): every suite + every smoke against a fresh staging copy, sequential (shared $env(TEMP) forbids parallel VMD), markers parsed from logs (never $?), full-log false-PASS scans, static gates, the 2 known pre-existing reds confirmed unchanged, and counts RE-DERIVED at execution time.

Purpose: HEADLESS-GREEN declaration for Phase 19 — after this, only the GUI checkpoint (19-16/17, batch D) remains.
Output: gate record in the SUMMARY (no code files touched).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-in-game-actions/19-RESEARCH-integration.md
@.planning/phases/19-in-game-actions/19-11-SUMMARY.md
@.planning/phases/19-in-game-actions/19-12-SUMMARY.md
@.planning/phases/19-in-game-actions/19-13-SUMMARY.md
@.planning/phases/19-in-game-actions/19-14-SUMMARY.md
@.planning/phases/18-materials-exploration/18-14-PLAN.md

Load-bearing research: integration §7 runner rules (suites under headless VMD via the staging-only suite_driver.tcl wrapper — suites lack explicit exit and VMD's console hangs past stdin EOF; PASS=1 x3 for new/updated smokes; 8.6-idiom + grab-set greps; SEQUENTIAL VMD runs; marker parse NEVER $?; full-log scan, never marker-trust) and §5 W6 (the gate row). 18-14-PLAN is the verbatim shape template (single gate task, SUMMARY-only artifact, pre-existing-red discipline). COUNTS ARE NEVER COPIED STALE: the 17.2-11 numbers (6 suites 205/205, 29/31 smokes) and even 18-14's planned numbers (7 suites, 33 smokes) are HISTORY — derive everything at execution time.
</context>

<tasks>

<task type="auto">
  <name>Task 1: FULL-SUITE gate run + record (counts derived, never hardcoded)</name>
  <files>.planning/phases/19-in-game-actions/19-15-SUMMARY.md</files>
  <action>
1. Stage a FRESH copy of the repo's vmd/ tree to `tmp/cap19-gate/` (Windows-visible path).
2. DERIVE the suite list: `ls vmd/tests/test_*.test` AT EXECUTION TIME (expect the 6 standing + test_materials from Phase 18 + any 18 additions — whatever ls returns IS the list). Run each SEQUENTIALLY via the staging-only suite_driver.tcl wrapper (17.2-11 pattern: a one-purpose staging file that sources the suite by name then calls `exit`; sed-swap the suite name per run): `bash -ic 'cd tmp/cap19-gate && vmd -dispdev text -e suite_driver.tcl -eofexit < /dev/null'` per suite. Parse BCHM_TEST_RESULT from each log: record Total/Failed; require Failed=0 everywhere. DERIVE the expected totals: read the latest gate SUMMARY available under .planning/phases/ (18-14-SUMMARY if present, else 17.2-11's record) for the prior totals, add the phase-19 case counts recorded in 19-01/19-02/19-03-SUMMARY.md; RECORD the derivation arithmetic in the SUMMARY. A mismatch vs the derived expectation = investigate before declaring green (new cases may have landed beyond the records — record, don't guess).
3. DERIVE the smoke list: `ls vmd/smoke/*.tcl` AT EXECUTION TIME (expect 31 standing + Phase-18's additions + phase19_actions_smoke — whatever ls returns IS the list; COUNT it and record the number). Run SEQUENTIALLY, one VMD process at a time: `bash -ic 'cd tmp/cap19-gate && vmd -dispdev text -e vmd/smoke/<file>.tcl -eofexit < /dev/null'` per file.
   - x3 (independent processes, sequential): phase19_actions_smoke.tcl (new, 19-12) AND every smoke Phase 19 UPDATED: phase16_restart_smoke.tcl (19-06), phase16_gametab_smoke.tcl + phase16_entry_smoke.tcl (19-11).
   - x1: every untouched standing smoke (their x3 baselines are recorded from prior gates).
   - Full-log scan EVERY log: 0 `ERROR)` (Stride-classified where the 17.2 convention applies to residue/strand-running smokes), 0 `bad switch`, `Exiting normally`.
4. Static gates: 8.6-idiom grep over vmd/lib/ vmd/gui/ = 0; `grab set` grep over vmd/gui/ = 0; phase-19 bans: `ttk::combobox` and `tk_chooseColor` greps over vmd/gui/ = 0; hardcoded `ColorID <n>` grep over vmd/gui/game_tab.tcl = 0; the 19-13 hint text present in setup_tab.tcl (grep "setup fields only" == 1); the driver sanity grep: `on_reveal_one|on_reveal_all` in vmd/tests/rep_verify.tcl == 0.
5. rep_verify DEFINITION-GATE (one text-mode run in the staging): fake `::tk_version` + `::pv_probe 1` + source vmd/tests/rep_verify.tcl -> all pv_* procs defined incl. pv_round5, 0 `ERROR)` / 0 `bad switch`, `Exiting normally`.
6. Pre-existing-red discipline: phase17_dispatch_smoke.tcl (step-8 supply-0 degrade) and phase17_licorice_smoke.tcl (stale P pins) are EXPECTED red with recipes recorded in 17.2-11 + STATE.md — confirm each failure SHAPE matches those records (not a new failure mode); do NOT fix, do NOT re-diagnose; record "pre-existing, unchanged" per file in the SUMMARY.
7. Record everything in 19-15-SUMMARY.md: the derived suite list + per-suite totals + the derivation arithmetic; the derived smoke count + per-smoke PASS/expected-red table; static gate outputs; the driver definition-gate line; sequential-run confirmation; the verdict line `PHASE HEADLESS-GREEN`; and the handoff note that only the GUI checkpoint (19-16/17) remains.
   </action>
   <verify>
The SUMMARY's gate table shows: every derived suite Failed=0; every derived smoke PASS except EXACTLY the 2 expected pre-existing reds with matching shapes; all scanned logs clean; static gates zero; the definition-gate green; the HEADLESS-GREEN verdict present. Any OTHER red = a Phase-19 regression -> STOP and gap-close before the GUI checkpoint.
   </verify>
   <done>Phase 19 is HEADLESS-GREEN: full gate recorded with derived counts, zero new regressions, and the 2 known reds confirmed unchanged.</done>
</task>

</tasks>

<verification>
- Derived suite list: all green (Failed=0), totals recorded with derivation arithmetic.
- Derived smoke list: all PASS x1/x3 per the rule, except the 2 pre-existing reds (shapes confirmed, not fixed).
- Every log: 0 `ERROR)` / 0 `bad switch` / `Exiting normally`; static gates 0 (incl. phase-19 bans + hint text); driver definition-gate green.
- No code files modified by this plan (SUMMARY only); commit `docs(19-15): record phase-19 full-suite gate (HEADLESS-GREEN)`.
</verification>

<success_criteria>
The full-suite gate declares Phase 19 headless-green with re-derived evidence — the GUI checkpoint is the only remaining plan, and the gate's numbers are trustworthy because they were counted, not copied.
</success_criteria>

<output>
After completion, create `.planning/phases/19-in-game-actions/19-15-SUMMARY.md` (the gate record itself)
</output>
