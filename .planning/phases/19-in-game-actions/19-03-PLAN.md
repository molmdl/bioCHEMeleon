---
phase: 19-in-game-actions
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [vmd/lib/setup_state.tcl, vmd/tests/test_setup_state.test]
autonomous: true

must_haves:
  truths:
    - "The GUI found-color palette vocabulary exists as a shared setup_state constant (the DIFF-04 menubutton batch reads it; no Game-tab palette hardcodes ColorIDs)"
    - "The shipped default found color is pinned to the same ColorID the mol bridge ships (7), so default behavior is byte-unchanged"
    - "The palette is NOT persisted form state (no new DEFAULTS keys, no validate_state/randomize_state changes)"
  artifacts:
    - path: "vmd/lib/setup_state.tcl"
      provides: "GAME_FOUND_COLORS flat name-id palette constant + DEFAULT_FOUND_COLOR, namespace-exported"
      min_lines: 345
      contains: "GAME_FOUND_COLORS"
    - path: "vmd/tests/test_setup_state.test"
      provides: "tcltest cases pinning palette membership, id validity/uniqueness, default value + membership, exports, and no-form-state guard"
      min_lines: 240
      contains: "DEFAULT_FOUND_COLOR"
  key_links:
    - from: "vmd/lib/setup_state.tcl DEFAULT_FOUND_COLOR"
      to: "vmd/lib/hiders.tcl found_colorid default"
      via: "cross-module semantic coupling pinned by tests on both sides (setup_state owns the vocabulary, hiders owns the runtime - hiders sources NOTHING, mirroring the GAME_REPS/GameBlend pattern)"
      pattern: "DEFAULT_FOUND_COLOR 7"
    - from: "vmd/lib/setup_state.tcl GAME_FOUND_COLORS"
      to: "ColorID table 0-32 (probe-verified: 33 slots)"
      via: "every id is an integer ColorID in [0, 33); orange (ColorID 3) deliberately EXCLUDED (hint-highlight collision)"
      pattern: "white 8"
---

<objective>
Ship the DIFF-04 lib half's shared vocabulary: the curated found-color palette `GAME_FOUND_COLORS` (flat name-id pairs, dict-compatible via `{*}`) + `DEFAULT_FOUND_COLOR 7` in the pure form-state module, namespace-exported, mirroring exactly how 18-01 shipped GAME_MATERIALS/GAME_REPS.

Purpose: the GUI batch (19-08..11) builds the found-color menubutton from this constant - it must NOT hardcode ColorIDs; 19-04's hiders work consumes the DEFAULT=7 coupling. Pure layer + tcltest, same shape as 18-01.
Output: 2 new exported constants + extended test_setup_state.test, all pre-existing cases still green.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-in-game-actions/19-RESEARCH-integration.md
@.planning/phases/19-in-game-actions/19-RESEARCH-mechanisms.md
@.planning/phases/18-form-persistence/18-01-SUMMARY.md
@vmd/lib/setup_state.tcl
@vmd/tests/test_setup_state.test

Load-bearing research: integration section 4.3 (gap table - the palette constant), section 6 (DIFF-04 lib/GUI half split). Mechanisms section 1C (VMD ColorID table facts: 33 slots 0-32, 7=green the shipped found color, 3=orange the HINT color; probe-verified via tmp/p19-probe p19out5.txt). 18-01-SUMMARY (the GAME_MATERIALS/GAME_REPS pattern to mirror - constants live in setup_state, namespace-exported, test-pinned). PINNED membership (mechanisms section 8.1): green 7, red 1, yellow 4, cyan 10, pink 9, purple 11, white 8; orange 3 EXCLUDED (a found hider colored like a hint region would be indistinguishable). Tcl 8.5 ONLY.
</context>

<tasks>

<task type="auto">
  <name>Task 1: palette constant + default in setup_state.tcl</name>
  <files>vmd/lib/setup_state.tcl</files>
  <action>
1. Add a constant block NEXT TO the existing GAME_REPS / GAME_MATERIALS constant block (the 18-01 additions; anchor on the constant names, not line numbers):
```tcl
    # DIFF-04 (Phase 19): curated found-highlight palette for the Game-tab
    # found-color menubutton. Flat {name id} pairs (dict-compatible via {*});
    # ids are VMD ColorIDs (33 slots 0-32, probe-verified). DEFAULT FIRST -
    # the menubutton iterates pairs in list order. Orange (ColorID 3) is
    # DELIBERATELY EXCLUDED: it is the hint-highlight color (game.tcl hint
    # rep), and a found hider colored orange would be indistinguishable from
    # a hint region.
    variable GAME_FOUND_COLORS {green 7 red 1 yellow 4 cyan 10 pink 9 purple 11 white 8}
    # The shipped default - MUST equal the mol bridge's hiders::found_colorid
    # default (7, the pre-19 hardcoded green). setup_state owns the palette
    # vocabulary; hiders owns the runtime (hiders sources NOTHING, so the
    # coupling is pinned by tests on both sides - the GAME_REPS/GameBlend
    # cross-module pattern).
    variable DEFAULT_FOUND_COLOR 7
```
2. Extend the `namespace export` line: append ` GAME_FOUND_COLORS DEFAULT_FOUND_COLOR`.
3. Do NOT touch DEFAULTS, validate_state, or randomize_state - add a one-line comment near the constants stating why: the found color is Game-tab UI vocabulary, not persisted form state (DIFF-04 has no form field; the GUI's dropdown selection is session-scoped like the mol-bridge preference it drives).
Tcl 8.5 only; comment hygiene (no 8.6-idiom words anywhere, comments included).
  </action>
  <verify>
`tclsh vmd/lib/setup_state.tcl` loads clean (pure layer). Static gate: `grep -rnE "\blmap\b|\btry\b|\bthrow\b|\btailcall\b|\bcoroutine\b|\byield\b|\bfinally\b" vmd/lib/ vmd/gui/` returns 0 matches. (The tcltest cases in Task 2 are the behavioral proof; this task's verify is load + gate.)
  </verify>
  <done>GAME_FOUND_COLORS (7 curated pairs, green-7 first, orange-3 excluded) + DEFAULT_FOUND_COLOR 7 are namespace-exported setup_state constants; no form-state changes.</done>
</task>

<task type="auto">
  <name>Task 2: tcltest cases in test_setup_state.test</name>
  <files>vmd/tests/test_setup_state.test</files>
  <action>
Add a `# ---- Phase 19 additions: found-color palette constants ----` section BEFORE the final BCHM_TEST_RESULT marker block, mirroring the file's style. Cases (exact -result pins):
1. `game_found_colors_membership`: `::biochemeleon::setup_state::GAME_FOUND_COLORS` equals the exact flat list `green 7 red 1 yellow 4 cyan 10 pink 9 purple 11 white 8` (order + membership pinned; default first).
2. `game_found_colors_ids_are_colorids`: loop the even positions (ids); every id passes `string is integer -strict` and satisfies `0 <= id < 33` (the probe-verified table size); `list` of two 1s returns `1 1`.
3. `game_found_colors_ids_unique`: `llength` of the id sublist equals `llength [lsort -unique $ids]` (no duplicate ColorIDs) -> 1.
4. `default_found_color_value`: `::biochemeleon::setup_state::DEFAULT_FOUND_COLOR` returns `7`.
5. `default_found_color_in_palette`: the id paired with name `green` (lindex of the pair after green) equals DEFAULT_FOUND_COLOR -> 1 (pins BOTH the default-membership and the green-7 semantic).
6. `palette_procs_exported`: `set exp [namespace eval ::biochemeleon::setup_state { namespace export }]`; lsearch -exact finds GAME_FOUND_COLORS and DEFAULT_FOUND_COLOR -> `1 1`.
7. `palette_not_form_state`: loop over `[dict keys $::biochemeleon::setup_state::DEFAULTS]` - NO key contains the substring `color` or `found` (pins the no-new-form-keys decision; the count itself is 18-01's pin).
Run the suite from the fresh staging copy - all cases (pre-existing + 7 new) green in ONE run.
  </action>
  <verify>
Headless suite from a FRESH Windows-visible staging copy (e.g. `tmp/p19-03/`, a copy of the vmd/ tree): `bash -ic 'cd tmp/p19-03 && vmd -dispdev text -e vmd/tests/test_setup_state.test -eofexit < /dev/null'` - parse the `BCHM_TEST_RESULT` marker (NEVER $?): Failed=0 (all pre-existing + the 7 new cases green). Full-log scan 0 `ERROR)` / 0 `bad switch`; `Exiting normally`. VMD runs are SEQUENTIAL (R5).
  </verify>
  <done>Palette membership, id validity/uniqueness, default value/membership, exports, and the no-form-state guard are all test-pinned; full suite green. Commit (single, plan granularity): `feat(19-03): add GAME_FOUND_COLORS palette + DEFAULT_FOUND_COLOR to setup_state`.</done>
</task>

</tasks>

<verification>
- BCHM_TEST_RESULT Failed=0 (all pre-existing test_setup_state.test cases + 7 new) from the headless suite run on the fresh staging copy.
- Full-log scan 0 `ERROR)` / 0 `bad switch`; `Exiting normally` in the log.
- 8.6-idiom gate 0 matches over vmd/lib/ vmd/gui/.
- Single feat(19-03) commit covering both files.
</verification>

<success_criteria>
The DIFF-04 GUI batch can build the found-color menubutton from `setup_state::GAME_FOUND_COLORS` (labels + ids, default first) and seed its initial selection from `DEFAULT_FOUND_COLOR`, with zero hardcoded ColorIDs in gui code and zero form-state drift.
</success_criteria>

<output>
After completion, create `.planning/phases/19-in-game-actions/19-03-SUMMARY.md`
</output>
