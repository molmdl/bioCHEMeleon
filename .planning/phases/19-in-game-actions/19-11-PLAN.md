---
phase: 19-in-game-actions
plan: 11
type: execute
wave: 5
depends_on: ["19-08", "19-09", "19-10"]
files_modified: [vmd/smoke/phase16_gametab_smoke.tcl, vmd/smoke/phase16_entry_smoke.tcl]
autonomous: true

must_haves:
  truths:
    - "The gametab load-gate asserts every new game_tab proc by name (end_round, reset_view_state, _clear_log_view, and the desync-verdict-conditional trace trio), the reveal_text unset-at-source invariant, and the structural widget/dispatch strings incl. the widget bans"
    - "The entry load-gate asserts all seven dialog-scope handlers + the private _game_stash + the verbatim confirm copy + the on_close adoption strings + dialog.tcl widget bans"
    - "Both extended smokes pass PASS=1 x3 SEQUENTIAL with full-log scans 0 ERROR) / 0 bad switch / Exiting normally (R5: shared $env(TEMP) forbids parallel VMD runs)"
  artifacts:
    - path: "vmd/smoke/phase16_gametab_smoke.tcl"
      provides: "Phase-19 extensions: new proc asserts, reveal_text var check, structural widget/dispatch/ban greps"
      contains: "end_round"
    - path: "vmd/smoke/phase16_entry_smoke.tcl"
      provides: "Phase-19 extensions: dialog-handler proc asserts, confirm-copy structural checks, on_close adoption checks"
      contains: "on_found_mgmt"
  key_links:
    - from: "gametab smoke proc asserts"
      to: "19-08/19-09's defined game_tab procs"
      via: "info procs per pinned name + verdict-conditional branch from 19-08-SUMMARY"
      pattern: "end_round"
    - from: "entry smoke handler asserts"
      to: "19-10's dialog-scope handlers"
      via: "info procs per pinned name"
      pattern: "on_found_mgmt"
    - from: "both smokes' structural greps"
      to: "the pinned -command/-message literals"
      via: "read-source + string first, the established step-5 pattern"
      pattern: "Give up on one random hider"
---

<objective>
Extend the two Phase-16 load-gate smokes to gate the Phase-19 GUI batch's new surface: every new game_tab proc/widget var, every dialog-scope handler, the v1-verbatim confirm copy, the on_close adoption, and the banned-widget/string classes — headlessly (Tk does not load in text mode, so widget RENDERING stays the GUI checkpoint's job; this is the load/structure gate).

Purpose: the established 16-09/16-10 load-gate pattern applied to Phase 19 — no new smoke files (the phase19_actions_smoke + suites + gate belong to the 19-12..15 batch).
Output: two extended smokes, PASS=1 x3 sequential.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-in-game-actions/19-08-SUMMARY.md
@.planning/phases/19-in-game-actions/19-09-SUMMARY.md
@.planning/phases/19-in-game-actions/19-10-SUMMARY.md
@vmd/smoke/phase16_gametab_smoke.tcl
@vmd/smoke/phase16_entry_smoke.tcl

Load-bearing precedent: the two smokes' own structure (failures list + marker line, NEVER $?; full-log scan 0 `ERROR)` / 0 `bad switch`; `Exiting normally`; staging-cwd + [pwd] file resolution; the step-5 read-source + string-first forbidden-string pattern). 19-08-SUMMARY carries the desync PROBE VERDICT (LIVE/DEAD) — the gametab extension branches on it: LIVE -> hard-assert all three trace procs exist; DEAD -> hard-assert NO trace procs exist (the deferral left no half-code). Tcl 8.5 only inside the smokes.

NOTE ON SCOPE: the integration research's wave matrix lists these load-gate extensions as "W3/W4"; they land at wave 5 here because they must gate 19-09's wave-4 game_tab output (a wave-4 placement would race 19-09). The separate `phase19_actions_smoke.tcl` (lib action proof) + test suites + full-suite gate are the 19-12..15 batch's — NOT this plan.
</context>

<tasks>

<task type="auto">
  <name>Task 1: extend phase16_gametab_smoke.tcl for the Phase-19 game_tab surface</name>
  <files>vmd/smoke/phase16_gametab_smoke.tcl</files>
  <action>
1. Header comment: add a "Phase 19 extension" paragraph listing what the new asserts cover (procs, reveal_text var, structural widget/dispatch strings, bans, verdict-conditional trace trio).
2. Step 2 proc list: append `end_round reset_view_state _clear_log_view` to the foreach (info procs sees private procs too — export status is irrelevant to existence). Keep the `< 11` count floor as-is (it is a floor, now trivially exceeded).
3. Desync-verdict branch (READ `.planning/phases/19-in-game-actions/19-08-SUMMARY.md` first; encode the verdict as a literal in the smoke with a comment citing the summary): if VERDICT LIVE -> append `_on_mode_var_write _register_mode_trace unregister_mode_trace` to the required list; if VERDICT DEAD -> assert the ABSENCE instead: `if {[llength [info procs ::biochemeleon::game_tab::_register_mode_trace]] > 0} { lappend failures "trace_procs_present_after_dead_verdict" }`.
4. Step 4 var list: append `reveal_text` to the unset-at-source foreach.
5. New step 6 (structural, the step-5 read-source pattern on game_tab.tcl — string first, case-sensitive):
   - required strings: `-command {::biochemeleon::on_hint}`, `-command {::biochemeleon::on_reveal_one}`, `-command {::biochemeleon::on_reveal_all}`, `-command {::biochemeleon::on_restart}`, `-command {::biochemeleon::on_cleanup}`; `reveal_text` (textvariable + reset_view_state init); `Found hiders`; `on_found_mgmt hide`; `on_found_mgmt show`; `on_found_mgmt recolor`; `GAME_FOUND_COLORS`; `on_found_color $cid` (the [list] dispatch); `reset_view_state` called from start_round (match the bare call ` reset_view_state` following the step-1.5 comment); `end_round` in the namespace export line; `game_logic::round_reset` inside end_round (match `game_logic::round_reset` occurring at least twice — start_round + end_round).
   - forbidden strings (append to the existing forbidden foreach): `tk_chooseColor`, `ttk::combobox`, `menubutton $help.found.menu add check` (sanity — commands only), and keep `mouse mode` / `vwait` / `grab set`.
   Each missing required string -> `lappend failures "game_tab_missing:<key>"`.
6. Keep every existing assert (steps 1-5) UNTOUCHED — this is an extension, not a rewrite.
  </action>
  <verify>
Fresh staging copy (`tmp/p19-11/`, a copy of the vmd/ tree): run THREE times SEQUENTIALLY: `bash -ic 'cd tmp/p19-11 && vmd -dispdev text -e vmd/smoke/phase16_gametab_smoke.tcl -eofexit < /dev/null'` -> PASS=1 each, full-log scan 0 `ERROR)` / 0 `bad switch`, `Exiting normally` each.
  </verify>
  <done>The gametab load-gate covers the full Phase-19 game_tab surface incl. the verdict-conditional trace branch.</done>
</task>

<task type="auto">
  <name>Task 2: extend phase16_entry_smoke.tcl for the dialog-scope handlers</name>
  <files>vmd/smoke/phase16_entry_smoke.tcl</files>
  <action>
1. Header comment: add the Phase-19 extension paragraph.
2. Step 3 proc foreach: append `::biochemeleon::on_hint`, `::biochemeleon::on_reveal_one`, `::biochemeleon::on_reveal_all`, `::biochemeleon::on_restart`, `::biochemeleon::on_cleanup`, `::biochemeleon::on_found_mgmt`, `::biochemeleon::on_found_color`, `::biochemeleon::_game_stash`. In the game_tab foreach, append `end_round reset_view_state _clear_log_view` (+ the same verdict-conditional trace trio branch as Task 1 step 3, citing 19-08-SUMMARY).
3. New step (structural on dialog.tcl source, read-source + string first):
   - required: `Give up on one random hider? This counts as a reveal use.`; `Reveal one hider?`; `Give up and reveal ALL remaining hiders? This ends the game.`; `Reveal all hiders?`; `-type yesno`; `game::reveal_one`; `game::reveal_all`; `game::hint`; `game::restart`; `game::cleanup`; `game_tab::end_round`; `game_tab::unregister_mode_trace`; `set_found_visible`; `set_found_color`; the on_close stash-conditional adoption (`_game_stash]] > 0` + `catch {::biochemeleon::on_cleanup}`).
   - forbidden: `grab set`, `tk_chooseColor`, `ttk::combobox` (dialog.tcl scan; the repo-level greps remain the primary gate — this bakes them into the smoke).
4. Keep every existing assert (steps 0-5 incl. the public-API regression) UNTOUCHED.
  </action>
  <verify>
Same staging, run THREE times SEQUENTIALLY: `bash -ic 'cd tmp/p19-11 && vmd -dispdev text -e vmd/smoke/phase16_entry_smoke.tcl -eofexit < /dev/null'` -> PASS=1 each, 0 `ERROR)` / 0 `bad switch`, `Exiting normally` each. (VMD runs strictly sequential — R5.)
  </verify>
  <done>The entry load-gate covers the dialog handlers, confirm copy, close adoption, and widget bans.</done>
</task>

</tasks>

<verification>
- Both extended smokes PASS=1 x3 sequential on the fresh staging copy; full-log scans 0 `ERROR)` / 0 `bad switch`; `Exiting normally` in every run.
- The verdict branch matches 19-08-SUMMARY's recorded desync verdict (LIVE -> trace procs required; DEAD -> trace procs must be absent).
- No edits to any vmd/gui/ or vmd/lib/ file in this plan (`git diff --stat` shows only the two smoke files).
- Single commit: `feat(19-11): extend load-gate smokes for the phase 19 GUI surface`.
</verification>

<success_criteria>
The Phase-19 GUI batch's entire headless-verifiable surface (procs, vars, dispatch literals, confirm copy, bans, verdict-conditional trace) is permanently gated by the established load-gate smokes, green x3 sequential, ready for the 19-12..15 batch's deeper action proof and gate.
</success_criteria>

<output>
After completion, create `.planning/phases/19-in-game-actions/19-11-SUMMARY.md`
</output>
