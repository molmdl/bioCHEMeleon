---
phase: 19-in-game-actions
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [vmd/lib/hiders.tcl]
autonomous: true

must_haves:
  truths:
    - "Found hiders can be recolored to any palette ColorID at the rep level, and every future round's found reps are BUILT in the chosen color (found_colorid preference)"
    - "Found reps can be hidden and re-shown at the state level, with the showstate round-tripping through the mol showrep getter"
    - "A hint press adds ONE highlight rep with the parenthesized within-selector that excludes resname GAM (the hinted hider and all other hiders)"
    - "The never-hide rule is NARROWED, not broken: the HIDDEN rep of every pair is never toggled; only the FOUND rep may be hidden by found-mgmt"
    - "R7 is closed with probe evidence: modselect does not flip rep showstate"
  artifacts:
    - path: "vmd/lib/hiders.tcl"
      provides: "found_colorid var + getter, add_hider_reps consumption, set_found_color, set_found_visible, add_hint_rep, narrowed NEVER-HIDE header"
      min_lines: 330
      contains: "set_found_color"
  key_links:
    - from: "vmd/lib/hiders.tcl found_colorid (var + getter)"
      to: "add_hider_reps found-rep creation loop"
      via: "mol modcolor ColorID $found_colorid replaces the hardcoded ColorID 7 (default byte-unchanged)"
      pattern: "ColorID \\$found_colorid"
    - from: "vmd/lib/hiders.tcl add_hint_rep selector"
      to: "the parenthesized within gotcha (17.2-04) + never-hide GAM exclusion"
      via: "PARENTHESIZED (within R of (index N)) and not resname GAM - the bare form swallows the trailing conjunct"
      pattern: "within \\\$radius of \\\(index \\\$idx\\\) and not resname GAM"
    - from: "vmd/lib/hiders.tcl set_found_visible"
      to: "mol showrep ARG ORDER (molid first - probe3 gotcha)"
      via: "mol showrep <molid> <repid> on|off, the REVERSE of modstyle/modcolor/modselect; getter form round-trips"
      pattern: "mol showrep \\\$molid"
---

<objective>
Add the GAME-08 mol-bridge half + the hint highlight to the rep-ownership module: (1) `found_colorid` preference (var + getter + add_hider_reps consumption) so found reps recolor globally and future rounds inherit the choice; (2) `set_found_color` / `set_found_visible` found-mgmt procs; (3) `add_hint_rep` - the hint's accumulate-highlight rep (probe4 is the literal script); (4) the NEVER-HIDE header narrowed to the documented found-rep exception; (5) R7 closed with a probe (modselect vs showstate).

Purpose: research gap section 4.4 - the mol bridge has mark/restore but no recolor/visibility/hint primitives. Everything here is rep-level mechanics; the game-layer orchestration (who calls what, when) is 19-05.
Output: extended hiders.tcl (~90 new lines), probe-verified headlessly; NO new game.tcl/registry logic.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-in-game-actions/19-RESEARCH-integration.md
@.planning/phases/19-in-game-actions/19-RESEARCH-mechanisms.md
@.planning/phases/18-materials-exploration/18-02-SUMMARY.md
@vmd/lib/hiders.tcl
@vmd/lib/generators.tcl

Load-bearing research: mechanisms sections 1C/3 (VMD command facts: mol repindex/molinfo "{rep i}" / "{selection i}" / "{color i}" / "{material i}" combined-braces read-back forms; mol showrep ARG ORDER gotcha - molid first, probe3's swapped form erred; mol showrep getter round-trip probe5; VDW representation on protein atoms; within-selector PARENTHESIZED gotcha 17.2-04/probe2; ColorID 7 default, 3=orange; modselect does NOT flip showstate). Integration section 4.4 (gap table + GUI row), section 5 (W1 mol-bridge tasks), section 7 (verification). Mechanisms section 3 E.3 (hint reps ACCUMULATE - one addrep per call, never delrep mid-round; v1 PRAGMA-verified). 18-02-SUMMARY (add_hider_reps is 3-arg post-18 with tier_materials; anchor on code shape, not line numbers). Tcl 8.5 ONLY; brace expr; $sel delete discipline.
</context>

<tasks>

<task type="auto">
  <name>Task 1: found-mgmt primitives (found_colorid, set_found_color, set_found_visible) + header narrowing</name>
  <files>vmd/lib/hiders.tcl</files>
  <action>
1. Add the preference var + getter. In the `variable` block after tier_styles: `variable found_colorid 7` (ONE line, with comment: the found-rep ColorID preference; default 7 = green = the pre-19 hardcoded value, so default behavior is byte-unchanged; NOT reset by add_hider_reps - a user preference persisting across rounds, unlike tier_reps/tier_styles/tier_materials). Add exported getter proc `found_colorid {}` mirroring the state-getter precedent (variable declaration + return). Append `set_found_color set_found_visible add_hint_rep found_colorid` to the namespace export list.
2. In `add_hider_reps`'s found-rep creation loop, change `mol modcolor $fidx $molid ColorID 7` to `mol modcolor $fidx $molid ColorID $found_colorid` (anchor: the ColorID 7 modcolor call inside the foreach; add `variable found_colorid` to the proc's variable declarations WITHOUT resetting it). Update the adjacent comment ("ColorID 7 = green") to say the color comes from the found_colorid preference (default 7).
3. Add `set_found_color {molid colorid}`: validates colorid (string is integer -strict, 0 <= colorid < 33 - the probe-verified table size - error otherwise: "not a ColorID in [0, 33)"); sets `found_colorid $colorid` FIRST (the preference is recorded even when no round is live); if `dict size $tier_reps` is 0, return (preference recorded, nothing to recolor - a legal pre-round call, unlike mark_found_visual which hard-errors); otherwise walk tier_reps EXACTLY like mark_found_visual (tier_reps -> repindex at USE time; repindex -1 or >= numreps = hard error "found rep X is gone"), and for each found repindex: `mol modcolor $fidx $molid ColorID $colorid`, then P-1 read-back via the combined-braces form `foreach {rstyle rsel rcol rmat} [molinfo $molid get "{rep $fidx} {selection $fidx} {color $fidx} {material $fidx}"] { break }` - compare $rcol to `ColorID $colorid` exactly (modcolor never raises; mismatch = hard error "color read-back mismatch").
4. Add `set_found_visible {molid flag}`: flag is 0/1 (coerce via `expr {$flag ? 1 : 0}`); if tier_reps is empty, silent no-op return; walk tier_reps for FOUND repindexes ONLY (the hname/hidden rep is NEVER touched - the narrowed rule); ARG ORDER GOTCHA pinned in a comment: `mol showrep $molid $fidx $mode` where mode is `on`/`off` (molid FIRST - the REVERSE of modstyle/modcolor/modselect; probe3's swapped form erred); then read-back assert: `mol showrep $molid $fidx` (getter form, no mode arg) equals the coerced flag - mismatch = hard error "showstate read-back mismatch".
5. Narrow the header comment: rewrite the NEVER-HIDE block (lines ~66-71) to: the HIDDEN rep of every pair (user2 < 1 - UNFOUND hiders) is NEVER hidden (its pickability is load-bearing); DELIBERATE EXCEPTION - the FOUND rep (user2 > 0 only) MAY be hidden/shown by set_found_visible, because every atom it can contain is already scored (registry status found), so hiding it cannot make an unfound hider unclickable, and double-count prevention stays the caller's registry-status guard; set_found_visible is this module's ONLY showrep caller (the earlier "no showrep call exists in this module" statement is superseded by this narrowed rule). Also reword the matching sentence in add_hider_reps' doc comment ("NO showrep calls anywhere") to say add_hider_reps itself never hides - found-mgmt may toggle the FOUND rep afterwards.
Keep every comment 8.6-idiom-gate-clean (no lmap/try/throw/tailcall/coroutine/yield/finally words, comments included). Tcl 8.5 only.
  </action>
  <verify>
`tclsh` cannot load hiders.tcl (mol-coupled - skip; the probe in Task 2 is the load check). Static gate: `grep -rnE "\blmap\b|\btry\b|\bthrow\b|\btailcall\b|\bcoroutine\b|\byield\b|\bfinally\b" vmd/lib/ vmd/gui/` returns 0 matches.
  </verify>
  <done>found_colorid var/getter wired through add_hider_reps (default byte-unchanged); set_found_color and set_found_visible exist with P-1 read-back asserts and the probe3 arg-order comment; the NEVER-HIDE rule is narrowed with the found-rep exception documented.</done>
</task>

<task type="auto">
  <name>Task 2: add_hint_rep + headless probe (R7 closure + all-primitive verification)</name>
  <files>vmd/lib/hiders.tcl</files>
  <action>
1. Add `add_hint_rep {molid idx {radius 5} {colorid 3}}`: guard idx is an integer atom index (error otherwise); normalize `set radius [expr {double($radius)}]` (so the default renders 5.0 - the research-pinned selector string); `mol addrep $molid`; compute `set hidx [expr {[molinfo $molid get numreps] - 1}]`; `mol modstyle $hidx $molid VDW`; `mol modcolor $hidx $molid ColorID $colorid`; build the PARENTHESIZED selector `set hsel "(within $radius of (index $idx)) and not resname GAM"` (comment: PARENTHESIZED within - the bare form swallows the trailing conjunct, the 17.2-04 gotcha probe2 discriminated; not resname GAM excludes the hinted hider's fake-residue N/C/O/CB AND all other hiders - v1 `not segi GAME` semantics, all hider atoms carry resname GAM); `mol modselect $hidx $molid $hsel`; P-1 read-back via the combined-braces form - style equals `VDW`, selection equals `$hsel` exactly, color equals `ColorID $colorid` (mismatch = hard error). Comment: the hint rep is NOT recorded in tier_reps - it ACCUMULATES (one addrep per call, never modified again, never deleted mid-round; dies with the molecule on cleanup/restart; v1 parity). NO delrep calls.
2. Write an ephemeral headless probe `tmp/p19-04/probe.tcl` (tmp/ is gitignored - the PERMANENT regression asserts are the 19-12 smoke batch's job; this summary carries the evidence). The probe (fresh staging copy `tmp/p19-04/` of the vmd/ tree; source the lib in the established order - mirror the source block of vmd/smoke/phase16_restart_smoke.tcl):
   a. load_demo 1znf -> start_game on it (4 hiders, VDW-only per_rep dict) -> game molid m, hider indices {555 556 557 558}.
   b. add_hint_rep $m 555 -> numreps +1; read-back style VDW, selection equals the exact string `(within 5.0 of (index 555)) and not resname GAM`, color `ColorID 3`; GAM-exclusion arithmetic (probe2's discriminator): num(selector) + num(`resname GAM and within 5.0 of (index 555)`) == num(`within 5.0 of (index 555)`).
   c. add_hint_rep $m 557 -> numreps +1 again (accumulate, two hint reps coexist).
   d. set_found_color $m 11 -> every tracked found rep read-backs `ColorID 11`; found_colorid getter returns 11.
   e. set_found_visible $m 0 -> getter round-trip 0; set_found_visible $m 1 -> getter 1 (probe5-proven round-trip).
   f. R7: set_found_visible $m 0 (found rep OFF); registry::mark_found 555 + hiders::mark_found_visual $m 555 (the re-assert modselects the OFF found rep); assert the found rep showstate is STILL 0 (modselect does not flip showstate - R7 CLOSED); set_found_visible $m 1 to restore.
   g. start_game again (guard auto-cleans; new game molid) -> the new round's found rep color read-back equals `ColorID 11` (found_colorid persisted across rounds - NOT reset by add_hider_reps).
   h. Print a `BCHM_SMOKE_RESULT PASS=1 FAIL=none` marker (or FAIL=<case>) so the run parses like a smoke; run `vmd -dispdev text -e tmp/p19-04/probe.tcl -eofexit < /dev/null`.
3. Full-log discipline on the probe run: 0 `ERROR)` / 0 `bad switch`, `Exiting normally`.
  </action>
  <verify>
Probe run: `BCHM_SMOKE_RESULT PASS=1 FAIL=none` parsed from the log (NEVER $?); full-log scan 0 `ERROR)` / 0 `bad switch` + `Exiting normally`; all step b-g asserts present in the log. Static gate: 8.6-idiom grep 0 matches over vmd/lib/ vmd/gui/.
  </verify>
  <done>add_hint_rep ships the parenthesized GAM-excluding VDW highlight with P-1 read-back; the probe proves set_found_color recolor + preference persistence, set_found_visible round-trip, R7 closure (modselect does not flip showstate), and accumulate semantics. Commit (single, plan granularity): `feat(19-04): add found-mgmt + hint-highlight primitives to hiders`.</done>
</task>

</tasks>

<verification>
- Probe (fresh staging tmp/p19-04): BCHM_SMOKE_RESULT PASS=1; 0 `ERROR)` / 0 `bad switch`; `Exiting normally`.
- Every probe step's assert appears in the log (hint selector exact string + GAM arithmetic, recolor read-back on ALL found reps, visibility round-trip, R7 still-off assert, cross-round color persistence).
- 8.6-idiom gate 0 matches over vmd/lib/ vmd/gui/.
- NO changes to game.tcl, registry.tcl, or game_logic.tcl (19-05 owns orchestration).
- Single feat(19-04) commit.
</verification>

<success_criteria>
The mol bridge can recolor found hiders (globally + across future rounds), toggle found-rep visibility with a round-tripping showstate, and add hint highlights with the correct parenthesized GAM-excluding selector - all probe-verified, with the never-hide rule narrowed exactly as the research prescribes and R7 closed with evidence 19-12 can cite.
</success_criteria>

<output>
After completion, create `.planning/phases/19-in-game-actions/19-04-SUMMARY.md`
</output>
