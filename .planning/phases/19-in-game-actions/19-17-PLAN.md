---
phase: 19-in-game-actions
plan: 17
type: execute
wave: 7
depends_on: ["19-16"]
files_modified: [vmd/AGENTS.md]
autonomous: false

must_haves:
  truths:
    - "G1: all new controls render in the 'Actions' labelframe (Hint / Reveal one / Reveal all help row, Restart / Cleanup model round-end row, 'Found hiders' + 'Color' menubuttons) and 'Reveals: 0' sits in the status row between Remaining and Mouse — layout does not crowd the log/mouse frame"
    - "G2: Hint shows an orange region AROUND (never on) a hider, repeated presses ACCUMULATE regions, the log gains 'Hint: highlighted neighbors of one hider.' per press"
    - "G3/G10: Reveal one shows the v1-verbatim confirm (title 'Reveal one hider?', body 'Give up on one random hider? This counts as a reveal use.'); No = TRUE no-op (counter, registry, log, viewer all unchanged); Yes = one random hider green + 'Revealed one! N remaining' + 'Reveals: 1'"
    - "G4/G9: Reveal all shows the v1-verbatim confirm; Yes = all remaining green + 'Revealed all N hiders. Game over.' + the win box ~100 ms AFTER the confirm (two boxes in sequence, never stacked); the timer kept ticking under the confirm (expected) and is frozen at the win"
    - "G5/G6: Found hiders -> Hide found vanishes the found hiders / Show found returns them; the Color palette offers NAMED entries (labels, not numbers) and a choice recolors ALL found hiders immediately; a subsequent find renders in the CHOSEN color (DIFF-04 label-driven accessibility)"
    - "G7: Restart replays — fresh countdown, timer restarts, same hider count (6), positions re-randomized, 'Reveals: 0', the found-color preference persists, and the first-click p-press quirk applies again"
    - "G8: the Game-tab 'Cleanup model' button restores the original molecule (atom count intact), resets labels/log, raises the Setup tab; closing the dialog (WM_DELETE) with a live round runs the SAME cleanup silently (cleanup-on-close)"
    - "POST-WIN (v2-specific, no v1 analog): Hide found / Show found / Recolor all still work after the win box — the molecule survives the win"
    - "G11 (desync-verdict item): recorded per the 19-08 branch — if the probe verdict was LIVE, hotkey r flips the Rotate/Pick radios back to Rotate while a round is live; if DEFERRED, the desync is recorded as known 16-12 behavior"
    - "19-13's in-panel Reset hint label is visible and readable on the Setup tab (rendering class owned by this checkpoint)"
    - "G12 (optional different-target check, 17.1-14 defect): after a mid-round Start on a different target, the restored original no longer intercepts picks (or recorded N/A — headless-proven by 19-06's adapted restart smoke)"
  artifacts:
    - path: ".planning/phases/19-in-game-actions/19-17-SUMMARY.md"
      provides: "The G1-G12 verdict table (pass/fail per item + evidence from actions_verify_log.txt), the desync branch taken, the APPROVED (or defect-recorded) checkpoint record"
      contains: "APPROVED"
    - path: "vmd/AGENTS.md"
      provides: "Refreshed Phase-19 note: actions GUI shipped (buttons/dropdowns/palette/confirm copy/cleanup-on-close), the desync verdict, the checkpoint date"
      contains: "Phase-19"
  key_links:
    - from: "the human session"
      to: "vmd/tests/actions_verify.tcl (19-16 driver)"
      via: "3 pastes (source / pv_a_mid / pv_a_finish) + real button clicks + dialog answers"
      pattern: "actions_verify"
    - from: "the verdict table"
      to: "actions_verify_log.txt"
      via: "auto-logged state/counter/showrep/color/desync/teardown lines as per-item evidence"
      pattern: "actions_verify_log"
---

<objective>
Run the ONE consolidated human-verify checkpoint for the whole Phase 19 (all eight requirements + the 16-12 defect dispositions), driven by 19-16's actions_verify.tcl: the human clicks the real buttons, answers the real v1-verbatim confirms, and walks the win/post-win/restart/cleanup/close flows; the driver auto-logs every verdict's evidence. Then close out the docs (vmd/AGENTS.md Phase-19 note).

Purpose: the last unverified surface of Phase 19 — everything Tk cannot prove headless (widget rendering, modal confirms, dropdown/palette interaction, visual hint regions, first-click quirk, teardown legs). 19-15 declared HEADLESS-GREEN; this plan is the only remaining work.
Output: an APPROVED (or precisely defect-recorded) checkpoint verdict + refreshed vmd/AGENTS.md Phase-19 note.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/19-in-game-actions/19-RESEARCH-gui.md
@.planning/phases/19-in-game-actions/19-16-SUMMARY.md
@.planning/phases/19-in-game-actions/19-15-SUMMARY.md

Load-bearing facts:
- 19-16-SUMMARY carries the driver's EXACT paste names + instruction texts — quote them verbatim in the steps below (the driver is the source of truth; fix drift by re-reading the driver, not by paraphrasing).
- Confirm copy is v1-VERBATIM (research §2.3): Reveal one = title `Reveal one hider?` body `Give up on one random hider? This counts as a reveal use.`; Reveal all = title `Reveal all hiders?` body `Give up and reveal ALL remaining hiders? This ends the game.`. The session checks the REAL dialogs show EXACTLY this copy and that cancel is a true no-op.
- The timer keeps ticking under a modal confirm (research §4.1 — v1 parity, EXPECTED, not a bug); the win box appears ~100 ms after the Reveal-all confirm closes (two boxes in sequence, never stacked).
- Desync verdict comes from 19-08-SUMMARY (LIVE = trace fix landed; DEFERRED = recorded). The session handles BOTH branches and records which one ran.
- 19-13's Setup-Reset hint label rendering is verified here (its plan assigns the rendering class to batch D).
- The first-click quirk is LOCKED: ONE keyboard `p` press arms C-side pick delivery per round; pasted `mouse mode` never arms. Instructions lead with it and re-state it after every restart.
- 19-15's gate is GREEN before this session; its staging root was tmp/cap19-gate — this session stages FRESH (do not reuse).
- Scope discipline: no .planning/STATE.md or ROADMAP.md edits — decision recording happens via this plan's SUMMARY; REQUIREMENTS.md checkbox flips are the orchestrator's.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The complete Phase 19 in-game actions feature end-to-end in a real VMD GUI: the Actions labelframe (Hint / Reveal one / Reveal all / Restart / Cleanup model), the "Reveals: N" status label (DIFF-01), the Found-hiders menubutton (Hide/Show/Recolor found) and the named Color palette menubutton (DIFF-04), the v1-verbatim reveal confirms with true no-op cancels, hint-region accumulation, the restart replay, both cleanup legs (button + WM-close), post-win found-mgmt usability (v2-specific), the desync-verdict item, and 19-13's Setup-Reset hint label — driven by actions_verify.tcl with every state read auto-logged.</what-built>
  <how-to-verify>
1. Stage fresh (WSL): `cp -r vmd tmp/biochemeleon-vmd19/`. Open a real VMD GUI on Windows, Extensions -> Tk Console, `cd` to the staging root (Windows path of tmp/biochemeleon-vmd19).
2. Paste 1: `source vmd/tests/actions_verify.tcl` — EXPECT: 1znf loads, the dialog opens, the 6-hider mixed-tier round starts (VDW 2 / Lines 2 / Licorice 2, easy), 3-2-1-GO countdown, and the first state dump lands in actions_verify_log.txt. VERIFY G1 now: the Actions labelframe shows Hint / Reveal one / Reveal all, then Restart / Cleanup model, plus the "Found hiders" and "Color" menubuttons; the status row reads timer, "Remaining: 6", "Reveals: 0", Mouse — nothing crowds the log or mouse frame.
3. Press `p` ONCE (the locked first-click quirk — required again after every restart). Click one hider — it turns green (baseline find; the pick-observer logs it).
4. Click Hint TWICE — VERIFY G2: an orange region appears AROUND a hider (not on it), the two regions accumulate, and the log gains "Hint: highlighted neighbors of one hider." per press.
5. Click Reveal one — VERIFY G3/G9/G10: the confirm shows EXACTLY title `Reveal one hider?` and body `Give up on one random hider? This counts as a reveal use.`; while it is up, the timer KEEPS counting (expected — deliberation costs time); answer **No** — NOTHING changes (counter still "Reveals: 0", no green change, no new log line). Click Reveal one again, answer **Yes** — EXPECT: one random hider turns green, log "Revealed one! N remaining", status "Reveals: 1".
6. Found hiders -> Hide found — VERIFY G5: the found hiders vanish. Show found — they return.
7. Color menubutton — VERIFY G6/DIFF-04: the entries are NAMED labels (green / red / yellow / cyan / pink / purple / white — not raw numbers). Pick e.g. Cyan — ALL found hiders recolor immediately.
8. Find one more hider (press `p` is still armed; click it) — EXPECT: it renders in the CHOSEN color, not green (the preference flowed into the find).
9. Desync step (G11 — both branches spelled out): press the `r` hotkey. If 19-08's verdict was LIVE: the Rotate/Pick radios and Mouse label flip back to Rotate while the round stays live. If DEFERRED: the radios stay on Pick — record the desync as the known 16-12 behavior. Either way finish with: click the Pick radio, press `p` once more.
10. Paste 2: `pv_a_mid` — EXPECT: the dump logs "Reveals: 1", counters, found-rep showstates + color read-backs, and the mouse-mode getter line; then the driver auto-restarts (no dialog) — VERIFY G7: fresh round, NEW game molid, same 6-hider count, "Reveals: 0", timer re-armed, the chosen found-color persists.
11. Press `p` once. Click Reveal all — VERIFY G4: the confirm shows EXACTLY title `Reveal all hiders?` and body `Give up and reveal ALL remaining hiders? This ends the game.`; answer **Yes** — all remaining hiders turn green, log "Revealed all N hiders. Game over.", and the win box appears ~100 ms AFTER the confirm closes (two boxes in sequence, never stacked). Timer frozen at the win.
12. Post-win block (v2-specific, no v1 analog): Found hiders -> Hide found -> Show found -> Color -> another named color — EXPECT: all three still work in the won state.
13. OPTIONAL (G12, 17.1-14 defect — skip if pressed for time and record N/A): Demos -> load 1k8p, select it, click Start mid-round(won) — EXPECT: the round-2 original (1znf) is restored AND hidden; clicks land on the NEW 1k8p round (the pick-observer log lines show the new molid); the restored original never intercepts.
14. Paste 3: `pv_a_finish` — EXPECT: the final dump (state won, frozen timer label, remaining 0, counters, found showstates/colors = the post-win evidence) + instructions. Leg A: click "Cleanup model" — VERIFY G8: the original 1znf returns, labels reset ("Reveals: 0", "Remaining: -"), log clears, the Setup tab raises; the watcher auto-logs teardown #1. Leg B: click Start again (form unchanged), and once the countdown starts close the dialog window (X) — EXPECT: it closes silently and the same cleanup ran; the watcher auto-logs teardown #2 (cleanup-on-close).
15. While on the Setup tab, VERIFY 19-13: the Reset hint label is visible and readable (Reset restores the setup fields only; points to Cleanup model on the Game tab).
16. Record the verdict: the G1-G12 table item by item (pass/fail + which log line proves it), the desync branch taken, the post-win verdict, and any oddities; attach actions_verify_log.txt (expect 0 `ERROR)` / 0 `bad switch`).
  </how-to-verify>
  <resume-signal>Type "approved" or describe the failing items (which G item / which step).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Post-approval doc close-out (vmd/AGENTS.md Phase-19 note) + commit</name>
  <files>vmd/AGENTS.md</files>
  <action>
ONLY after the checkpoint verdict is recorded (APPROVED, or defects precisely recorded for gap closure):
1. Rewrite vmd/AGENTS.md's stale Phase-19 note (currently "the Game tab has NO Cleanup/Restart buttons yet (Phase 19 scope); until then cleanup is console-only"): the actions GUI shipped — Hint / Reveal one / Reveal all / Restart / Cleanup model + the Found-hiders and named-Color menubuttons + the "Reveals: N" label; reveals confirm v1-verbatim (cancel = true no-op); cleanup runs from the Game tab button AND the WM-close path; post-win found-mgmt/restart/cleanup stay usable; the desync verdict (LIVE trace fix landed, or DEFERRED recorded) — mirror the 19-08-SUMMARY wording; append the checkpoint date + APPROVED (17.1-14-SUMMARY's checkpoint-record style). Keep the note SHORT (the existing paragraph's length class) and keep the Setup-Reset sentence updated to point at the 19-13 in-panel hint.
2. Do NOT touch .planning/STATE.md, ROADMAP.md, or REQUIREMENTS.md (orchestrator/verifier own those).
3. Commit (single): `docs(19-17): record Phase-19 GUI checkpoint approval + refresh AGENTS Phase-19 note`.
  </action>
  <verify>`git log --oneline -1` shows the docs(19-17) commit; `git status --porcelain` clean except .planning/ artifacts; the AGENTS.md Phase-19 note no longer claims the buttons are missing; the SUMMARY contains the verdict table.</verify>
  <done>The checkpoint verdict is recorded with per-item evidence and the repo-facing doc reflects the shipped actions surface.</done>
</task>

</tasks>

<verification>
- Checkpoint APPROVED (or every failing G item precisely recorded with evidence for gap closure) — the last unverified surface of Phase 19.
- actions_verify_log.txt: 0 `ERROR)` / 0 `bad switch`, both teardown proofs present, the desync getter line present.
- All eight Phase-19 requirements (GAME-05/06/07/08/10, BTN-06, DIFF-01, DIFF-04) have their FINAL human-verified closure recorded; the 16-12 defects (desync, Setup-Reset mismatch) have recorded dispositions.
- vmd/AGENTS.md Phase-19 note refreshed; single docs(19-17) commit.
</verification>

<success_criteria>
The ONE consolidated checkpoint is APPROVED with the auto-log as evidence — Phase 19's GUI surface is fully verified end-to-end — and the repo docs reflect it.
</success_criteria>

<output>
After completion, create `.planning/phases/19-in-game-actions/19-17-SUMMARY.md` (carry the G1-G12 verdict table + the desync branch + the teardown-proof log lines + the post-win verdict, mirroring 17.1-14-SUMMARY's checkpoint-record shape)
</output>
