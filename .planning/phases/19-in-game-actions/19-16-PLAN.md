---
phase: 19-in-game-actions
plan: 16
type: execute
wave: 7
depends_on: ["19-08", "19-10", "19-14", "19-15"]
files_modified: [vmd/tests/actions_verify.tcl]
autonomous: true
# 19-08/19-10: the game_tab -textvariable names + dialog handler names the
# driver reads/calls. 19-14: the driver FAMILY + the pv_round5 division
# (reconciled below) + the definition-gate pattern. 19-15: the
# HEADLESS-GREEN gate — this plan may only run after it is recorded.

must_haves:
  truths:
    - "Sourcing vmd/tests/actions_verify.tcl in a real GUI session auto-stages the whole Phase-19 actions session: sources the extension, loads a FRESH 1znf, applies the 6-hider mixed-tier round (VDW 2 / Lines 2 / Licorice 2, easy), calls on_start, and auto-dumps state after the countdown — the human's pastes are exactly 3 short commands (source / pv_a_mid / pv_a_finish)"
    - "Every dump auto-logs OBJECTIVE evidence to actions_verify_log.txt: the ACTUAL on-screen label texts via the -textvariable namespace vars (reveal_text/remain_text/timer_text/mode_text), game_logic state + reveal_count/hint_count getters, registry counts, per-tier showrep getters + found-rep color read-backs (hiders::tier_reps + mol repindex), the ::vmd_mouse_mode/::vmd_mouse_submode getter line (the desync-verdict evidence), and the log_lines tail"
    - "The driver NEVER calls the confirm-bearing handlers — on_reveal_one/on_reveal_all appear ZERO times in the file (grep-pinned); the HUMAN clicks the real buttons and answers the real dialogs; driver-callable handlers are limited to on_hint/on_restart (non-confirm) and pv_a_mid guards the stash non-empty before calling on_restart so no info box can pop"
    - "pv_a_instructions LEADS with the press-p-ONCE first-click quirk advice (17.2-12/17.1-14 precedent; re-armed after every restart), and the driver auto-verifies BOTH teardown legs via a stash-emptiness watcher: Leg A = the human clicks the Game-tab Cleanup model button; Leg B = the human starts a throwaway round and closes the dialog (WM_DELETE -> on_cleanup flow, 19-10)"
    - "Headless the file is a clean no-op: Tk-guard warn line only under -dispdev text; under the fake-::tk_version + ::pv_probe knob all pv_a_* procs define; 0 ERROR) / 0 bad switch; Exiting normally"
    - "The division from 19-14's pv_round5 is documented in the driver header: pv_round5 = LIB-level auto-drive in rep_verify.tcl (confirm-free surfaces only, regression evidence); actions_verify.tcl = the REAL-GUI session driver (human clicks real buttons, answers real dialogs) that plan 19-17's checkpoint runs"
  artifacts:
    - path: "vmd/tests/actions_verify.tcl"
      provides: "Tk-guarded actions GUI auto-driver: pv_a_log / pv_a_observe / pv_a_state / pv_a_instructions / pv_a_mid / pv_a_instruct2 / pv_a_finish / pv_a_watch_teardowns / pv_a_cleanup (recovery) / pv_a_autostart, ::pv_probe knob, cwd-relative path fallback"
      min_lines: 400
      contains: "pv_a_mid"
  key_links:
    - from: "pv_a_autostart"
      to: "setup_tab::apply_state + ::biochemeleon::on_start (the REAL GUI path)"
      via: "crafted 6-hider state dict -> validate_state -> apply_state -> on_start"
      pattern: "apply_state"
    - from: "pv_a_state"
      to: "game_tab -textvariable vars + game_logic/registry getters"
      via: "catch-guarded fully-qualified var reads + 0-arg getter calls"
      pattern: "game_tab::reveal_text"
    - from: "pv_a_mid"
      to: "::biochemeleon::on_restart (real non-confirm handler, 19-10)"
      via: "stash-size guard THEN handler call -> fresh-round auto-dump"
      pattern: "biochemeleon::on_restart"
    - from: "the whole file"
      to: "on_reveal_one / on_reveal_all (the confirm-bearing handlers)"
      via: "NEGATIVE link: zero occurrences, grep-pinned each verification"
      pattern: "on_reveal_(one|all)"
---

<objective>
Build `vmd/tests/actions_verify.tcl` — the Phase-19 ACTIONS GUI-session driver (new file, rep_verify.tcl harness patterns, pv_a_* proc family): auto-stages a deterministic 6-hider round, auto-dumps label/counter/showrep/color/mouse-mode evidence, routes the human through exactly 3 pastes + real-button blocks, and auto-watches both teardown legs — so plan 19-17's ONE consolidated human-verify checkpoint has a driver that does everything except click buttons and answer dialogs.

Purpose: batch D's driver half (integration §5 W7 row). The headless suites/smokes (19-12..15) prove the lib + load surface; THIS driver makes the GUI session objective (auto-logged verdicts) and simple (16-16 standing directive). Headless-verified BEFORE the human session.
Output: `vmd/tests/actions_verify.tcl` + headless definition-gate evidence. NO other file touched.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vmd/AGENTS.md
@.planning/phases/19-in-game-actions/19-RESEARCH-gui.md
@.planning/phases/19-in-game-actions/19-RESEARCH-integration.md
@vmd/tests/rep_verify.tcl
@.planning/phases/19-in-game-actions/19-08-SUMMARY.md
@.planning/phases/19-in-game-actions/19-10-SUMMARY.md
@.planning/phases/19-in-game-actions/19-14-SUMMARY.md
@.planning/phases/19-in-game-actions/19-15-SUMMARY.md

Load-bearing facts (do NOT re-derive):
- rep_verify.tcl (637 lines) is the harness template: Tk-guard if-wrap whole body (one warn line in text mode; an if-wrap is ONE command in both source and -e evaluation), `[info script]`-empty-under--e path fallback to cwd-relative `vmd/biochemeleon.tcl`, `pv_log` (open-append + flush per line + vmdcon echo), pick observer trace on `::vmd_pick_event`, `after 4500` post-countdown auto-dump, `::pv_probe` headless definition knob, re-source resets counters (log FILE is append-only).
- DIVISION (reconcile with 19-14 — quote this in the driver header): 19-14's `pv_round5` (rep_verify.tcl) drives LIB-LEVEL actions (`game::reveal_one/reveal_all`, `hiders::set_found_*`) plus the real non-confirm handlers — no dialog can ever appear, so it is usable as a hands-free regression round. `actions_verify.tcl` is the REAL-GUI session driver for 19-17's checkpoint: the human clicks the physical buttons and answers the real confirms; the driver only stages state, reads it back, and logs. Different file, own `pv_a_*` prefix (no shadowing even if both files are sourced in one session), zero edits to rep_verify.tcl.
- Widget-var names (19-08/19-09, game_tab namespace): `reveal_text` ("Reveals: N"), `remain_text`, `timer_text`, `mode_text`, `mouse_mode`, all `-textvariable`-bound — read the ACTUAL on-screen text via `set ::biochemeleon::game_tab::<var>` (catch-guarded: unset pre-build). The round stash is `::biochemeleon::game_tab::game_state` (dict; size 0 when absent/cleaned — 19-10's on_cleanup step 5 empties it).
- Handler names (19-10, dialog scope): `on_hint` / `on_restart` are confirm-free (driver-callable, but the session still instructs REAL clicks); `on_reveal_one` / `on_reveal_all` open blocking `tk_messageBox` (NEVER driver-callable — a driver call would stall the console mid-paste); `on_found_mgmt {hide|show|recolor}` / `on_found_color {colorid}` (menu-dispatch, no dialogs on the happy path).
- Getters (lib seams): `game_logic::state` / `reveal_count` / `hint_count` / `log_append` kinds via `log_lines`-style tail reads (19-02 — check 19-02-SUMMARY for the exact tail accessor name and use it), `registry::count_remaining` / `hidden_indices` / `found_indices` (19-01), `hiders::tier_reps` + `found_colorid` + `set_found_visible/set_found_color` (19-04), `game::restart` (19-05), demo load via `demos::load_demo 1znf` + `demos::atom_count`.
- Crafted round (research §6.2 paste-1): 1znf (PROTEIN — hints need real neighbors; 1k8p is DNA-only), `dict create target_mode loaded selected_object $m hider_count 6 per_rep [dict create VDW 2 Lines 2 Licorice 2] lock_scene 0 difficulty_easy 1 demo_id 1znf` — NO material keys (post-18 that shape still validates, 19-14 precedent).
- Desync verdict (19-08-SUMMARY): the dumps record `::vmd_mouse_mode` + `::vmd_mouse_submode` + the panel's `mouse_mode`/`mode_text` EITHER WAY; the INSTRUCTIONS branch on the verdict (LIVE: press r -> radios flip -> re-pick via radio + p; DEFERRED: press r -> record the desync as known 16-12 behavior -> re-pick via radio + p).
- Tcl 8.5.6 ONLY (no lmap/try/dict-get-default; one `variable` per line; braced expr; `::tk_version` qualifier); NO `grab set`; NO `mouse` commands (reading `::vmd_mouse_mode` is a var read, not a mouse command); NO ttk::combobox; NO tk_chooseColor; every mol/molinfo call catch-guarded (molids go stale).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the actions_verify.tcl driver (pv_a_* family, 3-paste session, teardown watcher)</name>
  <files>vmd/tests/actions_verify.tcl</files>
  <action>
Model on rep_verify.tcl's structure 1:1 (copy the harness patterns; rebuild ALL game-state reads on the Phase-19 seams above). Header comment must state: purpose (19-17's consolidated checkpoint driver), the DIVISION vs 19-14's pv_round5 (lib-level drive vs real-GUI drive), the 3-paste shape, the log file name, and the headless knob. Then:

1. Tk-guard if-wrap: `if {![info exists ::tk_version]} { vmdcon -warn {actions_verify.tcl is a GUI-only auto-driver ... nothing to do in -dispdev text mode.} } else { ...whole body... }`.
2. Path resolution at file top level INSIDE the guard (script-relative, then cwd-relative `vmd/biochemeleon.tcl` fallback) — rep_verify's exact block, adjusted variable names.
3. `pv_a_log {msg}` — timestamped append to `[file join [pwd] actions_verify_log.txt]` (open-append, flush per line) + vmdcon -info echo. ALL driver output goes through it.
4. `pv_a_observe {args}` — pick observer: `trace add variable ::vmd_pick_event write pv_a_observe` (remove-then-add idempotent registration), logging mol/index per pick (rep_verify's pv_observe format). This is the G12/G4 pick-attribution evidence.
5. `pv_a_state {}` — THE dump (every line catch-guarded, auto-stash `::pv_a_gs` from the game_tab stash): (a) labels line: reveal_text / remain_text / timer_text / mode_text / mouse_mode via the fully-qualified vars; (b) desync getter line: `::vmd_mouse_mode` + `::vmd_mouse_submode` + panel mouse_mode/mode_text; (c) counters: `game_logic::state`, `reveal_count`, `hint_count`; (d) registry: `count_remaining`, sizes of `hidden_indices`/`found_indices`; (e) game molecule: molid from the stash, atom count, numreps; (f) per-tier loop over `hiders::tier_reps` -> `mol repindex` (-1 guard) -> `molinfo get "{style $i} {selection $i} {color $i}"` + `mol showrep` getter; (g) `hiders::found_colorid` getter; (h) log tail (the accessor 19-02 shipped — last 3 lines); (i) stash a baseline `::pv_a_orig_atoms` (original molecule atom count, captured at autostart) for the teardown proofs.
6. `pv_a_autostart {}` — runs on source (rep_verify's pv_autostart skeleton): source the extension if absent -> load 1znf if absent (scan `molinfo list` filenames first like rep_verify, but ALWAYS prefer a FRESH load for the round target — never reuse a possibly-mutated molid) -> `open_dialog` -> crafted 6-hider state -> `validate_state` (atom_count) -> `apply_state` -> register the pick observer -> `::biochemeleon::on_start` -> `set ::pv_a_rounds 1` -> `after 4500 pv_a_state` -> `pv_a_instructions`.
7. `pv_a_instructions {}` — <= 8 short lines, LEADING with the quirk: "Press `p` ONCE now (arms pick delivery — the locked first-click quirk; press p again after every restart)." Then the human block 1: find ONE hider (greens); Hint twice (orange AROUND a hider, accumulates); Reveal one -> No (nothing may change) then Yes (a random hider greens, "Reveals: 1"); Found hiders -> Hide found -> Show found; Color -> pick a NAMED color (all found recolor) then find one more (renders in the NEW color); the desync step per the 19-08 verdict (both branches spelled out, ending "click the Pick radio, press p once more"); "Then paste: pv_a_mid".
8. `pv_a_mid {}` — guard: stash size > 0 else log a bail line and return (NEVER pop on_restart's "No game round" info box). Then: `pv_a_state` with a MID marker; `::biochemeleon::on_restart` (real handler, no confirm) -> `catch {unset ::pv_a_gs}` -> `after 4500 pv_a_state` -> `after 5300 pv_a_instruct2`.
9. `pv_a_instruct2 {}` — log the fresh-round expectation line (new molid, hider_count 6, Reveals: 0, timer re-armed, found_colorid PERSISTS — the chosen color carries into round 2), then instructions: "Press `p` once. Click Reveal all -> Yes (the v1-verbatim confirm -> all remaining green -> 'Revealed all N hiders. Game over.' -> the win box ~100 ms AFTER the confirm closes — two boxes in sequence, never stacked). Then, still in the won state: Found hiders -> Hide found -> Show found -> Color -> another named color (post-win found-mgmt — v2 behavior, no v1 analog). Then paste: pv_a_finish."
10. `pv_a_finish {}` — final dump (state won, frozen timer label, remaining 0, counters, per-tier found-rep showstates + color read-backs = the post-win evidence); instructions: "Leg A: click the Game-tab 'Cleanup model' button (original returns, labels reset, Setup tab raises). Leg B: click Start again (form unchanged), and once the countdown starts close the dialog window (X) — the same cleanup runs silently. Watch the auto-log for the two teardown proofs." Arm `after 2000 pv_a_watch_teardowns`; print the log path + "record your verdicts".
11. `pv_a_watch_teardowns {}` — poll `[dict size $::biochemeleon::game_tab::game_state]` (catch-guarded; missing var = empty). On a nonempty->empty transition: `incr ::pv_a_teardowns`; dump the teardown proof: scan `molinfo list` for the old game molid (GONE) vs the restored original (present, `molinfo get displayed` + atom count == `::pv_a_orig_atoms`); log `teardown N proof`. After teardown 2: log `TEARDOWN-COMPLETE` and STOP (return without re-scheduling). Cap at 90 polls (3 min) with a give-up line. Handle the G12 optional path gracefully: if the human ran the different-target check, extra molecule churn is fine — the poller keys ONLY on stash emptiness transitions.
12. `pv_a_cleanup {}` — RECOVERY helper only (never auto-called): bridge deactivate catch -> `game::cleanup` via the live stash catch -> `pv_a_state` post-dump. For a failed leg; no dialogs.
13. `::pv_probe` knob: skip autostart, print the proc count (rep_verify parity). Re-source discipline: counters reset, log file append-only, observer re-registered.
14. Comment hygiene everywhere: no lowercase `mouse mode `/`vwait`/`grab set` literals; no 8.6-idiom words (comments included); no ttk::combobox; no tk_chooseColor.
  </action>
  <verify>
Static: `grep -cE "on_reveal_one|on_reveal_all" vmd/tests/actions_verify.tcl` == 0; `grep -c "grab set" vmd/tests/actions_verify.tcl` == 0; `grep -cE "(^|[ ;{])mouse " vmd/tests/actions_verify.tcl` == 0; 8.6-idiom grep == 0; ttk::combobox + tk_chooseColor greps == 0; tclsh parse: `printf 'set fh [open vmd/tests/actions_verify.tcl r]; set d [read $fh]; close $fh; if {![info complete $d]} {puts INCOMPLETE; exit 1}; puts PARSE-OK' | tclsh` -> PARSE-OK.
True-headless no-op (fresh staging `tmp/p19-16/`, copy of the vmd/ tree): `bash -ic 'cd tmp/p19-16 && vmd -dispdev text -e vmd/tests/actions_verify.tcl -eofexit < /dev/null'` -> exactly ONE warn line, no dialog attempt, 0 `ERROR)` / 0 `bad switch`, `Exiting normally`.
Definition-gate (19-14's pattern; staging-only script, gitignored): `set ::tk_version 8.5.6; set ::pv_probe 1; source vmd/tests/actions_verify.tcl; if {[lsearch [info procs pv_a_*] pv_a_mid] < 0} { error "pv_a_mid missing" }; vmdcon -info "PVDEF-A procs=[llength [info procs pv_a_*]]"; exit` -> PVDEF-A line prints with all 10 procs, 0 `ERROR)` / 0 `bad switch`, `Exiting normally`. VMD runs SEQUENTIAL (R5: shared $env(TEMP)).
  </verify>
  <done>The driver exists with the full pv_a_* family, leads its instructions with the p-press quirk, never touches the confirm-bearing handlers, and passes the true-headless + definition gates headlessly.</done>
</task>

<task type="auto">
  <name>Task 2: Division cross-note consistency check + commit</name>
  <files>vmd/tests/actions_verify.tcl</files>
  <action>
1. Read the driver header + pv_a_instructions once more against the division contract: the header names pv_round5/rep_verify.tcl as the LIB-level drive and this file as the REAL-GUI checkpoint drive; pv_a_instructions' step text matches 19-17's how-to-verify steps (same order, same expected outcomes) — 19-17 is authored FROM this driver, so fix any drift NOW in this file (the checkpoint plan quotes it).
2. Confirm the file is standalone: no `source ...rep_verify` line, no pv_* (non-a) proc calls, no edits implied to any other file.
3. Commit (single): `test(19-16): add actions_verify.tcl GUI-session driver for the Phase-19 checkpoint`.
  </action>
  <verify>`git status --porcelain` shows ONLY vmd/tests/actions_verify.tcl (+ the gitignored staging); `git log --oneline -1` shows the test(19-16) commit; re-run the definition-gate after any drift fix.</verify>
  <done>Driver committed, division documented, instructions aligned with the checkpoint plan, repo otherwise untouched.</done>
</task>

</tasks>

<verification>
- Definition-gate headless run: all 10 pv_a_* procs defined, PVDEF-A marker, 0 `ERROR)` / 0 `bad switch`, `Exiting normally`; true-headless run = one warn line only.
- Static: 0 on_reveal_one/on_reveal_all; 0 grab set; 0 mouse commands; 0 8.6 idioms; 0 combobox/tk_chooseColor; tclsh info complete PARSE-OK.
- Division note present (pv_round5 = lib-level, actions_verify = real-GUI); paste budget = source / pv_a_mid / pv_a_finish (3).
- `git status` clean except the new file; single `test(19-16):` commit; rep_verify.tcl byte-untouched.
</verification>

<success_criteria>
The 19-17 checkpoint has a self-staging, auto-logging driver whose human budget is 3 short pastes + real clicks/dialog answers, whose dumps carry the desync getter and post-win/teardown evidence, and which provably never spawns a dialog the human did not click into existence.
</success_criteria>

<output>
After completion, create `.planning/phases/19-in-game-actions/19-16-SUMMARY.md` (carry the definition-gate evidence, the proc inventory, the EXACT paste names + the instruction-line texts — 19-17's checkpoint steps quote them)
</output>
