---
phase: 19-in-game-actions
plan: 10
type: execute
wave: 3
depends_on: ["19-04", "19-05"]
files_modified: [vmd/gui/dialog.tcl]
autonomous: true

must_haves:
  truths:
    - "Hint runs confirm-free behind silent no-op guards (no stash / not playing / zero remaining) — a dialog never appears for a dead action (GAME-05)"
    - "Reveal one / Reveal all guard FIRST, then show the v1-verbatim yesno confirms; No returns with ZERO side effects — counter, registry, log, viewer all unchanged (GAME-06/07)"
    - "Restart is a thin wrapper (guard -> bridge down -> game::restart -> start_round) with NO confirm — same-count REPLAY semantics, works from playing AND won states (GAME-10)"
    - "Cleanup: guard -> timers+bridge(+trace) down -> game::cleanup -> game_tab::end_round -> GUI stash cleared -> Setup tab raised (BTN-06); the Game-tab button is the only Cleanup surface (Setup duplicate rejected)"
    - "WM_DELETE runs the SAME cleanup flow when a round stash exists and closes silently when none (16-14 option-b adoption, decision 4)"
    - "Found-mgmt handlers no-op silently with no round or zero found, stay usable in the won state, and route to hiders::set_found_visible / set_found_color (GAME-08/DIFF-04 handler half)"
  artifacts:
    - path: "vmd/gui/dialog.tcl"
      provides: "on_hint / on_reveal_one / on_reveal_all / on_restart / on_cleanup / on_found_mgmt / on_found_color + _game_stash helper + on_close adoption"
      min_lines: 380
      contains: "Give up on one random hider? This counts as a reveal use."
  key_links:
    - from: "on_reveal_one / on_reveal_all"
      to: "game::reveal_one / reveal_all $gs (19-05 explicit-gs seam)"
      via: "tk_messageBox -type yesno confirm THEN lib call; cancel returns BEFORE any lib call"
      pattern: "tk_messageBox -type yesno"
    - from: "on_hint"
      to: "game::hint $gs (19-05)"
      via: "guards -> call; NO confirm (hint is help, never a give-up gate)"
      pattern: "game::hint"
    - from: "on_restart"
      to: "game::restart $gs -> game_tab::start_round $gs2"
      via: "thin-wrapper flow (research 3.5): the new round re-arms at GO"
      pattern: "game::restart"
    - from: "on_cleanup"
      to: "game_tab::end_round (19-08) + game::cleanup"
      via: "timers/bridge down BEFORE state teardown; shared view/model reset after"
      pattern: "game_tab::end_round"
    - from: "on_found_mgmt / on_found_color"
      to: "hiders::set_found_visible / set_found_color / found_colorid (19-04 seam)"
      via: "dialog-scope dispatch matching the 19-09 menu entries"
      pattern: "set_found_color"
    - from: "on_close"
      to: "on_cleanup (stash-conditional) + game_tab::unregister_mode_trace (catch-guarded)"
      via: "16-14 deferred decision option (b)"
      pattern: "unregister_mode_trace"
---

<objective>
Build the DIALOG-scope handler half of the GUI batch (the BTN-07 fan-in pattern): on_hint, on_reveal_one + on_reveal_all (v1-verbatim yesno confirms), on_restart, on_cleanup (shared with WM_DELETE per decision 4), and the found-mgmt pair on_found_mgmt/on_found_color. All guards/confirms live HERE; the lib procs stay confirm-free and state-gated (R2/R3); the Game-tab widgets (19-08/19-09) dispatch to these exact names.

Purpose: the callback half of GAME-05/06/07/08/10, BTN-06, DIFF-04. File is DISJOINT from game_tab.tcl -> parallel-safe with 19-08 in wave 3 via the pinned contract.
Output: extended dialog.tcl, headless-load-clean via the entry smoke.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-in-game-actions/19-RESEARCH-gui.md
@.planning/phases/19-in-game-actions/19-RESEARCH-integration.md
@vmd/gui/dialog.tcl
@vmd/gui/game_tab.tcl
@.planning/phases/19-in-game-actions/19-04-SUMMARY.md
@.planning/phases/19-in-game-actions/19-05-SUMMARY.md

Load-bearing research: GUI research §2.3 (confirm copy VERBATIM: Reveal one = title `Reveal one hider?` body `Give up on one random hider? This counts as a reveal use.`; Reveal all = title `Reveal all hiders?` body `Give up and reveal ALL remaining hiders? This ends the game.`; Hint has NO confirm; Restart has NO confirm; guard-before-confirm — handlers return silently BEFORE any dialog for a dead action), §2.5 (silent no-op guards, not enable/disable; reveal-all counts +N hiders not +1 action — lib-owned), §3.3 (per-req behavior contracts incl. post-win: found-mgmt/Restart/Cleanup stay usable in won state — reveals/hints gate to playing), §4.1 (tk_messageBox -type yesno returns exactly yes/no — Tk-doc-verified, FIRST exercise in v2; modal boxes block dialog interaction while up but `after` timers KEEP TICKING — expected, v1 parity, not a bug; no modal nesting is reachable: handlers return from the dialog before the lib call, the win box is delayed 100 ms; cancel paths are true no-ops by construction), §5 (desync: the trace fix lives in game_tab; on_close must catch-guard its unregister — verdict-safe in both branches), §7.6 (post-win found-mgmt is a v2 behavior with no v1 analog — extend the won-state allowance to found-mgmt). Integration §3.0 (fan-in shape: guards + confirm + catch -> warning box + return; lib calls take the EXPLICIT gs from the game_tab stash), §3.5 (restart flow: guard stash -> deactivate -> game::restart -> start_round; cleanup flow: guard -> stop timers + deactivate BEFORE teardown -> game::cleanup -> end_round -> raise Setup tab; cleanup-on-close option (b) — on_close calls the factored flow catch-guarded), §4.5 (explicit-gs signatures: handlers pass the stash dict). 19-05/19-04 SUMMARYs: the consumed proc names/signatures. Tcl 8.5.6: catch-based error handling, one `variable` per line, brace expr, comment hygiene (8.6-idiom words banned in comments too).

CONTRACT (pinned — the 19-08/19-09/19-10/19-11 interlock; parallel executors MUST NOT diverge):
- This plan DEFINES (dialog scope, ::biochemeleon::): `on_hint`, `on_reveal_one`, `on_reveal_all`, `on_restart`, `on_cleanup` (all 0-arg), `on_found_mgmt {action}` (action in hide|show|recolor), `on_found_color {colorid}`, plus the PRIVATE helper `_game_stash {}` (returns the game_tab stash dict, empty dict when absent/empty — NOT exported).
- 19-08's buttons -command literals and 19-09's menu -command lists dispatch to EXACTLY these names; 19-11's entry smoke asserts their existence.
- 19-08 exports `end_round` (always) and `unregister_mode_trace` (ONLY if 19-08's desync probe verdict was LIVE — this plan's calls to it are catch-guarded so both branches are safe).
- The GUI stash `::biochemeleon::game_tab::game_state` is read via the fully-qualified name (dialog scope cannot `variable` another namespace's var).
</context>

<tasks>

<task type="auto">
  <name>Task 1: _game_stash helper + on_hint / on_reveal_one / on_reveal_all</name>
  <files>vmd/gui/dialog.tcl</files>
  <action>
1. Private helper after on_start (same comment style, numbered steps):
```tcl
# ---------------------------------------------------------------------------
# _game_stash -- PRIVATE helper for the Phase-19 action handlers: returns the
# Game tab's round-state stash (dict) or an EMPTY dict when no round exists
# (never sourced / already cleaned). Callers test [dict size] == 0.
# ---------------------------------------------------------------------------
proc ::biochemeleon::_game_stash {} {
    if {[info exists ::biochemeleon::game_tab::game_state]
            && [dict size $::biochemeleon::game_tab::game_state] > 0} {
        return $::biochemeleon::game_tab::game_state
    }
    return [dict create]
}
```
2. `proc ::biochemeleon::on_hint {}`: `variable w`; `set gs [_game_stash]`; guard 1 `if {[dict size $gs] == 0} { return }`; guard 2 `if {[::biochemeleon::game_logic::state] ne "playing"} { return }`; guard 3 `if {[::biochemeleon::registry::count_remaining] == 0} { return }` — ALL silent no-ops BEFORE anything else (v1 gui_game.py:142-167 guard-before-confirm parity; during countdown the handler no-ops before any dialog). Then `if {[catch {::biochemeleon::game::hint $gs} err]} { tk_messageBox -parent $w -icon warning -title "bioCHEMeleon" -message "Could not compute hint: $err" }`. NO confirm (v1 decision: hint is help, never a give-up gate). The log line + hint counter flow through the lib's callbacks — no GUI re-render here (remaining is unchanged).
3. `proc ::biochemeleon::on_reveal_one {}`: the SAME three guards, THEN the v1-verbatim confirm (gui_game.py:154-155):
```tcl
    set ans [tk_messageBox -parent $w -type yesno -icon question \
        -title "Reveal one hider?" \
        -message "Give up on one random hider? This counts as a reveal use."]
    if {$ans ne "yes"} { return }
    if {[catch {::biochemeleon::game::reveal_one $gs} err]} {
        tk_messageBox -parent $w -icon warning -title "bioCHEMeleon" \
            -message "Could not reveal: $err"
    }
```
   Comment: `-type yesno` returns exactly `yes`/`no` (Tk 8.5 doc) — FIRST exercise in v2, verified at the Phase-19 GUI checkpoint; anything non-yes is treated as cancel (defensive); CANCEL IS A TRUE NO-OP — the return happens BEFORE any lib call, so counter/registry/log/viewer are untouched (asserted in the session, G3/G10).
4. `proc ::biochemeleon::on_reveal_all {}`: same shape; confirm VERBATIM (gui_game.py:164-165): title `Reveal all hiders?`, message `Give up and reveal ALL remaining hiders? This ends the game.`; then `game::reveal_all $gs` (the +N counting, single win flow and `Game over.` log are lib-owned). Comment: the win box appears ~100 ms AFTER this handler returns (on_win's tracked one-shot) — two boxes in sequence, never stacked; the modal confirm does NOT block the 1 Hz timer (expected, v1 parity — deliberating over a give-up costs time).
5. Shared comment header for the trio: guards gate on state `playing` (the "dialog during countdown" state is unreachable); the LIB procs are confirm-free and state-gated themselves (R2/R3 defense-in-depth) — the confirm lives ONLY here; error paths use the established warning-box idiom + return (no partial state).
   Tcl 8.5 only; comment hygiene (no 8.6-idiom words, no `grab set` literal).
  </action>
  <verify>
Headless entry load-gate on a FRESH staging copy (`tmp/p19-10/`): `bash -ic 'cd tmp/p19-10 && vmd -dispdev text -e vmd/smoke/phase16_entry_smoke.tcl -eofexit < /dev/null'` -> PASS=1 (dialog.tcl sources via the entry; the new procs parse), full-log scan 0 `ERROR)` / 0 `bad switch`, `Exiting normally`. Static: the three handler names defined; the two verbatim confirm strings + titles present EXACTLY; `grab set` grep vmd/gui/ = 0 (tk_messageBox's internal grab is not a grab-set call); 8.6-idiom grep = 0.
  </verify>
  <done>Hint/reveal handlers defined with guard -> confirm -> lib-call order, v1 copy verbatim, cancel paths side-effect-free.</done>
</task>

<task type="auto">
  <name>Task 2: on_restart / on_cleanup + WM_DELETE adoption (decision 4)</name>
  <files>vmd/gui/dialog.tcl</files>
  <action>
1. `proc ::biochemeleon::on_restart {}`: `variable w`; 1. `set gs [_game_stash]`; `if {[dict size $gs] == 0} { tk_messageBox -parent $w -icon info -title "bioCHEMeleon" -message "No game round to restart."; return }` (research 3.5 step 1 — NEW copy, not v1-verbatim: v1's Restart had no such guard). 2. NO confirm (v1 parity, locked). 3. `catch {::biochemeleon::pick_bridge::deactivate}` (16-14 step-3.5 precedent — the new round re-arms the bridge at GO). 4. `if {[catch {::biochemeleon::game::restart $gs} gs2]} { tk_messageBox -parent $w -icon warning -title "bioCHEMeleon" -message "Could not restart the game: $gs2"; return }`. 5. `::biochemeleon::game_tab::start_round $gs2` (which does stop_all_timers + the shared view reset incl. "Reveals: 0" + callbacks + round_reset + fresh countdown). Comment: thin wrapper over game::restart — SAME-COUNT REPLAY semantics (16-13-pinned): the stashed round's hider_count/per_rep replay with re-randomized positions; NOT a form re-derive (that is the 16-13 Start-guard's job). Works from playing AND won states (the v2 molecule survives a win — post-win usability, locked decision 7); a restart during countdown rides start_round's defensive timer-cancel (v1 "Restart mid-game" parity). No end_round call here — start_round covers the view reset.
2. `proc ::biochemeleon::on_cleanup {}`: `variable w`; 1. `set gs [_game_stash]`; empty -> `tk_messageBox -parent $w -icon info -title "bioCHEMeleon" -message "No game round to clean up."; return` (NEW copy, flagged). 2. Teardown BEFORE state work (the on_close order): `::biochemeleon::game_tab::stop_all_timers` + `catch {::biochemeleon::pick_bridge::deactivate}` + `catch {::biochemeleon::game_tab::unregister_mode_trace}` (the last is verdict-safe: the proc may not exist if 19-08's desync probe deferred the fix — catch swallows). 3. `if {[catch {::biochemeleon::game::cleanup $gs} restored]} { tk_messageBox -parent $w -icon warning -title "bioCHEMeleon" -message "Could not clean up the game: $restored"; return }` (on failure the stash is KEPT so a retry stays possible). 4. `::biochemeleon::game_tab::end_round` (the 19-08 shared reset: labels + "Reveals: 0" + log clear + round_reset -> state idle). 5. `set ::biochemeleon::game_tab::game_state [dict create]` — the GUI stash is STALE after cleanup (the game molecule is gone); clearing it makes the restart/cleanup/found-mgmt guards honest; game.tcl's own current_state (the 16-13 guard's concern) is untouched. 6. `catch {$w.nb select $w.nb.setup}` — raise the Setup tab (notebook SELECT, never raise on the child; mechanisms Open Q5 recommendation; both this and the close path run it, catch-guarded).
3. on_close extension (edit the existing proc): AFTER the existing `stop_all_timers` + `pick_bridge::deactivate` catch lines and BEFORE `destroy $w`, insert:
```tcl
    # Phase 19 (16-14 deferred decision, option b): when a round's stash
    # exists, run the SAME flow the Game-tab Cleanup button runs -- the game
    # molecule must not linger loaded after close. Catch-guarded: a failed
    # cleanup must not block the close (its error box shows, then the
    # destroy proceeds). No message box when no round exists -- a fresh
    # dialog closes silently.
    if {[dict size [::biochemeleon::_game_stash]] > 0} {
        catch {::biochemeleon::on_cleanup}
    }
    catch {::biochemeleon::game_tab::unregister_mode_trace}
```
   (The repeated stop_all_timers/deactivate inside on_cleanup are idempotent; the raise-Setup step is internally catch-guarded and harmless during close.)
  </action>
  <verify>
Entry load-gate again on the staging copy: PASS=1, 0 `ERROR)` / 0 `bad switch`, `Exiting normally`. Static: on_restart/on_cleanup defined; on_close contains the stash-conditional on_cleanup call + the catch-guarded unregister_mode_trace line; grab/8.6 gates 0.
  </verify>
  <done>Restart is a thin same-count-replay wrapper; Cleanup follows the research flow end-to-end; WM_DELETE adopts the cleanup flow stash-conditionally and closes silently with no round.</done>
</task>

<task type="auto">
  <name>Task 3: on_found_mgmt / on_found_color — the 19-09 menu interlock (GAME-08/DIFF-04 handler half)</name>
  <files>vmd/gui/dialog.tcl</files>
  <action>
1. `proc ::biochemeleon::on_found_mgmt {action}`: `variable w`; 1. `set gs [_game_stash]`; `if {[dict size $gs] == 0} { return }` (SILENT — v1 no-op-guard convention, no dialog). 2. `if {[llength [::biochemeleon::registry::found_indices]] == 0} { return }` (SILENT — zero found; also covers the countdown state where nothing is found yet). NO state gate on playing — found-mgmt stays usable in the WON state (post-win the molecule survives; v2 behavior with no v1 analog — the GUI session tests it). 3. `set molid [dict get $gs game_molid]`; dispatch:
   - `hide` -> `::biochemeleon::hiders::set_found_visible $molid 0`
   - `show` -> `::biochemeleon::hiders::set_found_visible $molid 1`
   - `recolor` -> `::biochemeleon::hiders::set_found_color $molid [::biochemeleon::hiders::found_colorid]` (re-apply the CURRENT preference)
   - anything else -> return (defensive).
   All three calls inside `if {[catch {...} err]} { tk_messageBox -parent $w -icon warning -title "bioCHEMeleon" -message "Could not update found hiders: $err" }`.
   Comment: the hide/show semantics are the 19-04 narrowed NEVER-HIDE exception (only FOUND reps toggle; hidden reps stay pickable); a NEW find while found reps are hidden renders into the hidden found rep — documented v2 behavior (R7 probe-closed), the dropdown state persists across finds.
2. `proc ::biochemeleon::on_found_color {colorid}`: `variable w`; 1. `set gs [_game_stash]`; empty -> return (SILENT — with no live round there is no molecule to mutate). 2. `if {![string is integer -strict $colorid]} { return }` (defensive; the lib re-validates 0..32 and hard-errors otherwise). 3. `if {[catch {::biochemeleon::hiders::set_found_color [dict get $gs game_molid] $colorid} err]} { tk_messageBox -parent $w -icon warning -title "bioCHEMeleon" -message "Could not set the found color: $err" }`. Comment: unlike on_found_mgmt this handler does NOT require found hiders — set_found_color records the found_colorid PREFERENCE even with zero found (a legal pre-round call per 19-04), so future finds inherit the choice; NO tk_chooseColor exists anywhere (locked decision 1 — the palette menu is the whole DIFF-04 UI); the choice persists across rounds/restarts/cleanup.
3. Both handlers' dispatch arg shapes MUST match 19-09's menu commands exactly (`hide|show|recolor` strings; integer colorid) — this is the pinned interlock.
  </action>
  <verify>
Entry load-gate again: PASS=1, 0 `ERROR)` / 0 `bad switch`, `Exiting normally`. Static: on_found_mgmt/on_found_color defined; `set_found_visible`/`set_found_color`/`found_colorid` referenced; the hide/show/recolor branches present; grab/8.6 gates 0.
  </verify>
  <done>Found-mgmt + color handlers defined at dialog scope, matching the 19-09 menu dispatch exactly, silent-guarded, won-state-usable, routed to the 19-04 lib procs.</done>
</task>

</tasks>

<verification>
- phase16_entry_smoke.tcl PASS=1 on the fresh staging copy; full-log scan 0 `ERROR)` / 0 `bad switch`; `Exiting normally` (the entry sources dialog.tcl -> all new procs parse; the Phase-15/16 regression steps stay green untouched).
- 8.6-idiom grep over vmd/lib/ vmd/gui/ = 0; `grab set` grep over vmd/gui/ = 0; `tk_chooseColor` grep vmd/gui/ = 0.
- The two v1 confirm strings appear EXACTLY once each; cancel paths return before any lib call (code-order inspection).
- Single commit: `feat(19-10): dialog-scope action handlers + cleanup-on-close adoption`.
</verification>

<success_criteria>
All seven dialog-scope handlers exist with the pinned names and shapes the parallel widget plans dispatch to; confirms are v1-verbatim and cancel-safe; restart/cleanup follow the research flows including the WM_DELETE adoption; the lib stays confirm-free (headless-drivable) with the GUI owning all guards + confirms.
</success_criteria>

<output>
After completion, create `.planning/phases/19-in-game-actions/19-10-SUMMARY.md`
</output>
