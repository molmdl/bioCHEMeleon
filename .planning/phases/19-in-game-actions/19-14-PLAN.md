---
phase: 19-in-game-actions
plan: 14
type: execute
wave: 5   # orchestrator-assigned; see the R6 HARD GATE note below
depends_on: ["19-12", "18-13"]   # 18-13 = the R6 hard gate (pv_round4 landed; Phase-18 plan ID)
files_modified: [vmd/tests/rep_verify.tcl]
autonomous: true
# R6 HARD GATE (integration §6): rep_verify.tcl is READ BY LIVE GUI SESSIONS.
# These edits may land ONLY after 18-13's pv_round4 has landed in this file
# AND no Phase-17.2/18 GUI-checkpoint session is still reading it. If, at
# execution time, 18-13 is unexecuted or a live session is open on this
# file, STOP and wait — do not edit.

must_haves:
  truths:
    - "One more paste (pv_round5) auto-drives the ACTIONS round in the live GUI session: hint via the REAL on_hint handler, reveal-one + reveal-all via the confirm-free LIB surface (game::reveal_one/reveal_all — the driver NEVER calls the confirm-bearing handlers), found-mgmt toggles/recolor via the lib surface, restart via the REAL on_restart handler — the human never answers a dialog"
    - "Every step's evidence is AUTO-LOGGED: reveal/hint counters, showrep getter states, found-rep color read-backs, registry hidden/found deltas, the log-model tail, and the restart's counter zeroing + fresh-round re-stash — the session verdict cites log lines, not human memory"
    - "The final reveal-all WINS the restarted round so the session ends report -> cleanup, and ::pv_gs is refreshed across the restart (the 17.2-12 stale-stash lesson)"
    - "The session stays simple: the round-5 segment costs the human ≤3 short pastes (pv_round5, pv_report, pv_cleanup) and pv_instructions still LEADS with the first-click p-press quirk"
  artifacts:
    - path: "vmd/tests/rep_verify.tcl"
      provides: "pv_round5 / pv_round5_actions / pv_round5_finish procs, pv_state counter lines, pv_report counters, pv_round3 handoff + instructions + header-steps updates"
      min_lines: 740
      contains: "pv_round5"
  key_links:
    - from: "pv_round5_actions"
      to: "game::reveal_one/reveal_all $gs (19-05 confirm-free lib seam)"
      via: "lib-level reveal drives — confirms stay GUI-only (R3 split)"
      pattern: "game::reveal_(one|all)"
    - from: "pv_round5_actions / pv_round5_finish"
      to: "::biochemeleon::on_hint / on_restart (19-10 non-confirm handlers)"
      via: "the REAL button call chains for the no-confirm actions"
      pattern: "biochemeleon::on_(hint|restart)"
    - from: "pv_round5_actions"
      to: "hiders::set_found_visible / set_found_color (19-04)"
      via: "lib-level found-mgmt with showrep getter + color read-back logging"
      pattern: "set_found_(visible|color)"
    - from: "pv_state / pv_report"
      to: "game_logic::reveal_count / hint_count (19-02)"
      via: "catch-guarded counter lines appended to every auto-dump + the session report"
      pattern: "game_logic::(reveal_count|hint_count)"
---

<objective>
Add the ACTIONS round to the rep_verify GUI auto-driver: `pv_round5` (a fresh 3-hider VDW round) whose follow-up procs AUTO-DRIVE hint → reveal-one → found-mgmt → restart → reveal-all win, logging the reveal counter and showrep/getter state at every step; plus the counter lines in pv_state/pv_report and the handoff/instructions updates that keep the session at ≤3 pastes per segment.

Purpose: the 19-16/17 GUI checkpoint's driver half (integration §5 W5 row) — the ONE consolidated human session validates the actions end-to-end (real handlers where no confirm is needed, lib surface where the GUI owns a confirm) with auto-logged evidence, per the 16-16 standing directive.
Output: extended rep_verify.tcl; headless definition-gate green; NO other file touched.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-in-game-actions/19-RESEARCH-integration.md
@.planning/phases/19-in-game-actions/19-RESEARCH-mechanisms.md
@.planning/phases/19-in-game-actions/19-04-SUMMARY.md
@.planning/phases/19-in-game-actions/19-05-SUMMARY.md
@.planning/phases/19-in-game-actions/19-10-SUMMARY.md
@.planning/phases/18-materials-exploration/18-13-SUMMARY.md
@vmd/tests/rep_verify.tcl

Load-bearing research: integration §5 (the rep_verify W5 row: "actions round: hint → reveal → restart → cleanup driver steps + reveal-counter logging") and §6 R6 (the live-session hard gate). 18-13-SUMMARY is the round-addition template (copy pv_round3's skeleton 1:1; catch-unset ::pv_gs before on_start; ::pv_rounds N; after 4500 pv_state; handoff + instructions updates; the fake-::tk_version + ::pv_probe definition gate). Mechanisms §1B/R3 (lib procs are confirm-free; confirms live in the GUI handlers — so the driver calls game::reveal_one/reveal_all DIRECTLY and must NEVER call on_reveal_one/on_reveal_all). 19-10-SUMMARY pins the handler names (on_hint/on_restart exist and are confirm-free). rep_verify.tcl is the file being extended — pv_state already dumps `shown=[mol showrep $gm $r]` per tier (line ~265/272), pv_cleanup falls back to game_tab's stash, pv_cleanup_check dumps post-state. Tcl 8.5 only; braced expr; every mol call catch-guarded (molids go stale); no grab; no mouse commands.
</context>

<tasks>

<task type="auto">
  <name>Task 1: pv_round5 + the auto-drive procs + counter logging in pv_state/pv_report</name>
  <files>vmd/tests/rep_verify.tcl</files>
  <action>
1. `proc pv_round5 {}` — copy pv_round3's skeleton (lines ~375-423) 1:1 with these changes:
   - Fresh 1znf target via `::biochemeleon::demos::load_demo 1znf` (never reuse a mutated molid); log molid + expected game atoms (orig + 3).
   - Crafted state: `dict create target_mode loaded selected_object $m5 hider_count 3 per_rep [dict create VDW 3] lock_scene 0 difficulty_easy 1 demo_id 1znf` (pv_round3's key set, single tier — NO material keys; that shape still validates post-18) -> validate_state -> apply_state.
   - `catch {unset ::pv_gs}` BEFORE on_start (the 17.2-12 stale-stash lesson — round 3's stash is stale).
   - `::biochemeleon::on_start` -> `set ::pv_rounds 5` -> `after 4500 pv_state` (the dump re-stashes the LIVE round-5 gs) -> `after 5300 pv_round5_actions`.
   - Log guidance: paste this AFTER the round-3 win box; the driver will auto-play the actions — press NOTHING (no p needed: no picking this round; the reveal/confirm split means no dialog appears).
2. `proc pv_round5_actions {}` (fires once playing; every step catch-guarded, log via pv_log, bail with a log line on missing ::pv_gs):
   a. `set gs $::pv_gs`; `set gm [dict get $gs game_molid]`; capture `set ::pv_gm5 $gm`. Log pre-state: hidden_indices, `game_logic::reveal_count`/`hint_count` (expect 0 0), count_remaining.
   b. `::biochemeleon::on_hint` (REAL handler — no confirm) -> log: hint_count (expect 1), numreps delta (+1), the LAST rep's combined-braces read-back (style/selection/color — expect VDW / the parenthesized GAM-excluding within-selector / ColorID 3), `lindex [game_logic::log_lines] end`.
   c. `::biochemeleon::game::reveal_one $gs` (LIB surface — confirm-free; NEVER call on_reveal_one) -> log: found_indices before/after (the delta IS the revealed index), count_remaining (expect 2), reveal_count (expect 1), the revealed atom's user2 read-back (atomselect index -> get user2 == 1.0), log_lines tail.
   d. Found-mgmt (lib surface): `hiders::set_found_visible $gm 0` -> log every tier's found-rep showrep getter (expect 0) AND hidden-rep getter (expect 1 — the narrowed rule); `set_found_visible $gm 1` -> expect 1; `hiders::set_found_color $gm 11` -> log each found rep's color read-back (expect `ColorID 11`) + the `hiders::found_colorid` getter.
   e. Restart via the REAL handler: `::biochemeleon::on_restart` -> `catch {unset ::pv_gs}` (re-stash happens via the next dump) -> `after 4500 pv_state` -> `after 5300 pv_round5_finish` -> log "restart driven — counters should zero at the fresh round's start_round".
3. `proc pv_round5_finish {}` (fires playing on the RESTARTED round): `set gs $::pv_gs` (re-stashed = the restarted round); log: reveal_count/hint_count (EXPECT 0 0 — the real-flow round_reset proof), count_hiders (expect 3, registry rebuilt), the new game molid vs `::pv_gm5` (different + monotonic), per_rep keys (VDW preserved); then `::biochemeleon::game::reveal_all $gs` -> log: count_remaining (0), reveal_count (EXPECT 3 — the +N form), state (won — the win box is on screen), log_lines tail (`Revealed all 3 hiders. Game over.`); close with "Then paste: pv_report, then pv_cleanup".
4. pv_state: after the game_logic state line (~199-210), add a catch-guarded counters line: `counters: reveals=[game_logic::reveal_count] hints=[game_logic::hint_count]` (guarded — absent game_logic must not break the dump). Keep EVERYTHING else in the dump byte-identical (18-13's comparability rule).
5. pv_report: extend the SESSION REPORT line with `reveals=$rev hints=$hint` (catch-guarded getters).
   </action>
   <verify>
Static: `grep -c "pv_round5" vmd/tests/rep_verify.tcl` >= 3 (proc + actions + finish); `grep -cE "on_reveal_one|on_reveal_all" vmd/tests/rep_verify.tcl` == 0 (the confirm-bearing handlers are NEVER driven); `grep -c "on_hint" ...` >= 1; grab-set grep 0. Headless DEFINITION-GATE (18-13's pattern): write a staging-only gate script (`set ::tk_version 8.5.6; set ::pv_probe 1; source vmd/tests/rep_verify.tcl; if {[lsearch [info procs pv_*] pv_round5] < 0} { error "pv_round5 missing" }; vmdcon -info "PVDEF procs=[llength [info procs pv_*]]"; exit`) and run it in the staging copy (`bash -ic 'cd tmp/p19-14 && vmd -dispdev text -e pvdef.tcl -eofexit < /dev/null'`) -> PVDEF line prints, procs count grew by 3 vs 637-line baseline (which defines 15 pv_* procs), 0 `ERROR)` / 0 `bad switch`, `Exiting normally`. VMD runs SEQUENTIAL (R5).
   </verify>
   <done>pv_round5/auto-drive/finish exist, pv_state + pv_report carry counters, and the definition-gate proves the file parses with all procs defined.</done>
</task>

<task type="auto">
  <name>Task 2: handoff + instructions + header steps + commit</name>
  <files>vmd/tests/rep_verify.tcl</files>
  <action>
1. pv_round3's closing log lines: route to the new round — replace "Then paste: pv_report, then pv_cleanup" with "Then paste: pv_round5 (ACTIONS round -- the driver auto-plays hint/reveal/found-mgmt/restart/reveal-all), then pv_report + pv_cleanup".
2. pv_instructions: insert the round-5 step between the round-3/4 step and the final report step; renumber; keep ONE short line per step and LEAD with the p-press quirk (steps 0-2 untouched); the hard directive stays ≤3 pastes per segment (pv_round5 / pv_report / pv_cleanup for this segment).
3. Header comment: extend the numbered human-steps list (the 1-8 list at the top) with the pv_round5 step (renumber report/cleanup); add one line under the EXTENDED-by banner: "EXTENDED by Plan 19-14 with the ACTIONS round (pv_round5): the driver auto-drives hint/reveal/found-mgmt/restart via non-confirm surfaces and logs the reveal counter + showrep/getter state."
4. Commit (single, plan granularity): `feat(19-14): add pv_round5 actions round to the rep_verify driver`.
   </action>
   <verify>
Re-run the definition-gate script (parses + procs defined after the comment/log edits). Static: the handoff chain greps — pv_round2 -> pv_round3 -> pv_round5 -> pv_report/pv_cleanup ordering present in the log lines; pv_instructions still starts with the quirk step; `grep -c "on_reveal_one|on_reveal_all"` still 0.
   </verify>
   <done>The driver routes rounds 1→2→3→(4)→5→report→cleanup with the actions round fully auto-driven and ≤3 pastes per segment.</done>
</task>

</tasks>

<verification>
- Definition-gate headless run: all pv_* procs defined (incl. pv_round5/pv_round5_actions/pv_round5_finish), 0 `ERROR)` / 0 `bad switch`, `Exiting normally`.
- Static: the driver calls on_hint/on_restart (real handlers) + game::reveal_one/reveal_all + hiders::set_found_* (lib) and NEVER on_reveal_one/on_reveal_all; ::pv_gs unset-before-on_start + post-restart refresh present; pv_state/pv_report counter lines present.
- grab-set gate 0; no mouse commands added; pick_bridge/game_tab/game.tcl byte-untouched (`git status` shows only rep_verify.tcl).
- R6 honored: the plan stopped if 18-13 was unexecuted (frontmatter note).
- Single feat(19-14) commit.
</verification>

<success_criteria>
The GUI checkpoint's driver can walk ONE human through the actions round with auto-issued commands and auto-logged counter/showrep/color evidence — the human only pastes, and never answers a confirm the driver spawned.
</success_criteria>

<output>
After completion, create `.planning/phases/19-in-game-actions/19-14-SUMMARY.md` (carry the definition-gate evidence + the paste-budget statement + the exact pv_round5 step sequence for 19-16/17's checkpoint author)
</output>
