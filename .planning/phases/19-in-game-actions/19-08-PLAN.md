---
phase: 19-in-game-actions
plan: 08
type: execute
wave: 3
depends_on: ["19-02"]
files_modified: [vmd/gui/game_tab.tcl]
autonomous: true

must_haves:
  truths:
    - "The Game tab status row shows 'Reveals: N' between the Remaining and Mouse labels, initialized 'Reveals: 0' at build and refreshed from the game_logic reveal-count getter on every remaining update (DIFF-01)"
    - "The Game tab has an 'Actions' labelframe packed directly above the Mouse-mode group: Hint / Reveal one / Reveal all on a help row, Restart / Cleanup model on a separated round-end row, each dispatching to the pinned dialog-scope handler name"
    - "The per-round view-state reset (timer / mode label / radios / reveal label) is ONE shared proc used by start_round AND the cleanup-path end_round; the reveal label resets every round start and every cleanup (C8 lesson)"
    - "The 16-12 hotkey/mode desync is probe-resolved: probe evidence recorded, then either the mode-var trace fix is wired with symmetric lifecycle + exported unregister_mode_trace, or the deferral is recorded in code comment + summary"
  artifacts:
    - path: "vmd/gui/game_tab.tcl"
      provides: "reveal_text var + status label, Actions labelframe (5 ttk::buttons), reset_view_state/_clear_log_view/end_round, update_remaining reveal pull, verdict-conditional mode-var trace"
      min_lines: 520
      contains: "reset_view_state"
  key_links:
    - from: "Actions buttons -command strings"
      to: "::biochemeleon::on_hint / on_reveal_one / on_reveal_all / on_restart / on_cleanup (dialog scope, plan 19-10)"
      via: "exact -command literals (the BTN-07 fan-in shape)"
      pattern: "on_reveal_one"
    - from: "update_remaining"
      to: "game_logic reveal-count getter (19-02 seam)"
      via: "0-arg PULL inside the registered remaining callback"
      pattern: "reveal_count"
    - from: "start_round step 1.5"
      to: "reset_view_state"
      via: "extracted shared reset (decision 9)"
      pattern: "reset_view_state"
    - from: "countdown_step GO branch"
      to: "_register_mode_trace (probe-verdict conditional)"
      via: "registered at the same lifecycle site as pick_bridge::activate"
      pattern: "_register_mode_trace"
---

<objective>
Build the Game tab's action WIDGETS (the 19-08 half of the GUI batch): the "Reveals: N" status label (DIFF-01), the "Actions" labelframe with Hint / Reveal one / Reveal all / Restart / Cleanup model buttons, the reset_view_state/end_round shared-reset refactor, and the probe-first hotkey-desync task. Buttons DISPATCH to dialog-scope handlers by pinned name (19-10 defines them in parallel); guards/confirms live in the handlers, never here.

Purpose: GAME-05/06/07/10 + BTN-06 + DIFF-01 GUI halves. Full-impl pattern (18-06 precedent) — no stub phase; the parallel-plan contract pins the seam.
Output: extended game_tab.tcl, headless-load-clean, with the desync probe verdict recorded.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-in-game-actions/19-RESEARCH-gui.md
@.planning/phases/19-in-game-actions/19-RESEARCH-integration.md
@vmd/gui/game_tab.tcl
@vmd/gui/dialog.tcl
@.planning/phases/19-in-game-actions/19-02-SUMMARY.md

Load-bearing research: GUI research §1 (widget matrix — ttk::button/label/labelframe all exercised in v2), §2.1/2.2 (v1 labels VERBATIM; Restart tooltip REWORDED), §2.5 (C8 counter-reset lesson; silent no-op guards NOT enable/disable), §3.1-3.3 (the canvas + pack mechanics — Actions packs `-side bottom -fill x` AFTER the Mouse labelframe line, NOT `-side top`; DIFF-01 label between remain and mode, updated by PULL), §5 (desync probe-gated fix — trace preferred if the variable is live, DEFER if dead; pick_bridge UNTOUCHED; never call set_view_mode from the trace), §4.1 (timers keep ticking under a modal confirm — expected, document it). Integration §3.0 (BTN-07 fan-in: handlers at DIALOG scope, lib calls from there), §4.2 (game_logic counters via getters — one `variable` per line). Tcl 8.5.6: one `variable` per line, brace expr, `::tk_version` qualifier, no try/finally.

CONTRACT (pinned — the 19-08/19-09/19-10 interlock; parallel executors MUST NOT diverge):
- Dialog-scope handler names (defined by 19-10 in dialog.tcl): `::biochemeleon::on_hint`, `::biochemeleon::on_reveal_one`, `::biochemeleon::on_reveal_all`, `::biochemeleon::on_restart`, `::biochemeleon::on_cleanup`, `::biochemeleon::on_found_mgmt {action}` (hide|show|recolor), `::biochemeleon::on_found_color {colorid}`.
- game_tab exports this plan adds: `end_round` (+ `unregister_mode_trace` ONLY if the desync probe verdict is LIVE; 19-10's on_close calls it catch-guarded so both verdict branches are safe).
- Widget paths: Actions labelframe = `$parent.actions`; help row = `$parent.actions.help`; round-end row = `$parent.actions.rend` (19-09 anchors its two menubuttons into `.help`).
- The game_tab smoke's forbidden-string check reads the WHOLE file INCLUDING comments: never write the lowercase literal `mouse mode ` (underscore form `::vmd_mouse_mode` is fine), `vwait`, or `grab set` anywhere — comments included. Also keep comments free of 8.6-idiom words (lmap/try/throw/tailcall/coroutine/yield/finally).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reveals status label (DIFF-01) + Actions labelframe with the five action buttons</name>
  <files>vmd/gui/game_tab.tcl</files>
  <action>
1. Namespace vars block (~49-71): add `variable reveal_text` (ONE line; comment: the "Reveals: N" DIFF-01 status text — widget-bound, initialized by build, reset by reset_view_state every round start and every cleanup, the v1 C8 lesson). Update the block comment listing the widget-bound vars. Do NOT initialize at source (the load-gate asserts widget-bound vars are UNSET pre-build).
2. build var-init block (~93-96): add `set reveal_text "Reveals: 0"` after the mouse_mode init.
3. build status row (~99-106): create `ttk::label $status.reveals -textvariable ::biochemeleon::game_tab::reveal_text` and pack it BETWEEN remain and mode — pack order becomes timer, remain, reveals, mode: `pack $status.reveals -side left -padx 12` inserted between the remain and mode pack lines (counts grouped: timer, remaining, reveals, then the mode label — GUI research §3.3 DIFF-01 row).
4. build: AFTER the mouse labelframe pack line (`pack $mf -side bottom -anchor w -pady 4`), add the Actions group with the EXACT pack mechanics from research §3.2 (first bottom pack claims the bottom edge; later bottom packs stack ABOVE it — so Actions must be packed `-side bottom -fill x` AFTER $mf, never `-side top` after the expanding log):
```tcl
    # -- "Actions" labelframe: help row + round-end row (Phase 19) ----------
    # Buttons dispatch to DIALOG-scope handlers (the BTN-07 fan-in shape,
    # integration 3.0): guards + confirms live in dialog.tcl, never here.
    # Silent no-op guards, NOT enable/disable (v1 07-RESEARCH convention):
    # buttons stay clickable in every game state, handlers early-return.
    # NOTE: a modal confirm blocks dialog interaction while up, but the
    # `after` timers KEEP TICKING under it (v1 parity -- expected, not a bug).
    set af [ttk::labelframe $parent.actions -text "Actions" -padding 6]
    set help [ttk::frame $af.help]
    ttk::button $help.hint   -text "Hint"        -command {::biochemeleon::on_hint}
    ttk::button $help.rev1   -text "Reveal one"  -command {::biochemeleon::on_reveal_one}
    ttk::button $help.revall -text "Reveal all"  -command {::biochemeleon::on_reveal_all}
    pack $help.hint $help.rev1 $help.revall -side left -padx 4
    set rend [ttk::frame $af.rend]
    ttk::button $rend.restart -text "Restart"       -command {::biochemeleon::on_restart}
    ttk::button $rend.cleanup -text "Cleanup model" -command {::biochemeleon::on_cleanup}
    pack $rend.restart $rend.cleanup -side left -padx 4
    pack $help -side top -fill x -pady 2
    pack $rend -side top -fill x -pady 2
    pack $af -side bottom -fill x -pady 4
```
   Button labels are v1-verbatim (gui_game.py:45/49/53/78 + gui_setup.py:287 "Cleanup model"). The handlers are defined by plan 19-10 in the SAME wave (parallel, disjoint file) — the load-gate verifies proc existence, clicks are the Phase-19 GUI checkpoint.
5. Restart-semantics comment (next to $rend.restart): "Restart REPLAYS this round from its initial state -- same hider count and per-rep mix, hider positions re-randomize. It does NOT re-read the Setup form and does NOT generate a new hider count (v1's 'new hiders' tooltip wording is superseded -- 16-13 same-count REPLAY semantics). No confirm (v1 parity)." Also note Cleanup model lives HERE on the Game tab only (16-12 defect resolution; the Setup-tab duplicate was rejected as scope creep).
   Comment hygiene: no lowercase `mouse mode `/`vwait`/`grab set` literals; no 8.6-idiom words; no `ttk::combobox`; no `tk_chooseColor`.
  </action>
  <verify>
Headless load-gate on a FRESH staging copy (e.g. `tmp/p19-08/`, a copy of the vmd/ tree): `bash -ic 'cd tmp/p19-08 && vmd -dispdev text -e vmd/smoke/phase16_gametab_smoke.tcl -eofexit < /dev/null'` -> `BCHM_SMOKE_RESULT PASS=1` (the 11-proc floor still holds; Task 1 adds no new procs), full-log scan 0 `ERROR)` / 0 `bad switch`, `Exiting normally`. VMD runs SEQUENTIAL (shared $env(TEMP) forbids parallel runs). Static: the five `-command {::biochemeleon::on_...}` strings present; `grab set` grep over vmd/gui/ = 0; 8.6-idiom grep over vmd/lib/ vmd/gui/ = 0.
  </verify>
  <done>Five action buttons + the "Reveals: 0" label exist in build with the pinned -command literals and pack order; the file loads headless-clean.</done>
</task>

<task type="auto">
  <name>Task 2: reset_view_state / end_round shared-reset refactor (decision 9 + C8)</name>
  <files>vmd/gui/game_tab.tcl</files>
  <action>
1. New PRIVATE proc `::biochemeleon::game_tab::reset_view_state {}`: declare `variable timer_text mode_text mouse_mode reveal_text` (one per line); body = the EXACT start_round step-1.5 resets (`set timer_text "0:00"`, `set mode_text "Mouse: Rotate"`, `set mouse_mode "rotate"`) PLUS `set reveal_text "Reveals: 0"`. Comment: extracted from start_round step 1.5 (the 16-14 view-state reset) so the cleanup path shares ONE reset (decision 9); programmatic variable sets fire no `-command` (no viewer call).
2. New PRIVATE proc `::biochemeleon::game_tab::_clear_log_view {}`: declare `variable w`; the guarded clear block lifted from start_round step 5 (winfo-exists guard + catch-wrapped state-normal -> delete 1.0 end -> state-disabled on `$w.nb.game.logf.log`).
3. start_round rewiring: step 1.5 body becomes a single `reset_view_state` call (update the comment: the reveal label now resets here too — C8); step 5's inline clear block becomes `_clear_log_view` (behavior identical). Steps 1-7 order otherwise UNTOUCHED.
4. New EXPORTED proc `::biochemeleon::game_tab::end_round {}` (append ` end_round` to the namespace export line): the cleanup-path reset — calls `reset_view_state`, then `set remain_text "Remaining: -"` (declare `variable remain_text`), then `_clear_log_view`, then `::biochemeleon::game_logic::round_reset` (the research-resolved decision: the documented whole-model reset leaves state idle so the next Start arms its countdown cleanly, and it zeroes the reveal/hint counters — belt-and-braces with the label reset). Comment: called by on_cleanup (dialog scope, plan 19-10); does NOT stop timers or tear down the bridge (the caller does that BEFORE calling end_round); end_round is view+model reset only.
5. Update the file header comment: the export list mention + the shared-reset statement.
  </action>
  <verify>
Load-gate smoke again on the same staging (re-copy or reuse): PASS=1, 0 `ERROR)` / 0 `bad switch`, `Exiting normally`. Static: `reset_view_state` defined and called from start_round; `end_round` defined AND in the namespace export line; `_clear_log_view` defined with exactly 2 call sites (start_round step 5 + end_round); grab/8.6 gates still 0.
  </verify>
  <done>The view-state reset is shared (start_round + end_round), the reveal label resets on every round start and every cleanup, start_round behavior is otherwise unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: hotkey/mode desync — HEADLESS probe FIRST, then verdict-gated fix or deferral (decision 8)</name>
  <files>vmd/gui/game_tab.tcl</files>
  <action>
1. PROBE FIRST (ephemeral `tmp/p19-08-probe/probe.tcl`, gitignored; needs NO repo files — pure VMD globals): register a write trace on `::vmd_mouse_mode` (`proc ::p19_probe_write {args} { lappend ::p19_fired $::vmd_mouse_mode }`), then `catch {mouse mode rotate}`, read back `::vmd_mouse_mode` + `::vmd_mouse_submode`; repeat for `mouse mode pick 2` and `mouse mode pick`; print one MODES line: before / after_rotate / after_pick2 / after_pick values + trace fire count; end with a `BCHM_SMOKE_RESULT PASS=1 FAIL=none` marker (the probe always passes — the VERDICT is read from the MODES line). Run: `bash -ic 'cd tmp/p19-08-probe && vmd -dispdev text -e probe.tcl -eofexit < /dev/null'`.
   WHY headless is valid (pin in the summary): VMD's hotkey bindings (`user add key r` -> hotkeys.tcl) execute the SAME `mouse mode` Tcl command as a pasted/scripted call, so whether the C side maintains `::vmd_mouse_mode` is ENTRY-PATH-INDEPENDENT — the headless probe discriminates for the hotkey path too. (Batch D's GUI session still folds in the research §6.2 hotkey probe line as belt-and-braces confirmation.)
2. Verdict LIVE (values change across commands and/or trace fires > 0) -> implement the trace fix in game_tab.tcl:
   a. Private `_on_mode_var_write {args}`: guard `if {![info exists w] || $w eq ""}` return, plus `[info exists mouse_mode]` guard; read `set vm $::vmd_mouse_mode`; IGNORE any value other than `rotate`/`pick` (unknown modes must never clobber the panel); if `$vm ne $mouse_mode`: `set mouse_mode $vm` + `set mode_text "Mouse: Pick"` (vm eq pick) or `"Mouse: Rotate"` (else). NEVER call set_view_mode from this proc — the viewer is ALREADY in that mode; calling it would fight the user's hotkey (research §5). Programmatic var sets fire no -command, so no viewer call happens. NOTE: `winfo` is Tk-only — the guard order above (info exists w FIRST) keeps the proc safe in text mode.
   b. Private `_register_mode_trace {}`: `catch {trace add variable ::vmd_mouse_mode write ::biochemeleon::game_tab::_on_mode_var_write}`.
   c. EXPORTED `unregister_mode_trace {}` (append to the namespace export line): `catch {trace remove variable ::vmd_mouse_mode write ::biochemeleon::game_tab::_on_mode_var_write}` — 19-10's on_close calls it catch-guarded (interlock); catch-guarded here too (removing an unregistered trace errors).
   d. Lifecycle (the pick-trace discipline): `_register_mode_trace` in countdown_step's GO branch AFTER the `set mode_text`/`set mouse_mode` pick sets (same lifecycle site as `pick_bridge::activate`); `unregister_mode_trace` in on_win (next to the bridge deactivate) and in end_round. pick_bridge.tcl stays BYTE-UNTOUCHED.
   e. Headless regression probe `tmp/p19-08-probe/probe2.tcl` (fresh staging; source the lib chain + game_tab like the gametab smoke's source block): override the absent Tk command with a probe-local stub `proc winfo {args} { return 1 }`, `set ::biochemeleon::game_tab::w dummy`, call `_register_mode_trace`, then `mouse mode pick 2` -> assert `mouse_mode` eq "pick" AND `mode_text` eq "Mouse: Pick"; `mouse mode rotate` -> assert rotate values; `unregister_mode_trace` -> further writes leave the panel vars untouched. Print a marker line; full-log scan 0 `ERROR)` / 0 `bad switch`.
3. Verdict DEAD (values identical, trace never fires) -> DEFER the defect (research §5: neither trace nor poll can observe a dead variable; do NOT build a poller): add a short comment block near set_mouse_mode recording the probe evidence + that 16-12 stays registered as deferred; NO trace code, NO poller. pick_bridge.tcl still byte-untouched.
4. In BOTH branches: record the MODES line + verdict + branch taken in 19-08-SUMMARY.md (19-11 consumes this verdict to hard-assert the right branch).
  </action>
  <verify>
Probe log shows the MODES line with concrete before/after values; the chosen branch's artifacts exist (LIVE: the three trace procs + GO/on_win/end_round wiring + probe2 asserts; DEAD: the deferral comment and NO trace procs). Load-gate smoke PASS=1 on the staging copy; grab/8.6 gates 0; `grep -c "mouse mode " vmd/gui/game_tab.tcl` = 0 (comments included).
  </verify>
  <done>Desync verdict recorded with evidence; either the trace fix is wired with symmetric lifecycle and an exported unregister_mode_trace (probe2 proves the panel-sync logic headlessly), or the deferral is recorded in code + summary. on_close's catch-guarded call is safe in both branches.</done>
</task>

</tasks>

<verification>
- phase16_gametab_smoke.tcl PASS=1 on the fresh staging copy (x1 here; 19-11 hardens it), full-log scan 0 `ERROR)` / 0 `bad switch`, `Exiting normally`.
- 8.6-idiom grep over vmd/lib/ vmd/gui/ = 0; `grab set` grep over vmd/gui/ = 0.
- The five -command literals + reveal_text plumbing + reset_view_state/end_round present; pick_bridge.tcl byte-untouched (`git diff --stat vmd/lib/pick_bridge.tcl` empty).
- Single commit: `feat(19-08): game tab action widgets, reveals label, shared view reset, desync probe`.
</verification>

<success_criteria>
The Game tab's action surface exists (5 buttons + DIFF-01 label + shared reset), dispatching by pinned name to the parallel 19-10 handlers; the desync question is settled by probe evidence with a verdict-safe interlock for on_close; everything headless-load-clean.
</success_criteria>

<output>
After completion, create `.planning/phases/19-in-game-actions/19-08-SUMMARY.md` (carry the desync MODES evidence + verdict — 19-11 consumes it)
</output>
