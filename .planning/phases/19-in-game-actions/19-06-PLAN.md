---
phase: 19-in-game-actions
plan: 06
type: execute
wave: 3
depends_on: ["19-05"]
files_modified: [vmd/lib/game.tcl, vmd/smoke/phase16_restart_smoke.tcl]
autonomous: true

must_haves:
  truths:
    - "After a different-target Start mid-round, the guard-restored original is hidden (mol off) so it can no longer intercept clicks - while its DATA-level survival guarantee is untouched"
    - "Same-target restart paths never hide anything (the remap branch reassigns the target to the restored molecule, so the hide condition cannot fire)"
    - "The 17.1-14 survivor guarantee still holds at the data level (atoms/sentinels intact), now with a pinned display-off assertion"
  artifacts:
    - path: "vmd/lib/game.tcl"
      provides: "guard insertion: catch-guarded mol off on the restored original when the new target is a DIFFERENT live molecule"
      min_lines: 670
      contains: "restored original hidden"
    - path: "vmd/smoke/phase16_restart_smoke.tcl"
      provides: "adapted stage C: survivor scan tracks the surviving original's molid and asserts displayed == 0"
      min_lines: 462
      contains: "c_survivor_displayed"
  key_links:
    - from: "vmd/lib/game.tcl start_game guard (the auto-restart cleanup/restore block)"
      to: "mol off $restored + molinfo $restored get displayed"
      via: "minimal catch-guarded insertion AFTER the remap branch, gated on restored non-empty AND restored ne target AND displayed == 1 (R1 mitigation - the deliberate unfreeze of the byte-frozen guard)"
      pattern: "mol off \\\$restored"
    - from: "vmd/smoke/phase16_restart_smoke.tcl stage C survivor block"
      to: "molinfo <survivor> get displayed == 0"
      via: "the adapted smoke tracks the survivor molid in the 555-atom scan and asserts display-off with a c_survivor_displayed failure token"
      pattern: "c_survivor_displayed"
---

<objective>
Fix the 17.1-14 defect inside start_game's auto-restart guard: when a mid-round Start targets a DIFFERENT live molecule, the guard restores the previous round's original for data preservation - and that restored molecule is now hidden (`mol off`) so it cannot intercept clicks aimed at the new game molecule. Same-target restarts are unaffected (the remap branch reassigns the target to the restored molecule itself).

Purpose: research section 6-R1 + mechanisms section D. The user's post-restart click MUST land on the live game molecule, not the zombie restore. This is the deliberate unfreeze of the byte-frozen guard, with the adapted phase16_restart_smoke as the regression net (data-level survival preserved, display-off newly pinned).
Output: ~10-line guarded insertion in game.tcl + stage-C smoke adaptation, smoke green x3 sequential.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-in-game-actions/19-RESEARCH-integration.md
@.planning/phases/19-in-game-actions/19-RESEARCH-mechanisms.md
@.planning/phases/19-in-game-actions/19-05-SUMMARY.md
@vmd/lib/game.tcl
@vmd/smoke/phase16_restart_smoke.tcl

Load-bearing research: mechanisms section 6-R1 (the mitigation decision - minimal catch-guarded insertion inside the guard, catch-wrapped mol off + molinfo get displayed round-trip; catch on BOTH the read and the write; the same-target no-op property via the remap branch), section D "Where the fix applies" (inside the guard, after the remap branch - NOT a post-guard helper, which would fire on fresh starts with no restored original and duplicate stash-existence logic; the guard owns the restored-original lifecycle). Integration section 2 (the 17.1-14 defect line), section 7 BTN-06 row. game.tcl's guard block (the restore/cleanup/remap sequence, lines ~160-190) - anchor on the remap branch + the end-guard comment, NOT line numbers. The smoke's stage C survivor scan (the 555-atom, 0-sentinel scan) and its header comment. Tcl 8.5 ONLY; brace expr.
</context>

<tasks>

<task type="auto">
  <name>Task 1: guard insertion - hide the restored original on different-target starts</name>
  <files>vmd/lib/game.tcl</files>
  <action>
Inside start_game's auto-restart guard, immediately AFTER the target-remap branch and BEFORE the end-guard comment, insert (~10 lines, catch-guarded on BOTH commands):
```tcl
        # 17.1-14 fix (Phase 19): hide the guard-restored original when the
        # new round targets a DIFFERENT live molecule (probe-verified mol off
        # + displayed round-trip; research V10). The remap branch above
        # reassigns $molid to $restored on the same-target path, so this
        # branch only fires on genuine different targets. Data-level
        # survival is untouched - only the display turns off.
        if {$restored ne "" && $restored ne $molid} {
            if {[catch {molinfo $restored get displayed} disp] == 0 && $disp == 1} {
                if {[catch {mol off $restored} offerr]} {
                    catch {vmdcon -warn "bioCHEMeleon game: could not hide the restored original ($offerr)"}
                } else {
                    catch {vmdcon -info {bioCHEMeleon game: restored original hidden to avoid pick interception}}
                }
            }
        }
```
Match the guard's existing local-variable names (the restored-original variable and the target variable as they actually read in the file - the block above uses $restored/$molid as the reference shape; adapt names to the file). Add a guard-comment line citing the 17.1-14 defect id, the probe-verified mol off/displayed round-trip, and that this is the Phase-19 R1 minimal insertion (regression net = the adapted phase16_restart_smoke).
Properties to preserve: the insertion NEVER fires when the remap branch ran (molid == restored there); it NEVER fires on fresh starts (restored is empty / the guard did not run); a dead restored molid makes `molinfo $restored get displayed` error -> catch -> skip. NO changes to cleanup/restore logic, NO gs shape changes, NO mouse commands, NO confirms.
Update the header contract comment's guard description with one sentence about the display-off behavior.
  </action>
  <verify>
Static: `grep -n "restored original hidden" vmd/lib/game.tcl` finds the insertion; 8.6-idiom gate 0 matches over vmd/lib/ vmd/gui/; `grep -cE "\blmap\b|\btry\b|\bthrow\b|\btailcall\b|\bcoroutine\b|\byield\b|\bfinally\b" vmd/lib/game.tcl` returns 0. Headless mini-probe (ephemeral tmp/p19-06/probe.tcl on a fresh staging copy): load 1kxk -> start round on it -> load 1znf (different live molecule) -> start on 1znf (guard fires, restores the 1kxk original, fix hides it) -> assert the restored original still EXISTS in molinfo list with intact atom count AND `[molinfo <restored> get displayed]` == 0 AND the new round's game molecule is displayed and functional; then `mol on <restored>` and assert displayed == 1 (the user's manual re-show workflow round-trips). Print `BCHM_SMOKE_RESULT PASS=1 FAIL=none`; full-log scan 0 `ERROR)` / 0 `bad switch` + `Exiting normally`.
  </verify>
  <done>The guard hides the restored original ONLY on different-target starts, catch-guarded on both commands, with the header contract updated and the mini-probe proving hide + survival + manual re-show.</done>
</task>

<task type="auto">
  <name>Task 2: adapt phase16_restart_smoke stage C + rerun x3</name>
  <files>vmd/smoke/phase16_restart_smoke.tcl</files>
  <action>
1. Adapt the stage-C survivor block: in the existing survivor scan (the 555-atom, 0-sentinel iteration over molinfo list), TRACK the surviving original's molid in a local variable (do not change the scan's data-level assertions); immediately after the scan, add the display-off assertion: `[molinfo $survivor_molid get displayed]` == 0, with failure token `c_survivor_displayed` and a comment: 19-06 - the guard now hides the restored original (17.1-14 fix); data-level survival is unchanged, display-off is the NEW expected state.
2. Update the smoke's header comment (the stage descriptions): stage C's "SURVIVES loaded" guarantee becomes "SURVIVES loaded with display OFF (19-06: the guard's restored-original fix - data-level survival preserved, display deliberately off)".
3. Do NOT touch the two stale-pin red smokes, any other smoke, or any data-level assertion. The smoke must remain runnable headlessly (its existing guards/style unchanged).
4. Rerun the adapted smoke THREE times SEQUENTIALLY on a FRESH staging copy (`tmp/p19-06/`): each run parses `BCHM_SMOKE_RESULT PASS=1` (NEVER $?); full-log scan per run: 0 `ERROR)` / 0 `bad switch` + `Exiting normally`; all three logs archived for the summary. Also rerun `vmd/smoke/phase16_onpick_smoke.tcl` once (guard-untouched path stays green).
  </action>
  <verify>
Three sequential adapted-smoke runs each print `BCHM_SMOKE_RESULT PASS=1` with the `c_survivor_displayed` assertion present in the log; the onpick smoke PASS=1; full-log scans clean on every run. The three PASS logs are the fresh headless probe proof required for the 17.1-14 closure.
  </verify>
  <done>Stage C pins the new display-off expectation without weakening any data-level guarantee; smoke green x3 sequential proves the fix end-to-end through the real guard path. Commit (single, plan granularity): `fix(19-06): hide guard-restored original on different-target starts (17.1-14)`.</done>
</task>

</tasks>

<verification>
- Adapted phase16_restart_smoke: PASS=1 on 3 SEQUENTIAL runs (fresh staging copy); full-log scan each run 0 `ERROR)` / 0 `bad switch` + `Exiting normally`; `c_survivor_displayed` assertion present.
- phase16_onpick_smoke PASS=1 (guard-untouched path).
- Mini-probe: hide + data-survival + mol on re-show round-trip all asserted; BCHM_SMOKE_RESULT PASS=1.
- 8.6-idiom gate 0 matches over vmd/lib/ vmd/gui/.
- Single fix(19-06) commit; the two stale-pin red smokes untouched.
</verification>

<success_criteria>
The 17.1-14 defect is fixed at its root: post-restart different-target clicks land on the live game molecule because the restored original is display-hidden (and manually re-showable), while the restart smoke's data-survival guarantee holds with the new expectation pinned. GAME-10/BTN-06's root-cause premise is eliminated for the composition layer before the GUI batch (19-08..11) wires the click flows.
</success_criteria>

<output>
After completion, create `.planning/phases/19-in-game-actions/19-06-SUMMARY.md` (carry the 3 PASS logs + the mini-probe evidence for the 17.1-14 closure)
</output>
