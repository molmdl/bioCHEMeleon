---
phase: 14-setup-tab-bundled-demos
plan: 04
type: execute
wave: 4
depends_on: ["14-03"]
files_modified:
  - vmd/gui/setup_tab.tcl
  - vmd/gui/dialog.tcl
autonomous: false
must_haves:
  truths:
    - "Selecting a loaded molecule from the dropdown updates the hider-count cap (spinbox -to reconfigured to hider_count_cap(atom_count))"
    - "Selecting a bundled demo loads it into VMD via mol new (DEMO-01: 1znf, 1xdn, 5e54, 1k8p, 2qbz, 4wb3)"
    - "Toggling a per-rep checkbox enables/disables its count spinbox (SETUP-04)"
    - "Reset restores DEFAULTS; Randomize produces a random valid state (quick-008 baked in); Save/Load round-trip the full setup to/from a .bcm file"
    - "Closing the dialog preserves in-progress edits (WM_DELETE_WINDOW handler) and cleans up the trace (trace vdelete — no leaked callbacks)"
    - "The dialog stays modeless (the 3D viewer remains interactive throughout — no grab set on the main panel)"
  artifacts:
    - path: vmd/gui/setup_tab.tcl
      provides: "Full callback implementations: refresh_mol_menu, select_demo, select_loaded_mol, on_rep_toggled, update_cap, recompute_per_rep_maxes, do_reset, do_randomize, do_save, do_load"
      contains: "proc ::biochemeleon::setup_tab::do_save"
    - path: vmd/gui/dialog.tcl
      provides: "WM_DELETE_WINDOW handler (collect_state + trace vdelete + destroy)"
      contains: "WM_DELETE_WINDOW"
  key_links:
    - from: "refresh_mol_menu"
      to: "trace variable vmd_molecule w"
      via: "auto-refreshes loaded-mol menu on mol add/delete; cleaned up via trace vdelete on dialog close"
      pattern: "trace variable vmd_molecule w"
    - from: "do_save"
      to: "::biochemeleon::demos::save_setup"
      via: "tk_getSaveFile -> save_setup [collect_state] $fname"
      pattern: "demos::save_setup"
    - from: "do_load"
      to: "::biochemeleon::demos::load_setup + validate_state"
      via: "tk_getOpenFile -> load_setup -> validate_state -> apply_state"
      pattern: "demos::load_setup"
    - from: "do_randomize"
      to: "::biochemeleon::setup_state::randomize_state"
      via: "apply_state [randomize_state \"\" $atom_count $lock_source [collect_state]]"
      pattern: "setup_state::randomize_state"
---

<objective>
Wire the Setup tab callbacks: replace the 10 stub bodies from Plan 03 with full implementations (refresh_mol_menu with trace, select_demo/select_loaded_mol with cap update, on_rep_toggled, update_cap, recompute_per_rep_maxes, do_reset/do_randomize/do_save/do_load). Add the WM_DELETE_WINDOW handler to dialog.tcl (preserve in-progress edits + trace vdelete cleanup). Then a GUI human-verify checkpoint confirming the entire pre-game configuration experience works end-to-end.

Purpose: This completes the Setup tab — the user can configure every parameter, load demos, and Save/Load. After this plan, Phase 14's success criteria 1-3 are met (criterion 4 — pure-layer tcltest — was met in Plan 01).

Output: Fully functional gui/setup_tab.tcl (callbacks wired) + gui/dialog.tcl (WM_DELETE handler). All 11 Phase 14 requirements satisfied.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-setup-tab-bundled-demos/14-RESEARCH-gui.md
@.planning/phases/14-setup-tab-bundled-demos/14-RESEARCH-mol-logic.md
@vmd/AGENTS.md
@vmd/gui/setup_tab.tcl
@vmd/gui/dialog.tcl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace stub callbacks with full implementations + add WM_DELETE handler</name>
  <files>vmd/gui/setup_tab.tcl, vmd/gui/dialog.tcl</files>
  <action>
    Read 14-RESEARCH-gui.md Patterns 3/5/6 + the action-buttons example (lines 455-480) + the Save/Load example (lines 416-452 — BUT use demos::save_setup/load_setup per LOCKED DECISION #1, NOT the [list]+source pattern shown there). Read 14-RESEARCH-mol-logic.md for the demos bridge proc signatures (Plan 02).

    Replace the 10 STUB bodies in vmd/gui/setup_tab.tcl (Plan 03 left them as `return`) with these full implementations:

    1. `refresh_mol_menu {args}` (Pattern 3, research lines 172-185): accept `args` (trace callback signature `{name1 name2 op}` — Pitfall 2). Resolve the menu path from `variable w` (e.g. `$w.nb.setup.target.pages.loaded.mol.menu` — use the actual path from build_target_group; guard with `winfo exists`). `$menu delete 0 end`. Loop `molinfo list`: skip `graphics` filetype mols (`molinfo $id get filetype`); add a radiobutton per mol with `-value $id -label "$id [molinfo $id get name]" -variable ::biochemeleon::setup_tab::selected_mol -command [list ::biochemeleon::setup_tab::select_loaded_mol $id]`. If no mols loaded, add a disabled "None loaded" command entry.

    2. `select_demo {demo_id}`: set `variable demo_id $demo_id` + `variable mode "demo"`. Call `switch_page`. `catch {::biochemeleon::demos::load_demo $demo_id} molid` — on success set `variable current_molid $molid` + call `update_cap`. On error, `tk_messageBox -icon warning -parent $w -message "Could not load demo: $molid"`.

    3. `select_loaded_mol {molid}`: set `variable selected_mol $molid` + `variable current_molid $molid` + `variable mode "loaded"`. Call `switch_page` + `update_cap`.

    4. `on_rep_toggled {rep}` (Pattern 4): if `rep_sel($rep)` is set, enable the per-rep spinbox (`$w...per_rep.s$row configure -state normal` — resolve the spinbox path; use the row index via `lsearch $GAME_REPS $rep`). If unchecked, disable the spinbox + set `rep_cnt($rep) 0`. Guard with `if {$::biochemeleon::setup_tab::_loading} { return }` at the top (suppress during apply_state). Call `recompute_per_rep_maxes`.

    5. `update_cap {}` (Pattern 5, research lines 239-247): compute atom_count from `variable current_molid` via `::biochemeleon::demos::atom_count $current_molid` (Plan 02). Compute cap via `::biochemeleon::setup_state::hider_count_cap $atom_count`. Reconfigure the hider-count spinbox `-to [expr {max(1,$cap)}]`. Clamp: `if {$hider_count > $cap} { set hider_count $cap }`. Guard with `_loading`.

    6. `recompute_per_rep_maxes {}`: reconfigure each per-rep spinbox `-to` to the current hider_count (so per-rep counts can't exceed the total). Guard with `_loading`. (Keep this simple — the pure-layer validate_state does the authoritative clamp on Save/Start.)

    7. `do_reset {}` (research line 468): `apply_state $::biochemeleon::setup_state::DEFAULTS`.

    8. `do_randomize {}` (research lines 472-479): get atom_count from current_molid, get lock_source from the variable, get the current state via `collect_state` (as locked_state). Call `apply_state [::biochemeleon::setup_state::randomize_state "" $atom_count $lock_source [collect_state]]` (no seed = random; Plan 01's randomize_state). The empty-string seed means random (non-deterministic).

    9. `do_save {}` (LOCKED DECISION #1 — key-value format via demos::save_setup, NOT [list]+source):
       `set fname [tk_getSaveFile -defaultextension ".bcm" -title "Save bioCHEMeleon Setup" -filetypes [list {{bioCHEMeleon} {.bcm}} {{All files} {*}}] -parent $w]`
       If `$fname eq ""` return. `catch {::biochemeleon::demos::save_setup [collect_state] $fname} err` — on error, `tk_messageBox -icon warning -parent $w -message "Save failed: $err"`.

    10. `do_load {}` (LOCKED DECISION #1 — via demos::load_setup + validate_state):
        `set fname [tk_getOpenFile -defaultextension ".bcm" -title "Load bioCHEMeleon Setup" -filetypes [list {{bioCHEMeleon} {.bcm}} {{All files} {*}}] -parent $w]`
        If `$fname eq ""` return. `catch {::biochemeleon::demos::load_setup $fname} result` — on error (catch returns 1), `tk_messageBox -icon warning -parent $w -message "Load failed: $result"`. On success, `apply_state $result` (load_setup already calls validate_state internally — Plan 02 — so $result is a validated dict).

    === vmd/gui/dialog.tcl: add WM_DELETE_WINDOW handler ===
    In `open_dialog`, after `pack $nb`, add:
    `wm protocol $w WM_DELETE_WINDOW ::biochemeleon::on_close`
    Define `proc ::biochemeleon::on_close {}` in dialog.tcl:
    - `variable w`.
    - `catch { ::biochemeleon::setup_tab::collect_state }` — preserve in-progress edits into `::biochemeleon::state setup` (collect_state does `dict set ::biochemeleon::state setup $state`). Wrap in catch so a half-built form doesn't error on close.
    - `catch { trace vdelete vmd_molecule w ::biochemeleon::setup_tab::refresh_mol_menu }` — clean up the trace (Pitfall 5 — leaked trace causes errors on later mol add/delete; the ramaplot.tcl:265-267 cleanup pattern). Wrap in catch (trace vdelete is a no-op if the trace doesn't exist).
    - `destroy $w`.

    Tcl 8.5 constraints: NO lmap/try/throw/finally/tailcall/coroutine/yield. Use catch, foreach. Brace all expr. `info exists ::tk_version` (global qualifier). Build -command scripts with `[list ...]` for runtime values (NOT string interpolation — Pitfall 7).

    Commit: `feat(14-04): wire setup tab callbacks + WM_DELETE handler`
  </action>
  <verify>
    1. MODELESS GATE: `grep -rnE 'grab set' vmd/gui/` -> zero (the WM_DELETE handler does NOT use grab).
    2. 8.6 GATE: `grep -rnE '\b(lmap|try|throw|finally|tailcall|coroutine|yield)\b' vmd/gui/` -> zero.
    3. ttk::spinbox absence: `grep -rnE 'ttk::spinbox' vmd/gui/` -> zero.
    4. trace vdelete present (cleanup): `grep -rn 'trace vdelete vmd_molecule' vmd/gui/` -> at least 1 match (the WM_DELETE handler).
    5. No bare `return` stubs left (all callbacks have real bodies): `grep -nE 'proc ::biochemeleon::setup_tab::(do_save|do_load|do_reset|do_randomize|refresh_mol_menu|select_demo|update_cap)' vmd/gui/setup_tab.tcl` -> 7 matches (the procs exist); spot-check each has a multi-line body (not just `return`).
    6. Re-run the headless GUI smoke (from Plan 03) to confirm no loading regression:
       `rm -rf tmp/biochemeleon-vmd && mkdir -p tmp/biochemeleon-vmd && cp -r vmd tmp/biochemeleon-vmd/`
       `timeout 60 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/smoke/phase14_gui_smoke.tcl -eofexit < /dev/null' 2>&1 | grep BCHM_SMOKE_RESULT`
       EXPECT: PASS=1 (the callback bodies don't affect headless loading — Tk isn't loaded).
  </verify>
  <done>All 10 stub callbacks replaced with full implementations. WM_DELETE_WINDOW handler added (collect_state + trace vdelete + destroy). Gates pass. Headless GUI smoke still PASS=1.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The complete Phase 14 Setup tab: a modeless dialog with a ttk::notebook Setup tab containing (1) a 3-mode target selector (Loaded molecule / Fetch from PDB / Bundled demo) with a menubutton+menu dropdown refreshed via `trace variable vmd_molecule w`, (2) a hider-count spinbox capped to the molecule's atom count, a lock-scene checkbox, and 10 per-rep rows (checkbutton + count spinbox), (3) a difficulty toggle (easy/hard), and (4) four action buttons (Reset / Randomize / Save Setup / Load Setup). Bundled demos load via `mol new`. Save/Load round-trips to a `.bcm` file. The pure-layer logic (validate_state, randomize_state, randomize_per_rep) is tcltest-verified (Plan 01). The mol bridge (demos.tcl) is smoke-verified (Plan 02).
  </what-built>
  <how-to-verify>
    Run in VMD GUI mode (NOT WSL headless — Tk needs a real display). From a Windows terminal or VMD's console:

    1. START VMD: open VMD 1.9.3 normally (double-click or from Windows). You should see the 3D viewer + the Tk console.

    2. SOURCE THE SCRIPT: in the VMD Tk console, type:
       `source C:/path/to/bioCHEMeleon/vmd/biochemeleon.tcl`
       (Use the Windows path with forward slashes, e.g. `C:/Users/nglok/Desktop/WORKDIR/molmdl/bioCHEMeleon/vmd/biochemeleon.tcl`.)
       EXPECT: no error. The Extensions menu should now have a "Visualization" submenu with "bioCHEMeleon".

    3. OPEN THE DIALOG: click Extensions -> Visualization -> bioCHEMeleon (or type `biochemeleon` in the console).
       EXPECT: a "bioCHEMeleon" window opens with a ttk::notebook showing "Setup" and "Game" tabs. The 3D viewer stays interactive (you can rotate the empty scene with the mouse while the dialog is open — MODELESS check).

    4. SETUP TAB — TARGET GROUP (SETUP-01):
       - The "Target" group has 3 radio buttons: "Loaded object" / "Fetch from PDB" / "Bundled demo". Click each — the page below should switch (stacked frames).
       - "Bundled demo" page: a menubutton listing 6 demos (1znf, 1xdn, 5e54, 1k8p, 2qbz, 4wb3). Click it, select "1k8p".
         EXPECT: 1k8p loads into VMD (the 3D viewer shows the molecule — default Lines rep). The hider-count spinbox cap should update to 11 (1k8p has 555 atoms -> cap = max(1, 555/50) = 11).
       - Try another demo (e.g. 1znf — cap should be 8; 424 atoms -> 424/50 = 8).
       - "Loaded object" page: a menubutton + Refresh button. After loading demos, the menubutton should list the loaded molecules (e.g. "0 1k8p.pdb", "1 1znf.pdb"). Select one.
       - "Fetch from PDB" page: an entry field + a label "Phase 14: bundled demos only; fetch at Start (Phase 16+)".

    5. SETUP TAB — HIDERS GROUP (SETUP-02/03/04):
       - Hider count spinbox: try setting it above the cap (e.g. 50 for 1k8p) — it should clamp to 11. Try 0 — should clamp to 1.
       - Lock-scene checkbox: toggle it on/off (no immediate visible effect in Phase 14 — it affects START in Phase 16).
       - Per-rep rows: 10 rows (Lines, VDW, Licorice, CPK, Cartoon, NewCartoon, Trace, Tube, Points, DynamicBonds). Each has a checkbox + a spinbox (disabled when unchecked). Check "VDW" — its spinbox should enable. Set a count (e.g. 3). Uncheck "VDW" — spinbox disables + clears. Check "Cartoon" + set a count.

    6. SETUP TAB — DIFFICULTY (SETUP-05):
       - Two radio buttons: Easy / Hard. Toggle between them. (Easy = show remaining per-rep; Hard = total only — affects the Game tab in Phase 16.)

    7. ACTION BUTTONS (BTN-01/02/03/04):
       - RESET (BTN-01): click it. EXPECT: all widgets return to defaults (mode=loaded, hider_count=10, no per-rep checked, difficulty=easy).
       - RANDOMIZE (BTN-02): click it. EXPECT: hider_count + per_rep + difficulty get random values. The per_rep should have a NON-EMPTY set of reps with counts (quick-008 — SETUP-06). Click Randomize a few times — each time should produce a different random distribution (non-deterministic). Check that per_rep is never empty when hider_count > 0.
       - SAVE SETUP (BTN-03): click it. A file dialog opens. Save to e.g. `C:/tmp/test_setup.bcm`. Open the file in a text editor — it should be human-readable key-value lines (format/target_mode/hider_count/per_rep_entry/etc.).
       - LOAD SETUP (BTN-04): click it. A file dialog opens. Load the file you just saved. EXPECT: all widgets reflect the saved state (round-trip). Try editing the .bcm file by hand (e.g. change hider_count to 5) + reload — the widget should show 5 (validate_state clamps if out of range).

    8. MODELESS + CLOSE (ENTRY-01, trace cleanup):
       - While the dialog is open, rotate the 3D viewer with the mouse (left-drag). It should rotate freely (dialog does NOT block the viewer).
       - Close the dialog (X button or `wm protocol WM_DELETE_WINDOW`). Re-open it (`biochemeleon` in console or menu). EXPECT: the dialog re-opens with the state you had when you closed it (WM_DELETE handler preserved in-progress edits via collect_state).
       - After closing, load a new molecule in the VMD console (e.g. `mol new` some PDB). EXPECT: NO error spam (the trace vdelete cleaned up the refresh_mol_menu callback — Pitfall 5).

    9. RE-SOURCE GUARD: source the entry again (`source ...biochemeleon.tcl`). EXPECT: a warning "bioCHEMeleon already loaded; ignoring re-source" (no duplicate dialog, no state reset).

    If ANYTHING fails: describe the issue (what you did, what you expected, what happened) and the executor will fix it. The most likely issues: ttk widget rendering surprises (can't verify headless), spinbox cap not updating on demo select, per-rep spinbox enable/disable not working, trace callback signature mismatch ("wrong # args").
  </how-to-verify>
  <resume-signal>Type "approved" if the Setup tab works end-to-end, or describe the issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- All 10 callbacks have full implementations (no stub `return` bodies).
- WM_DELETE_WINDOW handler: collect_state + trace vdelete + destroy (no leaked trace).
- Modeless gate: no `grab set` in vmd/gui/ (viewer stays interactive).
- 8.6 gate: zero matches in vmd/gui/.
- Headless GUI smoke: still PASS=1 (no loading regression).
- GUI human-verify checkpoint: approved (the full Setup tab works end-to-end — demos load, hider count caps, per-rep rows work, 4 buttons work, Save/Load round-trips, dialog stays modeless, close+reopen preserves state, trace cleanup works).
</verification>

<success_criteria>
- SETUP-01: molecule dropdown (loaded mols + bundled demos + fetch option); selecting a bundled demo loads via mol new.
- SETUP-02: hider count spinbox capped to atom count.
- SETUP-03: lock-scene checkbox (state reflected; detection is Phase 16 START).
- SETUP-04: per-rep rows with optional per-rep counts (checkbox toggles spinbox).
- SETUP-05: difficulty toggle (easy/hard).
- SETUP-06: Randomize distributes across a random non-empty subset of reps (quick-008 baked in — Plan 01's randomize_per_rep).
- BTN-01/02/03/04: Reset/Randomize/Save/Load each work as labeled; Save/Load round-trips to a .bcm file.
- DEMO-01: 6 bundled demos load (1znf, 1xdn, 5e54, 1k8p, 2qbz, 4wb3).
- Dialog is modeless (viewer interactive); close+reopen preserves state; trace cleanup works.
</success_criteria>

<output>
After completion, create `.planning/phases/14-setup-tab-bundled-demos/14-04-SUMMARY.md`
</output>
