---
phase: 14-setup-tab-bundled-demos
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - vmd/lib/demos.tcl
  - vmd/smoke/phase14_mol_smoke.tcl
autonomous: true
must_haves:
  truths:
    - "load_demo loads all 6 bundled demos via mol new with correct atom counts (1znf=424, 1xdn=2597, 5e54=2844, 1k8p=555, 2qbz=3408, 4wb3=3779)"
    - "get_active_reps detects reps matching GAME_REPS using the combined-braces molinfo form (single-field form FAILS)"
    - "list_loaded_molecules returns '<name> (<molid>)' display strings"
    - "save_setup/load_setup round-trips a setup dict with dict eq = 1 (DEFAULTS-key-order rebuild on load)"
    - "fetch_pdb stub returns -code error (Phase 14 deferral; Phase 21 implements real fetch)"
    - "Headless mol smoke passes (BCHM_SMOKE_RESULT PASS=1)"
  artifacts:
    - path: vmd/lib/demos.tcl
      provides: "mol bridge: to_vmd_path, list_loaded_molecules, load_demo, get_active_reps, fetch_pdb (stub), save_setup, load_setup, atom_count"
      contains: "namespace eval ::biochemeleon::demos"
    - path: vmd/smoke/phase14_mol_smoke.tcl
      provides: "Headless smoke verifying 6 demos load + rep detection + save/load round-trip"
      contains: "BCHM_SMOKE_RESULT"
  key_links:
    - from: "vmd/lib/demos.tcl"
      to: "vmd/lib/setup_state.tcl"
      via: "source for GAME_REPS/DEMO_MANIFEST/SETUP_FORMAT/DEFAULTS constants"
      pattern: "source.*setup_state\\.tcl"
    - from: "::biochemeleon::demos::load_demo"
      to: "mol new"
      via: "script-relative path via [file dirname [info script]] (always C:/ inside VMD)"
      pattern: "mol new .* type pdb"
    - from: "::biochemeleon::demos::load_setup"
      to: "::biochemeleon::setup_state::validate_state"
      via: "validates + canonicalizes loaded dict before returning"
      pattern: "validate_state \\$loaded"
---

<objective>
Mol bridge `demos.tcl`: the VMD mol/molinfo wrapper layer that mirrors v1's `demos.py`. Provides demo loading, molecule enumeration, active-rep detection, the fetch_pdb stub, the Save/Load setup file I/O, and an atom_count helper for the cap computation. Plus a headless smoke verifying all 6 demos load, rep detection works, and Save/Load round-trips.

Purpose: This is the mol-coupled layer the GUI (Plan 03) calls for every molecule operation. It sources the pure layer (Plan 01) for constants. Building + smoke-testing it in isolation (before the GUI) de-risks the mol command surface.

Output: vmd/lib/demos.tcl (8 procs in `::biochemeleon::demos`) + vmd/smoke/phase14_mol_smoke.tcl (headless verification).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-setup-tab-bundled-demos/14-RESEARCH-mol-logic.md
@vmd/AGENTS.md
@vmd/lib/setup_state.tcl
@vmd/smoke/phase13_smoke.tcl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create vmd/lib/demos.tcl (mol bridge — 8 procs)</name>
  <files>vmd/lib/demos.tcl</files>
  <action>
    Create vmd/lib/demos.tcl. Read 14-RESEARCH-mol-logic.md lines 235-317 (core procs skeleton) + lines 595-643 (save/load skeleton). Every proc was probe-verified against VMD 1.9.3 headless — transcribe the skeletons.

    Structure (file header + namespace + source the pure layer):
    - `source [file join [file dirname [info script]] setup_state.tcl]` (sources setup_state for constants — Pattern 1; `[info script]` is set correctly because demos.tcl is always `source`d, never `-e`d directly).
    - `namespace eval ::biochemeleon::demos { namespace export to_vmd_path list_loaded_molecules load_demo get_active_reps fetch_pdb save_setup load_setup atom_count }`

    The 8 procs (all from verified research skeletons):
    1. `to_vmd_path {path}` (research lines 253-258): WSL -> VMD path guard. `/mnt/c/...` -> `C:/...` (FORWARD slashes, NOT backslashes — vmd/AGENTS.md). No-op for other paths. Regex `^/mnt/([a-zA-Z])/(.*)$`.
    2. `list_loaded_molecules {}` (research lines 262-268): returns list of `"[molinfo $m get name] ($m)"` for each molid in `molinfo list`. Name is the FILENAME basename (read-only — Pitfall 1; you CANNOT rename a molecule).
    3. `load_demo {demo_id}` (research lines 273-293): resolves bundled demo path via `[file dirname [info script]]` (script-relative -> always C:/ inside VMD). Path = `[file normalize [file join [file dirname [info script]] .. data demos $cache_name]]`. Guard with `to_vmd_path` (defensive). `file exists` check. `catch {mol new $path type pdb} molid` — return molid on success, `-code error` with message on failure. Reject non-bundled source entries (Phase 14 manifest is bundled-only).
    4. `get_active_reps {mol}` (research lines 298-309): detect active reps matching GAME_REPS. Use the COMBINED-BRACES molinfo form: `molinfo $mol get "{rep $i} {selection $i} {color $i} {material $i}"` + `foreach {style sel col mat} ... { break }` (Pattern 2 — single-field `molinfo $mol get {rep $i}` FAILS, Pitfall 3). Filter with `lsearch -exact $GAME_REPS $style`. Wrap `molinfo $mol get numreps` in catch (bad molid -> empty list).
    5. `fetch_pdb {code}` (research lines 314-316): STUB. Returns `-code error "fetch_pdb not implemented in Phase 14 (VMD 1.9.3 lacks tls for HTTPS); use a bundled demo"`. VMD 1.9.3 has http but NO tls -> HTTPS (RCSB) impossible (Pitfall 6). The real fetch is Phase 21.
    6. `save_setup {state filepath}` (research lines 595-612): key-value line format (LOCKED DECISION #1 — NOT `[list]`+`source`). Writes `format $SETUP_FORMAT` first, then iterates the state dict: scalar keys as `$key $value`, per_rep as `per_rep_count N` + `per_rep_entry $rep $cnt` lines, pdb_pool as `pdb_pool_count N` + `pdb_pool_entry $code` lines. Skip the `format` key in the loop (already written). stdlib only (open/puts/close).
    7. `load_setup {filepath}` (research lines 614-643): parse the key-value format. `file exists` check -> `-code error`. Read lines with `gets`, split on space, `switch -- $key` on key. Collect per_rep + pdb_pool separately. CRITICAL: rebuild the loaded dict in DEFAULTS KEY ORDER (Pitfall 5 — tcl `dict eq` is ORDER-SENSITIVE; without this rebuild, round-trip eq=0 even with identical content). The key order list: `format target_mode selected_object pdb_code demo_id hider_count lock_scene per_rep difficulty_easy lock_source pdb_pool`. Then call `::biochemeleon::setup_state::validate_state $loaded` (Plan 01) to canonicalize/clamp before returning (defense in depth for hand-edited files).
    8. `atom_count {molid}` (NEW — not in research but needed by the GUI cap computation): returns `molinfo $molid get numatoms`, or 0 on error (bad molid / no mol loaded). Wrap in catch. The GUI research skeleton references `::biochemeleon::demos::atom_count_for_current` — implement the simpler `atom_count {molid}` form; the GUI (Plan 04) resolves the current molid from the target mode and calls this.

    Tcl 8.5 constraints (vmd/AGENTS.md): NO lmap/try/throw/finally/tailcall/coroutine/yield. Use foreach+lappend, catch. Brace all expr. Use `variable ::biochemeleon::setup_state::GAME_REPS` etc. for cross-namespace access (research uses this fully-qualified form — verified).

    Commit: `feat(14-02): add mol bridge demos.tcl`
  </action>
  <verify>
    1. Tcl 8.6 FEATURES GATE (demos.tcl uses mol — that is ALLOWED — but must have ZERO tk/toplevel/ttk and ZERO 8.6 idioms):
       `grep -rnE '\b(tk|toplevel|ttk)\b' vmd/lib/demos.tcl` -> zero matches.
       `grep -rnE '\b(lmap|try|throw|finally|tailcall|coroutine|yield)\b' vmd/lib/demos.tcl` -> zero matches.

    2. Quick syntax check (tclsh parses the file; mol commands won't resolve but proc DEFINITIONS parse fine):
       `tclsh -c 'source vmd/lib/setup_state.tcl; if {[catch {source vmd/lib/demos.tcl} err]} { puts "SYNTAX_ERROR: $err"; exit 1 } else { puts "OK" }'`
       EXPECT: OK (no syntax error). A syntax error in demos.tcl surfaces here.

    3. Do NOT run the mol smoke yet — that is Task 2.
  </verify>
  <done>vmd/lib/demos.tcl created with all 8 procs. Passes the 8.6-features gate + tk-gate. Sources cleanly under tclsh (syntax valid).</done>
</task>

<task type="auto">
  <name>Task 2: Create headless mol smoke + run full verification</name>
  <files>vmd/smoke/phase14_mol_smoke.tcl</files>
  <action>
    Create vmd/smoke/phase14_mol_smoke.tcl. Follow the Phase 13 smoke pattern (see @vmd/smoke/phase13_smoke.tcl): use `[pwd]` for path resolution (VMD cwd = staging root under `vmd -e`; `[info script]` is empty under `-e` — Pitfall 3), collect failures in a list, print `BCHM_SMOKE_RESULT PASS=1 FAIL=none` or `PASS=0 FAIL=...` marker at the end.

    The smoke sources demos.tcl directly (which sources setup_state.tcl):
    `source [file join [pwd] vmd lib demos.tcl]`

    Verify these checks (each appends to `failures` on mismatch — use `catch` around mol ops so a failure appends instead of aborting):
    1. `to_vmd_path` — `/mnt/c/Users/foo` -> `C:/Users/foo`; `C:/already` -> `C:/already`; `relative/path` -> `relative/path`.
    2. `load_demo` all 6 demos — load each, assert `molinfo <molid> get numatoms` matches the verified counts: 1znf=424, 1xdn=2597, 5e54=2844, 1k8p=555, 2qbz=3408, 4wb3=3779 (research lines 50-59).
    3. `list_loaded_molecules` — after loading 6 demos, returns 6 display strings of the form `<name> (<molid>)`.
    4. `get_active_reps` — on a freshly loaded mol, returns `Lines` (the default rep). Then add a rep (`mol representation VDW; mol addrep $mol`) -> returns `Lines VDW`. Then `mol delrep 0 $mol` -> returns `VDW` (rep0 deleted, VDW shifted to index 0 — verifies style detection survives renumbering). Use the LAST loaded molid for this test.
    5. `atom_count` — on a loaded molid, returns the same numatoms as `molinfo`. On a bad molid (e.g. 999), returns 0 (catch).
    6. `fetch_pdb` — `catch {::biochemeleon::demos::fetch_pdb 1CRN} err` returns 1 (error), and err contains "not implemented".
    7. `save_setup`/`load_setup` round-trip — build a test state via `::biochemeleon::setup_state::randomize_state 42 555` (Plan 01 — gives a complete 11-key state with per_rep populated), save to a temp file under `[pwd]` (e.g. `[file join [pwd] tmp_test_setup.bcm]`), load it back, assert `[dict eq $loaded $original]` = 1 (the DEFAULTS-key-order rebuild makes this pass — Pitfall 5). Clean up the temp file with `file delete`.

    Print the BCHM_SMOKE_RESULT marker at the end (VMD does NOT propagate tcl exit codes — the runner greps the marker, NEVER `$?`).

    Commit: `test(14-02): add mol bridge headless smoke`
  </action>
  <verify>
    Stage + run the smoke under headless VMD (the staging pattern from Phase 13; run from a /mnt/c cwd):
    ```
    rm -rf tmp/biochemeleon-vmd && mkdir -p tmp/biochemeleon-vmd && cp -r vmd tmp/biochemeleon-vmd/
    timeout 90 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/smoke/phase14_mol_smoke.tcl -eofexit < /dev/null' 2>&1 | tee /tmp/bchm_mol_smoke
    grep 'BCHM_SMOKE_RESULT' /tmp/bchm_mol_smoke
    ```
    EXPECT: `BCHM_SMOKE_RESULT PASS=1 FAIL=none`. If FAIL is not none, read the failure list + fix demos.tcl (the smoke identifies exactly which proc/check failed).

    NOTE: VMD may print a Stride secondary-structure warning ("Error reading PDB file") when adding Cartoon reps — this is harmless (the rep is still created). The smoke adds VDW reps (no Stride), so this should not appear, but if it does, it is not a failure.
  </verify>
  <done>Headless mol smoke passes (BCHM_SMOKE_RESULT PASS=1 FAIL=none). All 6 demos load with correct atom counts. get_active_reps detects reps. save/load round-trips with dict eq=1.</done>
</task>

</tasks>

<verification>
- Headless smoke: `BCHM_SMOKE_RESULT PASS=1 FAIL=none` (all 7 check groups pass).
- 8.6-features gate on demos.tcl: zero matches.
- tk-gate on demos.tcl: zero matches (demos.tcl is the mol bridge — no Tk).
- save/load round-trip: `dict eq $loaded $original` = 1 (DEFAULTS-key-order rebuild verified).
- All 6 bundled demos load with verified atom counts (DEMO-01).
</verification>

<success_criteria>
- demos.tcl provides all 8 procs (to_vmd_path, list_loaded_molecules, load_demo, get_active_reps, fetch_pdb stub, save_setup, load_setup, atom_count).
- load_demo loads all 6 bundled demos via mol new (DEMO-01).
- get_active_reps uses the combined-braces molinfo form (survives mol delrep renumbering).
- save_setup/load_setup round-trips setup parameters (BTN-03/BTN-04 backend).
- fetch_pdb is a clean stub (Phase 21 implements real fetch).
- Headless smoke passes (BCHM_SMOKE_RESULT PASS=1).
</success_criteria>

<output>
After completion, create `.planning/phases/14-setup-tab-bundled-demos/14-02-SUMMARY.md`
</output>
