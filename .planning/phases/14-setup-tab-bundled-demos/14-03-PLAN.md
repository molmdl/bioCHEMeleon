---
phase: 14-setup-tab-bundled-demos
plan: 03
type: execute
wave: 3
depends_on: ["14-01", "14-02"]
files_modified:
  - vmd/gui/dialog.tcl
  - vmd/gui/setup_tab.tcl
  - vmd/biochemeleon.tcl
  - vmd/smoke/phase14_gui_smoke.tcl
autonomous: true
must_haves:
  truths:
    - "Sourcing biochemeleon.tcl headlessly loads gui/dialog.tcl + gui/setup_tab.tcl + demos.tcl without error"
    - "open_dialog creates the toplevel + ttk::notebook + calls setup_tab::build to populate the Setup tab (in GUI mode)"
    - "setup_tab::build lays out all 4 groups: Target (3-mode selector + stacked pages), Hiders (spinbox + lock-scene + 10 per-rep rows), Difficulty, Actions (4 buttons)"
    - "collect_state/apply_state round-trip a state dict through the widgets (the _loading guard suppresses cascading callbacks during apply_state)"
    - "The entry script remains a thin bootstrap (open_dialog extracted to gui/dialog.tcl; public procs + menu registration kept)"
  artifacts:
    - path: vmd/gui/dialog.tcl
      provides: "::biochemeleon::open_dialog (toplevel + ttk::notebook Setup+Game tabs; sources setup_tab.tcl; calls setup_tab::build)"
      contains: "proc ::biochemeleon::open_dialog"
    - path: vmd/gui/setup_tab.tcl
      provides: "::biochemeleon::setup_tab namespace: build, build_target_group, build_hiders_group, build_diff_group, build_actions, collect_state, apply_state, switch_page + stub callbacks"
      contains: "proc ::biochemeleon::setup_tab::build"
    - path: vmd/biochemeleon.tcl
      provides: "Entry script modified to source demos.tcl + gui/dialog.tcl; inline open_dialog removed"
      contains: "source.*gui.*dialog"
    - path: vmd/smoke/phase14_gui_smoke.tcl
      provides: "Headless smoke: entry sources cleanly + all namespaces + setup_tab procs exist"
      contains: "BCHM_SMOKE_RESULT"
  key_links:
    - from: "vmd/biochemeleon.tcl"
      to: "vmd/gui/dialog.tcl"
      via: "source [file join $_dir gui dialog.tcl]"
      pattern: "source.*gui.*dialog"
    - from: "vmd/gui/dialog.tcl"
      to: "vmd/gui/setup_tab.tcl"
      via: "source + setup_tab::build $nb.setup"
      pattern: "setup_tab::build"
    - from: "vmd/gui/setup_tab.tcl"
      to: "vmd/lib/demos.tcl"
      via: "calls ::biochemeleon::demos::* for molecule operations (Plan 02)"
      pattern: "::biochemeleon::demos::"
    - from: "vmd/gui/setup_tab.tcl"
      to: "vmd/lib/setup_state.tcl"
      via: "calls ::biochemeleon::setup_state::DEFAULTS/validate_state (Plan 01)"
      pattern: "::biochemeleon::setup_state::"
---

<objective>
GUI Setup tab structure: extract `open_dialog` from the entry to `gui/dialog.tcl`, build the complete `gui/setup_tab.tcl` form (all widget groups + state plumbing via collect_state/apply_state + the _loading guard), and wire the entry to source the new GUI files. The form renders and state round-trips programmatically; callbacks are STUBS (Plan 04 fills them in + adds the human-verify checkpoint).

Purpose: This is the visible form the user interacts with. Building the full widget tree + state plumbing in isolation (with stub callbacks) means Plan 04 is purely "wire the callbacks" — a clean, focused task. The headless smoke verifies the entry sources cleanly + all namespaces/procs exist (Tk can't render headless; widget rendering is verified in Plan 04's GUI checkpoint).

Output: vmd/gui/dialog.tcl (extracted open_dialog) + vmd/gui/setup_tab.tcl (full form + stub callbacks) + modified vmd/biochemeleon.tcl + vmd/smoke/phase14_gui_smoke.tcl.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-setup-tab-bundled-demos/14-RESEARCH-gui.md
@vmd/AGENTS.md
@vmd/biochemeleon.tcl
@vmd/smoke/phase13_smoke.tcl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gui/dialog.tcl + gui/setup_tab.tcl + modify entry</name>
  <files>vmd/gui/dialog.tcl, vmd/gui/setup_tab.tcl, vmd/biochemeleon.tcl</files>
  <action>
    Read 14-RESEARCH-gui.md fully — it contains verified ttk widget patterns + a concrete setup_tab.tcl skeleton (lines 482-537) + collect_state/apply_state (Pattern 6, lines 249-324) + the 3-mode selector (Pattern 2, lines 99-146) + molecule dropdown (Pattern 3, lines 148-187) + per-rep rows (Pattern 4, lines 189-218) + hider count spinbox (Pattern 5, lines 220-247) + action buttons (lines 455-480).

    IMPORTANT CORRECTIONS to the GUI research (locked decisions override the research):
    - Use plain `spinbox` (NOT `ttk::spinbox` — ABSENT in Tk 8.5.6; Pitfall 1, research line 37).
    - Save/Load calls `::biochemeleon::demos::save_setup`/`load_setup` (key-value format, LOCKED DECISION #1) — NOT the research's `[list]`+`source` pattern (lines 416-452). The do_save/do_load stubs in THIS plan just `return`; Plan 04 fills them with tk_getSaveFile/tk_getOpenFile + demos::save_setup/load_setup.
    - The `.bcm` file extension (NOT `.bcm.setup`).
    - `::tk_version` (global qualifier) inside procs — NEVER bare `tk_version` (Phase 13 lesson).

    === FILE 1: vmd/gui/dialog.tcl ===
    Extract `::biochemeleon::open_dialog` from the entry (currently biochemeleon.tcl lines 85-104). The proc:
    - `variable w` (namespace scope).
    - Singleton re-show: `if {[winfo exists .biochemeleon]} { wm deiconify $w; return }`.
    - Create `toplevel .biochemeleon`, set title "bioCHEMeleon".
    - Create `ttk::notebook $w.nb` + `$nb.setup` (ttk::frame) + `$nb.game` (ttk::frame). Add both tabs.
    - `source [file join [file dirname [info script]] setup_tab.tcl]` (source the Setup tab module — sourced once; proc resolution is at call-time in tcl so sourcing at load-time is safe).
    - Call `::biochemeleon::setup_tab::build $nb.setup` to populate the Setup tab.
    - Game tab: keep the Phase 13 placeholder label "Game tab (Phase 16)".
    - `pack $nb -fill both -expand yes`.
    - NO `grab set` on the main panel (ENTRY-01 — modeless; viewer must stay interactive).
    - NO WM_DELETE_WINDOW handler yet (Plan 04 adds it — it calls collect_state + trace vdelete which reference Plan 04 callbacks).

    === FILE 2: vmd/gui/setup_tab.tcl ===
    Build the `::biochemeleon::setup_tab` namespace. Structural procs get FULL bodies; callback procs get STUB bodies (`return`). This makes the form render + state round-trip, while buttons/dropdown do nothing until Plan 04.

    Namespace variables (research lines 490-506):
    `mode` (loaded|fetch|demo), `selected_mol`, `pdb_code`, `demo_id` (default "1znf"), `hider_count` (default 10), `lock_scene` (0), `difficulty_easy` (1), `lock_source` (0), `rep_sel` (array), `rep_cnt` (array), `_loading` (0 — the guard), `_pages` (array mode->frame), `w` (toplevel), `current_molid` (for cap computation — set by Plan 04 callbacks).

    FULL-BODY procs:
    1. `build {parent}` (research lines 509-528): set `w [winfo toplevel $parent]`. Call build_target_group, build_hiders_group, build_diff_group, build_actions. Pack them ` -side top -fill x -padx 8 -pady 6`. Initialize: read `dict get $::biochemeleon::state setup`; if it has a `format` key, `apply_state [validate_state $saved]`; else `apply_state $DEFAULTS`. Register the trace `trace variable vmd_molecule w ::biochemeleon::setup_tab::refresh_mol_menu` (safe — refresh_mol_menu is a stub). Call `refresh_mol_menu` once (stub no-op in this plan; Plan 04 makes it real).
    2. `build_target_group {parent}` (Pattern 2, research lines 108-139): ttk::labelframe "Target" + 3 ttk::radiobuttons (Loaded/Fetch/Demo) sharing `-variable ::biochemeleon::setup_tab::mode` + `-command {::biochemeleon::setup_tab::switch_page}`. Stacked pages container (3 frames in ONE grid cell, `raise` the selected). Loaded page: `menubutton` + `menu` + `button` Refresh (the clonerep pattern, research lines 157-170). Fetch page: `ttk::entry` for pdb_code + `ttk::label` "Phase 14: bundled demos only; fetch at Start (Phase 16+)". Demo page: `menubutton` + `menu` with 6 entries from `$DEMO_MANIFEST` (each `-command [list ::biochemeleon::setup_tab::select_demo $id]`).
    3. `build_hiders_group {parent}` (Patterns 4+5): ttk::labelframe "Hiders". Hider count: `ttk::label` + plain `spinbox` (NOT ttk::spinbox) with `-from 1 -to 50 -increment 1 -textvariable ...hider_count -command {::biochemeleon::setup_tab::recompute_per_rep_maxes}`. Lock-scene: `ttk::checkbutton` bound to `lock_scene`. Per-rep: 10 rows (one per GAME_REPS rep) — each row: `ttk::checkbutton` (-variable rep_sel($rep), -command `[list ::biochemeleon::setup_tab::on_rep_toggled $rep]`) + plain `spinbox` (-from 0 -to 50, -textvariable rep_cnt($rep), -state disabled) + `ttk::label` "random". Use `grid` with `incr row` (research lines 199-217).
    4. `build_diff_group {parent}`: ttk::labelframe "Difficulty" + 2 ttk::radiobuttons (Easy=1/Hard=0) sharing `-variable ...difficulty_easy`. Easy = show remaining per-rep; Hard = total only (SETUP-05).
    5. `build_actions {parent}` (research lines 458-466): ttk::frame + 4 ttk::buttons (Reset/Randomize/Save Setup.../Load Setup...) with `-command` scripts referencing do_reset/do_randomize/do_save/do_load (stubs in this plan).
    6. `collect_state {}` (Pattern 6, research lines 263-285): snapshot all widget-bound vars into a dict. Build per_rep from rep_sel/rep_cnt arrays (only checked reps with count > 0). Return a dict with all 11 DEFAULTS keys. `pdb_pool` = `[list]` (pool editor is a later refinement).
    7. `apply_state {state}` (Pattern 6, research lines 287-322): set `_loading 1`, populate all widget vars from the state dict, call `switch_page`, update per-rep checkbuttons/spinboxes, call `on_rep_toggled` for each rep (guarded by _loading). Wrap the body in `catch` so `_loading` is ALWAYS reset to 0 after (tcl 8.5 has no try/finally — the catch pattern, research line 293-318). Use `dict get $state key $default` (3-arg form, available in 8.5) for safety on half-populated dicts. Persist: `dict set ::biochemeleon::state setup $state`.
    8. `switch_page {}` (research lines 141-145): `raise $_pages($mode)`.

    STUB procs (body = `return` — Plan 04 replaces with full impls):
    `refresh_mol_menu {args}`, `select_demo {demo_id}`, `select_loaded_mol {molid}`, `on_rep_toggled {rep}`, `update_cap {}`, `recompute_per_rep_maxes {}`, `do_reset {}`, `do_randomize {}`, `do_save {}`, `do_load {}`.
    NOTE: `refresh_mol_menu` takes `args` (the trace callback signature is `{name1 name2 op}` — Pitfall 2; the `args` form accepts and ignores them).

    Tcl 8.5 constraints (vmd/AGENTS.md): NO lmap/try/throw/finally/tailcall/coroutine/yield. Use foreach+lappend, catch. Brace all expr: `[expr {!!$lock_scene}]` for bool coercion. Inside procs: `variable name` for namespace vars. Use `info exists ::tk_version` (global qualifier) for any Tk guard.

    === FILE 3: vmd/biochemeleon.tcl (modify) ===
    In the source block (after `source registry.tcl`, line 72), ADD:
    - `source [file join $_dir lib demos.tcl]` (Plan 02's mol bridge — the GUI needs it).
    - `source [file join $_dir gui dialog.tcl]` (the extracted open_dialog + setup_tab).
    REMOVE the inline `proc ::biochemeleon::open_dialog {}` block (lines 85-104) — it now lives in gui/dialog.tcl. KEEP the public procs (`biochemeleon`, `biochemeleon_tk_cb`) + the menu registration — they call `::biochemeleon::open_dialog` which is now defined in dialog.tcl (proc resolution is at call-time, so this works).

    Commit: `feat(14-03): add GUI setup tab structure + extract open_dialog to dialog.tcl`
  </action>
  <verify>
    1. MODELESS GATE (no `grab set` on the main panel — ENTRY-01):
       `grep -rnE 'grab set' vmd/gui/` -> zero matches.
    2. TCL 8.6 FEATURES GATE on gui/:
       `grep -rnE '\b(lmap|try|throw|finally|tailcall|coroutine|yield)\b' vmd/gui/` -> zero matches.
    3. ttk::spinbox ABSENCE check (must use plain spinbox):
       `grep -rnE 'ttk::spinbox' vmd/gui/` -> zero matches.
    4. tk_version global-qualifier check (no bare tk_version inside procs):
       `grep -rnE '\binfo exists tk_version\b' vmd/gui/` -> zero matches (should be `::tk_version`).
    5. Quick syntax check (tclsh parses both GUI files; ttk commands won't resolve but proc definitions parse):
       `tclsh -c 'source vmd/lib/setup_state.tcl; source vmd/lib/demos.tcl; if {[catch {source vmd/gui/dialog.tcl} err]} { puts "DIALOG_ERR: $err" }; if {[catch {source vmd/gui/setup_tab.tcl} err]} { puts "SETUP_ERR: $err" }; puts OK'`
       EXPECT: OK (no syntax error). Note: dialog.tcl sources setup_tab.tcl, so sourcing both is redundant but harmless.
  </verify>
  <done>gui/dialog.tcl + gui/setup_tab.tcl created with full structural procs + stub callbacks. Entry modified to source them. Passes modeless gate, 8.6 gate, ttk::spinbox-absence check, tk_version-qualifier check. tclsh syntax clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create headless GUI smoke + run verification</name>
  <files>vmd/smoke/phase14_gui_smoke.tcl</files>
  <action>
    Create vmd/smoke/phase14_gui_smoke.tcl. Follow the Phase 13 smoke pattern (@vmd/smoke/phase13_smoke.tcl): use `[pwd]` for path resolution, collect failures, print BCHM_SMOKE_RESULT marker.

    The smoke sources the ENTRY script (which now sources setup_state + registry + demos + gui/dialog + gui/setup_tab):
    `source [file join [pwd] vmd biochemeleon.tcl]`

    In headless mode (`-dispdev text`), Tk does NOT load, so the GUI is NOT built. The smoke verifies the LOADING layer (namespaces + procs exist):
    1. Entry sources cleanly (catch -> no error).
    2. `biochemeleon` command exists (`info commands`).
    3. `::biochemeleon::open_dialog` proc exists (defined in dialog.tcl — `llength [info procs ::biochemeleon::open_dialog]` > 0).
    4. Namespace `::biochemeleon::setup_state` exists.
    5. Namespace `::biochemeleon::demos` exists (Plan 02, sourced by entry).
    6. Namespace `::biochemeleon::setup_tab` exists.
    7. `::biochemeleon::setup_tab::build` proc exists.
    8. `::biochemeleon::setup_tab::collect_state` proc exists.
    9. `::biochemeleon::setup_tab::apply_state` proc exists.
    10. Pure-layer tcltest still passes (re-run test_setup_state.test to confirm Plan 01's logic wasn't broken — optional but recommended: `source` the test file if it exists and check the marker; OR just assert the namespace + a spot-check like `validate_state [dict create]` returns a dict with `format` key).

    Do NOT call `biochemeleon` (it would try to open the GUI — tk_version-guarded no-op headless, but the smoke should focus on the loading layer). Do NOT call `open_dialog` (needs Tk).

    Print BCHM_SMOKE_RESULT marker.

    Commit: `test(14-03): add GUI headless smoke`
  </action>
  <verify>
    Stage + run the smoke:
    ```
    rm -rf tmp/biochemeleon-vmd && mkdir -p tmp/biochemeleon-vmd && cp -r vmd tmp/biochemeleon-vmd/
    timeout 60 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/smoke/phase14_gui_smoke.tcl -eofexit < /dev/null' 2>&1 | tee /tmp/bchm_gui_smoke
    grep 'BCHM_SMOKE_RESULT' /tmp/bchm_gui_smoke
    ```
    EXPECT: `BCHM_SMOKE_RESULT PASS=1 FAIL=none`. If FAIL, read the failure list (which namespace/proc is missing) + fix.

    Also re-run the Phase 13 smoke to confirm no regression:
    `grep 'BCHM_SMOKE_RESULT' <(timeout 60 bash -ic 'cd tmp/biochemeleon-vmd && vmd -dispdev text -e vmd/smoke/phase13_smoke.tcl -eofexit < /dev/null' 2>&1)`
    EXPECT: PASS=1.
  </verify>
  <done>Headless GUI smoke passes (BCHM_SMOKE_RESULT PASS=1). Entry sources cleanly with all GUI namespaces + procs loaded. Phase 13 smoke still passes (no regression).</done>
</task>

</tasks>

<verification>
- Headless GUI smoke: BCHM_SMOKE_RESULT PASS=1 (entry sources cleanly; ::biochemeleon::setup_tab namespace + build/collect_state/apply_state procs exist).
- Phase 13 smoke: still PASS=1 (no regression).
- Modeless gate: `grep -rnE 'grab set' vmd/gui/` -> zero (ENTRY-01).
- 8.6 gate on gui/: zero.
- ttk::spinbox absence: zero (use plain spinbox).
- tk_version qualifier: all `info exists ::tk_version` (no bare tk_version in procs).
</verification>

<success_criteria>
- gui/dialog.tcl defines ::biochemeleon::open_dialog (extracted from entry; modeless toplevel + ttk::notebook + setup_tab::build).
- gui/setup_tab.tcl defines the full form (4 groups: Target/Hiders/Difficulty/Actions) + collect_state/apply_state (with _loading guard) + switch_page + stub callbacks.
- Entry script sources demos.tcl + gui/dialog.tcl; inline open_dialog removed; public procs + menu registration intact.
- Headless smoke passes (entry loads cleanly; all namespaces + procs exist).
- The form uses plain spinbox (NOT ttk::spinbox), menubutton+menu for dropdowns, 3 radiobuttons + frame-raise for the mode selector.
</success_criteria>

<output>
After completion, create `.planning/phases/14-setup-tab-bundled-demos/14-03-SUMMARY.md`
</output>
