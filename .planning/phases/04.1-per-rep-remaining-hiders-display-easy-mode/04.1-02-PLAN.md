---
phase: 04.1
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - biochemeleon/setup_state.py
  - tests/test_setup_state.py
autonomous: true

must_haves:
  truths:
    - "format_remaining(total, counts_by_rep, easy_mode) returns the remaining-hiders label text (GAME-03)"
    - "easy_mode False (hard) → 'Remaining: {total}' only — unchanged from Phase 4 (SC2)"
    - "easy_mode True with some per-rep counts > 0 → 'Remaining: {total}  ({rep}: {count}, ...)' with TWO spaces before the parenthetical (SC1)"
    - "easy_mode True but all per-rep counts 0 (win state) → 'Remaining: {total}' (no empty parenthetical)"
    - "counts_by_rep None or empty → 'Remaining: {total}' (safe total-only default)"
    - "per-rep entries appear in GAME_REPS order, only reps with count > 0 (matches SC1's format; SC1 example is illustrative of format not exact rep order)"
    - "setup_state.py stays PURE (stdlib only — no pymol, no Qt) — WSL-unit-testable"
  artifacts:
    - path: "biochemeleon/setup_state.py"
      provides: "format_remaining(total, counts_by_rep, easy_mode) — pure label formatter (GAME-03)"
      contains: "def format_remaining"
    - path: "tests/test_setup_state.py"
      provides: "WSL unit tests for format_remaining (4 behaviors + ordering + separator + None/empty guards)"
      contains: "TestFormatRemaining"
  key_links:
    - from: "biochemeleon/gui_game.py _update_remaining (04.1-03)"
      to: "biochemeleon/setup_state.format_remaining"
      via: "from .setup_state import format_remaining; _update_remaining calls format_remaining(remaining, counts, easy_mode)"
      pattern: "format_remaining"
    - from: "biochemeleon/setup_state.format_remaining"
      to: "biochemeleon/setup_state.GAME_REPS"
      via: "iterates GAME_REPS for rep ordering in the parenthetical"
      pattern: "GAME_REPS"
---

<objective>
TDD a pure label-formatting helper `format_remaining(total, counts_by_rep,
easy_mode) -> str` in `setup_state.py` that produces the GAME-03 remaining-hiders
label: total-only in hard mode (Phase 4 behavior, SC2) and total + per-rep
breakdown in easy mode (SC1).

Purpose: This is the pure formatting brain behind the GameTab label. Keeping
it pure (stdlib + `GAME_REPS`) makes it WSL-unit-testable WITHOUT Qt/PyMOL —
the GUI (04.1-03) just calls it and sets the QLabel. Separating format from
render follows the repo's pure-layer pattern (setup_state.py already holds
`GAME_REPS`, `DEFAULTS`, `randomize_state`, `validate_state`, all pure +
WSL-tested). It lands FIRST (parallel with 04.1-01, disjoint files) so 04.1-03
can import it.

Output: `setup_state.py` gains `format_remaining()` (PURE) +
`tests/test_setup_state.py` gains a `TestFormatRemaining` class (~8 tests),
following RED → GREEN → REFACTOR.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04.1-per-rep-remaining-hiders-display-easy-mode/04.1-RESEARCH-callbacks.md
@.planning/phases/04.1-per-rep-remaining-hiders-display-easy-mode/04.1-RESEARCH-plumbing.md
@biochemeleon/setup_state.py
@tests/test_setup_state.py
</context>

<feature>
  <name>format_remaining(total, counts_by_rep, easy_mode) — pure GAME-03 label formatter</name>
  <files>biochemeleon/setup_state.py, tests/test_setup_state.py</files>
  <behavior>
    A pure function returning the remaining-hiders label text. Testable
    behaviors (each `expect(fn(input)).toBe(output)`); `GAME_REPS` order is
    `['lines','sticks','spheres','cartoon','ribbon']`:

    - (i) hard mode: `format_remaining(7, {'spheres':3,'sticks':3,'cartoon':2}, False)` == `"Remaining: 7"` (total-only; SC2; Phase 4 unchanged).
    - (ii) easy mode mixed: `format_remaining(7, {'spheres':2,'sticks':3,'cartoon':2}, True)` == `"Remaining: 7  (sticks: 3, spheres: 2, cartoon: 2)"` (GAME_REPS order: sticks before spheres before cartoon; TWO spaces before `(`; SC1 format).
    - (iii) easy mode filters zeros: `format_remaining(4, {'spheres':2,'sticks':0,'cartoon':2}, True)` == `"Remaining: 4  (spheres: 2, cartoon: 2)"` (sticks:0 omitted).
    - (iv) easy mode all-zero (win state): `format_remaining(0, {'spheres':0,'sticks':0}, True)` == `"Remaining: 0"` (NO empty parenthetical).
    - (v) counts_by_rep None: `format_remaining(7, None, True)` == `"Remaining: 7"` (safe total-only default).
    - (vi) counts_by_rep empty dict: `format_remaining(7, {}, True)` == `"Remaining: 7"`.
    - (vii) two-space separator: for any easy-mode output with a parenthetical, exactly TWO spaces separate `{total}` and `(` (assert `"  ("` substring present; assert `"  ("` not preceded by extra space).
    - (viii) GAME_REPS order: pass a dict whose insertion order is NOT GAME_REPS order (e.g. `{'cartoon':2,'spheres':1}`) → reps appear in GAME_REPS order in the output (`spheres` before `cartoon`), not insertion order.

    Purity: uses only `total` (int), `counts_by_rep` (dict|None), `easy_mode`
    (bool), and `GAME_REPS` (already in setup_state.py). NO `from pymol`, NO Qt.
    Uses `%`-formatting (matches codebase style: gui_game.py:99, game.py:187).
  </behavior>
  <implementation>
    Add `format_remaining` to `setup_state.py` near the other pure helpers
    (e.g. after `validate_state` / `randomize_state`, behind a
    `# ---- Remaining-hiders label formatter (Phase 4.1, GAME-03) ----` section
    comment). `GAME_REPS` is already defined at setup_state.py:23 — use it
    directly (no new import). Reference sketch (do NOT copy verbatim without
    reading setup_state.py):

    ```python
    def format_remaining(total, counts_by_rep, easy_mode):
        """Format the remaining-hiders label (GAME-03 per-rep display).

        Pure (stdlib only — no pymol, no Qt). WSL-unit-testable.

        Args:
            total (int): total remaining (hidden) hiders.
            counts_by_rep (dict|None): {rep: count} of REMAINING (hidden-only)
                hiders per rep (from registry.remaining_by_rep). None/empty ->
                total-only display.
            easy_mode (bool): True = easy (show per-rep breakdown); False = hard
                (total-only, unchanged from Phase 4 — SC2).

        Returns:
            str: the label text. Per-rep entries in GAME_REPS order, only reps
            with count > 0; TWO spaces before the parenthetical (SC1).
        """
        if not easy_mode or not counts_by_rep:
            return "Remaining: %d" % total
        parts = ["%s: %d" % (rep, counts_by_rep[rep])
                for rep in GAME_REPS if counts_by_rep.get(rep, 0) > 0]
        if not parts:
            return "Remaining: %d" % total
        return "Remaining: %d  (%s)" % (total, ", ".join(parts))
    ```

    `.get(rep, 0)` is defensive (caller's dict should be zero-filled like
    `remaining_by_rep`, but `.get` guards a sparse dict). Write the docstring
    in PROSE (avoid literal `from pymol` / `cmd.` tokens that trip the
    Pitfall-1 grep gate — mirrors the 03-02/06/09/10 precedent).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>RED: add failing TestFormatRemaining tests in tests/test_setup_state.py</name>
  <files>tests/test_setup_state.py</files>
  <action>
    Read `tests/test_setup_state.py` to confirm the top-of-file `sys.modules`
    stub pattern for `pymol` + `pymol.Qt` (needed because importing
    `biochemeleon` runs `__init__.py` which does `from pymol.Qt import ...` at
    module level — AGENTS.md documents this; the existing 90 tests already
    import successfully so the stub is present — follow it).

    Add a new test class `TestFormatRemaining` (plain `unittest.TestCase`,
    no setUp needed — `format_remaining` is a pure free function). Import
    `format_remaining` from `biochemeleon.setup_state` (add to the existing
    import line if there is one; else add `from biochemeleon.setup_state import
    format_remaining, GAME_REPS`). Tests (all `assertEqual`):

    1. `test_format_remaining_hard_mode` — `format_remaining(7, {'spheres':3,'sticks':3,'cartoon':2}, False)` == `"Remaining: 7"`.
    2. `test_format_remaining_easy_mode_mixed` — `format_remaining(7, {'spheres':2,'sticks':3,'cartoon':2}, True)` == `"Remaining: 7  (sticks: 3, spheres: 2, cartoon: 2)"` (GAME_REPS order).
    3. `test_format_remaining_easy_mode_filters_zero` — `format_remaining(4, {'spheres':2,'sticks':0,'cartoon':2}, True)` == `"Remaining: 4  (spheres: 2, cartoon: 2)"`.
    4. `test_format_remaining_easy_mode_all_zero` — `format_remaining(0, {'spheres':0,'sticks':0}, True)` == `"Remaining: 0"`.
    5. `test_format_remaining_none_counts` — `format_remaining(7, None, True)` == `"Remaining: 7"`.
    6. `test_format_remaining_empty_dict` — `format_remaining(7, {}, True)` == `"Remaining: 7"`.
    7. `test_format_remaining_two_space_separator` — `out = format_remaining(7, {'spheres':2}, True)`; assert `"  ("` in out (two spaces before paren); assert out == `"Remaining: 7  (spheres: 2)"`.
    8. `test_format_remaining_game_reps_order` — pass `{'cartoon':2,'spheres':1}` (insertion order anti-GAME_REPS) → `format_remaining(3, {'cartoon':2,'spheres':1}, True)` == `"Remaining: 3  (spheres: 1, cartoon: 2)"` (spheres before cartoon, GAME_REPS order not insertion order).

    Run the tests — they MUST fail (ImportError on `format_remaining` — RED).
    The existing 90 tests MUST still pass (no regression). Commit ONLY
    `tests/test_setup_state.py`:
    `git add tests/test_setup_state.py && git commit -m "test(04.1-02): add failing tests for format_remaining"`
  </action>
  <verify>
    - `python3.6 -m unittest tests.test_setup_state -v 2>&1 | tail -30` → new TestFormatRemaining tests FAIL (ImportError on format_remaining); existing 90 tests still PASS (no regression).
    - `python3.6 -m py_compile tests/test_setup_state.py` → clean.
  </verify>
  <done>
    RED confirmed: TestFormatRemaining added (~8 tests) failing on missing
    format_remaining; existing 90 tests green; committed as `test(04.1-02):`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>GREEN: implement format_remaining in setup_state.py</name>
  <files>biochemeleon/setup_state.py</files>
  <action>
    Implement the `<implementation>` section above: add `format_remaining` to
    `setup_state.py` (after `validate_state`/`randomize_state`, behind a
    `# ---- Remaining-hiders label formatter (Phase 4.1, GAME-03) ----`
    section comment). Use the reference sketch (do NOT copy verbatim — confirm
    `GAME_REPS` is at setup_state.py:23 and use it directly). Keep the module
    PURE (stdlib only — NO `from pymol`, NO Qt).

    Run the tests — they MUST pass (GREEN: 90 existing + ~8 new). Commit ONLY
    `biochemeleon/setup_state.py`:
    `git add biochemeleon/setup_state.py && git commit -m "feat(04.1-02): implement format_remaining (pure GAME-03 label formatter)"`
  </action>
  <verify>
    - `python3.6 -m unittest tests.test_setup_state -v 2>&1 | tail -20` → ALL pass (90 existing + new TestFormatRemaining).
    - `python3.6 -m py_compile biochemeleon/setup_state.py` → clean.
    - PURITY GATE: `grep -rnE "from pymol|import pymol|from pymol.Qt" biochemeleon/setup_state.py` → ZERO matches.
    - `grep -nE "def format_remaining" biochemeleon/setup_state.py` → exactly 1.
    - Pitfall-1 gate package-wide: `grep -rnE "import Tkinter|import tkinter|from tkinter|import Pmw|from Pmw|app\.root|grab_set|mainloop|Toplevel|menuBar\.addmenuitem|from PyQt5 import|import PyQt5" biochemeleon/` → ZERO matches.
    - Full suite no regression: `python3.6 -m unittest tests.test_setup_state tests.test_registry -v 2>&1 | tail -5` → all pass.
  </verify>
  <done>
    GREEN: all tests pass; setup_state.py still pure; format_remaining added;
    committed as `feat(04.1-02):`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>REFACTOR: cleanup if obvious (no behavior change)</name>
  <files>biochemeleon/setup_state.py</files>
  <action>
    Review `format_remaining` for obvious cleanup. NO behavior change. If no
    cleanup needed, SKIP the commit. If you edit, run tests — MUST still pass.
    Commit ONLY if changed:
    `git add biochemeleon/setup_state.py && git commit -m "refactor(04.1-02): clean up format_remaining"`

    Watch the AGENTS.md-documented grep false-positive pattern: docstrings
    with literal `from pymol` / `.exec_()` tokens trip the gates. Write in
    PROSE — mirrors the 03-02/06/09/10 precedent.
  </action>
  <verify>
    - `python3.6 -m unittest tests.test_setup_state -v 2>&1 | tail -10` → all pass.
    - `python3.6 -m py_compile biochemeleon/setup_state.py` → clean.
    - PURITY + Pitfall-1 gates still green.
  </verify>
  <done>
    REFACTOR complete (or skipped); tests green; gates green. setup_state.py
    is functionally complete for Phase 4.1's pure label formatter.
  </done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest tests.test_setup_state -v` → all pass (90 existing + new TestFormatRemaining, no regression).
- `python3.6 -m py_compile biochemeleon/*.py` → no syntax error.
- PURITY: `grep -rnE "from pymol|import pymol|from pymol.Qt" biochemeleon/setup_state.py` → ZERO (module stays stdlib-only).
- Pitfall-1 gate package-wide: ZERO matches.
- `format_remaining` matches SC1 format (two-space separator, GAME_REPS order, count>0 filter) and SC2 (hard mode = total-only).
</verification>

<success_criteria>
- `format_remaining(total, counts_by_rep, easy_mode)` provides the pure label text that the GameTab (04.1-03) sets on `self._remaining_label`.
- setup_state.py remains pure (stdlib + GAME_REPS) — WSL-verified.
- Hard mode is byte-identical to Phase 4 (`"Remaining: {total}"`); easy mode adds the per-rep parenthetical (SC1).
- Ready for 04.1-03 (which imports `format_remaining` into gui_game.py).
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-per-rep-remaining-hiders-display-easy-mode/04.1-02-SUMMARY.md`
</output>
