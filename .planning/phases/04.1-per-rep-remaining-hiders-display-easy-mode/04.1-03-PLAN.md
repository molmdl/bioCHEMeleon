---
phase: 04.1
plan: 03
type: execute
wave: 2
depends_on:
  - "04.1-01"
  - "04.1-02"
files_modified:
  - biochemeleon/__init__.py
  - biochemeleon/gui_game.py
  - smoke/phase4_1_smoke.py
autonomous: false

must_haves:
  truths:
    - "When the easy difficulty toggle is checked at game Start, the Game tab remaining-hiders label shows a per-rep breakdown in addition to the total (SC1)"
    - "When the easy difficulty toggle is unchecked (hard mode), the label shows only the total — unchanged from Phase 4 behavior (SC2)"
    - "The per-rep counts update correctly as hiders are found (SC3) — decrementing on each on_pick / reveal_one / reveal_all"
    - "difficulty_easy is plumbed to the GameTab via a single controller attribute _easy_mode set in _continue_after_large_demo_fetch (covers BOTH fresh-game paths: __init__.py:124 sync + :484 async)"
    - "No callback signature change; no game.py fire-site change; start_countdown signature unchanged (zero backward-compat breakage — Option c pull model + Option b plumbing)"
    - "Imported games (out of scope for 4.1) degrade to total-only via getattr default False"
  artifacts:
    - path: "biochemeleon/__init__.py"
      provides: "1-line _easy_mode plumbing in _continue_after_large_demo_fetch (after GameController construction)"
      contains: "_easy_mode"
    - path: "biochemeleon/gui_game.py"
      provides: "_update_remaining rewritten to pull remaining_by_rep + _easy_mode -> format_remaining -> label; format_remaining imported from setup_state"
      contains: "format_remaining"
    - path: "smoke/phase4_1_smoke.py"
      provides: "Headless cmd-only smoke: remaining_by_rep read path after start/on_pick/reveal_one/reveal_all/cleanup (SC3 at cmd layer)"
      contains: "remaining_by_rep"
  key_links:
    - from: "biochemeleon/__init__.py _continue_after_large_demo_fetch"
      to: "GameController._easy_mode attribute"
      via: "self._controller._easy_mode = bool(state.get('difficulty_easy', True)) right after construction"
      pattern: "_easy_mode"
    - from: "biochemeleon/gui_game.py _update_remaining"
      to: "biochemeleon/registry.HiderRegistry.remaining_by_rep (04.1-01)"
      via: "self._controller.registry.remaining_by_rep() (pull model, only when easy_mode)"
      pattern: "remaining_by_rep"
    - from: "biochemeleon/gui_game.py _update_remaining"
      to: "biochemeleon/setup_state.format_remaining (04.1-02)"
      via: "from .setup_state import format_remaining; format_remaining(remaining, counts, easy_mode)"
      pattern: "format_remaining"
---

<objective>
Wire the Phase 4.1 per-rep remaining-hiders display end-to-end: plumb
`difficulty_easy` to the GameTab (1-line controller attribute, Option b),
rewrite `_update_remaining` to pull `remaining_by_rep()` + `_easy_mode` and
render via the pure `format_remaining` helper (Option c pull model — zero
callback change), add a headless smoke for the read path, and human-verify the
Qt label renders per-rep in easy mode / total-only in hard mode.

Purpose: This closes GAME-03. The pure data source (`remaining_by_rep`,
04.1-01) and pure formatter (`format_remaining`, 04.1-02) are already built;
this plan CONSUMES them from the Qt+cmd layer. The plumbing is minimal (1 line,
`_found_color` precedent) and the callback API is UNCHANGED (pull model) — so
Phase 6/7/8 consumers, the unit tests, and the smoke lambdas all stay green.
Follows the Phase 7 human-verify checkpoint pattern (headless-smoke the cmd
read path; human-verify the Qt label).

Output: `__init__.py` +1 line; `gui_game.py` `_update_remaining` rewritten +
1 import; new `smoke/phase4_1_smoke.py`; human-verify checkpoint.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04.1-per-rep-remaining-hiders-display-easy-mode/04.1-RESEARCH-plumbing.md
@.planning/phases/04.1-per-rep-remaining-hiders-display-easy-mode/04.1-RESEARCH-callbacks.md
@.planning/phases/04.1-per-rep-remaining-hiders-display-easy-mode/04.1-01-SUMMARY.md
@.planning/phases/04.1-per-rep-remaining-hiders-display-easy-mode/04.1-02-SUMMARY.md
@biochemeleon/__init__.py
@biochemeleon/gui_game.py
@biochemeleon/game.py
@biochemeleon/registry.py
@biochemeleon/setup_state.py
@smoke/phase4_smoke.py
@smoke/phase6_smoke.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plumb _easy_mode + rewrite _update_remaining (Option b + Option c)</name>
  <files>biochemeleon/__init__.py, biochemeleon/gui_game.py</files>
  <action>
    **Plumbing (Option b — 1 line in __init__.py):**
    Read `biochemeleon/__init__.py` method `_continue_after_large_demo_fetch`
    (starts ~line 211). Find the line that constructs the controller:
    `self._controller = game.GameController(target_obj)` (plumbing research
    cites ~line 378 — read the method to confirm the EXACT line). Add
    IMMEDIATELY AFTER that line, BEFORE `self._controller.start(hider_specs)`:
    ```python
    self._controller._easy_mode = bool(state.get("difficulty_easy", True))  # Phase 4.1: easy-mode per-rep display (GAME-03)
    ```
    This single line covers BOTH fresh-game `start_countdown` call sites
    (`__init__.py:124` sync via `_on_start` and `:484` async via the large-demo
    drain 'done' branch) because both flow through
    `_continue_after_large_demo_fetch` where the controller is constructed.
    Do NOT touch the import/restart paths (`:710`, `:755`) — `getattr` default
    handles them (total-only, out of scope). Do NOT change `start_countdown`'s
    signature. Do NOT add `_easy_mode` to `game.py` `__init__` (it's a
    duck-typed attribute set by the caller — same pattern as `_found_color`
    which game.py:40 initializes to 'green' and gui_game.py:194 overrides; you
    MAY optionally add `self._easy_mode = True` to `GameController.__init__` in
    game.py as a default, but it is NOT required — the `getattr` default in
    the GUI handles absence. Prefer NOT touching game.py to keep the change
    minimal and avoid a 4th file — the research recommends the 1-line
    caller-set approach.)

    **Label rewrite (Option c pull model — gui_game.py):**
    1. Add the import at the top of `gui_game.py` alongside the existing
       `from .` imports (e.g. near `from .registry import ...`):
       `from .setup_state import format_remaining`
       (setup_state is the base of the dependency chain — NO circular import.)
    2. Rewrite `_update_remaining` (gui_game.py:98-99). Current body:
       ```python
       def _update_remaining(self, remaining):
           self._remaining_label.setText("Remaining: %d" % remaining)
       ```
       New body (pull model — NO callback signature change; `remaining` stays
       the only param so `on_remaining_changed=self._update_remaining` wiring
       at gui_game.py:242 + the initial call at gui_game.py:256 still work):
       ```python
       def _update_remaining(self, remaining):
           # Phase 4.1 (GAME-03): pull hidden-filtered per-rep counts at render
           # time + the easy-mode flag, format via the pure helper, set the label.
           # Pull model (Option c) — no callback signature change; reads the
           # duck-typed controller (same precedent as gui_game.py:149 registry.all()).
           easy_mode = getattr(self._controller, '_easy_mode', False)
           counts = self._controller.registry.remaining_by_rep() if easy_mode else None
           self._remaining_label.setText(format_remaining(remaining, counts, easy_mode))
       ```
       The `if easy_mode else None` short-circuits the registry access in hard
       mode (minor optimization; also safe if `self._controller` were None —
       `getattr(None, '_easy_mode', False)` is False → counts None → total-only).
       `format_remaining` (04.1-02) handles `counts=None` as total-only.

    **Do NOT change:**
    - `__init__.py:786` reset string `self.game_tab._remaining_label.setText("Remaining: -")` — that is the IDLE/reset state (total-only is correct there; per-rep only matters during active play). Leave it.
    - `game.py` fire sites (`:188`, `:294`, `:316`) — UNCHANGED (Option c).
    - `set_callbacks` signature (game.py:94-111) — UNCHANGED.
    - The `set_callbacks` wiring in `_begin_play` (gui_game.py:240-245) — UNCHANGED.
    - The initial `_update_remaining` call in `_begin_play` (gui_game.py:256) — UNCHANGED (still `self._update_remaining(self._controller._remaining())`; the method now reads `_easy_mode` + `remaining_by_rep` internally).

    Commit both files together:
    `git add biochemeleon/__init__.py biochemeleon/gui_game.py && git commit -m "feat(04.1-03): plumb _easy_mode + per-rep remaining label (GAME-03, Option b+c)"`
  </action>
  <verify>
    - `python3.6 -m py_compile biochemeleon/__init__.py biochemeleon/gui_game.py` → clean (gui_game.py imports Qt so it cannot be imported in WSL, but py_compile checks syntax).
    - `python3.6 -m unittest tests.test_setup_state tests.test_registry tests.test_game_controller -v 2>&1 | tail -10` → all pass (no regression; the controller tests are unaffected — on_remaining_changed signature unchanged).
    - Plumbing present: `grep -nE "_easy_mode" biochemeleon/__init__.py` → exactly 1 match (the new line in _continue_after_large_demo_fetch).
    - Label rewrite present: `grep -nE "format_remaining" biochemeleon/gui_game.py` → 2 matches (the import + the call in _update_remaining).
    - Pull model: `grep -nE "remaining_by_rep" biochemeleon/gui_game.py` → exactly 1 (in _update_remaining; NOT in game.py — game.py is unchanged: `grep -nE "remaining_by_rep" biochemeleon/game.py` → 0).
    - No callback signature change: `grep -nE "def _update_remaining" biochemeleon/gui_game.py` → still `def _update_remaining(self, remaining):` (1 positional arg, unchanged).
    - start_countdown signature unchanged: `grep -nE "def start_countdown" biochemeleon/gui_game.py` → `def start_countdown(self, controller, elapsed=0):` (unchanged).
    - Pitfall-1 gate package-wide: `grep -rnE "import Tkinter|import tkinter|from tkinter|import Pmw|from Pmw|app\.root|grab_set|mainloop|Toplevel|menuBar\.addmenuitem|from PyQt5 import|import PyQt5" biochemeleon/` → ZERO matches.
    - exec_ gate: `grep -rnE "\.exec_\(\)" biochemeleon/` → unchanged count (only QFileDialog/QMessageBox, NEVER the main dialog).
    - import sanity: `grep -nE "from .setup_state import format_remaining" biochemeleon/gui_game.py` → 1.
  </verify>
  <done>
    Plumbing (1-line _easy_mode) + label rewrite (pull-model _update_remaining
    via format_remaining) committed; py_compile clean; all WSL tests green; grep
    gates green; no callback/start_countdown/game.py changes. Ready for the
    headless smoke (Task 2) + human-verify (Task 3).
  </done>
</task>

<task type="auto">
  <name>Task 2: Headless smoke for the remaining_by_rep read path (SC3 at cmd layer)</name>
  <files>smoke/phase4_1_smoke.py</files>
  <action>
    Create `smoke/phase4_1_smoke.py` — a pure `pymol.cmd.*` script (NO Qt — like
    `smoke/phase4_smoke.py` / `smoke/phase6_smoke.py`) that verifies the per-rep
    remaining counts update correctly through the find paths (SC3) at the cmd
    layer. READ `smoke/phase4_smoke.py` AND `smoke/phase6_smoke.py` first and
    model the structure exactly (the `check(name, cond)` helper, the
    `cmd.fetch` + `orig_count` setup, the `GameController(obj).start(specs)`
    call, the direct `gc.on_pick(id)` logic-path simulation, the cleanup
    round-trip, the final `sys.exit(0/1)`).

    Import: `from biochemeleon import game, registry` (and `from pymol import
    cmd`). Do NOT import gui_game (Qt — would break headless). The smoke tests
    `registry.remaining_by_rep` (04.1-01) directly + the `game.py` find paths
    (UNCHANGED by this phase) — NOT the Qt label (that's Task 3).

    Sections + checks (use the `check()` helper pattern; print a header per
    section):
    1. SETUP: `cmd.fetch("1ubq", async_=0)`; `orig = cmd.count_atoms("1ubq")`.
    2. START MIXED-REP: build `hider_specs` with 4 hiders across 3 reps —
       e.g. `[(pos1, "spheres"), (pos2, "spheres"), (pos3, "sticks"), (pos4, "cartoon")]`
       using distinct 3D coords (e.g. `[10,10,10]`, `[11,11,11]`, `[12,12,12]`,
       `[13,13,13]` — exact coords don't matter; `insert_hider` places a
       sentinel pseudoatom at `pos`, visual rep is irrelevant for the data
       test). `gc = game.GameController("1ubq")`; `gc.start(hider_specs)`.
       Checks:
       - `len(gc.registry.all()) == 4`
       - `gc.registry.remaining_by_rep() == {"lines":0, "sticks":1, "spheres":2, "cartoon":1, "ribbon":0}`
       - `sum(gc.registry.remaining_by_rep().values()) == 4 == gc._remaining()` (SC3 sum contract)
       - `gc.registry.counts_by_rep() == {"lines":0, "sticks":1, "spheres":2, "cartoon":1, "ribbon":0}` (all hidden → equals counts_by_rep)
    3. FIND ONE SPHERE: get a sphere hider id: `sid = gc.registry.by_rep("spheres")[0].id`; `gc.on_pick(sid)`.
       Checks:
       - `gc.registry.remaining_by_rep()["spheres"] == 1` (decremented from 2)
       - `gc.registry.remaining_by_rep()["sticks"] == 1` and `["cartoon"] == 1` (unchanged)
       - `sum(...) == 3 == gc._remaining()`
       - `gc.registry.counts_by_rep()["spheres"] == 2` (counts_by_rep STILL counts the found one — confirms remaining_by_rep differs from counts_by_rep after a find)
    4. REVEAL_ONE: `gc.reveal_one()`.
       Checks:
       - `sum(gc.registry.remaining_by_rep().values()) == 2 == gc._remaining()` (one more found)
       - exactly one rep's count dropped by 1 vs section 3 (the revealed hider's rep)
    5. REVEAL_ALL: `gc.reveal_all()`.
       Checks:
       - `gc.registry.remaining_by_rep() == {"lines":0,"sticks":0,"spheres":0,"cartoon":0,"ribbon":0}` (all found)
       - `sum(...) == 0 == gc._remaining()`
       - `gc.registry.counts_by_rep() == {"lines":0,"sticks":1,"spheres":2,"cartoon":1,"ribbon":0}` (counts_by_rep UNCHANGED — it counts all records regardless of status; confirms remaining_by_rep is the hidden-filtered source)
    6. CLEANUP: `ok = gc.cleanup()`.
       Checks:
       - `ok is True` (verify_intact passed)
       - `cmd.count_atoms("1ubq") == orig` (back to original)
       - `cmd.count_atoms("1ubq and segi GAME") == 0` (no hider atoms remain)
    7. Final: `sys.exit(0)` if all checks passed (the `check()` helper tracks
       failures), else `sys.exit(1)`.

    Stage to the Windows-facing path and run headlessly (AGENTS.md pattern):
    ```bash
    bash wsl2win_cp.sh
    mkdir -p tmp/bioCHEMeleon/smoke && cp smoke/phase4_1_smoke.py tmp/bioCHEMeleon/smoke/
    cd tmp/bioCHEMeleon && timeout 90 cmd.exe /c "C:\\src\\run-conda-pymol.bat -cq smoke\\phase4_1_smoke.py" 2>&1 | tail -50
    ```
    Confirm exit 0 + all checks PASS. If a check fails, read the output, fix
    the SMOKE (not the product code — 04.1-01 already WSL-verified
    remaining_by_rep; a smoke failure is likely a smoke-logic bug or a
    stale-staging issue), re-stage, re-run. If a genuine product bug surfaces
    in `remaining_by_rep` or the find paths, fix it in 04.1-01's files
    (registry.py) and re-run the WSL unit tests + the smoke.

    Commit the smoke:
    `git add smoke/phase4_1_smoke.py && git commit -m "test(04.1-03): headless smoke for remaining_by_rep read path (SC3)"`
  </action>
  <verify>
    - Headless run: `cd tmp/bioCHEMeleon && timeout 90 cmd.exe /c "C:\\src\\run-conda-pymol.bat -cq smoke\\phase4_1_smoke.py" 2>&1 | tail -50` → all checks PASS, exit 0.
    - `python3.6 -m py_compile smoke/phase4_1_smoke.py` → clean.
    - Smoke is Qt-free: `grep -nE "from pymol.Qt|QtWidgets|QtCore|import PyQt5" smoke/phase4_1_smoke.py` → ZERO (must be pure pymol.cmd.* to run headless).
    - Smoke references remaining_by_rep: `grep -nE "remaining_by_rep" smoke/phase4_1_smoke.py` → >=1.
  </verify>
  <done>
    Headless smoke committed; all checks PASS (exit 0); SC3 verified at the cmd
    layer (per-rep counts decrement correctly through on_pick/reveal_one/
    reveal_all; remaining_by_rep differs from counts_by_rep after a find).
    Ready for the human-verify checkpoint (Task 3).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the Qt label renders per-rep in easy mode / total-only in hard mode</name>
  <what-built>
    Phase 4.1 per-rep remaining-hiders display (GAME-03): the Game tab
    remaining-hiders label now shows a per-rep breakdown when the easy
    difficulty toggle is ON (e.g. "Remaining: 6  (sticks: 2, spheres: 2,
    cartoon: 2)"), and total-only when OFF (unchanged from Phase 4). The pure
    data source (registry.remaining_by_rep) + pure formatter (format_remaining)
    are WSL-unit-tested; the cmd read path is headless-smoke-verified. This
    checkpoint verifies the Qt label actually renders in a real Windows PyMOL
    GUI session (WSL agent CANNOT run Qt).
  </what-built>
  <how-to-verify>
    In a real Windows PyMOL GUI session (activate the chemtools-win10 conda env
    via `setenv.bat`, then `pymol`), load the bioCHEMeleon plugin:

    **C1 — Easy mode shows per-rep (SC1):**
    1. Load a demo (e.g. 1znf). In the Setup tab, set per-rep to a MIX of
       spheres + sticks + cartoon (e.g. spheres=2, sticks=2, cartoon=2),
       hider count ~6.
    2. Ensure the "Easy: show remaining hiders per representation" checkbox
       is CHECKED.
    3. Press Start. After the 3-2-1 countdown, the Game tab remaining-hiders
       label should show a per-rep breakdown, e.g.
       `Remaining: 6  (sticks: 2, spheres: 2, cartoon: 2)` — TWO spaces before
       the parenthetical, reps in GAME_REPS order (lines, sticks, spheres,
       cartoon, ribbon — only reps with count > 0 shown).

    **C2 — Per-rep decrements on find (SC3):**
    4. Click a hider atom in the viewer. The total AND that hider's rep count
       should decrement (e.g. clicking a sphere hider → `Remaining: 5
       (sticks: 2, spheres: 1, cartoon: 2)`).
    5. Continue clicking until all found. The win dialog should appear and the
       label should show `Remaining: 0` (NO empty parenthetical).

    **C3 — Hard mode is total-only (SC2):**
    6. Press Cleanup (or start a new game). Set the easy checkbox to UNCHECKED
       (Hard). Press Start.
    7. The label should show ONLY the total, e.g. `Remaining: 6` —
       byte-identical to Phase 4 behavior (no parenthetical).
    8. Click a hider → label still total-only (`Remaining: 5`).

    **C4 — Rep order confirmation:**
    9. Confirm the per-rep order in the label matches your expectation. The
       implementation uses GAME_REPS order (lines, sticks, spheres, cartoon,
       ribbon) for codebase consistency — the SC1 example in the roadmap shows
       spheres-first (illustrative). If you want the exact SC1 spheres-first
       order instead, note it and a one-line helper tweak will follow up.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. wrong rep order, label not updating, per-rep not decrementing)</resume-signal>
</task>

</tasks>

<verification>
- WSL gates: `python3.6 -m py_compile biochemeleon/*.py` clean; `python3.6 -m unittest tests.test_setup_state tests.test_registry tests.test_game_controller -v` all pass (no regression); Pitfall-1=0; exec_ gate unchanged (only QFileDialog/QMessageBox).
- Plumbing: exactly 1 `_easy_mode` line in `__init__.py` (`_continue_after_large_demo_fetch`); `start_countdown` signature unchanged; `game.py` fire sites + `set_callbacks` signature unchanged (zero backward-compat breakage).
- Pull model: `_update_remaining` reads `remaining_by_rep()` + `_easy_mode` → `format_remaining`; `game.py` has 0 `remaining_by_rep` references (pull from GUI only).
- Headless smoke: `smoke/phase4_1_smoke.py` all checks PASS (exit 0) — SC3 at the cmd layer.
- Human-verify: easy ON → per-rep label (SC1); easy OFF → total-only (SC2); per-rep decrements on find (SC3).
</verification>

<success_criteria>
- GAME-03 closed: remaining-hiders count shows total always, and per-representation when the easy toggle is set.
- SC1 (easy → per-rep breakdown), SC2 (hard → total-only, Phase 4 unchanged), SC3 (per-rep updates on each find) all verified.
- Zero backward-compat breakage (no callback/game.py/start_countdown changes; Phase 6/7/8 + tests + smoke lambdas all green).
- Phase 4.1 complete; the deferred GAME-03 gap (04-RESEARCH.md:618) is closed.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-per-rep-remaining-hiders-display-easy-mode/04.1-03-SUMMARY.md`
</output>
