---
phase: 04.1-per-rep-remaining-hiders-display-easy-mode
verified: 2026-08-17T00:00:00Z
status: passed
score: 7/7 must-haves verified
re_verification:
  previous_status: none
  is_re_verification: false
human_verification:
  - test: "SC1 — easy mode label shows per-rep breakdown (e.g. 'Remaining: 7  (spheres: 2, sticks: 3, cartoon: 2)')"
    expected: "Game tab label renders the per-rep parenthetical in GAME_REPS order with a two-space separator"
    why_human: "WSL cannot run Qt / PyMOL GUI (AGENTS.md). Qt label rendering is a human-verify checkpoint by nature."
    status: APPROVED
    evidence: "User ran the 04.1-03 Task 3 checkpoint in a real Windows PyMOL GUI session with 1znf + 1ubq. Clicked sphere hiders (GAME/H/HIDER`9001/H000, H001, H002) and cartoon-segment hiders (GAME/H/LEU`10005, CYS`10006, ILE`10003, LYS`10006, PHE`10004); win triggered ('Executive: Colored 881 atoms and 2 objects'). SC1 confirmed. (04.1-03-SUMMARY.md:71,128)"
  - test: "SC2 — hard mode label shows total only, unchanged from Phase 4"
    expected: "Game tab label renders 'Remaining: N' only (byte-identical to Phase 4)"
    why_human: "WSL cannot run Qt / PyMOL GUI. Visual byte-identity of the rendered label vs Phase 4 is a human-verify checkpoint."
    status: APPROVED
    evidence: "User confirmed SC2 in the same real Windows PyMOL GUI session (04.1-03-SUMMARY.md:128). Automated byte-identity also verified: format_remaining(7, {...}, False) == 'Remaining: 7' (TestFormatRemaining.test_format_remaining_hard_mode), matching Phase 4's literal 'Remaining: %d' % remaining (commit f397045)."
  - test: "SC3 — per-rep counts decrement on each on_pick / reveal_one / reveal_all"
    expected: "Per-rep numbers in the label drop as hiders are found; total stays in sync"
    why_human: "Visual decrement observation in the GUI is human-verify; the underlying data path is WSL-automated."
    status: APPROVED
    evidence: "User confirmed SC3 in the real GUI session (04.1-03-SUMMARY.md:128). Automated: 18/18 headless smoke PASS (smoke/phase4_1_smoke.py: on_pick → spheres 2→1 sticks/cartoon unchanged; reveal_one → exactly one rep drops by 1; reveal_all → all zeros) + TestRemainingByRep.test_remaining_by_rep_mark_found_decrement + test_remaining_by_rep_sums_to_remaining."
---

# Phase 4.1: Per-rep remaining hiders display (easy mode) Verification Report

**Phase Goal:** Close the GAME-03 gap — when the easy difficulty toggle is set, the Game tab shows remaining hiders per representation (not just total). Phase 4 shipped total-only; the per-rep display was explicitly deferred (04-RESEARCH.md:618) and never picked up by any later phase.
**Verified:** 2026-08-17
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                      | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | When the easy difficulty toggle is checked at game Start, the Game tab remaining-hiders label shows a per-rep breakdown in addition to the total (SC1)                    | ✓ VERIFIED | `format_remaining` easy mode (setup_state.py:443-447) appends per-rep breakdown in GAME_REPS order with a 2-space separator. TestFormatRemaining.test_format_remaining_easy_mode_mixed asserts `format_remaining(7, {'spheres':2,'sticks':3,'cartoon':2}, True)` == `"Remaining: 7  (sticks: 3, spheres: 2, cartoon: 2)"` (the SC1 example verbatim). gui_game.py:108 sets this on `self._remaining_label`. `_easy_mode` plumbing at __init__.py:379 reads `state.get("difficulty_easy", True)`. **Human-verify APPROVED** (1znf+1ubq GUI session). |
| 2   | When the easy difficulty toggle is unchecked (hard mode), the label shows only the total — unchanged from Phase 4 behavior (SC2)                                         | ✓ VERIFIED | `format_remaining` hard mode (setup_state.py:441-442) returns `"Remaining: %d" % total` — byte-identical to Phase 4's `self._remaining_label.setText("Remaining: %d" % remaining)` (commit f397045). TestFormatRemaining.test_format_remaining_hard_mode asserts `format_remaining(7, {...}, False)` == `"Remaining: 7"`. gui_game.py:107 short-circuits `counts = ... if easy_mode else None` (no registry access in hard mode). **Human-verify APPROVED**.                                                                                       |
| 3   | The per-rep counts update correctly as hiders are found (SC3) — decrementing on each on_pick / reveal_one / reveal_all                                                     | ✓ VERIFIED | game.py fire sites unchanged: :188 `self._on_remaining_changed(remaining)` (on_pick), :294 (reveal_one), :316 `self._on_remaining_changed(0)` (reveal_all) — all 1-arg. gui_game._update_remaining (gui_game.py:107) pulls fresh `remaining_by_rep()` at render time → reflects the status flip. Headless smoke 18/18 PASS (smoke/phase4_1_smoke.py: on_pick → spheres 2→1, sticks/cartoon unchanged; reveal_one → exactly one rep drops by 1; reveal_all → all zeros). TestRemainingByRep.test_remaining_by_rep_mark_found_decrement + _sums_to_remaining. **Human-verify APPROVED**.   |
| 4   | registry.remaining_by_rep() provides the hidden-filtered per-rep data source (sums to total remaining, skips rep=None, zero-fills GAME_REPS)                             | ✓ VERIFIED | registry.py:274-300 `remaining_by_rep` defined. Mirrors `counts_by_rep` (line 250, UNALTERED) with one added `if r.status == HIDER_STATUS_HIDDEN:` filter (line 298). Zero-fills GAME_REPS (line 294). Skips rep=None (lines 296-297). Pure (PURITY gate 0 matches). 8 WSL unit tests (TestRemainingByRep) pin all behaviors.                                                                                                                                                                                                      |
| 5   | format_remaining(total, counts_by_rep, easy_mode) formats the label (SC1 easy / SC2 hard / None-safe / GAME_REPS order)                                                   | ✓ VERIFIED | setup_state.py:418-447 `format_remaining` defined. Hard mode (line 442): `"Remaining: %d" % total`. Easy mode (lines 443-447): per-rep breakdown in GAME_REPS order, >0 filter, 2-space separator. None/empty/all-zero safe-degrades (lines 441, 445-446). Pure (PURITY gate 0 matches). 8 WSL unit tests (TestFormatRemaining) pin all behaviors.                                                                                                                                                                                              |
| 6   | Zero backward-compat breakage (no callback signature change, no game.py fire-site change, start_countdown signature unchanged)                                            | ✓ VERIFIED | game.py has 0 `remaining_by_rep` references (grep EXIT=1 — pull from GUI only). gui_game.py:100 `def _update_remaining(self, remaining):` — signature UNCHANGED (1-arg + self). gui_game.py:212 `def start_countdown(self, controller, elapsed=0):` — signature UNCHANGED. game.py:94 `def set_callbacks(self, on_log=None, on_remaining_changed=None, ...)` — signature UNCHANGED. game.py fire sites :188/:294/:316 all pass 1 arg. 257 WSL tests pass (incl. test_game_controller which pins the callback contract).                                                       |
| 7   | registry.py and setup_state.py remain pure (no pymol, no Qt) — WSL-unit-testable                                                                                          | ✓ VERIFIED | PURITY gate: `grep -rnE "from pymol\|import pymol\|from pymol.Qt" biochemeleon/registry.py biochemeleon/setup_state.py` → 0 matches (EXIT=1). 257 WSL tests pass (test_setup_state + test_registry + test_game_controller).                                                                                                                                                                                                                                                                                                          |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact                                                              | Expected                                                                    | Status     | Details                                                                                                                                                                                                                      |
| --------------------------------------------------------------------- | --------------------------------------------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `biochemeleon/registry.py`                                            | `HiderRegistry.remaining_by_rep()` (def remaining_by_rep)                   | ✓ VERIFIED | EXISTS (line 274). SUBSTANTIVE (27 lines, real impl mirroring counts_by_rep + 1 added HIDER_STATUS_HIDDEN filter line 298, docstring at 275-293). WIRED (called by gui_game.py:107 + smoke/phase4_1_smoke.py ×16). counts_by_rep UNALTERED (line 250, still exactly 1). Pure. |
| `biochemeleon/setup_state.py`                                         | `format_remaining()` (def format_remaining)                                 | ✓ VERIFIED | EXISTS (line 418). SUBSTANTIVE (30 lines, real impl, docstring at 419-440). WIRED (imported by gui_game.py:15, called at :108). Pure.                                                                                              |
| `biochemeleon/__init__.py`                                             | `_easy_mode` plumbing in `_continue_after_large_demo_fetch`                 | ✓ VERIFIED | EXISTS (line 379, in `_continue_after_large_demo_fetch` defined at :211). SUBSTANTIVE (1 line with correct semantics: `bool(state.get("difficulty_easy", True))`, placed right after `GameController` construction at :378, before `start(hider_specs)` at :381). WIRED (read by gui_game.py:106 via `getattr(self._controller, '_easy_mode', False)`). Exactly 1 `_easy_mode` match in __init__.py. |
| `biochemeleon/gui_game.py`                                             | `_update_remaining` rewritten to pull remaining_by_rep + _easy_mode → format_remaining | ✓ VERIFIED | EXISTS (lines 100-108). SUBSTANTIVE (9-line pull-model body: getattr _easy_mode + conditional remaining_by_rep + format_remaining + setText). WIRED (registered as `on_remaining_changed` callback at :251; initial call at :265 in _begin_play). Signature `def _update_remaining(self, remaining):` UNCHANGED. `from .setup_state import format_remaining` at line 15. |
| `smoke/phase4_1_smoke.py`                                              | headless smoke for SC3 read path                                            | ✓ VERIFIED | EXISTS (113 lines). SUBSTANTIVE (18 check() calls across 7 sections: setup / start mixed-rep / find-one-sphere / reveal_one / reveal_all / cleanup / exit). Qt-free (0 matches for `from pymol.Qt\|QtWidgets\|QtCore\|import PyQt5`). Uses `remaining_by_rep` ×16 + `counts_by_rep` ×3. 18/18 PASS per executor (commit bbad79c). |
| `tests/test_registry.py`                                               | `TestRemainingByRep`                                                        | ✓ VERIFIED | EXISTS (line 363). SUBSTANTIVE (8 test methods: empty_registry, all_reps_present, hidden_only_filter, mixed_reps_mixed_statuses, sums_to_remaining, skips_rep_none, mark_found_decrement, equals_counts_by_rep_when_all_hidden). Runs green (part of 257 PASS). |
| `tests/test_setup_state.py`                                            | `TestFormatRemaining`                                                       | ✓ VERIFIED | EXISTS (line 682). SUBSTANTIVE (8 test methods: hard_mode, easy_mode_mixed, easy_mode_filters_zero, easy_mode_all_zero, none_counts, empty_dict, two_space_separator, game_reps_order). Runs green (part of 257 PASS). |

### Key Link Verification

| From                                                            | To                                            | Via                                                                                | Status   | Details                                                                                                                                                                                                                       |
| --------------------------------------------------------------- | --------------------------------------------- | ---------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `gui_game.py _update_remaining`                                 | `registry.remaining_by_rep`                   | `self._controller.registry.remaining_by_rep()` (pull model, gui_game.py:107)      | WIRED    | Called only when `easy_mode` is True; result passed to `format_remaining` as `counts`. Smoke confirms the registry returns the decremented counts after each find/reveal.                                                          |
| `gui_game.py _update_remaining`                                 | `setup_state.format_remaining`               | `from .setup_state import format_remaining` (gui_game.py:15) + call at :108       | WIRED    | Import at line 15; called at line 108 with `(remaining, counts, easy_mode)`. Returned string set on `self._remaining_label.setText(...)`.                                                                                         |
| `__init__.py _continue_after_large_demo_fetch`                 | `GameController._easy_mode` attribute         | `self._controller._easy_mode = bool(state.get("difficulty_easy", True))` (:379)   | WIRED    | Set right after `self._controller = game.GameController(target_obj)` (:378), before `start(hider_specs)` (:381). Read by gui_game.py:106 via `getattr(self._controller, '_easy_mode', False)`. Duck-typed caller-set attribute (same precedent as `_found_color`). |

### Requirements Coverage

| Requirement | Status      | Blocking Issue |
| ----------- | ----------- | -------------- |
| GAME-03      | ✓ SATISFIED | None. SC1 (easy → per-rep label), SC2 (hard → total-only, byte-identical to Phase 4), SC3 (per-rep decrements on find) all verified — automated (WSL unit tests + 18/18 headless smoke) + human-verify APPROVED in a real Windows PyMOL GUI session. REQUIREMENTS.md:48 defines GAME-03 as "Remaining hiders count — total, and per-representation when the easy difficulty toggle is set"; Phase 4 delivered total-only (REQUIREMENTS.md:156 marks it Complete for the total portion), Phase 4.1 closes the per-rep gap. |

### Anti-Patterns Found

| File                              | Line | Pattern                | Severity | Impact                                                                                                                                                                                                                          |
| --------------------------------- | ---- | ---------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `biochemeleon/gui_game.py`         | 82   | "placeholder" (comment) | ℹ️ Info   | Pre-existing Qt combo-box UI pattern: "the index-0 placeholder doesn't fire on" — the rep-selection QComboBox index 0 is a "Select..." no-op entry. NOT a Phase 4.1 stub. Unrelated to the per-rep display work. |
| `biochemeleon/gui_game.py`         | 178  | "placeholder" (comment) | ℹ️ Info   | Pre-existing Qt combo-box UI pattern: "Index 0 is the placeholder (no-op). Resets to 0 after handling". Same QComboBox pattern. NOT a stub.                                                                   |
| `biochemeleon/__init__.py`         | 42   | "placeholder" (comment) | ℹ️ Info   | Pre-existing Phase 1 history docstring: "Phase 1 ships placeholder tabs only. Phase 2 populates Setup; Phase 4 ...". NOT a stub in Phase 4.1 code.                                                            |

**No blocker anti-patterns found.** No TODO/FIXME/XXX/HACK/not-implemented/coming-soon in any Phase 4.1-modified file. No empty returns / `=> {}` / `pass$` stubs. The 3 "placeholder" mentions are all pre-existing Qt QComboBox UI patterns (index-0 "Select..." no-op entry) or Phase 1 historical docstrings — none are Phase 4.1 stubs.

### WSL Gate Results

| Gate                                                        | Expected            | Result                                                                                                                                                                                            |
| ----------------------------------------------------------- | ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `python3.6 -m py_compile biochemeleon/*.py smoke/*.py`      | clean (exit 0)      | ✓ exit 0                                                                                                                                                                                          |
| `python3.6 -m unittest tests.test_setup_state tests.test_registry tests.test_game_controller -v` | all green           | ✓ 257 tests pass (`OK`), 0.148s — no regression from the pull-model rewrite (callback signature + on_remaining_changed wiring unchanged)                                                              |
| PURITY: `grep -rnE "from pymol\|import pymol\|from pymol.Qt" biochemeleon/registry.py biochemeleon/setup_state.py` | 0 matches           | ✓ 0 matches (EXIT=1) — registry.py and setup_state.py remain pure (no pymol, no Qt), WSL-unit-testable                                                                                              |
| Pitfall-1: package-wide grep for Tkinter/Pmw/PyQt5/etc.     | 0 matches           | ✓ 0 matches (EXIT=1)                                                                                                                                                                              |
| exec_ gate: package-wide `grep -rnE "\.exec_\(\)"`          | only QFileDialog/QMessageBox | ✓ exactly 1 match: `gui_game.py:312 msg.exec_()` on a `QtWidgets.QMessageBox` (line 307) — ALLOWED per AGENTS.md (child dialogs may use `.exec_()`; only the main PluginDialog must stay modeless) |

### Human Verification Required

The Qt label rendering (SC1/SC2 visual) is HUMAN_NEEDED by nature — WSL cannot run Qt / the PyMOL GUI (AGENTS.md). **All three human-verify items were APPROVED by the user** in a real Windows PyMOL GUI session as part of the 04.1-03 Task 3 checkpoint (commit dabaaab is the SUMMARY metadata commit; the checkpoint itself is verification-only with no code commit):

1. **SC1 — easy mode per-rep label** — APPROVED. User tested with 1znf and 1ubq. Clicked sphere hiders (GAME/H/HIDER`9001/H000, H001, H002) and cartoon-segment hiders (GAME/H/LEU`10005, CYS`10006, ILE`10003, LYS`10006, PHE`10004). The Game tab label rendered the per-rep breakdown in easy mode.
2. **SC2 — hard mode total-only label** — APPROVED. User confirmed the label shows only the total in hard mode (byte-identical to Phase 4).
3. **SC3 — per-rep decrements on find** — APPROVED. User observed per-rep counts decrementing as hiders were found. Win triggered ("Executive: Colored 881 atoms and 2 objects").

Evidence cited from 04.1-03-SUMMARY.md:71 and :128. The `1ubq.cif` test artifact left in the repo root by the user's GUI session is untracked and intentionally NOT committed (excluded from the `docs(04.1-03)` metadata commit per 04.1-03-SUMMARY.md:134).

### Gaps Summary

**No gaps.** All 7 phase-level truths verified, all 7 artifacts pass the three-level check (exists + substantive + wired), all 3 key links wired, GAME-03 SATISFIED, no blocker anti-patterns, all WSL gates green (257 tests, py_compile clean, PURITY + Pitfall-1 + exec_ gates all clean), and the human-verify checkpoint APPROVED by the user in a real Windows PyMOL GUI session.

The deferred GAME-03 gap (04-RESEARCH.md:618) is closed: the Game tab label now shows a per-rep breakdown in easy mode and total-only in hard mode, with per-rep counts decrementing on each find. The pull-model design (Option c) delivered zero backward-compat breakage — no callback signature change, no game.py fire-site change, no `set_callbacks` change, no `start_countdown` change — so all Phase 6/7/8 consumers, the 257 WSL unit tests, and the existing Phase 4/6 smoke lambdas stay green.

---

_Verified: 2026-08-17_
_Verifier: OpenCode (gsd-verifier)_
