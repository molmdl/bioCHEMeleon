---
phase: 10-polish-endgame-help
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "10-04-AUDIT.md exists with 10 verified research claims, each PASS or FAIL-flagged with file:line evidence"
    - "Any FAIL claim is explicitly flagged as a blocker for the downstream plan(s) that depend on it"
    - "No source code is changed (this plan is a read-only audit)"
    - "The audit runs before any implementation plan that builds on the research claims"
  artifacts:
    - path: ".planning/phases/10-polish-endgame-help/10-04-AUDIT.md"
      provides: "Pre-implementation verification of research claims against current code"
      contains: "10-04-AUDIT"
  key_links:
    - from: "10-RESEARCH-endgame.md + 10-RESEARCH-help.md claims"
      to: "the actual shipped code files cited"
      via: "read each cited file:line and confirm the claim matches the code"
      pattern: "PASS|FAIL"
---

<objective>
Before any implementation plan builds on the research, verify the research's claims about the current codebase are accurate. Catch stale/wrong claims early so downstream plans (10-05 win-stats, 10-06 debrief, 10-07 game-tooltips, 10-08 smoke, 10-10 README) don't build on a false premise. This is a READ-ONLY audit — no source code changes.

Purpose: Both research files (`10-RESEARCH-endgame.md` + `10-RESEARCH-help.md`) cite specific file:line references as HIGH-confidence. Verifying those citations now (before implementation) prevents a class of bugs where the research assumed a code state that has since drifted (e.g. a refactor in Phase 11 moved `_finish_win` or renamed `_hint_count`). Any discrepancy found here blocks the dependent implementation plan until either the code or the plan is corrected.

Output:
- `.planning/phases/10-polish-endgame-help/10-04-AUDIT.md` — a table of 10 claims, each with: Claim | Source citation | Verified (file:line) | PASS/FAIL | Notes.
- No code changes.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-polish-endgame-help/10-RESEARCH-endgame.md
@.planning/phases/10-polish-endgame-help/10-RESEARCH-help.md

@biochemeleon/game.py
@biochemeleon/gui_game.py
@biochemeleon/registry.py
@biochemeleon/setup_state.py
@biochemeleon/wizard.py
@biochemeleon/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Audit 10 research claims against the current codebase; write 10-04-AUDIT.md</name>
  <files>.planning/phases/10-polish-endgame-help/10-04-AUDIT.md</files>
  <action>
Create `.planning/phases/10-polish-endgame-help/10-04-AUDIT.md` with a markdown table auditing exactly 10 research claims. For EACH claim: read the cited source file at the cited line range, confirm the claim matches the code, and record PASS or FAIL with the actual file:line as evidence. The audit is READ-ONLY — do NOT modify any source file.

The 10 claims to verify (use the EXACT wording + citations below; record evidence as the file:line you actually read):

**Claim 1 — game.py hint/reveal counters.**
Research cites `game.py:32-33` (init) + `game.py:62-63` (reset in start()).
Claim: `GameController.__init__` initializes `self._hint_count = 0` and `self._reveal_count = 0`, AND `start()` resets both to 0 per round.
Verify: read `biochemeleon/game.py` around lines 20-90; confirm both fields exist in `__init__` and are reset in `start()`.

**Claim 2 — gui_game.py _finish_win QMessageBox + exec_ gate baseline.**
Research cites `gui_game.py:307-312`.
Claim: `_finish_win` constructs a `QMessageBox(self.window())`, calls `setText`, sets `WindowStaysOnTopHint`, and calls `msg.exec_()`. This is the SOLE `.exec_()` hit in the package today (the exec_ grep gate = 1).
Verify: read `biochemeleon/gui_game.py` lines ~284-326 (the `_finish_win` method); confirm the QMessageBox + `.exec_()`. Then run `grep -rnE "\.exec_\(\)" biochemeleon/` and confirm EXACTLY 1 hit. Record the hit's file:line.

**Claim 3 — registry.counts_by_rep zero-fills GAME_REPS and skips rep=None.**
Research cites `registry.py:250-270`.
Claim: `counts_by_rep()` returns a dict with all 5 GAME_REPS keys (zero-filled), and SKIPS records with `rep is None` (never emits a None key).
Verify: read `biochemeleon/registry.py` the `counts_by_rep` method; confirm `out = {rep: 0 for rep in GAME_REPS}` and the `if r.rep is None: continue` skip.

**Claim 4 — HiderRecord has endpoint_resvs field.**
Research cites `registry.py:86-93`.
Claim: `HiderRecord` has an `endpoint_resvs` slot (None default or a 2-tuple of ints) used by the fragment-aware debrief show.
Verify: read `biochemeleon/registry.py` the `HiderRecord.__slots__` + `__init__`; confirm `endpoint_resvs` is in `__slots__` and accepted as a kwarg.

**Claim 5 — game.py _mark_found is fragment-aware.**
Research cites `game.py:204-213`.
Claim: `_mark_found` colors cartoon/ribbon FRAGMENT hiders by `segi GAME and resi rv1+1-rv2-1` (the middle), and single-atom hiders by `id`. The debrief show (Plan 10-06) mirrors this fragment-aware pattern (full rv1-rv2 range for show, not just anchor id).
Verify: read `biochemeleon/game.py` the `_mark_found` method; confirm the `if rec.endpoint_resvs is not None:` branch colors by `segi GAME and resi %d-%d` and the else branch colors by `id`.

**Claim 6 — wizard.py sets mouse_selection_mode=0, does NOT change button_mode.**
Research cites `wizard.py:42-45`.
Claim: `PickWizard.__init__` saves + sets `mouse_selection_mode=0` (single-atom pick) and does NOT change `button_mode` — so left-drag still rotates (basis for the controls text "left-click picks, left-drag rotates").
Verify: read `biochemeleon/wizard.py` lines ~34-50; confirm `cmd.set("mouse_selection_mode", 0)` and NO `button_mode` change.

**Claim 7 — setup_state.py has format_remaining as the precedent.**
Research cites `setup_state.py:418-447`.
Claim: `format_remaining(total, counts_by_rep, easy_mode)` is a PURE formatter (stdlib only) that the new `format_debrief_text` mirrors in structure + test pattern.
Verify: read `biochemeleon/setup_state.py` the `format_remaining` function; confirm it's pure (no `from pymol import cmd`, no `from pymol.Qt import`).

**Claim 8 — setup_state.py is PURE (no pymol, no Qt).**
Claim: `setup_state.py` has NO `from pymol import cmd` and NO `from pymol.Qt import` — the new debrief formatter (Plan 10-01) must preserve this purity.
Verify: read the top of `biochemeleon/setup_state.py` (imports section, lines 1-20); confirm only stdlib imports (`import random`, `import copy`). Run `grep -nE "from pymol|import pymol" biochemeleon/setup_state.py` and confirm 0 hits.

**Claim 9 — __init__.py PluginDialog is modeless (dialog.show(), never .exec_()); exec_ gate = 1.**
Claim: `run_plugin_gui` uses `dialog.show()` (NOT `.exec_()`). The main PluginDialog is MODELESS. The exec_ grep gate has exactly 1 hit today (the `_finish_win` QMessageBox from Claim 2). Plan 10-03 raises it to 2 (Help QDialog), Plan 10-06 raises it to 3 (debrief QMessageBox) — all child dialogs.
Verify: read `biochemeleon/__init__.py` the `run_plugin_gui` function (lines ~20-33); confirm `dialog.show()`. Run the exec_ grep again (already done in Claim 2 — reuse the result).

**Claim 10 — PyMOL controls: plain wheel = slab (NOT zoom).**
Research cites `tmp/pymol-src/modules/pymol/controlling.py:320-348`.
Claim: In the default `three_button_viewing` mode, the plain scroll wheel maps to `slab` (clipping slab, NOT zoom). Zoom = Right-drag (`movz`) or Ctrl+wheel. This is the critical counterintuitive claim the Help text (Plan 10-03) depends on.
Verify: read `tmp/pymol-src/modules/pymol/controlling.py` around lines 320-348 (the `three_button_viewing` mode dict); find the `('w','none',...)` entry (w = wheel) and confirm the third element is `slab` (NOT `movz`/`mvsz`). Also confirm Right-drag maps to `movz` (zoom). Record the exact line + the action code. If `tmp/pymol-src/` is not present in this worktree, read it from the absolute path `/mnt/c/Users/nglok/Desktop/WORKDIR/molmdl/bioCHEMeleon/tmp/pymol-src/modules/pymol/controlling.py`.

**Output format for 10-04-AUDIT.md:**

```markdown
# Phase 10 Pre-Implementation Audit: Research Claims vs Current Code

**Audited:** 2026-08-17
**Auditor:** Plan 10-04 (read-only)
**Purpose:** Verify research claims before implementation plans 10-05..10-08, 10-10 build on them.

## Summary

| # | Claim | Verdict |
|---|-------|---------|
| 1 | game.py hint/reveal counters | PASS/FAIL |
| 2 | gui_game.py _finish_win QMessageBox + exec_ gate = 1 | PASS/FAIL |
| ... | ... | ... |
| 10 | PyMOL wheel = slab (not zoom) | PASS/FAIL |

**Total:** X/10 PASS, Y FAIL.

## Detail

### Claim 1: game.py hint/reveal counters
- **Source citation:** game.py:32-33 (init), game.py:62-63 (reset)
- **Claim:** `GameController.__init__` initializes `self._hint_count = 0` + `self._reveal_count = 0`; `start()` resets both.
- **Verified at:** biochemeleon/game.py:NN (init), biochemeleon/game.py:NN (reset)
- **Verdict:** PASS / FAIL
- **Notes:** [any discrepancy; if FAIL, which downstream plan is blocked]

### Claim 2: ...
[repeat for each claim]

## Blockers (if any FAIL)

| Failed claim | Blocks plan(s) | Action needed |
|---|---|---|

## Conclusion

[All 10 PASS → Wave 1 parallel plans (10-01/02/03/05) proceed unaffected; Wave 2 (10-06) proceeds unconditionally. Any FAIL → flag blocker for the dependent plan (10-06 in Wave 2 for claims 3/4/5; 10-05 in Wave 1 for claims 1/2 — note: 10-05 runs parallel with this audit, so a FAIL on claims 1/2 is a concurrent-issue flag for human triage, not a pre-block) + recommend fix before the dependent plan executes.]
```

After writing the audit, run:
```
python3.6 -m py_compile biochemeleon/*.py   # confirm no source code was accidentally changed
grep -rnE "\.exec_\(\)" biochemeleon/        # confirm still 1 hit (audit is read-only)
```
Commit:
```
git add .planning/phases/10-polish-endgame-help/10-04-AUDIT.md
git commit -m "docs(10-04): pre-implementation audit of research claims vs code"
```
  </action>
  <verify>
`10-04-AUDIT.md` exists with all 10 claims verified (PASS or FAIL-flagged with evidence). `python3.6 -m py_compile biochemeleon/*.py` passes (audit changed no source). `grep -rnE "\.exec_\(\)" biochemeleon/` still returns exactly 1 hit (read-only audit). `grep -rnE "import Tkinter|...|import PyQt5" biochemeleon/` still 0.
  </verify>
  <done>10-04-AUDIT.md written with 10 verified claims (each PASS or FAIL-flagged + downstream-blocker noted); no source code changed; commit landed as docs(10-04).</done>
</task>

</tasks>

<verification>
Phase-10-04 (this plan alone):
- `10-04-AUDIT.md` exists in `.planning/phases/10-polish-endgame-help/`.
- All 10 claims have a verdict (PASS or FAIL) + a `Verified at: file:line` evidence row.
- Any FAIL claim has a "Blocks plan(s)" entry naming the dependent plan.
- No source code under `biochemeleon/` was modified (`git status` shows only the audit file added).
- `python3.6 -m py_compile biochemeleon/*.py` passes (unchanged codebase).
- `grep -rnE "\.exec_\(\)" biochemeleon/` still returns exactly 1 hit.
- Pitfall-1 grep gate still 0.
</verification>

<success_criteria>
- 10-04-AUDIT.md exists with a 10-row summary table + a per-claim detail section.
- Each claim's verdict is backed by a file:line evidence read (not just the research's citation).
- Any FAIL is explicitly flagged as a blocker for the downstream plan(s).
- No source code changed (read-only audit).
- One atomic commit lands as `docs(10-04)`.
- File ownership is strictly the audit markdown (parallel-safe with Plans 10-01/02/03 in Wave 1 — no source files touched).
</success_criteria>

<output>
After completion, create `.planning/phases/10-polish-endgame-help/10-04-SUMMARY.md` recording:
- The audit verdict (X/10 PASS, Y FAIL).
- Any blockers (if FAIL) and which downstream plan is affected.
- A one-line handoff: "Pre-implementation audit complete; research claims verified [all PASS / N blockers flagged]. Wave 1 parallel plans (10-05) proceeded concurrently; Wave 2 (10-06) may proceed [unconditionally / after blocker N is resolved]."
</output>
