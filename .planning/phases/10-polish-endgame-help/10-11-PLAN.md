---
phase: 10-polish-endgame-help
plan: 11
type: execute
wave: 7
depends_on:
  - "10-10"
files_modified: []
autonomous: true

must_haves:
  truths:
    - "10-11-AUDIT.md exists with 10 verified README/doc claims against the shipped code, each PASS or FAIL-flagged"
    - "The README's feature claims match the shipped code (no doc drift)"
    - "The exec_ grep gate is 3 (all child dialogs: _finish_win QMessageBox, _finish_debrief QMessageBox, Help QDialog) — NO .exec_() on the main PluginDialog/SetupTab/GameTab"
    - "The Pitfall-1 grep gate is 0"
    - "The full pure-layer test suite passes (incl the TestFormatDebrief from Plan 10-01)"
    - "Any FAIL from the 10-04 pre-implementation audit was addressed during implementation (no outstanding FAIL)"
    - "Any FAIL in this post-implementation audit is flagged as a gap for a follow-up"
  artifacts:
    - path: ".planning/phases/10-polish-endgame-help/10-11-AUDIT.md"
      provides: "Post-implementation verification of README + docs vs shipped code"
      contains: "10-11-AUDIT"
  key_links:
    - from: "README.md + planning docs claims"
      to: "the shipped code (biochemeleon/ + smoke/)"
      via: "read each shipped file + run the grep gates + run the test suite"
      pattern: "PASS|FAIL"
---

<objective>
Post-implementation audit — verify the README + planning docs accurately reflect the SHIPPED code. Catch any drift introduced during implementation (e.g. a README claim that doesn't match the actual shipped behavior, a doc that references a feature that didn't land, a grep gate that drifted). This is the bookend to Plan 10-04's pre-implementation audit: 10-04 verified the research claims before implementation; 10-11 verifies the shipped docs after implementation. READ-ONLY — no code or doc changes (this plan writes only the audit markdown).

Purpose: After 10 plans of implementation + 1 human-verify checkpoint, the README (Plan 10-10) and the planning docs describe the shipped product. This audit confirms every user-facing claim in the README is backed by shipped code, the grep gates are at their expected final state (exec_ = 3 all child dialogs, Pitfall-1 = 0), the test suite is green (incl the new TestFormatDebrief), and any 10-04 pre-implementation audit FAIL was addressed. Any drift found here is flagged as a gap for a follow-up (not fixed in this audit).

Output:
- `.planning/phases/10-polish-endgame-help/10-11-AUDIT.md` — a table of 10 claims, each with: Claim/Doc-statement | Shipped-code evidence (file:line) | PASS/FAIL | Notes.
- No code or doc changes (read-only audit).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-polish-endgame-help/10-RESEARCH-endgame.md
@.planning/phases/10-polish-endgame-help/10-RESEARCH-help.md
@.planning/phases/10-polish-endgame-help/10-04-AUDIT.md
@.planning/phases/10-polish-endgame-help/10-09-VERIFY.md

@README.md
@biochemeleon/gui_game.py
@biochemeleon/gui_setup.py
@biochemeleon/__init__.py
@biochemeleon/setup_state.py
@biochemeleon/game.py
@biochemeleon/registry.py
@smoke/phase10_smoke.py
</context>

<tasks>

<task type="auto">
  <name>Audit 10 README/doc claims against the shipped code; write 10-11-AUDIT.md</name>
  <files>.planning/phases/10-polish-endgame-help/10-11-AUDIT.md</files>
  <action>
Create `.planning/phases/10-polish-endgame-help/10-11-AUDIT.md` with a markdown table auditing exactly 10 README/doc claims against the shipped code. For EACH claim: read the shipped code, confirm the claim matches, record PASS or FAIL with the shipped file:line as evidence. The audit is READ-ONLY — do NOT modify any source or doc file.

The 10 claims to verify:

**Claim 1 — README "Endgame & debrief" claim matches shipped code.**
README claim (from Plan 10-10): "the win screen shows your time, hints used, and reveals used, then a debrief highlights each hider and explains why it was hard to spot."
Verify: read `biochemeleon/gui_game.py` — confirm `_finish_win` calls `setInformativeText` with Time/Hints/Reveals (Plan 10-05) AND `_finish_debrief` exists + calls `format_debrief_text` + shows a second QMessageBox (Plan 10-06). Record the file:line of `setInformativeText` in `_finish_win` + the `_finish_debrief` method.

**Claim 2 — README "Help & tooltips" claim matches shipped code.**
README claim: "every control has a tooltip; a Help button opens a panel explaining each representation, the PyMOL controls, and a tip on switching representations."
Verify: read `biochemeleon/__init__.py` — confirm `HELP_HTML` constant + `help_btn` + `_show_help` method exist (Plan 10-03). Read `biochemeleon/gui_setup.py` + `biochemeleon/gui_game.py` — confirm `setToolTip` calls are present on the widgets (Plans 10-02 + 10-07). Record the file:line of `HELP_HTML` + a count of `setToolTip` calls in each GUI file.

**Claim 3 — README "Controls" claim matches HELP_HTML.**
README claim (implicit via the Help bullet): the PyMOL controls are documented (rotate/pan/zoom/click).
Verify: read `biochemeleon/__init__.py` `HELP_HTML` — confirm it contains the verified controls text: "Left-drag" (rotate), "Right-drag" (zoom), "Ctrl + scroll" (zoom), "plain scroll wheel adjusts the clipping plane, not zoom" (the wheel=slab gotcha), "Left-click" (pick). Record the line of the wheel=slab gotcha text.

**Claim 4 — README Demo table matches setup_state.py DEMO_MANIFEST + TIER_LABELS.**
README claim: the 9 demos in the 4-tier table (Easy/Hard/Challenge/Very challenging + network column).
Verify: read `biochemeleon/setup_state.py` `DEMO_MANIFEST` (9 entries) + `TIER_LABELS` (4 keys: easy/hard/challenge/very_challenging). Confirm the README's 9 rows match the manifest's `difficulty` fields + `source` (bundled vs sasbdb/memprotmd = network). Record any mismatch (e.g. a README row whose ID isn't in DEMO_MANIFEST, or a tier label that doesn't match TIER_LABELS).

**Claim 5 — README Usage step 6 matches the actual win-then-debrief flow.**
README claim: "Find all hiders to win — the timer stops and a win screen shows your time, hints used, and reveals used, followed by a debrief highlighting each hider and why it was hard to spot."
Verify: read `biochemeleon/gui_game.py` `_on_win` → `_finish_win` → `_show_all_hiders_for_debrief` → `_finish_debrief` flow. Confirm the sequence matches (win dialog → show hiders → debrief dialog). Record the file:line of each step.

**Claim 6 — README "Tips — How to spot hiders" section is present + framed as legitimate.**
README claim: the Tips section says "switch reps to make an impostor stand out" + "legitimate observation strategy, not a cheat."
Verify: read `README.md` the Tips section — confirm both phrases are present. Record the line numbers.

**Claim 7 — exec_ grep gate = 3, all child dialogs.**
Expected: 3 hits after all Phase 10 plans execute — `gui_game.py` `_finish_win` QMessageBox, `gui_game.py` `_finish_debrief` QMessageBox, `__init__.py` `_show_help` QDialog. NO hit on the main PluginDialog/SetupTab/GameTab.
Verify: run `grep -rnE "\.exec_\(\)" biochemeleon/`. Record the count + each hit's file:line. Confirm all 3 are on child dialogs (QMessageBox or QDialog), NONE on the main PluginDialog (which uses `dialog.show()` in `run_plugin_gui`). If the count is not 3, flag FAIL + note which plan introduced/excluded a hit.

**Claim 8 — Pitfall-1 grep gate = 0.**
Verify: run `grep -rnE "import Tkinter|import tkinter|from tkinter|import Pmw|from Pmw|app\.root|grab_set|mainloop|Toplevel|menuBar\.addmenuitem|from PyQt5 import|import PyQt5" biochemeleon/`. Record the count (MUST be 0).

**Claim 9 — Full pure-layer test suite passes (incl TestFormatDebrief).**
Verify: run `python3.6 -m unittest tests.test_setup_state -v`. Record the count of tests run + the pass/fail count. Confirm `TestFormatDebrief` (5 tests from Plan 10-01) is in the suite + passes. Confirm no regression to the existing 222+ tests.

**Claim 10 — The 10-04 pre-implementation audit's findings (if any FAIL) were addressed.**
Verify: read `.planning/phases/10-polish-endgame-help/10-04-AUDIT.md`. If 10-04 recorded any FAIL claims, confirm the dependent implementation plan addressed them (e.g. if 10-04 Claim 2 FAILED because `_finish_win` was at a different line, confirm 10-05/10-06 still located + modified the right block). If 10-04 was all PASS, this claim is PASS trivially (no outstanding FAIL). Record the 10-04 FAIL count + whether each was addressed.

**Output format for 10-11-AUDIT.md:**

```markdown
# Phase 10 Post-Implementation Audit: README + Docs vs Shipped Code

**Audited:** 2026-08-17
**Auditor:** Plan 10-11 (read-only)
**Purpose:** Verify the README + planning docs accurately reflect the SHIPPED code after all Phase 10 plans. Catch any drift.

## Summary

| # | Claim | Verdict |
|---|-------|---------|
| 1 | README "Endgame & debrief" matches shipped code | PASS/FAIL |
| 2 | README "Help & tooltips" matches shipped code | PASS/FAIL |
| ... | ... | ... |
| 10 | 10-04 pre-impl audit findings addressed | PASS/FAIL |

**Total:** X/10 PASS, Y FAIL.

## Grep Gates

- exec_ gate: [N] hits (expected 3, all child dialogs) — [file:line list]
- Pitfall-1 gate: [N] hits (expected 0)
- Test suite: [N] tests, [M] passed (expected all pass, incl 5 TestFormatDebrief)

## Detail

### Claim 1: README "Endgame & debrief" matches shipped code
- **README claim:** "the win screen shows your time, hints used, and reveals used, then a debrief..."
- **Shipped code evidence:** biochemeleon/gui_game.py:NN (_finish_win setInformativeText), biochemeleon/gui_game.py:NN (_finish_debrief)
- **Verdict:** PASS / FAIL
- **Notes:** [any drift]

[repeat for each claim]

## Gaps (if any FAIL)

| Failed claim | Drift description | Recommended follow-up |
|---|---|---|

## Conclusion

[All 10 PASS → Phase 10 is complete + documented accurately. Any FAIL → gap for a follow-up (not fixed in this audit).]
```

After writing the audit, run (confirm read-only):
```
python3.6 -m py_compile biochemeleon/*.py   # confirm no source code was accidentally changed
python3.6 -m py_compile smoke/phase10_smoke.py  # confirm smoke unchanged
```
Commit:
```
git add .planning/phases/10-polish-endgame-help/10-11-AUDIT.md
git commit -m "docs(10-11): post-implementation audit of README + docs vs shipped code"
```
  </action>
  <verify>
`10-11-AUDIT.md` exists with all 10 claims verified (PASS or FAIL-flagged with shipped-code evidence). The grep gates section records exec_ = 3 (all child dialogs), Pitfall-1 = 0, test suite all pass. `python3.6 -m py_compile biochemeleon/*.py` passes (audit changed no source). `python3.6 -m py_compile smoke/phase10_smoke.py` passes (smoke unchanged).
  </verify>
  <done>10-11-AUDIT.md written with 10 verified claims (each PASS or FAIL-flagged + gap-noted); grep gates at expected final state (exec_=3 child dialogs, Pitfall-1=0, tests green); no source/doc code changed; commit landed as docs(10-11).</done>
</task>

</tasks>

<verification>
Phase-10-11 (this plan alone):
- `10-11-AUDIT.md` exists in `.planning/phases/10-polish-endgame-help/`.
- All 10 claims have a verdict (PASS or FAIL) + a `Shipped code evidence: file:line` row.
- The grep gates section records: exec_ = 3 (all child dialogs — _finish_win QMessageBox, _finish_debrief QMessageBox, Help QDialog), Pitfall-1 = 0, test suite all pass (incl 5 TestFormatDebrief).
- Any FAIL is explicitly flagged as a gap for a follow-up (not fixed in this audit).
- No source code or doc file was modified (`git status` shows only the audit file added).
- `python3.6 -m py_compile biochemeleon/*.py` passes (unchanged codebase).
- `python3.6 -m py_compile smoke/phase10_smoke.py` passes (smoke unchanged).
</verification>

<success_criteria>
- 10-11-AUDIT.md exists with a 10-row summary table + a per-claim detail section + a grep-gates section + a gaps section.
- Each claim's verdict is backed by a shipped-code file:line evidence read (not just the README's text).
- The exec_ grep gate is confirmed at 3 (all child dialogs; NO main-dialog hit).
- The Pitfall-1 grep gate is confirmed at 0.
- The test suite is confirmed green (incl TestFormatDebrief).
- Any 10-04 pre-implementation audit FAIL is confirmed addressed.
- Any FAIL in this audit is flagged as a gap (not fixed here).
- No source or doc code changed (read-only audit).
- One atomic commit lands as `docs(10-11)`.
- File ownership is strictly the audit markdown (Wave 7, the final plan of Phase 10).
</success_criteria>

<output>
After completion, create `.planning/phases/10-polish-endgame-help/10-11-SUMMARY.md` recording:
- The audit verdict (X/10 PASS, Y FAIL).
- The grep-gate final state (exec_ = 3 child dialogs, Pitfall-1 = 0, tests green).
- Any gaps (if FAIL) + recommended follow-up.
- A one-line conclusion: "Phase 10 post-implementation audit complete; README + docs match shipped code [all PASS / N gaps flagged for follow-up]. Phase 10 is COMPLETE."
</output>
