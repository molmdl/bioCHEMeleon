---
phase: 10-polish-endgame-help
plan: 10
type: execute
wave: 5
depends_on:
  - "10-09"
files_modified:
  - README.md
autonomous: true

must_haves:
  truths:
    - "README.md reflects the final shipped v1 feature set (incl endgame + debrief + help + controls + tooltips)"
    - "The vibe-coding warning block (current lines 1-2) is KEPT VERBATIM (unchanged)"
    - "The 'UNDER DEVELOPMENT' line (current line 4) is REMOVED — v1 PyMOL plugin is COMPLETE"
    - "The Status line reflects v1 complete + v2 deferred"
    - "Usage step 6 reflects the win-then-debrief flow (time + hints + reveals + debrief)"
    - "A 'Tips — How to spot hiders' section frames the switch-reps strategy as legitimate (not a cheat)"
    - "The Demo Molecules table uses the 4-tier labels (Easy/Hard/Challenge/Very challenging) + a network column"
    - "The README contains NO phase numbers, plan IDs, or GSD workflow mentions (user-facing)"
    - "The tone is clear, concise, engaging, professional but simple — understandable by students"
  artifacts:
    - path: "README.md"
      provides: "Finalized README reflecting the shipped v1 product"
      contains: "bioCHEMeleon"
  key_links:
    - from: "README.md feature claims"
      to: "the shipped code (verified by Plan 10-11 audit)"
      via: "every README claim matches a shipped feature; Plan 10-11 audits this"
      pattern: "Endgame|Help|debrief|controls"
---

<objective>
Rewrite `README.md` per `10-RESEARCH-help.md` "README: Feature Inventory + Update Guidance". KEEP the top vibe-coding warning block (lines 1-2) VERBATIM. REMOVE "UNDER DEVELOPMENT" (line 4) — the v1 PyMOL plugin is COMPLETE. Update the Status line, What It Does (add Endgame & debrief + Help & tooltips), Usage step 6 (win screen = time + hints + reveals + debrief), add a Tips section (switch-reps strategy, legitimate not cheat), update the Demo Molecules table (4-tier + network column), and the last-updated date. Tone: clear, concise, engaging, professional but simple — understandable by students. NO phase numbers / GSD workflow mentions (user-facing).

Purpose: SC5 requires the README to reflect the final shipped feature set, install instructions, and usage. Phase 10 ships endgame (win stats + debrief), help (tooltips + Help dialog + controls), and the README is the user-facing entry point. The research provides a full outline + feature inventory; this plan executes it. Sequenced AFTER Plan 10-09's human-verify (so the README reflects the verified-shipped behavior, not aspirational behavior — the research flagged this dependency).

Output:
- `README.md` rewritten per the research outline. KEEP lines 1-2 verbatim; REMOVE line 4; update Status, What It Does, Usage, Demo table, add Tips section, update last-updated date.
- No code changes.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-polish-endgame-help/10-RESEARCH-help.md
@.planning/phases/10-polish-endgame-help/10-09-VERIFY.md

@README.md
@biochemeleon/setup_state.py
</context>

<tasks>

<task type="auto">
  <name>Rewrite README.md to reflect the shipped v1 feature set (SC5)</name>
  <files>README.md</files>
  <action>
Rewrite `README.md` per `10-RESEARCH-help.md` "README: Feature Inventory + Update Guidance" (the full outline + feature inventory table). Use the EXACT structure from the research's "Recommended README structure" outline. Tone: clear, concise, engaging, professional but simple — understandable by students. NO phase numbers, plan IDs, or GSD workflow mentions (the README is user-facing; the current README already follows this — keep it).

**MUST KEEP VERBATIM** (current README lines 1-2 — the vibe-coding warning):
```
> This is a vibe-coding project. While the human attempt to verify the source of all 
> contents, if you find any issues please contact me.
> 
```
Do NOT change a single character of these two lines (including the trailing `> ` blank line).

**MUST REMOVE** (current README line 4): the `> UNDER DEVELOPMENT` line. The v1 PyMOL plugin is COMPLETE. Do NOT replace it with another tag — just remove the line (the research outline shows the line removed, with the `# bioCHEMeleon` heading following the vibe-coding warning directly).

**Update the Status line** (current line 12). Replace:
`> **Status:** Planning complete. v1 (PyMOL plugin) is being built; v2 (VMD tcl script) is deferred.`
with:
`> **Status:** v1 (PyMOL plugin) is complete. v2 (VMD tcl script) is deferred.`
Keep the `> See `.planning/ROADMAP.md` for the phase breakdown.` line (line 13) as-is.

**Update "What It Does"** (current lines 17-24). Keep the existing accurate bullets (Setup tab, Game tab, Core loop, Persistence, Demos) and ADD two new bullets per the research:
- `**Endgame & debrief** — the win screen shows your time, hints used, and reveals used, then a debrief highlights each hider and explains why it was hard to spot (the teachable moment).`
- `**Help & tooltips** — every control has a tooltip; a Help button opens a panel explaining each representation, the PyMOL controls (rotate/pan/zoom/click), and a tip on switching representations to spot hiders.`

Place the two new bullets AFTER "Core loop" and BEFORE "Persistence" (matching the research outline order: Setup, Game, Core loop, Endgame, Help, Persistence, Demos). Update the existing Game tab bullet to mention found-management + color picker if not already there (the current README mentions "found-hider management dropdown" — keep it).

**Update "Usage" step 6** (current line 50). Replace:
`6. Find all hiders to win — the timer stops and shows your time (plus hints/reveals used).`
with:
`6. Find all hiders to win — the timer stops and a win screen shows your time, hints used, and reveals used, followed by a debrief highlighting each hider and why it was hard to spot.`

**Add a "Tip" line at the end of Usage** (after step 7), per the research outline:
`> Tip: stuck? Open the Help button for how to rotate, zoom, and click — and for a strategy on switching representations to spot hiders.`

**Add a NEW "Tips — How to spot hiders" section** AFTER the Usage section and BEFORE the Demo Molecules section (per the research outline). Use this text (from the research, student-friendly):
```
## Tips — How to spot hiders

- Hiders blend into one representation. Switch reps to make an impostor stand out (e.g. hide cartoon, show spheres — a cartoon hider will stick out as an extra sphere). See the in-app Help panel for details.
- This is a legitimate observation strategy, not a cheat.
```

**Update the Demo Molecules table** (current lines 55-66). Replace the ad-hoc tier labels with the 4-tier system + a network column, per the research outline. The 9 demos come from `setup_state.py DEMO_MANIFEST` (verified). Use this table:
```
## Demo Molecules

| Tier | Category | Source | ID(s) | Needs network? |
|------|----------|--------|-------|----------------|
| Easy | Protein | RCSB PDB | 1znf | no (bundled) |
| Easy | RNA | RCSB PDB | 5e54 | no (bundled) |
| Easy | DNA | RCSB PDB | 1k8p | no (bundled) |
| Hard | Protein | RCSB PDB | 1xdn | no (bundled) |
| Hard | RNA | RCSB PDB | 2qbz | no (bundled) |
| Hard | Mixed | RCSB PDB | 4wb3 | no (bundled) |
| Challenge | Glycoprotein | SASBDB | sasdpg4 | yes (fetched) |
| Very challenging | Membrane protein | MemProtMD | 1gzm | yes (fetched) |
| Very challenging | Membrane protein | MemProtMD | 3gp6 | yes (fetched) |

Full attribution in `DATA_SOURCES.md`.
```
Keep the existing `Full attribution in `DATA_SOURCES.md`.` line (it already exists at line 67 — keep it).

**KEEP UNCHANGED** (accurate sections — do NOT modify):
- "Requirements" section (lines 25-31) — accurate.
- "Install" section (lines 33-41) — accurate.
- "Project Structure" section (lines 69-81) — accurate, EXCEPT for two inline code-block comments that mention GSD/Phase (they would trip the `grep -cE "Phase [0-9]|plan 10|gsd|GSD" README.md` check, returning 2 instead of 0). CLEAN these two comment lines as an EXCEPTION to "KEEP UNCHANGED":
  - Line 73: `├── .planning/          # GSD workflow artifacts (PROJECT, ROADMAP, REQUIREMENTS, research)` → change the comment to `# project planning docs` (so the line reads `├── .planning/          # project planning docs (PROJECT, ROADMAP, REQUIREMENTS, research)`). Keep the directory name + the parenthetical; only change the `GSD workflow artifacts` phrase.
  - Line 74: `├── biochemeleon/       # The PyMOL plugin package (created during Phase 1)` → drop the parenthetical (so the line reads `├── biochemeleon/       # The PyMOL plugin package`). Keep the directory name + the `# The PyMOL plugin package` text; only drop `(created during Phase 1)`.
  - Keep the rest of the Project Structure tree unchanged (the other directory names + comments are fine — only these two lines mention GSD/Phase).
- "License" section (lines 83-85) — accurate.
- "Acknowledgements" section (lines 87-89) — accurate.

**Update the last-updated date** (current line 91). Replace:
`*This README is updated as the project evolves. Last updated: 2026-08-03 after project initialization.*`
with:
`*Last updated: 2026-08-17.*`
(Use today's date — the ship date. Drop the "updated as the project evolves" phrasing since v1 is complete.)

**After writing, verify:**
```
grep -c "UNDER DEVELOPMENT" README.md          # MUST return 0
grep -c "vibe-coding project" README.md        # MUST return 1 (the kept warning)
grep -c "Endgame" README.md                    # MUST return >=1 (the new bullet)
grep -c "debrief" README.md                    # MUST return >=1
grep -c "Help & tooltips" README.md            # MUST return >=1
grep -c "Tips — How to spot hiders" README.md  # MUST return 1 (the new section)
grep -c "legitimate observation strategy" README.md  # MUST return 1
grep -cE "Phase [0-9]|plan 10|gsd|GSD" README.md  # MUST return 0 (no phase/plan/GSD mentions)
```
Read the README end-to-end to confirm: markdown renders cleanly (no broken tables, no dangling headings), tone is student-friendly, no jargon without explanation.

Commit:
```
git add README.md
git commit -m "docs(10-10): finalize README for v1 ship (SC5)"
```
  </action>
  <verify>
`grep -c "UNDER DEVELOPMENT" README.md` returns 0. `grep -c "vibe-coding project" README.md` returns 1 (kept verbatim). `grep -c "Endgame" README.md` returns >=1. `grep -c "debrief" README.md` returns >=1. `grep -c "Help & tooltips" README.md` returns >=1. `grep -c "Tips — How to spot hiders" README.md` returns 1. `grep -c "legitimate observation strategy" README.md` returns 1. `grep -cE "Phase [0-9]|plan 10|gsd|GSD" README.md` returns 0 (no phase/plan/GSD mentions). The Demo table has 9 rows + a network column. The README markdown renders cleanly (read end-to-end). Tone is student-friendly.
  </verify>
  <done>README finalized: vibe-coding warning kept verbatim, UNDER DEVELOPMENT removed, v1 complete status, Endgame + Help bullets added, Usage step 6 updated, Tips section added, Demo table 4-tier + network column, last-updated date updated, no phase/GSD mentions; commit landed as docs(10-10).</done>
</task>

</tasks>

<verification>
Phase-10-10 (this plan alone):
- `grep -c "UNDER DEVELOPMENT" README.md` returns 0.
- `grep -c "vibe-coding project" README.md` returns 1 (the warning kept verbatim).
- `grep -c "Endgame" README.md` returns >=1.
- `grep -c "debrief" README.md` returns >=1.
- `grep -c "Help & tooltips" README.md` returns >=1.
- `grep -c "Tips — How to spot hiders" README.md` returns 1.
- `grep -c "legitimate observation strategy" README.md` returns 1.
- `grep -cE "Phase [0-9]|plan 10|gsd|GSD" README.md` returns 0 (user-facing, no internal references).
- The Demo Molecules table has 9 rows + a network column + the 4-tier labels (Easy/Hard/Challenge/Very challenging).
- The README markdown renders cleanly (no broken tables, no dangling headings).
- Tone is clear, concise, engaging, professional but simple — understandable by students.
</verification>

<success_criteria>
- The README reflects the final shipped v1 feature set (SC5): endgame, debrief, help, tooltips, controls.
- The vibe-coding warning (lines 1-2) is KEPT VERBATIM.
- "UNDER DEVELOPMENT" is REMOVED.
- The Status line says v1 complete + v2 deferred.
- Usage step 6 reflects the win-then-debrief flow.
- The Tips section frames the switch-reps strategy as legitimate (not a cheat).
- The Demo table uses the 4-tier labels + a network column.
- NO phase numbers / plan IDs / GSD workflow mentions (user-facing).
- Tone is student-friendly.
- One atomic commit lands as `docs(10-10)`.
- File ownership is strictly `README.md` (Wave 5, after the human-verify checkpoint 10-09 so the README reflects verified-shipped behavior).
</success_criteria>

<output>
After completion, create `.planning/phases/10-polish-endgame-help/10-10-SUMMARY.md` recording:
- The README changes (sections added/updated, vibe-coding warning kept, UNDER DEVELOPMENT removed).
- The grep verifications (UNDER DEVELOPMENT=0, vibe-coding=1, no phase/GSD mentions).
- A one-line handoff to Plan 10-11: "README finalized; Plan 10-11 audits the README claims against the shipped code."
</output>
