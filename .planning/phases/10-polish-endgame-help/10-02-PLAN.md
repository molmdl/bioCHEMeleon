---
phase: 10-polish-endgame-help
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - biochemeleon/gui_setup.py
autonomous: true

must_haves:
  truths:
    - "Every user-facing widget in gui_setup.py's SetupTab has a setToolTip() call with clear, concise, student-friendly text"
    - "Each of the 5 per-rep checkboxes carries BOTH the generic 'check to put hiders in this rep' text AND the short rep explanation (UX-01 'what each representation means' on hover)"
    - "Tooltips render without changing any widget behavior (pure additive — no signal, no slot, no layout change)"
    - "The Pitfall-1 grep gate stays at 0 (no new PyQt5 / Tkinter / Pmw violations)"
    - "The exec_ grep gate stays at 1 (this plan adds NO .exec_() — tooltips are not modal dialogs)"
  artifacts:
    - path: "biochemeleon/gui_setup.py"
      provides: "setToolTip() calls on every previously-untooltipped widget in SetupTab"
      contains: "setToolTip"
  key_links:
    - from: "biochemeleon/gui_setup.py per-rep cb loop"
      to: "biochemeleon/setup_state.py::GAME_REPS"
      via: "the per-rep checkbox tooltip is built inside the `for rep in GAME_REPS` loop using the loop variable `rep`"
      pattern: "for rep in GAME_REPS"
    - from: "gui_setup.py tooltip text (per-rep)"
      to: "10-RESEARCH-help.md Representation Explanations table"
      via: "the short rep explanation text comes verbatim from the research's verified per-rep descriptions"
      pattern: "Thin lines|Each bond|Each atom|A cartoon|A flat ribbon"
---

<objective>
Add `setToolTip()` calls to every previously-untooltipped widget in `biochemeleon/gui_setup.py`'s SetupTab — ~25 widgets per the help-research tooltip inventory table, including per-rep checkboxes that carry BOTH the generic "check to put hiders in this rep" text AND the short rep explanation (UX-01 "what each representation means" on hover). Pure additive pass — no behavior change, no new signal/slot, no layout change.

Purpose: UX-01 (SC1) requires tooltips / a help panel explaining what each button does and what each representation means. This plan delivers the Setup-tab tooltip half of SC1; Plan 10-03 delivers the Help panel; Plan 10-07 delivers the Game-tab tooltips. Deferring the human-verify (hover each widget in a real PyMOL session) to Plan 10-09's human-verify checkpoint keeps the parallel Wave-1 plans autonomous (Qt tooltips can't be rendered headlessly from WSL — AGENTS.md).

Output:
- `biochemeleon/gui_setup.py` modified: ~25 new `setToolTip()` calls added near the existing widget construction in `_build_ui` (one call per widget, immediately after the widget is created OR grouped by section).
- No new tests (tooltips are Qt-string-only — `python3.6 -m py_compile` syntax-checks them; the grep gates are unaffected — `setToolTip` matches neither gate).
- No new files.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-polish-endgame-help/PLANNER_BRIEF.md
@.planning/phases/10-polish-endgame-help/10-RESEARCH-help.md

@biochemeleon/gui_setup.py
@biochemeleon/setup_state.py
</context>

<tasks>

<task type="auto">
  <name>Add setToolTip() to every un-tooltipped Setup-tab widget</name>
  <files>biochemeleon/gui_setup.py</files>
  <action>
Add `setToolTip(text)` calls to every previously-untooltipped widget in `SetupTab._build_ui` per the help-research "UX-01: Widget Tooltip Inventory" table (gui_setup.py section). Keep each tooltip to one clear, concise, student-friendly sentence. Use the EXACT draft text from the research table — do NOT paraphrase.

**Existing tooltips to LEAVE AS-IS** (already set — do NOT change):
- `obj_refresh_btn` (line 86): "Refresh loaded objects"
- `export_btn` (lines 207-211): the existing export tooltip
- `cleanup_btn` (lines 215-217): the existing cleanup tooltip

**Tooltips to ADD** (place each `setToolTip()` immediately AFTER the widget's construction / configuration — match the existing `obj_refresh_btn.setToolTip("...")` pattern at line 86). Use these EXACT draft strings from `10-RESEARCH-help.md` (the "Tooltips to ADD — gui_setup.py" table):

Target group widgets:
- `mode_combo` (line 72) → `"Choose where the target molecule comes from: a loaded object, a PDB code fetched online, or a bundled demo."`
- `obj_combo` (line 81) → `"Pick a molecule already loaded in PyMOL, or type its object name."`
- `pdb_edit` (line 92) → `"Type a 4-character PDB code (e.g. 1ubq) to download from the RCSB. Needs an internet connection."`
- `fetch_btn` (line 94) → `"Download the typed PDB code from the RCSB website."`
- `pool_list` (line 103) → `"Your pool of PDB codes for Randomize's fetch mode. Empty list = use the bundled pool."`
- `pool_add_btn` (line 111) → `"Add a new PDB code to the pool (must be 4 lowercase letters/digits)."`
- `pool_edit_btn` (line 112) → `"Edit the selected PDB code in the pool."`
- `pool_remove_btn` (line 113) → `"Remove the selected PDB code(s) from the pool."`
- `pool_default_btn` (line 114) → `"Reset the pool to the 34 bundled, pre-verified PDB codes."`
- `pool_choose_btn` (line 115) → `"Pick a random code from the pool and put it in the fetch field."`
- `demo_combo` (line 124) → `"Pick a bundled demo molecule. Tier (Easy → Very challenging) is shown in the name."`

Hiders group widgets:
- `lock_source_cb` (line 146) → `"When checked, Randomize keeps the current target and only re-rolls the hider composition."`
- `hider_spin` (line 155) → `"How many hider atoms to insert. Capped to a sane max for the chosen molecule."`
- `lock_scene_cb` (line 159) → `"Use the molecule's currently-shown representations instead of picking reps manually (locks the per-rep checkboxes to match the scene)."`

Per-rep rows (the loop at lines 166-179 — modify INSIDE the `for rep in GAME_REPS:` loop so each per-rep checkbox + spinbox gets a rep-specific tooltip built from the loop variable `rep`):
- For the per-rep checkbox `cb` (line 168): use a CONCATENATION of the generic tooltip + the short rep explanation, with a blank line between them. Build a per-rep explanation lookup using the verified text from `10-RESEARCH-help.md` "Representation Explanations" table:
  - `lines` → `"Thin lines connecting bonded atoms. A lightweight overview of the whole molecule — fast and simple."`
  - `sticks` → `"Each bond drawn as a thin cylinder. Shows the bonding detail more clearly than lines."`
  - `spheres` → `"Each atom drawn as a sphere (roughly its Van der Waals radius). Gives a space-filling view of the molecule's surface."`
  - `cartoon` → `"A cartoon drawn through the backbone: helices as ribbons/coils, sheets as arrows, loops as tubes. The classic way to see secondary structure."`
  - `ribbon` → `"A flat ribbon drawn through the backbone. Simpler than cartoon — shows the overall fold without secondary-structure detail."`

  The per-rep checkbox tooltip format: `"Check to put hiders in the %s representation. %s Leave unchecked to let the game decide (random)."` % (rep, rep_explanation). E.g. for `cartoon`: `"Check to put hiders in the cartoon representation. A cartoon drawn through the backbone: helices as ribbons/coils, sheets as arrows, loops as tubes. The classic way to see secondary structure. Leave unchecked to let the game decide (random)."`

  Define the explanation lookup as a module-level constant dict near the top of gui_setup.py (or inline in the loop — your call, but a module-level dict keeps the rep text in one place). Suggested name: `REP_EXPLANATIONS` (mirrors the `DEBRIEF_EXPLANATIONS` constant Plan 10-01 adds to setup_state.py, but lives in gui_setup.py since it's UX-01 tooltip text, not the debrief's longer domain text). Alternative: import a shared `REP_SHORT_EXPLANATIONS` from setup_state.py — but to keep files_modified strictly to gui_setup.py for parallel-safe Wave 1, define the dict IN gui_setup.py.

- For the per-rep spinbox `spin` (line 169): `"Number of hiders to hide in this representation (counts toward the total hider count)."` (the generic text from the research — same for all 5 reps; doesn't need rep-specific wording since the checkbox tooltip already names the rep).

Difficulty group:
- `diff_easy_cb` (line 188) → `"Easy: show how many hiders remain per representation. Hard: show only the total remaining."`

Setup actions group:
- `reset_btn` (line 197) → `"Reset every Setup field to the defaults."`
- `random_btn` (line 198) → `"Randomize the target, hider count, and representations."`
- `save_btn` (line 199) → `"Save your current Setup to a .bcm.setup.json file to reuse later."`
- `load_btn` (line 200) → `"Load a previously-saved Setup from a .bcm.setup.json file."`
- `start_btn` (line 218) → `"Generate the hiders and start the game (switches to the Game tab after a 3-2-1 countdown)."`

**Implementation rules:**
- DO NOT change any existing widget construction, configuration, signal, slot, or layout. Add ONLY `setToolTip(text)` calls (and the optional `REP_EXPLANATIONS` module-level dict if you choose that placement).
- DO NOT add a new `from ... import ...` line for the per-rep explanation text — define it in gui_setup.py (keep `files_modified` strictly to gui_setup.py for parallel-safe Wave 1).
- The `lock_source_cb` construction at line 146 is followed by `setChecked(...)` at line 148 — place the setToolTip AFTER the setChecked call to match the existing pattern.
- For widgets constructed via `self.X = QtWidgets.QWidget(...)` followed by `self.X.setLayout(...)` (e.g. no widgets that need a parent set), the setToolTip can go on its own line right after construction.

After the change, run:
```
python3.6 -m py_compile biochemeleon/*.py
grep -rnE "\.exec_\(\)" biochemeleon/                       # expect 1 (unchanged)
grep -rnE "import Tkinter|import tkinter|from tkinter|import Pmw|from Pmw|app\.root|grab_set|mainloop|Toplevel|menuBar\.addmenuitem|from PyQt5 import|import PyQt5" biochemeleon/   # expect 0
```
Commit:
```
git add biochemeleon/gui_setup.py
git commit -m "feat(10-02): add tooltips to all Setup-tab widgets (UX-01)"
```
  </action>
  <verify>
`python3.6 -m py_compile biochemeleon/*.py` — all modules syntax-clean. `grep -rnE "\.exec_\(\)" biochemeleon/` — exactly 1 hit (the existing `_finish_win` QMessageBox in gui_game.py; this plan touches only gui_setup.py and adds NO .exec_() — tooltips are not modal dialogs). `grep -rnE "import Tkinter|import tkinter|from tkinter|import Pmw|from Pmw|app\.root|grab_set|mainloop|Toplevel|menuBar\.addmenuitem|from PyQt5 import|import PyQt5" biochemeleon/` — 0 hits. `python3.6 -m unittest tests.test_setup_state -v` — full suite still passes (no regression — gui_setup.py is Qt-coupled and not unit-tested in WSL, but the test stub confirms the import path still resolves).
  </verify>
  <done>All ~25 Setup-tab widgets have setToolTip() calls; per-rep checkboxes carry both the generic and the short rep explanation; py_compile clean; exec_ gate stays 1; Pitfall-1 gate stays 0; commit landed as feat(10-02).</done>
</task>

</tasks>

<verification>
Phase-10-02 (this plan alone):
- `python3.6 -m py_compile biochemeleon/gui_setup.py` — syntax clean.
- `grep -rnE "\.exec_\(\)" biochemeleon/` — exactly 1 hit (unchanged; this plan adds NO Qt modal dialog).
- `grep -rnE "import Tkinter|import tkinter|from tkinter|import Pmw|from Pmw|app\.root|grab_set|mainloop|Toplevel|menuBar\.addmenuitem|from PyQt5 import|import PyQt5" biochemeleon/` — 0 hits (no new import violations; the new code uses only `pymol.Qt`).
- Every previously-untooltipped widget in `SetupTab._build_ui` has a `setToolTip()` call (count: ~25 new setToolTip calls added; the 3 existing ones unchanged).
- The 5 per-rep checkboxes carry BOTH the generic "check to put hiders in this rep" text AND the short rep explanation (visible on hover).
- No widget behavior changed (pure additive — no new signal, slot, or layout).
- Human-verify (hovering each widget in a real PyMOL session) is DEFERRED to Plan 10-09's human-verify checkpoint.
</verification>

<success_criteria>
- ~25 new `setToolTip()` calls added to `gui_setup.py`'s SetupTab.
- Per-rep checkbox tooltips include the rep explanation (UX-01 "what each representation means" on hover).
- Tooltip text is clear, concise, engaging, professional but simple — understandable by students (no jargon without explanation, per the user constraint).
- The Pitfall-1 grep gate stays at 0; the exec_ grep gate stays at 1 (this plan adds NO Qt modal).
- `python3.6 -m py_compile` clean; existing test suite still passes.
- One atomic commit lands as `feat(10-02)`.
- File ownership is strictly `biochemeleon/gui_setup.py` (parallel-safe with Plans 10-01 [setup_state.py+tests] and 10-03 [__init__.py] in Wave 1).
</success_criteria>

<output>
After completion, create `.planning/phases/10-polish-endgame-help/10-02-SUMMARY.md` recording:
- The widgets that gained tooltips (count + the per-rep explanation strategy).
- The grep-gate state (Pitfall-1 = 0, exec_ = 1 — unchanged).
- A one-line handoff to Plan 10-09: "Setup-tab tooltips are in place; human-verify (hover rendering in real PyMOL) is deferred to the Plan 10-09 checkpoint."
</output>
