---
phase: 10-polish-endgame-help
plan: 07
type: execute
wave: 3
depends_on:
  - "10-06"
files_modified:
  - biochemeleon/gui_game.py
autonomous: true

must_haves:
  truths:
    - "Every previously-untooltipped user-facing widget in gui_game.py's GameTab has a setToolTip() call"
    - "Tooltips render without changing any widget behavior (pure additive — no signal, no slot, no layout change)"
    - "The existing tooltips on _color_btn, _restart_btn, _import_btn, _save_btn are UNCHANGED"
    - "The exec_ grep gate stays at 3 (unchanged from post-10-06 state: 10-03 Help QDialog + 10-05/10-06 _finish_win QMessageBox + 10-06 _finish_debrief QMessageBox; this plan adds NO new .exec_() — tooltips are not modal dialogs)"
    - "The Pitfall-1 grep gate stays at 0"
  artifacts:
    - path: "biochemeleon/gui_game.py"
      provides: "setToolTip() calls on the ~8 previously-untooltipped Game-tab widgets"
      contains: "setToolTip"
  key_links:
    - from: "gui_game.py tooltip text"
      to: "10-RESEARCH-help.md gui_game.py tooltip table"
      via: "the draft text comes verbatim from the research's verified tooltip inventory"
      pattern: "setToolTip"
---

<objective>
Add `setToolTip()` calls to the ~8 previously-untooltipped widgets in `biochemeleon/gui_game.py`'s GameTab (UX-01 / SC1 game-tab half). Pure additive pass — no behavior change, no new signal/slot, no layout change. This plan completes the tooltip coverage started by Plan 10-02 (Setup tab) — together they close SC1's "tooltips explain what each button does" half.

Purpose: UX-01 (SC1) requires tooltips explaining what each button does. Plan 10-02 covered the Setup tab; this plan covers the Game tab. The ~8 widgets are: Hint, Reveal one, Reveal all, found-management combo, timer label, remaining label, reveal label, and optionally the info log. The draft text comes verbatim from `10-RESEARCH-help.md`'s gui_game.py tooltip table — do NOT paraphrase. Human-verify (hover rendering in real PyMOL) is deferred to Plan 10-09's checkpoint (Qt tooltips can't render headlessly from WSL).

Output:
- `biochemeleon/gui_game.py` modified: ~7-8 new `setToolTip()` calls added near the existing widget construction in `GameTab.__init__`.
- No new tests (Qt-string-only; `py_compile` syntax-checks them; grep gates unaffected).
- No new files.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-polish-endgame-help/10-RESEARCH-help.md
@.planning/phases/10-polish-endgame-help/10-06-SUMMARY.md

@biochemeleon/gui_game.py
</context>

<tasks>

<task type="auto">
  <name>Add setToolTip() to the ~8 un-tooltipped Game-tab widgets</name>
  <files>biochemeleon/gui_game.py</files>
  <action>
Add `setToolTip(text)` calls to every previously-untooltipped widget in `GameTab.__init__` per the help-research "UX-01: Widget Tooltip Inventory" table (gui_game.py section). Keep each tooltip to one clear, concise, student-friendly sentence. Use the EXACT draft text from the research table — do NOT paraphrase.

**Existing tooltips to LEAVE AS-IS** (already set — do NOT change):
- `_color_btn` (line 54): "Choose highlight color for found hiders"
- `_restart_btn` (line 57): "Start a fresh round with new hiders"
- `_import_btn` (lines 67-68): "Load a puzzle prepared by 'Generate & export' and play it."
- `_save_btn` (lines 70-71): "Save the current game state to resume later."

**Tooltips to ADD** (place each `setToolTip()` immediately AFTER the widget's construction — match the existing `_color_btn.setToolTip("...")` pattern at line 54). Use these EXACT draft strings from `10-RESEARCH-help.md` (the "Tooltips to ADD — gui_game.py" table):

- `_info_log` (line 24) → `"Rolling log of game events: hits, misses, hints, reveals."` (optional but recommended — the QTextEdit is a user-facing widget; place the setToolTip right after `self._info_log.setReadOnly(True)` at line 25).
- `_timer_label` (line 26) → `"Elapsed time since the round began (counts up)."`
- `_remaining_label` (line 27) → `"How many hiders are still hidden. Easy mode shows a per-representation breakdown."`
- `_hint_btn` (line 38) → `"Reveal a clue: temporarily highlights atoms near one hider to point you toward it (counts as a hint used)."`
- `_reveal_one_btn` (line 39) → `"Give up on one random hider — it gets revealed and marked found (counts as a reveal used)."`
- `_reveal_all_btn` (line 40) → `"Give up and reveal every remaining hider at once. This ends the game."`
- `_found_mgmt_combo` (line 47) → `"After finding hiders, choose how to display them: Hide, Show, or Recolor the found hiders."`
- `_reveal_label` (line 41) → `"How many hiders you've revealed (via Reveal one / Reveal all). Shown on the win screen too."`

**Implementation rules:**
- DO NOT change any existing widget construction, configuration, signal, slot, or layout. Add ONLY `setToolTip(text)` calls.
- DO NOT add a new `from ... import ...` line (the tooltips are plain strings; no new dependency).
- The widgets are constructed in `GameTab.__init__` (lines ~21-94). Place each `setToolTip()` on its own line immediately after the widget's construction (or after its configuration method like `setReadOnly()` for `_info_log`). Match the existing `_color_btn.setToolTip(...)` / `_restart_btn.setToolTip(...)` placement pattern (right after construction).
- For `_reveal_label` (line 41), it's constructed in the same btn_row block — place the setToolTip right after its construction, before the `btn_row.addWidget(self._reveal_label)` line (if any; check the current layout — the label is added to btn_row at line 60).

After the change, run:
```
python3.6 -m py_compile biochemeleon/*.py
grep -rnE "\.exec_\(\)" biochemeleon/                       # expect 3 (unchanged from post-10-06 state: 10-03 Help QDialog + 10-05/10-06 _finish_win QMessageBox + 10-06 _finish_debrief QMessageBox; this plan adds NO .exec_())
grep -rnE "import Tkinter|import tkinter|from tkinter|import Pmw|from Pmw|app\.root|grab_set|mainloop|Toplevel|menuBar\.addmenuitem|from PyQt5 import|import PyQt5" biochemeleon/   # expect 0
python3.6 -m unittest tests.test_setup_state -v             # no regression
```
Commit:
```
git add biochemeleon/gui_game.py
git commit -m "feat(10-07): add tooltips to Game-tab widgets (UX-01)"
```
  </action>
  <verify>
`python3.6 -m py_compile biochemeleon/*.py` — syntax clean. `grep -rnE "\.exec_\(\)" biochemeleon/` — 3 hits (unchanged from post-10-06 state: 10-03 Help QDialog + 10-05/10-06 _finish_win QMessageBox + 10-06 _finish_debrief QMessageBox; this plan adds NO `.exec_()` — tooltips are not modal dialogs). `grep -rnE "import Tkinter|...|import PyQt5" biochemeleon/` — 0 hits. `python3.6 -m unittest tests.test_setup_state -v` — full suite passes (no regression). Every previously-untooltipped Game-tab widget has a `setToolTip()` call (count: ~8 new setToolTip calls; the 4 existing ones unchanged).
  </verify>
  <done>~8 Game-tab widgets have setToolTip() calls; existing tooltips on _color_btn/_restart_btn/_import_btn/_save_btn unchanged; py_compile clean; exec_ gate stays 3 (unchanged from post-10-06); Pitfall-1 gate stays 0; commit landed as feat(10-07). Human-verify (hover each widget in real PyMOL) is DEFERRED to Plan 10-09's checkpoint.</done>
</task>

</tasks>

<verification>
Phase-10-07 (this plan alone):
- `python3.6 -m py_compile biochemeleon/gui_game.py` — syntax clean.
- `grep -rnE "\.exec_\(\)" biochemeleon/` — 3 hits (unchanged from post-10-06 state; this plan adds NO Qt modal).
- `grep -rnE "import Tkinter|...|import PyQt5" biochemeleon/` — 0 hits.
- Every previously-untooltipped widget in `GameTab.__init__` has a `setToolTip()` call (~8 new calls; 4 existing unchanged).
- No widget behavior changed (pure additive — no new signal, slot, or layout).
- Human-verify (hovering each widget in a real PyMOL session) is DEFERRED to Plan 10-09's checkpoint.
</verification>

<success_criteria>
- ~8 new `setToolTip()` calls added to `gui_game.py`'s GameTab.
- Tooltip text is clear, concise, engaging, professional but simple — understandable by students (verbatim from the research, no paraphrasing).
- The Pitfall-1 grep gate stays at 0; the exec_ grep gate stays at 3 (unchanged from post-10-06 state; this plan adds NO Qt modal).
- `python3.6 -m py_compile` clean; existing test suite still passes.
- One atomic commit lands as `feat(10-07)`.
- File ownership is strictly `biochemeleon/gui_game.py` (Wave 3, sequential after 10-06 which also touched gui_game.py; PARALLEL with 10-08 which touches the disjoint `smoke/phase10_smoke.py`).
</success_criteria>

<output>
After completion, create `.planning/phases/10-polish-endgame-help/10-07-SUMMARY.md` recording:
- The widgets that gained tooltips (count + the list).
- The grep-gate state (Pitfall-1 = 0, exec_ = 3 — unchanged from post-10-06 state; this plan adds NO new .exec_()).
- A one-line handoff to Plan 10-09: "Game-tab tooltips are in place; human-verify (hover rendering in real PyMOL) is deferred to the Plan 10-09 checkpoint."
</output>
